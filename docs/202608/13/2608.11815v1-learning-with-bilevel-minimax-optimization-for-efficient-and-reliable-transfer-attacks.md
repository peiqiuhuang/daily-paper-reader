---
title: Learning with Bilevel-Minimax Optimization for Efficient and Reliable Transfer Attacks
title_zh: 基于双层极大极小优化的学习，用于高效且可靠的迁移攻击
authors: "Yaohua Liu, Yifan Guo, Jiaxin Gao"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.11815v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于对抗迁移攻击的双层极大极小优化
tldr: 本研究针对基于迁移的对抗攻击，提出了一种名为BMAT的双层极小极大优化框架。传统方法往往忽略了初始化、替代模型适配与梯度动力学之间的耦合关系，而BMAT通过双层建模捕捉这些依赖项，并利用内部极小极大问题提升替代模型的鲁棒性，从而增强跨架构泛化能力。该方法结合了软权重调制器和隐式梯度近似器，在分类和分割任务中均显著优于现有基准，为高效可靠的迁移攻击提供了新视角。
selection_source: fresh_fetch
motivation: 旨在解决迁移攻击中初始化、替代模型适配和扰动优化之间耦合不足导致的跨架构泛化能力受限问题。
method: 提出了BMAT框架，通过双层极小极大优化结合软权重调制器与隐式梯度近似器，实现初始化与扰动的协同优化。
result: 在30多个受害者模型上进行的实验表明，BMAT在分类和分割任务中均优于10余种强基准方法，显著提升了攻击成功率。
conclusion: 双层极小极大优化能有效捕捉攻击过程中的复杂依赖关系，是提升对抗样本迁移性和可靠性的关键。
---

## 摘要
基于迁移的对抗攻击利用替代模型生成对抗样本，以误导黑盒受害者模型。除了扰动生成之外，迁移性从根本上受初始化、替代模型自适应和梯度动力学之间耦合关系的支配。我们从双层极大极小（bilevel-minimax）的角度重新审视这一挑战，并提出了 BMAT（双层极大极小对抗迁移）。双层公式捕捉了初始化与扰动之间的依赖关系，而内部的极大极小问题则提升了替代模型的鲁棒性，以实现跨架构泛化。在算法上，我们开发了一个集成的自下而上求解器，结合了软权重调制器（Soft Weight Modulator）和隐式梯度近似器（Implicit Gradient Approximator），以实现初始化、替代模型自适应和扰动优化之间的三元耦合。我们进一步为所提出的双层极大极小框架的优化动力学提供了理论见解。在分类和分割基准测试上的广泛实验表明，BMAT 在超过 30 个受害者模型上优于 10 多个强基准方法，改善了架构内和跨架构的迁移效果，并使 mIoU 降低了高达 2 倍。代码可在 https://github.com/callous-youth/BMAT 获取。

## Abstract
Transfer-based adversarial attacks craft adversarial examples using surrogate models to mislead black-box victim models. Beyond perturbation generation, transferability is fundamentally governed by the coupling of initialization, surrogate adaptation, and gradient dynamics. We revisit this challenge from a bilevel-minimax perspective and propose BMAT (Bilevel-Minimax Adversarial Transfer). The bilevel formulation captures the dependency between initialization and perturbation, while the inner minimax problem promotes surrogate robustness for cross-architecture generalization. Algorithmically, we develop an integrated bottom-up solver that combines a Soft Weight Modulator and an Implicit Gradient Approximator to enable ternary coupling among initialization, surrogate adaptation, and perturbation optimization. We further provide theoretical insights into the optimization dynamics of the proposed bilevel-minimax framework. Extensive experiments on classification and segmentation benchmarks show that BMAT outperforms more than 10 strong baselines across more than 30 victim models, improving both intra- and cross-architecture transfer and yielding up to a 2x reduction in mIoU. Code is available at https://github.com/callous-youth/BMAT.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **BMAT (Bilevel-Minimax Adversarial Transfer)** 的新型对抗攻击框架，旨在通过双层极大极小优化显著提升对抗样本在黑盒模型间的迁移性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的基于迁移的对抗攻击通常将**初始化扰动（IP）**、**对抗扰动生成**和**替代模型适配**视为孤立的环节。这种“碎片化”的优化方式导致对抗样本容易过拟合于特定的替代模型，而在未知的受害者模型上表现不佳（即迁移性差）。
*   **研究动机**：作者认为迁移性的本质源于初始化、扰动和模型梯度动力学之间的**三元耦合交互**。为了解决优化动力学不一致的问题，需要一个统一的数学框架来协同调度这些因素。

### 2. 方法论：核心思想与技术细节
BMAT 将迁移攻击建模为一个**双层极大极小优化（Bilevel-Minimax Optimization）**问题：
*   **核心思想**：
    *   **内层极大极小问题**：在生成扰动的同时，动态调整替代模型的“软权重”，以寻找更具鲁棒性和通用性的梯度特征，从而增强跨架构泛化能力。
    *   **外层优化问题**：利用伪替代模型的反馈来学习最佳的初始化扰动（IP），为攻击轨迹提供更好的“种子”。
*   **关键技术细节**：
    *   **软权重调制器 (SWM)**：在内层循环中，通过单次反向传播同时更新扰动和替代模型权重，避免了高昂的计算开销。
    *   **隐式梯度近似器 (IGA)**：在外层优化中，利用隐式函数定理和 Fletcher-Reeves 共轭梯度法计算超梯度（Hypergradient），无需展开整个优化轨迹，极大地提升了内存和计算效率。
    *   **快速迁移机制**：学习到的初始化扰动 $\delta$ 被用作后续快速攻击的起点，显著加速了收敛并提升了攻击强度。

### 3. 实验设计
*   **数据集**：图像分类任务使用 **ImageNet**；语义分割任务使用 **Cityscapes** 和 **ADE20K**。
*   **受害者模型 (Victim Models)**：涵盖了 30 多种模型，包括 CNN（如 ResNet, MobileNet, Inception）、鲁棒集成模型以及 Transformer 架构（如 ViT, Swin, Segformer）。
*   **对比方法 (Baselines)**：对比了 10 多种强基准方法，包括基础攻击（PGD, MI, DI, TI, SI）、模型增强攻击（SGM, Ghost）、动量增强（VMI, GMI）、集成攻击（MBA, EBAD）以及最新的初始化学习方法（BETAK）和替代模型适配方法（DRA, FAUG）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号和数量。
*   **效率分析**：作者提供了详细的运行时间（Runtime）和内存占用（Peak Memory）对比。例如，在 ImageNet 上，BMAT (BP=40) 的运行时间约为 4.64 秒，内存占用约 7.89 GB，虽然高于基础 PGD，但在可接受范围内，且效率远高于基于集成的 BETAK 方法。

### 5. 实验数量与充分性
*   **实验规模**：实验非常充分，跨越了分类和分割两个大类任务，涉及 30+ 受害者模型和 10+ 基准算法。
*   **消融实验**：作者针对 IGA 模块、SWM 模块、伪替代模型的选择、攻击迭代次数（K, T）以及超参数 $\tau$ 进行了详尽的消融研究。
*   **客观性**：通过在不同架构（CNN 到 Transformer）之间进行交叉验证，证明了该方法的通用性，实验设计逻辑严密，对比公平（如在相同反向传播次数 BP 下对比）。

### 6. 主要结论与发现
*   **性能提升**：BMAT 在分类任务中平均提升了 **23.28%** 的攻击成功率（ASR）；在分割任务中，使受害者模型的 mIoU 降低了高达 **2 倍**。
*   **跨架构优势**：BMAT 在从 CNN 迁移到 Transformer 模型时表现尤为突出，证明了双层优化能有效捕捉跨架构的通用脆弱性。
*   **初始化重要性**：实验证明，学习到的初始化扰动（IP）比随机初始化或零初始化能更有效地引导攻击轨迹进入高损失区域。

### 7. 优点（亮点）
*   **理论完备**：首次将双层极大极小优化引入迁移攻击，并提供了优化动力学的稳定性分析和收敛性证明。
*   **高效求解**：通过 IGA 避免了二阶梯度的显式计算，解决了双层优化在深度学习中计算量巨大的痛点。
*   **即插即用**：BMAT 可以作为一种增强模块，与现有的多种攻击方法（如 MI, DI, SI）结合，均能产生显著增益。

### 8. 不足与局限
*   **计算开销**：尽管进行了优化，但 BMAT 仍然比传统的单层攻击（如 PGD）需要更多的计算资源和时间（约 1.5-2 倍）。
*   **伪模型依赖**：虽然论文展示了使用单模型（贝叶斯版本）作为伪替代模型也有效，但在实际应用中，选择一个合适的伪模型（如 Inception-v3）仍会对最终效果产生一定影响。
*   **超参数敏感性**：双层优化涉及多个超参数（如步长、正则化系数 $\tau$），在不同任务间可能需要微调以达到最优效果。

（完）
