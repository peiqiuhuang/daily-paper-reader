---
title: "DINO-VPT: Hierarchical Visual Prompt Tuning for Joint Physical-Digital Face Anti-Spoofing"
title_zh: DINO-VPT：用于物理-数字联合人脸反欺诈的分层视觉提示微调
authors: "Pierre Gallin-Martel, Mika Feng, Koichi Ito, Takafumi Aoki"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.20900v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于高效模型适配的分层视觉提示微调
tldr: 针对日益多样化的物理和数字人脸欺诈攻击，本文提出了DINO-VPT，一种基于分层视觉提示微调的轻量级纯视觉框架。该方法通过提示路由网络（PRN）动态注入与输入特征相关的提示，有效解耦了多种欺诈伪影，无需复杂的多模态融合或外部文本编码器。在UniAttackData基准上的实验证明，该模型在统一防欺诈任务中超越了现有的多模态大模型，为高效的人脸安全检测提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决现有统一防欺诈模型依赖复杂多模态融合和外部文本编码器导致的计算冗余与效率问题。
method: 采用纯视觉架构，利用提示路由网络根据输入特征动态调整分层视觉提示，以捕捉不同类型的欺诈特征。
result: 在UniAttackData基准测试中，DINO-VPT的性能优于目前最先进的基于视觉语言模型（VLM）的方法。
conclusion: 实验证明，无需多模态监督，结构合理的纯视觉架构也能在处理物理与数字混合欺诈检测中实现卓越性能。
---

## 摘要
随着欺诈攻击的多样性日益增加，对能够同时检测物理和数字威胁的统一化人脸反欺诈（FAS）模型的需求也日益增长。虽然现有的视觉语言模型（VLM）在此背景下表现出很高的泛化能力，但它们严重依赖复杂的跨模态融合和外部文本编码器。在本文中，我们提出了 DINO-VPT，这是一个利用分层视觉提示微调的轻量级纯视觉框架。通过提示路由网络（PRN）根据输入特征动态注入提示，我们的方法在不需要多模态融合的情况下，有效地解耦了各种欺诈伪影。在 UniAttackData 基准测试上的评估表明，DINO-VPT 达到了比最先进的基于 VLM 的方法更高的准确率。我们的结果表明，一个结构合理的纯视觉架构可以在不需要多模态监督的情况下，在统一 FAS 任务中实现最先进的性能。

## Abstract
With the increasing diversity of spoofing attacks, there is a growing demand for unified Face Anti-Spoofing (FAS) models capable of detecting both physical and digital threats. While existing Vision-Language Models (VLMs) demonstrate high generalization in this context, they heavily rely on complex multimodal fusion and external text encoders. In this paper, we propose DINO-VPT, a lightweight, vision-only framework leveraging hierarchical visual prompt tuning. By dynamically injecting prompts conditioned on input features via a Prompt Routing Network (PRN), our method effectively disentangles diverse spoofing artifacts without requiring multimodal fusion. Evaluations on the UniAttackData benchmark demonstrate that DINO-VPT achieves higher accuracy than state-of-the-art VLM-based methods. Our results indicate that a properly structured vision-only architecture can achieve state-of-the-art performance in unified FAS without the need for multimodal supervision.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DINO-VPT** 的新型人脸反欺诈（FAS）框架，旨在解决物理攻击与数字攻击并存的复杂安全威胁。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：随着生成式 AI 的发展，人脸识别系统面临着**物理演示攻击**（PA，如照片、屏幕重放、面具）和**数字操纵攻击**（DA，如换脸、Deepfake、对抗攻击）的双重威胁。
*   **研究背景**：现有的统一物理-数字攻击检测（UAD）方法多依赖于大型视觉语言模型（VLM），虽然泛化性强，但存在计算开销巨大、依赖外部文本编码器、多模态融合复杂等问题。
*   **研究动机**：探索是否能通过一个轻量级的、**纯视觉**的架构，在不依赖文本监督的情况下，实现对多样化欺诈伪影的高效解耦和检测。

### 2. 论文提出的方法论
DINO-VPT 采用分层视觉提示微调（Hierarchical Visual Prompt Tuning）策略，核心组件包括：
*   **骨干网络（Backbone）**：采用带有寄存器（Registers）的 **DINOv2 (ViT-B/14)**。寄存器能吸收非信息区域的异常特征，使注意力图更稳定，有助于捕捉微小的欺诈线索。
*   **分层视觉提示树（VP-Tree）**：将欺诈知识组织成树状结构。
    *   **Level 1（粗粒度）**：区分“活体”、“数字攻击”和“物理攻击”。
    *   **Level 2（细粒度）**：将数字攻击细分为“伪造”和“对抗”，将物理攻击细分为“打印”和“重放”。
*   **提示路由网络（PRN）**：这是一个轻量级模块，直接作用于输入的 Patch Embeddings。它根据输入特征动态预测所属类别索引，从而选择对应的视觉提示（Prompt Token）注入到 Transformer 的不同深度（如第 1 层和第 5 层之前）。
*   **模拟欺诈线索（SSC）**：一种动态重标签的数据增强机制。通过随机应用颜色畸变、摩尔纹（模拟物理攻击）或几何不一致性（模拟数字攻击）将部分活体样本转为合成欺诈样本，解决训练中类别不平衡和缺失模态的问题。
*   **损失函数**：由最终的二分类 Focal Loss 和各层路由器的交叉熵辅助损失（$L_{route}$）加权组成，实现端到端优化。

### 3. 实验设计
*   **数据集**：
    *   **UniAttackData**：目前最大的统一物理-数字攻击数据集，包含 1800 个身份的多种攻击类型。测试协议包括 P1（同域测试）和 P2（跨模态测试，如数字训练、物理测试）。
    *   **MICO 协议**：包含 MSU-MFSD、CASIA-FASD、Replay-Attack 和 OULU-NPU 四个数据集，用于验证在传统物理攻击下的跨域泛化能力。
*   **对比方法**：对比了 ResNet50、ViT、CDCN 等传统模型，以及 UAD+、HiPTune、FLIP、CFPL 等基于 VLM 的最先进（SOTA）多模态模型。
*   **评价指标**：ACER（主要指标）、AUC、ACC、HTER。

### 4. 资源与算力
*   **硬件环境**：使用 **NVIDIA A100 80GB GPU** 进行实验。
*   **训练细节**：采用 AdamW 优化器，训练 200 个 epoch，设置了早停机制（patience=20）。
*   **效率表现**：模型总参数量为 **87.4M**（远低于某些 VLM 模型的 3B），推理延迟仅为 **0.29 ms**，计算量为 44.94 GFLOPs。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准上进行了详尽测试，涵盖了内域、跨模态、跨数据集等多种场景。
*   **消融实验**：针对 SSC 模块的有效性、VP-Tree 的深度（None vs Level 1 vs Level 2）、以及动态路由 vs 静态提示进行了对比实验。
*   **充分性评价**：实验设计客观、公平。通过 P2.1 和 P2.2 的对称实验，证明了模型在物理和数字攻击之间双向迁移的稳定性，消融实验有力地支撑了各组件的必要性。

### 6. 主要结论与发现
*   **性能领先**：DINO-VPT 在 UniAttackData 的跨模态协议（P2）上达到了 **0.63% ACER**，显著优于之前 SOTA 的 VLM 方法（如 HiPTune 的 10.38%）。
*   **纯视觉潜力**：证明了结构合理的纯视觉架构在统一 FAS 任务中可以超越复杂的多模态模型。
*   **动态路由的重要性**：动态选择提示比固定提示在处理跨域攻击时表现出更强的鲁棒性，有效防止了模型对特定攻击类型的过拟合。

### 7. 优点
*   **高效轻量**：无需外部文本编码器，推理速度极快，适合资源受限的实际部署环境。
*   **结构化解耦**：通过 VP-Tree 成功将复杂的欺诈特征在特征空间中进行了有效分离。
*   **泛化能力强**：在完全未见的攻击模态上表现出极高的检测准确率。

### 8. 不足与局限
*   **对抗攻击模拟缺失**：SSC 模块目前无法有效模拟“对抗攻击”（Adversarial Attacks），这部分仍依赖真实数据。
*   **骨干网络依赖**：虽然比 VLM 轻量，但 DINOv2 (ViT-B) 本身仍具有一定规模，在极低功耗设备上的适配性有待验证。
*   **路由策略优化**：目前 PRN 仅基于输入 Patch，未来可探索基于中间层特征的更复杂的路由机制。

（完）
