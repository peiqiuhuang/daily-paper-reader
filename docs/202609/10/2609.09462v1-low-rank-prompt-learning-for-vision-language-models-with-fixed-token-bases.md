---
title: Low-Rank Prompt Learning for Vision-Language Models with Fixed-Token Bases
title_zh: 基于固定标记基的视觉-语言模型低秩提示学习
authors: "Tanvir Muntakim Tonoy, Sajjad Ghiasvand, Mahnoosh Alizadeh, Ramtin Pedarsani"
date: 2026-09-08
pdf: "https://arxiv.org/pdf/2609.09462v1"
tags: ["query:bopl"]
score: 9.0
evidence: 视觉语言模型的低秩提示词学习
tldr: 本文针对视觉-语言模型（如CLIP）的提示学习（Prompt Learning）中参数过多的问题，提出了一种低秩提示学习方法。通过将提示矩阵分解为两个低秩矩阵的乘积，并固定其中一个因子为随机基，大幅减少了可训练参数量。实验表明，该方法在少样本学习和泛化任务中不仅保持了与全参数提示学习相当的性能，甚至在某些场景下表现更优，证明了固定基底在提示优化中的有效性。
selection_source: fresh_fetch
motivation: 现有提示学习方法（如CoOp）中提示矩阵参数冗余，导致在少样本场景下容易过拟合。
method: 将提示矩阵分解为两个低秩矩阵，并固定其中一个因子为随机生成的基底，仅训练另一个因子。
result: 在七个少样本基准测试中，该方法在显著降低参数量的同时，匹配或超越了传统密集提示学习的性能。
conclusion: 提示矩阵的适应性主要由嵌入侧系数决定，固定令牌侧基底是实现高效且鲁棒的提示学习的有效策略。
---

## 摘要
提示学习通过用学习到的连续上下文向量替换手工编写的模板，使 CLIP 能够适应下游识别任务。在上下文优化（CoOp）中，这些向量构成了一个稠密的提示矩阵 $\mathbf{P}\in\mathbb{R}^{m\times d}$，仅需每个类别少量样本即可训练。我们研究了该矩阵是否存在过参数化问题，通过将其分解为 $\mathbf{P}=\mathbf{B}\mathbf{A}$，将可训练的提示参数从 $md$ 减少到 $r(m+d)$，而在固定标记侧因子 $\mathbf{B}$ 后进一步减少至 $rd$。在七个少样本基准测试和两个 CLIP 主干网络上的实验表明，低秩提示在参数量大幅减少的情况下，能够达到或超过稠密 CoOp 的性能，且在少样本基类到新类的泛化任务中增益最为显著。我们进一步发现，标记侧因子无需学习：将 $\mathbf{B}$ 固定为高斯基、正交基、SVD 派生基甚至随机基，仅训练嵌入侧因子 $\mathbf{A}$，其效果与完全可训练的分解相当，且源训练得到的 $\mathbf{B}$ 相比随机基并无优势。提示因子的不对称性和局部更新空间维度的差异解释了为何固定 $\mathbf{B}$ 比固定 $\mathbf{A}$ 的限制要小得多，而平滑性保证证明了在固定 $\mathbf{B}$ 的前提下优化 $\mathbf{A}$ 是收敛的。在 CLIP 提示设置中，嵌入侧系数承担了适配任务，而标记基完全可以保持固定。

## Abstract
Prompt learning adapts CLIP to downstream recognition by replacing hand-written templates with learned continuous context vectors, which in Context Optimization (CoOp) form a dense prompt matrix $\mathbf{P}\in\mathbb{R}^{m\times d}$ trained from only a few examples per class. We study whether this matrix is over-parameterized by factorizing it as $\mathbf{P}=\mathbf{B}\mathbf{A}$, which cuts the trainable prompt parameters from $md$ to $r(m+d)$, and to $rd$ once the token-side factor $\mathbf{B}$ is fixed. Across seven few-shot benchmarks and two CLIP backbones, low-rank prompts match or improve dense CoOp at far fewer parameters, with the clearest gains on low-shot base-to-new generalization. We then find that the token-side factor need not be learned at all: fixing $\mathbf{B}$ to a Gaussian, orthogonal, SVD-derived, or even random basis and training only the embedding-side factor $\mathbf{A}$ stays on par with the fully trainable factorization, and a source-trained $\mathbf{B}$ offers no advantage over a random one. A prompt-factor asymmetry and a local update-space dimension gap show why fixing $\mathbf{B}$ is far less restrictive than fixing $\mathbf{A}$, and a smoothness-only guarantee certifies that optimizing $\mathbf{A}$ over a fixed $\mathbf{B}$ converges. In the CLIP prompt setting, the embedding-side coefficients carry the adaptation while the token basis can simply be fixed.

---

## 论文详细总结（自动生成）

这是一份关于论文《Low-Rank Prompt Learning for Vision-Language Models with Fixed-Token Bases》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：视觉-语言模型（如 CLIP）在下游任务中通常采用提示学习（Prompt Learning）进行适配，其中 CoOp 方法通过优化一个稠密的上下文矩阵 $\mathbf{P} \in \mathbb{R}^{m \times d}$ 来实现。
*   **核心问题**：在少样本场景下，稠密提示矩阵存在严重的过参数化问题，导致模型容易在训练集上过拟合，且参数效率低下。
*   **研究动机**：作者质疑是否真的需要如此多的自由度来完成提示适配，并探索通过低秩分解和固定基底来简化提示学习过程。

### 2. 方法论
*   **低秩分解**：将提示矩阵 $\mathbf{P}$ 分解为 $\mathbf{P} = \mathbf{B}\mathbf{A}$，其中 $\mathbf{B} \in \mathbb{R}^{m \times r}$ 为标记侧因子（Token-side factor），$\mathbf{A} \in \mathbb{R}^{r \times d}$ 为嵌入侧因子（Embedding-side factor），且 $r \ll \min(m, d)$。
*   **固定基底策略**：核心发现是标记侧因子 $\mathbf{B}$ 无需训练。作者提出将 $\mathbf{B}$ 固定为随机高斯矩阵、正交矩阵或 SVD 派生矩阵，仅通过梯度下降优化嵌入侧因子 $\mathbf{A}$。
*   **理论支撑**：
    *   **提示因子不对称性**：证明了在嵌入空间维度 $d$ 远大于标记空间维度 $m$ 的情况下，固定 $\mathbf{B}$ 带来的近似误差远小于固定 $\mathbf{A}$。
    *   **更新空间维度差距**：分析表明固定 $\mathbf{B}$ 训练 $\mathbf{A}$ 提供的局部更新方向是反向操作的 $d/m$ 倍（本实验中为 32 倍）。
    *   **收敛性保证**：证明了在平滑性假设下，固定 $\mathbf{B}$ 后优化 $\mathbf{A}$ 能够收敛到一阶平稳点。

### 3. 实验设计
*   **数据集**：使用了 7 个标准的 CLIP 图像分类基准数据集（Caltech101, DTD, FGVC Aircraft, Food101, Oxford Flowers, Oxford Pets, UCF101）。
*   **基准对比**：对比了原始的稠密 CoOp 方法，以及不同秩（$r \in \{1, 2, 4, 8\}$）的联合训练分解方法和固定基底方法。
*   **评估指标**：主要评估少样本分类准确率（1-shot, 4-shot, 16-shot）以及基类到新类的泛化能力（Harmonic Mean）。

### 4. 资源与算力
*   **硬件**：实验在 NVIDIA Tesla V100 (32GB) GPU 上进行。
*   **算力消耗**：论文未提供具体的总训练时长，但指出使用了 8 个数据加载器并行处理，且实验配置为单 GPU 训练。由于参数量大幅减少（从 8192 降至 2048 甚至更低），该方法在计算资源需求上具有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个数据集、3 种少样本设置、4 种秩选择以及多种固定基底类型（高斯、正交、SVD、已训练基底），实验组数非常丰富。
*   **客观性与公平性**：所有对比实验均保持了与 CoOp 相同的优化器、学习率调度、数据增强和训练/验证/测试划分，确保了结果的对比公平性。

### 6. 主要结论与发现
*   **参数效率**：低秩提示在参数量仅为稠密 CoOp 的 1/4 甚至更少时，性能持平或更优。
*   **固定基底的有效性**：标记侧因子 $\mathbf{B}$ 不需要学习，随机初始化的固定基底表现与学习到的基底几乎无异。
*   **泛化优势**：在少样本场景下，低秩约束起到了正则化作用，显著提升了模型在新类上的泛化能力。

### 7. 优点
*   **极简高效**：证明了提示学习中大部分参数是冗余的，通过固定随机基底即可实现高效适配，极大降低了部署成本。
*   **理论深度**：通过线性代理模型和局部更新空间分析，从数学上解释了为什么“固定标记侧、训练嵌入侧”是更优的策略。
*   **鲁棒性**：实验结果在不同数据集和主干网络（RN50, ViT-B/16）上表现出高度的一致性。

### 8. 不足与局限
*   **任务局限**：目前仅针对 CoOp 风格的文本提示学习进行了验证，未扩展到深度提示（Deep Prompting）或多模态提示（Multi-modal Prompting）。
*   **主干网络限制**：实验主要基于 CLIP 主干，对于其他基础模型（如 ALIGN, LiT）的适用性需进一步验证。
*   **数据依赖**：虽然证明了固定基底的有效性，但对于极度复杂的下游任务，是否仍能保持这种性能优势尚待观察。

（完）
