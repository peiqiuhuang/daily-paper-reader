---
title: "AudioICL-Bench: A Benchmark for Large Audio Language Model In-Context Learning"
title_zh: AudioICL-Bench：大型音频语言模型上下文学习基准测试
authors: "Jia-Hung Chen, Yi-Cheng Lin, Kai-Wei Chang, Ke-Han Lu, Hung-Yi Lee"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11252v1"
tags: ["query:bopl"]
score: 6.0
evidence: 音频语言模型的上下文学习 (ICL)
tldr: AudioICL-Bench是一个针对大型音频语言模型（LALM）上下文学习（ICL）能力的诊断基准。研究指出，现有研究多关注任务识别而非真正的任务学习，因此该基准通过重采样规则确保答案无法通过先验知识获取。通过九项任务，该基准评估了模型在不同感知与学习维度下的表现，揭示了当前模型在处理复杂音频任务时的局限性，为提升音频模型的泛化能力提供了重要参考。
selection_source: fresh_fetch
motivation: 现有音频ICL研究多依赖模型先验知识，缺乏对模型从演示中学习全新输入-标签映射能力的评估。
method: 构建了包含九项任务的AudioICL-Bench基准，通过重采样规则消除先验知识影响，并从感知与学习两个维度对模型进行诊断。
result: 实验发现最强模型在简单感知任务中表现良好，但在时间测量和多规则组合任务上表现出显著的性能瓶颈。
conclusion: 研究明确了当前大型音频语言模型在时间感知和多规则组合方面的两大能力边界，为未来模型架构优化指明了方向。
---

## 摘要
上下文学习（ICL）为音频处理提供了无需训练的适应能力，这在标注每个新条件成本高昂的情况下尤为重要。然而，现有的音频 ICL 研究大多侧重于“任务识别”，即演示仅起到提示预训练能力的作用，而非“任务学习”，即必须仅从演示中推断出全新的输入-标签映射。我们推出了 AudioICL-Bench，这是一个诊断性基准测试，其每个片段的规则均经过重采样，确保无法从先验知识中获取正确答案。该基准包含九项任务，并沿两个维度进行组织，将“必须从演示中学习的内容”与“必须从信号中感知的内容”区分开来，从而能够将模型失败归因于其中任何一个来源。在对五种大型音频语言模型的测试中，最强的模型在感知任务简单时能够轻松地将任意声音绑定到新标签，但在处理时间测量和组合多个诱导规则时表现不佳，这揭示了模型能力的两个主要边界：时间感知与多规则组合。

## Abstract
In-context learning (ICL) promises training-free adaptation for audio, where labeling every new condition is costly. Yet existing audio ICL studies largely measure Task Recognition, where demonstrations merely cue pre-trained capabilities, rather than Task Learning, where a genuinely new input-label mapping must be inferred from demonstrations alone. We introduce AudioICL-Bench, a diagnostic benchmark whose per-episode rules are resampled so that no correct answer is recoverable from prior knowledge. Its nine tasks are organized along two axes that separate what must be learned from demonstrations from what must be perceived in the signal, enabling failures to be attributed to either source. Across five Large Audio Language Models, the strongest models readily bind arbitrary sounds to new labels when perception is easy, but collapse on temporal measurement and composing multiple induced rules, revealing two primary capability boundaries: temporal perception and multi-rule composition.