---
title: "CPInj: Uncovering Prompt Injection Risks in Textual Collaborative Prompt Optimization"
title_zh: CPInj：揭示文本协作提示优化中的提示注入风险
authors: "Xinting Liao, Behnoosh Zamanlooy, Masoumeh Shafieinejad, David B. Emerson, Ruinan Jin, Deval Pandya, Xiaoxiao Li"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18622v1"
tags: ["query:bopl"]
score: 8.0
evidence: 文本协作提示词优化针对优化循环
tldr: 本研究探讨了文本协作提示优化（TCPO）中的安全风险，揭示了恶意指令可能通过本地提示注入并经由服务器聚合传播。作者提出了CPInj攻击方法，该方法能有效污染全局提示、降低任务性能并规避现有防御。为应对此威胁，研究还提出了一种防御性聚合方法APAgg。实验证明，TCPO存在严重漏洞，现有防御手段不足，亟需更稳健的保护机制。
selection_source: fresh_fetch
motivation: 旨在揭示去中心化协作提示优化过程中，自由格式文本更新机制所带来的提示注入安全风险。
method: 提出了CPInj攻击框架以注入持久且隐蔽的恶意指令，并设计了APAgg防御聚合方法来净化恶意内容。
result: 实验显示CPInj在多种模型和任务上均能成功规避防御并显著降低性能，而APAgg虽有缓解作用但无法完全消除威胁。
conclusion: 协作提示优化面临严峻的注入攻击挑战，现有的检测和防御手段仍不完善，需要进一步研究更强大的防御策略。
---

## 摘要
文本协作提示优化（TCPO）将 Textgrad（Yuksekgonul 等人，2025）扩展到去中心化设置，允许多个客户端在保留本地数据的情况下共同改进大语言模型（LLM）的提示。由于其依赖于自由格式的文本更新和聚合，这引入了一个全新且在很大程度上尚未被探索的攻击面，即恶意指令可以被注入到本地提示中，并通过服务器端的提示聚合进行传播。与传统的提示注入攻击不同，针对 TCPO 的攻击目标是其协作优化循环。这种设置更具挑战性，因为恶意指令必须在聚合中存活，在后续的良性提示优化中持续存在，并规避服务器端的防御。为了揭示这一风险，我们提出了 CPInj，这是一种协作提示注入攻击，它利用恶意指令污染聚合后的全局提示，降低下游任务性能，抵抗良性客户端提示优化的净化，并规避服务器端先进的基于检测的防御。我们发现当前的防御方法对 CPInj 无效。为了缓解这种攻击，我们进一步提出了一种面向防御的聚合方法，即 APAgg，它可以净化恶意指令并部分恢复 TCPO 的效用。我们在三个 LLM 系列和五个涵盖数学、逻辑及医学的推理任务上进行了广泛的实验。结果表明，我们提出的攻击揭示了 TCPO 中的一个关键漏洞。尽管我们在缓解方面迈出了第一步，但该攻击仍然非常有效且远未完全解决，呼吁对 TCPO 进行更强大的防御研究。

## Abstract
Textual Collaborative Prompt Optimization (TCPO) extends Textgrad (Yuksekgonul et al., 2025) to a decentralized setting by allowing multiple clients to jointly improve prompts for large language models (LLMs) while keeping their data locally. Its reliance on free-form textual updating and aggregation introduces a new and largely unexplored attack surface, i.e., malicious instructions can be injected into local prompts and propagated through server-side prompt aggregation. Unlike conventional prompt injection attacks, attacking TCPO targets the collaborative optimization loop in TCPO. This setting is more challenging because malicious instructions must survive aggregation, persist through subsequent benign prompt optimization, and evade server-side defenses. To expose this risk, we propose CPInj, a collaborative prompt injection attack that contaminates the aggregated global prompt with malicious instructions, degrades downstream task performance, resists purification by prompt optimization on benign clients, and evades advanced detection-based defenses on the server. We find that current defense methods are ineffective against CPInj. To mitigate this attack, we further propose a defense-oriented aggregation method, i.e., APAgg, which purifies malicious instructions and partially recovers TCPO utility. We conduct extensive experiments across three LLM families and five reasoning tasks in math, logic, and medicine. The results demonstrate that our proposed attack reveals a critical vulnerability in TCPO. Although we take a first step toward mitigation, the attack remains highly effective and far from fully resolved, calling for more robust defense for TCPO.

---

## 论文详细总结（自动生成）

这篇论文《CPInj: Uncovering Prompt Injection Risks in Textual Collaborative Prompt Optimization》深入探讨了在去中心化环境下，大语言模型（LLM）协作提示优化过程中存在的安全隐患。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着隐私保护需求的增加，文本协作提示优化（TCPO，如 Fed-TextGrad）应运而生。它允许不同客户端在不共享本地数据的情况下，通过交换“文本梯度”（即改进建议）来共同优化一个全局提示词。
*   **核心问题**：TCPO 依赖自由格式的自然语言进行聚合和更新，这引入了一个全新的、尚未被探索的攻击面。
*   **研究动机**：传统的提示注入攻击（如 CatAttack 或 GCG）通常针对单轮交互，难以在 TCPO 的多次迭代、聚合和良性优化循环中存活。论文旨在揭示恶意指令如何通过协作循环持久地污染全局提示，并规避现有的服务器端防御。

### 2. 方法论
论文提出了攻击框架 **CPInj** 和防御方法 **APAgg**：
*   **CPInj 攻击核心思想**：利用“以毒攻毒”的策略，将提示优化本身作为攻击工具。恶意客户端使用 TextGrad 迭代重写恶意提示，通过六个维度的文本损失函数（TextLoss）进行多目标优化：
    1.  **行为损失 (Behavior Loss)**：引导模型产生微妙的推理错误，而非明显的胡言乱语。
    2.  **优先级损失 (Priority Loss)**：确保恶意指令在聚合和压缩后仍具有高执行优先级。
    3.  **相似性损失 (Similarity Loss)**：使恶意载荷在风格上与全局提示对齐，增强隐蔽性。
    4.  **隐蔽性损失 (Stealth Loss)**：通过模拟检测器反馈来规避服务器端的过滤。
    5.  **差异性损失 (Distinctness Loss)**：避免简单的重复或改写，使注入内容更自然。
    6.  **重定向损失 (Redirect Loss)**：强化“最新指令覆盖旧指令”的逻辑。
*   **APAgg 防御核心思想**：一种面向防御的聚合方法。它在聚合前对客户端提交的提示进行“任务锚定净化”（Task-Anchored Purification），利用原始任务描述作为锚点，过滤掉与任务无关或具有潜在重定向意图的指令，同时保留有益的优化信号。

### 3. 实验设计
*   **数据集/场景**：涵盖五个代表性基准测试，包括数学推理（AIME, AoPS, LiveBench-M）、逻辑推理（FOLIO）和生物医学问答（PubMedQA）。
*   **模型（Backbones）**：使用了 GPT-4o、Gemini-2.5-Pro、Qwen3-32B 和 GPT-4o-mini。
*   **对比方法**：
    *   **攻击对比**：Vanilla FedTextGrad（无攻击）、CatAttack、QueryGCG。
    *   **防御对比**：PromptGuard-2、DataSentinel、AttentionTracker、基于 LLM 的检测器。
*   **评估指标**：任务准确率（Accuracy）和攻击成功率（ASR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：实验主要通过调用商用模型 API（GPT, Gemini）和运行开源模型（Qwen）完成。实验设置包括 3 轮通信，每轮客户端进行 4 次本地更新，Batch Size 为 3。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个 LLM 系列和 5 个任务上进行了交叉验证，涵盖了从 6.67% 到 50% 的不同恶意客户端比例。
*   **充分性评价**：实验设计较为充分。作者不仅对比了多种攻击和防御基准，还进行了详细的消融实验（针对六种损失项）和敏感性分析（针对通信轮数、本地步数等），验证了攻击在不同聚合方法（如拼接、摘要）下的鲁棒性。

### 6. 主要结论与发现
*   **TCPO 存在严重漏洞**：CPInj 在所有测试模型和任务中均表现出极强的破坏力，显著降低了下游任务的准确率。
*   **现有防御失效**：现有的单轮提示注入防御方法（如 PromptGuard）无法有效应对 TCPO 中的持续性攻击，有时甚至会因误删良性指令而进一步降低性能。
*   **攻击的持久性**：CPInj 注入的指令能够经受住后续良性客户端的优化洗礼，具有很强的生存能力。
*   **防御的局限性**：虽然提出的 APAgg 能缓解攻击，但仍无法完全恢复到无攻击时的性能水平，说明该问题尚未完全解决。

### 7. 优点（亮点）
*   **视角新颖**：首次识别并定义了“协作提示优化”这一特定场景下的迭代式注入风险。
*   **攻击设计精巧**：CPInj 不仅仅是简单的字符注入，而是通过多目标文本优化生成的、具有语义深度的对抗性文本。
*   **实战价值高**：实验跨越了多个闭源和开源模型，证明了风险的普遍性。

### 8. 不足与局限
*   **防御依赖性**：APAgg 本身也依赖于 LLM 的净化能力，如果净化模型本身被绕过，防御将失效。
*   **计算开销**：CPInj 的多目标优化过程涉及多次 LLM 调用，攻击成本相对较高。
*   **任务特定性**：虽然在数学和逻辑任务上表现良好，但对于更开放、非结构化的文本生成任务，攻击的效果和隐蔽性平衡可能更难把握。
*   **算力细节缺失**：缺乏对运行这些实验所需的实际计算资源和成本的量化描述。

（完）
