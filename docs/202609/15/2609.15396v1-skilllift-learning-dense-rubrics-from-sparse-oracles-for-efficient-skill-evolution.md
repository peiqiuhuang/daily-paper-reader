---
title: "SkillLift: Learning Dense Rubrics from Sparse Oracles for Efficient Skill Evolution"
title_zh: SkillLift：从稀疏预言机中学习密集评分标准以实现高效的技能演化
authors: "Haoxiang Kang, Ming Wen"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15396v1"
tags: ["query:bopl"]
score: 10.0
evidence: 将技能演化形式化为双层优化问题
tldr: SkillLift 是一种旨在提升大模型智能体技能演化效率的新框架。针对现有方法依赖完整执行反馈导致评估成本高昂的问题，该研究提出通过学习“稠密评分准则”来解耦技能搜索与评估成本。通过双层优化策略，利用准则作为廉价代理进行技能迭代，仅需少量真实评估即可对齐准则，在复杂任务中显著降低了 token 消耗并提升了演化效果。
selection_source: fresh_fetch
motivation: 现有智能体技能演化方法过度依赖昂贵的完整执行反馈，导致监督信号稀疏且搜索效率低下。
method: 提出 SkillLift 框架，通过双层优化交替进行：内层利用学习到的准则进行低成本技能修订，外层通过少量评估对齐准则。
result: "在复杂智能体任务基准测试中，该方法在保持领先性能的同时，相比现有前沿演化方法降低了 40% 至 70% 的 token 成本。"
conclusion: 通过将绝对分数回归转化为更平滑的排序监督，SkillLift 有效解决了技能演化中的监督瓶颈，实现了高效的技能自我进化。
---

## 摘要
基于大语言模型（LLM）的智能体日益依赖持久化技能（即可重用的程序化提示词）来实现无需权重更新的自适应。现有的技能自演化方法直接根据执行反馈修改技能文本，但每次预言机（oracle）评估都需要完整的智能体运行，这造成了监督瓶颈，将搜索限制在针对失败的修补式更新上。我们的核心洞察是：相比于绝对结果回归，排序是一种更平滑的监督目标——确定哪个技能更好所需的预言机评估次数远少于预测精确分数。基于此，我们提出了 SkillLift，通过学习一个与预言机对齐的评分标准（rubric）作为结构化评估空间，将技能搜索与预言机成本解耦。我们将此形式化为一个通过交替优化求解的双层优化问题：内循环使用冻结的评分标准作为廉价代理，在零预言机成本下指导技能修订；外循环则调用少量预言机运行，通过秩相关性重新对齐评分标准，从而分摊预言机成本并稳定文本空间的更新。在复杂智能体任务基准上的实验表明，与前沿的演化方法相比，我们的方法在性能上更优，且 Token 成本降低了 40%--70%。代码已开源至 https://github.com/WalteR-MittY-pro/SkillLift。

## Abstract
LLM-based agents increasingly rely on persistent skills, i.e., reusable procedural prompts, to adapt without weight updates. Existing skill self-evolution methods directly revise skill text based on execution feedback, but each oracle evaluation requires a full agent rollout, creating a supervision bottleneck that confines search to failure-patching updates. Our key insight is that ranking is a smoother supervision target than absolute outcome regression: identifying which skill is better requires fewer oracle evaluations than predicting exact scores. Building on this insight, we propose SkillLift, which decouples skill search from oracle cost by learning an oracle-aligned rubric as a structured evaluation space. We formalize this as a bilevel optimization problem solved via alternating optimization: an inner loop uses the frozen rubric as a cheap surrogate to guide skill revision at no oracle cost, while an outer loop invokes a small number of oracle rollouts to re-align the rubric via rank correlation, amortizing oracle cost and stabilizing text-space updates. Experiments on complex agent task benchmarks show that our method outperforms existing auto-skill methods with 40--70\% less token cost compared to frontier evolving methods. Codes are available at https://github.com/WalteR-MittY-pro/SkillLift.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SkillLift: Learning Dense Rubrics from Sparse Oracles for Efficient Skill Evolution》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：基于 LLM 的智能体通过“技能”（可重用的程序化提示词）实现无需权重更新的自适应。
*   **痛点**：现有的技能自演化方法（如 SkillOpt, CoEvoSkills）通常直接根据执行反馈（Oracle）修改技能文本。由于每次评估都需要完整的智能体运行（Rollout），导致监督信号稀疏且成本高昂，使得搜索过程往往局限于针对失败的“修补式”更新，难以实现大规模、高质量的技能演化。

### 2. 方法论：SkillLift
*   **核心思想**：将技能演化形式化为**双层优化问题**，通过学习一个“与预言机对齐的评分标准（Rubric）”作为结构化评估空间，将技能搜索与昂贵的预言机评估解耦。
*   **关键技术**：
    *   **评分标准（Rubric）**：由一组二进制准则（Criteria）和权重组成，通过 LLM 调用即可评估技能，无需运行智能体。
    *   **双层优化（Alternating Optimization）**：
        *   **内循环（Inner Loop）**：固定评分标准，将其作为廉价代理，指导技能生成器进行多轮修订，提供稠密的反馈信号。
        *   **外循环（Outer Loop）**：固定技能，调用少量预言机运行，通过 Kendall’s $\tau$ 秩相关性评估评分标准与真实预言机排序的一致性，并根据差异对评分标准进行修正（Rubricator）。
    *   **排序监督**：相比于预测绝对分数，排序监督更平滑且对噪声更鲁棒。

### 3. 实验设计
*   **基准测试（Benchmarks）**：
    *   **WildClawBench**：包含 60 个双语、多模态任务，使用 Docker 容器环境。
    *   **SkillsBench**：包含 87 个任务，使用 OpenHands 环境，具有确定性验证器。
*   **对比方法**：
    *   基线：人工编写技能、LLM 一次性生成技能、SkillOpt（文本空间优化器）、CoEvoSkills（协同演化生成器-验证器）。
    *   公平性：所有演化方法共享相同的种子技能，且基线方法被分配了 2 倍于 SkillLift 的 Token 预算。

### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的 GPU 型号、数量或训练时长。
*   **成本度量**：论文主要通过“Token 消耗量”来衡量计算成本，并强调了 SkillLift 在达到相同性能水平时，Token 消耗显著低于基线方法。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了 3 种不同的 LLM 后端（GPT-5.4, GLM-5.1, DeepSeek-V4-Pro），在两个基准测试上进行了全面对比。
*   **消融实验**：进行了关于评分标准修正器（Rubricator）、种子技能质量、排序对齐与分数回归对比的消融实验。
*   **客观性**：实验设计考虑了不同模型和任务类别的差异，通过多次种子运行取平均值，且在 Token 预算上对基线方法给予了宽容（2 倍预算），体现了评估的客观与公平。

### 6. 主要结论与发现
*   **性能提升**：SkillLift 在所有模型和基准测试中均优于现有方法，在 SkillsBench 上性能提升显著（1.5–2.8 倍）。
*   **效率优势**：在达到目标性能时，Token 成本降低了 40%–70%。
*   **机制验证**：消融实验证实，预言机对齐的评分标准修正（Rubricator）是性能提升的核心驱动力，而排序监督优于绝对分数回归。

### 7. 优点
*   **解耦设计**：巧妙地将昂贵的评估过程与频繁的技能修订过程解耦，极大地提升了搜索效率。
*   **可解释性**：基于评分标准的反馈是结构化的（二进制准则），相比于模糊的标量分数，更能指导智能体进行针对性的改进。
*   **鲁棒性**：通过排序对齐而非绝对分数回归，增强了对预言机噪声的抵抗力。

### 8. 不足与局限
*   **任务依赖性**：对于主观性强、难以定义明确验证准则的任务（如“创意写作”类），评分标准的改进效果有限。
*   **模型依赖**：框架的有效性依赖于评分标准修正器（Rubricator）的推理能力，如果修正器无法识别出正确的改进方向，可能会导致错误的指导。
*   **延迟问题**：虽然降低了 Token 成本，但由于涉及多轮 LLM 调用和迭代，在实时性要求极高的场景下可能存在延迟。

（完）
