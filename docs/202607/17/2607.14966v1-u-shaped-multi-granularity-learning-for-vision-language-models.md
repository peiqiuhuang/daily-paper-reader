---
title: U-shaped Multi-granularity Learning for Vision-Language Models
title_zh: 视觉-语言模型的U型多粒度学习
authors: "Biao Chen, Yunqian Yu, Xiangxu Zhao, Zhongshu Chen, Mengmeng Jing, Lin Zuo"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14966v1"
tags: ["query:bopl"]
score: 9.0
evidence: U型多粒度提示学习框架
tldr: 针对视觉语言模型中全局提示缺乏细节与局部提示缺乏上下文的粒度困境，本文受U-Net启发提出了UPrompt框架。该方法在视觉和文本模态中构建并行的多粒度表示，通过粗到细的级联增强传播全局上下文，以及细到粗的分层监督确保语义一致性。在17个基准测试中，UPrompt在跨任务泛化和效率上均显著优于现有方法，实现了性能与成本的平衡。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型提示学习在全局语义感知与局部上下文关联之间存在难以兼顾的粒度困境。
method: 提出受U-Net启发的UPrompt框架，通过对称的编码器-解码器路径和跨层连接，在双模态中实现多粒度特征的协同增强与一致性监督。
result: 在MSCOCO等17个基准测试上，UPrompt在性能上显著超越了MAMET和CoCoA-Mix等模型，并以更低的计算成本达到了领先水平。
conclusion: UPrompt通过U型多粒度学习有效解决了提示学习中的粒度失衡问题，显著提升了视觉语言模型的泛化能力与推理效率。
---

## 摘要
视觉-语言模型的提示学习范式虽然有效，但面临着粒度困境：全局提示缺乏细粒度的语义感知，而局部提示则忽略了上下文关联，从而限制了跨任务的泛化能力。这种困境在密集预测任务中普遍存在。受能够统一跨粒度多级表示的 U-Net 启发，我们提出了 UPrompt，一种用于视觉-语言模型的 U 型多粒度提示学习框架。类似于 U-Net 通过带有跨层连接的对称编码器-解码器路径整合细粒度和粗粒度特征，UPrompt 在视觉和文本模态中构建了并行的多粒度表示。其中，从粗到细的级联增强将全局上下文传播以优化局部细节，而从细到粗的分层监督则确保了跨尺度的语义一致性。在 17 个基准测试上的广泛实验验证了我们方法的有效性。UPrompt 在 MSCOCO 数据集上的 rSum 指标分别超过 MAMET 和 VPKE 4.1 和 7.3，在从基础到新类的泛化性能上超过 CoCoA-Mix 5.09%，同时在保持极低开销（粗粒度）的情况下具有竞争力的性能，并能以 1/3 的成本（中粒度）达到与 PSRC 相当的水平。

## Abstract
The prompt learning paradigm for vision-language models is effective yet faces a granularity dilemma: global prompts lack fine-grained semantic awareness, while local prompts ignore contextual associations, limiting cross-task generalization. This dilemma exists in dense prediction tasks. Inspired by U-Net, which unifies multi-level representations across granularities, we propose UPrompt, a U-shaped multi-granularity prompt learning framework for vision-language models. Similar to how U-Net integrates fine and coarse features through symmetric encoder-decoder pathways with cross-level connections, UPrompt constructs parallel multi-granularity representations in both visual and textual modalities, where coarse-to-fine cascaded enhancement propagates global context to refine local details, while fine-to-coarse hierarchical supervision ensures semantic consistency across scales. Extensive experiments on 17 benchmarks validate our effectiveness. UPrompt outperforms MAMET and VPKE by 4.1 and 7.3 rSum on MSCOCO, surpasses CoCoA-Mix by 5.09% in base-to-novel generalization, while maintaining competitive performance with minimal overhead (coarse-grained) and matching PSRC with 1/3 cost (medium-grained).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **UPrompt** 的新型视觉-语言模型（VLM）提示学习框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言模型提示学习（Prompt Learning）面临**“粒度困境”**。
    *   **全局提示（Global Prompts）**：擅长捕捉宏观上下文，但缺乏对细粒度语义（如物体局部特征）的感知。
    *   **局部提示（Local Prompts）**：能保留局部细节，但往往忽略了区域间的上下文关联。
*   **研究动机**：受医学图像分割网络 **U-Net** 的启发，作者认为需要一种能够统一多尺度表示的架构，在保留高层语义的同时捕捉底层细节，从而打破单一粒度带来的性能瓶颈。

### 2. 方法论
UPrompt 核心思想是构建一个 **U 型多粒度提示学习框架**，在视觉和文本模态中建立并行的层级结构，并通过**双向连接（Bidirectional Connection）**实现信息流转。

*   **多粒度表示构建**：
    *   **视觉端**：通过对图像嵌入进行递归空间池化（如 14×14 → 7×7 → 4×4 → 1×1），生成从细到粗的视觉特征。
    *   **文本端**：利用大语言模型（LLM）生成语义层级（如：核心词 → 属性短语 → 详细描述），构建嵌套的文本嵌入。
*   **关键技术细节（双向连接）**：
    1.  **从粗到细的级联增强（Cascaded Enhancement, CE）**：在正向传播中，利用交叉注意力机制（Cross-Attention）将粗粒度的全局上下文注入到细粒度特征中，为局部建模提供全局指导。
    2.  **从细到粗的分层监督（Hierarchical Supervision, HS）**：在反向优化中，将最细粒度（特征最丰富）的对齐结果作为“教师信号”，通过知识蒸馏监督粗粒度层级，防止粗粒度表示出现语义漂移。
*   **自适应推理**：支持根据预测置信度进行“早期退出（Early-exit）”，简单样本使用粗粒度推理以节省算力，复杂样本则进入细粒度层级。

### 3. 实验设计
*   **数据集与场景**：
    *   **跨模态检索**：Flickr30K, MSCOCO。
    *   **少样本分类与泛化**：包含 ImageNet、Caltech101、OxfordPets、FGVCAircraft 等 11 个主流分类数据集。
    *   **分布外（OOD）泛化**：ImageNet-A, -R, -S, -V2。
*   **Benchmark 与对比方法**：
    *   对比了 **CoOp**（全局提示基准）、**MAPLE**（多模态提示）、**TAP**（属性树提示）、**PSRC**、**MAMET** 等最新的 SOTA 方法。
*   **评估指标**：Recall@K（检索）、Harmonic Mean（基础到新类泛化）、Top-1 Accuracy（分类）。

### 4. 资源与算力
*   **模型骨干**：默认使用 CLIP ViT-B/16，同时也验证了 ViT-B/32、ViT-L/14 以及 SigLIP 和 EVA-CLIP。
*   **LLM 辅助**：使用了 Llama 3-8B、Qwen3-4B/14B 来预生成文本层级。
*   **算力说明**：论文**未明确列出具体的 GPU 型号、数量及总训练时长**。但文中提到，通过自适应推理，UPrompt 在 ImageNet 上实现了 **2.1 倍的加速**，且中等粒度配置下仅需 PSRC 方法 **1/3 的计算成本（FLOPs）**。

### 5. 实验数量与充分性
*   **实验规模**：在 **17 个基准测试**上进行了实验，涵盖了检索、分类、泛化、OOD 等多个维度。
*   **消融实验**：非常详尽。分别验证了 CE（级联增强）和 HS（分层监督）的有效性、粒度数量的影响、提示词长度、分辨率敏感度以及不同 LLM 生成文本层级的鲁棒性。
*   **客观性**：通过 3 次独立运行的误差棒分析（Error Bar Analysis）确保了结果的统计可靠性。实验对比涵盖了 2022 年到 2025 年的最新方法，对比基准选择合理且公平。

### 6. 主要结论与发现
*   **性能领先**：UPrompt 在所有任务中均表现优异。在 MSCOCO 检索任务中 rSum 提升显著；在基础到新类泛化中，平均谐波均值（HM）达到 82.12%，超过了所有对比方法。
*   **效率优势**：证明了多粒度架构不仅能提升精度，还能通过自适应路由实现计算效率的动态平衡。
*   **双向连接的必要性**：实验证明，单纯堆叠粒度效果有限，只有通过 CE 引入上下文和 HS 保持一致性，才能发挥多尺度的潜力。

### 7. 优点（亮点）
*   **架构创新**：首次将 U-Net 的层级融合思想系统性地引入到视觉-语言模型的提示学习中。
*   **解决粒度困境**：通过 CE 和 HS 巧妙地解决了局部细节与全局上下文的矛盾。
*   **灵活性高**：支持自适应推理，能够根据硬件资源在性能和速度之间灵活切换。
*   **通用性强**：在多种 VLM 骨干网络（如 SigLIP, EVA-CLIP）上均表现出一致的增益。

### 8. 不足与局限
*   **层级深度限制**：目前的层级深度（通常为 3-4 层）仍相对较浅，对于极度复杂的语义结构建模能力可能仍有提升空间。
*   **依赖 LLM 预处理**：虽然实验证明了对不同 LLM 的鲁棒性，但文本层级的构建仍依赖于外部 LLM 的先验知识，增加了流程的复杂性。
*   **训练成本**：虽然推理可以加速，但训练阶段需要同时处理多个粒度的特征和蒸馏损失，初始训练开销可能高于单粒度方法。

（完）
