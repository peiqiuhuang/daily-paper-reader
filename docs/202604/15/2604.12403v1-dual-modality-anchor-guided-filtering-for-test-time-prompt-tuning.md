---
title: Dual-Modality Anchor-Guided Filtering for Test-time Prompt Tuning
title_zh: 双模态锚点引导过滤的测试时提示微调
authors: "Jungwon Choi, Eunwoo Kim"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12403v1"
tags: ["query:bopl"]
score: 9.0
evidence: 视觉语言模型的测试时提示微调
tldr: 针对测试时提示微调（TPT）中基于熵的过滤方法在分布偏移下易产生置信度误判的问题，本文提出了一种双模态锚点引导过滤框架。该方法通过引入富含属性的文本锚点和自适应图像锚点，利用语义对齐和置信度双重机制筛选有益的增强视图，并结合锚点辅助预测构建加权集成信号以提供稳定监督，在15个基准数据集上显著提升了模型性能。
selection_source: fresh_fetch
motivation: 传统的基于熵的过滤方法在分布偏移下容易对无关区域产生高置信度误判，难以准确识别对模型微调有益的增强视图。
method: 提出双模态锚点框架，结合属性丰富的文本锚点与自适应图像锚点进行视图过滤，并利用锚点辅助预测的加权集成作为监督信号。
result: 在15个基准数据集上的实验结果表明，该方法超越了现有技术，达到了新的最先进性能水平。
conclusion: 锚点引导的语义对齐和监督机制为鲁棒的测试时提示微调奠定了基础，有效解决了分布偏移下的视图选择难题。
---

## 摘要
测试时提示微调（TPT）利用增强视图来适配视觉语言模型，但其有效性受限于难以确定哪些视图是有益的。标准的基于熵的过滤依赖于模型的内部置信度分数，而这些分数在分布偏移下往往校准不良，会将高置信度分配给无关的裁剪或背景区域，而忽略了语义内容。为了解决这一问题，我们提出了一个双模态锚点引导框架，将视图选择建立在语义证据的基础上。我们引入了来自丰富属性描述的文本锚点以提供细粒度的类别语义，以及一个捕获演进中测试时统计数据的自适应图像锚点。利用这些锚点，我们根据对齐度和置信度对视图进行过滤，确保只有包含信息的视图引导适配过程。此外，我们将锚点视为辅助预测头，并将其预测结果与原始输出在置信度加权集成中结合，从而为提示更新产生稳定的监督信号。在 15 个基准数据集上的广泛实验证明了新的最先进性能，突出了锚点引导监督作为鲁棒提示更新基础的贡献。

## Abstract
Test-Time Prompt Tuning (TPT) adapts vision-language models using augmented views, but its effectiveness is hindered by the challenge of determining which views are beneficial. Standard entropy-based filtering relies on the internal confidence scores of the model, which are often miscalibrated under distribution shift, assigning high confidence to irrelevant crops or background regions while ignoring semantic content. To address this, we propose a dual-modality anchor-guided framework that grounds view selection in semantic evidence. We introduce a text anchor from attribute-rich descriptions, to provide fine-grained class semantics, and an adaptive image anchor that captures evolving test-time statistics. Using these anchors, we filter views based on alignment and confidence, ensuring that only informative views guide adaptation. Moreover, we treat the anchors as auxiliary predictive heads and combine their predictions with the original output in a confidence-weighted ensemble, yielding a stable supervision signal for prompt updates. Extensive experiments on 15 benchmark datasets demonstrate new state-of-the-art performance, highlighting the contribution of anchor-guided supervision as a foundation for robust prompt updates.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dual-Modality Anchor-Guided Filtering for Test-time Prompt Tuning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**测试时提示微调（Test-time Prompt Tuning, TPT）**。TPT 的核心是在推理阶段通过对单张测试样本进行数据增强（如随机裁剪），并最小化这些增强视图预测结果的熵来更新提示词（Prompt），从而使视觉语言模型（如 CLIP）适配未见的分布。

**核心痛点：** 现有的 TPT 方法主要依赖“预测熵”来筛选视图。然而，在分布偏移（Distribution Shift）下，模型的置信度往往是失准的——它可能对背景区域或无关的裁剪给出极高的置信度（低熵），而忽略了真正的语义内容。这会导致提示词向错误的方向更新，损害模型性能。

### 2. 方法论：核心思想与关键技术
本文提出了一个**双模态锚点引导框架**，通过语义证据来约束视图的选择和更新。

*   **文本引导的语义过滤（Text-Guided Filtering）：**
    *   **属性生成：** 预先利用大语言模型（LLM）生成每个类别的细粒度属性描述（如“狗有柔软的毛、下垂的耳朵”）。
    *   **对齐加权聚合：** 在测试时，根据增强视图与这些描述的对齐程度，动态聚合生成“文本锚点”。
    *   **联合评分：** 结合视图与文本锚点的对齐度（Alignment）和预测置信度（Confidence）进行评分，筛选出语义最相关的视图。
*   **图像引导的语义过滤（Image-Guided Filtering）：**
    *   **原型库（Prototype Bank）：** 维护一个类别相关的图像特征原型库，通过累积已选视图的特征进行更新。
    *   **图像锚点：** 利用原型库生成的图像锚点再次对视图进行筛选，捕捉测试集的视觉分布特征。
*   **自适应多模态预测集成（Adaptive Ensemble）：**
    *   将原始提示词、文本锚点和图像锚点视为三个独立的预测源。
    *   根据各自预测的最大概率（置信度）进行动态加权集成，生成一个稳定的目标分布。
*   **优化目标：** 放弃了传统的熵最小化，改为最小化原始预测与集成目标分布之间的 **KL 散度（KLD）**，以实现更稳健的监督。

### 3. 实验设计
*   **数据集：** 共使用了 **15 个基准数据集**。
    *   **领域泛化（Domain Generalization）：** ImageNet 及其四个变体（ImageNet-A, -R, -V2, -Sketch）。
    *   **跨数据集转移（Cross-dataset Transfer）：** 包含 Flower102, DTD, Pets, Cars, UCF101, Caltech101, Food101, SUN397, Aircraft, EuroSAT。
*   **对比方法：**
    *   **零样本基准：** CLIP。
    *   **提示学习方法：** CoOp, CoCoOp, MaPLe, CoPrompt 等。
    *   **测试时微调方法：** TPT, DiffTPT, C-TPT, DynaPrompt, PromptAlign。

### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA RTX 6000 Ada GPU**。
*   **效率：** 论文强调了方法的轻量性。在 ImageNet-R 上，单张图片的推理/微调时间约为 **0.2 秒**，与原始 TPT 相当，远快于需要扩散模型增强的 DiffTPT（>0.6 秒）或维护多个提示词的 DynaPrompt（0.4 秒）。
*   **预处理：** LLM 描述生成和编码是离线完成的一次性步骤，不占用推理时间。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从通用物体识别到细粒度分类、卫星图像、动作识别等多种场景，实验覆盖面非常广。
*   **消融实验：** 详细分析了文本锚点、图像锚点、集成策略、损失函数设计以及超参数敏感性（如 $\alpha, \beta$ 比例）。
*   **客观性：** 实验不仅对比了最终精度，还对比了推理延迟（Latency），并提供了视图选择的可视化分析，证明了方法在提升性能的同时保持了实用性。

### 6. 主要结论与发现
*   **性能领先：** 在 15 个数据集上均取得了 SOTA 性能，平均准确率比现有方法提升了约 **3.36%**。
*   **视图选择至关重要：** 实验证明，通过语义锚点筛选出的视图比单纯基于熵筛选的视图更能代表目标物体，有效避免了模型被背景噪声误导。
*   **即插即用：** 该方法可以无缝集成到 CoOp 或 MaPLe 等现有的提示学习框架中，并进一步提升它们的鲁棒性。

### 7. 优点（亮点）
*   **双模态约束：** 结合了 LLM 的先验知识（文本）和测试时的实时统计（图像），解决了单一模态在分布偏移下的不可靠问题。
*   **高效性：** 在不引入重型生成模型（如扩散模型）的情况下，通过简单的相似度计算实现了高质量的视图筛选。
*   **稳定性：** 引入 KL 散度和置信度加权集成，替代了容易导致模型坍缩的自监督熵最小化。

### 8. 不足与局限
*   **LLM 依赖：** 虽然描述生成是离线的，但方法的效果仍受限于 LLM 生成属性的质量和覆盖范围。
*   **原型库初始化：** 在测试初期或类别分布极度不平衡时，图像原型库的积累可能存在偏差。
*   **超参数调节：** 尽管论文给出了推荐值，但在极端不同的新领域，对齐度与置信度的权重比例（$\alpha, \beta$）可能仍需微调。

（完）
