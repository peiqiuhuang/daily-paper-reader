---
title: "FlowBot: Inducing LLM Workflows with Bilevel Optimization and Textual Gradients"
title_zh: FlowBot：利用双层优化和文本梯度诱导 LLM 工作流
authors: "Hongyeon Yu, Young-Bum Kim, Yoon Kim"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26258v2"
tags: ["query:bopl"]
score: 10.0
evidence: 用于诱导LLM工作流和提示词的双层优化
tldr: 针对现有大语言模型（LLM）工作流高度依赖人工设计和提示词工程的局限性，本文提出了 FlowBot。该方法将工作流诱导建模为双层优化问题：外层优化工作流的高层草图结构，内层通过“文本梯度”逐层反向传播来优化具体的 LLM 调用。实验表明，FlowBot 自动发现的工作流在性能上可媲美甚至超越人工设计的强基准模型，为构建高效、自动化的 AI 系统提供了新路径。
selection_source: fresh_fetch
motivation: 现有 LLM 工作流主要依赖人工设计的管道和提示词，这在实际部署中构成了显著的效率瓶颈。
method: 提出一种双层优化框架，利用文本梯度在外层优化工作流结构，在内层通过模块化反向传播优化单个 LLM 调用。
result: FlowBot 自动诱导出的工作流在多个任务上表现出色，达到了与人工精心设计或生成的强基准相当的水平。
conclusion: 通过双层优化和文本梯度实现的数据驱动工作流诱导，是自动化构建高性能 LLM 智能体系统的有效方法。
---

## 摘要
LLM 工作流通过协调对单个 LLM/智能体（agent）的结构化调用来实现特定目标，为构建能够处理多样化任务的强大 AI 系统提供了一条充满前景的路径。然而，现有的构建此类工作流的方法通常依赖于人工设计的流水线和提示词，这在实际部署中构成了重大瓶颈。我们如何以数据驱动的方式自动诱导基于 LLM 的智能体和工作流？本文描述了一种简单的、用于自动诱导智能体和 LLM 工作流的数据驱动方法。我们将工作流诱导公式化为一个双层优化（bilevel optimization）问题：外层循环优化工作流的高层草图（特别是 LLM 调用的结构化方式），内层循环则逐一优化每个单独的 LLM 调用。两个循环都使用“文本梯度”（textual gradients）进行优化，其中内层循环通过逐层“反向传播”文本梯度，以模块化的方式优化每个组件。我们发现，通过我们的 FlowBot（通过双层优化和文本梯度进行工作流诱导）方法发现的 LLM 工作流，其性能与使用人工设计或生成的工作流的强基准模型相比具有竞争力。

## Abstract
LLM workflows, which coordinate structured calls to individual LLMs/agents to achieve a particular goal, offer a promising path towards building powerful AI systems that can tackle diverse tasks. However, existing approaches for building such workflows generally rely on human-crafted pipelines and prompts, which presents a substantial bottleneck in real world deployment. How can we automatically induce LLM-based agents and workflows in a data-driven way? This paper describes a simple data-driven approach for automatically inducing agents and LLM workflows. We formulate workflow induction as a bilevel optimization problem: an outer loop which optimizes a high-level sketch of the workflow (in particular how the LLM calls should be structured), and an inner loop which optimizes each individual LLM call one-by one. Both loops are optimized with ``textual gradients'' where for the inner loop we optimize each component in a modular way through ``backpropagating'' textual gradients layer-by-layer. We find that LLM workflows discovered through our \textsc{FlowBot} (work\textbf{flow} induction through \textbf{b}ilevel \textbf{o}ptimization and \textbf{t}extual gradients) approach performs competitively against strong baselines that make use of human-crafted or generated workflows.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FlowBot** 的框架，旨在通过数据驱动的方法自动诱导（Induce）和优化大语言模型（LLM）的工作流结构及其内部提示词。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的 LLM 应用正从单一调用转向复杂的“复合 AI 系统”（工作流），即通过结构化的 LLM 调用序列、工具使用和推理步骤来解决复杂任务。
*   **核心瓶颈**：现有的工作流构建高度依赖人工设计（Human-crafted），难以跨领域扩展。
*   **核心问题**：如何利用数据驱动的方法，同时自动发现最优的工作流结构（即哪些步骤、如何排序）以及每个步骤的最优提示词（Prompts）？

### 2. 方法论
FlowBot 将工作流诱导建模为一个**双层优化（Bilevel Optimization）**问题，并引入了**文本梯度（Textual Gradients）**的概念：
*   **外层循环（工作流草图优化）**：优化高层结构 $W$（如添加、删除或合并 LLM 调用步骤）。它根据执行轨迹和最终损失生成“工作流梯度”，指导结构的演变。
*   **内层循环（提示词优化）**：在固定结构下，优化每个步骤 $a_i$ 的具体提示词 $\theta_i$。
*   **关键技术：文本反向传播（Textual Backpropagation）**：
    *   类比神经网络的链式法则，FlowBot 将工作流视为模块化层。
    *   **前向传播**：输入经过各层 LLM 调用产生输出。
    *   **反向传播**：从最终损失开始，由 LLM 生成自然语言形式的反馈（即“文本梯度”）。该梯度逐层向前传递，每一层都会根据下游层的反馈和自身的表现来更新其提示词。
*   **初始化**：通过让 LLM 观察少量样本并生成初始草图，随后进入迭代优化。

### 3. 实验设计
*   **数据集/场景**：涵盖了 10 个主流基准测试，包括多跳问答（HotpotQA, HoVer）、指令遵循（IFBench）、数学推理（GSM8K, MATH）、代码生成（HumanEval, MBPP）和阅读理解（DROP）等。
*   **对比方法（Baselines）**：
    *   **提示词优化类**：GEPA（强进化算法基准）、Trace、MIPROv2、TextGrad。
    *   **工作流生成类**：AFlow（基于代码搜索的自动化工作流生成）、ADAS。
    *   **基础策略**：直接 Prompting、思维链（CoT）、自洽性（Self-Consistency）。
*   **模型使用**：主要使用 GPT-4o mini 作为执行器和优化器，消融实验中测试了 Qwen3.5 系列模型。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长，因为该方法主要基于 API 调用。
*   **成本分析**：论文详细对比了 **API 调用次数和费用**。例如，在 6 个基准测试的总成本上，FlowBot 约为 47.67 美元（5.47万次调用），而对比方法 AFlow 约为 130.95 美元（33.8万次调用）。这表明 FlowBot 在数据效率和成本控制上具有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同类型的任务上进行了测试，并进行了多轮独立运行取平均值。
*   **消融实验**：非常充分。作者分别验证了“双层优化结构”和“逐层反向传播”的必要性，并测试了不同规模的开源模型（如 Qwen 系列）作为 Meta-LLM 的效果。
*   **客观性**：通过在相同模型（GPT-4o mini）下重新实现对比方法（如 AFlow），确保了公平性。

### 6 主要结论与发现
*   **性能卓越**：FlowBot 在多数任务上优于现有的自动化工作流生成方法（如 AFlow）和人工设计结构的提示词优化方法（如 GEPA）。
*   **结构合理性**：自动诱导出的工作流在逻辑上与人类设计的专家级工作流高度相似（例如在多跳问答中自动加入实体消歧和验证步骤）。
*   **双层优化的必要性**：消融实验证明，仅优化提示词而不优化结构，或不采用逐层反向传播，都会导致性能大幅下降。
*   **通用性**：初步证明了诱导“统一工作流”（一个工作流处理多种任务）的可行性。

### 7. 优点
*   **全自动化**：实现了从结构到内容的端到端自动发现，减少了对人工工程的依赖。
*   **模块化反馈**：引入文本反向传播，使得错误定位更精准，提示词更新更具针对性。
*   **高效率**：相比于基于蒙特卡洛树搜索或进化算法的方法，FlowBot 达到最佳性能所需的 API 调用次数更少。

### 8. 不足与局限
*   **计算开销**：虽然比 AFlow 便宜，但优化过程仍需大量 LLM 调用，对于简单任务可能存在“过度优化”的问题。
*   **收敛瓶颈**：在某些任务上，模型会迅速达到性能天花板，后续数据无法进一步提升效果，这反映了当前提示词优化空间的局限性。
*   **延迟风险**：诱导出的复杂工作流包含多个步骤，虽然准确率高，但会增加推理时的延迟和成本。

（完）
