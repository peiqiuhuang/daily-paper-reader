---
title: Evolving Prompt Adaptation for Vision-Language Models
title_zh: 视觉-语言模型的演化提示适配
authors: "Enming Zhang, Jiayang Li, Yanru Wu, Zhenyu Liu, Yang Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09493v1"
tags: ["query:bopl"]
score: 9.0
evidence: 参数高效提示学习与提示轨迹引导
tldr: 针对视觉语言模型在下游任务微调时易出现灾难性遗忘的问题，本文提出EvoPrompt框架。该框架通过模态共享提示投影器生成层级提示，并采用进化训练策略将更新解耦为方向和幅度，在保留预训练语义方向的同时仅调整幅度。结合特征几何正则化，EvoPrompt在提升少样本学习性能的同时，有效保留了模型的零样本泛化能力。
selection_source: fresh_fetch
motivation: 现有的参数高效提示学习方法在适应下游任务时，往往会因破坏预训练知识而导致严重的灾难性遗忘问题。
method: 提出EvoPrompt框架，利用模态共享投影器生成提示，并通过解耦方向与幅度的进化策略及特征几何正则化来稳定微调过程。
result: 实验证明该方法在少样本学习任务中达到了当前领先水平，并能稳健地保持预训练模型的原始零样本泛化能力。
conclusion: 通过显式引导提示的进化轨迹，可以实现既高效又具备知识保留能力的视觉语言模型适配。
---

## 摘要
将大规模视觉-语言模型 (VLMs) 适配到标注数据有限的下游任务中仍然是一个重大挑战。虽然参数高效的提示学习方法提供了一条有前景的路径，但它们往往面临预训练知识灾难性遗忘的问题。为了解决这一局限性，我们的工作基于这样一种见解：控制提示的演化路径对于实现无遗忘适配至关重要。为此，我们提出了 EvoPrompt，这是一个旨在显式引导提示轨迹以实现稳定且保留知识的微调的新型框架。具体而言，我们的方法采用模态共享提示投影器 (MPP) 从统一的嵌入空间生成分层提示。关键在于，一种演化训练策略将低秩更新解耦为方向和幅度分量，在仅调整幅度的同时保留早期学习到的语义方向，从而使提示能够在不丢弃基础知识的情况下进行演化。该过程通过特征几何正则化 (FGR) 进一步稳定，该正则化强制执行特征去相关以防止表示崩溃。大量的实验表明，EvoPrompt 在少样本学习中实现了最先进的性能，同时稳健地保留了预训练 VLM 原始的零样本能力。

## Abstract
The adaptation of large-scale vision-language models (VLMs) to downstream tasks with limited labeled data remains a significant challenge. While parameter-efficient prompt learning methods offer a promising path, they often suffer from catastrophic forgetting of pre-trained knowledge. Toward addressing this limitation, our work is grounded in the insight that governing the evolutionary path of prompts is essential for forgetting-free adaptation. To this end, we propose EvoPrompt, a novel framework designed to explicitly steer the prompt trajectory for stable, knowledge-preserving fine-tuning. Specifically, our approach employs a Modality-Shared Prompt Projector (MPP) to generate hierarchical prompts from a unified embedding space. Critically, an evolutionary training strategy decouples low-rank updates into directional and magnitude components, preserving early-learned semantic directions while only adapting their magnitude, thus enabling prompts to evolve without discarding foundational knowledge. This process is further stabilized by Feature Geometric Regularization (FGR), which enforces feature decorrelation to prevent representation collapse. Extensive experiments demonstrate that EvoPrompt achieves state-of-the-art performance in few-shot learning while robustly preserving the original zero-shot capabilities of pre-trained VLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Evolving Prompt Adaptation for Vision-Language Models》的结构化深度总结：

### 1. 核心问题与背景
*   **研究动机**：大规模预训练视觉-语言模型（如 CLIP）在下游任务适配时面临挑战。传统的全参数微调成本极高，而现有的参数高效微调（PEFT）方法（如提示学习 Prompt Learning）虽然节省资源，但容易导致**灾难性遗忘**，即模型在学习特定下游任务时，会迅速偏离预训练的语义锚点，丧失原始的零样本（Zero-shot）泛化能力。
*   **核心问题**：如何在有限的标注数据下，既能实现高效的任务适配，又能通过引导提示（Prompt）的演化轨迹来保留预训练知识。

### 2. 方法论：EvoPrompt 框架
EvoPrompt 通过显式管理提示的演化路径来实现稳定适配，核心包含三个组件：
*   **模态共享提示投影器 (MPP)**：
    *   不再为每一层独立设置提示，而是从一个统一的、可学习的嵌入空间出发。
    *   使用**解耦的低秩扩展**（类似于 LoRA 的改进版）生成各层提示。通过一个模态特定的共享权重 $W_{shared}$ 和层特定的低秩适配器（$A_i, B_i$）相结合，增强了跨层和跨模态的协同效应。
*   **演化轨迹感知学习策略 (Evolutionary Strategy)**：
    *   **增量幅度-方向解耦**：将低秩更新分解为“方向”和“幅度”两个分量。
    *   **历史方向冻结**：在训练过程中，将早期 epoch 学习到的语义方向视为基础知识并予以冻结，仅允许微调其权重幅度（Magnitude）。新 epoch 则负责学习新的方向增量。这种方式允许模型在不破坏已有几何结构的前提下，逐步累积任务特定知识。
    *   **自适应秩削减**：随着训练进行，逐步降低新学习层的秩（Rank），以防止后期过度拟合。
*   **特征几何正则化 (FGR)**：
    *   基于 Soft-HGR 最大相关性理论，引入正则化项强制执行特征去相关（Decorrelation）。
    *   通过最小化模态协方差矩阵的乘积，促进特征空间的正交性，防止在小样本场景下出现表示崩溃（Representation Collapse）。

### 3. 实验设计
*   **数据集**：涵盖了 11 个标准图像分类基准（ImageNet, Caltech101, OxfordPets, StanfordCars, Flowers102, Food101, FGVCAircraft, SUN397, UCF101, DTD, EuroSAT）。
*   **评估场景**：
    1.  **基类到新类泛化 (Base-to-Novel)**：在基类训练，测试新类表现。
    2.  **跨数据集传输**：在 ImageNet 训练，直接在其他 10 个数据集测试。
    3.  **领域泛化**：在 ImageNet 变体（V2, Sketch, A, R）上测试鲁棒性。
    4.  **少样本学习**：1, 2, 4, 8, 16-shot 性能评估。
*   **对比方法 (Benchmarks)**：包括 CLIP (Zero-shot), CoOp, CoCoOp, MaPLe, PromptSRC, ProVP, TCP, MMA 等当前最先进的（SOTA）方法。

### 4. 资源与算力
*   **硬件**：实验在一台 **NVIDIA A800 GPU** 上完成。
*   **效率指标**：
    *   **可训练参数量**：约 0.764M（比 MaPLe 减少了约 4.6 倍）。
    *   **训练速度**：每张图像约 4.5ms。
    *   **推理速度**：1282.1 FPS（Batch size=100）。
    *   **训练时长**：通常仅需训练 5-10 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文在 11 个数据集上进行了详尽的测试，并提供了 3 个随机种子的平均结果。
*   **消融实验**：针对 MPP 投影器、共享权重 $W_s$、演化策略（E.T.）、知识恒定损失（$L_{kcl}$）和几何正则化（$L_{fgr}$）分别做了剔除实验，验证了各组件的必要性。
*   **充分性评价**：实验设计非常全面，不仅对比了准确率，还深入分析了训练动力学（如幅度 $\alpha$ 的演化曲线）和过拟合现象，客观地展示了该方法在平衡“适配”与“泛化”方面的优势。

### 6. 主要结论
*   **性能领先**：EvoPrompt 在 11 个数据集的平均调和平均值（HM）上达到了 **80.73%**，显著优于之前的 SOTA 方法（如 PromptSRC 的 79.97%）。
*   **泛化能力强**：在 Novel 类（未见类别）上的表现提升尤为明显，证明了其有效缓解了灾难性遗忘。
*   **鲁棒性高**：在跨数据集和领域偏移场景下，EvoPrompt 均保持了最佳的平均准确率，证明其学习到的提示具有更强的迁移性。

### 7. 优点
*   **创新的演化视角**：将提示微调视为一个动态演化的轨迹，通过冻结方向、调整幅度的策略，巧妙地解决了知识保留问题。
*   **参数高效且结构优雅**：通过模态共享和低秩分解，在极低参数量下实现了深层多模态提示的同步更新。
*   **理论支撑**：引入 Soft-HGR 理论进行特征几何正则化，从数学层面提升了特征空间的判别力。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文给出了推荐值，但 $L_{fgr}$ 和 $L_{kcl}$ 的权重（$\gamma, \eta$）以及秩削减的节奏可能需要针对不同规模的预训练模型进行微调。
*   **架构依赖**：目前主要基于 CLIP (ViT) 架构验证，对于其他非 Transformer 结构或纯生成式多模态大模型的适用性尚待进一步探索。
*   **任务覆盖**：实验主要集中在分类任务，对于目标检测、分割或更复杂的视觉推理任务的适配效果有待验证。

（完）
