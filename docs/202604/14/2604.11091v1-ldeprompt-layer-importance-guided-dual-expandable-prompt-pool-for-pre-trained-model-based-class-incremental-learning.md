---
title: "LDEPrompt: Layer-importance guided Dual Expandable Prompt Pool for Pre-trained Model-based Class-Incremental Learning"
title_zh: LDEPrompt：用于基于预训练模型的类增量学习的层重要性引导双可扩展提示池
authors: "Linjie Li, Zhenyu Wu, Huiyu Xiao, Yang Ji"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11091v1"
tags: ["query:bopl"]
score: 8.0
evidence: 可训练的关键提示和提示池扩展
tldr: 针对基于提示词的类增量学习中提示池固定、手动选择嵌入及过度依赖预训练骨干等问题，本文提出LDEPrompt。该方法引入了层重要性引导的双重可扩展提示池，实现了自适应层选择以及提示池的动态冻结与扩展。在多个主流基准测试上的实验证明，LDEPrompt在保持预训练模型知识的同时，能有效提升增量学习的性能与可扩展性，达到领域领先水平。
selection_source: fresh_fetch
motivation: 现有的提示词增量学习方法存在提示池固定、嵌入选择依赖人工且过度依赖预训练骨干网络进行匹配的局限性。
method: 提出LDEPrompt，通过层重要性引导实现自适应层选择，并构建可动态冻结与扩展的双重提示池。
result: 在广泛使用的类增量学习基准测试中，LDEPrompt 取得了当前最先进的性能表现。
conclusion: LDEPrompt 通过动态管理提示池和自适应层级策略，显著提升了预训练模型在处理类增量任务时的有效性和扩展性。
---

## 摘要
基于提示的类增量学习方法通常构建一个由多个可训练关键提示组成的提示池，并进行实例级匹配以选择最合适的提示嵌入，这类方法已展现出良好的应用前景。然而，现有方法存在一些局限性，包括提示池固定、提示嵌入需手动选择，以及在提示选择上对预训练骨干网络存在强依赖。为了解决这些问题，我们提出了一种层重要性引导的双可扩展提示池（LDEPrompt），该方法支持自适应层选择以及提示池的动态冻结与扩展。在广泛使用的类增量学习基准测试上进行的大量实验表明，LDEPrompt 达到了最先进的性能，验证了其有效性和可扩展性。

## Abstract
Prompt-based class-incremental learning methods typically construct a prompt pool consisting of multiple trainable key-prompts and perform instance-level matching to select the most suitable prompt embeddings, which has shown promising results. However, existing approaches face several limitations, including fixed prompt pools, manual selection of prompt embeddings, and strong reliance on the pretrained backbone for prompt selection. To address these issues, we propose a \textbf{L}ayer-importance guided \textbf{D}ual \textbf{E}xpandable \textbf{P}rompt Pool (\textbf{LDEPrompt}), which enables adaptive layer selection as well as dynamic freezing and expansion of the prompt pool. Extensive experiments on widely used class-incremental learning benchmarks demonstrate that LDEPrompt achieves state-of-the-art performance, validating its effectiveness and scalability.

---

## 论文详细总结（自动生成）

这是一份关于论文《LDEPrompt: Layer-importance guided Dual Expandable Prompt Pool for Pre-trained Model-based Class-Incremental Learning》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在基于预训练模型（PTM）的类增量学习（CIL）中，现有的提示词（Prompt）方法存在三大局限：
    1.  **提示池固定**：无法随任务增加动态扩展容量，限制了新知识的吸收。
    2.  **层选择手动化**：通常手动指定插入提示词的层，缺乏对任务特性的自适应性。
    3.  **骨干网络依赖强**：提示词的选择过度依赖冻结的预训练骨干网络生成的嵌入，缺乏任务特定信息，且容易导致旧任务提示词被覆盖（灾难性遗忘）。
*   **研究背景**：旨在利用预训练模型的强大表示能力，通过轻量级的提示词微调实现增量学习，同时避免存储旧数据。

### 2. 方法论
LDEPrompt 提出了**层重要性引导的双可扩展提示池**架构，核心步骤如下：
*   **层重要性评估（Layer Importance Evaluation）**：
    *   在训练新任务前，通过计算每一层的**信息增益（Information Gain, IG）**来评估其重要性。
    *   公式：$IG(l) = I(x; h_l) - I(x; h_{l-1})$，其中 $h$ 是层的表示，$I$ 是互信息。
    *   通过 Softmax 归一化得分，选择得分高于平均值的层来插入提示词，实现自适应层选择。
*   **双可扩展提示池（Dual Expandable Prompt Pools）**：
    *   **全局提示池 ($P_g$)**：存储所有已完成任务的提示词，并保持冻结以防止遗忘。
    *   **训练提示池 ($P_c$)**：具有固定容量，用于当前任务的训练。
    *   **交互机制**：利用 [CLS] 标记的余弦相似度从全局池中检索最相关的 $s$ 个提示词，并随机初始化剩余的 $S-s$ 个提示词组成当前训练池。训练结束后，新学习的提示词被合并入全局池，实现动态扩展。
*   **优化目标**：使用标准的交叉熵损失函数对训练池中的提示词和分类器进行联合优化。

### 3. 实验设计
*   **数据集**：CIFAR100（100类）、CUB（200类鸟类细粒度分类）、VTAB（50类）。
*   **实验设置**：采用 "B-m Inc-n" 模式（初始 $m$ 类，后续每阶段增加 $n$ 类），例如 CIFAR100 B0-Inc10。
*   **对比方法（Benchmark）**：包括 L2P, DualPrompt, CODA-Prompt, Aper-VPT, Evoprompt, Cprompt, Convprompt 等当前最先进的（SOTA）提示词增量学习方法。
*   **评估指标**：平均准确率（Avg Accuracy）和最终阶段准确率（Last Accuracy）。

### 4. 资源与算力
*   **硬件**：使用了 2 台 **NVIDIA GeForce RTX 3090 GPU**。
*   **软件框架**：基于 PyTorch 实现，并使用了 PILOT 增量学习工具箱。
*   **预训练模型**：采用在 ImageNet21K 上预训练的 **ViT-B/16**。
*   **训练细节**：每个任务训练 20 个 epoch，batch size 为 24，使用 SGD 优化器，初始学习率 0.001。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准数据集上进行了完整测试。
*   **重复性与客观性**：为了保证公平性，实验采用了三个不同的随机种子（1991, 1993, 1995）取平均值，并遵循了 PILOT 框架的标准设置。
*   **消融实验**：设计了 4 组消融实验，分别验证了双池设计、提示词冻结机制、提示词扩展机制以及层重要性评估的有效性。实验设计较为充分，逻辑严密。

### 6. 主要结论与发现
*   **性能领先**：LDEPrompt 在所有数据集上均优于现有方法。在最终准确率上，分别比次优方法在 CIFAR100、CUB 和 VTAB 上高出 **0.50%、2.91% 和 1.37%**。
*   **层选择的重要性**：消融实验证明，将提示词插入所有层并非最优，通过信息增益选择特定层不仅能减少参数冗余，还能提升任务适配度。
*   **知识保留与共享**：双池结构有效地平衡了旧知识的保留（通过冻结全局池）和新知识的获取（通过扩展训练池）。

### 7. 优点
*   **自适应性强**：不再依赖人工指定提示词插入的位置，而是根据数据流动态计算层重要性。
*   **可扩展性好**：双池设计解决了固定提示池容量受限的问题，支持模型在更长的任务序列中持续学习。
*   **抗遗忘能力强**：通过冻结已学习提示词并利用相似度检索实现知识共享，显著缓解了灾难性遗忘。

### 8. 不足与局限
*   **存储开销**：虽然提示词本身很轻量，但随着任务数量极度增加，全局提示池（Global Pool）会不断膨胀，论文未详细讨论极端长序列下的存储压力。
*   **计算复杂度**：在每个任务开始前需要进行多次前向传播来评估层重要性（信息增益计算），这增加了一定的预训练开销。
*   **论文篇幅限制**：作为一篇 5 页的短论文，对于互信息计算的具体实现细节（如具体采用哪种互信息估计方法）描述略显简略。

（完）
