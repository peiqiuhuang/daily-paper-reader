---
title: "Bi-EZP: LLM-Guided Bilevel Program Evolution for Ensemble Zero-Cost Proxy Discovery"
title_zh: Bi-EZP：用于集成零成本代理发现的大语言模型引导双层程序演化
authors: "Yutao Lai, Kezhao Lai, Hai-Lin Liu"
date: 2026-08-22
pdf: "https://arxiv.org/pdf/2608.21927v1"
tags: ["query:bopl"]
score: 8.0
evidence: 提出了一种用于优化离散结构和连续系数的双层优化框架
tldr: 零成本代理（ZCP）能加速神经架构搜索，但单一代理在不同搜索空间表现不一。本文提出Bi-EZP框架，利用大语言模型（LLM）生成集成代理的聚合程序，并结合CMA-ES算法优化其连续参数。通过双层优化解耦结构发现与参数校准，Bi-EZP在多个基准测试中显著提升了架构排序的准确性与泛化能力，为自动化构建高效集成代理提供了新方案。
selection_source: fresh_fetch
motivation: 现有的零成本代理在不同搜索空间表现不稳，且自动化构建集成代理时难以同时优化离散结构和连续参数。
method: 提出双层优化框架，上层由LLM生成聚合程序结构，下层利用CMA-ES在训练集上校准程序参数。
result: 在NATS-Bench和网络设计空间等实验中，该方法显著提高了肯德尔秩相关系数，并增强了下游架构搜索的性能。
conclusion: 将程序结构发现与数值校准解耦，是实现自动化、高性能集成零成本代理构建的有效途径。
---

## 摘要
零成本代理使神经架构搜索（NAS）能够根据初始化时计算的统计数据对候选网络进行排序，从而避免了重复训练。然而，不同的代理捕捉不同的属性，且在不同搜索空间中往往产生不一致的排名。集成代理可以结合互补信号，但自动化发现必须同时优化离散聚合结构及其连续系数，这使得结构质量难以与参数校准分离。我们提出了 Bi-EZP，一个将这些决策解耦的双层框架。在上层，大语言模型基于四个互补的基础代理生成可执行的聚合程序，并带有特定于程序的参数边界。在下层，协方差矩阵自适应演化策略（CMA-ES）在内部训练划分上优化每个固定程序的连续参数。随后，在不相交的验证划分上使用肯德尔秩相关系数（Kendall's rank correlation）评估校准后的程序，使演化选择能够倾向于那些在校准数据之外具有泛化能力的结构。在 NATS-Bench 和 Network Design Spaces 上的实验评估了跨异构搜索空间的排名性能，DARTS 实验评估了下游架构搜索。结果表明，将程序发现与数值校准分离为自动化集成零成本代理构建提供了一种有效的方法。源代码可在以下网址获取：https://anonymous.4open.science/r/Bi-EZP-318D

## Abstract
Zero-cost proxies enable neural architecture search (NAS) to rank candidate networks from statistics computed at initialization, avoiding repeated training. However, different proxies capture different properties and often produce inconsistent rankings across search spaces. Ensemble proxies can combine complementary signals, but automated discovery must optimize both discrete aggregation structures and their continuous coefficients, making structural quality difficult to separate from parameter calibration. We propose Bi-EZP, a bilevel framework that decouples these decisions. At the upper level, a large language model generates executable aggregation programs over four complementary base proxies with program-specific parameter bounds. At the lower level, covariance matrix adaptation evolution strategy (CMA-ES) optimizes the continuous parameters of each fixed program on an inner training split. The calibrated programs are then evaluated using Kendall's rank correlation on a disjoint validation split, enabling evolutionary selection to favor structures that generalize beyond their calibration data. Experiments on NATS-Bench and Network Design Spaces evaluate ranking performance across heterogeneous search spaces, and DARTS experiments assess downstream architecture search. Results show that separating program discovery from numerical calibration provides an effective approach to automated ensemble zero-cost proxy construction. The source code is available at: https://anonymous.4open.science/r/Bi-EZP-318D

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Bi-EZP** 的新框架，旨在通过大语言模型（LLM）引导的双层程序演化，自动化地发现高性能的集成零成本代理（Zero-Cost Proxies, ZCP），以加速神经架构搜索（NAS）。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：零成本代理（ZCP）虽然能极大地提高 NAS 的效率（无需训练即可评估架构），但其可靠性受限。单一代理在不同搜索空间中表现不一，而现有的集成代理方法在同时优化“离散聚合结构”和“连续参数系数”时存在耦合问题，导致难以区分是结构优越还是参数调优的效果。
*   **研究背景**：现有的自动化代理发现方法（如符号回归或遗传编程）往往受限于预定义的语法，且在处理结构相关的连续参数时缺乏有效的校准机制。

### 2. 方法论
Bi-EZP 的核心思想是将代理发现建模为一个**双层优化问题（Bilevel Optimization）**，实现结构发现与数值校准的解耦：
*   **基础代理池**：选取了四个互补的基础代理：NASWOT（激活多样性）、MeCo（特征相关性）、Sweet-SNIP（参数显著性）和 SZiCo（梯度一致性）。
*   **上层优化（结构演化）**：
    *   利用 **LLM（如 GLM-4.7-Flash）** 作为结构生成器，生成可执行的 Python 聚合程序（$G$）及其对应的参数边界（$B$）。
    *   通过提示词引导 LLM 进行语义交叉和变异。
    *   引入代码验证与修正机制，确保生成的程序符合语法且接口一致。
*   **下层优化（数值校准）**：
    *   对于上层给定的每一个固定结构，使用 **CMA-ES（协方差矩阵自适应演化策略）** 在内部训练集上优化其连续参数（$\theta$）。
    *   目标是最大化代理得分与真实精度之间的肯德尔秩相关系数（Kendall’s $\tau$）。
*   **适应度评估**：校准后的程序在不相交的验证集上计算 $\tau$ 值，作为上层演化的适应度，从而筛选出泛化能力强的结构。

### 3. 实验设计
*   **数据集**：CIFAR-10, CIFAR-100, ImageNet-16-120, 以及大规模的 ImageNet。
*   **搜索空间（Benchmarks）**：
    *   **NATS-Bench**：包括拓扑搜索空间（TSS）和大小搜索空间（SSS）。
    *   **NDS (Network Design Spaces)**：涵盖 DARTS, ENAS, PNAS, NASNet, Amoeba 等异构空间。
    *   **DARTS 空间**：用于下游架构搜索任务。
*   **对比方法**：包括单一代理（如 ZiCo, NASWOT, SynFlow 等）和先进的集成/自动化代理方法（如 AZ-NAS, EZNAS, ECP, ParZC 等）。

### 4. 资源与算力
*   **LLM 资源**：使用了智谱 AI 的 `glm-4.7-flash` 模型 API。
*   **下游搜索算力**：在 DARTS 空间进行架构搜索时，CIFAR-10 上的搜索成本约为 **0.06 GPU-days**，ImageNet 上约为 **0.07 GPU-days**。
*   **未明确说明的部分**：论文未详细列出离线“代理发现阶段”的总 Token 消耗量和具体的 CPU/GPU 总机时，但指出该阶段是一次性投入，发现后的代理是冻结使用的。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 NATS-Bench 和 NDS 的多个子空间进行了详尽的秩相关性测试。
    *   进行了**跨数据集**和**跨搜索空间**的迁移性实验（Frozen Proxy Evaluation）。
    *   在 DARTS 空间完成了端到端的架构搜索并验证了最终分类精度。
    *   **消融实验**：对比了单层优化（无双层解耦）、无 LLM 引导、以及不同下层优化器（GA, DE, PSO）的效果。
*   **充分性评价**：实验设计非常充分，涵盖了从理论相关性到实际搜索性能的完整闭环，且通过跨空间迁移实验证明了方法的鲁棒性，对比基准涵盖了近年来的主流方法，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先**：Bi-EZP 在所有评估的基准测试中均取得了最高的肯德尔秩相关系数（例如在 NATS-TSS CIFAR-10 上达到 0.809）。
*   **解耦有效性**：消融实验证明，将结构发现与参数校准分离显著优于 LLM 直接生成参数或使用固定结构的优化。
*   **迁移能力强**：在一个空间发现的代理程序，无需重新校准即可在其他异构空间保持竞争力。
*   **下游收益**：引导出的架构在 CIFAR 和 ImageNet 上均达到了与 SOTA 相当或更优的分类错误率。

### 7. 优点
*   **创新性解耦**：通过双层优化解决了集成代理中离散与连续变量混合优化的难题。
*   **LLM 的巧妙应用**：将 LLM 作为灵活的符号程序生成器，而非简单的文本生成器，利用其代码编写能力突破了传统遗传编程的语法限制。
*   **鲁棒性验证**：严谨的跨空间/跨数据集实验证明了该方法发现的是具有通用物理意义的评价逻辑，而非简单的过拟合。

### 8. 不足与局限
*   **成本与延迟**：离线发现阶段依赖 LLM API 调用，存在一定的 Token 成本和网络延迟。
*   **基础代理依赖**：Bi-EZP 的上限受限于初始选取的四个基础代理池，若基础代理均失效，集成效果也会受限。
*   **搜索空间定义**：虽然 LLM 提供了灵活性，但搜索空间本质上仍受限于 Prompt 的约束和 Python 接口的定义。
*   **复杂性控制**：论文未对生成程序的复杂度（如计算开销、代码行数）进行显式惩罚，可能产生过于复杂的聚合逻辑。

（完）
