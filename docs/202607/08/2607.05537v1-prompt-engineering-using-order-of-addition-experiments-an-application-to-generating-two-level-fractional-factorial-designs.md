---
title: "Prompt engineering using order-of-addition experiments: An application to generating two-level fractional factorial designs"
title_zh: 基于加料顺序实验的提示工程：在生成两水平部分因子设计中的应用
authors: "Duoduo Danny Ying, Alan R. Vazquez, Hongquan Xu"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05537v1"
tags: ["query:bopl"]
score: 8.0
evidence: 提示词工程与位置优化的系统方法
tldr: 大语言模型在工程和科学领域应用广泛，但其性能常受提示词中短语顺序的影响（即顺序依赖性）。本研究提出了一种系统性的“添加顺序实验”方法，旨在量化提示词元素的顺序效应并确定最佳排列位置。通过在生成两水平分数阶乘设计任务中的应用，该方法成功揭示了主流LLM的顺序依赖性，并识别出了高质量的提示词配置，为提示词工程提供了科学的优化手段。
selection_source: fresh_fetch
motivation: 针对大语言模型在处理任务时存在的提示词短语顺序依赖性问题，寻求一种系统化的优化方法。
method: 引入添加顺序实验（Order-of-Addition Experiments）来量化提示词中不同元素的顺序影响并寻找最优组合。
result: 在构建两水平分数阶乘设计的案例中，该方法有效识别了LLM的顺序敏感性并优化了提示词性能。
conclusion: 添加顺序实验为提示词工程提供了一种量化且系统的方法，能显著提升LLM在复杂工程任务中的表现。
---

## 摘要
大语言模型（LLMs）在工程和科学领域正变得无处不在，因为它们可以将提示词转化为数据分析代码、实验设计、优化问题的公式化表述等。然而，许多大语言模型都存在一种被称为“顺序依赖性”的现象，即提示词中短语的顺序会影响其在特定任务上的表现。为了克服这一问题，我们引入了一种系统化方法，利用加料顺序实验来量化提示词中元素的排序效应，并确定其最佳位置。我们通过使用最先进的大语言模型构建两水平部分因子设计来演示该方法。结果表明，加料顺序实验可以阐明这些大语言模型中的顺序依赖性，并帮助我们为该任务确定高质量的提示词配置。

## Abstract
Large language models (LLMs) are becoming ubiquitous in engineering and science because they can turn prompts into data analysis code, experimental designs, formulations of optimization problems, among other applications. However, many LLMs suffer from a phenomenon called order dependency, in which the order of phrases in the prompt affects their performance on a given task. To overcome this issue, we introduce a systematic method that uses order-of-addition experiments to quantify the ordering effect of elements in a prompt and identify their best positions. We demonstrate our methodology by constructing two-level fractional factorial designs using state-of-the-art LLMs. We show that order-of-addition experiments can elucidate order dependency in these LLMs, and can help us to identify a high-quality prompt configuration for the task.

---

## 论文详细总结（自动生成）

这篇论文题为《基于加料顺序实验的提示工程：在生成两水平部分因子设计中的应用》，由加州大学洛杉矶分校（UCLA）和墨西哥蒙特雷科技大学的研究人员合作完成。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在处理复杂工程和科学任务（如实验设计、代码生成）时表现出色，但存在**顺序依赖性（Order Dependency）**现象。即在语义不变的情况下，提示词（Prompt）中短语或元素的排列顺序会显著影响模型的输出质量。
*   **核心动机**：现有的提示词优化方法（如随机搜索或试错法）缺乏系统性，且在调用高昂的商业 API 时成本过高。
*   **研究目标**：引入统计学中的**加料顺序实验（Order-of-Addition, OofA）**方法，系统地量化提示词元素的顺序效应，并寻找最优的提示词配置。

### 2. 方法论
*   **核心思想**：将提示词中的不同组成部分（如角色设定、任务指令、输出格式、示例等）视为实验设计中的“组件”，通过改变它们的添加顺序来观察模型成功率的变化。
*   **关键技术细节**：
    *   **逻辑回归模型**：由于响应变量是二元的（任务成功或失败），研究者采用了逻辑回归来建模成功率与顺序之间的关系。
    *   **成对排序（PWO）模型**：引入伪因子 $z_{ij}$，若组件 $i$ 排在组件 $j$ 之前则取 1，否则取 -1。这允许研究者量化任意两个元素相对位置的影响。
    *   **复合 PWO 模型**：除了顺序，还考虑了组件的“水平”（Levels），即同一个意思的不同表述方式（如“n runs and m factors”与“m factors and n runs”）。
    *   **实验设计生成**：为了降低实验成本，研究者没有采用全排列（$q!$），而是利用 **组件正交阵列（COA）** 算法生成高效的分数 OofA 设计，在保证估计精度的前提下大幅减少了实验次数。

### 3. 实验设计
*   **应用场景**：构建 **16 轮（runs）两水平部分因子设计**。这是一个经典的统计学难题，要求 LLM 生成具有最大分辨率和最小偏差的实验矩阵。
*   **测试模型（Benchmark）**：
    *   主要使用 `gpt-4o` (OpenAI) 和 `gemini-1.5-flash` (Google)。
    *   补充实验涉及 `gpt-o1` 和 `gemini-2.0-flash-thinking` 等最新模型。
*   **对比基准**：研究者设定了一个包含五个元素（ROLE, INSTRUCTION, STEP-BY-STEP, FORMAT, SHOT）的**基础提示词（Base Prompt）**作为对照。
*   **实验变量**：改变这五个元素的排列顺序，以及指令中关键参数的表述顺序。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的本地 GPU 算力，因为实验完全基于 OpenAI 和 Google 的 **API 调用**。
*   **成本考量**：论文详细列出了 API 的 Token 价格，强调了 OofA 方法通过减少实验运行次数（如从 240 种排列缩减至 80 种）来节省经济成本的优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **实验 1**：针对 6 种不同因子数量的任务，进行了 48 个测试点 × 40 次重复实验。
    *   **实验 2**：针对 16 因子 9 运行任务，进行了 80 个测试点 × 20 次重复实验，并进行了 60 次重复的验证实验。
*   **充分性评价**：实验设计非常严谨。研究者不仅使用了统计显著性检验（Wald test），还通过 McFadden 伪 $R^2$ 评估模型拟合度，并针对二项分布的过度离散（Overdispersion）进行了标准误调整。实验覆盖了多个模型和多种任务难度，结果具有统计学说服力。

### 6. 主要结论与发现
*   **顺序依赖性显著**：提示词元素的顺序对 LLM 性能有巨大影响。例如，仅仅是将“因子数”放在“运行数”之前，就能显著改变 GPT-4o 的表现。
*   **性能大幅提升**：
    *   对于 **GPT-4o**，优化后的提示词顺序将 16 因子 9 运行任务的成功率从 **11.7% 提升至 98.3%**。
    *   对于 **Gemini 1.5 Flash**，成功率从 **35% 提升至 100%**。
*   **模型特异性**：不同模型对顺序的偏好不同。例如，GPT-4o 倾向于将示例（SHOT）放在指令之前，而 Gemini 则有不同的偏好。
*   **最新模型仍受影响**：即使是具备推理能力（Reasoning）的新一代模型（如 gpt-o1），依然表现出明显的顺序依赖性。

### 7. 优点
*   **科学性**：将传统的提示词“炼金术”转化为可量化、可建模的统计科学。
*   **经济性**：通过分数阶乘设计，在有限的预算内找到了接近全局最优的提示词配置。
*   **可解释性**：PWO 模型能够明确指出哪些元素的相对位置是关键驱动因素，为理解 LLM 的注意力机制提供了统计学视角。

### 8. 不足与局限
*   **任务单一性**：实验主要集中在“实验设计生成”这一特定领域，虽然具有代表性，但在创意写作或常识问答等非结构化任务上的泛化性尚待验证。
*   **动态性风险**：LLM 的 API 经常更新（版本迭代），当前版本下的最优顺序在模型更新后可能会失效，需要重新进行实验。
*   **交互项复杂性**：当提示词组件数量增加时，模型中的高阶交互项会变得非常复杂，难以直观解释。

（完）
