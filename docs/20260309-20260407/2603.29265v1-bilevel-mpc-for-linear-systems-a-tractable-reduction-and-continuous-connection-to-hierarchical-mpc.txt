Title: Bilevel MPC for Linear Systems: A Tractable Reduction and Continuous Connection to Hierarchical MPC

URL Source: https://arxiv.org/pdf/2603.29265v1

Published Time: Wed, 01 Apr 2026 00:31:43 GMT

Number of Pages: 12

Markdown Content:
# Bilevel MPC for Linear Systems: A Tractable Reduction and Continuous Connection to Hierarchical MPC 

Ryuta Moriyasu 1,2, Carmen Amo Alonso 1 and Marco Pavone 1,3

Abstract — Model predictive control (MPC) has been widely used in many fields, often in hierarchical architectures that combine controllers and decision-making layers at different levels. However, when such architectures are cast as bilevel op-timization problems, standard KKT-based reformulations often introduce nonconvex and potentially nonsmooth structures that are undesirable for real-time verifiable control. In this paper, we study a bilevel MPC architecture composed of (i) an upper layer that selects the reference sequence and (ii) a lower-level linear MPC that tracks such reference sequence. We propose a smooth single-level reduction that does not degrade perfor-mance under a verifiable block-matrix nonsingularity condition. In addition, when the problem is convex, its solution is unique and equivalent to a corresponding centralized MPC, enabling the inheritance of closed-loop properties. We further show that bilevel MPC is a natural extension of standard hierarchical MPC, and introduce an interpolation framework that continu-ously connects the two via move-blocking. This framework re-veals optimal-value ordering among the resulting formulations and provides inexpensive a posteriori degradation certificates, thereby enabling a principled performance–computational effi-ciency trade-off. Code for this project is available at https: //github.com/StanfordASL/Reduced_BMPC .

I. I NTRODUCTION 

A. Background 

Hierarchical control architectures, where upper-level de-cisions (e.g., economics, safety, system-wide performance) are coupled with the optimal response of a lower-level con-troller, are widespread in process control [1], [2], automotive systems [3], [4], power/microgrids [5], buildings/HVAC [6], spacecraft [7], traffic networks [8], and others. One reason is that separating design by layer, where each has dis-tinct functional-safety/responsibility requirements, is com-mon practice [1], [9]. Another reason is that confining learning or policy tuning to the upper layer is desirable [10] as it preserves the structure of the lower-level controller and thereby makes safety, verification, and deployment more manageable. Hence, hierarchical paradigms are naturally preferred in many fields [2]. In the context of model predictive control (MPC), hi-erarchical MPC (HMPC) [2], [11] is widespread. In this setting, the upper level selects steady-state targets/schedules and the lower level tracks them via dynamic MPC. 1Hence, HMPC is effective when the upper-level objective depends     

> This manuscript has been submitted to the IEEE Conference on Decision and Control (CDC) 2026 for possible publication. This work was supported by Toyota Motor Corporation
> 1Department of Aeronautics & Astronautics, Stanford University.
> {moriyasu,camoalon,pavone }@stanford.edu
> 2Toyota Motor North America. ryuta.moriyasu@toyota.com
> 3NVIDIA Research.

primarily on steady-state quantities and transient dynamics have limited influence [1], [12]. Yet, when transients affect cost or constraint usage, ignoring dynamics at the upper layer can incur non-negligible performance loss [13]. To avoid such losses while retaining the hierarchical architecture, approaches for bilevel MPC have been proposed [14], [15]. Bilevel MPC is formulated as a bilevel optimization prob-lem in which the upper-level optimization is constrained by the solution of a lower-level optimization problem [16], [17], [18]. A standard approach, especially when the lower-level problem is convex and regular, is to replace the lower-level problem with its KKT conditions to obtain a single-level 

mathematical program with complementarity constraints 

(MPCC) (see, e.g., [19]). In control, this approach is used in bilevel command governors [7], RTO–MPC embedding of KKT systems [11], differentiable MPC layers [20], and safety-oriented hierarchies [21], among others. Despite the widespread interest in bilevel MPC across different applications, current approaches still face significant challenges. In particular, MPCC formulations are inherently nonconvex due to complementarity and possible constraint qualification (CQ) violations (LICQ/MFCQ) [19], [22], [23], which can lead to local/multiple solutions and numerical instabilities. A common workaround is to encode comple-mentarity with binaries, yielding mixed-integer formulations. However, such reformulations lack scalability and cause policy discontinuities, which often leads to nonuniqueness and switching [24], [25], [26]. These drawbacks are at odds with control applications where speed, continuity, and uniqueness are critical. 

B. Aims and Contributions 

Motivated by the issues with MPCC, we seek a reduced bilevel MPC formulation that is solvable as a convex program to ensure the uniqueness of the solution while maintaining optimality. Particularly, we assume a bilevel MPC architec-ture where an upper layer selects a reference sequence that is given to the lower-level linear tracking MPC formulated as a strongly convex quadratic program (QP). Focusing on settings where the upper layer does not directly depend on the reference sequence, we provide a tractable alternative to MPCC-based bilevel MPC. Moreover, to make the resulting bilevel design easily deployable and comparable to standard HMPC baselines, a move-blocking [27] technique, which 

> 1The term “hierarchical MPC” is sometimes used with different meanings in the literature. Here, HMPC refers to the two-layer architecture with an upper-level steady-state target optimizer (or scheduler) and a lower-level tracking MPC, as formalized in Section IV.
> arXiv:2603.29265v1 [eess.SY] 31 Mar 2026

divides the horizon into some blocks and fixes the signal in each block, is introduced to connect bilevel MPC to HMPC and enable a principled trade-off between computational efficiency and performance.  

> •

Tractable reduction (QP-solvable bilevel consistency). 

We propose a smooth, complementarity-free single-level formulation by lifting the lower-level inequalities to the upper level and enforcing only stationarity. Under a ver-ifiable block-matrix nonsingularity condition, it is exact: it recovers the same optimal value and realized input tra-jectory as the original bilevel MPC, independently of the active-set pattern. Moreover, under a standard convexity assumption, the realized input optimizer is unique and coincides with that of a corresponding centralized MPC.  

> •

HMPC–BMPC connection (performance–complexity trade-off with certificates). We interpret standard HMPC as a one-block reference policy and connect it to bilevel MPC via move-blocking [27]. We establish optimal-value ordering among hierarchical, bilevel, and central-ized formulations and their blocked variants, and derive inexpensive a posteriori certificates that upper-bound the degradation induced by blocking. We also provide a con-structive procedure to build low-rank blocking matrices from HMPC data that improve over HMPC on sampled points/regions. In this paper, some proofs and remarks are omitted and provided in the appendix of the arXiv preprint [28]. 

Notation: Let us denote the set of all n × n symmetric matrices by Sn and symmetric positive (semi-)definite ma-trices by Sn 

> ++

(Sn

> +

); the identity matrix and zero matrix of appropriate size by I and O, respectively; the identity matrix with an explicit size of n×n by In; the n-dimensional vector whose all elements are 1 by 1n; the Moore–Penrose inverse of a matrix A by A†; the i-th element of a vector v by (v)i;the sub-vector consisting of (v)i, i ∈ J by (v)J ; a collective vector [v⊤ 

> 1

· · · v⊤ 

> n

]⊤ by [v1; · · · ; vn]; Kronecker product by 

⊗; a weighted norm (x⊤P x )1/2 with a vector x ∈ Rn and a matrix P ∈ Sn 

> ++

by ∥x∥P .II. S TANDARD BILEVEL MPC F ORMULATION 

In this section, we define the baseline bilevel MPC. This is a bilevel controller where the upper level selects the reference sequence as a stack of steady-state pairs, and the lower level decides the input to track the reference (see Fig. 1). Therefore, we introduce a minimal parameterization of the steady-state pair and then formulate the bilevel MPC. 

A. Plant and Steady-State 

Consider a discrete-time LTI system 

xk+1 = Ax k + Bu k,

with state xk ∈ Rn and input uk ∈ Rm.

Assumption 1. The matrix S := [ I − A, −B ] ∈ Rn×(n+m)

has full row rank (rank( S) = n).

The controllability of (A, B ) is sufficient to satisfy the above assumption, but it is not a necessary condition.     

> P1PLLTI
> Upper MPC Lower MPC Plant Reference sequence
> Θ
> Input
> u
> State, Output
> x, y

Fig. 1: Bilevel MPC architecture. The upper MPC selects the reference sequence Θ, the lower MPC computes the input u

to track Θ, and the plant returns the state and output (x, y ).The set of all steady-state pairs (¯ x, ¯u), i.e., S[¯ x; ¯ u] = 0, is an m-dimensional linear subspace of Rn+m because 

dim ker S = ( n + m) − rank( S) = m. We fix a basis of 

ker S and write the basis matrix as 

Z := 

Zx

Zu



∈ R(n+m)×m (im Z = ker S).

Hence, any steady-state pair (¯ x, ¯u) can be written as [¯ x; ¯ u] = 

Zθ with a minimal-dimensional parameter θ ∈ Rm.

Remark 1 (Steady-state map) . If I − A is nonsingular, one admissible choice for Z is Zx = ( I − A)−1B and Zu = Im,in which case ¯u = θ, ¯x = ( I − A)−1B ¯u. If not, i.e., for systems with integrators, one can use Z = null(S) (in MATLAB) to obtain one of the admissible Z. When I − A is nonsingular but ill-conditioned, a safe choice is to use Z =null(S, tol) with numerical tolerance tol .B. Control Problem 

Collect the input sequence U := [ u0; . . . ; uN −1] over the horizon length N , and define the predicted state stack 

X(U, x 0) := [ x1; . . . ; xN ] = ¯Ax 0 + ¯BU, 

¯A := 



AA2

...

AN

 , ¯B := 



B O · · · OAB B . . . ...... . . . . . . OAN −1B · · · AB B

 .

In addition, define the steady-state parameter sequence 

Θ := [θ0; . . . ; θN −1], meaning that each stage-wise ref-erence (¯ x, ¯u) is a steady-state pair parameterized via θ.Accordingly, we define the stacked steady-state references 

¯X(Θ) := [¯ x1; . . . ; ¯ xN ] and ¯U (Θ) := [¯ u0; . . . ; ¯ uN −1], with 

[¯ xk+1 ; ¯ uk] = Zθ k for k = 0 , . . . , N − 1.The lower-level MPC is defined as a strongly convex QP 

PL : min  

> U

Fl(U ; Θ , x 0) s.t. Gl(U ; x0) ≤ 0, where (1a) 

Fl := 12 X(U, x 0) − ¯X(Θ) 2¯Q + 12 U − ¯U (Θ) 2¯R, (1b) 

¯Q ∈ SN n  

> +

, ¯R ∈ SN m  

> ++

, and Gl encode linear constraints (e.g., input, state, and output bounds). 

Example 1 (Standard block–diagonal weights) . A standard weight setting for PL is to use block–diagonal stacks: 

¯Q = blkdiag( Q, . . . , Q 

| {z }  

> N−1times

, P ), ¯R = blkdiag( R, . . . , R 

| {z } 

> Ntimes

),

with stage, terminal, and input weights Q ∈ Sn

> +

, P ∈ Sn

> +

,R ∈ Sn

> ++

, respectively. We now state the standard bilevel MPC studied in this pa-per. It chooses Θ to minimize an application-level objective 

Fu(U ; x0) under upper-level constraints Gu(U ; x0) ≤ 0:

P1 : min  

> Θ, U

Fu(U ; x0) s.t. Gu(U ; x0) ≤ 0, (2a) 

U = arg min  

> V

{ Fl(V ; Θ , x 0) | Gl(V ; x0) ≤ 0 } , (2b) while enforcing that U is generated by PL. The standard method to solve P1 is to reformulate it as an MPCC: 

min  

> Θ,U,μ

Fu(U ; x0) s.t. Gu(U ; x0) ≤ 0, ∇U Fl +∇U Glμ = 0 , (3a) 

Gl(U ; x0) ≤ 0, μ ≥ 0, G ⊤ 

> l

μ = 0 , (3b) where μ is the Lagrange multiplier. This problem is non-convex due to its complementarity condition (3b) even if 

Gl is affine. This often causes issues such as a lack of reproducibility, insufficient performance, and discontinuity of the solution. 

Remark 2 (Why we parameterize references via θ). In tracking MPC, it is natural to restrict reference candidates to steady-state pairs. The parameterization (i) excludes unre-alizable ( ¯X, ¯U ), and thus, makes it easy to discuss stability, (ii) uses the minimal number of decision variables compared to treating ( ¯X, ¯U ) with the constraint (I − A) ¯X − B ¯U = 0 

explicitly, and also in our case, (iii) integrates smoothly with our method introduced in Section III. 

III. A T RACTABLE REDUCTION 

In this section, we propose a smooth single-level reduction of P1 to avoid the issues with the complementarity condition (3b) described above, and we show that the reduction can preserve performance and ensure the uniqueness of the solution and closed-loop stability under certain conditions. We propose the reduction for P1 by lifting the lower-level constraints to the upper level and replacing the lower-level problem with its unconstrained first-order optimality condition. Hence, the complementarity condition disappears: 

P2 : min  

> Θ, U

Fu(U ; x0) s.t. Gu(U ; x0) ≤ 0, G l(U ; x0) ≤ 0, (4a) 

∇U Fl(U ; Θ , x 0) = 0 . (4b) This can be viewed as a restriction of the design space of the MPCC (3) to μ = 0 . The condition (4b) is an affine equation in U, Θ for any fixed x0. Indeed, differentiating (1b) yields 

∇U Fl =   ¯B⊤ ¯Q ¯B + ¯RU − ¯ΓΘ + ¯B⊤ ¯Q ¯Ax 0 = 0 , (5) 

¯Γ := ¯B⊤ ¯Q ¯Zx + ¯R ¯Zu ∈ RN m ×N m .

Although P2 is a different problem from P1, the solution set , which is the set of points that have the minimum value within the feasible set, of P2 becomes a subset of that of P1,under the nonsingularity of ¯Γ. This implies that the optimal value does not change. 

Assumption 2. The matrix ¯Γ is nonsingular. 

Theorem 1 (Feasible/solution-set inclusion) . Under Assump-tion 1 and 2, let Fi, V i, Si be the feasible set, optimal value, and solution set of Pi, for a fixed x0. Then, 

(1) F2 ⊆ F 1, (2) V1 = V2, (3) S2 ⊆ S 1.

Proof. By Assumption 2, for any U and x0, (5) admits the unique solution Θ = Θ ⋆(U ; x0), where 

Θ⋆(U ; x0) := ¯Γ−1   ¯B⊤ ¯Q ¯B + ¯RU + ¯B⊤ ¯Q ¯Ax 0

, (6) which is obtained by solving (5) with respect to Θ.1) Let (Θ , U ) be feasible for P2. Then Gl(U ; x0) ≤ 0 holds and Θ satisfies the lower-level stationarity. Hence U solves the lower-level problem at (Θ , x 0), so (Θ , U ) is feasible for 

P1. This proves F2 ⊆ F 1.2) From (1), V1 ≤ V2. Conversely, take any feasible (Θ ◦, U ◦)

of P1. Replacing Θ◦ by Θ⋆(U ◦, x 0) preserves feasibility in 

P2 and leaves the upper-level objective unchanged as Fu

depends only on U . Hence V2 ≤ V1, which yields V1 = V2.3) Let (Θ ⋆, U ⋆) ∈ S 2. By (1), it is feasible for P1, and by (2), it attains the value V1; therefore (Θ ⋆, U ⋆) ∈ S 1.This shows that enforcing the first-order stationarity relation in P2 provides a direct handle on bilevel-feasible pairs, i.e., one can obtain (a subset of) P1 solutions without tackling an MPCC (3) directly. In this sense, P2 provides a tractable route to solutions of P1: solving P2 already returns candidates that are bilevel-consistent and hence eligible to be optimal for P1 once the upper objective Fu is minimized. On the other hand, the whole bilevel MPC system, i.e., a cascade connection of the upper-level MPC ( P1 or P2), the lower-level MPC ( PL), and the plant, is still structurally difficult to analyze for its closed-loop properties, such as stability. Therefore, as an analytical tool for the bilevel system, we next introduce an associated centralized MPC: 

P3 : min  

> U

Fu(U ; x0) s.t. Gu(U ; x0) ≤ 0, G l(U ; x0) ≤ 0. (7) Under strong convexity, we show that the cascade of P1 or 

P2 and PL is equivalent to the centralized MPC P3, and therefore, the closed-loop properties are inherited from P3.

Assumption 3 (Upper-level convexity) . For fixed x0,

Fu(·; x0) and Gu(·; x0) in (2) are strongly convex and convex, respectively, and C(x0) := {U | Gu(U ; x0) ≤

0, G l(U ; x0) ≤ 0}̸ = ∅.

Lemma 1 (Uniqueness of solution) . Under Assumptions 1, 2, and 3, for any fixed x0, P1, P2, and P3 have the same unique optimizer in U , denoted by U ⋆. In addition, P2 has the unique optimizer (Θ ⋆(U ⋆; x0), U ⋆).Proof. Under Assumption 2, using Θ⋆(U ; x0) in (6), the feasible set of P2 is F2 = { (Θ ⋆(U ; x0), U ) | U ∈ C (x0) },so its U -projection is exactly C(x0). By Theorem 1, F2 ⊆F1, and thus every U ∈ C (x0) is attained by some feasible pair of P1. Conversely, if (Θ , U ) is feasible for P1, then necessarily Gu(U ; x0) ≤ 0 and Gl(U ; x0) ≤ 0, i.e., U ∈C(x0). Therefore, the U -projection of the feasible set of P1

is also exactly C(x0).P1/P2 PL LTI     

> Plant
> Θ
> Bilevel MPC
> ux, y
> P3LTI
> Centralized MPC Plant
> ux, y
> = equivalent

Fig. 2: Equivalence between bilevel MPC and centralized MPC: the cascade of P1 or P2 and PL realizes the same input as P3, under Assumptions 1, 2, and 3. Thus, P1, P2, and P3 all reduce, at the level of U ,to minimizing Fu(U ; x0) over the same set C(x0). Since 

Fu(·; x0) is strongly convex by Assumption 3, this minimiza-tion admits a unique optimizer U ⋆. Hence all three problems share the same unique optimizer in U . Finally, since P2

associates a unique Θ⋆(U ; x0) with each U , its optimizer is uniquely given by (Θ ⋆(U ⋆; x0), U ⋆).

Theorem 2 (Equivalence to centralized MPC) . Under As-sumptions 1, 2, and 3, for any fixed x0, let (Θ , U ) be any optimizer of P1 or P2. Then, solving PL with Θ returns the unique optimizer of P3, namely U ⋆. Hence, the cascades 

P1→PL and P2→PL are equivalent to P3 in their realized input (Fig. 2). Proof. From Lemma 1, U = U ⋆. For P1, feasibility already means that U ⋆ solves PL with Θ. For P2, the conditions 

Gℓ(U ⋆; x0) ≤ 0, ∇U Fℓ(U ⋆; Θ , x 0) = 0 

are precisely the first-order optimality conditions of PL.Since PL is strongly convex, U ⋆ is the unique global optimizer. Thus, in either case, solving PL with Θ yields 

U = U ⋆, which is also the unique optimizer of P3.

Corollary 1 (Inheritance of closed-loop properties) . Under Assumptions 1, 2, and 3, suppose the closed-loop system with 

P3 is (globally or locally) stable with respect to a fixed point 

x∞. Then, the bilevel architectures combining PL with P1

or P2 inherit the corresponding stability property. Proof. The proof follows directly from the equivalence be-tween P3 and the cascade connection of P1 or P2 and PL.The details are provided in Appendix I [28]. 

P2 can be viewed as a problem that computes the refer-ence sequence Θ that reproduces the optimal U of P3 as an unconstrained solution for the lower-level MPC. Indeed, it is also possible to first solve P3, then solve (5) to obtain 

Θ, and feed that into PL. Therefore, if there is no need for a hierarchical design, directly using P3 is the best option. Crucially, even when a hierarchical implementation is desired (for purposes such as reusing existing controllers and dividing development tasks), one can achieve the same performance as P1 by using P2 and provide the stability guarantee for the bilevel implementation P1 or P2 with PL

by checking P3.

Remark 3 (Implicit assumption) . The guarantees estab-lished above assume that the underlying optimization prob-lems are solved to their intended optima. Under Assump-tions 1, 2, and 3, problems P2 and P3 meet this requirement when directly solved with off–the–shelf solvers. In contrast, tackling the original bilevel formulation P1 yields a non-convex problem that is sensitive to initialization and prone to spurious local minima. In practice, the returned solution may deviate from the behavior anticipated by Theorems 1 and 2 and Corollary 1. Section V demonstrates this empirically. 

IV. H IERARCHICAL –TO –B ILEVEL CONNECTION 

This section connects bilevel MPC and standard HMPC through move-blocking, which reduces the dimension of the decision variable in MPC. This demonstrates that bilevel MPC is a natural extension of HMPC and enables the design of trade-offs between computational efficiency and performance with certified bounds. 

A. HMPC as a One-Block Reference Policy 

We first recall a standard HMPC baseline that solves a steady-state optimization: 

P0 : θ∗ = arg min 

> θ



ℓ(x, u )

 gl(x, u )

gu(x, u )



≤ 0



,

xu



= Zθ (8) where ℓ is the cost function and gl, g u are the constraints for the lower and upper levels, respectively. Its solution is given to the lower-level MPC PL by stacking it as 

Θ0 := M1θ∗, M 1 := 1N ⊗ Im ∈ RN m ×m. (9) The matrix M1 represents the one-block (constant-in-horizon) blocking, so any θ ∈ Rm generates a stacked reference Θ = M1θ. Therefore, P0 can be seen as a problem of selecting Θ ∈ im M1; i.e., HMPC is a one-block reference policy. Here, we consider measuring the transient 

performance of P0 with the cost function Fu for P1–P3:

V0(x0) := Fu(U0(x0); x0),U0(x0) := arg min  

> U

{Fl(U ; Θ 0, x 0) | Gl(U ; x0) ≤ 0}.

Although P0 has a different objective function ℓ than Fu, this can be justified if we define Fu(U ; x0) = PN −1 

> k=0

ℓ(xk, u k),which is the natural extension of ℓ to measure the transient performance. We can also add the terminal cost to it to ensure convergence of P1–P3 to the optimal steady-state of P0

formally (see Appendix II [28]). 

B. Move-Blocked Bilevel Problems 

Assumption 4 (Blocking matrix) . The blocking matrix M ∈

RN m ×p has full column rank, i.e., rank( M ) = p.

For the above M , restricting Θ to the subspace im M via 

Θ = M Φ, Φ ∈ Rp,

leads the move-blocked version of P1 and P2:

P1(M ) : min  

> Φ, U

Fu(U ; x0) s.t. Gu(U ; x0) ≤ 0, (10) 

U = arg min  

> V

{ Fl(V ; M Φ, x 0) | Gl(V ; x0) ≤ 0 },P2(M ) : min  

> Φ, U

Fu(U ; x0) s.t. Gu(U ; x0) ≤ 0, (11) 

Gl(U ; x0) ≤ 0, ∇U Fl(U ; M Φ, x 0) = 0 .

For a fixed x0, let Vi(M ) denote the optimal value of Pi(M ),respectively. HMPC P0 computes a fixed reference and stacks it as 

Θ0 ∈ im M1. In contrast, P1(M ) and P2(M ) optimize transient behavior through Fu(U ; x0) while still restricting reference to a structured subspace im M . By enlarging im M

(relaxing blocking), we obtain a continuous interpolation from HMPC-like one-block references to full bilevel MPC. 

C. Optimal-Value Ordering 

The order of the optimal values for P0–P3 and move-blocked variants P1(M ), P2(M ) is summarized as: 

Theorem 3 (Optimal-value ordering) . Suppose Assumption 4 holds for any blocking matrices below. Under Assump-tions 1, 2 and a fixed x0, the following hold. 

1) V1 = V2 ≤ V1(M ) ≤ V2(M ) for any M .

2) Vi( ˆM ) ≤ Vi(M ) ( i = 1 , 2) for any M, ˆM satisfying 

im( M ) ⊆ im( ˆM ).

3) V1(M1) ≤ V0 if Gu(U0; x0) ≤ 0.

4) V2(M1) ≤ V0 if Gu(U0; x0) ≤ 0, ∇U Fl(U0; Θ 0, x 0) = 0 .Proof. 

1) The equality V1 = V2 follows from Theorem 1. The move-blocking restricts reference candidates, hence V1 ≤

V1(M ), V 2 ≤ V2(M ). If (Φ , U ) is feasible for P2(M ), then Gl(U ; x0) ≤ 0 and ∇U Fl(U ; M Φ, x 0) = 0 hold, which implies that U is the (unique) lower-level optimizer at 

(M Φ, x 0); thus (Φ , U ) is feasible for P1(M ) and V1(M ) ≤

V2(M ) holds. 2) im( M ) ⊆ im( ˆM ) implies the existence of T such that 

M = ˆM T ,2 hence the feasible set under M is contained in that under ˆM .3) By construction, we have Θ0 = M1θ∗ and U0 =arg min U { Fl(U ; Θ 0, x 0) | Gl(U ; x0) ≤ 0 }, hence (θ∗, U 0)

satisfies the bilevel constraint in P1(M1). Together with 

Gu(U0; x0) ≤ 0, it is feasible and yields V1(M1) ≤ V0.4) In the proof of (3), if ∇U Fl(U0; Θ 0, x 0) = 0 also holds, (θ∗, U 0) satisfies the constraint in P2(M1). Similarly, 

V2(M1) ≤ V0 holds. Note that if Assumption 3 is additionally satisfied, V1 =

V2 = V3 ≤ V1(M ) ≤ V2(M ) holds from Theorem 2. The control sequence U0 from P0 does not guarantee the upper-level constraint satisfaction Gu(U0; x0) ≤ 0. This fact motivates the extension from P0 to P1(M ) or P2(M ). Even if P0 satisfies Gu(U0; x0) ≤ 0, we have V1(M1) ≤

V0, showing the upper-dominance of P1(M1). Furthermore, 

∇U Fl(U0; Θ 0, x 0) = 0 is required to have V2(M1) ≤ V0.However, we can show the existence of a low-rank M such that V2(M ) ≤ V0 holds in a certain operational range of x0.                    

> 2Inclusion im( M)⊆im( ˆM)can be confirmed by the equivalent condition: rank ˆM= rank[ M, ˆM]. In this case, one matrix Ts.t. M=ˆM T can be obtained by T=ˆM†M.

Algorithm 1: Construction of M from P0 data 

Require: Samples {x(i)0 }Ns 

> i=1

⊂ ∪ Jj=1 Rj , tolerance 

tol > 0.

Ensure: A matrix M satisfying V2(M ) ≤ V0 on the sampled points.  

> 1:

For each sample x(i)0 : 

> 2:

Run P0 to obtain U (i)0 := U0(x(i)0 ). 

> 3:

Compute Θ(i) := Θ ⋆(U (i)0 ; x(i)0 ). 

> 4:

Form Θ := [ Θ (1) Θ(2) · · · Θ(Ns) ]. 

> 5:

Get M by M = orth( Θ,tol) (MATLAB). 

Proposition 1 (Low-rank M satisfying V2(M ) ≤ V0 on re-gion(s)) . Under Assumptions 1 and 2, assume that {R j }Jj=1 

satisfy Gu(U0(x0); x0) ≤ 0 for all x0 ∈ ∪ Jj=1 Rj and define 

SRj := {Θ⋆(U0(x0); x0) | x0 ∈ R j }.

1) There exists M such that ∪Jj=1 SRj ⊆ im( M ), and any such M guarantees 

V2(M ; x0) ≤ V0(x0) ∀x0 ∈

> J

[

> j=1

Rj .

2) In case {R j }Jj=1 is chosen so that the realized control law of P0 is affine for each region: U0(x0) = Aj x0 +

bj ∀x0 ∈ R j , one may choose M with 

p = rank( M ) ≤ J(n + 1) .

Proof. The proof follows from the piecewise affine nature of the control law of P0. The full details are provided in Appendix III [28]. In practice, since the spans usually overlap across regions, the required p can be much smaller than J(n+1) . The actual 

M with the minimal rank can be obtained by Algorithm 1, which ensures V2(M ) ≤ V0 on the sampled points . To ensure it in the regions that include sampled points, one needs n+1 

affine independent samples from each region. 

D. Degradation Bound 

We provide inequalities that bound the performance degra-dation due to the simplification of the problem by considering a generic constrained optimization problem: 

Proposition 2 (Optimal-value gap for generic optimization problem) . Consider an optimization problem: 

min  

> w∈Rq

f (w) s.t. g(w) ≤ 0. (12) 

Suppose that f is C1 and H-strongly convex 3 for some H ∈

Sq

> ++

; and g is C1 and convex. Let V be the optimal value and I := {i | gi(w) = 0 } be the active set, then 

0 ≤ f (w) − V (13) 

≤ 12 min  

> μ≥0

∥∇ f (w) + ∇gI (w)μ∥2 

> H−1

=: ∆( w),

holds for any w ∈ C := { w | g(w) ≤ 0 } satisfying Mangasarian–Fromovitz constraint qualification (MFCQ) 4.                      

> 3Satisfies f(v)≥f(w) + ∇f(w)⊤(v−w) + 12∥v−w∥2
> H∀w, v ∈Rq.
> 4There exists dsuch that ∇gI(w)⊤d < 0. Linear independent constraint qualification (LICQ) or Slater condition is sufficient for MFCQ.

Proof. Trivially, we have f (w) − V ≥ 0. Let v = w∗ =arg min w∈C f (w), then the condition f (v) ≥ f (w) + 

∇f (w)⊤(v − w) + 12 ∥v − w∥2 

> H

∀w, v ∈ Rq yields f (w) −

V ≤ − ( 12 ∥d∥2 

> H

+ ∇f (w)⊤d) ∀w ∈ C, where d := w∗ − w.Introducing the tangent cone TC (w) ( ⊇ C −{ w} ∋ d) yields 

0 ≥ V − f (w) ≥ inf 

> d∈C−{ w}

n 12 ∥d∥2 

> H

+ ∇f (w)⊤d

o

≥ inf  

> d∈TC(w)

n 12 ∥d∥2 

> H

+ ∇f (w)⊤d

o

. (14) Under MFCQ at w, TC (w) =  d ∈ Rq ∇gI (w)⊤d ≤ 0 .Therefore, the right-hand side reduces to the convex QP 

min  

> d
> 12

∥d∥2 

> H

+ ∇f (w)⊤d s.t. ∇gI (w)⊤ d ≤ 0,

and it is lower-bounded by its Lagrange dual: 

max  

> μ≥0

− 12 ∥∇ f (w) + ∇gI (w)μ∥2 

> H−1

= −∆( w),

which proves the claim, combining it with (14). The computation for evaluating ∆( w) is inexpensive because the problem reduces to a nonnegative least-squares problem with dimension |I| , which is usually much smaller than q.

Remark 4 (Tighter bound) . The bound ∆( w) can be con-servative when only a few constraints are active (e.g., under coarse blocking such as M1). To evaluate a tighter bound, one can use 

∆ε(w) := min 

> μ≥0

 12 ∥∇ f (w)+ ∇gJ (w)μ∥2 

> H−1

−gJ (w)⊤μ



,

J (w; ε) := { i | gi(w) ≥ − ε } (ε > 0) ,

instead of ∆( w). Its detail is provided in Appendix IV [28]. 

Utilizing this, we provide the upper-bounds of perfor-mance degradation. 

Theorem 4 (A posteriori bounds for degradation) . For a fixed x0, suppose that Assumptions 1, 2, and 3 hold; 

Fu(·; x0), G u(·; x0) are C1. Let M, ˆM satisfy Assumption 4 and im M ⊆ im ˆM ; and T satisfies M = ˆM T . Set (12) as: 

f (w) = Fu(U ⋆( ˆM w ; x0); x0), (15) 

g(w) = G(U ⋆( ˆM w ; x0); x0), w = ˆΦ,

where U ⋆(Θ; x0) :=   ¯B⊤ ¯Q ¯B + ¯R−1(¯ΓΘ − ¯B⊤ ¯Q ¯Ax 0),G(U ; x0) := [ Gl(U ; x0); Gu(U ; x0)] .

1) If P2(M ) is feasible and satisfies MFCQ at its optimal solution Φ⋆, then the following holds. 

0 ≤ V2(M ) − V2( ˆM ) ≤ ∆( T Φ⋆).

2) Consider ˆM = IN m (therefore T = M ). If P0 is feasible and its solution U0(x0) satisfies Gu(U0; x0) ≤ 0 and MFCQ for (15) at w = Θ ⋆(U0; x0) (see (6) ), then the following holds. 

0 ≤ V0 − V2 ≤ ∆(Θ ⋆(U0(x0); x0)) .

Proof. The proof follows from applying Proposition 2. The details are provided in Appendix V [28]. The bounds in Theorem 4 are designed for one-sided evaluation: it suffices to solve only the “efficient” tractable problem ( P2(M ) and P0 for cases (1) and (2) of Theorem 4, respectively) and then evaluate ∆( ·) at the resulting candi-date point. Importantly, the “expensive” counterpart ( P2( ˆM )and P2 for cases (1) and (2) of Theorem 4, respectively) does 

not need to be solved to certify a performance gap. After computing the “efficient” problem, the inequality provides a quantitative limit on the maximum performance improvement achievable by considering the transient behav-ior (case (2)) or enlarging the blocking subspace im( M ) ⊆

im( ˆM ) (case (1)). This enables a principled “stop-or-refine” decision: if the bound is already small, increasing degrees of freedom or switching to the heavier formulation is guaran-teed to yield only marginal improvement. Finally, computing ∆( ·) is inexpensive. It amounts to solving a nonnegative least-squares problem in the multipli-ers associated with the active inequalities at the evaluation point, which is typically much lower-dimensional than the original optimization variables. Hence, the proposed bounds act as lightweight, practically computable certificates with negligible overhead. V. N UMERICAL EVALUATION 

Here, we present a toy example to verify the validity of the theory with minimal complexity. An example demonstrating the applicability to realistic problems, using a quadrotor as the target, is provided in Appendix VI [28]. 

A. Settings 1) Target system: We compare the solutions of problems 

P0–P3 and their variants for a simple linear system: 

xk+1 = Ax k + Bu k, x =

pv



∈ R2, u ∈ R,A =

1 Ts

0 1



, B =

 12 T 2

> s

Ts



, x 0 =

−10



,

where Ts = 0 .3 s is the sampling period, p is position, and 

v is velocity. This satisfies Assumption 1 ( rank( S) = 2 ). 

2) Lower-level MPC: The lower-level tracking MPC PL

(1) is constructed according to Example 1 with (Q, P, R ) = (I, I, 0.1 I), N = 10 , which makes Assumption 2 hold. Although I − A is not invertible for this system, its steady-state can be parameterized by [¯ x; ¯ u] = [ Zx; Zu]θ, Z x =[1; 0] , Z u = 0 , implying θ means the steady position. We set the input box constraint −1N ≤ U ≤ 1N encoded via 

Gl(U ; x0) ≤ 0, which is the stack of stage-wise constraints 

gl(xk, u k) = [ uk − 1; −uk − 1] ≤ 0. Due to strong convexity, this problem has a unique solution for any x0.

3) Upper-level concept: As an upper-level goal, we as-sume a fixed target state ˇx = [1; 0] and input ˇu = 0 , and the state (position) bound p ≤ 0 encoded as gu(x, u ) ≤ 0.Therefore, the target state is outside the feasible set. Under this position constraint and the lower-level input constraint, we aim to minimize the upper-level cost: 

ℓ(x, u ) := 12 ∥x − ˇx∥2 

> Q

+ 12 ∥u − ˇu∥2

> R

.Here, we used the same Q and R as the lower-level, for simplicity. 

4) Hierarchical MPC: Before considering a bilevel MPC, we construct HMPC P0 (8) as a baseline, using the above 

ℓ, g l and gu. For this problem setup, the solution of the steady-state optimization is θ∗ = 0 , meaning [¯ x; ¯ u] = [0; 0] .

5) Bilevel and centralized MPCs: We define a bilevel MPC P1 (2), its reduction P2 (4), their move-blocked variants P1(M ) (10) and P2(M ) (11), and the corresponding centralized MPC P3 (7), using: 

Fu(U ; x0) = Vf (xN ) +  

> N−1

X

> k=0

ℓ(xk, u k),Gu(U ; x0) = 



gu(x0, u 0)

...

gu(xN −1, u N −1)

 , V f (x) := 12 ∥x − ˇx∥2 

> P

,

which satisfies Assumption 3 for the initial state x0 shown above. To examine the effect of move-blocking, we use 

Mi := 

 IiO, 1N −i



⊗ Im ∈ RN m ×im (i ∈ [1 , N ]) , (16) that satisfies Assumption 4 and makes the first i steps in the horizon free and the last (N − i) steps blocked. This also satisfies im Mi ⊆ im Mi+1 for any i ∈ [1 , N −1] and corresponds to M1 in (9) in case i = 1 . In addition, i = N

(in this case, i = 10 ) means that the problem has full degrees of freedom, i.e., P1(MN )/ P2(MN ) are equivalent to P1/P2,respectively. A low-rank move-blocking matrix M ⋆ is also prepared by generating 50 initial states around x0 and giving it to Algorithm 1. The obtained M ⋆ had rank( M ⋆) = 2 .

6) Numerical methods: P1 is converted into an equivalent single-level problem: 

min  

> Θ

Fu(U ∗(Θ; x0); x0) s.t. Gu(U ∗(Θ; x0); x0) ≤ 0, where 

U ∗(Θ; x0) := arg min  

> V

{ Fl(V ; Θ , x 0) | Gl(V ; x0) ≤ 0 } ,

and is solved by SQP solver (MATLAB fmincon ) using implicit differentiation to obtain ∇ΘU ∗. P1(M ) is also converted in the same manner. P0, P2, P2(M ), and P3

are solved by QP solver (MATLAB quadprog ). 

B. Results 

Fig. 3 shows the numerical solutions for P0, P1, and P2.Fig. 3a and 3d are for P0 and P2, respectively, and Fig. 3b and 3c are for P1 obtained with (b) Θ = 0 initialization and (c) random Θ initialization. In each figure, colors distinguish the elements of the state (blue: p, red: v); the results for each problem are shown in solid lines; the optimal sequence of P3

(denoted as u∗, x ∗) is shown in dotted lines as a baseline; the upper and lower bounds are shown in dashed lines. From these results, we can confirm:  

> •

P0 (Fig. 3a) provided the sequence obtained from PL with 

Θ = 0 , which was different from that of P3, showing that the result was not optimal in the measure of Fu. 

> •

P1 with Θ = 0 initialization (Fig. 3b) and P2 (Fig. 3d) provided the optimal sequence that coincided with the        

> (a) P0
> (b) P1with initial solution Θ = 0 .
> (c) P1with random initial solution.
> (d) P2

Fig. 3: Numerical comparison of P0, P1, and P2. P2

reliably reproduces the centralized solution P3 (dotted lines), whereas P1 does so only under favorable initialization, high-lighting the practical robustness of the proposed reduction. result of P3. Both had different sequences in Θ, implying there were multiple (actually, infinite) solutions for P1.

P2 successfully provided one of them as a unique solution.  

> •

P1 with random initialization (Fig. 3c) failed to obtain the optimal sequence. This showed sensitivity to the choice of the initial solution due to the non-convexity of P1,motivating the use of the proposed problem P2.Fig. 4 summarizes the performance degradations and their estimated bounds of P0, P1(Mi), and P2(Mi) ( i =1, . . . , 10 ), compared to P2 (= P1, P3). In this figure, blue, green, and red circles show the performance degradations 

V2(Mi) − V2, V 1(Mi) − V2 (i = 1 , . . . , 10) and V0 − V2,respectively; stems represent the upper bounds, i.e., blue and red ones show ∆( T Φ⋆), ∆(Θ ⋆), respectively; cross markers represent the tighter bounds, i.e., the blue and red ones show 

∆ε(T Φ⋆) and ∆ε(Θ ⋆) with ε = 0 .5; and the blue dashed line represents V2(M ⋆) − V2. Note that P1(M ) is solved with Θ = 0 initialization. From the figure, we can see: Fig. 4: Performance degradation relative to P2 for move-blocked problems (blue circle: P2(M ), green circle: P1(M )) and HMPC P0 (red circle), together with the upper bounds. The plot shows monotonic performance improvement as the blocking is relaxed; the upper bound (bar) captures the degradation trend well, while the tighter bound (cross) is more effective under coarse blocking; and P2(M ) with the low-rank matrix M ⋆ (broken line) outperforms P0.

• The results are consistent with the orders shown in The-orem 3, i.e., V0 ≥ V1(M1), V j (Mi) ≥ Vj (Mi+1 ) ( i =1, . . . , 9, j = 1 , 2) , V 2(Mi) ≥ V1(Mi) ( i = 1 , . . . , 10) .

• Although V0 ≥ V2(M1) did not hold because ∇Fl̸ = 0 at the solution of P0, V0 ≥ V2(M ⋆) is satisfied as intended. 

• ∆ was confirmed to work as an upper-bound of degrada-tion for P2(M ) and P0. It looks looser in small i, but ∆ε

succeeded in tightening the bound. VI. C ONCLUSIONS 

We studied a bilevel MPC architecture in which an upper level selects steady-state/reference parameters, while a lower level implements a linear tracking MPC formulated as a strongly convex QP, assuming that the upper objective and constraints depend on the lower-level outcome only through the realized input trajectory. We derived a smooth single-level reduction that avoids complementarity by lifting lower-level inequalities to the upper level and enforcing only stationarity. Under a verifiable nonsingularity condition, the reformulation is exact and recovers the same optimal value and realized input trajec-tory as the original bilevel MPC. Under standard convexity assumptions, the realized input optimizer is unique and coin-cides with that of the associated centralized MPC, enabling certification of closed-loop properties. A nullspace-based steady-state parameterization using S = [ I − A, −B] further accommodates integrator-containing or ill-conditioned plants without requiring (I − A)−1.Finally, we connected HMPC and bilevel MPC via move-blocking, establishing optimal-value ordering among hierar-chical, bilevel, and centralized formulations and their blocked variants. This connection provides computable certificates for blocking-induced degradation and enables a principled trade-off between performance and computational complexity. Numerical results on an integrator-containing toy example illustrate the recovery of the centralized solution and the effectiveness of certificate-guided move-blocking. These results make bilevel control architectures, often avoided in practice due to their complexity, more practical to deploy. They also reduce reliance on ad hoc heuristics and support a more systematic design process across a broad range of applications. REFERENCES 

[1] S. J. Qin and T. A. Badgwell, “A survey of industrial model predictive control technology,” Control Engineering Practice , 2003. [2] R. Scattolini, “Architectures for distributed and hierarchical model predictive control,” Journal of Process Control , 2009. [3] P. Falcone, F. Borrelli, H. E. Tseng, J. Asgari, and D. Hrovat, “A hierarchical model predictive control framework for autonomous ground vehicles,” in 2008 American Control Conference , IEEE, 2008. [4] E. Garone, S. Di Cairano, and I. V. Kolmanovsky, “Reference and command governors for systems with constraints: A survey on theory and applications,” 

Automatica , 2017. [5] J. M. Guerrero, J. C. Vasquez, J. Matas, L. Garc´ ıa de Vicu˜ na, and M. Castilla, “Hierarchical control of droop-controlled AC and DC microgrids–a general approach toward standardization,” IEEE Transactions on Industrial Electronics ,2011. [6] G. Serale, M. Fiorentini, A. Capozzoli, D. Bernardini, and A. Bemporad, “Model predictive control (MPC) for enhancing building and HVAC system energy efficiency: Problem formulation, applications and opportunities,” Ener-gies , 2018. [7] I. V. Kolmanovsky, T. Cunis, and E. Garone, “Command governors based on bilevel optimization for constrained spacecraft orbital transfer,” in AIAA SciTech Forum , 2024. [8] Z. Zhou, B. De Schutter, S. Lin, and Y. Xi, “Two-level hierarchical model-based predictive control for large-scale urban traffic networks,” IEEE Transactions on Control Systems Technology , 2017. [9] A. Benveniste et al., Contracts for System Design . Now Publishers, 2018. [10] L. Hewing, K. Wabersich, M. Menner, and M. N. Zeilinger, “Learning-based model predictive control: Toward safe learning in control,” Annual Reviews in Control , 2020. [11] A. G. Marchetti, A. Ferramosca, and A. H. Gonz´ alez, “Steady-state target optimization designs for integrating real-time optimization and model predictive control,” Journal of Process Control , 2014. [12] J. B. Rawlings, D. Q. Mayne, and M. Diehl, Model Predictive Control: Theory, Computation, and Design , 2nd ed. Nob Hill Publishing, 2017. [13] D. Angeli, R. Amrit, and J. B. Rawlings, “On average performance and stability of economic MPC,” IEEE Transactions on Automatic Control , 2012. [14] S. Avraamidou and E. Pistikopoulos, “A multi-parametric optimization ap-proach for bilevel mixed-integer linear and quadratic programming problems,” 

Computers & Chemical Engineering , 2019. [15] Y. Mintz, J. A. Cabrera, J. R. Pedrasa, and A. Aswani, “Control synthesis for bilevel linear model predictive control,” in 2018 Annual American Control Conference (ACC) , 2018. [16] B. Colson, P. Marcotte, and G. Savard, “An overview of bilevel optimization,” 

Annals of Operations Research , 2007. [17] S. Dempe, Foundations of Bilevel Programming . Springer, 2002. [18] J. F. Bard, Practical Bilevel Optimization . Springer, 1998. [19] Z.-Q. Luo, J.-S. Pang, and D. Ralph, Mathematical programs with equilibrium constraints . Cambridge University Press, 1996. [20] B. Amos, I. D. Jimenez Rodriguez, J. Sacks, B. Boots, and J. Z. Kolter, “Differentiable MPC for end-to-end planning and control,” in Advances in Neural Information Processing Systems (NeurIPS) , 2018. [21] M. K¨ ogel, M. Ibrahim, C. Kallies, and R. Findeisen, “Safe hierarchical model predictive control and planning for autonomous systems,” International Journal of Robust and Nonlinear Control , 2025. [22] H. Scheel and S. Scholtes, “Mathematical programs with complementarity constraints,” Mathematical Programming , 2000. [23] J. J. Ye, “Constraint qualifications and KKT conditions for bilevel programming problems,” Mathematics of Operations Research , 2006. [24] T. Kleinert, M. Labb´ e, I. Ljubi´ c, and M. Schmidt, “A survey on mixed-integer programming techniques in bilevel optimization,” EURO Journal on Computational Optimization , 2021. [25] T. Kleinert, M. Labb´ e, F. Plein, and M. Schmidt, “There´ s no free lunch: On the hardness of choosing a correct big-M in bilevel optimization,” Operations Research , 2020. [26] Modeling cookbook: Mixed-integer optimization , Edition 2023, MOSEK, 2023. [27] R. Cagienard, P. Grieder, E. C. Kerrigan, and M. Morari, “Move blocking strategies in receding horizon control,” Journal of Process Control , 2007. [28] R. Moriyasu, C. A. Alonso, and M. Pavone, “Bilevel MPC for linear systems: A tractable reduction and continuous connection to hierarchical MPC,” arXiv preprint , 2026. [29] M. Korda and I. Mezi´ c, “Linear predictors for nonlinear dynamical systems: Koopman operator meets model predictive control,” Automatica , 2018. [30] R. Moriyasu, T. Ikeda, S. Kawaguchi, and K. Kashima, “Structured Hammerstein-Wiener model learning for model predictive control,” IEEE Control Systems Letters , 2021. [31] S. S. Narayanan, D. Tellez-Castro, S. Sutavani, and U. Vaidya, “SE(3) Koopman-MPC: Data-driven learning and control of quadrotor UAVs,” IFAC-PapersOnLine , 2023, 3rd Modeling, Estimation and Control Conference MECC 2023, ISSN : 2405-8963. [32] L. Gr¨ une and M. Stieler, “A Lyapunov function for economic MPC without terminal conditions,” in 53rd IEEE Conference on Decision and Control , 2014. 

APPENDIX IPROOF OF COROLLARY 1Let κ denote the receding-horizon control law induced by 

P3 and suppose the closed-loop with P3, i.e., xk+1 = Ax k +

Bκ (xk), has the stated stability property. By Theorem 2, at each time step and for any initial state, the cascade connec-tion of P1 or P2 and PL produces the same optimizing input sequence in U as P3. Hence, the implemented first control move is identical to κ(xk). Therefore, the closed-loop trajectories coincide, and the claimed stability property is inherited. APPENDIX II CONVERGENCE DESIGN FOR MPC S

P0 decides the optimal steady state and input pair 

(x∗, u ∗), and the tracking MPC PL can ensure convergence to the point by appropriate design. The bilevel cascade of 

P1 or P2 and PL and its equivalent P3 can also ensure convergence to the same point under some natural settings. Given that the upper-level goal is to minimize ℓ under the constraints gu ≤ 0 and gl ≤ 0, P0 is the natural design for the goal when we can ignore the transient behavior. On the other hand, when we consider the transient, setting 

Fu(U ; x0) = Vf (xN ) + PN −1 

> k=0

ℓ(xk, u k) and constructing 

Gu and Gl by stacking gu and gl is also natural, and it can be seen as an extension of P0 to the transient. In this setting, P3 generally reduces to an economic MPC, and the appropriate terminal cost Vf ensures convergence to (x∗, u ∗)

that minimizes ℓ(x∗, u ∗) within the constraints. Extensive theories have been studied to ensure the convergence of the economic MPC [12]. Under Assumptions 1, 2, and 3, the cascade of P1 or P2 and PL inherits this convergence property from Corollary 1. Therefore, all of these controllers can achieve convergence to the optimal steady point of P0

by appropriate design. Under such a scenario, using Fu as the measure of the 

transient performance for P0 is justified. APPENDIX III PROOF OF PROPOSITION 11) M = IN m trivially satisfies ∪Jj=1 SRj ⊆ im( M ) = RN m .Under ∪Jj=1 SRj ⊆ im( M ), for any x0 ∈ ∪ Jj=1 Rj , there exists a solution Φ = Φ ⋆ ∈ Rp for Θ⋆(U0(x0); x0) = M Φ

since Θ⋆(U0(x0); x0) ∈ im M . This implies (Φ ⋆, U 0) is a feasible solution for P2(M ), and therefore, V2(M ; x0) ≤

Fu(U0; x0) = V0(x0) holds for any x0 ∈ ∪ Jj=1 Rj .2) Since x0 7 → Θ⋆(U0(x0); x0) is affine in each region, 

dim(span( SRj )) ≤ n + 1 holds for each j. This yields 

dim(span( ∪Jj=1 SRj )) ≤ PNj=1 dim(span( SRj )) ≤ J(n + 1) .Thus, there exists M s.t. ∪Jj=1 SRj ⊆ span( ∪Jj=1 SRj ) ⊆

im( M ) with dim im M = rank( M ) ≤ J(n + 1) .APPENDIX IV TIGHTER BOUND IN PROPOSITION 2We can obtain a tighter bound by incorporating nearly-active constraints through a slack-preserving linearization. For any ε > 0, J (w; ε) := { i | gi(w) ≥ − ε } satisfies J ⊇ I(w). The convexity of g implies that for any feasible v ∈ C

and d := v − w, gJ (v) ≥ gJ (w) + ∇gJ (w)⊤d ≤ 0, and hence C − { w} ⊆  d ∈ Rq ∇gJ (w)⊤d ≤ − gJ (w) .Replacing TC (w) with this outer approximation yields the tightened bound 0 ≤ f (w) − V ≤ ∆ε(w), where ∆ε(w) can be computed via the following problem: 

∆ε(w) := min 

> μ≥0

 12 ∥∇ f (w) + ∇gJ (w)μ∥2 

> H−1

− gJ (w)⊤μ



,

which remains inexpensive to solve since it is still a non-negative least-squares problem. This reduces to ∆( w) when 

ε = 0 because J (w; 0) = I(w) and gI (w) = 0 . Moreover, enlarging J (taking larger ε > 0) yields a monotonically tighter certificate. APPENDIX VPROOF OF THEOREM 41) Since ∇U Fl(U ; ˆM ˆΦ, x 0) = 0 is equivalent to U =

U ⋆( ˆM ˆΦ; x0) (see (5)), P2( ˆM ) can be rewritten as: 

min 

> ˆΦ

Fu(U ⋆( ˆM ˆΦ; x0); x0) s.t. G(U ⋆( ˆM ˆΦ; x0); x0) ≤ 0.

This reduces to (12) by setting f, g as in (15). From As-sumption 3, Fu(·; x0) is strongly convex (let ¯H ∈ SN m 

> ++

make it ¯H-strongly convex) and Gu(·; x0) is convex. To-gether with the fact that U ⋆( ˆM ˆΦ; x0); x0) is affine with respect to ˆΦ, both f (w), g (w) become convex. In particular, 

f (w) is ˆH-strongly convex with ˆH = W ⊤ ¯HW ∈ Sdim ˆΦ++ 

because W :=   ¯B⊤ ¯Q ¯B + ¯R−1 ¯Γ ˆM ∈ RN m ×(dim ˆΦ) has full column rank. Since P2(M ) is feasible and M Φ⋆ =ˆM T Φ⋆ holds, ˆΦ = T Φ⋆ is feasible for P2( ˆM ). Define 

∇(·)G⋆(Θ) := ∇(·)GI (U ⋆(Θ)) , then MFCQ at Φ⋆ for 

P2(M ) implies that there exists d s.t. ∇ΦG⋆(M Φ⋆)⊤d =

∇U G⋆(M Φ⋆)⊤∇ΘG⋆(M Φ⋆)⊤M dM = ∇U G⋆( ˆM T Φ⋆)⊤

∇ΘG⋆( ˆM T Φ⋆)⊤ ˆM T d = ∇ˆΦgI (T Φ⋆)⊤(T d ) < 0, which means MFCQ at T Φ⋆ for P2( ˆM ). Therefore, we can apply (13) with w = T Φ⋆ to prove the claim. 2) P2 can be rewritten as (12) by setting f, g as (15) with ˆM = IN m , ˆΦ = Θ. Under Gu(U0; x0) ≤ 0,

(Θ ⋆(U0; x0), U 0) is feasible for P2. Therefore, applying this w = Θ ⋆(U0; x0) to (13) leads to 0 ≤ V0 − V2 ≤

∆(Θ ⋆(U0; x0)) .APPENDIX VI QUADROTOR CONTROL VIA KOOPMAN EDMD Our method can be applied to a wide range of nonlin-ear systems by using linearizing techniques, e.g., Koop-man extended dynamic mode decomposition (EDMD) [29], Hammerstein–Wiener [30], etc. We address the control of quadrotors as a specific example, referring to [31]. 

A. Modeling and Pre-stabilization 

We consider the following dynamics for a quadrotor: 

˙xp = xv , ˙xv = vt

m xr ez − ge z ,

˙xR = xR ˆω, ˙xω = J−1(vm − xω × Jx ω ),

where xp ∈ R3 is the position, xv ∈ R3 is the velocity, 

xR ∈ SO(3) ⊂ R3×3 is the rotation matrix, xω ∈ R3

is the angular velocity, m = 4 .34 kg is the mass, J =diag(0 .0820 , 0.0845 , 0.1377) kg · m2 is the matrix of the moment of inertia, g = 9.81 m /s2 is the gravitational acceleration, ez = [0; 0; 1] is the unit vector, and ˆ(·) : R3 →

SO(3) is the hat operator: a × b = ˆ ab for a, b ∈ R3. It has a thrust force vt ∈ R and a total moment vm ∈ R3 as control inputs, both are defined in the body frame. An LTI model is learned from the above nonlinear dynam-ics via Koopman EDMD: 

xk+1 = A0xk + Bv k, v = [ ut; vm] ∈ R4,x = [ xp; xv ; vec (xR − I); xω ;(xR − I)ez ; vec (xR ˆω); J−1 ˆω(Jω )] ∈ R27 ,

with time-step ∆t = 0 .03 s. Although the learning process is omitted as it is beyond the scope, if the dynamics are learned appropriately, A0 should be unstable and approximately satisfy (xp)k+1 = ( xp)k + ( xv )k∆t, i.e., A0 has integrators (eigenvalues 1), and equivalently, I − A0 is singular. To avoid internal divergence in MPC computation due to instability, we apply a local pre-stabilizing feedback vk =

−Kx k + uk with a new input uk ∈ R4, resulting in 

xk+1 = Ax k + Bu k, A := A0 − BK. (17) The gain K is designed to preserve the integrator modes, to prevent the stable position from being fixed by this local control 5.

B. Lower-level MPC 

We construct the lower-level tracking MPC PL (1) for the pre-stabilized LTI (17), mainly aiming to track given position                                                               

> 5Assume A0includes lintegrators ( rank( I−A0) = n−l), let V∈
> Rn×l, W ∈Rn×(n−l)be basis matrices of ker( I−A0)and im( I−
> A0), respectively (so T:= [ W, V ]is nonsingular). This yields ˜A0:=
> T−1A0T= [ A11 , O ;A21 , I l],˜B:= T−1B= [ B1;B2], and then we can design K1to allow A11 −B1K1to have eigenvalues within the unit circle. Finally, we have ˜K:= KT = [ K1, O ], preserving integrators in
> A=T( ˜A0−˜B˜K)T−1=T[A11 −B1K1, O ;A21 −B2K1, I l]T−1.

trajectories. The objective function Fl(U ; Θ , x 0) is set as in Example 1, and in this case, Fl can be rewritten as: 

Fl(U ; Θ , x 0) = 12 ∥xN − ¯xN ∥2

> P

+ 12 

> N−1

X

> k=0

(∥xk − ¯xk∥2 

> Q

+ ∥uk − ¯uk∥2

> R

),

where [¯ xk+1 ; ¯ uk] = Zθ k (k = 0 , . . . , N − 1) , ¯x0 = x0.We set N = 30 , Q = diag  I3, 10 −4I24 

 ≻ 0, aim-ing to impose a larger cost on the position error, and 

R = 0.01 I4 ≻ 0. The terminal weight P ≻ 0 is de-termined by solving the discrete-time Lyapunov equation 

(A − BK f )⊤P (A − BK f ) − P = −(Q + K⊤ 

> f

RK f ), where 

Kf is the virtual gain obtained by discrete-time LQR with weights (Q, R ). Since I − A is singular due to integrators in A, the matrix Z = [ Zx; Zu] is chosen from ker S (S =[I − A, −B]) ; specifically, we take it by Z = null(S) 

in MATLAB. In addition, we impose the stage-wise input box constraint −20 · [2; 1; 1; 1] ≤ vk (= −Kx k + uk) ≤

20 · [2; 1; 1; 1] ( k = 0 , ..., N − 1) encoded as gl(xk, u k) ≤

0 ( k = 0, ..., N − 1) , and by stacking them, we have 

Gl(U ; x0) = [ gl(x0, u 0); gl(x1, u 1); ... ; gl(xN −1, u N −1)] ≤

0. The terminal set is omitted here for simplicity, so stability is not fully guaranteed; however, the terminal cost ensures local asymptotic stability around (x, u ) = (0 , 0) in the case 

(¯ xk+1 , ¯uk) = (0 , 0) ( k = 0 , ..., N − 1) , since that point is a steady-state in the interior of the feasible set. The initial state is set to x0 = 0 .

C. Upper-level MPCs 

As an upper-level goal, we assume a fixed target state 

ˇx = [ˇ xp; 0; 0; ... ; 0] , ˇxp = [0; 0; 1] , target input ˇu = 0,and the x, z –position bound xpx ≤ 0, x pz ≤ 0 encoded as gu(x, u ) ≤ 0. Therefore, the target state is outside the feasible set. Under this position constraint and the lower-level input constraint, we aim to minimize the upper-level cost: 

ℓ(x, u ) := 12



∥xk − ˇx∥2 

> Q

+ ∥uk − ˇu∥2

> R



.

Here, we used the same Q and R as the lower-level, for simplicity. Before considering a bilevel MPC, we construct HMPC 

P0 (8) as a baseline, using ℓ(x, u ), g u(x, u ), g l(x, u ). For our problem setup, the solution is (x∗, u ∗) = (0 , 0) ( θ∗ = 0) .Next, we construct bilevel MPC P1 (2), its reduction P2

(4), their move-blocked variants P1(M ) (10) and P2(M )(11), and the corresponding centralized MPC P3 (7). The objective and inequality constraint functions are set as 

Fu(U ; x0) := Vf (xN ) +  

> N−1

X

> k=0

ℓ(xk, u k),Vf (x) := 12 ∥x − x∗∥2 

> P

− ν⊤(x − x∗),Gu(U ; x0) := [ gu(x0, u 0) ; · · · ; gu(xN −1, u N −1) ] ,where ν is a vector satisfying (SZ )⊤ν = 0 6. For simplicity, we again omit terminal constraints here. However, since this setting establishes strict dissipativity 7 with respect to 

(x, u ) = ( x∗, u ∗), practical convergence to that point can be achieved under additional conditions such as recursive feasibility and the turnpike property [32]. Although the additional assumptions have not been verified, we confirmed that it converged under broad initial conditions, including an example shown below. 

D. Closed-loop simulation 

We examined the closed-loop performance of MPCs P0–

P3. For two MPCs, HMPC P0 and the reduced bilevel MPC 

P2, the controlled trajectory and the performance measures are compared in Figs. 5 and 6, respectively. The results of bilevel MPC P1 (with zero initialization) and centralized MPC P3 are omitted, as they were equivalent to the result of 

P2, except for very slight numerical errors. In these figures, the blue and green solid lines represent the results of bilevel MPC (BMPC) P2 and HMPC P0, respectively; the purple broken lines show the upper bounds on the x and z positions; and the red circle and cross are the optimal steady-state position and the fixed target, respectively. In Fig. 5, we can see that the fixed target is outside the feasible region; the optimal steady position is the closest point to the target in the feasible region; and both BMPC and HMPC converged to the optimal position. However, since HMPC handles the upper-level constraint (position bounds) only for the steady-state, it violates the bounds in the transient phase, as seen in Fig. 6 (from 0.8 s to 2.3 s). In contrast, BMPC satisfied the upper-level constraint in the transient phase. In addition, Fig. 6 shows that BMPC converged to the point faster than HMPC, as seen in the steeper decrease of the stage cost around 0.7 s. The times at which they achieved stabilization around the optimum point without constraint violation are approximately 0.8 s for BMPC and 2.3 s for HMPC. The average stage costs were slightly better in BMPC, i.e., 0.892 for BMPC and 0.900 for HMPC, although BMPC must pay extra cost to avoid violating constraints. Regarding the upper-level cost function ℓ(x, u ), HMPC minimizes it only for the steady-state. In contrast, BMPC directly optimizes ℓ(x, u ) as a stage cost in the horizon, yielding the cost advantage in general.                                                                                                                                

> 6Let z:= [x;u],g(z):= [gu(x, u ); gl(x, u )] ,then νcorre-sponds to the Lagrange multiplier for the equality constraint in P′
> 0:min z{ℓ(z)|g(z)≤0, Sz = 0 }, which is equivalent to P0. From its KKT condition, we have ∇zℓ(z) + ∇zg(z)μ+S⊤ν= 0 ⇒Z⊤(∇zℓ(z) +
> ∇zg(z)μ) + ( SZ )⊤ν=0with the Lagrange multipliers μ, ν . From the KKT condition of (8), we also have ∇θℓ(z(θ)) + ∇θg(z(θ)) μ=
> Z⊤(∇zℓ(z) + ∇zg(z)μ) = 0 . Therefore, we have (SZ )⊤ν= 0 .
> 7Define the storage function λ(x) := ν⊤(x−x∗), and let μbe the Lagrange multiplier for g(z)≤0at the solution z=z∗:= [ x∗;u∗]of
> P′
> 0. Due to μ≥0, g (z∗)≤0, and convexity of g(z),(∇zg(z∗)μ)⊤(z−
> z∗)≤μ⊤g(z)≤0∀zs.t. g(z)≤0holds. Considering it with ∇zℓ(z∗)+
> ∇zg(z∗)μ+S⊤ν= 0 , the rotated stage cost ˜ℓ(z) := ℓ(z)−ℓ(z∗) +
> λ(x)−λ(Ax +Bu )satisfies ˜ℓ(z) = 1 /2( ∥x−x∗∥2
> Q+∥u−u∗∥2
> R)−
> (∇zg(z∗)μ)⊤(z−z∗)≥1/2( ∥x−x∗∥2
> Q+∥u−u∗∥2
> R). It yields the strict dissipativity: λ(Ax +Bu )−λ(x)≤˜ℓ(z)−˜ℓ(z∗)−1/2( ∥x−x∗∥2
> Q+
> ∥u−u∗∥2
> R)∀zs.t. g(z)≤0.BMPC HMPC Constraint Optimal steady-state Fixed target

Fig. 5: Controlled trajectories with HMPC P0 (green) and BMPC P2 (blue) for quadrotor example. Although the fixed target (cross) is infeasible, both controllers converge to the optimal steady state (circle); BMPC does so while satisfying the upper-level constraint (dotted lines), whereas HMPC violates it during the transient. BMPC HMPC 

Fig. 6: Performance measures of HMPC P0 (green) and BMPC P2 (blue) for the quadrotor example. BMPC avoids transient constraint violation and converges faster than HMPC, yielding slightly lower average stage cost. The average computation times for each MPC are ( P0)17.7 ms, ( P1) 14300 ms, ( P2) 54.4 ms, and ( P3) 38.3 ms, where P0–P2 include the computation time for the lower-level MPC. This shows that the computational burden for P2

is about the sum of those for P0 and P3; corresponding to Fig. 7: Performance degradation relative to P2 for move-blocked problems (blue circle: P2(M ), green circle: P1(M )) and HMPC P0 (red circle) in the quadrotor example, to-gether with the proposed upper bounds; the overall trends are similar to those in Fig. 4. In addition, the results for 

P1(M ) do not always satisfy the expected monotonicity 

V1(Mi+1 ) ≤ V1(Mi) or the dominance relation V1(Mi) ≤

V2(Mi), reflecting the nonconvexity of P1(M ) and further motivating the use of P2(M ) for reliable control design. the fact that the size of the upper-level problems in P0 is significantly smaller compared to the lower-level problems, and the complexity of the upper-level problems in P2 is nearly equivalent to that of P3. Since the performance of 

P1–P3 is equivalent, the most computationally efficient choice is naturally P3, when hierarchical control is not required. When hierarchical control is desirable, BMPC P1

incurs an unrealistic computational cost, necessitating the selection of HMPC P0 in most past cases. The above results demonstrated that using the proposed MPC P2 allows BMPC to be implemented with a sufficiently fast computation while achieving equivalent performance to P1 and better reliability than P1.

E. Performance degradation and its bounds 

Next, we also consider the move-blocked problems 

P1(M ) and P2(M ), and verify the results on performance ordering and bounds via single optimization results at the initial condition x0 = 0 of the above problem. We again used move-blocking matrices Mi (16) that make the first i

steps in the horizon free and the last (N − i) steps blocked. We also prepared a low-rank move-blocking matrix M ⋆ by generating 50 initial states that have different positions with 

x0 and giving it to Algorithm 1. The obtained M ⋆ had 

rank( M ⋆) = 16 that was equivalent to rank( M4).Fig. 7 shows the degradation ratio of the performance of P0, P1(Mi) and P2(Mi) ( i = 1 , .., 30 ) compared to 

P2. The blue, magenta, and red bullets represent the actual performance degradations V2(Mi) − V2, V 1(Mi) − V2 (i =1, ..., 30) and V0 − V2, respectively. The bars represent the lower and upper bounds, i.e., the blue and red ones show 

0 ≤ V2(Mi) − V2 ≤ ∆( T Φ⋆), 0 ≤ V0 − V2 ≤ ∆(Θ ⋆),respectively. The cross markers represent the tighter bounds, i.e., the blue and red ones show ∆ε(T Φ⋆) and ∆ε(Θ ⋆) with 

ε = 0 .5. The green line represents V2(M ⋆) − V2.Since the initial condition x0 = 0 is far from the border of the upper-level position constraint, the result of P0 satisfies 

Gu(U0; x0) ≤ 0. The figure shows V0 ≥ V1(M1) as anticipated from Theorem 3-3, under Gu(U0; x0) ≤ 0. In contrast, V0 ≥ V2(M1) did not hold in this case because some of the lower-level constraints were active; therefore, 

∇U Fl(U0; Θ 0, x 0)̸ = 0. However, the result shows that 

P2(M ) recovers the performance with small refinement of 

M , i.e., V0 ≥ V2(Mi) ( i ≥ 3) and V0 ≥ V2(M ⋆) hold. It is notable that V2(Mi) − V2 decreases drastically with 

i = 3 and the proposed bounds adequately estimate that the performance degradation is small with i ≥ 3. Therefore, when we consider the workflow to decide Mi by increasing 

i in order, we will find a well-balanced Mi at i = 3 by checking the proposed bounds. The pre-determination of 

M ⋆ is also effective in achieving a good balance without iterative computation of P2(Mi). The result also shows that the monotonicity V2(Mi) ≥ V2(Mi+1 ) holds for any i ∈

[1 , N −1] as anticipated in Theorem 3-2, however, V1(Mi) ≥

V1(Mi+1 ) and V2(Mi) ≥ V1(Mi) did not hold for some i,e.g., the figure shows V1(M10 ) ≤ V1(M11 ), V2(M11 ) ≤

V1(M11 ), etc. These inconsistencies occurred due to the nonconvexity of P1(M ), which motivates us to employ 

P2(M ) for reliable control design.