---
title: "Not All Prompts Are Equal: Exploration-Guided Prompt Scaffolding for Multimodal Reinforcement Post-Training"
title_zh: 并非所有提示词生而平等：用于多模态强化后训练的探索引导式提示词脚手架
authors: "Yuanhao Yue, Qianli Ma, Chengyu Wang, Haoting Wang, Lei Shen, Jun Huang"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15051v1"
tags: ["query:bopl"]
score: 8.0
evidence: 自动提示词脚手架与分布优化
tldr: 针对多模态大模型（MLLM）强化学习训练中提示词信息量不均的问题，本文提出了一种探索引导的提示词脚手架框架。该方法通过计算探索潜力分数（EPS）动态评估提示词效用，并利用教师模型对低效提示词进行重写优化，从而在保持任务意图的同时提升训练信号质量，显著增强了模型在多模态推理任务中的表现。
selection_source: fresh_fetch
motivation: 现有强化学习训练中对所有提示词分配相同的计算资源，忽略了提示词在学习阶段信息量差异巨大的问题。
method: 引入探索潜力分数（EPS）动态评估提示词效用，并利用教师模型对低效提示词进行重写以增强训练信息量。
result: "在Geo3K和MMK12等基准测试中，该方法实现了显著性能提升，在MathVision和MMMU-Pro上分别取得了11.5%和11.1%的增益。"
conclusion: 通过动态调整提示词分布和内容，该框架有效解决了多模态强化学习中的训练效率瓶颈，实现了更优的泛化能力。
---

## 摘要
在线强化学习（RL）中的训练提示词在为当前策略提供信息量方面存在显著差异：有些提示词已达到饱和，而另一些则因难度过高难以产生可靠的学习信号，但在标准训练中，两者却被分配了相同的采样预算。我们提出了一种探索引导式提示词脚手架框架，该框架在多模态大语言模型（MLLMs）的强化后训练过程中动态调整训练提示词的分布。我们方法的核心是“探索潜力分数”（Exploration Potential Score, EPS），这是一种基于 KL 正则化策略改进理论的轻量级采样代理指标，可直接从在线采样统计数据中计算得出，无需额外开销。我们并未直接丢弃低效提示词，而是利用教师模型生成脚手架重写版本，在保留原始任务意图的同时，使后续训练更具信息量，从而将教师监督重新定义为训练数据的精炼，而非简单的输出模仿。将该方法与 GRPO 集成并在 Geo3K 和 MMK12 数据集上进行测试，结果表明我们的方法在域内和域外基准测试中均持续优于基线模型，在域内任务中实现了高达 9.7% 的相对提升，并在 MathVision 和 MMMU-Pro 上分别取得了 11.5% 和 11.1% 的性能增益。

## Abstract
Training prompts in online reinforcement learning (RL) differ substantially in how informative they are for the current policy: some are already saturated while others are too difficult to yield reliable learning signals, yet both receive equal rollout budget under standard training. We propose an exploration-guided prompt scaffolding framework that adapts the training prompt distribution dynamically throughout RL post-training of multimodal large language models (MLLMs). Central to our approach is the $\textit{Exploration Potential Score} (EPS)$, a lightweight rollout-based proxy for prompt utility derived from KL-regularized policy improvement theory, computable directly from on-policy rollout statistics without additional overhead. Rather than discarding low-utility prompts, we use a teacher model to generate scaffolded rewrites that preserve the original task intent while making subsequent training more informative, reframing teacher supervision as training-data refinement rather than output imitation. Integrated with GRPO on Geo3K and MMK12, our method consistently outperforms the baseline on both in-domain and out-of-distribution benchmarks, achieving up to 9.7\% relative improvement in-domain and gains of 11.5\% on MathVision and 11.1\% on MMMU-Pro.

---

## 论文详细总结（自动生成）

这是一份关于论文《Not All Prompts Are Equal: Exploration-Guided Prompt Scaffolding for Multimodal Reinforcement Post-Training》的结构化总结：

### 1. 核心问题与研究动机
在多模态大语言模型（MLLM）的在线强化学习（RL）后训练中，现有的训练范式通常将所有提示词（Prompts）视为同等重要，并分配相同的采样预算。然而，论文指出这种假设是不成立的：
*   **饱和提示词**：模型已掌握，无法提供额外的梯度信息。
*   **困难提示词**：难度过高，导致模型产生大量低质量采样，无法提供有效的学习信号。
*   **研究动机**：如何动态识别并优化提示词的“探索潜力”，从而提高强化学习的训练效率和模型推理能力。

### 2. 方法论：探索引导式提示词脚手架
论文提出了一种三阶段循环框架，核心在于将提示词视为动态优化的对象：
*   **探索潜力分数 (EPS)**：基于 KL 正则化策略改进理论，通过在线采样统计数据（Rollout statistics）计算。它衡量了当前策略在特定提示词下进一步改进的潜力。公式上，它通过对高奖励采样进行 Softmax 加权，估算局部改进空间。
*   **过滤与重写**：设定阈值 $\tau=0$，将 EPS 低于阈值的提示词识别为“低效提示词”。
*   **教师引导的脚手架 (Scaffolding)**：利用教师模型（如 Qwen-VL-Max）根据学生的采样反馈，将原始提示词重写为包含引导性提示（Scaffold）的版本。这些脚手架旨在通过分解子目标或纠正推理路径来降低学习难度，而非直接给出答案。
*   **动态池管理**：重写后的提示词被重新注入训练池，形成一个持续进化的数据飞轮。

### 3. 实验设计
*   **数据集**：使用 Geometry3K 和 MMK12 作为强化学习训练集。
*   **基准测试 (Benchmarks)**：包括域内测试（Geo3K, MMK12）以及域外泛化测试（MathVerse, MathVision, MMMU, MMMU-Pro）。
*   **对比方法**：主要对比标准 GRPO（Group Relative Policy Optimization）基线，即在不进行提示词筛选和重写的情况下的训练效果。

### 4. 资源与算力
*   **硬件配置**：实验在 8 张 GPU 上进行，使用 FSDP（Fully Sharded Data Parallel）进行分布式训练。
*   **模型规模**：使用了 Qwen3-VL-2B 和 Qwen3-VL-4B 作为基础模型。
*   **训练规模**：RL 训练步数为 3,000 步，全局批次大小为 16。

### 5. 实验数量与充分性
*   **实验覆盖**：论文进行了多维度实验，包括不同模型规模（2B/4B）、不同训练数据集（Geo3K/MMK12）以及多个跨域基准测试。
*   **消融分析**：通过对比不同 EPS 阈值下的训练表现，验证了 EPS 作为提示词效用指标的有效性。
*   **客观性**：实验设计较为严谨，通过对比“仅高 EPS”、“仅低 EPS”和“全量池”的训练表现，客观展示了该方法的增益。

### 6. 主要结论与发现
*   **性能提升**：该方法在所有测试中均优于 GRPO 基线，域内任务最高提升 9.7%，在 MathVision 和 MMMU-Pro 上分别取得了 11.5% 和 11.1% 的显著增益。
*   **泛化能力**：证明了通过脚手架重写提示词不仅能优化域内表现，还能显著增强模型在未见过的推理任务上的泛化能力。
*   **数据飞轮效应**：随着训练进行，提示词池的质量不断提升，从而产生更具信息量的梯度更新。

### 7. 优点
*   **创新性**：将“提示词”从静态数据转变为动态可优化的资源，将教师监督从“输出模仿”转向“数据精炼”。
*   **轻量化**：EPS 计算直接利用 GRPO 现有的采样统计数据，无需额外的辅助模型或额外的 Rollout 开销。
*   **异步架构**：重写过程与主训练循环异步进行，最大限度减少了对训练吞吐量的影响。

### 8. 不足与局限
*   **教师依赖**：目前的脚手架生成是“答案感知型”（Answer-aware），即教师模型在生成提示时已知晓参考答案，这在实际应用中可能存在局限。
*   **EPS 的局限性**：EPS 仅为一种基于采样的代理指标，存在有限样本噪声，可能无法完美覆盖所有复杂场景。
*   **任务局限**：目前主要针对数学和视觉推理任务（具有可验证的奖励函数），对于非结构化或难以验证的任务，该方法的适用性有待进一步研究。

（完）
