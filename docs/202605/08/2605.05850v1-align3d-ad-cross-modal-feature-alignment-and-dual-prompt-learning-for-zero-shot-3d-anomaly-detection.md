---
title: "Align3D-AD: Cross-Modal Feature Alignment and Dual-Prompt Learning for Zero-shot 3D Anomaly Detection"
title_zh: Align3D-AD：用于零样本 3D 异常检测的跨模态特征对齐与双提示学习
authors: "Letian Bai, Xuanming Cao, Juan Du, Chengyu Tao"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05850v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于零样本检测的双重提示学习
tldr: 针对零样本3D异常检测中投影表示与预训练编码器间的领域鸿沟问题，本文提出Align3D-AD框架。该框架通过跨模态特征对齐将渲染特征映射至RGB语义空间，并引入语义一致性重加权策略。同时，利用双提示对比学习捕捉跨模态互补语义。实验证明，该方法在多个主流数据集上显著优于现有零样本方法，展现了极强的泛化性。
selection_source: fresh_fetch
motivation: 现有方法将3D观测投影为多视图表示时，与预训练RGB编码器之间存在显著的领域鸿沟，且缺乏真实的视觉语义。
method: 提出一种包含跨模态特征对齐（结合语义一致性重加权）和双提示对比学习的统一两阶段框架。
result: 在MVTec3D-AD、Eyecandies和Real3D-AD等数据集上的实验结果表明，该方法在多种设置下均优于现有零样本检测方法。
conclusion: Align3D-AD通过利用辅助RGB模态的跨模态引导，有效提升了零样本3D异常检测的判别能力和泛化性能。
---

## 摘要
零样本 3D 异常检测旨在不获取目标类别训练数据的情况下识别异常。然而，现有方法主要依赖于将 3D 观测投影为多视图表示，这些表示主要捕捉几何线索而非真实的视觉语义，并使用在 RGB 数据上预训练的视觉编码器进行处理，导致编码器与投影表示之间存在显著的领域差异（domain gap）。为解决这一问题，我们提出了 Align3D-AD，这是一个统一的两阶段框架，利用辅助类别的 RGB 模态作为跨模态引导，用于零样本 3D 异常检测。首先，我们引入了一种跨模态特征对齐范式，将渲染特征映射到 RGB 语义空间中。与以往隐式依赖预训练编码器的工作不同，我们的方法实现了从 RGB 观测中直接进行语义迁移。此外，我们还引入了一种语义一致性重加权策略，通过根据整体语义一致性对局部区域进行重加权，来优化特征对齐。其次，我们提出了一种具有双提示对比对齐的模态感知提示学习框架。通过为 RGB 对齐特征和渲染特征分配独立的提示，我们的方法捕捉了跨模态的互补语义，而对比对齐进一步增强了提示表示以提高辨别力。在 MVTec3D-AD、Eyecandies 和 Real3D-AD 上的大量实验表明，Align3D-AD 在 one-vs-rest 和跨数据集设置下均持续优于现有的零样本方法，突显了其泛化能力和鲁棒性。代码和数据集将在论文被接收后公开。

## Abstract
Zero-shot 3D anomaly detection aims to identify anomalies without access to training data from target categories. However, existing methods mainly rely on projecting 3D observations into multi-view representations that primarily capture geometric cues rather than realistic visual semantics and process them with vision encoders pretrained on RGB data, leading to a significant domain gap between the encoder and the projected representations. To address this issue, we propose Align3D-AD, a unified two-stage framework that leverages the RGB modality from auxiliary categories as cross-modal guidance for zero-shot 3D anomaly detection. First, we introduce a cross-modal feature alignment paradigm that maps rendering features into the RGB semantic space. Unlike prior works that implicitly rely on pretrained encoders, our method enables direct semantic transfer from RGB observations. A semantic consistency reweighting strategy is further introduced to refine feature alignment by reweighting local regions according to holistic semantic consistency. Second, we propose a modality-aware prompt learning framework with dual-prompt contrastive alignment. By assigning independent prompts to RGB-aligned and rendering features, our method captures complementary semantics across modalities, while the contrastive alignment further enhances prompt representations to improve discriminability. Extensive experiments on MVTec3D-AD, Eyecandies, and Real3D-AD demonstrate that Align3D-AD consistently outperforms existing zero-shot methods under both one-vs-rest and cross-dataset settings, highlighting its generalization capability and robustness. Code and the dataset will be made available once our paper is accepted.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《Align3D-AD: Cross-Modal Feature Alignment and Dual-Prompt Learning for Zero-shot 3D Anomaly Detection》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的零样本（Zero-shot）3D 异常检测方法通常将 3D 点云投影为多视图 2D 渲染图，并利用预训练的视觉语言模型（如 CLIP）进行检测。然而，**渲染图仅包含抽象的几何信息，缺乏丰富的视觉语义**，而 CLIP 的视觉编码器是在 RGB 图像上预训练的。这导致了渲染表示与预训练编码器之间存在显著的**领域鸿沟（Domain Gap）**，限制了模型提取深层判别特征的能力。
*   **研究动机**：利用辅助类别的 RGB 模态作为训练时的引导，将 3D 渲染特征对齐到 RGB 语义空间，从而在推理阶段（仅使用 3D 数据）也能获得具备语义信息的特征表示。

### 2. 方法论
论文提出了一个名为 **Align3D-AD** 的统一两阶段框架：
*   **第一阶段：跨模态特征对齐（Cross-Modal Feature Alignment）**
    *   **特征对齐器**：使用可学习的多层感知机（MLP）将渲染特征映射到 RGB 特征空间，通过余弦相似度损失（全局和局部）强制两者一致。
    *   **语义一致性重加权（SCR）**：提出一种自适应策略，计算渲染补丁与所有 RGB 补丁的全局语义一致性。对于跨模态差异较大的区域（通常包含更复杂的几何/语义信息）赋予更高权重，以实现更稳健的对齐。
*   **第二阶段：模态感知提示学习（Modality-aware Prompt Learning）**
    *   **双提示学习**：为“RGB 对齐分支”和“原始渲染分支”分别设置独立的学习提示词（Normal/Anomalous），以捕捉互补的模态特定语义。
    *   **双提示对比对齐（DpCA）**：在训练后期引入对比损失，增强提示词在语义空间中的判别力，同时保持跨模态的一致性。
*   **推理阶段**：将两个分支的预测结果进行线性融合，生成最终的 3D 异常得分图和图像级分类结果。

### 3. 实验设计
*   **数据集**：
    *   **MVTec3D-AD**：高分辨率工业 3D 扫描。
    *   **Eyecandies**：合成的多种类异常检测数据集。
    *   **Real3D-AD**：真实的工业点云异常检测数据集（不含 RGB，用于测试泛化性）。
*   **Benchmark 与对比方法**：
    *   对比了 CLIP、AA-CLIP、MVP-PCLP、GS-CLIP、PointAD 和 ZUMA 等最先进的零样本方法。
*   **评估设置**：
    *   **One-vs-rest**：使用一个类别作为辅助训练，其余类别测试。
    *   **Cross-dataset**：在一个数据集上训练，在完全不同的数据集上测试。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX 4090 GPU** 上运行。
*   **训练细节**：
    *   第一阶段（对齐）：250 个 epoch（最后 50 个 epoch 开启 SCR）。
    *   第二阶段（提示学习）：15 个 epoch（最后 5 个 epoch 开启 DpCA）。
    *   学习率设为 $1 \times 10^{-3}$，使用 Adam 优化器。
*   **效率**：推理速度约为 5.30 FPS，显存占用约 2791 MB，优于 GS-CLIP，略逊于 PointAD。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流数据集，进行了大量的定量对比（AUROC, AP, PRO 指标）。
*   **消融实验**：详细验证了 SCR、DpCA、双分支融合、提示长度、视图数量等关键组件的有效性。
*   **客观性**：所有实验重复三次取平均值；采用了跨数据集验证，证明了模型并非仅仅记住了特定分布，具有较强的泛化能力。

### 6. 主要结论与发现
*   **性能领先**：Align3D-AD 在 MVTec3D-AD 和 Eyecandies 上的表现均优于现有 3D 零样本方法。
*   **跨模态互补性**：实验证明 RGB 对齐特征和原始渲染特征具有互补性。渲染特征擅长捕捉几何结构，而对齐后的特征在语义边界定位上更精准。
*   **泛化性强**：即使在完全没有 RGB 信息的 Real3D-AD 数据集上，通过在其他数据集上学到的对齐能力，模型依然取得了 SOTA 性能。

### 7. 优点（亮点）
*   **创新的对齐视角**：不同于以往只关注 3D 几何的方法，该方法通过“训练时 RGB 引导”成功弥补了视觉编码器的领域鸿沟。
*   **两阶段策略**：将特征对齐与任务特定的提示学习解耦，使模型训练更加稳定。
*   **重加权机制**：SCR 策略能够自动关注几何复杂的区域，提高了特征对齐的质量。

### 8. 不足与局限
*   **模型复杂度**：引入了额外的 RGB 引导分支和多个 MLP 对齐器，增加了训练负担和模型参数量。
*   **渲染依赖性**：模型性能高度依赖于 3D 到 2D 投影的质量。如果点云本身存在大量噪声或缺失（如文中 Failure Cases 所示），渲染图的缺陷会直接导致对齐失败。
*   **超参数敏感性**：如融合系数 $\alpha$ 和对比损失权重 $\lambda_{con}$ 可能需要针对不同数据集进行微调以达到最优效果。

（完）
