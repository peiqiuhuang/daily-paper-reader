---
title: "SAMPLe: SAM-based Optimizer for Prompt Learning in VLMs"
title_zh: SAMPLe：基于 SAM 的视觉语言模型提示学习优化器
authors: "Hossein Rajoli, Fatemeh Lotfi, Niloufar Alipour Talemi, Hossein Kashiani, Xiaolong Ma, Fatemeh Afghah"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05727v1"
tags: ["query:bopl"]
score: 8.0
evidence: 视觉语言模型提示学习的优化器
tldr: 预训练视觉语言模型在提示学习中常面临性能与泛化性的权衡难题，即在已知分布上微调容易导致对未知数据的泛化能力下降。本文提出 SAMPLe，一种基于锐度感知最小化（SAM）的即插即用优化器。该方法通过考虑损失景观的锐度，动态调整优化状态以平衡探索与利用，从而减少过拟合。实验证明，SAMPLe 能无缝集成至 CoOp、MaPLe 等多种框架，在提升特定任务表现的同时，显著增强了模型对未见数据的泛化能力。
selection_source: fresh_fetch
motivation: 解决视觉语言模型在提示学习中，针对特定任务微调导致的过拟合以及在未见数据上泛化能力下降的性能-泛化权衡难题。
method: 提出一种名为 SAMPLe 的锐度感知优化器，通过动态适应局部曲率和梯度属性来平滑损失景观，从而增强提示的泛化性。
result: 在 CoOp、MaPLe 等多种提示学习框架上的实验表明，SAMPLe 在不同设置下均能显著提升模型在分布内外的性能。
conclusion: SAMPLe 是一种鲁棒且模型无关的优化方案，有效缓解了提示学习中的过拟合问题，提升了预训练视觉语言模型的适应能力。
---

## 摘要
预训练视觉语言模型（VLM）如 CLIP 已被证明是各种下游应用中非常有效的基础模型。然而，VLM 中的提示学习面临着性能与泛化能力的困境：虽然可以通过调整提示在已知分布上获得高准确率，但这一微调过程往往会损害其对未知数据的泛化能力。有限的可学习提示集在预训练 VLM 中对输入进行上下文处理和条件约束，以引导其完成任务，但这些提示往往会过拟合训练数据，导致任务特定性能与保持泛化能力之间存在权衡。为了解决这一困境，我们引入了 SAMPLe（锐度感知最小化提示学习），这是一种插件式的锐度感知优化器，通过考虑损失景观的锐度来增强提示的泛化能力。与传统方法不同，SAMPLe 通过在每一步满足目标函数约束来平衡探索与利用，并根据局部曲率和梯度特性动态适应当前的优化状态。这种方法减少了在已知分布上的过拟合，并提高了对未知数据的适应性，从而保留了预训练 VLM 模型的泛化潜力。我们将 SAMPLe 集成到多个提示学习框架中，包括 CoOp、CoCoOp、MaPLe、TCP 和 Co-Prompt，展示了其在不同方法中的有效性。实验表明，SAMPLe 提升了提示学习框架的性能，并在各种设置下始终优于现有的优化器，证明了其作为一种鲁棒且模型无关的提示学习解决方案的地位。

## Abstract
Pre-trained Vision-Language Models (VLMs) like CLIP have proven highly effective as foundation models for various downstream applications. However, prompt learning in VLMs encounters a performance-generalization dilemma: while prompts can be tuned to achieve high accuracy on seen distributions, this tuning process often undermines their generalizability to unseen data. The limited set of learnable prompts, which contextualize and condition the input to steer it toward the task within the pretrained VLM, tends to overfit the training data, leading to a trade-off between task-specific performance and preserving generalization. To address this dilemma, we introduce SAMPLe (Sharpness-Aware Minimization Prompt Learning), a plug-in sharpness-aware optimizer that enhances prompt generalizability by accounting for loss landscape sharpness. Unlike conventional methods, SAMPLe balances exploration and exploitation by satisfying objective function constraints at each step, dynamically adapting to the current optimization state based on the local curvature and gradient properties. This approach reduces overfitting on seen distributions and improves adaptability to unseen data, preserving the generalization potential of pre-trained VLM models. We integrate SAMPLe into multiple prompt learning frameworks, including CoOp, CoCoOp, MaPLe, TCP, and Co-Prompt, demonstrating its effectiveness across diverse methods. Experiments show that SAMPLe elevates prompt learning frameworks and consistently outperforms existing optimizers across diverse settings, establishing itself as a robust, model-agnostic solution for prompt learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《SAMPLe: SAM-based Optimizer for Prompt Learning in VLMs》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于视觉语言模型（VLM，如 CLIP）在**提示学习（Prompt Learning）**中的核心矛盾：**性能与泛化性的权衡困境**。
*   **研究动机**：虽然通过微调少量的提示参数可以在特定任务（已知分布）上获得极高准确率，但由于提示参数空间极小，模型极易陷入损失景观（Loss Landscape）中“锐利”的局部极小值（Sharp Minima）。
*   **核心问题**：这种对训练数据的过拟合会导致模型在未见过的类别或分布（Unseen Data）上泛化能力大幅下降。论文旨在引入“锐度感知”机制，引导模型寻找更平坦的极小值区域，从而同时提升任务准确率和跨域泛化性。

### 2. 方法论：SAMPLe 优化器
SAMPLe（Sharpness-Aware Minimization Prompt Learning）是一种基于 SAM 的改进型优化器，其核心思想是**动态平衡“利用”（低训练损失）与“探索”（平坦景观）**。

*   **核心思想**：在每一步迭代中，不仅最小化当前的经验风险（ERM），还通过扰动点来感知周围景观的平坦度。
*   **关键技术细节**：
    1.  **双重目标优化**：目标函数结合了原始损失和扰动后的损失，确保找到既低损又平坦的解。
    2.  **梯度对齐与正交约束**：SAMPLe 强制要求扰动点的梯度更新与原始梯度方向保持一致（确保稳定性），同时约束更新方向与“全批次梯度”（Full-batch gradient）的近似值正交。
    3.  **动态适应性**：利用指数移动平均（EMA）来近似全批次梯度，根据当前的优化状态（局部曲率和梯度属性）自动调整探索与利用的比例，无需像前人工作（如 GCSCoOP）那样依赖复杂的启发式超参数。
*   **算法流程**：计算当前梯度 -> 更新全批次梯度近似值 -> 计算 SAM 扰动 -> 根据正交约束调整梯度 -> 更新参数。

### 3. 实验设计
*   **数据集**：使用了 11 个主流图像分类数据集（ImageNet, Caltech101, OxfordPets, StanfordCars, Flowers102, Food101, FGVCAircraft, SUN397, DTD, EuroSAT, UCF101）。此外，还使用了 4 个 ImageNet 变体（V2, Sketch, A, R）进行跨域泛化测试。
*   **Benchmark（基准框架）**：将 SAMPLe 集成到 5 种主流提示学习方法中：**CoOp, CoCoOp, MaPLe, TCP, Co-Prompt**。
*   **对比方法**：
    *   基础优化器：SGD。
    *   锐度感知优化器：**SAM, F-SAM, SAGM**。
*   **实验场景**：
    1.  **Base-to-New Generalization**：在基础类训练，在基础类和新类上测试。
    2.  **Cross-Dataset Zero-Shot**：在 ImageNet 训练，在其他 10 个数据集直接测试。
    3.  **Cross-Domain Generalization**：在 ImageNet 训练，在不同分布的 ImageNet 变体上测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的硬件型号（如 GPU 数量、型号）及总训练时长。
*   **实现细节**：提到遵循了原始框架（如 CoOp, MaPLe）的超参数设置，包括学习率调度、Backbone 架构等。由于 SAM 类方法通常涉及两次前向/后向传播，可以推断其计算开销约为标准 SGD 的 2 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 15 个数据集的广泛测试。
    *   在 5 种不同的模型架构上验证了通用性。
    *   进行了详细的消融实验（Term-wise ablation），验证了目标函数中每一项的有效性。
    *   提供了损失景观的可视化对比（图 1）。
*   **充分性评价**：实验设计非常**充分且客观**。通过在多个框架上的一致性提升，证明了 SAMPLe 是模型无关（Model-agnostic）的。使用了调和平均值（HM）作为主要指标，公平地衡量了性能与泛化性的平衡。

### 6. 主要结论与发现
*   **打破困境**：SAMPLe 显著提升了“新类”（New classes）的准确率，同时没有显著牺牲“基础类”（Base classes）的性能。
*   **全面领先**：在所有 5 种提示学习框架中，SAMPLe 的表现均优于传统的 SAM、F-SAM 和 SAGM 优化器。
*   **鲁棒性**：SAMPLe 对扰动半径（$\rho$）和梯度平滑因子（$\lambda$）等超参数表现出更强的鲁棒性（见图 3）。
*   **理论保证**：论文在附录中给出了收敛性证明，证明其收敛速度与 SGD/Adam 相当。

### 7. 优点
*   **即插即用**：作为优化器层面的改进，无需修改模型架构即可提升现有提示学习方法的性能。
*   **动态平衡**：通过正交约束解决了 SAM 在小参数空间（提示学习）中容易过平滑或不稳定的问题。
*   **泛化能力强**：在跨数据集和跨域任务中表现优异，证明了其寻找的“平坦极小值”确实具有更好的迁移性。

### 8. 不足与局限
*   **计算成本**：虽然论文未详述，但 SAM 类算法通常需要双倍的梯度计算量，这在超大规模训练中可能是一个考虑因素。
*   **超参数依赖**：尽管比前人方法更鲁棒，但仍引入了 $\alpha$ 和 $\rho$ 等超参数，在极端不同的任务中可能仍需微调。
*   **应用范围**：目前仅在视觉语言模型的提示学习上进行了验证，尚未探讨其在纯文本大模型（LLM）或其他微调技术（如 LoRA）中的表现。

（完）
