---
title: Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation
title_zh: 通过符号蒸馏获得的可解释解析卡拉比-丘度量
authors: D Yang Eng
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07834v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用符号回归从神经网络中提取可解释公式
tldr: 本研究针对弦理论中难以计算的卡拉比-丘流形度规，提出了一种基于符号回归的蒸馏方法。通过将复杂的神经网络近似模型转化为简洁、可解释的数学公式，研究者仅用五个项就达到了与神经网络相当的精度（R²=0.9994），且参数量减少了3000倍。该公式不仅能跨模空间保持稳定，还能准确复现体积积分和汤川耦合等物理观测值，为复杂几何量的解析表达提供了新途径。
selection_source: fresh_fetch
motivation: 旨在解决卡拉比-丘流形度规计算困难且神经网络模型缺乏可解释性的问题。
method: 利用符号回归技术从预训练的神经网络教师模型中提取简洁的解析表达式。
result: 得到的五项公式在极低参数量下实现了极高的拟合精度，并成功预测了关键物理量。
conclusion: 符号蒸馏能够将黑盒模型转化为紧凑、可解释的解析模型，为物理和几何研究提供了有效的经验假设。
---

## 摘要
卡拉比-丘流形对弦理论至关重要，但其度量的计算往往难以处理。在本文中，我们展示了符号回归可以将神经网络近似蒸馏为简单且可解释的公式。我们的五项表达式在参数量减少 3000 倍的情况下，达到了与神经网络相当的精度（$R^2 = 0.9994$）。多种子验证确认了几何约束会筛选出基本特征（特别是幂和与对称多项式），同时允许结构多样性。在所研究的模参数范围（$ψ \in [0, 0.8]$）内，该函数形式可以保持不变，且系数平滑变化；我们将这些趋势解释为在局部训练的教师模型精度范围内（当 $ψ \neq 0$ 时，$σ \approx 8-9\%$）的经验假设。该公式重现了物理可观测物理量——体积积分和汤川耦合——验证了符号蒸馏能够为此前仅能通过黑盒网络获取的物理量恢复出紧凑、可解释的模型。

## Abstract
Calabi--Yau manifolds are essential for string theory but require computing intractable metrics. Here we show that symbolic regression can distill neural approximations into simple, interpretable formulas. Our five-term expression matches neural accuracy ($R^2 = 0.9994$) with 3,000-fold fewer parameters. Multi-seed validation confirms that geometric constraints select essential features, specifically power sums and symmetric polynomials, while permitting structural diversity. The functional form can be maintained across the studied moduli range ($ψ\in [0, 0.8]$) with coefficients varying smoothly; we interpret these trends as empirical hypotheses within the accuracy regime of the locally-trained teachers ($σ\approx 8-9\%$ at $ψ\neq 0$). The formula reproduces physical observables -- volume integrals and Yukawa couplings -- validating that symbolic distillation recovers compact, interpretable models for quantities previously accessible only to black-box networks.

---

## 论文详细总结（自动生成）

以下是对论文《Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：卡拉比-丘（Calabi-Yau, CY）流形的里奇平坦度量（Ricci-flat metrics）是弦理论计算物理观测值（如汤川耦合、模空间几何）的基础，但其定义的 Monge-Ampère 方程在数学上难以精确求解。
*   **研究动机**：
    *   **传统方法局限**：数值求解极其耗时（每个点需数百 CPU 小时）。
    *   **神经网络（NN）局限**：虽然 NN 代理模型评估速度快，但属于“黑盒”，缺乏物理直觉和可解释性。
    *   **科学 AI 的需求**：不仅需要预测，更需要恢复产生复杂现象的底层生成机制。本研究旨在探索是否能找到既紧凑、可解释，又能保持神经网络级精度的解析公式。

### 2. 方法论
*   **核心思想：符号蒸馏（Symbolic Distillation）**。利用高精度的神经网络作为“教师”，通过符号回归（Symbolic Regression）从中提取简洁的数学表达式作为“学生”。
*   **关键技术细节**：
    *   **教师模型**：使用 Donaldson 算法训练的 $k=10$ 阶代数度量（H-矩阵），在费马点处里奇误差 $\sigma < 1\%$。
    *   **特征工程**：构造了两个规范不变的几何不变量：幂和 $p_2 = \sum |z_i|^4$ 和第三初等对称多项式 $\sigma_3$。
    *   **回归目标**：学习从几何不变量到度量行列式比值 $\log(\det g_{alg} / \det g_{FS})$ 的映射。
    *   **符号回归工具**：使用 PySR 库，通过帕累托准则（Pareto criterion）平衡公式复杂度和拟合损失。
*   **核心公式结构**：发现了一个五项解析式，包含常数项、奇异修正项（如 $1/p_2^2$）、体项（Bulk term）和角各向异性修正项。
*   **分解假设（Factorization Principle）**：提出度量行列式比值可以分解为“模无关的函数骨架”与“随模参数 $\psi$ 平滑变化的系数”之积。

### 3. 实验设计
*   **数据集/场景**：研究了 Dwork 家族的五次超曲面（Quintic Hypersurfaces），模参数范围 $\psi \in [0, 0.8]$。
*   **Benchmark（基准）**：
    *   **教师模型**：Donaldson H-矩阵（875个参数）。
    *   **对比方法**：3阶多项式拟合、Ashmore 等人提出的神经网络代理模型（15,000个参数）。
*   **物理验证**：通过计算体积积分（Volume Integrals）和汤川耦合（Yukawa couplings）来验证公式的物理保真度。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA A100 GPU（Google Colab Pro 环境）。
*   **训练细节**：
    *   符号回归运行 200 次迭代，种群大小 60。
    *   每个模参数点采样 $10^5$ 个点进行训练。
    *   H-矩阵优化使用 Adam 优化器，运行 15 个迭代步。
*   **效率提升**：符号公式的评估速度比 H-矩阵快 10,000 倍，比神经网络快 3,000 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 $\psi = 0$（费马点）进行了 10 个独立随机种子的集成验证，以确认特征发现的稳健性。
    *   在 $\psi \in \{0.0, 0.2, 0.4, 0.6, 0.8\}$ 多个点进行了跨模空间拟合。
    *   进行了特征消融实验（证明了 $\sigma_3$ 的必要性）。
    *   进行了跨多项式阶数（$k=6, 8, 10$）的曲率级收敛验证。
*   **充分性评价**：实验设计较为全面，涵盖了统计显著性测试（置换检验）、残差分析和物理基准测试，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **极度压缩**：五项符号公式实现了 $R^2 = 0.9994$ 的精度，参数量比神经网络减少了 3,000 倍。
*   **结构稳定性**：在研究的模空间范围内，函数形式保持不变，几何变形完全由平滑变化的系数编码。
*   **层次化调制**：系数随 $\psi$ 的变化呈现层次化特征，奇异项出现符号反转，而对称项单调增强。
*   **物理一致性**：成功复现了汤川耦合（误差 < 0.001%）和体积积分（误差约 2%），证明了符号模型在物理扫描中的实用性。

### 7. 优点
*   **高可解释性**：将黑盒模型转化为具有明确几何意义的项（如径向各向异性、角结构）。
*   **发现奇异结构**：符号回归自动发现了多项式拟合无法捕捉的奇异项（$1/p_2^n$），这与轨道折叠（Orbifold）渐近几何的预期一致。
*   **计算效率极高**：极大地降低了在模空间中进行大规模参数扫描的计算成本。

### 8. 不足与局限
*   **教师噪声限制**：在 $\psi \neq 0$ 时，教师模型的里奇误差较高（8-9%），这限制了对系数轨迹进行更精确几何推导的能力。
*   **范围局限**：目前仅验证了 Dwork 五次超曲面家族，尚未推广到其他复杂的卡拉比-丘流形或更高维的模空间。
*   **全局误差累积**：体积积分的误差（5.5%）高于局部里奇误差，反映了蒙特卡洛积分和行列式近似在全局上的误差累积。
*   **经验性而非证明**：公式的模空间稳定性是基于实验观察的经验假设，而非严格的数学证明。

（完）
