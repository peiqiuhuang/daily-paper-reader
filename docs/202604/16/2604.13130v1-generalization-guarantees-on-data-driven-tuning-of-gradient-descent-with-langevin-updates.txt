Title: Generalization Guarantees on Data-Driven Tuning of Gradient Descent with Langevin Updates

URL Source: https://arxiv.org/pdf/2604.13130v1

Published Time: Thu, 16 Apr 2026 00:06:00 GMT

Number of Pages: 22

Markdown Content:
# Generalization Guarantees on Data-Driven Tuning of Gradient Descent with Langevin Updates 

## Saumya Goyal ∗

## , Rohith Rongali †

## , Ritabrata Ray ∗

## , Barnab´ as P´ oczos ∗

Abstract 

We study learning to learn for regression problems through the lens of hyperparameter tuning. We propose the Langevin Gradient Descent Algorithm (LGD), which approximates the mean of the posterior distribution defined by the loss function and regularizer of a convex regression task. We prove the existence of an optimal hyperparameter configuration for which the LGD algorithm achieves the Bayes’ optimal solution for squared loss. Subsequently, we study generalization guarantees on meta-learning optimal hyperparameters for the LGD algorithm from a given set of tasks in the data-driven setting. For a number of parameters d and hyperparameter dimension h, we show a pseudo-dimension bound of O(dh ), upto logarithmic terms under mild assumptions on LGD. This matches the dimensional dependence of the bounds obtained in prior work for the elastic net, which only allows for h = 2 hyperparameters, and extends their bounds to regression on convex loss. Finally, we show empirical evidence of the success of LGD and the meta-learning procedure for few-shot learning on linear regression using a few synthetically created datasets. 

# 1 Introduction 

The success of modern machine learning models increasingly depends on the careful selection of hyper-parameters, a task that remains one of the most challenging and resource intensive aspects of developing high-performance systems. Unlike model parameters, which are learned directly from data through optimiza-tion, hyperparameters must be specified prior to training and fundamentally shape the learning dynamics, model capacity, and generalization behavior. As models have grown in scale and complexity, from deep neu-ral networks with billions of parameters to foundation models trained on vast datasets, the hyperparameter search space has expanded dramatically, making effective tuning both more critical and more computation-ally prohibitive. Traditional approaches to hyperparameter optimization have evolved from manual tuning and grid search to more sophisticated methods like Bayesian optimization, multi-armed bandit algorithms, etc. While such strategies can discover well-performing hyperparameter combinations, they require extensive computational resources, often necessitating hundreds or thousands of complete training runs. Despite sub-stantial progress in automated hyperparameter optimization, current methodologies exhibit a fundamental gap: they provide limited insight into the underlying mechanisms by which hyperparameters influence learn-ing dynamics and fail to leverage the rich information available during training. As noted in Balcan et al. [2022], such heuristic often offer no guarantees on optimality, or require strong data-dependent assumptions. In this work, we propose the Langevin Gradient Descent Algorithm, that performs gradient descent with Langevin updates. We first show the existence of a regularizer and learning rate for which our estimator achieves achieves Bayes’ optimality. We then study generalization guarantees on learning the optimal regu-larizer and learning rate from tasks. Concretely, we consider a regression problem with inputs X ∈ Rn×dx

and outputs y ∈ Rn, where n is the number of training samples and dx is the feature space dimension. For  

> ∗Machine Learning Department, Carnegie Mellon University
> †Electrical and Computer Engineering Department, Carnegie Mellon University. Correspondance to Saumya Goyal, <saumyag@andrew.cmu.edu >

1

> arXiv:2604.13130v1 [cs.LG] 13 Apr 2026

a function f (x; w) : Rdx × Rd → R, and test/validation inputs Xv ∈ Rnv ×dx , we want to find an estima-tor ˆfv (., ., . ) : Rnv ×dx × Rn×dx × Rn → Rnv that takes as input Xv , X, y and outputs an estimate of the validation/test output, yv , that minimizes the validation error, l(yv , ˆfv (Xv , X, y )) for some loss function l.Directly optimizing to minimize the validation error often leads to overfitting, which motivates the use of regularizers. Regularizers allow for the possibility of finding a good balance between avoiding overfitting while allowing generalization. We incoporate regularizers of the form r(w; θ) in our objective where θ ∈ Θare hyperparameters. We restrict our study to the case when l(y, f (X; w)) is convex in w and the regularizer 

r(w; θ) is strongly convex in w for all θ ∈ Θ. Strong convexity of the objective is a common assumption in optimization that leads to tighter bounds [Nesterov, 2018]. We propose a Langevin diffusion-inspired gradient descent on w that follows: 

w(k+1) = w(k) − η(∇l(y, f (X; w)) + r(w; θ)) + p2ηξ k,

followed by a consolidation step, where ξk is standard Gaussian noise. The consolidation step averages the predictions of final few iterates of the diffusion stage. We begin with a brief background into statistical generalization guarantees and Langevin algorithms in Section 2. We then formalize our setting in Section 3, and introduce the proposed algorithm in Algorithm 2. We show in Section 4 that the proposed strategy converges close to the Bayes optimal solution for appropriately chosen θ and η for the squared loss. In Section 5, we discuss finding the optimal values of η, θ . We assume access to a set of regression tasks sampled similarly, and propose using η, θ that minimize the average validation loss across tasks. This makes our setting similar to multi-task learning since we use previously seen tasks to develop a procedure for future, unseen tasks. We compute the pseudo-dimension of the function class corresponding to validation losses for η, θ in Theorem 5.1, and find a pseudo-dimension of O(dh ), upto logarithmic terms. For the case of linear regression with Elastic Net, this matches the worst-case bounds developed in Balcan et al. [2023] upto logarithmic terms. Interestingly, we show a dependence on the number of parameters d, instead of the feature space dimension dx, and we extend their analysis to any regression problem with a convex objective. We show generalization guarantees on this procedure in Theorem 5.2. We further show convergence of the hyperparameter tuning algorithm to the Bayes’ optimal solution in Theorem 5.4. Finally, we present empirical evidence of the success of the proposed Langevin Gradient Descent algorithm and the meta-learning procedure for few-shot learning on linear regression on a few synthetic datasets in Section 6, where we observe that the meta-learned algorithm performs at par with the optimal algorithm with as little as 1 /10th the number of iterations. 

Summary of Contributions. Our contributions are summarized as follows: 1. We propose the Langevin Descent Algorithm for linear regression in Algorithm 1, and present the general version in Algorithm 2. The algorithm makes Langevin-diffusion inspired updates to estimate the mean prediction of a posterior distribution corresponding to the loss function and a regularizer. As opposed to prior work [Balcan et al., 2022, 2023, 2025] that restrict their attention to estimators of the parameters w, we propose a consolidation procedure that directly outputs an estimate of f (Xv , w )for some test/validation input Xv based on the training data. We illustrate the significance of our consolidation step in Theorem 4.1 which shows the Bayes’ optimality of our procedure for the squared loss. We note further, that the proof of the theorem does not assume that training and test datasets are sampled i.i.d., meaning that the Bayes’ optimality of the procedure holds even under distribution shift. 2. We utilize the Goldberg-Jerrum framework for computing the pseudo-dimension of a function class as introduced in Goldberg and Jerrum [1995] to compute the pseudo-dimension of the function class corresponding to validation loss values for the hyperparameters η, θ . While Balcan et al. [2023] compute pseudo-dimension for linear regression with elastic net, we extend their results to any regression problem with a convex objective, and any parametrized family of regularizers. Further, while Balcan et al. [2023, 2025] assume perfect optimization of the elastic net, we study optimization using our proposed Algorithm 2, and hence account for errors in optimization. Despite these generalizations, we obtain a pseudo-dimension bound that is linear (upto logarithmic terms) in the product of the number of 2hyperparameters h and the mixing time of the algorithm, which is typically linear in the number of parameters d. We thus obtain a pseudo-dimension bound of O(dh log( d)), which recovers the pseudo-dimension bounds of Balcan et al. [2023] for the case of linear regression on elastic net upto logarithmic terms. 3. We show generalization guarantees on estimating the optimal η, θ from previously seen tasks sampled similar to the target task in Theorem 5.2. We then use these results to obtain concentration bounds of the ERM hyperparameters to the Bayes’ optimal solution. We show that for well-specified problems with squared loss having log-strongly-concave priors, T = Oδ

 dh log( d)

> ϵ6



tasks are sufficient to learn hyperparameters that achieve a generalization gap of less than ϵ from the Bayes’ optimal solution with probability ≥ 1−δ in Theorem 5.4, where Oδ suppresses the dependence on δ. This further extends the treatment of prior work [Balcan et al., 2023, 2025] that fail to give concentration inequalities to Bayes’ optimal solution outside of Gaussian and Laplace priors while also assuming perfect optimization. 4. Finally, we present empirical evidence on the success of the proposed Langevin Gradient Descent (LGD) algorithm and meta-learning hyperparameters for this algorithm in Section 6. We observe that LGD greatly out-performs plain gradient descent with no regularization on few-shot learning for linear regression, and outperforms an oracle gradient descent algorithm with perfect regularization on an atypical prior with mean ̸ = mode. For the atypical prior, we additionally observe that the meta-learned algorithm beats the theoretical optimal algorithm with the same number of iterations, and performs at-par with the theoretically best algorithm at 10 × the number of iterations. 

## 1.1 Related Work 

Hyperparameter optimization is a well studied problem in machine learning literature. Well studied and widely adopted techniques include random search Bergstra and Bengio [2012], Bayesian optimization Snoek et al. [2012], Bergstra et al. [2011], population-based training Jaderberg et al. [2017], and bandit algorithms Li et al. [2018]. Another commonly studied approach for hyperparameter optimization and meta-learning is to formulate them as bi-level optimization problems Finn et al. [2017], Nichol et al. [2018], Franceschi et al. [2018], Rajeswaran et al. [2019], Liu et al. [2022]. Our approach falls under the data-driven algorithm design framework of Balcan [2020] where the authors suggest using data for provable tuning of hyperparameters in algorithms. This approach has been successful for designing various learning algorithms such as in Bartlett et al. [2022], Balcan et al. [2022, 2023, 2025]. Balcan et al. [2022, 2023] give pseudo-dimension based generalization guarantees on tuning the elastic net, Balcan et al. [2025] give a data-dependent analysis using Rademacher complexities that improves upon the analysis of Balcan et al. [2023] for nice distributions, and gives bounds that get tighter with increasing number of samples per task. In this work, we generalize the setting of the elastic net and provide guarantees on learning descent algorithms instead of just regularization coefficients. We additionally show that this provides the optimal regularization approach for any log-strongly-concave prior (as opposed to just Gaussian and Laplacian). The consolidation step of our algorithm, involving averaging the predictions of the final few iterates, can be thought of as a generalization of Polyak averaging [Polyak and Juditsky, 1992, Meta AI, 2024] which is an averaging of the final few iterates of Stochastic Gradient Descent. On the statistical front, our approach falls under the g-modelling framework for Empirical Bayes methods, a connection explored in Balcan et al. [2025]. While we assume access to the ERM solution for hyperparameters in this paper, finding hyperparameters through gradient descent is a popular approach in empirical literature. Notably, Andrychowicz et al. [2016] suggest a similar approach to ours where they model the update step of gradient descent using an LSTM, and propose learning the parameters of the LSTM by meta-learning but don’t give any theoretical guarantees. This is different from our approach where we suggest langevin-style updates that use the provided loss function with a regularizer. The popular MAML algorithm [Finn et al., 2017] simplified their approach by only optimizing for initial neural network parameters that optimize well on downstream tasks. Other approaches such as the hypergradient descent algorithm [Maclaurin et al., 2015, Baydin et al., 2018] also 3suggest optimising over hyperparameters by approximating the gradients over the hyperparameters. More recently, the metagradient descent algorithm was proposed in Engstrom et al. [2025]. DataRater Calian et al. [2025] suggest a similar approach for assigning optimal weights to training samples using meta-learning. On the theory side, Balcan et al. [2019], Khodak et al. [2019] have shown provable guarantees for MAML like meta-learning algorithms where the initialization of the weights for each task is meta-learned. Here, a geometric notion of task similarity is assumed (the optimal weights for each task are geometrically close ), as opposed to a prior-based assumption as in our work, and techniques from online convex optimization are used to obtain regret upper bounds. Learning hyperparameters for optimization also fall under the framework of learning to optimise [Chen et al., 2022]. Proving generalization guarantees on learning algorithms using pseudo-dimension is a popular approach in machine learning literature [Anthony and Bartlett, 1999]. There are several approaches to bound the pseudo-dimension of a function class, like covering-number based arguments. In this work, we use the algorithmic framework of Goldberg and Jerrum [1995], which upper bounds the pseudo-dimension of a function class F

by constructing an algorithm Γ x,r to calculate whether f (x) ≥ r for any input f ∈ F . Bartlett et al. [2019] use this approach to show an almost-tight upper bound on the pseudo-dimension of a neural network class using ReLU activation with W number of parameters and L layers to be O(W L log( W )). Building on these results, we assume that the predicate complexity and degree of computing the given functions is known and use these values to calculate the pseudo-dimension of the validation loss function class. The foundational idea of using gradient based diffusion processes to sample from a target density traces its roots from statistical physics literature, but it was introduced in modern statistics by Grenander [1983], and formalized as the Unadjusted Langevin Algorithm (ULA) by Parisi [1981]. The adoption in machine learning was encouraged by Welling and Teh [2011], who proposed stochastic gradient Langevin dynamics (SGLD), replacing exact gradients with minibatch stochastic gradients and eliminating the Metropolis correction for scalability to large datasets. Non-asymptotic convergence guaranties for the ULA in Wasserstein and TV distance for log-concave distributions were established by Dalalyan [2017] and Durmus and Moulines [2017]. Raginsky et al. [2017], Xu et al. [2018] analyse the convergence of langevin-based gradient algorithms to minimizers of the loss function for non-convex settings by assuming dissipativity of the energy function. Further refinements such as preconditioning with curvature information Girolami and Calderhead [2011], stochastic gradient MCMC frameworks unifying Langevin and Hamiltonian variants Ma et al. [2015], and variance-reduced estimators such as SVRG-Langevin Dubey et al. [2016] were developed, which improve convergence rates while retaining the minibatch scalability, thus making Langevin methods attractive for modern Bayesian deep learning and large-scale latent variable models. As a result, Langevin dynamics has now become a central tool for scalable posterior sampling in Bayesian inference and in the diffusion based generative modeling literature [Nemeth and Fearnhead [2021], Chung et al. [2023], Song and Ermon [2019], Song et al. [2021]]. 

# 2 Background 

We begin our discussion with relevant background on statistical concentration inequalities and generalization error bounds using pseudo-dimension. We discuss the Goldberg-Jerrum framework that is the primary tool used in this paper to calculate the pseudo-dimension of the validation loss function loss. Finally, we present a brief discussion on sampling using the Unadjusted Langevin Algorithm. 

## 2.1 Concentration Inequalities and Uniform Convergence 

We begin with Hoeffding’s inequality, which shows that the mean of random variables concentrates expo-nentially fast around their expected value. 

Theorem 2.1 (Hoeffding’s inequality [Wasserman, 2010]) . For random numbers Z1, . . . , Z N sampled i.i.d., denote ZN =  

> PZi
> N

and E[Zi] = μ. The following hold given that Zi ∈ [0 , C ]:

41. 

Pr( |ZN − μ| ≥ t) ≤ 2 exp 

 −2N t 2

C2



2. 

Pr( ZN − μ ≥ t) ≤ exp 

 −2N t 2

C2



3. For any δ > 0, with probability ≥ 1 − δ,

ZN ≤ μ + C

r ln 1 /δ 

2N

We also present a version of the Bernstein’s inequality that shows tighter concentration bounds if the variance of i.i.d. observations is known. 

Theorem 2.2 (Bernstein’s inequality [Shalev-Shwartz and Ben-David, 2014]) . For random numbers Z1, . . . , Z N

sampled i.i.d., denote ZN =  

> PZi
> N

, E[Zi] = μ and V ar (Zi) = V . Given that Zi ∈ [−C, C ] for t > 0:

Pr( ZN − μ ≥ t) ≤ exp 

 −3N t 2

2(3 V + Ct )



While concentration inequalities show concentration of means of random variables, we are often in-terested in uniform convergence , that show that means of functions of random variables concentrated to their expected values uniformly over all functions in a class . Notationally, we are interested in calculating sup g∈G 1

> N

P g(Zi) − E[g(Z)] for some function class G. We provide these guarantees for our algorithm using Psuedo-dimension, as defined below. 

Definition 2.3 ([Anthony and Bartlett, 1999]) . Let G be a set of functions mapping from Z to R, and suppose that S = {z1, ..., z m} ⊆ Z . Then S is pseudo-shattered by G if there are real numbers r1, ..., r m

such that for each b ∈ { 0, 1}m there is a function fb in G with sign (fb(zi) − ri) = bi for i ∈ [m]. We say that r = ( r1, ..., r m) witnesses the shattering. We say that G has pseudo-dimension k if k is the maximum cardinality of a subset S of Z that is pseudo-shattered by G, denoted P dim (G) = k. If no such maximum exists, we say that G has infinite pseudo-dimension. 

Calculating the pseudo-dimension of a function class allows us to show uniform convergence results of the form below: 

Theorem 2.4 ([Anthony and Bartlett, 1999]) . Suppose G is a class of real-valued functions with range in 

[0 , H ] and finite P dim (G). Then for any ϵ > 0 and δ ∈ (0 , 1) , for any distribution D and for any set S of 

m = O( H2 

> ϵ2

(P dim (G) + log(1 /δ ))) samples drawn from D, w.p. at least 1 − δ, we have 

1

m

X

> zi∈S

g(zi) − Ez∼D [g(z)] ≤ ϵ, for all g ∈ G .

## 2.2 Goldberg-Jerrum Framework 

The Goldberg-Jerrum framework [Goldberg and Jerrum, 1995] is a popular tool for computing the Psuedo dimension of a function class. 

Definition 2.5 (GJ algorithm [Bartlett et al., 2022, Balcan et al., 2023]) . A GJ algorithm Γ operates on real-valued inputs, and can perform two types of operations: 

• Arithmetic operators of the form ν′′ = ν ⊙ ν′, where ⊙ ∈ { +, −, ×, ÷} for ν, ν ′, ν ′′ ∈ R

• Conditional statement of the form “if ν ≥ 0. . . else . . . ”. 

5In both cases, ν and ν′ are either inputs or values previously computed by the algorithm. 

Definition 2.6 ([Bartlett et al., 2022, Balcan et al., 2023]) . The degree of a GJ algorithm is the maximum degree of any rational function it computes of the inputs. The predicate complexity of the GJ algorithm is the number of distinct rational functions that appear in its conditional statements. 

Lemma 2.7 ([Bartlett et al., 2022, Balcan et al., 2023]) . Suppose that each function g ∈ G is specified by 

m real parameters. Suppose that for every x ∈ X and r ∈ R, there is a GJ algorithm Γx,r that given g ∈ G ,returns ”true” if g(x) ≥ r, and ”false” otherwise. Assume that Γx,r has degree ∆ and predicate complexity 

Λ. Then, P dim (G) = O(m log(∆Λ)) .

## 2.3 Langevin Algorithms 

Langevin diffusion is a popular technique to sample from a distribution with a known score function (gradient of log of pdf). For a distribution π ∝ exp( −U ) defined over Rd, Langevin diffusion follows a continuous process governed by the following Stochastic Differential Equation (SDE): dZt = −∇ U (Zt)d t + √2d ξt,

where ξt is corresponds to Brownian motion in Rd. In practice, we simulate this continuous process us-ing a discretization called the Unadjusted Langevin Algorithm (ULA). Starting with a sample Z(0) , ULA constitutes the following iterative updates: 

Z(k+1) = Z(k) − ηk+1 ∇U + p2ηk+1 ξk; ξk ∼ N (0 , I).ηk represent step-sizes for each step of the ULA. For simplicity, we also state ULA with constant step sizes, where ηk = η ∀k. We call this the homogeneous ULA that performs the following iterative updates to produce samples from π ∝ exp( −U ): 

Z(k+1) = Z(k) − η∇U + p2ηξ k; ξk ∼ N (0 , I).

We will use results from [Durmus and Moulines, 2019] which provide a detailed analysis of homogeneous and inhomogeneous ULA. To begin, we note the following two assumptions: 

Assumption 1. The function U is continuously differentiable on Rd and gradient Lipschitz: there exists 

L ≥ 0 such that for all z1, z 2 ∈ Rd, ∥∇ U (z1) − ∇ U (z2)∥ ≤ L∥z1 − z2∥.

Assumption 2. U is strongly convex, i.e. there exists m > 0 such that for all z1, z 2 ∈ Rd,

U (z2) ≥ U (z1) + ⟨∇ U (z1), z 2 − z1⟩ + ( m/ 2) ∥z1 − z2∥2.

Note that for both of these assumptions to hold, m ≤ L. We now discuss some important results about the homogeneous ULA with η ≤ 1/(m + L) and κ = 2Lm L+m . The Proposition 2.9 shows that homogeneous ULA samples from a stationary distribution that is close to the target distribution as measured by the Wasserstein-2 metric, which is defined below: 

Definition 2.8 (Wasserstein-2 metric) . For two distributions π, π ′, and Π( π, π ′) being the set of all couplings between the two distributions, we define the Wasserstein-2 ( W2) distance as: 

W 22 (π, π ′) = inf  

> γ∈Π( π,π ′)

Ez,z ′∼γ

∥z − z′∥2 .

We get the following result on the W2 distance between the true and target distribution of samples, where we denote the distribution of z(k) as πk.6Proposition 2.9. Under the assumptions 1 and 2, assuming we start the homoegeneous ULA from π0 = δz

for some z ∈ Rd, we get that for any k ≥ 1,

W2(πk, π ) ≤ ϵ, 

using η = O(ϵ2/d ) and k = Ω 

 dϵ2 log 

 d+∥z−z∗∥2

> ϵ2

 

, where we hide constants that depend on m, L . Here z∗

is the unique minimizer of the potential function U .Proof. Deferred to Appendix A. The following Proposition shows convergence guarantees on the empirical average of a function g applied to the final few iterates of the homoegeneous ULA algorithm. Define ∥g∥Lip to be the Lipschitz constant of 

g and ˆ πBb (g) as the empirical average of g applied to the last b iterates after B burn-in steps. Thus, ˆπBb (g) = 1

b

> b+B

X

> i=B+1

g(Xi). (1) 

Proposition 2.10. If we run a homogeneous ULA with potential function U that satisfies the assump-tions 1 and 2 and η = O(ϵ2/(∥g∥2

> Lip

d)) , B = Ω

 d∥g∥2 

> Lip
> ϵ2

log 

 (d+∥z−z∗∥2)∥g∥2

> Lip
> ϵ2

 

and b that satisfies 

bη ≥ 128 ∥g∥2  

> Lip log(1 /δ )
> ϵ2κ2

,

V ar (ˆ πBb ) ≤ ϵ2

8 log(1 /δ ) and, 

|ˆπBb (g) − π(g)| ≤ ϵ w.p. ≥ 1 − δ. 

Proof. Deferred to Appendix A. These and other relevant results on the ULA are proved in Appendix A. 

# 3 Problem Setting and Notation 

We follow the notation of Balcan et al. [2025]. We denote a regression task as the quadruple of training and validation datasets ( X, y, X v , y v ). Each input x in either the training or validation set lies in Rdx . The matrices X ∈ Rn×dx and Xv ∈ Rnv ×dx collect the n training inputs and nv validation inputs, respectively. The outputs are real-valued, with y ∈ Rn and yv ∈ Rnv . We study regression with respect to weights w ∈ Rd

for the function f (x; w) : Rdx × Rd → R. As a special case, f (x; w) = xw recovers standard linear regression. When applying f to a dataset, we interpret this operation row-wise; for instance, f (X; w) denotes the vector obtained by evaluating f (·; w) on each row of the matrix X. Additionally, we will denote the ith entry of an input/output matrix with a superscript with parenthesis. For example, X(i) is the ith input in the training set. We denote the predictions of a learning algorithm on Xv as ˆfv (Xv , X, y ; θ, ξ ), for ˆfv : Rnv ×dx × Rn×dx ×

Rn × Rh × Re → Rnv where θ ∈ Rh represent the hyperparameters of the learning algorithm, and ξ ∈ Re is the randomness of the algorithm sampled as ξ ∼ D E . When the data and hyperparameters are obvious from context, we will use ˆfv as a shorthand. For a loss function l : R × R → R, we denote the validation loss as: 

lv (θ, ξ, (X, y, X v , y v )) = 1

nv

X

> i

l(y(i) 

> v

, ˆfv (X(i) 

> v

, X, y ; θ, ξ )) .

We consider the problem of finding the optimal hyperparameters, θ∗ from T tasks. We assume that each task is sampled from a distribution D such that ( X, y, X v , y v ) ∼ D and denote the support of D as P, the set of all regression tasks. We define a problem instance as an ordered set of tasks sampled i.i.d.: 

S = {(Xt, y t, X tv , y tv )Tt=1 : ( Xt, y t, X tv , y tv ) i.i.d. 

∼ D, ∀t ∈ [T ]}.

7The superscript t without any paranthesis denotes the task, so that Xt(i) is the ith input in the training set of the tth task. Define the expected validation error as follows: 

lv (θ) = E(X,y,X v ,y v )∼D ,ξ ∼D E [lv (θ, ξ, (X, y, X v , y v ))] = E(X,y,X v ,y v )∼D ,ξ ∼D E

"

1

nv

X

> i

l(y(i) 

> v

, ˆfv (X(i) 

> v

, X, y ; θ, ξ )) 

#

,

which allows us to define θ∗ as θ∗ = arg min θ lv (θ). We will similarly define the empirical validation error using randomness ξ in each task as follows: 

lv (θ, ξ, S ) = 1

T

X

> t

1

nv

X

> i

l(yt(i) 

> v

, ˆfv (Xt(i) 

> v

, X t, y t; θ, ξ )) ,

where we represent each individual task with a superscript and write ˆwtθ,ξ as shorthand for ˆwθ,ξ (X, y ). This allows us to define the ERM estimator of θ as θERM (ξ) which satisfies θERM (ξ) = arg min θ lv (θ, ξ, S ). Similarly, we define θ∗(ξ) = arg min θ ES [lv (θ, ξ, S )]. For an estimator ˆfv (Xv , X, y ; θ, ξ ), and a prior π over the parameter w, we define the Bayes risk as: 

Bπ ( ˆfv ) = Eξ [Ew|X,y [Eyv |X,X v ,w l(yv , ˆfv (Xv , X, y ; θ, ξ ))]] = lv (θ).

The Bayes’ optimal estimator minimizes the Bayes’ risk such that ˆf opt v = arg min ˆfv Bπ ( ˆfv ). We present closeness of θ∗ to Bayes’ optimal solutions with the LGD algorithm for well-specified problems in Section 4. We then present generalization guarantees on estimating θ∗ using θERM (ξ) in Section 5. 

# 4 Langevin Gradient Descent 

We propose the Langevin Gradient Descent (LGD) algorithm in Algorithms 1 and 2 for linear regression and the general regression case respectively. Notably, while typical regression algorithms compute the minimizer of regularised loss, the LGD performs Langevin-style updates on the regularised loss, motivated by the Unadjusted Langevin Algorithm as discussed in Section 2. Intuitively, when the regularizer represents the score of a prior over parameters w, the langevin style updates should mimic the ULA over the posterior distribution. We show this explicitly for the squared loss in Theorem 4.1. Further note that the proof of Theorem 4.1 assumes that the training and validation outputs are followed from the same ground truth, but doesn’t assume that they are sampled i.i.d. Thus the Bayes’ optimality of our algorithm holds even under distribution shift. We average the final few iterates of the ULA step to achieve an estimate of the mean predictions using the posterior. Theorem 4.1 shows that this procedure achieves Bayes’ optimality for the squared loss given optimal hyperparameters for the gradient of regulariser function r(.). This is in sharp contradiction to gradient descent algorithms, where we only obtain the mode of the posterior distribution, which is not necessarily the Bayes’ optimal solution, even with optimal hyperparameter configurations. To begin we make the following assumptions on the loss function l(·, ·) the gradient of regulariser r(·; ·) and the function f (·; ·), valid for all training pairs X, y in P, w, w ′ ∈ Rd and θ ∈ Rh:

Assumption 3. l(y, f (X; w)) is gradient-Lipschitz and convex in w.

Assumption 4. r(w; θ) is Lipschitz in w and strictly monotone satisfying ∥r(w; θ) − r(w′; θ)∥ ≤ μr ∥w − w′∥

for some μr .

Assumption 5. f (x; w) is Lipschitz in w for all x, such that f (x; w) − f (x; w′) ≤ ∥ f ∥Lip ∥w − w′∥.

The theorem below establishes that our algorithm computes estimators close to the Bayes’ optimal. 

Theorem 4.1. Assume a well-specified regression problem such that there exists a w∗ for which y ∼N (f (X; w∗), I n) and yv ∼ N (f (Xv ; w∗), I nv ). That is, there exists a ground truth w∗ for which the problem 

8Algorithm 1 LGDLinear 

Require: Learning rate η, regulariser parameters 

θ, data (X, y ), gradient of regulariser r(.), Langevin noise ξ, burn-in steps B, and averag-ing steps b. 

> 1:

w(0) ← 0

Step 1 : ULA starting at w(0)  

> 2:

for k = 0 to B + b − 1 do  

> 3:

g(k) ← X⊤∇y′ l(y, y ′ = Xw (k)) + r(w(k); θ) 

> 4:

w(k+1) ← w(k) − η g (k) + √2η ξ (k) 

> 5:

end for Step 2 : Empirical mean of predictions  

> 6:

ˆy ← 1

> b

PB+bk=B+1 Xv w(k) 

> 7:

return ˆy

Algorithm 2 LGD 

Require: Learning rate η, regulariser parameters 

θ, training data ( X, y ), regression function f (.), gradient of regulariser r(.), langevin noise ξ,burn-in steps B, averaging steps b, and valida-tion input Xv . 

> 1:

w(0) ← 0

Step 1 : ULA starting at w(0)  

> 2:

for k = 0 to B + b − 1 do  

> 3:

g(k) ← ∇ wf (X; w(k))∇y′ l(y, y ′ = f (X; w(k))) + r(w(k); θ) 

> 4:

w(k+1) ← w(k) − η g (k) + √2η ξ (k) 

> 5:

end for Step 2 : Empirical mean of predictions  

> 6:

ˆfv ← 1

> b

PB+bk=B+1 f (Xv ; w(k)) 

> 7:

return ˆfv

is exactly solvable upto Gaussian noise. Further assume that the ground truth w∗ ∼ D W . For the squared loss l(y, y ′) = ∥y − y′∥2, the Bayes’ optimal predictions ˆfv satisfy: 

ˆf opt v (Xv ) = Ew∗|X,y [f (Xv ; w∗)] .

If the Assumptions 3-5 are satisfied, θ is given such that r(w; θ) = ∇w log DW (w), then for η = O(ϵ2/(∥f ∥2

> Lip

d)) 

and within B = O

 d∥f ∥2 

> Lip
> ϵ2

log 

 (d+∥w(0) −wmin ∥2)∥f ∥2

> Lip
> ϵ2

 

, the LGD algorithm as given in Algorithm 2 is ϵ-close in expectation to the Bayes’ optimal solution for each entry of Xv :

|E[ ˆfv (X(i) 

> v

)] − Ew∗|X,y [f (X(i) 

> v

, ; w∗)] | ≤ ϵ ∀i ∈ [nv ]=⇒ ∥ E[ ˆfv ] − Ew∗|X,y [f (Xv , ; w∗)] ∥ ≤ √nv ϵ

Where wmin is the mode of the posterior wmin = arg max w P r (w|X, y ). Further, b = O

 ∥f ∥2 

> Lip log(1 /δ )
> ηϵ 2



is sufficient to guarantee, for a particular entry of Xv denoted X(i) 

> v

:

P r (| ˆfv (X(i) 

> v

) − Ew∗|X,y [f (X(i) 

> v

; w∗)] | ≥ ϵ) ≤ δ. 

Alternatively, 

P r (∥ ˆfv − Ew∗|X,y [f (Xv ; w∗)] ∥ ≥ √nv ϵ) ≤ nv δ. 

Proof. We first prove the Bayes’ optimality of the estimator ˆfv (Xv ) = Ew∗|X,y [f (Xv ; w∗)]. For any estimator ˆfv (·) with randomness ξ, we write the Bayes’ risk from Section 3 as: 

Bπ ( ˆfv ) = Eξ [Ew|X,y [Eyv |Xv ,w [∥ ˆfv (Xv , X, y ) − yv ∥2]]] 

≥ Ew|X,y [Eyv |Xv ,w [∥Eξ [ ˆfv (Xv , X, y )] − yv ∥2]] ,

by Jensen’s inequality. We can thus focus on deterministic estimators in this discussion. We note that yv is sampled from the same ground truth w∗ as y so we can re-write this as: 

Bπ ( ˆfv ) = Ew|X,y [Eϵv [∥ ˆfv (Xv , X, y ) − f (Xv ; w) − ϵv ∥2]] = Ew|X,y [∥ ˆfv (Xv , X, y ) − f (Xv ; w)∥2 + nv E[∥ϵv ∥2]] = nv E[∥ϵv ∥2] + Ew|X,y [∥ ˆfv (Xv , X, y ) − f (Xv ; w)∥2],

9where nv is the number of examples in the validation set Xv . Now, we want to find the ˆfv that mini-mizes the Bayes error above, so we only focus on the second term. Since the mean of a distribution mini-mizes the expected value of squared distances, we can conclude that the minimizer satisfies: ˆfv (Xv , X, y ) = 

Ew∗|X,y [f (Xv ; w∗)]. To show Bayes’ optimality of our algorithm, we first note that the posterior satisfies P r (w|X, y ) ∝

P r (y|X, w )P r (w) so that ∇w log P r (w) = −∇ wf (X; w)( f (X; w) − y) − r(w; θ) from the given conditions. Thus, the ULA in Step 2 of Algorithm 2 is a ULA on the posterior distribution. Using results from Proposition 2.10, we can say that for each element X(i) 

> v

of Xv , E[ ˆfv (X(i) 

> v

)] − Ew∗|X,y [f (X(i) 

> v

; w∗)] ≤ ϵ for the given values of B and η by replacing the function f (.) in the Proposition with f (X(i) 

> v

; .). Alternatively, ∥E[ ˆfv ] −

Ew∗|X,y [f (Xv , ; w∗)] ∥ ≤ √nv ϵ. Similarly, for the chosen value of b, we can give the high probability bound 

P r (| ˆfv (X(i) 

> v

)−Ew∗|X,y [f (X(i) 

> v

; w∗)] | ≥ ϵ) ≤ δ, or alternatively, P r (∥ ˆfv (Xv )−Ew∗|X,y [f (Xv ; w∗)] ∥ ≥ √nv ϵ) ≤

nv δ by a union bound. 

# 5 Generalization Guarantees 

In this Section we show generalization guarantees on θERM , which are the set of hyperparameters that minimize the meta-learning loss as we note in Section 3. While there are several empirical approaches to find the minimizer of the meta-learning loss as mentioned in Section 1.1 such as the hypergradient or metagradient methods [Maclaurin et al., 2015, Engstrom et al., 2025], we assume access to the ERM solution directly. We present some empirical evidence using gradient-based methods for finding ERM in Section 6. The following Theorem establishes the Pseudo-dimension of the validation loss function class over all hyperparameters θ

and learning rates η, using the Goldberge-Jerrum framework from Section 2. 

Theorem 5.1. In Algorithm 2, assume that all entries of r(.; .) can be computed using a GJ algorithm with degree ∆R and predicate complexity ΛR in its inputs w(k) and θ. Similarly, assume the regression function 

f (.; .) is differentiable and can be computed using a GJ algorithm with degree ∆F and predicate complexity ΛF

in its inputs. Finally, assume that the loss function l(., . ) can be computed using a GJ algorithm with degree 

∆L and predicate complexity ΛL. Define the function class Lv = lv ({θ, √η}, ., . ) : R(B+b)×d × P → R|θ ∈ Rh ,as the class of validation loss functions for given hyperparameter values for Algorithm 2. The pseudo-dimension of Lv satisfies: 

P dim (Lv ) = O (( B + b)h log(∆ R + ∆ F ∆L) + hlog (( B + b)(( n + nv )(Λ F + Λ L) + Λ R)) .

Proof. In the following, we give a GJ algorithm corresponding to the function class Lv . For a task P ∈ P 

and langevin noise ξ ∈ R(B+b)×d, the inputs to the GJ algorithm Γ (ξ,P ),z are {θ, √η}, and we want it to compute whether lv ({θ, √η}, ξ, P ) ≥ z for z ∈ R. To do so, we first compute the predicted ˆwt for each task t following the GJ algorithms for computing f and r respectively and Algorithm 2 which only performs arithmetic operations on these values. The update step for w(k) can be analyzed as follows: 1. r(w(k); θ): Predicate complexity Λ R and degree ∆ R in the inputs θ and in w(k).2. ∇y′ l(y, y ′ = f (X; w(k))): We compute this as the derivative of the final rational functions in the computation of l(y, y ′ = f (X; w(k))). Thus, for each element in the inputs y and f (X; w(k)), we would get a degree of 2∆ L and predicate complexity of Λ L. Over n entries, we get a degree of 2∆ L and predicate complexity of nΛL. The input f (X; w(k)) has degree ∆ F and predicate complexity Λ F in each of its inputs from given assumptions, resulting in an overall degree ∆ F and predicate complexity 

nΛF . Thus, this term has an overall degree of 2∆ F ∆L and predicate complexity nΛL + nΛF in the inputs X, y, w (k).3. ∇wf (X; w(k)): We compute this as the derivative of the final rational functions in the computation of 

f (X; w(k)). Thus for each entry in X, it has predicate complexity Λ F and degree at most 2∆ F . Over all entries in the input, we have predicate complexity of nΛF and degree 2∆ F .10 4. g(k): Combining the above steps, we get that this term has a degree of 2∆ F ∆L + 2∆ F + ∆ R and a predicate complexity of n(2Λ F + Λ L) + Λ R.5. w(k+1) : Since η has degree 2 in √η, this term has a degree of 2∆ F ∆L + 2∆ F + ∆ R + 2 and a predicate complexity of n(2Λ F + Λ L) + Λ R in terms of w(k), θ and √η.Now, we are interested in computing the degree and predicate complexity of w(k+1) in terms of θ, √η,

w(0) , instead of w(k). Solving for this recurrence relation we can conclude that w(k+1) has a degree of (2∆ F (∆ L + 1) + ∆ R + 2) (k+1) in the inputs θ, √η. Since each step of the algorithm uses n(2Λ F + Λ L) + Λ R

rationals in conditional statements, the overall algorithm will need ( k + 1)( n(2Λ F + Λ L) + Λ R) rationals in conditional statements. We can thus conclude that f (Xv ; w(k)) has a predicate complexity of k(n(2Λ F + Λ L) + Λ R) + nv ΛF and degree ∆ F (2∆ F (∆ L + 1) + ∆ R + 2) k. Thus computing ˆfv has a predicate complexity of ( B + b)( n(2Λ F +ΛL)+Λ R)+ nv ΛF and a degree of ∆ F (2∆ F (∆ L +1)+∆ R +2) B+b. Finally, the validation loss has degree ∆ L

and predicate complexity Λ L for each entry of ˆfv . Thus it has a degree of ∆ F ∆L(2∆ F (∆ L + 1) + ∆ R + 2) k in the algorithm’s inputs and a predicate complexity of ( B + b)( n(2Λ F + Λ L) + Λ R) + nv ΛF + nv ΛL. Computing 

lv ({θ, √η}, ξ, P ) ≥ r takes one more conditional query. We can thus conclude from Lemma 2.7 that the pseudo-dimension is given as follows: 

P dim (Lv ) = O(h log   ∆F ∆L(2∆ F (∆ L + 1) + ∆ R + 2) B+b [( B + b)( n(2Λ F + Λ L) + Λ R) + nv ΛF + nv ΛL]

= O (( B + b)h log(∆ R + ∆ F ∆L) + hlog (( B + b)(( n + nv )(Λ F + Λ L) + Λ R)) .

In order to show our generalization guarantees, we make the following assumption on the bounded-ness of the loss function, similar to Balcan et al. [2023, 2025], on the loss function l(yp, y t), valid over all (X, y, X v , y v ) ∈ P and x(i) 

> v

, y (i) 

> v

∈ Xv , y v , and for all possible estimators ˆfv , ˆf ′ 

> v

:

Assumption 6. |l( ˆfv (x(i) 

> v

, X, y ), y (i) 

> v

)| ≤ C.

Below we give generalization guarantees on θERM (ξ). 

Theorem 5.2. Assume there exist GJ algorithms to compute the functions l(·, ·), r(·; ·),f (·; ·) with a dimension-independent degree and predicate complexity. Given Assumption 6 holds, T = O

 C2 

> ϵ2

(P dim (Lv ) + log(1 /δ )) 



tasks are enough to ensure that with probability ≥ 1 − δ,

lv (ϕERM (ξ)) − lv (ϕ∗) ≤ ϵ. 

Here we denote the combined set of hyperparameters {θ, √η} by ϕ.Proof. We begin by defining the expected validation error using fixed randomness ξ as follows, where ξt and 

St represent the noise used in Algorithm 2 of the tth task and the tth task of S respectively, consistent with our notation of using superscripts to denote task numbers: 

lv (ϕ, ξ ) = ES

"

1

T

X

> t

lv (ϕ, ξ t, S t)

#

= 1

T

X

> t

EP [lv (ϕ, ξ t, P )] = ES [lv (ϕ, ξ, S )] .

The second equation above follows from the i.i.d. assumption of tasks within each problem set. We can now decompose the target error term as follows: 

lv (ϕERM (ξ)) − lv (ϕ∗) ≤ lv (ϕERM (ξ)) − lv (ϕERM (ξ), ξ, S )+ lv (ϕERM (ξ), ξ, S ) − lv (ϕ∗(ξ), ξ, S )+ lv (ϕ∗(ξ), ξ, S ) − lv (ϕ∗(ξ), ξ )+ lv (ϕ∗(ξ), ξ ) − lv (ϕ∗, ξ )+ lv (ϕ∗, ξ ) − lv (ϕ∗).

11 Since by definition lv (ϕ∗(ξ), ξ ) − lv (ϕ∗, ξ ) ≤ 0 and lv (ϕERM (ξ), ξ, S ) − lv (ϕ∗(ξ), ξ, S ) ≤ 0, we can simplify the above expression as follows: 

lv (ϕERM (ξ)) − lv (ϕ∗) ≤ lv (ϕERM (ξ)) − lv (ϕERM (ξ), ξ, S )+ lv (ϕ∗(ξ), ξ, S ) − lv (ϕ∗(ξ), ξ )+ lv (ϕ∗, ξ ) − lv (ϕ∗). (2) We bound the first term using standard pseudo-dimension theory. Notably, lv (ϕERM (ξ)) −lv (ϕERM (ξ), ξ, S ) ≤

sup ϕ |lv (ϕ) − lv (ϕ, ξ, S )| ≤ ϵ for T = O

 C2 

> ϵ2

(P dim (Lv ) + log(1 /δ )) 



using uniform convergence bounds from Theorem 2.4. For the second term in Equation 2, we use Assumption 6 and Hoeffding’s inequality 2.1 to note that 

T = O

 C2 

> 2ϵ2

log(1 /δ )



are enough to ensure with probability ≥ 1 − δ over the sampling of tasks in S, that 

lv (ϕ∗(ξ), ξ, S ) − lv (ϕ∗(ξ), ξ ) ≤ ϵ. Similarly for the last term, the same number of tasks are enough to ensure with probability ≥ 1 − δ over the sampling of noise ξ for all tasks, that lv (ϕ∗, ξ ) − lv (ϕ∗) ≤ ϵ. We recover the desired bounds by combining the above inequalities and replacing δ with δ/ 3 and ϵ with ϵ/ 3 and absorbing constants into the O(.) notation. 

Remark 5.3. We note that the above bounds can be tightened upto constant factors by accounting for the variance of algorithm 2 which decreases with B, b as seen from Proposition 2.10. Denote V ar ξ ( ˆfv (xv , X, y )) = 

V as the variance of predictions of any validation input xv . Assume that the loss function l(., . ) is Lipschitz in the first term with Lipschitz constant L so that the variance of the loss function V ar ξ (l( ˆfv (xv , X, y ), y v )) = 

L2V . Thus, V ar ξ (lv (θ, ξ, P )) ≤ L2V , for all θ and P since lv (θ, ξ, P ) is an average over nv validation examples, which are not necessarily i.i.d. Thus T = O

 L2(V +Cϵ ) 

> ϵ2

log(1 /δ )



are enough to ensure lv (ϕ∗, ξ ) −

lv (ϕ∗) ≤ ϵ using Bernstein’s inequality 2.2. It may be possible to further tighten this analysis to get a 

O(1 /(nv ϵ2)) bound on T by assuming i.i.d. for the validation examples and keeping track of the correlation induced by a common noise vector ξ and common training data between predictions of different validation inputs. However since the dominant terms on T in the bound of Theorem 5.2 remain O(C2/ϵ 2), we defer such a discussion to future work. 

We are now able to combine the bounds of Theorems 4.1 and 5.2 to give a bound on the convergence of the ERM estimator to the Bayes optimal estimator as given below. 

Theorem 5.4. Assume the setting of Theorem 4.1 where we assume a well-specified regression problem such that there exists a w∗ for which y ∼ N (f (X; w∗), I n) and yv ∼ N (f (Xv ; w∗), I nv ). That is, there exists a ground truth w∗ for which the problem is exactly solvable upto Gaussian noise. Further assume that the ground truth w∗ ∼ D W , we use the squared loss, l(y, y ′) = ∥y−y′∥2, and there exist GJ algorithms to compute the functions l(·, ·), r(·; ·),f (·; ·) with a dimension-independent degree and predicate complexity. Denote α =  

> ∥f∥Lip
> ϵ2

. If we learn ERM hyperparameters of Algorithm 2 with B within O  dα 2 log  (d + ∥w(0) − wmin ∥2)α2 

and b within O  d log(2 nv /δ )α4, then 

T = O

 C2

ϵ21

dh log( d(n + nv ))  α2 log ( α) + α4 log(2 /δ )

tasks are enough to ensure that, with probability ≥ 1 − δ,

Bπ ( ˆfERM,ξ ) − Bπ ( ˆf opt v ) ≤ ϵ1 + ϵ2.

Proof. Similar to Theorem 4.1, define θp such that r(w; θp) = ∇w log DW (w) and ηp such that Ω 

 α2 log(1 /δ )

> b



≤

ηp ≤ O   1

> α2d

. We decompose the target bound as follows: 

Bπ ( ˆfERM,ξ ) − Bπ ( ˆf opt v ) = lv (ϕERM (ξ)) − Bπ ( ˆf opt v )= lv (ϕERM (ξ)) − lv (ϕ∗) + lv (ϕ∗) − lv ({θp, η p}) + lv ({θp, η p}) − Bπ ( ˆf opt v ).

12 Note by definition that lv (ϕ∗) − lv ({θp, η p}) ≤ 0, since ϕ∗ is the best hyperparameter configuration for ϕ and 

η. Theorem 4.1 implies lv ({θp, η (p)}) − Bπ ( ˆf opt v ) ≤ ϵ2 with probability ≥ 1 − nv δ for η = O(1 /(α2d)), b =

O

 α2 log(1 /δ )

> ηp



and B = O  dα 2 log  (d + ∥w(0) − wmin ∥2)α2 . We substitute our choice of ηp in the bound for b and replace δ with δ/ 2nv to obtain b = O  dα 4 log(2 nv /δ ) . Thus, with probability ≥ 1 − δ/ 2, 

lv ({θp, η }) − Bπ ( ˆf opt v ) ≤ ϵ2.Further, we know from Theorem 5.2 that lv (ϕERM (ξ)) − lv (ϕ∗) ≤ ϵ1 with probability ≥ 1 − δ for a choice of T = O

 C2

> ϵ21

(P dim (Lv ) + log(3 /δ )) 



. We instantiate the pseudo-dimension bounds from Theorem 5.1, as: 

P dim (Lv ) = O(( B + b)h + h log(( B + b)( n + nv )) = O  (dα 2 log( dα 2) + dα 4 log(2 nv /δ )) h + h log(( dα 2 log( dα 2) + dα 4 log(2 nv /δ ))( n + nv )) 

= O  (dα 2 log( dα 2) + dα 4 log(2 nv /δ )) h + h(log( d(n + nv )) + log( α2 + α4 log(2 nv /δ ))) 

= O  d log( d(n + nv ))( α2 log( α2) + α4 log(2 /δ )) h + h(log( d(n + nv )) + log( α2 + α4 log(2 /δ ))) 

= O  dh log( d(n + nv ))( α2 log( α) + α4 log(2 /δ ))  .

Thus, we have lv (ϕERM (ξ)) − lv (ϕ∗) ≤ ϵ1 with probability ≥ 1 − δ/ 2 for a choice of T that satisfies: 

T = O

 C2

ϵ21

 dh log( d(n + nv ))  α2 log ( α) + α4 log(2 /δ ) + log(2 /δ )

= O

 C2

ϵ21

 dh log( d(n + nv ))  α2 log ( α) + α4 log(2 /δ ) 

.

We thus recover the target bounds by combining both inequalities in the decomposition. We observe that the bound in the above theorem achieves a conservative bound on T of the form O(1 /ϵ 6)if ϵ1 = ϵ2 = ϵ, while typical generalization error bounds (for example, [Balcan et al., 2023, 2025]) only suffer from a quadratic rate of O(1 /ϵ 2). We note however that prior work assumes perfect optimization of the loss functions, and fail to achieve any sort of concentration to the Bayes’ optimal if the prior density is not Gaussian, Laplace or a product of the two. 

# 6 Experimental Results 

In this Section, we present experimental results on the Langevin Gradient Descent (LGD) applied to few-shot learning of linear regression presented in Algorithm 1. We present results on three distinct priors with increasing complexity as detailed below. For each of the three priors, we generate a synthetic dataset in a 

d = 10 dimensional space consisting of T = 250 tasks. For each task, we sample a ground truth w∗t from the prior, and sample n + nv = 500 inputs Xt ∼ N (0 , 10 I10 )⊗(n+nv ). We then sample standard normal noise 

ϵt ∼ N (0 , 1) ⊗(n+nv ) and set yt = Xtw∗t + ϵt. We measure loss using the squared loss, l(yp, y t) = ( yp − yt)2

to match the setting of Theorem 4.1 under which the optimal LGD algorithm is also Bayes’ optimal as long as the prior is log-strongly-concave. Figure 1 shows the average validation loss on the held-out set of 200 tasks against increasing number of training samples for the theoretically optimal algorithms against the meta-learned algorithm trained on 50 tasks. We describe the priors and optimal algorithms in detail below. 

## 6.1 Isotropic Gaussian Prior 

Under the isotropic Gaussian prior, we sample each entry of w∗t from a Gaussian distribution of variance 0.1 so that w∗t ∼ N (0 , 0.1I10 ). Under this prior, Theorem 4.1 gives that the Bayes’ optimal estimator is the mean of the posterior distributed as P r (w|X, y ) ∝ exp 



− ∥y−Xw ∥2 

> 2

− ∥w∥2

> 2×0.1



. Since the mean of this posterior is equal to it’s mode, the optimal Bayes’ estimator is equal to both the LGD estimator and GD estimator using an l2 regularizer θ∥w∥2 with θ∗ = 10. We show the performance of LGD run with burn-in 13 1                            

> 3
> 5
> 8
> 10
> 13
> 15
> 20
> 25
> Number of per-task training samples
> 0
> 10
> 20
> 30
> 40
> 50
> 60
> 70
> Avg. held-out validation loss
> LGD, meta (ours)
> LGD, *(oracle)
> GD, *(oracle)
> GD, = 0 (no reg.) 1
> 3
> 5
> 8
> 10
> 13
> 15
> 20
> 25
> Number of per-task training samples
> 0
> 10
> 20
> 30
> 40
> 50
> 60
> 70
> 80
> Avg. held-out validation loss
> LGD, meta (ours)
> LGD, *(oracle)
> GD, *(oracle)
> GD, = 0 (no reg.) 1
> 3
> 5
> 8
> 10
> 13
> 15
> 20
> 25
> Number of per-task training samples
> 0
> 100
> 200
> 300
> 400
> 500
> Avg. held-out validation loss
> LGD, meta (ours)
> LGD, *(oracle)
> LGD, *(oracle, 10 × mixing)
> GD, *(oracle)
> GD, = 0 (no reg.)

Figure 1: Comparison of oracle gradient descent (GD) and Langevin Gradient Descent (LGD) algorithms against meta-learned LGD algorithms. We synthetically generate 250 linear regression tasks in a d = 10 dimensional space by sampling ground truth vectors from log-strongly-concave priors, inputs Xt ∼ N (0 , I10 )⊗(n+nv ) and noise ϵt ∼

N (0 , 1) ⊗n+nv and set yt = Xtw∗t + ϵt. We meta-learn using the first 50 tasks and evaluate on the last 200 for all algorithms. (Left) Shows comparison on an isotropic Gaussian prior on w∗ where the oracle LGD and GD algorithms perform comparatively and outperform GD without regularization. The meta-learned LGD algorithm matches the oracle baselines. (Middle) Shows comparison on a non-isotropic Gaussian prior, where the LGD oracle performs slightly worse than the GD oracle due to noise, but both out-perform plain GD with no regularization. The meta-learned LGD performs almost at-par with the oracle LGD. (Right) Shows comparison on a Gaussian prior scaled with Softplus, resulting in a prior with mean ̸=mode. We see that the LGD oracle beats the GD oracle due to the assymetric nature of the prior, and the GD oracle beats GD without regularization. The meta-learned LGD algorithm beats both of these oracles, and performs at-par or better than the LGD oracle with a 10 × higher number of iterations. 

B = 500, b = 5000 averaging steps and learning rate η = 9 e − 4, and GD with a total of 5500 iterations and η = 9 e − 4 in Figure . We note that the oracle LGD performs at par with the oracle GD, and much better than plain GD without regularization, as expected. We additionally plot LGD using meta-learned θ

and learning rate η with same values of B, b . Meta-learning is done using the Adam optimizer [Kingma and Ba, 2014] with a learning rate of 0 .4 and 50 steps with the log-values of θ and η as parameters for stability, and clamping η in the range [1 e − 7, 1.2e − 3]. We observe that the meta-learned LGD matches the oracle baselines even using 1 training sample per task. Note that practically, when the underlying prior is unknown, the oracle algorithms are unachievable whereas the meta-learned algorithm is still realizable. 

## 6.2 Non-Isotropic Gaussian 

We extend the above prior to a non-isotropic Gaussian. We first sample d = 10 variance values v1, . . . , v d

uniformly between 0 .05 and 0 .5 and create the diagonal covariance matrix Σ = diag (v1, . . . , v d). We now use this covariance matrix to sample the ground truth for each task, w∗t ∼ N (0 , Σ). Similar to above, the Bayes’ optimal estimator for this prior is the mean of the posterior distributed as P r (w|X, y ) ∝

exp 



− ∥y−Xw ∥2

2 −

P w2

> i

2vi

 

. Since this is again a Gaussian posterior, the mean and the mode are equal and equal to the minimizer of ∥Xw − y∥2 + P w2

> i

vi . We show the performance of LGD run with burn-in B = 500, 

b = 5000 averaging steps and learning rate η = 9 e − 4 using the gradient of regularizer r(w; θ) = P wiθi

for θ∗ = ( v1, . . . , v d), as well as GD run with the same regularizer for 5500 iterations and η = 9 e − 4. We observe that the oracle LGD algorithm performs slightly worse than plain GD due to noise in the algorithm and a sub-optimal mixing time, although both oracle algorithms massively outperform plain GD with no regularization. Lastly, we also plot the performance of the meta-learned LGD algorithm, where we again use the Adam optimizer [Kingma and Ba, 2014] with learning rate 0 .4 using 50 steps, and optimize the log of θ and η while clamping η in the range [1 e − 7, 1.2e − 3]. We observe that the meta-learned LGD matches the performance of the oracle LGD even with 1 training example per task. We note again that the oracle algorithms are unachievable in practice, while the meta-learned algorithm is. 14 6.3 Gaussian with Softplus Scaling 

Finally, we conduct experiments on a prior with a clear separation between mean and mode, to better illustrate the advantages of LGD over plain GD. Towards this, we study the prior 

πα,β,γ (u) ∝ exp 



− γu 2

2 − α log(1 + exp( βu )) 



= exp 



− γu 2

2

  11 + exp( βu )

α

.

While this prior is still log-strongly-concave as required by the conditions of Theorem 4.1, the mean of the prior is well-separated from the mode. This creates a posterior distribution where the mean and mode as separated as well, and thus LGD performs better than GD. Formally, first note that we sample each element of each ground truth vector from this prior, w∗t(i) ∼ π1,10 ,0.1. We then construct the gradient of regularizer r(w; θ) = θ[0] w + βθ [1] 

 11+exp( −βw )



, where we treat β as a constant for simplicity. We plot the performance of LGD run with θ∗ = (0 .1, 1) with burn-in B = 5000, b = 50000 averaging steps and learning rate η = 1 e − 4, and the oracle GD run with a total of 55000 steps and η = 1 e − 4 in Figure. We see that the oracle LGD beats the oracle GD algorithm and massively out-performs the GD algorithm without regularization. We additionally plot the meta-learned LGD algorithm, where meta-learning is done using the Adam optimizer [Kingma and Ba, 2014] with learning rate 0 .4 using 50 steps, and optimize the log of θ

and η while clamping η in the range [1 e − 7, 1e − 4]. We observe that the meta-learned algorithm manages to outperform both of these algorithms by a wide margin. We hypothesize that the LGD oracle performs worse because of the poor condition number of the Hessian of the log-prior requires a higher mixing time, and the meta-learned algorithm finds a different prior with a lower mixing time. To confirm this, we plot another oracle LGD using 10 × the values of B and b and η = 1 e − 4. We see that the meta-learned algorithm performs almost at-par with the longer oracle. Thus, the meta-learned algorithm manages to perform at-par with the optimal algorithm using 1 /10th the number of iterations! As mentioned in previous Sections, none of the baselines other than gradient descent without regularization are achievable in practice (we can expect hand-tuned gradient descent to perform somewhere between GD with no regularization and the oracle GD), which illustrates the advantages of using our approach. 

# 7 Discussion and Future Work 

In this work we study the generalized setting of learning hyperparameters for gradient descent with a convex objective. We propose the Langevin Gradient Descent algorithm which performs langevin-style updates on gradient descent and uses these updates to directly estimate the outputs for test/validation samples. We show that the langevin-style sampling followed by a consolidation involving averaging predictions of the final iterates results in Bayes’ optimality of the algorithm’s estimates under mild assumptions. We additionally show generalization guarantees for finding the hyperparameters corresponding to regularisation and learning rate from multiple tasks in a data-driven setting using pseudo-dimension based arguments. We show that while our setting extends the setting of tuning elastic net significantly, our bounds match the worst-case bounds for elastic net upto logarithmic terms. We further provide experimental results on three different synthetic datasets that demonstrate the massive advantage that the proposed Langevin Gradient Descent algorithm, and meta learning provide for few-shot learning of regression with convex objectives. Exploring data-dependent bounds using Rademacher complexities is an interesting direction for future research. Another interesting direction would be to explore extensions of the algorithm for multi-modal priors, as opposed to strongly convex priors, and correspondingly extend the framework to the analysis of gradient descent on neural networks. 15 References 

Marcin Andrychowicz, Misha Denil, Sergio G´ omez, Matthew W Hoffman, David Pfau, Tom Schaul, Brendan Shillingford, and Nando de Freitas. Learning to learn by gradient descent by gradient descent. In D. Lee, M. Sugiyama, U. Luxburg, I. Guyon, and R. Garnett, editors, Advances in Neural Information Processing Systems , volume 29. Curran Associates, Inc., 2016. Martin Anthony and Peter L. Bartlett. Neural Network Learning: Theoretical Foundations . Cambridge University Press, 1999. Maria-Florina Balcan. Data-Driven Algorithm Design (book chapter). In Beyond Worst-Case Analysis of Algorithms, Tim Roughgarden (Ed) . Cambridge University Press, 2020. Maria-Florina Balcan, Mikhail Khodak, and Ameet Talwalkar. Provable guarantees for gradient-based meta-learning. In Kamalika Chaudhuri and Ruslan Salakhutdinov, editors, Proceedings of the 36th International Conference on Machine Learning , volume 97 of Proceedings of Machine Learning Research , pages 424–433. PMLR, 09–15 Jun 2019. Maria-Florina Balcan, Misha Khodak, Dravyansh Sharma, and Ameet Talwalkar. Provably tuning the Elas-ticNet across instances. In S. Koyejo, S. Mohamed, A. Agarwal, D. Belgrave, K. Cho, and A. Oh, editors, 

Advances in Neural Information Processing Systems , volume 35, pages 27769–27782. Curran Associates, Inc., 2022. Maria-Florina Balcan, Saumya Goyal, and Dravyansh Sharma. Distribution-dependent generalization bounds for tuning linear regression across tasks, 2025. Maria-Florina F Balcan, Anh Nguyen, and Dravyansh Sharma. New bounds for hyperparameter tuning of regression problems across instances. In A. Oh, T. Naumann, A. Globerson, K. Saenko, M. Hardt, and S. Levine, editors, Advances in Neural Information Processing Systems , volume 36, pages 80066–80078. Curran Associates, Inc., 2023. Peter Bartlett, Piotr Indyk, and Tal Wagner. Generalization bounds for data-driven numerical linear algebra. In Po-Ling Loh and Maxim Raginsky, editors, Proceedings of Thirty Fifth Conference on Learning Theory ,volume 178 of Proceedings of Machine Learning Research , pages 2013–2040. PMLR, 02–05 Jul 2022. Peter L. Bartlett, Nick Harvey, Christopher Liaw, and Abbas Mehrabian. Nearly-tight vc-dimension and pseudodimension bounds for piecewise linear neural networks. Journal of Machine Learning Research , 20 (63):1–17, 2019. Atilim Gunes Baydin, Robert Cornish, David Martinez Rubio, Mark Schmidt, and Frank Wood. Online learning rate adaptation with hypergradient descent. In International Conference on Learning Represen-tations , 2018. James Bergstra and Yoshua Bengio. Random search for hyper-parameter optimization. Journal of Machine Learning Research , 13(10):281–305, 2012. James Bergstra, R´ emi Bardenet, Yoshua Bengio, and Bal´ azs K´ egl. Algorithms for hyper-parameter opti-mization. In J. Shawe-Taylor, R. Zemel, P. Bartlett, F. Pereira, and K.Q. Weinberger, editors, Advances in Neural Information Processing Systems , volume 24. Curran Associates, Inc., 2011. Dan A. Calian, Gregory Farquhar, Iurii Kemaev, Luisa Zintgraf, Matteo Hessel, Jeremy Shar, Junhyuk Oh, Andr´ as Gy¨ orgy, Tom Schaul, Jeff Dean, Hado van Hasselt, and David Silver. Datarater: Meta-learned dataset curation. In The Thirty-ninth Annual Conference on Neural Information Processing Systems ,2025. 16 Tianlong Chen, Xiaohan Chen, Wuyang Chen, Howard Heaton, Jialin Liu, Zhangyang Wang, and Wotao Yin. Learning to optimize: A primer and a benchmark. Journal of Machine Learning Research , 23(189): 1–59, 2022. Hyungjin Chung, Jeongsol Kim, Michael Thompson Mccann, Marc Louis Klasky, and Jong Chul Ye. Diffu-sion posterior sampling for general noisy inverse problems. In The Eleventh International Conference on Learning Representations , 2023. Arnak Dalalyan. Further and stronger analogy between sampling and optimization: Langevin monte carlo and gradient descent. In Satyen Kale and Ohad Shamir, editors, Proceedings of the 2017 Conference on Learning Theory , volume 65 of Proceedings of Machine Learning Research , pages 678–689. PMLR, 07–10 Jul 2017. Kumar Avinava Dubey, Sashank J. Reddi, Sinead A Williamson, Barnabas Poczos, Alexander J Smola, and Eric P Xing. Variance reduction in stochastic gradient langevin dynamics. In D. Lee, M. Sugiyama, U. Luxburg, I. Guyon, and R. Garnett, editors, Advances in Neural Information Processing Systems ,volume 29. Curran Associates, Inc., 2016. Alain Durmus and ´Eric Moulines. Nonasymptotic convergence analysis for the unadjusted Langevin algo-rithm. The Annals of Applied Probability , 27(3):1551 – 1587, 2017. doi: 10.1214/16-AAP1238. Alain Durmus and ´Eric Moulines. High-dimensional Bayesian inference via the unadjusted Langevin algo-rithm. Bernoulli , 25(4A):2854 – 2882, 2019. doi: 10.3150/18-BEJ1073. Logan Engstrom, Andrew Ilyas, Benjamin Chen, Axel Feldmann, William Moses, and Aleksander Madry. Optimizing ml training with metagradient descent, 2025. URL https://arxiv.org/abs/2503.13751 .Chelsea Finn, Pieter Abbeel, and Sergey Levine. Model-agnostic meta-learning for fast adaptation of deep networks. In Doina Precup and Yee Whye Teh, editors, Proceedings of the 34th International Conference on Machine Learning , volume 70 of Proceedings of Machine Learning Research , pages 1126–1135. PMLR, 06–11 Aug 2017. Luca Franceschi, Paolo Frasconi, Saverio Salzo, Riccardo Grazzi, and Massimilano Pontil. Bilevel program-ming for hyperparameter optimization and meta-learning, 2018. URL https://arxiv.org/abs/1806. 04910 .Mark Girolami and Ben Calderhead. Riemann manifold langevin and hamiltonian monte carlo methods. 

Journal of the Royal Statistical Society: Series B (Statistical Methodology) , 73(2):123–214, 2011. Paul W. Goldberg and Mark R. Jerrum. Bounding the vapnik-chervonenkis dimension of concept classes parameterized by real numbers. Machine Learning , 18(2–3):131–148, February 1995. ISSN 0885-6125. doi: 10.1007/BF00993408. Ulf Grenander. Tutorial in pattern theory. Report, Division of Applied Mathematics , 1983. Max Jaderberg, Valentin Dalibard, Simon Osindero, Wojciech M. Czarnecki, Jeff Donahue, Ali Razavi, Oriol Vinyals, Tim Green, Iain Dunning, Karen Simonyan, Chrisantha Fernando, and Koray Kavukcuoglu. Population based training of neural networks, 2017. URL https://arxiv.org/abs/1711.09846 .Mikhail Khodak, Maria-Florina F Balcan, and Ameet S Talwalkar. Adaptive gradient-based meta-learning methods. In H. Wallach, H. Larochelle, A. Beygelzimer, F. d 'Alch´ e-Buc, E. Fox, and R. Garnett, editors, 

Advances in Neural Information Processing Systems , volume 32. Curran Associates, Inc., 2019. Diederik P. Kingma and Jimmy Ba. Adam: A method for stochastic optimization. CoRR , abs/1412.6980, 2014. 17 Lisha Li, Kevin Jamieson, Giulia DeSalvo, Afshin Rostamizadeh, and Ameet Talwalkar. Hyperband: A novel bandit-based approach to hyperparameter optimization. Journal of Machine Learning Research , 18(185): 1–52, 2018. Risheng Liu, Jiaxin Gao, Jin Zhang, Deyu Meng, and Zhouchen Lin. Investigating bi-level optimization for learning and vision from a unified perspective: A survey and beyond. IEEE Transactions on Pattern Analysis and Machine Intelligence , 44(12):10045–10067, 2022. doi: 10.1109/TPAMI.2021.3132674. Yi-An Ma, Tianqi Chen, and Emily Fox. A complete recipe for stochastic gradient mcmc. In C. Cortes, N. Lawrence, D. Lee, M. Sugiyama, and R. Garnett, editors, Advances in Neural Information Processing Systems , volume 28. Curran Associates, Inc., 2015. Dougal Maclaurin, David Duvenaud, and Ryan Adams. Gradient-based hyperparameter optimization through reversible learning. In Francis Bach and David Blei, editors, Proceedings of the 32nd Inter-national Conference on Machine Learning , volume 37 of Proceedings of Machine Learning Research , pages 2113–2122, Lille, France, 07–09 Jul 2015. PMLR. Meta AI. The llama 3 herd of models, 2024. Christopher Nemeth and Paul Fearnhead. Stochastic gradient markov chain monte carlo. Journal of the American Statistical Association , 116(533):433–450, 2021. doi: 10.1080/01621459.2020.1847120. Yurii Nesterov. Lectures on Convex Optimization . Springer Optimization and Its Applications. Springer, Cham, Switzerland, 2018. Alex Nichol, Joshua Achiam, and John Schulman. On first-order meta-learning algorithms, 2018. URL 

https://arxiv.org/abs/1803.02999 .G Parisi. Correlation functions and computer simulations, May 1981. B. T. Polyak and A. B. Juditsky. Acceleration of stochastic approximation by averaging. SIAM Journal on Control and Optimization , 30(4):838–855, 1992. doi: 10.1137/0330046. Maxim Raginsky, Alexander Rakhlin, and Matus Telgarsky. Non-convex learning via stochastic gradient langevin dynamics: a nonasymptotic analysis. In Satyen Kale and Ohad Shamir, editors, Proceedings of the 2017 Conference on Learning Theory , volume 65 of Proceedings of Machine Learning Research , pages 1674–1703. PMLR, 07–10 Jul 2017. Aravind Rajeswaran, Chelsea Finn, Sham Kakade, and Sergey Levine. Meta-learning with implicit gradients, 2019. URL https://arxiv.org/abs/1909.04630 .Shai Shalev-Shwartz and Shai Ben-David. Understanding Machine Learning: From Theory to Algorithms .Cambridge University Press, 2014. Jasper Snoek, Hugo Larochelle, and Ryan P Adams. Practical bayesian optimization of machine learning algorithms. In F. Pereira, C.J. Burges, L. Bottou, and K.Q. Weinberger, editors, Advances in Neural Information Processing Systems , volume 25. Curran Associates, Inc., 2012. Yang Song and Stefano Ermon. Generative modeling by estimating gradients of the data distribution. In H. Wallach, H. Larochelle, A. Beygelzimer, F. d 'Alch´ e-Buc, E. Fox, and R. Garnett, editors, Advances in Neural Information Processing Systems , volume 32. Curran Associates, Inc., 2019. Yang Song, Jascha Sohl-Dickstein, Diederik P Kingma, Abhishek Kumar, Stefano Ermon, and Ben Poole. Score-based generative modeling through stochastic differential equations. In International Conference on Learning Representations , 2021. Larry Wasserman. All of Statistics . Springer, New York, 2010. 18 Max Welling and Yee Whye Teh. Bayesian learning via stochastic gradient langevin dynamics. In Proceedings of the 28th International Conference on Machine Learning (ICML-11) , pages 681–688. Omnipress, 2011. Pan Xu, Jinghui Chen, Difan Zou, and Quanquan Gu. Global convergence of langevin dynamics based algorithms for nonconvex optimization. In S. Bengio, H. Wallach, H. Larochelle, K. Grauman, N. Cesa-Bianchi, and R. Garnett, editors, Advances in Neural Information Processing Systems , volume 31. Curran Associates, Inc., 2018. 

# A Convergence Guarantees on Langevin Algorithms 

We recall the Unadjusted Langevin Algorithm (ULA) from Section 2. Starting with a sample Z(0) , ULA constitutes the following iterative updates: 

Z(k+1) = Z(k) − ηk+1 ∇U + p2ηk+1 ξk; ξk ∼ N (0 , I).ηk represent step-sizes for each step of the ULA. The homoegeneous ULA uses the same step size for all steps, resulting in the update rule: 

Z(k+1) = Z(k) − η∇U + p2ηξ k; ξk ∼ N (0 , I).

We will use results from [Durmus and Moulines, 2019] which provide a detailed analysis of homogeneous and inhomogeneous ULA. We recall the following two assumptions: 

Assumption A.1. The function U is continuously differentiable on Rd and gradient Lipschitz: there exists 

L ≥ 0 such that for all z1, z 2 ∈ Rd, ∥∇ U (z1) − ∇ U (z2)∥ ≤ L∥z1 − z2∥.

Assumption A.2. U is strongly convex, i.e. there exists m > 0 such that for all z1, z 2 ∈ Rd,

U (z2) ≥ U (z1) + ⟨∇ U (z1), z 2 − z1⟩ + ( m/ 2) ∥z1 − z2∥2.

Note that for both of these assumptions to hold, m ≤ L. In the following, assume κ = 2Lm L+m , and the sequence of step-sizes ηk is non-increasing with η1 ≤ 1/(m + L). Define the distribution of z(k) as πk. We get the following results: 

Lemma A.1 (Thm 5, [Durmus and Moulines, 2019]) . Under the assumptions A.1 and A.2, assuming we start the ULA from π0 = δz for some z ∈ Rd, we get that for any k ≥ 1,

W 22 (πk, π ) ≤ u(1)  

> k

(η){∥ z − z∗∥2 + d/m } + u(2)  

> k

(η),

where, 

u(1)  

> k

(η) = 2 

> k

Y

> i=1

(1 − κη k/2) ,

and, 

u(2)  

> k

(η) = L2d

> k

X

> i=1

η2 

> i

{κ−1 + ηi}



2 + L2ηi

m + L2η2

> i

6

 kY

> j=i+1

(1 − κη j /2) 

 .

We denote by z∗, the unique minima of the potential function U .

We consider the following proposition to derive an asymptotic bound on wasserstein distance, for a homo-geneous ULA: 19 Proposition A.2 (Restated Proposition 2.9) . Under the assumptions 1 and 2, assuming we start the ho-moegeneous ULA from π0 = δz for some z ∈ Rd, we get that for any k ≥ 1,

W2(πk, π ) ≤ ϵ, 

using η = O(ϵ2/d ) and k = Ω 

 dϵ2 log 

 d+∥z−z∗∥2

> ϵ2

 

, where we hide constants that depend on m, L . Here z∗

is the unique minimizer of the potential function U .Proof. We simplify terms from Lemma A.1: 

u(1)  

> k

(η) = 2 

> k

Y

> i=1

(1 − κη k/2) = 2(1 − κη/ 2) k

≤ 2 exp( − kκη 

2 ).

And, 

u(2)  

> k

(η) = L2d

> k

X

> i=1

η2 

> i

{κ−1 + ηi}



2 + L2ηi

m + L2η2

> i

6

 kY

> j=i+1

(1 − κη j /2) 



= L2d

> k

X

> i=1



η2{κ−1 + η}



2 + L2ηm + L2η2

6



(1 − κη/ 2) (k−i)



= L2dη 2{κ−1 + η}



2 + L2ηm + L2η2

6

 1 − (1 − κη/ 2) k

κη/ 2

≤ 2 L2dη κ {κ−1 + η}



2 + L2ηm + L2η2

6



= L2dη m2

(m + L)

L

 m + L

2L + mη 

  

2 + L2ηm + L2η2

6



≤ 2 L2dη m2 {1 + mη }



2 + L2ηm + L2η2

6



(since m ≤ L)

≤ const. × dη (L2/m 2 + L4/m 4) ≤ const. × dηL 4/m 4.

Where in the last step we set η ≤ 1/(m + L) =⇒ η ≤ 1/m . We want u(2)  

> k

(η) < ϵ 2/2, which happens with η ≤ const. × m4ϵ2/(dL 4). Finally, we want u(1)  

> k

(η){∥ z − z∗∥2 + d/m } ≤ ϵ2/2, which we get with 

k = Ω 

 dϵ2 log 

 d+∥x−x∗∥2

> ϵ2

 

, where we hide constants that depend on m and L.We will be analyzing MCMC using samples from ULA. Define ∥g∥Lip to be the Lipschitz constant of g,and the empirical mean starting from the Bth iterate of homogeneous ULA as follows: ˆπBb (g) = 1

b

> b+B

X

> i=B+1

g(Xi). (3) We also define the expected value of g as, π(g) = Eπ [g]. The following result on convergence of empirical mean will be crucial to our analysis of MCMC. 

Theorem A.3 (Thm 15, 17, [Durmus and Moulines, 2019]) . Under the assumptions A.1 and A.2, for all 

B ≥ 0, b ≥ 1, r > 0 and Lipschitz functions g : Rd → R:

V ar (ˆ πBb (g)) ≤

8∥g∥2

> Lip



1 + (κ−1+2 /(m+L)) 

> bη



κ2bη ,

20 and, 

P r (ˆ πBb (g) − E[ˆ πBb (g)] ≥ r) ≤ exp 

− r2κ2bη 

16 ∥g∥2

> Lip



1 + (κ−1+2 /(m+L)) 

> bη

 .

Finally, we are interested in the gap between E[ˆ πBb (g)] and π(g). We recall the following analysis from Section 4 of Durmus and Moulines [2019], 

{E[ˆ πBb (g)] − π(g)}2 ≤ ∥g∥2

> Lip

b

> b+B

X

> i=B+1

W 22 (πi, π )

≤ ∥ g∥2

> Lip

W 22 (πB , π )We combine these results into the following proposition, which bounds the number of iterations and step-size for constant error in empirical mean with high probability. 

Proposition A.4 (Restated Proposition 2.10) . If we run a homogeneous ULA from π0 = δz for some 

z ∈ Rd with potential function U that satisfies the assumptions A.1 and A.2 and η = O(ϵ2/(∥g∥2

> Lip

d)) ,

B = Ω 

 d∥g∥2 

> Lip
> ϵ2

log 

 (d+∥z−z∗∥2)∥g∥2

> Lip
> ϵ2

 

and b that satisfies bη ≥ 128 ∥g∥2  

> Lip log(1 /δ )
> ϵ2κ2

,

V ar (ˆ πBb ) ≤ ϵ2

8 log(1 /δ ) and, 

|ˆπBb (g) − π(g)| ≤ ϵ w.p. ≥ 1 − δ. 

Proof. From the definition of the Wasserstein distance, W 22 (πi, π ) = inf γ∈Π( πi,π ) E(x,y )∼γ [∥x − y∥2], for Π being the set of all couplings. In the following we assume the optimal coupling γi between πi and π such that W 22 (πi, π ) = E(x,y )∼γi [∥x − y∥2], which allows us to upper bound |E[ˆ πBb (g)] − π(g)| as follows: 

{E[ˆ πBb (g)] − π(g)}2 =

(

1

b

> B+b

X

> k=B+1

Eπ,π k [g(z(k)) − g(z)] 

)2

≤

(

∥g∥Lip 

b

> B+b

X

> k=B+1

E[z(k) − z]

)2

≤ ∥g∥2

> Lip

b2

> B+b

X

> k=B+1

E[z(k) − z]2

≤ ∥g∥2

> Lip

b

> B+b

X

> i=B+1

W 22 (πi, π )

≤ ∥ g∥2

> Lip

W 22 (πB , π )=⇒ | E[ˆ πBb (g)] − π(g)| ≤ ∥ g∥Lip W2(πB , π ).

We know from Proposition 2.9 that W2(πB , π ) ≤ ϵ for η = O(ϵ2/d ) and B = Ω 

 dϵ2 log 

 d+∥z−z∗∥2

> ϵ2

 

. Thus, 

|E[ˆ πBb (g)] −π(g)| ≤ ϵ/ 2 if η = O(ϵ2/(∥g∥2

> Lip

d)) and B = Ω 

 d∥g∥2 

> Lip
> ϵ2

log 

 (d+∥z−z∗∥2)∥g∥2

> Lip
> ϵ2

 

, which completes the first part of the proof. Next, we need to bound ˆ πBb (g) − E[ˆ πBb (g)]. For the derived value of η = O(ϵ2/(∥g∥2

> Lip

d)) and a b that satisfies bη ≥ 128 ∥g∥2  

> Lip log(1 /δ )
> ϵ2κ2

, we have that bη ≫ 1 resulting in V ar (ˆ πBb ) ≤ ϵ2 

> 8 log(1 /δ )

using Theorem A.3. Theorem A.3 would thus also imply |ˆπBb (g) − E[ˆ πBb (g)] | ≤ ϵ/ 2 with probability ≥ 1 − δ, which finishes the proof. 21 Remark A.5. We use the above Proposition to prove Theorem 4.1, where we replace g(.) with g(Xv ; .), such that ˆπBb is equivalent to ˆgv and the distribution π is equivalent to the posterior distribution w|X, y .

22