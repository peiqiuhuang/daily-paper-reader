---
title: Generalizable Self-Evolving Memory for Automatic Prompt Optimization
title_zh: 面向自动提示词优化的可泛化自演进记忆框架
authors: "Guanbao Liang, Yuanchen Bei, Sheng Zhou, Yuheng Qin, Huan Zhou, Bingxin Jia, Bin Li, Jiajun Bu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21520v1"
tags: ["query:bopl"]
score: 10.0
evidence: 用于自动提示优化的记忆驱动框架
tldr: 针对现有自动提示优化方法难以跨任务泛化且无法积累经验的问题，本文提出MemAPO框架。该框架通过双重记忆机制，将成功的推理轨迹提炼为策略模板，并将错误生成整理为失败模式。在处理新任务时，它能检索相关经验以优化提示词，并通过持续的自我反思与记忆编辑实现能力的自我进化，显著提升了优化效率与泛化性能。
selection_source: fresh_fetch
motivation: 现有的提示优化方法通常局限于特定任务的搜索，缺乏跨异构查询的泛化能力以及长期积累可复用知识的机制。
method: 提出一种双重记忆驱动的框架，通过提炼成功策略和总结错误模式来构建可进化的知识库，并利用检索增强生成来优化提示词。
result: 实验表明MemAPO在多个多样化基准测试中均优于现有基准方法，并显著降低了提示优化的计算成本。
conclusion: 该研究通过将提示优化重新定义为可泛化的经验积累过程，为实现大语言模型的持续自我进化提供了有效路径。
---

## 摘要
自动提示词优化是将大语言模型（LLMs）适配到下游任务的一种极具前景的方法，然而现有方法通常是针对固定任务搜索特定的提示词。这种范式限制了在异构查询之间的泛化能力，并阻碍了模型随时间积累可重用的提示词知识。在本文中，我们提出了 MemAPO，这是一个记忆驱动的框架，它将提示词优化重新定义为可泛化且自演进的经验积累过程。MemAPO 维护一种双重记忆机制，将成功的推理轨迹提炼为可重用的策略模板，同时将错误的生成结果组织成结构化的错误模式，以捕捉循环出现的失败模式。给定一个新的提示词，该框架会检索相关的策略和错误模式，以构建既能促进有效推理又能避免已知错误的提示词。通过迭代的自我反思和记忆编辑，MemAPO 持续更新其记忆，使提示词优化能够随时间不断改进，而不是针对每个任务都从零开始。在多种基准测试上的实验表明，MemAPO 一贯优于代表性的提示词优化基准方法，同时显著降低了优化成本。

## Abstract
Automatic prompt optimization is a promising approach for adapting large language models (LLMs) to downstream tasks, yet existing methods typically search for a specific prompt specialized to a fixed task. This paradigm limits generalization across heterogeneous queries and prevents models from accumulating reusable prompting knowledge over time. In this paper, we propose MemAPO, a memory-driven framework that reconceptualizes prompt optimization as generalizable and self-evolving experience accumulation. MemAPO maintains a dual-memory mechanism that distills successful reasoning trajectories into reusable strategy templates while organizing incorrect generations into structured error patterns that capture recurrent failure modes. Given a new prompt, the framework retrieves both relevant strategies and failure patterns to compose prompts that promote effective reasoning while discouraging known mistakes. Through iterative self-reflection and memory editing, MemAPO continuously updates its memory, enabling prompt optimization to improve over time rather than restarting from scratch for each task. Experiments on diverse benchmarks show that MemAPO consistently outperforms representative prompt optimization baselines while substantially reducing optimization cost.

---

## 论文详细总结（自动生成）

### 论文总结：面向自动提示词优化的可泛化自演进记忆框架 (MemAPO)

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLMs）的性能高度依赖于提示词（Prompt）的设计。现有的自动提示优化（APO）方法（如基于文本梯度或进化算法）通常将优化视为针对**特定固定任务**的搜索过程。这种范式存在两个主要缺陷：
*   **缺乏泛化性**：针对某一任务优化的提示词难以直接应用于其他异构查询。
*   **无法积累经验**：每次新任务都需要从零开始搜索，无法像人类一样通过历史经验（成功策略或失败教训）来持续改进。
**MemAPO** 的核心动机是将提示词优化从“孤立的搜索”转变为“持续的经验积累与自演进”，使模型能够跨任务复用知识并降低优化成本。

#### 2. 方法论：核心思想与关键技术
MemAPO 提出了一个**双重记忆驱动框架**，将提示词建模为通用指令与查询相关记忆的组合：$p = [p_{Inst}, p_{Mem}(q)]$。

*   **核心思想**：通过维护一个动态更新的知识库，存储“怎么做是对的”（成功策略）和“怎么做会错”（错误模式），在面对新查询时按需检索并合成最优提示词。
*   **关键技术细节**：
    1.  **双重记忆机制**：
        *   **正确模板记忆 (CTM)**：存储成功的推理轨迹，提炼为“索引（适用场景）+ 策略（推理步骤）+ 案例（示例）”。
        *   **错误模式记忆 (EPM)**：将失败案例抽象为高层的反思规则，用于提供负面约束。
    2.  **三个运行阶段**：
        *   **记忆检索**：基于语义相似度从 CTM 检索 Top-K 策略，并提取所有相关的 EPM 规则。
        *   **提示词评估**：构建增强提示词，利用**自我反思机制**进行多轮尝试（最多 $z$ 次），直到获得正确答案或达到上限。
        *   **记忆更新**：根据尝试结果，对记忆库执行 `ADD`（新增）、`UPDATE`（精炼）、`DELETE`（删除矛盾项）或 `MERGE`（合并冗余项）操作。

#### 3. 实验设计
*   **数据集/场景**：涵盖三大领域共 6 个任务：
    *   **逻辑推理**：BBH-Geometric Shapes, BBEH-Geometric Shapes（更难的几何形状识别）。
    *   **数学计算**：AGIEval-AQuA-RAT, AGIEval-Gaokao MathQA。
    *   **知识密集型**：AGIEval-Gaokao Geography, AGIEval-Gaokao History。
*   **对比方法 (Baselines)**：
    *   **APO 类**：ProTeGi, OPRO, TextGrad, PromptBreeder, SPO。
    *   **传统提示词类**：IO (直接推理), CoT (思维链), Step-Back, RaR (重述并回答)。
*   **实验设置**：分别在 **GPT-4o-mini** 和 **Qwen3-8B** 上作为目标模型和优化器进行测试。

#### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号和训练时长。
*   **成本信息**：作者提到所有实验通过官方 API 完成，总 API 预算控制在 **1000 美元以内**。
*   **实现细节**：使用了 Linux 系统，向量存储基于开源的 **ChromaDB**，嵌入模型使用 Qwen3-Embedding-8B。

#### 5. 实验数量与充分性
*   **实验规模**：在 2 个不同架构的模型（闭源轻量级 vs 开源中量级）上进行了 6 个任务的完整对比。
*   **消融实验**：
    *   验证了 CTM 和 EPM 各自的贡献（两者结合效果最好）。
    *   验证了“自演进”范式的有效性（对比了使用更强模型如 GPT-5 作为优化器的效果）。
    *   分析了检索模板数量（Top-K）对性能与 Token 消耗的影响。
*   **客观性与公平性**：实验采用了统一的测试集划分，并对比了优化成本（API 调用次数和 Token 数），证明了 MemAPO 在提升性能的同时显著降低了开销（比 TextGrad 降低约 57%），实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **性能领先**：MemAPO 在所有基准测试中均优于代表性 APO 方法，在 GPT-4o-mini 上平均准确率达到 70.7%（比最强基准 TextGrad 高出 7.1%）。
*   **高性价比**：通过经验复用，MemAPO 大幅减少了优化所需的 API 调用次数和 Token 消耗，使其更适合大规模部署。
*   **强泛化性**：在跨领域混合任务实验中，传统方法因提示词冲突导致性能下降，而 MemAPO 凭借动态检索机制保持了性能稳定。

#### 7. 优点
*   **范式创新**：将提示词优化从“参数搜索”视角转向“知识管理”视角，更符合人类的学习逻辑。
*   **双重约束**：同时利用正向策略和负向错误模式，有效减少了 LLM 的幻觉和重复错误。
*   **效率极高**：推理阶段只需单次生成（无需优化时的反思循环），且优化阶段的经验可跨任务摊销成本。

#### 8. 不足与局限
*   **模态限制**：目前仅限于文本推理任务，尚未扩展到多模态（视觉、音频）环境。
*   **检索依赖**：框架的有效性高度依赖于向量检索的准确性，若检索到无关经验可能引入噪声。
*   **复杂性管理**：随着记忆库规模增长，如何保持记忆的紧凑性（虽然有合并机制）和处理冲突知识仍是长期运行中的挑战。

（完）
