---
title: Parameter-efficient Prompt Tuning of Vision Foundation Model With Adaptive Focal Loss for Interpretable MCI Screening
title_zh: 基于自适应 Focal Loss 的视觉基础模型参数高效提示微调，用于可解释的 MCI 筛查
authors: "Javad Khoramdel, Farhad Hoseyni, Amirhossein Nikoofard"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15047v1"
tags: ["query:bopl"]
score: 9.0
evidence: 利用可学习提示词令牌的参数高效框架
tldr: 本研究针对轻度认知障碍（MCI）筛查中数据稀缺和诊断模糊的问题，提出了一种基于冻结DINOv2视觉大模型的参数高效提示微调框架。通过引入可学习的提示标记和交叉注意力机制，实现了仅需119万参数的内生空间可解释性。结合融入MoCA评分的自适应焦点损失函数，该方法在保持低计算成本的同时，显著提升了MCI识别的准确率和模型透明度。
selection_source: fresh_fetch
motivation: 传统的MCI自动检测方法面临数据稀缺、类别不平衡及模型缺乏内在可解释性等挑战，且全量微调计算成本高昂。
method: 采用冻结的DINOv2模型配合可学习提示标记进行参数高效微调，并设计了融合认知评分的自适应焦点损失函数。
result: 在五折交叉验证中，该模型在MCI类别的F1分数达到0.641，AUC为0.795，性能显著优于计算量更大的ResViT基准模型。
conclusion: 该框架证明了通过参数高效的提示微调和任务相关的损失函数优化，可以在降低计算开销的同时实现高精度且具可解释性的医学影像筛查。
---

## 摘要
轻度认知障碍（MCI）是认知能力下降的关键早期阶段，通常发生在阿尔茨海默病之前。然而，利用神经心理学绘图测试进行自动化检测，在根本上仍受限于数据稀缺、类别不平衡以及临床边界附近的诊断模糊性。现有方法试图通过计算昂贵、全量微调的混合架构来绕过这些限制，但这将空间可解释性归结为事后近似，而非模型的内在属性。我们提出了一个参数高效的框架，利用冻结的 DINOv2-Small 模型，通过三个模态特定的可学习提示标记（prompt tokens）进行适配。该框架仅包含 119 万个可训练参数，每个标记在源图像块标记（patch tokens）的共享交叉注意力层中充当查询（query）。至关重要的是，空间可解释性是作为架构的结构性结果，直接通过这些注意力图实现的。随后，通过注意力模块融合任务条件嵌入，以量化每个受试者的模态级重要性。为了处理边界模糊性，引入了一种适配 MoCA 的 Focal Loss，将连续认知得分整合到训练目标、损失调制和自适应样本加权中，严格推广了标准的软标签方法。在分层五折交叉验证下，所提架构的 MCI 类别 F1 分数为 0.641，AUC 为 0.795，在 MCI 类别 F1 分数上比计算量更大的 ResViT 基准模型高出 0.110。

## Abstract
Mild Cognitive Impairment is a critical early stage of cognitive decline that frequently precedes Alzheimer's disease, yet its automated detection from neuropsychological drawing tests remains fundamentally constrained by data scarcity, class imbalance, and diagnostic ambiguity near clinical boundaries. Existing methodologies attempt to bypass these constraints using computationally expensive, fully fine-tuned hybrid architectures that relegate spatial explainability to a post-hoc approximation rather than an intrinsic model property. We propose a parameter-efficient framework utilizing frozen DINOv2-Small model adapted via three modality-specific learnable prompt tokens while Operating with 1.19 million trainable parameters, each token serves as a query in a shared cross-attention layer over the source image patch tokens. Crucially, spatial explainability is achieved directly through these attention maps; as a structural consequence of the architecture. Then task-conditioned embeddings fused via an attention module to quantify modality-level importance per subject. To handle boundary ambiguity, a MoCA-adapted focal loss introduced that integrates continuous cognitive scores into the training target, loss modulation, and adaptive sample weighting, strictly generalizing standard soft-label approaches. Under stratified five-fold cross-validation, the proposed architecture yields an MCI-class F1 of 0.641 and an AUC of 0.795, outperforming the computationally heavier ResViT baseline by 0.110 in MCI-class F1.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于视觉基础模型（Vision Foundation Model）的参数高效微调框架，专门用于通过神经心理学绘图测试筛查轻度认知障碍（MCI）。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：轻度认知障碍（MCI）是阿尔茨海默病的早期预警信号。传统的自动化筛查方法（如基于时钟绘图测试）面临三大挑战：
    1.  **数据稀缺与不平衡**：医疗数据集通常规模较小且正负样本比例失调。
    2.  **诊断边界模糊**：临床上常以 MoCA 评分 25 分为界，但 24 分与 26 分的受试者在表现上往往具有高度相似性，硬分类标签忽略了这种不确定性。
    3.  **缺乏可解释性**：现有模型多为黑盒，或依赖事后近似（如 Grad-CAM），无法提供内在的诊断依据。
*   **核心目标**：开发一个计算廉价、参数高效且具有内在可解释性的多模态融合模型，利用基础模型的强大表征能力解决 MCI 筛查难题。

### 2. 方法论
该框架主要由三个核心组件构成：
*   **参数高效特征提取（PEFT）**：
    *   使用冻结的 **DINOv2-Small** 作为骨干网络。
    *   引入**任务特定提示标记（Task-Specific Prompt Tokens）**：为时钟（CDT）、立方体（CCT）和连线（TMT）三种测试分别设计可学习的 Prompt Token。
    *   **交叉注意力聚合**：Prompt Token 作为 Query，与 DINOv2 输出的图像 Patch Tokens 进行交叉注意力运算，提取任务相关的特征嵌入。
*   **多任务融合模块**：
    *   采用**可学习查询注意力（Learnable Query Attention）**机制，自动学习三个绘图模态在最终决策中的权重（$\alpha$），实现受试者级别的模态重要性量化。
*   **MoCA 适配的 Focal Loss**：
    *   **软标签（Soft Anchor）**：将连续的 MoCA 分数通过 Sigmoid 函数转化为 0 到 1 之间的软目标，缓解硬阈值带来的边界模糊。
    *   **MoCA 概率调制器**：设计了一种新型损失调制项 $|s_i - p_i|^b$，专门惩罚模型预测概率与 MoCA 预期得分之间的偏差。
    *   **自适应样本加权**：根据 MoCA 分数分布的频率进行加权，修正类别不平衡和分数分布倾斜。
*   **增强策略**：引入了图像反转、类别平衡采样、类型保留 Mixup 以及基于 MoCA 邻居的绘图交换（Drawing Swap）等策略。

### 3. 实验设计
*   **数据集**：使用来自泰国曼谷朱拉隆功纪念医院的公开数据集，包含 918 名受试者（651 名健康对照，267 名 MCI 患者），每人提供三张绘图（共 2,754 张图像）。
*   **基准模型（Benchmark）**：
    *   **ResViT**：一种结合了 ResNet50 和 ViT-B/16 的重型混合架构。
    *   **软标签 BCE**：之前研究中使用的标准软标签方法。
*   **对比实验**：
    *   对比了不同的 PEFT 策略（Top Tuning, LoRA, Prompt Tuning）。
    *   对比了不同的 Prompt 设计（共享 Token vs 任务特定 Token）。
    *   进行了详尽的消融实验，验证 Loss 组件和增强策略的有效性。
*   **评估协议**：采用分层五折交叉验证，以 MCI 类别的 F1 分数作为主要评价指标。

### 4. 资源与算力
*   **参数量**：模型总参数量较小，可训练参数仅为 **119 万**（约占总参数的 6%）。
*   **训练细节**：Batch size 为 4，使用 AdamW 优化器，训练 40 个 Epoch，包含线性预热和余弦退火。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和具体训练时长。但从模型规模（DINOv2-Small）和 Batch size 来看，该模型对算力要求极低，可在消费级 GPU 上轻松运行。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括 8 组训练策略的独立贡献分析、3 组损失函数组件的消融实验、3 组 Prompt 设计对比以及 4 组 PEFT 策略对比。
*   **充分性与公平性**：
    *   使用了固定的随机种子和分层交叉验证，确保了结果的可重复性和客观性。
    *   通过与重实现后的 ResViT 在相同条件下对比，证明了所提轻量级架构的优越性。
    *   实验覆盖了从架构设计到损失函数优化的全过程，逻辑闭环完整。

### 6. 主要结论与发现
*   **性能突破**：全系统配置下，MCI 类别 F1 达到 **0.641**，AUC 达到 **0.795**，在 F1 指标上比 ResViT 高出 **0.110**。
*   **PEFT 优越性**：Prompt Tuning 在参数量极少的情况下，性能优于参数量更多的 LoRA，说明在小规模医学绘图数据上，修改输入空间比修改模型内部权重更有效。
*   **可解释性价值**：通过交叉注意力图可以清晰看到模型关注的绘图区域（如连线测试中的错误连接点），模态权重 $\alpha$ 则揭示了不同认知任务对个体诊断的贡献度。
*   **损失函数作用**：MoCA 概率调制器被证明是提升 F1 分数最关键的组件，其贡献超过了传统的软标签方法。

### 7. 优点
*   **内在可解释性**：空间注意力和模态权重是架构的直接产物，无需额外计算，具有极高的临床参考价值。
*   **参数效率极高**：通过冻结大模型并仅微调极少量参数，解决了医疗领域小样本训练难的问题。
*   **临床相关性强**：损失函数设计紧扣 MoCA 评分这一临床金标准，充分考虑了诊断的不确定性。

### 8. 不足与局限
*   **地理与人口学偏差**：数据集仅来自泰国单一机构，模型在不同文化背景或教育程度人群中的泛化性有待验证。
*   **静态图像限制**：仅处理绘图的最终静态图像，丢失了绘图过程中的动态运动学特征（如笔尖压力、速度、停顿等），这些特征在 MCI 诊断中同样重要。
*   **缺乏专家验证**：虽然展示了注意力图，但尚未通过临床专家对这些热力图进行系统性的定性评估和对齐。

（完）
