---
title: "GRADRAG: Cross-Component Prompt Adaptation for Coordinated Multi-Agent RAG"
title_zh: GRADRAG：协同多智能体 RAG 的跨组件提示词适配
authors: "Paolo Pedinotti, Enrico Santus"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21324v1"
tags: ["query:bopl"]
score: 9.0
evidence: 多智能体RAG的迭代提示词自适应与优化
tldr: 针对多智能体RAG系统各组件优化孤立的问题，本文提出GRADRAG框架。该框架将RAG流程建模为计算图，通过评估器对下游输出的反馈来迭代更新上游智能体（如检索器、图构建器）的提示词。实验表明，这种跨组件的协同自适应机制在SQUALITY等基准上显著优于传统单步优化方法，提升了系统的整体协调性与生成质量。
selection_source: fresh_fetch
motivation: 现有的多智能体RAG系统通常孤立地优化各个组件，缺乏跨组件的协同优化机制来提升整体性能。
method: 将RAG流程建模为计算图，利用评估器生成的结构化反馈，通过提示词优化器迭代更新上游智能体的提示词并支持提前停止。
result: 在SQUALITY和QMSUM基准测试中，GRADRAG相比单步优化基准实现了12-15个百分点的净偏好优势，且大部分提升在两次迭代内完成。
conclusion: 跨组件的提示词自适应是提升多智能体RAG系统协同能力的有效途径，能显著增强复杂检索与生成任务的表现。
---

## 摘要
检索增强生成 (RAG) 系统越来越多地采用多个大语言模型 (LLM) 智能体。然而，先前的大多数工作都是孤立地优化各个组件，而不是在整个流水线中协调改进。我们引入了 GRADRAG，这是一个用于跨组件提示词适配的框架，它将 RAG 流水线建模为计算图，并传播结构化的评估反馈以更新上游智能体。评估器 (Evaluator) 对下游答案和支持证据进行评判，生成可操作的反馈，提示词优化器 (Prompt Optimizer) 利用这些反馈迭代更新自适应智能体，如检索器、图构建器和回答器。当输出被认为令人满意时，评估器还会触发提前停止。我们在 SQUALITY 和 QMSUM 基准测试上评估了 GRADRAG，涵盖两种检索范式：使用 IRCoT 风格查询细化的扁平块检索 (Trivedi et al., 2023)，以及从文档中构建并迭代丰富实体关系图的基于图的检索。在这两种设置下，GRADRAG 始终优于仅更新最终生成器的单步细化基线，在 LLM 评判的两两比较中实现了 12-15 个百分点的净偏好优势，且大部分收益在两次细化迭代内即可实现。

## Abstract
Retrieval-Augmented Generation (RAG) systems increasingly employ multiple LLM agents. Yet, most prior work optimizes components in isolation rather than coordinating improvements across the pipeline. We introduce GRADRAG, a framework for cross-component prompt adaptation that models the RAG pipeline as a computational graph and propagates structured evaluation feedback to update upstream agents. An Evaluator critiques downstream answers and supporting evidence, producing actionable feedback that a Prompt Optimizer uses to iteratively update adaptive agents, such as retrievers, graph constructors, and answerers. The Evaluator also triggers early stopping when the output is deemed satisfactory. We evaluate GRADRAG on the SQUALITY and QMSUM benchmarks under two retrieval paradigms: flat chunk-based retrieval using IRCoT-style query refinement (Trivedi et al., 2023), and graph-based retrieval that constructs and iteratively enriches an entity-relation graph from the document. Across both settings, GRADRAG consistently outperforms one-step refinement baselines that update only the final generator, achieving a 12-15 percentage point net preference margin in LLM-judged pairwise comparisons, with most gains realized within two refinement iterations.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **GRADRAG**，一个旨在解决多智能体检索增强生成（RAG）系统中组件间缺乏协同优化问题的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现代 RAG 系统已演变为由多个 LLM 智能体（如检索器、重写器、生成器）组成的复杂流水线。然而，现有方法通常**孤立地优化**各个组件（例如仅进行查询重写或答案修正）。
*   **核心问题**：流水线早期的错误（如检索不全、证据结构差）会向下游传播，而下游的评估信号很少能反馈给上游组件。
*   **整体含义**：论文提出了一种“跨组件提示词适配”机制，通过将 RAG 建模为计算图，利用下游的评估反馈来迭代更新上游智能体的提示词，从而实现全系统的协同改进。

### 2. 方法论
*   **核心思想**：借鉴“文本梯度”（Textual Gradients）的概念，将评估器对最终答案的批评意见转化为对上游智能体（如检索代理、图提取代理）提示词的修改指令。
*   **关键技术细节**：
    *   **计算图抽象**：将 RAG 流程建模为节点（智能体/程序）和边（数据流）。
    *   **评估器（Evaluator）**：分析答案及其支持证据，识别缺失信息或逻辑漏洞，生成结构化反馈，并决定是否“提前停止”（Early Stopping）。
    *   **提示词优化器（Prompt Optimizer）**：将反馈转化为具体的提示词更新建议。
    *   **两种范式实现**：
        1.  **Vector RAG**：自适应更新检索代理的查询生成策略。
        2.  **GraphRAG**：自适应更新图提取代理的本体和提取规则，以构建更符合查询需求的实体关系图。
*   **算法流程**：前向传播生成答案 -> 评估器提供反馈 -> 若不满意，优化器更新相关智能体提示词 -> 带着新提示词进入下一轮迭代。

### 3. 实验设计
*   **数据集/场景**：
    *   **SQUALITY**：长文档叙事文本摘要。
    *   **QMSUM**：长会议纪要查询式摘要。
*   **Benchmark 与对比方法**：
    *   **对比基准**：单步细化（One-step refinement）基线，即仅允许反馈更新最终的答案生成器，而保持上游组件（检索/图构建）固定。
    *   **实验变体**：分别在扁平向量检索（IRCoT 风格）和基于图的检索（GraphRAG）两种架构下进行对比。
*   **评估方式**：采用 LLM-as-a-Judge（使用 DeepSeek-V3.1）进行两两偏好比较，并辅以人工验证。

### 4. 资源与算力
*   **模型使用**：
    *   核心智能体（检索、图提取、生成）：**Gemini-2.5-Flash**。
    *   重复性子任务（子查询生成、社区摘要）：**Gemini-2.5-Flash-Lite**。
    *   外部评估器：**DeepSeek-V3.1**。
*   **算力说明**：文中未明确提及具体的 GPU 型号或训练时长（因为是基于 API 的推理和提示词优化，无需训练）。但提到了推理开销：GRADRAG 每条查询平均耗时 35 秒（比基线增加约 10%），Token 消耗从 6,040 增加到 8,140。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流长文本基准上进行了测试，涵盖了向量和图两种主流 RAG 范式。
*   **充分性分析**：
    *   **消融实验**：通过对比“仅更新生成器”与“更新全组件”，清晰地证明了跨组件适配的有效性。
    *   **客观性**：引入了位置一致性检查（Position Consistency）和双侧二项式显著性检验（p-value），确保 LLM 评判的可靠性。
    *   **人工验证**：人工与 LLM 评判的一致性达 75.86%（Cohen’s κ = 0.59），证明了自动化评估的有效性。

### 6. 主要结论与发现
*   **性能提升**：GRADRAG 在两两比较中比单步基线高出 **12-15 个百分点**的净胜率。
*   **迭代效率**：大多数性能提升在 **2 次迭代**内即可完成，平均迭代次数少于 2 次。
*   **响应演变**：随着迭代进行，答案的长度增加后趋于稳定，词汇密度和 ROUGE-1 分数单调提升，主题熵降低，表明答案变得更加聚焦和密集。

### 7. 优点
*   **协同性强**：打破了 RAG 组件优化的孤岛效应，使检索和结构化过程能根据生成需求进行动态调整。
*   **通用性**：框架抽象为计算图，可无缝应用于向量 RAG、图 RAG 等多种架构。
*   **无需训练**：完全基于提示词自适应，适合闭源模型和即时部署场景。

### 8. 不足与局限
*   **计算成本**：迭代过程显著增加了 LLM API 的调用次数和 Token 消耗，延迟较高。
*   **评估依赖**：系统高度依赖内部评估器的质量，若评估器产生幻觉或偏见，可能导致提示词优化方向错误。
*   **扩展性限制**：目前主要针对单条查询进行测试时优化，尚未探讨如何将学到的优化策略泛化到整个数据集或进行批量优化。

（完）
