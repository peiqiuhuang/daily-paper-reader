---
title: "GRaSp: Automatic Example Optimization for In-Context Learning in Low-Data Tasks"
title_zh: GRaSp：低数据任务中上下文学习的自动示例优化
authors: "Simen Bihaug-Frøyland, Henrik Brådland"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07454v1"
tags: ["query:bopl"]
score: 9.0
evidence: 自动上下文示例优化框架
tldr: 针对大语言模型在少样本、特定领域任务中对上下文学习（ICL）示例选择高度敏感的问题，本文提出GRaSp框架。该框架通过合成候选池、聚类降维结构化及遗传算法优化，实现了示例的自动筛选。引入多样性自适应变异机制，在金融命名实体识别（FiNER-139）任务上显著优于零样本和随机少样本基准，为低数据环境下的示例优化提供了新方案。
selection_source: fresh_fetch
motivation: 大语言模型在特定领域低数据任务中的上下文学习效果受示例选择影响巨大，且高质量示例难以获取。
method: 提出GRaSp框架，结合合成数据生成、聚类结构化处理及带有自适应变异机制的遗传算法来搜索最优示例组合。
result: "在金融NER任务中，GRaSp利用非合成数据达到了45.84%的F1值，一致优于零样本和随机少样本基准。"
conclusion: 自动化的示例优化能显著提升模型性能，但候选池的分布多样性对于提升泛化能力至关重要。
---

## 摘要
上下文学习（In-context learning）使大语言模型能够适应新任务，但其性能对所选示例高度敏感。在高质量示例稀缺的特定领域、低数据场景下，寻找有效的演示示例尤为困难。我们提出了 GRaSp，一个用于自动上下文示例优化的三阶段框架。该框架首先生成一个大规模的合成候选池，然后通过聚类和降维对其进行结构化处理，最后利用遗传算法寻找最优的上下文示例，在命名实体识别（NER）任务上表现出持续的改进。我们还引入了一种自定义的多样性自适应变异机制，使其能够在种群收敛时，从初始的广泛簇间探索过渡到聚焦的簇内细化。我们在金融命名实体识别（FiNER-139）上评估了 GRaSp，比较了规模为 500 和 5000 的合成候选池与人工标注候选池。在使用非合成数据时，GRaSp 达到了 45.84% 的 micro-F1 值，始终优于零样本（zero-shot）和随机少样本（random few-shot）基准。合成数据达到了随机基准的水平但未能超越，这表明候选池中的分布多样性对于泛化至关重要。

## Abstract
In-context learning enables large language models to adapt to new tasks, but their performance is highly sensitive to the selected examples. Finding effective demonstrations is particularly difficult in domain-specific, low-data settings where high-quality examples are scarce. We propose GRaSp, a three-stage framework for automatic in-context example optimization. By first generating a large synthetic candidate pool, then structuring it with clustering and dimensionality reduction, and finally using genetic algorithms to find the optimal in-context examples, the framework shows consistent improvements on the NER task. We also introduce a custom diversity-adaptive mutation mechanism, allowing it to transition from the initial broad inter-cluster exploration to focused intra-cluster refinement as the population converges. We evaluate GRaSp on financial named entity recognition (FiNER-139), comparing synthetic and human-annotated candidate pools across pool sizes of 500 and 5000. With non-synthetic data, GRaSp achieves 45.84% micro-F1, consistently outperforming both zero-shot and random few-shot baselines. Synthetic data matches the random baseline but does not exceed it, suggesting that distributional variety in the candidate pool is critical for generalization.

---

## 论文详细总结（自动生成）

以下是对论文《GRaSp: Automatic Example Optimization for In-Context Learning in Low-Data Tasks》的结构化深入总结：

### 1. 核心问题与整体含义
大语言模型（LLM）的上下文学习（In-Context Learning, ICL）性能对演示示例的选择极其敏感。在特定领域（如金融、医疗）的低数据场景下，高质量的人工标注示例稀缺，且随机选择的示例往往无法覆盖推理时可能遇到的多样化输入分布。论文的核心动机是：**如何自动化地从有限或合成的数据中，筛选并优化出一组能够最大化提升模型性能的静态 ICL 示例？**

### 2. 方法论：GRaSp 框架
论文提出了 **GRaSp**（Generate, Reduce, Select）三阶段框架：
*   **Generate（生成阶段）**：利用 LLM 从无标注的领域语料（如 PDF 报告）中生成大规模合成示例池。为了保证平衡，分别生成包含实体的“正例”和不含实体的“负例”。
*   **Reduce（缩减与结构化阶段）**：
    1.  **嵌入与降维**：使用预训练模型（如 Qwen3-Embedding-4B）将示例转化为向量，并用 UMAP 降维。
    2.  **聚类**：采用 HDBSCAN 密度聚类算法识别语义结构，并剔除噪声（离群点），确保候选池由具有代表性的语义簇组成。
*   **Select（选择与优化阶段）**：
    1.  **遗传算法（GA）**：将 ICL 示例组合视为“个体”，通过选择、交叉和变异进行进化。
    2.  **多样性自适应变异机制**：这是本文的核心创新。算法根据当前种群的聚类多样性 $D_g$ 动态调整变异策略。当多样性高时，倾向于“簇间变异”（探索新语义区）；当种群趋于收敛时，转向“簇内变异”（精细化调整具体示例）。

### 3. 实验设计
*   **任务与数据集**：金融命名实体识别（FiNER-139），涉及 139 种复杂的 XBRL 标签。数据源自微软和苹果的 10-K 年度报告。
*   **Benchmark（基准）**：
    *   **Zero-shot**：无示例提示。
    *   **Random Few-shot**：从候选池中随机抽取 5 个示例。
    *   **监督学习基准**：包括 spaCy、BiLSTM、BERT、FinBERT 等在全量数据上微调的模型。
*   **对比变量**：
    *   候选池规模 $k \in \{500, 5000\}$。
    *   数据来源：合成数据（LLM 生成） vs. 非合成数据（人工标注）。

### 4. 资源与算力
*   **模型使用**：生成阶段使用了 `gpt-oss-120b`（文中注脚称其为 OpenAI 发布的开源模型，可能指代特定实验环境下的模型版本），嵌入阶段使用了 `Qwen3-Embedding-4B`。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练/搜索时长。但提到遗传算法运行了 20 代，每代包含 180 个后代，且 LLM 推理设置为确定性模式（Temperature=0.0）。

### 5. 实验数量与充分性
*   **实验组数**：针对合成与非合成数据、两种候选池规模分别进行了完整的进化实验，并提供了随机基准的均值与标准差（3 次独立运行）。
*   **充分性评价**：实验设计较为客观，通过对比合成数据与真实数据的表现差异，深入探讨了“数据质量”对优化算法的影响。然而，实验**仅限于 NER 这一种任务**，缺乏在分类、摘要或推理等其他 NLP 任务上的泛化验证。

### 6. 主要结论与发现
*   **性能提升**：在非合成数据下，GRaSp 选出的示例在 FiNER-139 任务上达到了 45.84% 的 $\mu$-F1，显著优于 Zero-shot（40.23%）和随机基准（42.91%）。
*   **合成数据的局限性**：虽然合成数据在验证集上表现出极高的 Fitness（约 0.73），但在真实测试集上仅能达到随机水平。这表明合成数据虽然能让模型“学会”处理特定模式，但缺乏真实文本的分布多样性，导致泛化失败。
*   **自适应机制的有效性**：实验证实了变异概率随代际演进的自动衰减，证明了从“广泛探索”到“局部开发”转换的合理性。

### 7. 优点
*   **全自动化流程**：从原始文档到最优 Prompt 的生成无需人工干预，适合低资源领域。
*   **创新的变异策略**：引入聚类信息指导遗传算法，解决了传统 GA 在 Prompt 优化中容易陷入局部最优或搜索空间过大的问题。
*   **抗噪性**：通过 HDBSCAN 过滤噪声，提高了候选池的质量。

### 8. 不足与局限
*   **合成数据质量瓶颈**：实验表明，如果初始合成数据与真实分布存在偏差，再强大的优化算法也无法弥补这一缺陷。
*   **任务复杂度挑战**：尽管 GRaSp 优于随机基准，但与全量微调的监督模型（F1 约 75%）相比仍有巨大差距，说明 5 个示例对于 139 类 NER 任务的信息量依然不足。
*   **计算开销**：虽然是静态优化（推理时无额外开销），但优化过程涉及大量 LLM 调用（GA 的每一代评估），在实际应用中成本较高。

（完）
