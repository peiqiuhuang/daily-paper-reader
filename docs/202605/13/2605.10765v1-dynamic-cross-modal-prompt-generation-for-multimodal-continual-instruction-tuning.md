---
title: Dynamic Cross-Modal Prompt Generation for Multimodal Continual Instruction Tuning
title_zh: 多模态持续指令微调的动态跨模态提示生成
authors: "Tao Hu, Da-Wei Zhou"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10765v1"
tags: ["query:bopl"]
score: 8.0
evidence: 动态跨模态提示生成
tldr: 针对多模态大模型在持续指令微调（MCIT）中面临的任务内样本差异大和灾难性遗忘问题，本文提出了DRAPE框架。该框架通过跨模态注意力机制，根据具体的指令和图像动态生成实例级软提示，而非简单的任务级模块组合。结合零空间梯度投影和原型路由技术，DRAPE在保持新任务学习能力的同时有效抑制了遗忘，在多个基准测试中达到了领先水平。
selection_source: fresh_fetch
motivation: 现有的持续学习方法多基于任务级模块，难以应对同一任务内不同样本在视觉场景和推理需求上的显著差异。
method: 提出DRAPE框架，利用文本指令引导并跨模态关注视觉特征来生成实例特定的动态提示，并结合零空间梯度投影和原型路由来缓解遗忘。
result: 在MCIT基准测试中，DRAPE在性能上显著优于现有的基于提示和LoRA的持续学习基线方法。
conclusion: 实例级的动态跨模态提示生成是提升多模态大模型持续学习能力和泛化性能的有效途径。
---

## 摘要
多模态大语言模型 (MLLMs) 通过指令微调获得了强大的性能，但在实际部署中，通常需要跨序列任务进行持续的能力扩展。在这种场景下，多模态持续指令微调 (MCIT) 旨在获取新能力的同时限制灾难性遗忘。现有方法主要遵循模块组合范式：它们维护任务级提示或 LoRA 专家，并在推理时动态路由或聚合其中的一个子集。然而，同一任务中的样本在视觉场景、问题意图和推理需求方面仍可能存在显著差异。这激发了针对单个查询-图像对的实例级自适应，而不仅仅是选择或组合任务级模块。为此，我们提出了 DRAPE（动态跨模态提示生成），这是一个为 MCIT 合成连续的实例特定软提示的提示学习框架。DRAPE 不是从固定池中选择提示，而是从文本指令中导出提示查询，并与视觉补丁特征进行交叉注意力计算，从而生成以查询-图像为条件的提示，并将其添加到冻结的 LLM 前面。为了减轻序列更新过程中的遗忘，DRAPE 对共享投影器应用零空间梯度投影，并在推理时使用基于 CLIP 的原型路由进行无需任务标签的生成器选择。在 MCIT 基准测试上的广泛实验表明，DRAPE 在代表性的基于提示和基于 LoRA 的持续学习基准模型中实现了最先进的性能。

## Abstract
Multimodal Large Language Models (MLLMs) achieve strong performance through instruction tuning, yet real-world deployment often requires continual capability expansion across sequential tasks. In such scenarios, Multimodal Continual Instruction Tuning (MCIT) aims to acquire new capabilities while limiting catastrophic forgetting. Existing methods mainly follow a module-composition paradigm: they maintain task-level prompts or LoRA experts and dynamically route or aggregate a subset of them at inference. However, samples within the same task can still differ substantially in visual scenes, question intents, and reasoning demands. This motivates instance-level adaptation to individual query-image pairs rather than only selecting or combining task-level modules. To this end, we propose DRAPE (Dynamic Cross-Modal Prompt Generation), a prompt-learning framework that synthesizes continuous instance-specific soft prompts for MCIT. Instead of selecting prompts from a fixed pool, DRAPE derives prompt queries from the textual instruction and cross-attends to visual patch features, producing query-image conditioned prompts that are prepended to the frozen LLM. To mitigate forgetting during sequential updates, DRAPE applies null-space gradient projection to the shared projector and uses CLIP-based prototype routing for task-label-free generator selection at inference. Extensive experiments on MCIT benchmarks show that DRAPE achieves state-of-the-art performance among representative prompt-based and LoRA-based continual-learning baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dynamic Cross-Modal Prompt Generation for Multimodal Continual Instruction Tuning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：多模态大语言模型（MLLM）在部署后需要不断学习新任务（MCIT），但面临“灾难性遗忘”挑战。
*   **核心问题**：现有的持续学习方法（如提示学习或LoRA专家）大多基于**任务级（Task-level）**的模块组合。然而，同一任务内的不同样本在视觉场景、问题意图和推理需求上存在巨大差异。
*   **研究动机**：作者发现静态的任务级提示无法捕捉任务内部的样本多样性。因此，论文提出需要一种**实例级（Instance-level）**的自适应机制，根据每个具体的“指令-图像”对动态生成提示。

### 2. 方法论
DRAPE 框架包含三个核心组件：
*   **跨模态提示生成器（Cross-Modal Prompt Generator）**：
    *   **指令感知查询初始化**：将文本指令切分为段，通过掩码平均池化和多头注意力提取指令意图，生成初始查询向量。
    *   **视觉引导提示合成**：利用指令查询向量与视觉特征进行交叉注意力计算，提取任务相关的视觉信息，最后通过 MLP 生成实例特定的软提示（Soft Prompts）。
*   **零空间梯度投影（Null-Space Gradient Projection）**：
    *   为了保护共享的视觉投影器（Projector）不被新任务破坏，利用奇异值分解（SVD）计算旧任务特征空间的零空间。
    *   在更新投影器梯度时，将其投影到与旧任务特征正交的子空间中，从而在学习新知识时最小化对旧知识的干扰。
*   **CLIP 引导的原型路由（CLIP-Guided Prototype Router）**：
    *   在推理阶段（无任务标签），利用冻结的 CLIP 空间存储每个任务的特征原型（文本+图像融合特征）。
    *   通过计算输入样本与各任务原型的余弦相似度，自动选择最匹配的任务生成器。

### 3. 实验设计
*   **数据集**：
    *   **CoIN 基准**：包含 ScienceQA, TextVQA, ImageNet, GQA, VizWiz, Grounding, VQAv2, OCR-VQA 八个序列任务。
    *   **UCIT 基准**：包含 ArxivQA, CLEVR-Math, IconQA, ImageNet-R, VizWiz-caption, Flickr30k 六个序列任务。
*   **基线方法**：
    *   **提示学习类**：L2P, DualPrompt, CODA-Prompt, ModalPrompt。
    *   **LoRA/专家类**：MoELoRA, ProgLoRA, HiDe-LLaVA, SEFE 等。
*   **主干模型**：LLaVA-v1.5-7B。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **训练细节**：采用 DeepSpeed ZeRO-2 优化器，使用 bf16 精度。
*   **训练时长**：在 CoIN 基准上，DRAPE 的平均每任务训练时间约为 **1.59 小时**，比 MoELoRA（7.27h）和 CODA-Prompt（3.43h）更高效。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流 MCIT 基准（共 14 个数据集）上进行了完整测试。
*   **消融实验**：针对“跨模态注意力”和“零空间投影”分别做了消融，验证了各组件对性能的提升（分别贡献了约 1.17% 和 2.62% 的准确率）。
*   **敏感性分析**：研究了生成器隐藏层维度、提示向量数量对结果的影响。
*   **可视化分析**：通过 t-SNE 聚类和注意力热力图展示了动态提示如何精准捕捉图像中的关键区域。
*   **评价**：实验设计全面，对比了从经典提示学习到最新 MCIT 专用模型的多类方法，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：DRAPE 在 CoIN 基准上达到 67.48% 的平均准确率，比之前的最强基线 ProgLoRA 高出 **8.39%**。
*   **遗忘抑制**：通过零空间投影，DRAPE 的后向转移（BWT）指标显著优于其他非重放类方法，证明了其在无数据重放情况下的抗遗忘能力。
*   **实例自适应的价值**：实验证明，针对具体样本生成的动态提示比固定的任务提示能更好地处理复杂的视觉推理任务（如 OCR-VQA）。

### 7. 优点
*   **粒度更细**：突破了传统“一任务一提示”的局限，实现了样本级的动态自适应。
*   **无需重放**：在不存储任何旧任务数据的情况下，通过数学投影手段有效缓解了遗忘。
*   **高效轻量**：训练速度快，且推理时仅需增加极小的 CLIP 路由开销。

### 8. 不足与局限
*   **架构限制**：目前主要针对提示学习（Prompt Tuning）设计，尚未探讨如何将这种实例级动态生成机制应用于 LoRA 等其他参数高效微调（PEFT）架构。
*   **存储增长**：虽然每个生成器很小（约 21MB），但随着任务数量无限增加，模型仍需线性增加存储空间来保存不同任务的生成器。
*   **长序列挑战**：虽然在 8 个任务上表现良好，但在更长、更复杂的任务流中的累积误差和投影空间压缩问题仍待进一步验证。

（完）
