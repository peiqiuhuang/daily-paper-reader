---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：策略与提示词联合优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v1"
tags: ["query:bopl"]
score: 9.0
evidence: 联合提示优化与策略优化
tldr: 针对大语言模型在强化学习（RLVR）中面临的“硬样本”探索效率低、奖励稀疏问题，本文提出了 P^2O 框架。该框架将提示词优化与策略优化相结合，利用 GeneticPareto 算法为硬样本进化出更优提示词，引导模型发现成功路径，并将这些推理增益蒸馏至模型参数中。实验证明，P^2O 在分布内和分布外基准测试中均显著提升了模型性能和收敛速度。
selection_source: fresh_fetch
motivation: 传统的 RLVR 在面对成功率接近零的硬样本时，由于奖励稀疏导致模型缺乏有效的监督信号，限制了推理能力的进一步提升。
method: 提出 P^2O 框架，通过 GeneticPareto 算法动态优化硬样本的提示词，并将优化后的推理路径作为密集监督信号蒸馏到模型策略中。
result: "实验显示 P^2O 不仅在分布内任务表现优异，在分布外（OOD）基准测试中也实现了平均 4.7% 的显著性能提升。"
conclusion: P^2O 通过协同提示词与策略优化，有效解决了强化学习中的探索难题，增强了语言模型的复杂推理能力和泛化性。
---

## 摘要
带有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原始的 RLVR 面临探索效率低下的问题，特别是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但模型实际上却缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示词优化（Prompt Optimization）与策略优化（Policy Optimization）的新型框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示词优化算法来演化提示词模板，引导模型发现成功的轨迹。关键在于，与依赖输入增强的传统提示词工程方法不同，P^2O 将这些优化提示词所带来的推理增益直接蒸馏到模型参数中。该机制为困难样本提供了更密集的正向监督信号，并加速了收敛。广泛的实验表明，P^2O 不仅在分布内数据集上取得了卓越的性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

以下是对论文《P^2O: Joint Policy and Prompt Optimization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**带有可验证奖励的强化学习（RLVR）**在提升大语言模型（LLM）推理能力时的局限性。
*   **核心问题**：在 RLVR 训练中，模型经常遇到“硬样本”（Hard Samples），即模型在多次采样中成功率为零。由于奖励极其稀疏，这些样本无法产生有效的梯度信号（优势估计为零），导致模型在简单样本上过拟合，而在复杂推理任务上停滞不前。
*   **研究动机**：作者观察到，虽然模型在标准提示词下无法解决硬样本，但通过精心设计的提示词往往能激发其潜在的推理路径。因此，研究如何将“提示词优化”与“策略优化”结合，利用提示词作为桥梁跨越探索鸿沟，是提升模型上限的关键。

### 2. 方法论：核心思想与技术细节
P^2O 框架采用**交替最大化**的策略，包含两个主要阶段：

*   **核心思想**：将提示词模板视为离散的隐变量，通过进化算法为硬样本寻找能触发正确答案的提示词，随后通过“上下文蒸馏”将这种能力内化到模型参数中。
*   **关键技术细节**：
    1.  **硬样本挖掘**：在每个训练周期，识别成功率低于阈值 $\tau$ 的样本。
    2.  **进化提示词优化（GEPA 算法）**：利用 Genetic-Pareto 算法，通过一个“反思模型”（Reflection LLM）对失败案例进行分析并产生变异提示词。该算法维护一个 Pareto 前沿，确保提示词的多样性和有效性。
    3.  **上下文蒸馏（Context Distillation）**：这是 P^2O 的关键。在训练时，模型使用优化后的提示词 $\tilde{x}$ 生成成功的推理轨迹 $\tilde{y}$，但在计算策略梯度更新参数时，输入却是原始查询 $x$。
*   **公式逻辑**：通过最大化联合目标函数 $J_{joint}$，使模型在不依赖推理时提示词的情况下，也能在原始输入上实现高奖励。

### 3. 实验设计
*   **数据集**：使用了两个规模为 5,000 的训练集，分别采样自 **DeepScaler** 和 **DeepMath**（难度 $\ge 7$）。
*   **基准测试（Benchmarks）**：涵盖了 6 个极具挑战性的数学竞赛数据集：AIME24、AIME25、AMC、MATH500、MINERVA 和 OLYMPIAD。
*   **对比方法**：
    *   基础模型：Qwen3 (0.6B, 1.7B, 4B)。
    *   基准 RL 算法：**GRPO**（Group Relative Policy Optimization）。
    *   P^2O 变体：P^2O Self-Ref（自反思）和 P^2O Teacher-Ref（使用 Kimi-K2 作为外部老师进行反思）。

### 4. 资源与算力
*   **模型规模**：主要使用 **Qwen3-4B** 作为骨干模型。
*   **训练配置**：训练 5 个 Epoch，全局 Batch Size 为 128，最大生成长度 12k tokens，Rollout 采样数 $K=6$。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）及数量，也未提及总训练时长。但提到 GRPO 算法相比 PPO 节省了约一半的显存开销。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的训练分布（DeepScaler 和 DeepMath）上进行了完整实验，并在 6 个 OOD（分布外）基准上验证。
*   **消融实验**：针对“上下文蒸馏”的必要性以及“提示词多样性”的影响做了专门的对比实验。
*   **充分性评价**：实验设计较为充分，不仅有量化指标，还有训练曲线分析和定性的案例研究（Case Study）。对比 GRPO 这一强基准线，证明了方法的有效性。实验过程考虑了自反思与教师反思的区别，体现了客观性。

### 6. 主要结论与发现
*   **性能显著提升**：P^2O 在 DeepScaler-5K 上比 GRPO 平均提升 **4.7%**，在 DeepMath-5K 上提升 **6.9%**。
*   **攻克硬样本**：在极难的 AIME 榜单上，提升幅度高达 **12.3%** 以上，证明了提示词引导对跨越探索瓶颈的巨大作用。
*   **蒸馏的必要性**：如果不使用上下文蒸馏（即推理时也带提示词），模型会产生“提示词依赖”，导致在标准测试中性能大幅下降（甚至低于 GRPO）。
*   **反思源的影响**：外部强模型（Teacher-Ref）在某些数据集上表现更好，但在特定领域，模型自身的反思（Self-Ref）可能更契合其推理能力。

### 7. 优点
*   **解决稀疏奖励问题**：通过动态进化的提示词为硬样本提供“脚手架”，有效解决了 RL 训练中的信号缺失问题。
*   **推理零成本**：通过蒸馏机制，模型在推理阶段不需要复杂的提示词工程，保持了原生输入的简洁与高效。
*   **自适应进化**：GEPA 算法能随模型能力的提升动态寻找新的“硬样本”边界，形成持续改进的闭环。

### 8. 不足与局限
*   **计算开销**：提示词的进化过程（GEPA）涉及多次采样和反思模型的调用，虽然提升了样本效率，但增加了训练阶段的计算复杂度。
*   **任务局限性**：实验主要集中在数学推理任务上，尚未在代码生成、创意写作或多模态任务中验证其普适性。
*   **反思模型依赖**：P^2O 的效果部分取决于反思模型（Mutation Operator）的质量，对于极小规模模型，自反思能力可能不足以支撑提示词的有效进化。

（完）
