---
title: Federated stochastic bilevel optimization with fully first-order gradients
title_zh: 基于完全一阶梯度的联邦随机双层优化
authors: "Yihan Zhang, Rohit Dhaipule, Chiu C Tan, Haibin Ling, Hongchang Gao"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.16350v1"
tags: ["query:bopl"]
score: 9.0
evidence: 联邦随机双层优化
tldr: 针对联邦随机双层优化中现有算法依赖二阶导数导致计算开销大的问题，本文提出了一种仅需一阶梯度的联邦随机方差缩减双层梯度下降算法。该方法通过引入常数单时间尺度学习率机制，在无需计算Hessian和Jacobian矩阵的情况下实现了高效优化，并提供了新的收敛性分析策略，显著提升了联邦学习场景下的计算效率与实用性。
selection_source: fresh_fetch
motivation: 现有联邦随机双层优化算法高度依赖二阶导数计算，导致实际运行效率低下。
method: 提出了一种仅依赖一阶梯度预言机且结合方差缩减与常数单时间尺度学习率的联邦优化算法。
result: 实验结果表明，该算法在无需二阶矩阵计算的情况下，显著降低了运行时间并保持了良好的收敛性能。
conclusion: 该研究为联邦双层优化提供了一种高效、低计算成本的一阶解决方案。
---

## 摘要
近年来，联邦随机双层优化因其在机器学习中的广泛应用而受到积极研究。然而，大多数现有的联邦随机双层优化算法需要计算二阶海森矩阵（Hessian）和雅可比矩阵（Jacobian），这导致在实际应用中运行时间较长。为了应对这些挑战，我们提出了一种仅依赖一阶预言机（first-order oracles）的新型联邦随机方差缩减双层梯度下降算法。具体而言，我们的方法不需要计算二阶海森矩阵和雅可比矩阵，从而显著缩短了运行时间。此外，我们引入了一种新的学习率机制，即常数单时间尺度学习率，以协调不同变量的更新。我们还提出了一种建立算法收敛速率的新策略。最后，大量的实验结果证实了我们所提算法的有效性。

## Abstract
Federated stochastic bilevel optimization has been actively studied in recent years due to its widespread applications in machine learning. However, most existing federated stochastic bilevel optimization algorithms require the computation of second-order Hessian and Jacobian matrices, which leads to longer running times in practice. To address these challenges, we propose a novel federated stochastic variance-reduced bilevel gradient descent algorithm that relies solely on first-order oracles. Specifically, our approach does not require the computation of second-order Hessian and Jacobian matrices, significantly reducing running time. Furthermore, we introduce a novel learning rate mechanism, i.e., a constant single-timescale learning rate, to coordinate the update of different variables. We also present a new strategy to establish the convergence rate of our algorithm. Finally, the extensive experimental results confirm the efficacy of our proposed algorithm.

---

## 论文详细总结（自动生成）

这是一份关于论文《Federated Stochastic Bilevel Optimization with Fully First-Order Gradients》的结构化总结：

### 1. 核心问题与研究动机
联邦随机双层优化（FedSBO）在元学习、超参数优化和神经架构搜索等领域应用广泛。然而，现有的 FedSBO 算法通常需要计算二阶导数（Hessian 和 Jacobian 矩阵），这在联邦学习的高维数据场景下带来了巨大的计算开销和通信负担。本文旨在解决这一痛点，开发一种**仅依赖一阶梯度**的联邦优化算法，以降低计算成本并提升实际运行效率。

### 2. 方法论：FedSVRBGD-FO
*   **核心思想**：将双层优化问题转化为带约束的优化问题，进而利用惩罚函数法（Penalty Approach）将其转化为无约束的极小极大（minimax）优化问题。通过这种转换，算法仅需一阶梯度即可求解。
*   **关键技术**：
    *   **方差缩减**：引入了随机方差缩减技术，以保证在非凸设置下的收敛性。
    *   **单时间尺度常数学习率**：不同于传统方法依赖复杂的迭代相关学习率或双时间尺度学习率，本文设计了单时间尺度常数学习率，简化了超参数调优，并增强了隐私安全性（防止攻击者通过学习率推断训练阶段）。
    *   **收敛性分析**：构建了一个包含多个误差项的潜在函数（Potential Function），通过精心设计的系数消除了各分量间的耦合影响，从而证明了算法的收敛性。

### 3. 实验设计
*   **任务场景**：超参数优化（Logistic 回归的正则化系数学习）和超表示学习（深度神经网络的权重学习）。
*   **数据集**：使用了 LIBSVM 库中的 `a9a`、`w8a` 和 `covtype` 三个基准数据集。
*   **对比方法**：与四种主流的二阶方法进行了对比：FedNEST、FedMBO、LocalBSGVRM 和 FedBiOAcc。

### 4. 资源与算力
*   **硬件配置**：实验在配备 4 张 NVIDIA A5000 GPU 的工作站上进行。
*   **模拟方式**：每张 GPU 运行两个线程，共模拟 8 个工作节点（workers）。
*   **训练时长**：文中未明确给出总训练时长，但通过图表展示了算法在达到特定损失值或精度所需的运行时间（秒），体现了计算效率的提升。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了多个数据集、不同的通信周期（$p=4$ 和 $p=16$）以及超参数敏感性分析（不同 $\epsilon$ 的影响）。
*   **客观性与公平性**：实验设置了统一的迭代次数（16,000 次）和相同的计算环境，对比了多种 SOTA 方法，实验设计较为全面且客观，能够充分验证所提算法的计算效率优势。

### 6. 主要结论与发现
*   **计算效率**：所提算法（FedSVRBGD-FO）在所有测试数据集上均表现出比现有二阶方法更快的收敛速度（运行时间更短）。
*   **理论保证**：证明了该算法能达到 $O(1/N\epsilon^5)$ 的收敛速率，且在设备数量 $N$ 上实现了线性加速。
*   **易用性**：验证了单时间尺度常数学习率在实际应用中的有效性和易调优性。

### 7. 优点
*   **计算成本低**：彻底摆脱了对二阶矩阵的计算需求，显著降低了单机计算压力。
*   **理论创新**：首次在联邦双层优化中实现了单时间尺度常数学习率的收敛性证明。
*   **通信友好**：通过方差缩减和合理的通信周期设计，在保持收敛性能的同时兼顾了通信效率。

### 8. 不足与局限
*   **惩罚参数依赖**：算法依赖于惩罚超参数 $\lambda$，虽然理论上给出了设置建议，但在极端非凸或异构程度极高的场景下，其鲁棒性仍需进一步验证。
*   **收敛速率**：虽然实现了线性加速，但 $O(\epsilon^{-5})$ 的收敛速率在某些极高精度要求下可能仍有提升空间。
*   **应用限制**：实验主要集中在分类任务，对于更复杂的生成模型或大规模分布式系统下的表现未做深入探讨。

（完）
