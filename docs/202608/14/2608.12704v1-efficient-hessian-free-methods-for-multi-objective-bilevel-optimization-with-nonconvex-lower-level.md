---
title: Efficient Hessian-Free Methods for Multi-Objective Bilevel Optimization with Nonconvex Lower Level
title_zh: 针对非凸下层问题的多目标双层优化的高效无 Hessian 方法
authors: "Yicong Jiang, Feihu Huang"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.12704v1"
tags: ["query:bopl"]
score: 9.0
evidence: 非凸下层问题的多目标双层优化
tldr: 本研究针对多目标双层优化中下层问题非凸的挑战，提出了一类基于Moreau包络的无Hessian算法（MOMEHA）。该方法通过Moreau包络将原问题转化为带约束的单层多目标优化，并结合平滑加权切比雪夫标量化实现了单循环且无需计算Hessian矩阵的高效求解。此外，针对随机场景提出了动量变体MB-MOMEHA，并在理论上证明了收敛性。实验证明该方法在少样本元学习和神经架构搜索任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的多目标双层优化方法大多依赖下层问题的凸性假设，难以处理实际应用中普遍存在的非凸下层问题。
method: 提出MOMEHA算法，利用Moreau包络将双层问题转化为单层优化，并结合标量化技术实现无Hessian的单循环求解。
result: 实验结果显示，该方法在少样本元学习和神经架构搜索任务的帕累托前沿表现上均优于现有方法。
conclusion: 该研究为非凸下层多目标双层优化提供了高效且具有理论收敛保障的无Hessian解决方案。
---

## 摘要
多目标双层优化在人工智能领域具有广泛的应用，例如自动学习和多任务元学习。尽管最近已有一些工作开始研究多目标双层优化，但所提出的方法依赖于（强）凸下层问题。事实上，这些多目标双层学习问题通常是非凸的，特别是它们的下层问题是非凸的。为了填补这一空白，我们提出了一类基于多目标 Moreau 包络的无 Hessian 算法（MOMEHA），用于解决具有非凸下层问题的多目标双层学习问题。具体而言，我们的方法利用 Moreau 包络将原始问题转化为具有包络约束的多目标单层优化问题。特别地，通过结合平滑加权切比雪夫标量化（Tchebycheff scalarization），我们的方法在多目标设置中保留了单循环和无 Hessian 的计算优势。此外，我们提出了一种基于动量的 MOMEHA 变体（即 MB-MOMEHA）方法，用于解决随机多目标双层学习问题。在理论上，我们提供了算法在确定性和随机设置下的收敛性质。在少样本元学习和神经架构搜索上的一些实验表明，我们的方法在 Pareto 前沿方面优于现有方法，验证了其有效性和鲁棒性。

## Abstract
Multi-objective bilevel optimization has wide applications in the AI area such as automated learning and multi-task meta-learning. Although recently some works have been begun to study the multi-objective bilevel optimization, the proposed methods rely on the (strongly) convex lower level problems. In fact, these multi-objective bilevel learning problems are generally nonconvex, and particularly their lower level problems are nonconvex. To fill this gap, we propose a class of Multi-Objective Moreau Envelope based Hessian-free Algorithms (MOMEHA) to solve the multi-objective bilevel learning problems with nonconvex lower level. Specifically, our method uses the Moreau envelope to convert the original problem into a multi-objective single-level optimization with an envelope constraint. In particular, our method retains computational advantages of being single-loop and Hessian-free in the multi-objective setting by incorporating a smooth weighted Tchebycheff scalarization. Furthermore, we propose a momentum-based variant of MOMEHA (i.e., MB-MOMEHA) method to solve the stochastic multi-objective bilevel learning problems. In theory, we provide the convergence properties of our algorithms under both deterministic and stochastic setting. Some experiments on few-shot meta-learning and neural architecture search demonstrate that our methods outperform the existing approaches in Pareto front, validating its effectiveness and robustness.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Hessian-Free Methods for Multi-Objective Bilevel Optimization with Nonconvex Lower Level》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：多目标双层优化（MOBL）在自动机器学习、多任务元学习和神经架构搜索（NAS）中应用广泛。
*   **核心挑战**：现有的 MOBL 算法大多依赖于下层（Lower Level, LL）问题是**强凸或凸**的假设。然而，在深度学习等实际应用中，下层问题通常是非凸的。此外，计算 Hessian 矩阵及其逆矩阵的开销巨大，限制了算法在大规模问题上的扩展性。
*   **论文目标**：提出一种无需计算 Hessian 矩阵（Hessian-free）、支持单循环更新、且能处理**非凸下层问题**的高效多目标双层优化框架。

### 2. 方法论
*   **核心思想**：利用 **Moreau 包络（Moreau Envelope）** 重新表述双层优化问题。通过将下层最优性条件转化为一个显式的标量约束，将原始的双层问题转化为带约束的单层优化问题。
*   **关键技术细节**：
    *   **Moreau 包络重构**：引入辅助变量 $\theta$ 来近似下层问题的最优解，将复杂的隐式梯度计算转化为对包络函数的梯度估计。
    *   **平滑加权切比雪夫标量化（STCH）**：为了处理多个上层目标并探索 Pareto 前沿，采用 STCH 将多目标转化为单目标，同时通过调节偏好向量（Preference Vector）来搜索不同的最优权衡点。
    *   **惩罚函数法**：将包络约束通过惩罚因子 $c_t$ 纳入目标函数，实现单循环（Single-loop）更新，避免了昂贵的内层迭代。
    *   **算法流程**：
        *   **MOMEHA（确定性版本）**：交替更新辅助变量 $\theta$、上层变量 $x$ 和下层变量 $y$。
        *   **MB-MOMEHA（随机版本）**：在随机梯度设置下引入**动量（Momentum）**技术，以降低随机噪声并加速收敛。
*   **理论保障**：定义了 $\epsilon_c-\epsilon_s$-Pareto 驻留点，并证明了算法在确定性下达到 $O(T^{-1/4})$、在随机设置下达到 $O(T^{-1/16})$ 的非渐近收敛速率。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **多领域少样本元学习**：使用 **FC-100**（用于灵敏度分析）和 **Caltech-256**（用于对比实验）数据集。任务是跨 4 个领域进行 5-way 5-shot 分类。
    2.  **多任务神经架构搜索（NAS）**：在 **CIFAR-10** 数据集上搜索最优架构，同时优化验证集损失、FLOPS、跳跃连接密度和池化密度等 4 个冲突目标。
*   **对比方法（Benchmarks）**：
    *   MOML (2021), FORUM (2024), MoCo (2022), WC-MHGD (2026a), WC-penalty (2026b)。
*   **评估指标**：Pareto 前沿的可视化分布、超体积（Hypervolume, HV）值、测试准确率。

### 4. 资源与算力
*   **硬件配置**：Intel i5-13600K CPU，32 GB RAM，单张 **NVIDIA RTX 4090 GPU**。
*   **软件环境**：Python 3.12.11, PyTorch 2.5.1。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到元学习实验运行了 2500 到 5000 个迭代周期，NAS 实验运行了 50 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对超参数 $\mu$（平滑参数）和 $\gamma$（包络正则项）进行了详细的消融实验（灵敏度分析）。
    *   展示了不同偏好强度（弱、中、强、极端偏好）下的 Pareto 前沿探索结果。
    *   在元学习和 NAS 两个完全不同的任务上验证了算法。
*   **充分性评价**：实验设计较为充分，涵盖了确定性和随机两种场景。通过可视化 Pareto 前沿和量化 HV 指标，客观地展示了该方法在处理非凸下层问题时比现有基于凸性假设的方法更具优势。

### 6. 主要结论与发现
*   **性能优越性**：MOMEHA 及其随机变体在所有测试任务中均获得了质量更高、覆盖范围更广的 Pareto 前沿。
*   **偏好引导有效**：通过调整偏好向量，算法能精准地在冲突目标之间找到用户预期的权衡解。
*   **非凸处理能力**：在下层问题明显非凸的 NAS 任务中，现有方法（如 WC-MHGD）可能无法收敛或表现较差，而本方法表现稳健。
*   **Hessian-free 优势**：无需二阶导数计算，显著降低了单步迭代的计算复杂度。

### 7. 优点
*   **适用性广**：打破了下层问题必须为凸的限制，使其能应用于现代深度神经网络的优化。
*   **计算高效**：单循环结构且无需 Hessian 矩阵，适合大规模参数模型。
*   **理论完备**：提供了严谨的非渐近收敛性证明，填补了随机 Moreau 包络框架在动量设置下的理论空白。

### 8. 不足与局限
*   **收敛速率较慢**：随机版本 MB-MOMEHA 的理论收敛速率为 $O(T^{-1/16})$，虽然在理论上是可行的，但在极大规模问题上可能需要较多迭代次数。
*   **超参数敏感性**：虽然实验显示 $\mu$ 和 $\gamma$ 相对鲁棒，但惩罚因子 $c_t$ 的增长策略对最终收敛质量仍有较大影响，实际调参可能需要经验。
*   **实验偏差风险**：对比实验中提到 WC-MHGD 在某些设置下无法收敛，这可能与该算法对二阶信息的敏感性有关，但也反映了本方法在复杂环境下的生存能力。

（完）
