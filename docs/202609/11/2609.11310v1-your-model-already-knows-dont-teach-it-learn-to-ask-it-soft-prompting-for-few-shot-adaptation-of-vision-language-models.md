---
title: "Your Model Already Knows Don't Teach It, Learn to Ask It: Soft Prompting for Few-Shot Adaptation of Vision-Language Models"
title_zh: 你的模型已经知晓，无需教导，学会提问即可：视觉-语言模型的少样本适应软提示方法
authors: "Gautam Rajendrakumar Gare, Siyi Li, Hewei Wang, Cesar Daniel Hernandez, Wei Zhao, Wolfgang M. Pauli, John Galeotti, Deva Ramanan"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11310v1"
tags: ["query:bopl"]
score: 8.0
evidence: 优化连续提示词标记的软提示技术
tldr: 本文针对少样本目标检测任务，提出了一种基于软提示（Soft Prompting）的视觉语言模型（VLM）适配方法。通过在跨模态边界处插入少量可学习的连续提示词，并采用空空间标记初始化，该方法在保持预训练模型能力的同时，仅需极少量参数即可达到与LoRA微调相当的性能，且有效避免了灾难性遗忘，证明了通过优化“提问方式”即可激活模型在特定领域的潜力。
selection_source: fresh_fetch
motivation: 现有微调方法（如LoRA）在少样本场景下易导致预训练模型性能遗忘，且参数量较大。
method: 在视觉与文本标记的跨模态边界处插入少量连续提示词，并使用空空间标记进行初始化，同时冻结预训练模型主干。
result: "仅需训练极少量参数（约7,168个），在少样本目标检测和机器人操作任务中达到与LoRA相当的性能，且完全保留了原始模型的通用能力。"
conclusion: 现代视觉语言模型已具备特定领域所需的知识，通过软提示学习如何“提问”比直接修改模型权重更高效且稳健。
---

## 摘要
我们针对航空、工业和医疗影像等域外场景下的少样本目标检测问题，仅使用十张标注图像进行监督学习。现有的适应方法包括离散提示优化和 LoRA 微调。我们重新审视了第三种选择：软提示（soft prompting），即在保持预训练主干网络冻结的情况下，优化少量连续的提示标记。我们确定了两个关键的设计选择。首先，将提示标记放置在视觉和文本标记之间的跨模态边界处，其性能优于其他放置方式（10.0 vs. 8.4 mAP）。其次，从空空间标记（empty space token）初始化提示，其效果优于语义初始化和随机初始化。基于这些选择，仅需一到三个学习到的标记（平均 7,168 个参数）即可在 Roboflow20-VL 上达到与最佳 LoRA 配置相当的性能（14.2 mAP，10-shot），同时训练参数量减少了 20,000 倍以上。软提示的优化难度依然较大，在不同随机种子下表现出更高的方差。然而，与 LoRA 不同的是，它不会导致遗忘：与我们准确率相当的 LoRA 秩会导致 NaturalBench VQA 准确率相对下降 35%，在最大秩时甚至下降 56%，而软提示则保持了预训练性能不变。学习到的标记表现得更像是提示而非权重。它们可以迁移到更新的模型而无需重新训练（在 Qwen3.5-9B 上提升了 0.8 mAP），并且可以转化为可读的提示，其竞争力可与提示搜索方法相媲美（匹配 DetPO 并优于 GEPA）。该方法还扩展到了检测之外的领域。在 RoboCasa 操作任务中，冻结的 π0.5 视觉-语言-动作策略受益于软提示，当标记放置在梯度瓶颈处时，在三个任务中的两个上达到了与 LoRA 基线相当的水平。这些结果表明，现代视觉-语言模型已经编码了专业领域所需的大部分知识；挑战在于学习如何提问。

## Abstract
We address few-shot object detection with vision-language models (VLMs) in out-of-domain settings such as aerial, industrial, and medical imagery, using only ten annotated images for supervision. Existing adaptation methods are discrete prompt optimization and LoRA fine-tuning. We revisit a third option: soft prompting, where a small number of continuous prompt tokens are optimized while the pretrained backbone remains frozen.   We identify two key design choices. First, placing prompt tokens at the cross-modal boundary between visual and text tokens outperforms other placements (10.0 vs. 8.4 mAP). Second, initializing prompts from the empty space token outperforms semantic and random initialization.   With these choices, one to three learned tokens (7,168 parameters on average) match the best LoRA configuration on Roboflow20-VL (14.2 mAP, 10-shot) while training over 20,000x fewer parameters. Soft prompting remains harder to optimize, exhibiting higher variance across random seeds. Unlike LoRA, however, it causes no forgetting: the LoRA rank matching our accuracy reduces NaturalBench VQA accuracy by 35% relative, rising to 56% at the largest rank, whereas soft prompting leaves pretrained performance unchanged.   The learned tokens behave like prompts rather than weights. They transfer to a newer model without retraining (+0.8 mAP on Qwen3.5-9B) and can be verbalized into readable prompts competitive with prompt-search methods (matching DetPO and outperforming GEPA).   The approach also extends beyond detection. On RoboCasa manipulation tasks, the frozen $π_{0.5}$ vision-language-action policy benefits from soft prompting, matching the LoRA baseline on two of three tasks when tokens are placed at the gradient bottleneck. These results suggest modern VLMs already encode much of what is needed for specialized domains; the challenge is learning how to ask.

---

## 论文详细总结（自动生成）

这是一份关于论文《你的模型已经知晓，无需教导，学会提问即可：视觉-语言模型的少样本适应软提示方法》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：在航空、工业及医疗等特定领域，获取大规模标注数据成本极高，因此“少样本（Few-shot）”适应成为关键。
*   **背景挑战**：现有的微调方法（如 LoRA）虽然有效，但存在两个主要问题：一是参数量较大，二是容易导致“灾难性遗忘”，即模型在学习新任务时丧失了原有的通用能力。
*   **核心理念**：现代视觉-语言模型（VLM）本身已具备强大的领域知识，无需通过修改权重来“教导”模型，只需通过优化“提问方式”（即软提示）来激活模型潜力。

### 2. 方法论：软提示（Soft Prompting）
*   **核心思想**：保持预训练模型主干完全冻结，仅在输入端优化极少量的连续提示标记（Prompt Tokens）。
*   **关键技术细节**：
    *   **跨模态边界放置**：将提示标记插入视觉特征与文本特征的交界处，实验证明该位置比其他位置更能有效引导模型。
    *   **空空间标记初始化（Empty Space Initialization）**：使用模型词表中的“空空间”标记作为初始值，比随机初始化或语义初始化更具稳定性与效果。
    *   **极简参数量**：仅需训练 1-3 个标记，平均参数量仅约 7,168 个，相比 LoRA 减少了 20,000 倍以上的训练参数。

### 3. 实验设计
*   **数据集与场景**：
    *   **目标检测**：Roboflow20-VL 数据集（涵盖航空、工业、医疗等领域）。
    *   **机器人操作**：RoboCasa 任务。
    *   **通用能力评估**：NaturalBench VQA（用于评估是否存在灾难性遗忘）。
*   **对比方法**：离散提示优化（Prompt Search）、LoRA 微调、以及不同初始化策略的软提示。

### 4. 资源与算力
*   **说明**：论文未明确列出具体的 GPU 型号、数量或总训练时长。但强调了该方法在参数量上的极度轻量化（仅 7k 参数），暗示其对计算资源的需求远低于传统的全参数或 LoRA 微调。

### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了从目标检测到机器人动作控制的跨领域任务，并进行了消融实验（如位置选择、初始化方式）。
*   **公平性**：通过对比相同性能水平下的 LoRA 秩（Rank），客观展示了软提示在保持通用能力方面的优势，实验设计较为严谨且具有说服力。

### 6. 主要结论与发现
*   **性能表现**：在 10-shot 场景下，软提示达到了与最佳 LoRA 配置相当的性能。
*   **抗遗忘性**：软提示完全保留了预训练模型的通用能力，而 LoRA 在达到同等性能时会导致 VQA 准确率大幅下降（35%-56%）。
*   **可迁移性**：学习到的提示标记具有通用性，可直接迁移至更新的模型（如 Qwen3.5-9B）并获得性能提升。
*   **可解释性**：学习到的标记可转化为可读文本，其效果优于现有的提示搜索方法。

### 7. 优点
*   **极高效率**：参数量极小，训练成本极低。
*   **零遗忘**：完美解决了少样本微调中的灾难性遗忘问题。
*   **通用性强**：不仅适用于视觉检测，还能扩展至视觉-语言-动作（VLA）策略。
*   **可迁移**：提示标记不绑定特定模型权重，具备跨模型复用价值。

### 8. 不足与局限
*   **优化难度**：软提示的训练过程比 LoRA 更不稳定，在不同随机种子下表现出较高的性能方差。
*   **应用限制**：虽然在特定任务上表现优异，但对于极其复杂的任务，其性能上限可能仍受限于提示标记的表达能力。
*   **缺乏算力细节**：未提供详细的训练环境配置，使得复现时的资源评估存在一定模糊性。

（完）
