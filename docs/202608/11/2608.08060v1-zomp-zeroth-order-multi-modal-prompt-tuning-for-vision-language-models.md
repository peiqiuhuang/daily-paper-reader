---
title: "ZOMP: Zeroth-Order Multi-Modal Prompt Tuning for Vision-Language Models"
title_zh: ZOMP：视觉-语言模型的零阶多模态提示微调
authors: "Sajjad Ghiasvand, Yifan Yang, Mahnoosh Alizadeh, Ramtin Pedarsani"
date: 2026-08-08
pdf: "https://arxiv.org/pdf/2608.08060v1"
tags: ["query:bopl"]
score: 8.0
evidence: 零阶多模态提示微调
tldr: 针对内存受限设备或闭源模型无法进行反向传播的问题，本文提出ZOMP，一种高效的零阶多模态提示微调方法。ZOMP通过SPSA算法同时优化CLIP的视觉和文本分支，并引入跨模态低秩重参数化、梯度校正动量及动态秩调度机制。在13个基准测试中，ZOMP在查询效率、少样本准确率及泛化能力上均显著优于现有零阶方法，为实用化零阶微调提供了新路径。
selection_source: fresh_fetch
motivation: 传统的反向传播微调在边缘设备或仅提供推理接口的闭源模型上不可行，而现有的零阶方法往往效率低下且仅限于单模态。
method: 提出ZOMP方法，利用跨模态低秩重参数化降低搜索空间，并结合梯度校正动量和动态秩调度进行双模态提示优化。
result: 在13个视觉语言基准测试中，ZOMP在有限的查询预算下表现出优异的少样本准确率、跨数据集迁移能力和分布外泛化性。
conclusion: 实验证明，结合多模态协同优化与低秩结构是实现高效、实用的无反向传播提示微调的有效途径。
---

## 摘要
微调 CLIP 等视觉-语言模型通常需要对整个模型进行反向传播 (BP)，但在仅提供前向传播访问权限的情况下（这在内存受限的边缘设备和专有模型部署中很常见），这是不可行的。先前的无 BP 零阶提示微调方法避免了这一要求，但通常仅在单一模态中微调提示，或者在足够大的搜索空间上进行优化，以至于收敛需要数千次前向传播，这在现实的查询预算下是不切实际的。我们提出了 ZOMP（零阶多模态提示微调），这是一种查询高效、完全仅前向的方法，它使用同时扰动随机逼近在冻结的 CLIP 模型的视觉和文本分支中微调深度提示。ZOMP 结合了三个要素：一种跨模态低秩重参数化，通过共享因子将两个分支联系起来并保持较小的有效搜索维度；一个梯度修正动量项，用于稳定噪声较大的零阶估计；以及一个预算索引的秩调度，随着查询预算的消耗逐步释放容量。在 5,000 次查询预算下的 13 个视觉-语言基准测试中，ZOMP 在少样本准确率和查询效率方面始终优于先前的无 BP 提示微调方法，并且在从基础到新类、跨数据集迁移和分布外设置中具有更好的泛化能力。我们的结果表明，共同利用多模态和低秩结构是实现实用、查询高效的无 BP 提示微调的有效途径。

## Abstract
Fine-tuning vision-language models such as CLIP typically requires backpropagation (BP) through the full model, which is infeasible when only forward-pass access is available, as is common for memory-constrained edge devices and proprietary model deployments. Prior BP-free, zeroth-order prompt-tuning methods avoid this requirement but often tune prompts in a single modality or optimize over a search space large enough that convergence requires thousands of forward passes, which is impractical under realistic query budgets. We propose ZOMP (Zeroth-Order Multimodal Prompt tuning), a query-efficient, fully forward-only method that tunes deep prompts in both the vision and text branches of a frozen CLIP model using simultaneous perturbation stochastic approximation. ZOMP combines three ingredients: a cross-modal low-rank reparameterization that ties the two branches through a shared factor and keeps the effective search dimensionality small, a gradient-correction momentum term that stabilizes the noisy zeroth-order estimate, and a budget-indexed rank schedule that unlocks capacity as the query budget is spent. Across 13 vision-language benchmarks under a matched 5,000-query budget, ZOMP consistently outperforms prior BP-free prompt-tuning methods in both few-shot accuracy and query efficiency, and it generalizes better across base-to-new, cross-dataset transfer, and out-of-distribution settings. Our results show that jointly exploiting multimodality and low-rank structure is an effective route to practical, query-efficient BP-free prompt tuning.

---

## 论文详细总结（自动生成）

以下是对论文《ZOMP: Zeroth-Order Multi-Modal Prompt Tuning for Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义
论文针对视觉-语言模型（如 CLIP）在特定场景下的微调难题展开研究。在边缘设备（内存受限无法存储梯度图）或商业模型 API（仅开放推理接口，无法获取权重和梯度）的情况下，传统的反向传播（BP）微调无法实施。现有的无梯度（BP-free）或零阶（ZO）优化方法存在搜索空间过大、查询效率低、或仅能优化单一模态等局限。ZOMP 旨在通过一种高效的零阶优化方案，在极有限的查询预算内实现深度多模态提示微调。

### 2. 论文提出的方法论
ZOMP 的核心思想是利用低秩结构和动态调度来降低零阶优化的方差并提高收敛速度。
*   **跨模态低秩重参数化**：不直接优化高维提示矩阵，而是将其分解为共享因子 $U$ 和模态特定因子 $V$。这种设计通过共享因子将视觉和文本分支耦合，使一次查询产生的信号能同时更新两个分支，显著降低了有效搜索维度。
*   **预算索引的秩调度（Progressive Rank Expansion）**：在优化初期仅激活秩为 1 的子空间进行粗粒度搜索，随着查询预算的消耗（如达到 20% 时），再解锁全部秩。这种“由简入繁”的策略避免了初期在高维空间中的无效搜索。
*   **梯度校正动量（SPSA-GC）**：采用同时扰动随机逼近（SPSA）估计梯度，并引入动量项来平滑零阶估计带来的高噪声，确保优化轨迹的稳定性。

### 3. 实验设计
*   **数据集与场景**：涵盖了 13 个视觉语言基准数据集（包括 ImageNet、Caltech101、OxfordPets、EuroSAT 等），测试场景包括少样本分类（Few-shot）、基础类到新类泛化（Base-to-new）、跨数据集迁移（CDT）以及分布外（OOD）鲁棒性测试。
*   **Benchmark 与对比方法**：以 5,000 次前向查询为统一预算上限。对比了多种无 BP 的前沿方法，包括 BAR、BlackVIP、BPT-VLM 以及目前最强的零阶方法 ZIP。

### 4. 资源与算力
*   **硬件环境**：实验在 4 台 NVIDIA A6000 GPU 上运行。
*   **训练成本**：论文详细记录了训练时长（平均约 57.7 分钟）和推理延迟。所有方法均在相同的 5,000 次查询预算下进行公平比较。

### 5. 实验数量与充分性
*   **实验规模**：论文在 13 个不同领域的分类任务上进行了广泛测试，并针对 ImageNet 的 4 个变体进行了 OOD 评估。
*   **消融实验**：作者进行了详尽的消融研究，验证了低秩分解、动量机制、秩调度以及跨模态共享因子各自的贡献。
*   **客观性**：所有基准测试均采用相同的骨干网络（CLIP ViT-B/16）和相同的随机种子（3 次重复实验取均值），确保了对比的公平性。

### 6. 主要结论与发现
*   ZOMP 在 13 个数据集上的平均准确率达到 68.2%，显著超过了 ZIP（62.2%）和其他零阶方法。
*   在泛化能力上，ZOMP 在新类识别和跨数据集迁移中表现出更强的鲁棒性，证明了多模态协同优化优于单模态优化。
*   秩调度与动量机制具有协同效应：秩调度在初期降低了噪声，而动量则帮助模型在秩解锁后平稳过渡。

### 7. 优点
*   **查询效率极高**：在极少的模型调用次数下即可达到较高的准确率，非常适合 API 调用受限的场景。
*   **多模态深度融合**：突破了以往零阶方法只能做浅层或单模态提示的限制，实现了深层视觉和文本的联合微调。
*   **参数高效**：通过低秩分解，在保持表达能力的同时极大地压缩了可学习参数量。

### 8. 不足与局限
*   **超参数依赖**：秩调度的解锁时机（如 20% 预算处）虽然在文中表现稳健，但在极端不同的任务中可能仍需微调。
*   **架构限制**：目前主要针对 CLIP 这种双塔结构设计，对于更复杂的交织型多模态大模型（如 GPT-4V 类）的适用性尚待验证。
*   **查询成本**：尽管比同类方法高效，但 5,000 次查询对于某些实时性要求极高的边缘端在线学习场景可能依然偏重。

（完）
