---
title: "Naive Prompt Optimization: Rethinking the Need for Complex Prompt Search"
title_zh: 朴素提示词优化：重新思考复杂提示词搜索的必要性
authors: "Yuan Chang, Xiaoqi Chen"
date: 2026-08-27
pdf: "https://arxiv.org/pdf/2608.27266v1"
tags: ["query:bopl"]
score: 9.0
evidence: 迭代提示词优化与搜索
tldr: 本研究针对当前提示词优化方法日益复杂化的问题，提出了“朴素提示词优化”（NPO）。NPO 是一种轻量级的单谱系方法，通过教师模型结合反馈迭代修正提示词。实验表明，NPO 在性能上可媲美甚至超越 GEPA 等复杂搜索算法，且所需采样次数更少。研究发现，增强教师模型的推理能力可替代复杂的优化搜索，且优化后的提示词在不同模型间具有良好的迁移性，为实现高效的智能体递归自我提升提供了新思路。
selection_source: fresh_fetch
motivation: 旨在挑战当前提示词优化领域过度追求复杂搜索算法的趋势，探索更高效、简洁的优化路径。
method: 提出 NPO 方法，利用教师模型根据任务反馈对提示词进行单线性的迭代修订，而非进行复杂的树状或图状搜索。
result: NPO 在多个任务中以更少的采样次数达到了与复杂方法相当或更优的性能，且优化结果在同系列模型间表现出显著的迁移性。
conclusion: 简单的线性提示词优化足以抗衡复杂的搜索程序，强大的教师模型推理能力可以有效降低优化算法的复杂度。
---

## 摘要
在各种任务中高效地改进自主智能体是加速代理式人工智能（agentic AI）递归自我改进（RSI）的核心，而提示词优化作为一种极具前景的方法脱颖而出，它能够提供与微调模型权重相当的性能提升，同时降低优化和推理过程中的计算成本。然而，近期的发展趋势日益倾向于使用不必要且复杂的提示词优化器。我们引入了朴素提示词优化（NPO），这是一种轻量级的单谱系方法，它利用带有展开（rollout）反馈的教师模型来迭代修订提示词。NPO 以更少的展开次数实现了与 GEPA 相当或更好的性能，且其优势随着教师能力的增强而扩大，这表明更强的教师推理能力可以部分替代优化器端的搜索复杂度。在交互式游戏中，NPO 与 GEPA 相比保持了广泛的竞争力，而 GRPO 在一些不太适合提示词优化的任务上表现更好。我们还展示了经 NPO 优化的提示词在逐字应用于其他学生模型时，也能诱发出类似的性能提升，尤其是在同一系列的模型之间。总之，我们的初步结果表明，简单的线性提示词优化可以与更为精密和复杂的搜索程序相媲美。

## Abstract
Efficiently improving autonomous agents across diverse tasks is central to accelerating recursive self-improvement (RSI) in agentic AI, with prompt optimization emerging as a promising approach capable of delivering performance gains comparable to those achieved by fine-tuning model weights, while reducing computational costs in both optimization and serving. However, recent developments increasingly favor unnecessarily complex prompt optimizers. We introduce Naive Prompt Optimization (NPO), a lightweight single-lineage method that iteratively revises prompts using a teacher model with rollout feedback. NPO achieves comparable or better performance than GEPA with fewer rollouts, and its advantage increases with stronger teacher models, suggesting that stronger teacher reasoning can partially substitute for optimizer-side search complexity. In interactive games, NPO remains broadly competitive with GEPA, while GRPO performs better on some tasks less amenable to prompt optimization. We also show that NPO-optimized prompts elicit similar performance improvements when applied verbatim to other student models, especially across models within the same family. Overall, our preliminary results show that simple, linear prompt optimization can rival substantially more sophisticated and complex search procedures.

---

## 论文详细总结（自动生成）

这篇论文对当前大语言模型（LLM）提示词优化领域日益复杂化的趋势提出了质疑，并提出了一种极其简洁但高效的替代方案。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的自动提示词优化（APO）方法（如 GEPA、OPRO、ProTeGi 等）正变得越来越复杂，通常涉及复杂的搜索策略（如波束搜索、帕累托选择、贝叶斯优化等）。作者质疑：**这种复杂的搜索过程是否真的必要？**
*   **研究动机**：在代理式 AI（Agentic AI）的递归自我改进（RSI）过程中，提示词优化比权重微调更具轻量化和可移植性。作者希望探索一种更简单、线性的优化路径，并验证强大的教师模型推理能力是否可以弥补算法复杂度的不足。

### 2. 论文提出的方法论：Naive Prompt Optimization (NPO)
*   **核心思想**：NPO 抛弃了维护多个提示词候选池或复杂搜索树的做法，采用**单谱系（Single-lineage）迭代修订**。
*   **关键技术细节**：
    *   **教师-学生架构**：一个强大的“教师模型”（如 GPT-5.5）负责观察“学生模型”（如 Qwen3-8B）在任务中的表现并修订提示词。
    *   **滑动窗口反馈（Sliding-Window Feedback）**：教师模型不仅看到当前的提示词和得分，还能看到最近 $W$ 次迭代的完整**执行轨迹（Rollout Traces）**和具体奖励。
    *   **线性演化**：每一轮只生成一个新版本的提示词，直接替换旧版本，形成一条线性的改进链条。
*   **算法流程**：
    1. 使用当前提示词 $P(i)$ 运行学生模型，收集 $N$ 个任务的执行轨迹和奖励 $R_i$。
    2. 将最近 $W$ 轮的提示词、轨迹和奖励输入教师模型。
    3. 教师模型根据反馈生成修订后的提示词 $P(i+1)$。
    4. 重复上述过程直至达到迭代上限。

### 3. 实验设计
*   **数据集/场景**：
    *   **IFBench**：评估模型在可验证约束下的指令遵循能力。
    *   **HotpotQA**：多跳问答基准测试。
    *   **TextArena**：包含 22 种交互式游戏（如扫雷、2048、国际象棋、扑克等），涉及复杂的战略规划。
*   **对比方法（Baselines）**：
    *   **GEPA**：一种基于帕累托选择和反射的复杂提示词搜索方法。
    *   **GRPO**：一种基于权重的强化学习微调方法（作为性能上限参考）。
*   **受控变量**：
    *   **教师模型变量**：对比了 Qwen3-8B、DeepSeek-V4-Flash 和 GPT-5.5 作为优化器的效果。
    *   **学生模型变量**：主要使用 Qwen3-8B 和 Llama-3.1-8B。

### 4. 资源与算力
*   **算力设备**：论文提到在训练 GRPO 基准时使用了 **2 台 NVIDIA H100 GPU**，并采用了 ZeRO-3 优化。
*   **优化成本**：提示词优化（NPO 和 GEPA）主要依赖 API 调用。NPO 在 IFBench 上使用了约 3500 次展开（Rollouts），在 HotpotQA 上使用了约 6800 次，略低于 GEPA 的预算。
*   **上下文窗口**：教师模型使用了高达 100 万 token 的上下文窗口（针对 GPT-5.5 和 DeepSeek），以容纳长轨迹反馈。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础问答到复杂策略游戏的 24 个不同任务环境，实验设计较为全面。
*   **客观性与公平性**：
    *   **共享伪随机性**：在对比不同方法时，使用相同的随机种子生成环境实例，确保评估在相同难度的任务上进行。
    *   **约束解码（Constrained Decoding）**：为了消除格式错误对性能评估的干扰，强制模型输出符合特定结构的 token，从而纯粹比较决策质量。
    *   **迁移性实验**：额外进行了跨模型（同系列及跨系列）的提示词迁移实验，验证了优化结果的普适性。

### 6. 论文的主要结论与发现
*   **简单胜过复杂**：NPO 这种简单的线性方法在性能上可以媲美甚至超越复杂的 GEPA 搜索程序，且收敛速度更快（所需展开次数更少）。
*   **教师能力替代搜索复杂度**：随着教师模型能力的增强（如从 Qwen 升级到 GPT-5.5），NPO 的优势更加明显。这表明**强大的推理能力可以抵消对复杂搜索算法的需求**。
*   **良好的迁移性**：在小模型上优化出的提示词，直接应用到同系列或跨系列的大模型上，通常也能带来显著的性能提升。
*   **与 RL 的互补性**：在某些提示词优化难以奏效的任务上，GRPO（权重微调）表现更好；但在大多数逻辑和指令遵循任务中，提示词优化已足够强大。

### 7. 优点：亮点与创新
*   **方法论的极简主义**：挑战了“越复杂越好”的固有思维，证明了反馈质量（完整轨迹）比搜索算法更重要。
*   **严谨的实验控制**：通过约束解码和共享种子，极大地降低了实验噪声，使结论更具说服力。
*   **实用性强**：展示了提示词的跨模型迁移能力，这对于实际部署中节省优化成本具有重要意义。

### 8. 不足与局限
*   **任务复杂度上限未知**：目前的实验主要集中在短中程任务，尚不清楚 NPO 在极长程、超大规模智能体环境中的表现。
*   **学生模型限制**：未在最顶尖的闭源模型（如 GPT-5.5 本身）作为学生模型时进行测试，主要是受限于 API 的 logit 访问和解码控制权限。
*   **RL 不稳定性**：论文提到 RL 训练在某些任务上仍不稳定，这可能影响了提示词优化与 RL 之间对比的全面性。
*   **数据隐私**：NPO 需要将详细的执行轨迹发送给第三方教师模型 API，在处理敏感数据时存在隐私风险。

（完）
