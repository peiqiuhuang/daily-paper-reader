---
title: "PISmith: Reinforcement Learning-based Red Teaming for Prompt Injection Defenses"
title_zh: PISmith：基于强化学习的提示注入防御红队测试框架
authors: "Chenlong Yin, Runpeng Geng, Yanting Wang, Jinyuan Jia"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13026v1"
tags: ["query:bopl"]
score: 8.0
evidence: 训练攻击LLM以优化注入提示词
tldr: 针对大语言模型应用中日益严重的提示注入风险，本文提出了 PISmith，一个基于强化学习的红队测试框架。PISmith 在黑盒环境下通过训练攻击模型来优化注入提示，并引入自适应熵正则化和动态优势加权，解决了标准 GRPO 在面对强防御时因奖励稀疏导致的训练困难。实验表明，PISmith 在 13 个基准测试中表现优于 7 种基准方法，揭示了现有 SOTA 防御手段在自适应攻击面前的脆弱性。
selection_source: fresh_fetch
motivation: 现有的提示注入防御措施缺乏针对自适应攻击的鲁棒性评估，可能导致开发者产生虚假的安全感。
method: 提出基于强化学习的 PISmith 框架，通过自适应熵正则化和动态优势加权技术，在黑盒设置下优化攻击提示词。
result: PISmith 在 13 个基准测试中一致取得了最高的攻击成功率，并成功突破了包括 GPT-4o 在内的多种模型的防御。
conclusion: 研究证明现有的 SOTA 提示注入防御手段在自适应攻击面前依然脆弱，亟需更强大的安全防护机制。
---

## 摘要
提示注入对现实世界的 LLM 应用（特别是自主智能体）构成了严重的安全风险。尽管目前已提出了许多防御措施，但它们针对自适应攻击的鲁棒性仍未得到充分评估，这可能导致虚假的安全感。在这项工作中，我们提出了 PISmith，这是一个基于强化学习（RL）的红队测试框架，通过在实际的黑盒设置下训练攻击 LLM 来优化注入提示，从而系统地评估现有的提示注入防御。在这种设置下，攻击者仅能查询受保护的 LLM 并观察其输出。我们发现，由于极端的奖励稀疏性，直接应用标准 GRPO 攻击强防御措施会导致性能欠佳——大多数生成的注入提示会被防御拦截，导致策略熵在发现有效攻击策略之前就发生坍缩，且罕见的成功案例无法得到有效学习。为此，我们引入了自适应熵正则化和动态优势加权，以维持探索并强化对稀缺成功案例的学习。在 13 个基准测试上的广泛评估表明，最先进的提示注入防御在自适应攻击面前依然脆弱。我们还将 PISmith 与静态、基于搜索和基于 RL 的 7 个基准攻击方法进行了对比，结果显示 PISmith 始终保持最高的攻击成功率。此外，PISmith 在 InjecAgent 和 AgentDojo 的智能体场景中，针对开源和闭源 LLM（如 GPT-4o-mini 和 GPT-5-nano）均表现出强劲性能。我们的代码已开源至 https://github.com/albert-y1n/PISmith。

## Abstract
Prompt injection poses serious security risks to real-world LLM applications, particularly autonomous agents. Although many defenses have been proposed, their robustness against adaptive attacks remains insufficiently evaluated, potentially creating a false sense of security. In this work, we propose PISmith, a reinforcement learning (RL)-based red-teaming framework that systematically assesses existing prompt-injection defenses by training an attack LLM to optimize injected prompts in a practical black-box setting, where the attacker can only query the defended LLM and observe its outputs. We find that directly applying standard GRPO to attack strong defenses leads to sub-optimal performance due to extreme reward sparsity -- most generated injected prompts are blocked by the defense, causing the policy's entropy to collapse before discovering effective attack strategies, while the rare successes cannot be learned effectively. In response, we introduce adaptive entropy regularization and dynamic advantage weighting to sustain exploration and amplify learning from scarce successes. Extensive evaluation on 13 benchmarks demonstrates that state-of-the-art prompt injection defenses remain vulnerable to adaptive attacks. We also compare PISmith with 7 baselines across static, search-based, and RL-based attack categories, showing that PISmith consistently achieves the highest attack success rates. Furthermore, PISmith achieves strong performance in agentic settings on InjecAgent and AgentDojo against both open-source and closed-source LLMs (e.g., GPT-4o-mini and GPT-5-nano). Our code is available at https://github.com/albert-y1n/PISmith.

---

## 论文详细总结（自动生成）

以下是对论文《PISmith: Reinforcement Learning-based Red Teaming for Prompt Injection Defenses》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的提示注入（Prompt Injection, PI）防御措施（如过滤型和预防型防御）虽然在静态测试中表现良好，但在面对能够根据防御策略进行自我调整的“自适应攻击”时，其鲁棒性仍未得到充分验证。
*   **研究背景**：提示注入对自主智能体（Agents）等现实应用构成严重威胁。现有的红队测试工具（如基于搜索或基础强化学习的方法）在面对强防御时，往往会遇到“奖励极度稀疏”的问题——即攻击模型生成的绝大多数提示都会被拦截，导致模型无法获得有效的学习信号，进而产生策略熵坍缩（停止探索）或学习效率低下的问题。

### 2. 方法论：PISmith 框架
PISmith 是一个基于强化学习（RL）的黑盒红队测试框架，其核心思想是优化攻击 LLM（策略网络），使其能生成突破防御的注入提示。
*   **基础算法**：采用 **GRPO**（群体相对策略优化）作为基础，通过群体内的相对奖励来计算优势（Advantage），无需额外的价值模型。
*   **关键技术 1：自适应熵正则化（Adaptive Entropy Regularization）**：
    *   为了防止在奖励稀疏时模型陷入重复的失败模式（熵坍缩），PISmith 引入了动态熵奖励。
    *   当平均奖励 $\bar{r}$ 较低时，增大熵正则化系数 $\beta(\bar{r})$，强制模型进行更多样化的探索；当奖励提升后，逐渐减小系数以转向利用（Exploitation）。同时设置熵上限（Entropy Cap）防止生成无意义的乱码。
*   **关键技术 2：动态优势加权（Dynamic Advantage Weighting）**：
    *   针对“成功案例极少”的情况，引入放大因子 $\gamma(\bar{r})$。
    *   当成功率极低时，人为放大那些罕见成功样本的梯度贡献，确保模型能从极少数的突破中高效学习，而不被大量的失败样本淹没。
*   **流程**：攻击者仅需通过黑盒接口查询受保护的 LLM，观察输出并利用“LLM-as-judge”判定是否攻击成功，以此作为奖励信号训练攻击模型。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用任务（13 个基准）**：涵盖问答（SQuAD v2, Dolly）、检索增强生成（NQ, HotpotQA, MS-MARCO）和长文本任务（LongBench 中的多个子集）。
    *   **智能体场景**：在 InjecAgent 和 AgentDojo 上进行评估，涉及工具调用和多步执行。
*   **对比方法（7 种基准）**：
    *   **静态攻击**：直接注入、组合攻击（Combined Attack）。
    *   **基于搜索**：TAP、PAIR、Strategy。
    *   **基于强化学习**：Vanilla GRPO、RL-Hammer。
*   **目标模型与防御**：评估了 Meta-SecAlign-8B 以及闭源模型 GPT-4o-mini、GPT-4.1-nano 和 GPT-5-nano。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：论文提供了一份“查询效率”对比。PISmith 在训练阶段仅需 15,000 次查询，推理阶段 17,000 次，总查询量（32k）远低于基于搜索的方法（如 Strategy 需要 194k 次），且优于同类 RL 方法 RL-Hammer（47k 次）。
*   **模型规模**：攻击模型使用的是 Qwen3-4B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：在 13 个不同的数据集上进行了跨任务泛化测试，并在 8 种不同的防御机制上进行了压力测试。
*   **消融实验**：专门针对“自适应熵正则化”和“动态优势加权”进行了消融研究，证明了两个组件在克服奖励稀疏方面的必要性。
*   **客观性**：实验涵盖了开源和最先进的闭源模型（如 GPT-5 预览版），并使用了统一的 LLM 裁判标准，评估较为全面且公平。

### 6. 主要结论与发现
*   **防御的脆弱性**：即使是目前最先进的防御（如 Meta-SecAlign），在 PISmith 的自适应攻击下也几乎完全失效（ASR@10 达到 1.0，平均 ASR@1 达到 0.87）。
*   **效用-鲁棒性权衡**：实验揭示了一个严峻的现实——现有的防御措施无法在保持高任务效用（Utility）的同时有效抵御自适应攻击。强力拦截攻击的防御往往会严重损害模型处理正常请求的能力。
*   **跨模型泛化**：在较弱模型（Qwen3-4B）上训练的攻击策略可以成功迁移并突破 GPT-5-nano 等顶级闭源模型的防御。

### 7. 优点（亮点）
*   **解决核心痛点**：通过自适应机制解决了强化学习在安全领域常见的“奖励稀疏”和“熵坍缩”难题。
*   **高效率**：相比于每条数据都要重新搜索的 TAP/PAIR，PISmith 训练出的策略模型在推理时速度更快、成本更低。
*   **实战价值**：在复杂的智能体工作流（Agentic workflows）中表现出色，证明了其在真实安全评估中的潜力。

### 8. 不足与局限
*   **黑盒依赖**：虽然是黑盒攻击，但仍需要能够获取目标模型的文本输出以进行奖励计算，对于完全不返回结果或仅返回错误码的极端防御可能受限。
*   **训练数据依赖**：虽然仅需 100 条数据，但仍需要针对特定任务类型进行微调才能达到最佳效果。
*   **防御建议较少**：论文重点在于“破”，虽然揭示了防御的不足，但对于如何构建“不可破”的防御仅给出了方向性的建议，缺乏具体的新型防御方案。

（完）
