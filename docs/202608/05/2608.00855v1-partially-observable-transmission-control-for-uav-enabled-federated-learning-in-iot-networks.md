---
title: Partially-Observable Transmission Control for UAV-Enabled Federated Learning in IoT Networks
title_zh: 物联网中无人机辅助联邦学习的部分可观测传输控制
authors: "Masoud Ghazikor, Zhou Ni, Morteza Hashemi"
date: 2026-08-01
pdf: "https://arxiv.org/pdf/2608.00855v1"
tags: ["query:bopl"]
score: 8.0
evidence: 公平共识双层优化 (FCB)
tldr: 针对无人机辅助的物联网联邦学习中，非授权频段干扰导致的传输不可靠问题，本文提出了一个包级别的传输框架。通过建立丢包模型并引入包交付率（PDR）来衡量部分更新聚合，构建了公平共识双层（FCB）优化问题。利用交替优化算法协同控制传输阈值与功率，在保证公平性的同时提升了聚合效率和模型训练性能。
selection_source: fresh_fetch
motivation: 无人机辅助联邦学习在共享非授权频段中面临严重的干扰耦合和传输不可靠挑战。
method: 提出一种公平共识双层优化框架，通过共识阈值控制器和公平功率控制器协同优化包交付率。
result: 实验表明，该优化器显著提升了包级别的更新交付成功率，在CNN联邦学习任务中表现优于基准策略。
conclusion: 该研究通过精细化的包级别传输控制，有效解决了物联网环境下联邦学习的通信瓶颈与公平性问题。
---

## 摘要
无人机（UAV）辅助的联邦学习（FL）可以为大规模物联网部署提供灵活、按需的边缘智能，但在共享的非授权频段运行使得上行链路更新传输受到干扰耦合且不可靠。本文开发了一个数据包级传输框架，该框架捕捉了缓冲区溢出、延迟违规和传输错误，并利用由此产生的数据包交付率（PDR），通过数据包化的伯努利掩码 FL 聚合过程来表示部分更新的接收情况。随后，我们制定了一个公平-共识双层（FCB）优化问题，共同控制：(i) 传输阈值，以在部分可观测性下达成共识的同时最大化平均 PDR；(ii) 传输功率，以改善最差的 PDR 并确保物联网学习者之间的公平性。为了解决这一问题，我们提出了一种交替 FCB 优化器，由基于共识的阈值控制器（CTC）和基于公平的功率控制器（FPC）组成。CTC 驱动物联网学习者在传输阈值上达成高效 PDR 的共识，而 FPC 则在生成的共识阈值下更新传输功率，以改善最差 PDR 并确保公平性。基于 CNN 的 FL 任务数值结果表明，FCB 优化器通过增强数据包级更新传输，提升了 FL 聚合和训练性能，且始终优于基准传输策略。

## Abstract
Uncrewed aerial vehicle (UAV)-enabled federated learning (FL) can provide flexible, on-demand edge intelligence for large-scale IoT deployments, but operating in shared unlicensed bands makes uplink update delivery interference-coupled and unreliable. In this paper, we develop a packet-level transmission framework that captures buffer overflow, delay violations, and transmission errors, and uses the resulting packet delivery ratio (PDR) to represent partial-update reception through a packetized, Bernoulli-masked FL aggregation process. We then formulate a fairness-consensus bilevel (FCB) optimization that jointly controls (i) transmission thresholds to maximize the average PDR while reaching consensus under partial observability and (ii) transmission powers to improve the worst PDR and enforce fairness across IoT learners. To solve this problem, we propose an alternating FCB optimizer composed of a consensus-based threshold controller (CTC), which drives the IoT learners toward a PDR-efficient consensus on transmission thresholds, and a fairness-based power controller (FPC), which updates transmission powers to improve the worst PDR and ensure fairness under the resulting consensus thresholds. Numerical results on CNN-based FL tasks show that the FCB optimizer improves FL aggregation and training performance by enhancing packet-level update delivery, consistently outperforming baseline transmission policies.

---

## 论文详细总结（自动生成）

以下是对论文《Partially-Observable Transmission Control for UAV-Enabled Federated Learning in IoT Networks》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：无人机（UAV）作为移动边缘服务器，能为大规模物联网（IoT）提供灵活的联邦学习（FL）支持。然而，在共享的非授权频段（如ISM频段）中，上行链路传输面临严重的干扰耦合、缓冲区溢出和延迟违规问题。
*   **核心问题**：现有的无线联邦学习研究大多假设模型更新是“全有或全无”的可靠传输，忽略了数据包级别的丢失。本文旨在解决在**部分可观测**环境下，如何通过精细化的传输控制（阈值与功率），处理因信道不可靠导致的部分模型更新（Partial-update）接收问题。

### 2. 方法论
论文提出了一个**公平共识双层（FCB）优化框架**，核心思想是将通信可靠性与FL聚合过程深度耦合：
*   **数据包级传输模型**：综合考虑了缓冲区溢出概率（$P_{on}$）、延迟违规概率（$P_{dn}$）和干扰导致的传输错误率（$P_{en}$），定义了数据包交付率（PDR）。
*   **部分更新聚合**：将本地模型更新拆分为多个数据包，利用伯努利掩码（Bernoulli Masking）模拟数据包丢失，使UAV服务器仅能聚合部分受损的更新。
*   **FCB 优化器**：
    *   **内层（共识阈值控制 - CTC）**：在部分可观测（存在丢包或节点不在线）的情况下，通过分布式坐标搜索，使所有学习者在传输阈值 $\beta$ 上达成共识，以最大化平均 PDR。
    *   **外层（公平功率控制 - FPC）**：在共识阈值基础上，动态调整各节点的发射功率 $P$，利用 Jain's Index 确保公平性，并最大化最差节点的 PDR（瓶颈优化）。
*   **算法流程**：采用交替优化策略，CTC 负责效率与共识，FPC 负责公平与瓶颈提升。

### 3. 实验设计
*   **场景设置**：100x100 $m^2$ 区域，20个物联网学习者，1个高度为100m的无人机服务器。
*   **数据集与模型**：使用 **CIFAR-10** 数据集，模型为 **ResNet18**（约1100万参数）。
*   **数据分布**：涵盖了 IID（独立同分布）、中度 Non-IID（Dirichlet $\alpha_d=0.25$）和重度 Non-IID（$\alpha_d=0.05$）三种场景。
*   **基准方法（Benchmarks）**：
    1.  **理想信道（Ideal Channel）**：无损传输上限。
    2.  **激进策略（Aggressive）**：低阈值、高功率，倾向于频繁发送但干扰大。
    3.  **保守策略（Conservative）**：高阈值、低功率，倾向于排队等待但利用率低。
    4.  **FCB（本文方法）**：在不同观测概率 $\psi$ 下的表现。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA RTX 3090 GPU**。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：模型大小约 42.63 MB，每个更新拆分为 37,247 个数据包。文中未明确提及总训练时长，但给出了通信轮数（最高达1000轮）。

### 5. 实验数量与充分性
*   **实验规模**：针对三种数据分布（IID, Mid Non-IID, Strong Non-IID）分别进行了长达 300 到 1000 轮的收敛性实验。
*   **消融与鲁棒性**：测试了不同部分可观测因子（$\psi=0.1$ 和 $0.4$）对算法收敛的影响，证明了 CTC 算法在节点间歇性参与时的稳健性。
*   **评价指标**：包括平均 PDR、Jain's 公平指数、测试准确率和收敛速度。实验设计较为全面，对比了极端策略，能够客观反映 FCB 在复杂无线环境下的增益。

### 6. 主要结论与发现
*   **传输性能**：FCB 优化器实现了 0.90 的平均 PDR 和 0.99 的公平指数，显著优于激进策略（PDR 0.39）和保守策略（PDR 0.49）。
*   **收敛速度**：在 IID 场景下，FCB 达到 55% 准确率的速度比基准方法快 **30% - 44%**。
*   **Non-IID 鲁棒性**：在重度 Non-IID 场景下，FCB 的最终准确率比基准方法高出 **32% - 79%**，说明在数据异构时，保证传输可靠性对模型性能至关重要。
*   **部分可观测性**：即使在较高的观测丢弃率下，FCB 仍能通过增加内部迭代次数收敛至最优解。

### 7. 优点
*   **模型精细**：不同于传统的“丢弃整个模型”假设，本文建立了包级别的部分接收模型，更符合真实无线通信物理特性。
*   **双层优化设计**：巧妙地结合了“共识”（提升整体效率）与“公平”（照顾弱势节点）两个维度，解决了干扰耦合环境下的资源分配难题。
*   **实用性强**：考虑了非授权频段的干扰和缓冲区限制，对无人机边缘计算的实际部署有指导意义。

### 8. 不足与局限
*   **节点规模较小**：实验仅模拟了 20 个学习者，在大规模（成百上千节点）物联网环境下的算法计算开销和收敛压力尚待验证。
*   **无人机移动性简化**：虽然背景提到无人机是移动服务器，但实验中无人机位置似乎是固定的，未深入探讨无人机轨迹优化与传输控制的联合动态影响。
*   **缺乏实测验证**：目前完全基于数值仿真，虽然计划未来在 SDR（软件定义无线电）平台上验证，但当前缺乏真实环境中的多径衰落和动态干扰数据支持。

（完）
