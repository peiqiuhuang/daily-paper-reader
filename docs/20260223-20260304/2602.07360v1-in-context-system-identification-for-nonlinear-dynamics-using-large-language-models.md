---
title: In-Context System Identification for Nonlinear Dynamics Using Large Language Models
title_zh: 基于大语言模型的非线性动力学上下文系统辨识
authors: Linyu Lin
date: 2026-02-07
pdf: "https://arxiv.org/pdf/2602.07360v1"
tags: ["query:sr"]
score: 10.0
evidence: 利用大语言模型辅助SINDy发现简约的控制方程
tldr: 本研究针对非线性动力学系统识别中SINDy方法依赖专家调优候选库的局限，提出了一种LLM辅助的SINDy流水线。该方法利用大语言模型的上下文学习能力，通过迭代循环不断优化候选方程。在每一轮迭代中，LLM根据当前误差和约束建议新的方程结构，并结合仿真误差进行评估。实验涵盖了63个动力系统数据集及复杂的核反应堆模型，结果显示该方法在符号恢复的结构相似性和预测精度上均显著优于传统SINDy，为自动化发现可解释的物理规律提供了新途径。
selection_source: fresh_fetch
motivation: 传统的SINDy方法在发现非线性动力学方程时高度依赖专家对候选库的预设和调优，难以处理复杂的未知动力学系统。
method: 提出一种将LLM引入循环的SINDy流水线，通过上下文学习迭代优化方程结构，并利用仿真误差和结构相似性作为反馈指标。
result: 在63个ODEBench数据集和核反应堆模型上的测试表明，该方法在符号恢复准确性和测试RMSE方面均优于基准SINDy。
conclusion: 研究证明了LLM能有效引导SINDy在方程空间中的搜索，将数据驱动的误差反馈与符号推理相结合，实现了更准确且具解释性的系统识别。
---

## 摘要
非线性动力学稀疏识别（SINDy）是从数据中发现简洁控制方程的强大方法，但它通常需要专家对候选库进行调优。我们提出了一种 LLM 辅助的 SINDy 流水线，通过上下文学习（in-context learning）将大语言模型（LLM）引入循环，迭代地优化候选方程。该流水线首先使用自适应库拟合基准 SINDy 模型，然后进入 LLM 引导的优化循环。在每次迭代中，当前的最佳方程、误差指标和领域特定约束被汇总到 LLM 的提示词中，由其建议新的方程结构。这些候选方程根据定义的符号形式进行解析，并在训练和测试数据上进行评估。该流水线将基于模拟的误差作为主要指标，同时也评估与真实情况（ground truth）的结构相似性，包括匹配函数形式、关键项、耦合关系和定性行为。如果测试误差低于阈值（NRMSE < 0.1）或达到最大 10 次迭代，迭代停止标准将提前结束优化。最后，选择最佳模型，我们在 63 个动力系统数据集（ODEBench）以及用于沸水核反应堆的 March-Leuba 模型上评估了这种 LLM 辅助的 SINDy。结果与经典 SINDy 进行了对比，表明在复杂动力学情况下，LLM 循环一致地提高了符号恢复能力，与基准 SINDy 相比，具有更高的方程结构相似性和更低的测试均方根误差（RMSE）。这项工作证明了 LLM 可以有效地引导 SINDy 在方程空间中的搜索，将数据驱动的误差反馈与受领域启发的符号推理相结合，从而发现不仅准确而且在结构上具有可解释性的控制方程。

## Abstract
Sparse Identification of Nonlinear Dynamics (SINDy) is a powerful method for discovering parsimonious governing equations from data, but it often requires expert tuning of candidate libraries. We propose an LLM-aided SINDy pipeline that iteratively refines candidate equations using a large language model (LLM) in the loop through in-context learning. The pipeline begins with a baseline SINDy model fit using an adaptive library and then enters a LLM-guided refinement cycle. At each iteration, the current best equations, error metrics, and domain-specific constraints are summarized in a prompt to the LLM, which suggests new equation structures. These candidate equations are parsed against a defined symbolic form and evaluated on training and test data. The pipeline uses simulation-based error as a primary metric, but also assesses structural similarity to ground truth, including matching functional forms, key terms, couplings, qualitative behavior. An iterative stopping criterion ends refinement early if test error falls below a threshold (NRMSE < 0.1) or if a maximum of 10 iterations is reached. Finally, the best model is selected, and we evaluate this LLM-aided SINDy on 63 dynamical system datasets (ODEBench) and march leuba model for boiling nuclear reactor. The results are compared against classical SINDy and show the LLM-loop consistently improves symbolic recovery with higher equation similarity to ground truth and lower test RMSE than baseline SINDy for cases with complex dynamics. This work demonstrates that an LLM can effectively guide SINDy's search through equation space, integrating data-driven error feedback with domain-inspired symbolic reasoning to discover governing equations that are not only accurate but also structurally interpretable.

---

## 论文详细总结（自动生成）

### 论文总结：基于大语言模型的非线性动力学上下文系统辨识

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对**非线性动力学系统辨识**中的经典方法 **SINDy（非线性动力学稀疏识别）** 的局限性展开研究。SINDy 虽然能从数据中提取简洁的物理定律，但其性能高度依赖于人工预设的“候选函数库”。如果真实的物理项不在库中，模型便无法准确恢复方程。
*   **研究动机**：传统方法需要领域专家手动调优函数库，过程繁琐且难以处理复杂未知的动力学。
*   **核心目标**：利用大语言模型（LLM）的科学常识和符号推理能力，自动化地引导方程搜索过程，实现“人在回路”式的迭代优化，从而发现更准确、更具解释性的控制方程。

#### 2. 方法论：核心思想与算法流程
论文提出了一种 **LLM-aided SINDy 流水线**，将 LLM 作为约束性假设生成器，与数值优化闭环结合。
*   **核心思想**：通过上下文学习（In-Context Learning），将当前的拟合误差、数据定性特征（如振荡、饱和）和物理约束反馈给 LLM，由其建议新的函数结构。
*   **关键技术细节**：
    1.  **基准拟合与特征提取**：首先运行标准 SINDy 获得基准模型，并计算状态变量的统计特征（量程、单调性、周期性等）。
    2.  **结构化 Prompt 构造**：向 LLM 提供变量元数据、当前最佳方程、误差诊断信息、结构先验（Soft Priors）以及被拒绝的候选记录。
    3.  **约束性生成**：LLM 必须按照严格的符号语法生成候选方程，且方程必须对未知系数保持线性，形式为：$\dot{x}_i(t) = \sum \theta_{i,k} \phi_k(x, u, t)$。
    4.  **验证与评估**：通过符号解析器验证语法，使用稀疏回归拟合系数，并进行前向仿真。
    5.  **多目标评分与迭代**：结合测试集 NRMSE（归一化均方根误差）、符号复杂度（树节点数）和物理先验惩罚项进行综合评分。若 NRMSE < 0.1 或达到 10 次迭代则停止。

#### 3. 实验设计
*   **数据集/场景**：
    *   **ODEBench**：包含 63 个不同的常微分方程（ODE）系统，涵盖 1D 到 4D 动力学，包括混沌系统（Lorenz）、捕食者- prey 模型、振荡器和反应动力学。
    *   **March-Leuba 模型**：一个复杂的 7 变量沸水核反应堆（BWR）稳定性模型，具有强耦合和非线性反馈。
*   **Benchmark 与对比方法**：
    *   **基准方法**：经典 SINDy（使用固定的、由专家预设的广泛候选库，包含多项式、三角函数、指数等）。
    *   **评估指标**：预测精度（$R^2$、NRMSE）和结构恢复质量（是否捕获了真实方程的关键项）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **资源消耗分析**：由于该方法主要涉及 LLM 的 API 调用（推理阶段）和轻量级的稀疏回归及数值积分，其主要的计算开销在于迭代过程中的多次前向仿真评估，而非深度学习意义上的大规模模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 63 个标准基准系统上进行了全面测试，并增加了一个工业级的核反应堆案例。
*   **充分性评价**：
    *   **覆盖面广**：涵盖了从简单线性到复杂混沌、从低维到高维的多种动力学行为。
    *   **对比客观**：直接与固定库的 SINDy 对比，展示了在库失效时的自适应能力。
    *   **局限性展示**：论文诚实地记录了失败案例（如旋转剪切流动力学），证明了实验的客观性。

#### 6. 主要结论与发现
*   **精度大幅提升**：在 63 个系统中，LLM 辅助方法在 48 个系统上达到了停止标准（NRMSE < 0.1），而基准 SINDy 仅为 30 个。
*   **误差显著降低**：中值误差从基准的 62% 降至 **3.4%**。在基准 SINDy 导致仿真发散的 5 个案例中，LLM 辅助流水线均找到了稳定模型。
*   **结构恢复更优**：LLM 能够识别出基准库中缺失的项（如 $e^{-x}$ 或特定的双线性耦合项），并能作为“结构去噪器”剔除无意义的冗余项。
*   **核反应堆应用**：在复杂的 March-Leuba 模型中，LLM 成功发现了稀疏的双线性结构，而基准方法因过拟合导致仿真崩溃。

#### 7. 优点（亮点）
*   **自动化专家经验**：将原本需要人类专家反复试错的“调整函数库”过程自动化。
*   **可解释性强**：保留了 SINDy 的符号化特征，生成的模型是人类可读的物理公式，而非黑盒。
*   **鲁棒性**：通过仿真误差反馈和结构惩罚，有效防止了纯数据驱动拟合中常见的过拟合和数值不稳定问题。

#### 8. 不足与局限
*   **依赖 LLM 性能**：系统的成功高度依赖于 LLM 建议相关项的能力，若 LLM 无法理解特定复杂的组合，搜索仍会失败。
*   **先验偏见风险**：LLM 可能会倾向于它在训练数据中常见的函数形式，从而忽略某些极端特殊的物理规律。
*   **计算开销**：迭代循环中的多次仿真评估在处理超大规模系统或极长轨迹时可能存在效率瓶颈。
*   **复杂组合限制**：对于像 $\cot(y) \cos(x)$ 这种极特殊的非线性组合，如果 Prompt 中没有明确的基函数引导，LLM 仍难以直接命中。

（完）
