---
title: "Meta-Aligner: Bidirectional Preference-Policy Optimization for Multi-Objective LLMs Alignment"
title_zh: Meta-Aligner：面向多目标大语言模型对齐的双向偏好-策略优化
authors: "Wenzhe Xu, Biao Liu, Yiyang Sun, Xin Geng, Ning Xu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24178v1"
tags: ["query:bopl"]
score: 9.0
evidence: 用于大模型对齐的双层元学习框架
tldr: 针对大语言模型在多目标对齐中静态偏好权重无法捕捉训练中间信息的局限，本文提出Meta-Aligner（Meal）框架。该框架采用双层元学习机制，通过偏好权重网络动态生成自适应权重，并与策略模型进行双向优化。实验证明，该方法在多个多目标基准测试中表现优异，有效提升了模型在处理冲突人类价值观时的对齐效果，为复杂对齐任务提供了更稳健的解决方案。
selection_source: fresh_fetch
motivation: 现有的多目标对齐方法主要依赖静态偏好权重，难以捕捉训练过程中响应所蕴含的动态权衡信息。
method: 提出一种双层元学习框架，通过元学习器动态生成自适应偏好权重，并配合拒绝采样策略优化策略模型。
result: 实验结果显示，该方法在多个多目标基准测试中均取得了优于现有主流方法的性能。
conclusion: 动态双向偏好-策略优化框架能更有效地处理多目标对齐中的冲突，提升了LLM对齐的灵活性和稳定性。
---

## 摘要
多目标对齐旨在通过同时优化多个目标，使大语言模型（LLMs）与多样且往往相互冲突的人类价值观保持一致。现有方法主要依赖于静态偏好权重构建策略。然而，僵化地对齐固定目标会丢弃有价值的中间信息，因为训练响应即使在偏离目标时，其本身也体现了有效的偏好权衡。为了解决这一局限性，我们提出了 Meal（即 MEta ALigner），这是一个双层元学习框架，能够实现偏好与策略响应之间的双向优化，从而生成具有指导意义的动态偏好，使训练更加稳定。具体而言，我们引入了一个偏好权重网络（preference-weight-net）作为元学习器，根据输入提示生成自适应偏好权重，并将偏好权重作为可学习参数进行更新；同时，LLM 策略作为基础学习器，在这些偏好的约束下通过拒绝采样策略优化响应生成。广泛的实验结果表明，我们的方法在多个多目标基准测试中取得了优异的性能，验证了动态双向偏好-策略优化框架的有效性。

## Abstract
Multi-Objective Alignment aims to align Large Language Models (LLMs) with diverse and often conflicting human values by optimizing multiple objectives simultaneously. Existing methods predominantly rely on static preference weight construction strategies. However, rigidly aligning to fixed targets discards valuable intermediate information, as training responses inherently embody valid preference trade-offs even when deviating from the target. To address this limitation, we propose Meal, i.e., MEta ALigner, a bi-level meta-learning framework enabling bidirectional optimization between preferences and policy responses, generating instructive dynamic preferences for steadier training. Specifically, we introduce a preference-weight-net as a meta-learner to generate adaptive preference weights based on input prompts and update the preference weights as learnable parameters, while the LLM policy acts as a base-learner optimizing response generation conditioned on these preferences with rejection sampling strategy. Extensive empirical results demonstrate that our method achieves superior performance on several multi-objective benchmarks, validating the effectiveness of the dynamic bidirectional preference-policy optimization framework.

---

## 论文详细总结（自动生成）

这是一份关于论文《Meta-Aligner: Bidirectional Preference-Policy Optimization for Multi-Objective LLMs Alignment》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何让大语言模型（LLM）在面对多个且往往相互冲突的人类价值观（如“有用性”与“无害性”、“简洁性”与“创造性”）时，实现更灵活、更稳健的对齐。
*   **研究背景**：现有的多目标对齐方法（如 RLHF 的变体）大多依赖于**静态偏好权重**。这种方式存在局限性：它强制模型拟合固定的目标，忽略了训练过程中模型生成的中间响应所蕴含的有效权衡信息。
*   **整体含义**：本文提出了 **MEAL (Meta-Aligner)** 框架，将对齐过程视为一个**双层元学习问题**，通过偏好网络与策略模型之间的双向动态优化，捕捉模型演进过程中的偏好状态，从而提升对齐效果。

### 2. 方法论
*   **核心思想**：建立一个“元学习器-基础学习器”的双层架构。元学习器负责根据输入 Prompt 动态生成偏好权重，基础学习器（LLM）则根据这些权重生成响应。两者在训练中互为反馈，共同进化。
*   **关键技术细节**：
    *   **偏好权重网络 (Preference-weight-net)**：一个轻量级辅助网络，将 Prompt 语义编码为归一化的偏好向量 $w$。
    *   **潜空间注入 (Latent Injection)**：生成的权重 $w$ 通过一个 MLP 投影层映射到 LLM 的隐藏空间，并与 LLM 最后一层的隐藏状态融合，直接干预 Token 的生成概率。
    *   **双层优化机制**：
        *   **内循环 (Inner Loop)**：冻结偏好网络，优化 LLM 策略，使其在当前偏好指导下最大化高奖励响应的似然概率。
        *   **外循环 (Outer Loop)**：冻结 LLM，通过内循环传回的梯度更新偏好网络，使其生成的偏好既符合外部奖励模型（弱监督），又易于被当前策略模型执行。
    *   **在线学习与拒绝采样**：在训练迭代中，利用多个奖励模型对生成的响应进行评分，通过拒绝采样筛选出高质量样本进行在线微调。

### 3. 实验设计
*   **数据集/场景**：
    *   **Reddit Summary**：14.9k 条数据，目标是平衡摘要的“质量（summary）”、“事实一致性（faithful）”和“语言模型评估（deberta）”。
    *   **Helpful Assistant (HH-RLHF)**：160k 条数据，目标是平衡“无害性（harmless）”、“有用性（helpful）”和“幽默感（humor）”。
*   **Benchmark**：主要评估多维度奖励得分构成的 **Pareto 前沿面 (Pareto Frontier)**，即在不同偏好配置下模型所能达到的最优性能边界。
*   **对比方法**：
    *   **MORLHF**：基于固定权重的标量化 PPO 方法。
    *   **Reward Soups**：针对不同目标训练专家模型并进行参数插值。
    *   **RIC (Rewards-in-Context)**：通过 Prompt 拼接奖励分数进行对齐。
    *   **PARM**：基于偏好感知的自回归奖励模型引导生成。

### 4. 资源与算力
*   **模型规模**：使用了 Qwen3-0.6B（作为 Reddit 任务基础模型及偏好网络骨干）和 Qwen3-4B（作为 Assistant 任务基础模型）。
*   **算力说明**：**论文未明确指出具体的 GPU 型号、数量及总训练时长**。但从模型规模（0.6B 到 4B）和训练参数量（仅训练投影层或 LM Head）来看，该方法对算力的需求相对主流全参数微调较为节省。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流对齐基准上进行了大规模测试，每个任务涉及 3 个目标的权衡。
*   **消融实验**：对比了在线元学习与离线训练的效果，并验证了不同偏好权重来源（随机、奖励模型直接导出、元学习生成）的影响。
*   **敏感性分析**：对平衡系数 $\lambda$ 和 Softmax 温度系数 $\tau$ 进行了详细的参数敏感性测试。
*   **充分性评价**：实验设计较为全面，涵盖了定量指标（Pareto 曲线）、定性分析（Case Study）和鲁棒性验证。通过与多种 SOTA 方法对比，证明了其在复杂冲突场景下的优越性。

### 6. 主要结论与发现
*   **性能领先**：MEAL 在所有测试场景中均实现了更优的 Pareto 前沿，即在相同偏好下能获得更高的综合奖励得分。
*   **训练稳定性**：相比于 RIC 等方法在极端偏好下出现的性能剧烈波动，MEAL 的表现更加平滑且可预测。
*   **动态适应性**：实验证明，静态偏好权重（直接从奖励模型得分归一化）的效果甚至不如随机权重，这验证了“动态生成偏好”以适应模型当前状态的必要性。
*   **效率优势**：相比于需要为每个目标单独训练模型的 Reward Soups，MEAL 仅需单次训练即可覆盖整个偏好空间。

### 7. 优点
*   **双向优化创新**：打破了“偏好引导策略”的单向逻辑，引入了“策略反馈偏好”的闭环，使训练过程更具启发性。
*   **即插即用**：通过潜空间注入而非 Prompt 工程实现偏好控制，减少了对 Prompt 模板的依赖。
*   **计算友好**：采用轻量级元学习器和部分参数微调策略，在保证性能的同时降低了训练成本。

### 8. 不足与局限
*   **算力细节缺失**：未提供具体的硬件环境和训练耗时，不便于复现者评估资源投入。
*   **依赖奖励模型质量**：作为一种基于奖励模型引导的方法，其上限仍受限于底层奖励模型（RM）的准确性。
*   **任务覆盖面**：目前主要集中在 SFT 阶段的对齐，尚未深入探讨该框架在纯强化学习（Online RL）阶段的长期稳定性。
*   **偏差风险**：元学习器可能会过度拟合某些“容易实现”的偏好组合，从而在极端的价值观冲突中产生偏差。

（完）
