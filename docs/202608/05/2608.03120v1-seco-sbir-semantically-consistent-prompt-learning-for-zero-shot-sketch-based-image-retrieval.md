---
title: "SeCo-SBIR: Semantically Consistent Prompt Learning for Zero-Shot Sketch-Based Image Retrieval"
title_zh: SeCo-SBIR：面向零样本草图图像检索的语义一致性提示学习
authors: "Long Hoang Dang, Tuan Nguyen Huu, Nguyen Minh Hieu, Tu Minh Phuong"
date: 2026-08-04
pdf: "https://arxiv.org/pdf/2608.03120v1"
tags: ["query:bopl"]
score: 9.0
evidence: 可微多模态提示学习框架
tldr: 本文针对零样本草图检索（ZS-SBIR）中CLIP适配面临的领域迁移与过拟合矛盾，提出了SeCo-SBIR框架。该框架通过文本引导的多模态提示策略，将文本编码器的语义知识注入视觉路径，并利用基于扰动的一致性约束将模型锚定在CLIP的通用特征空间。结合轻量化适配器与多目标损失函数，该方法在多个基准测试中显著提升了泛化能力，达到了当前最先进的性能水平。
selection_source: fresh_fetch
motivation: 旨在解决在利用CLIP进行零样本草图检索时，任务特定适配导致的过拟合与零样本泛化能力下降之间的核心矛盾。
method: 提出文本引导的多模态提示策略将语义知识注入视觉编码器，并结合扰动一致性约束将学习到的表示锚定在CLIP的通用特征空间。
result: 在三个标准的零样本草图检索基准测试中，该方法在类别内、广义及跨数据集设置下均取得了最先进的实验结果。
conclusion: SeCo-SBIR通过语义一致的提示学习，有效平衡了领域适配与泛化性能，为零样本跨模态检索任务提供了高效的解决方案。
---

## 摘要
通过提示学习将 CLIP 适配于零样本草图图像检索（ZS-SBIR）面临着一个根本性的矛盾：模型必须通过特定任务的适配来弥合草图与照片之间的领域鸿沟，但增加的灵活性又存在过拟合于已知训练类别并削弱 CLIP 零样本泛化能力的风险。我们提出了 SeCo-SBIR，这是一个从两方面解决该矛盾的语义一致性提示学习框架。首先，一种文本引导的多模态提示策略将可学习的提示向量引导通过 CLIP 的文本编码器，并通过可学习的耦合函数将生成的中间表示在每一层投影到视觉编码器中。由于文本编码器已经从大规模语言监督中学习到了鲁棒的、抽象的类别级语义，这种机制将可迁移的语义知识直接注入视觉路径——在使模型适配草图-照片领域的同时，本质上有利于向未见类别的泛化。其次，一种基于扰动的一致性约束通过使用非对称 InfoNCE 目标将适配后的模型与冻结的 CLIP 参考分支对齐，从而解决了可学习耦合函数带来的残余过拟合风险——增强后的输入馈送至冻结分支，而干净的输入馈送至可训练分支——将学习到的表示锚定在 CLIP 的可泛化特征空间中。结合轻量级适配器以及包含三元组（triplet）、NT-Xent 和分类项的多目标损失，SeCo-SBIR 在所有三个标准 ZS-SBIR 基准测试的类别内、广义和跨数据集设置下均实现了最先进的结果。

## Abstract
Adapting CLIP for zero-shot sketch-based image retrieval (ZS-SBIR) via prompt learning faces a fundamental tension: the model must bridge the sketch-photo domain gap through task-specific adaptation, yet the added flexibility risks overfitting to seen training categories and eroding CLIP's zero-shot generalization. We present SeCo-SBIR, a semantically consistent prompt learning framework that resolves this tension from both sides. First, a text-guided multi-modal prompting strategy routes learnable prompt vectors through CLIP's text encoder and projects the resulting intermediate representations into the visual encoder at every layer via learnable coupling functions. Because the text encoder has already learned robust, abstract category-level semantics from large-scale language supervision, this mechanism injects transferable semantic knowledge directly into the visual pathway - adapting the model to the sketch-photo domain while inherently favoring generalization to unseen classes. Second, a perturbation-based consistency constraint addresses the residual overfitting risk from the learnable coupling functions by aligning the adapted model with a frozen CLIP reference branch using an asymmetric InfoNCE objective - augmented inputs feed the frozen branch while clean inputs feed the trainable branch - anchoring the learned representations to CLIP's generalizable feature space. Together with lightweight adapters and a multi-objective loss combining triplet, NT-Xent, and classification terms, SeCo-SBIR achieves state-of-the-art results on all three standard ZS-SBIR benchmarks across categorical, generalized, and across-dataset settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《SeCo-SBIR: Semantically Consistent Prompt Learning for Zero-Shot Sketch-Based Image Retrieval》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在零样本草图图像检索（ZS-SBIR）任务中，如何平衡**领域适配**（弥合草图与照片的巨大鸿沟）与**零样本泛化**（保留 CLIP 在未见类别上的能力）之间的矛盾。
*   **研究背景**：现有的基于 CLIP 的方法要么只进行浅层视觉提示（如 CLIP-AT），要么缺乏对已知类过拟合的显式约束（如 SpLIP）。当模型为了拟合草图领域而进行深度调整时，往往会丢失 CLIP 原有的通用语义空间，导致在未见类别上表现下降。

### 2. 方法论：核心思想与关键技术
SeCo-SBIR 提出了一个语义一致的提示学习框架，包含以下核心组件：
*   **文本引导的多模态提示（Text-guided Multi-modal Prompting）**：
    *   **核心思想**：利用 CLIP 文本编码器中已有的、跨类别的抽象语义来引导视觉编码器的适配。
    *   **技术细节**：将可学习的提示向量输入文本编码器，提取每一层的中间表示，通过可学习的耦合函数 $F_l$ 投影到视觉编码器的对应层。这使得视觉提示具有语义基础，而非仅仅是视觉特征的拟合。
*   **扰动一致性约束（Perturbation-based Consistency Constraint）**：
    *   **核心思想**：通过一个冻结的 CLIP 分支作为锚点，防止可训练分支偏离通用的特征空间。
    *   **技术细节**：采用非对称设计，冻结分支输入经过增强（扰动）的图像，可训练分支输入原始图像。使用 **InfoNCE 损失**进行对齐，确保模型学习的是结构化的相对关系而非绝对数值的复制。
*   **轻量级适配器（Lightweight Adapters）**：在照片、草图和文本分支末端添加两层 MLP 结构，进一步微调特征映射。
*   **多目标损失函数**：结合了跨模态三元组损失（Triplet）、双向对比损失（NT-Xent）、分类损失（Classification）以及上述的一致性损失。

### 3. 实验设计
*   **数据集**：
    *   **Sketchy-Ext**：包含 125 个类别，常用于评估细粒度检索。
    *   **TU-Berlin-Ext**：包含 250 个类别，类别数最多，最易发生过拟合。
    *   **QuickDraw-Ext**：包含 110 个类别，草图高度抽象且噪声多。
*   **评估场景（Benchmark）**：
    *   **Categorical ZS-SBIR**：标准零样本设置。
    *   **Generalized ZS-SBIR (GZS-SBIR)**：检索库包含已知类和未知类照片，难度更高。
    *   **Across-dataset**：在 Sketchy 上训练，直接在 TU-Berlin 或 QuickDraw 上测试，验证跨域鲁棒性。
*   **对比方法**：包括基于 ViT 的传统方法（TVT, PSKD）和基于 CLIP 的前沿方法（CLIP-AT, SpLIP, Dr.CLIP, MARL, MCG 等）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA H100 GPU** 进行实验。
*   **软件/框架**：PyTorch，基于 CLIP (ViT-B/32) 权重。
*   **训练细节**：使用 SGD 优化器，学习率为 $2 \times 10^{-5}$，batch size 为 32，训练 10 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   在 3 个数据集、4 种分割设置下的性能对比。
    *   **消融实验**：针对提示设计、损失函数组合、一致性约束形式（MSE vs InfoNCE）、适配器作用、提示 Token 数量、损失权重 $\lambda$ 等进行了多组对比。
    *   **可视化分析**：提供了 t-SNE 特征分布图和 Top-K 检索定性结果。
    *   **鲁棒性分析**：测试了不同训练数据比例和已知类别数量对性能的影响。
*   **充分性评价**：实验设计非常全面且客观，涵盖了从性能指标到模型行为分析的各个维度，尤其是跨数据集实验有力地证明了其泛化能力。

### 6. 主要结论与发现
*   **性能领先**：SeCo-SBIR 在所有基准测试中均达到 SOTA。在 TU-Berlin-Ext 上，mAP@all 提升了 **5.6%**；在跨数据集任务（QuickDraw）中，提升了 **6.8%**。
*   **语义引导的有效性**：通过文本编码器生成的视觉提示比独立学习的视觉提示更能抵抗过拟合。
*   **一致性约束的必要性**：非对称扰动一致性损失是保持 CLIP 零样本能力的关键，InfoNCE 优于传统的 MSE 或 L1 损失。

### 7. 优点（亮点）
*   **理论闭环**：从“注入语义知识”和“限制漂移”两个维度同时解决适配与泛化的矛盾，逻辑严密。
*   **设计巧妙**：利用文本编码器的中间层作为视觉提示的来源，充分挖掘了 CLIP 的多模态潜力。
*   **泛化性强**：在极具挑战性的跨数据集和广义零样本设置下表现出极强的鲁棒性。

### 8. 不足与局限
*   **计算开销**：训练阶段需要维护一个冻结的参考分支并处理增强输入，这增加了训练时的显存占用和计算耗时（虽然推理时不需要）。
*   **超参数敏感性**：实验表明模型对损失权重 $\lambda_1, \lambda_2$ 以及提示 Token 的数量较为敏感，可能需要针对不同数据集进行微调。
*   **应用限制**：主要针对类别级检索，对于极细粒度（如同一类别内不同实例）的检索能力未做专门深入探讨。

（完）
