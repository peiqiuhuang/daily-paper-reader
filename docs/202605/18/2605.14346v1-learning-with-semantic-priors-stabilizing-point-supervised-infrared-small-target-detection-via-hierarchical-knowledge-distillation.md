---
title: "Learning with Semantic Priors: Stabilizing Point-Supervised Infrared Small Target Detection via Hierarchical Knowledge Distillation"
title_zh: 基于语义先验的学习：通过分层知识蒸馏稳定点监督红外小目标检测
authors: "Yuanhang Yao, Ping Qian, Zhu Liu, Long Ma, Weimin Wang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14346v1"
tags: ["query:bopl"]
score: 8.0
evidence: 将学习过程建模为双层优化过程
tldr: 针对红外小目标检测中像素级标注成本高的问题，本文提出一种基于分层视觉基础模型（VFM）驱动的知识蒸馏框架。该框架通过双层优化策略，利用冻结的VFM作为教师模型引导轻量化CNN学生模型，并引入语义条件仿射调制（SCAM）和动态协作学习，有效解决了点监督学习中伪标签噪声和训练不稳定的难题，显著提升了检测精度与鲁棒性。
selection_source: fresh_fetch
motivation: 点监督红外小目标检测中，轻量化CNN因缺乏语义先验常导致伪掩码质量差且训练过程不稳定。
method: 提出一种分层VFM驱动的知识蒸馏框架，通过双层优化过程和语义条件仿射调制（SCAM）将VFM的语义先验注入学生网络。
result: 在多个主流ISTD数据集上的实验证明，该方法在不同骨干网络下均能显著提升检测精度并增强训练稳定性。
conclusion: 通过引入视觉基础模型的语义先验，可以有效缓解点监督学习中的噪声干扰，为低成本红外目标检测提供了高效的解决方案。
---

## 摘要
单帧红外小目标检测（ISTD）旨在强背景杂波下定位弱小目标，但密集的像素级标注成本高昂。带有在线标签演化的点监督可以降低标注成本；然而，轻量级 CNN 检测器通常缺乏足够的语义信息，导致伪掩码（pseudo-masks）含有噪声且优化过程不稳定。为解决这一问题，我们提出了一种分层 VFM 驱动的知识蒸馏框架，在训练过程中使用冻结的视觉基础模型（VFM）。我们将点监督学习建模为一个双层优化过程：内层循环在重加权的训练样本上调整嵌入 VFM 的教师模型，而外层循环将验证集引导的知识迁移给轻量级学生模型，以减轻伪标签噪声和训练集偏差。我们进一步引入了语义条件仿射调制（SCAM），将 VFM 语义注入到多层 CNN 特征中。此外，一种带有聚类级样本重加权的动态协作学习策略增强了对不完美伪掩码的鲁棒性。在多个 ISTD 骨干网络上进行的各种挑战性案例实验表明，该方法在检测精度和训练稳定性方面均有持续提升。我们的代码可在 https://github.com/yuanhang-yao/semantic-prior 获取。

## Abstract
Single-frame Infrared Small Target Detection (ISTD) aims to localize weak targets under heavy background clutter, yet dense pixel-wise annotations are expensive. Point supervision with online label evolution reduces annotation cost; however, lightweight CNN detectors often lack sufficient semantics, leading to noisy pseudo-masks and unstable optimization. To address this, we propose a hierarchical VFM-driven knowledge distillation framework that uses a frozen Vision Foundation Model (VFM) during training. We formulate point-supervised learning as a bilevel optimization process: the inner loop adapts a VFM-embedded teacher on reweighted training samples, while the outer loop transfers validation-guided knowledge to a lightweight student to mitigate pseudo-label noise and training-set bias. We further introduce Semantic-Conditioned Affine Modulation (SCAM) to inject VFM semantics into CNN features at multiple layers. In addition, a dynamic collaborative learning strategy with cluster-level sample reweighting enhances robustness to imperfect pseudo-masks. Experiments on diverse challenging cases across multiple ISTD backbones demonstrate consistent improvements in detection accuracy and training stability. Our code is available at https://github.com/yuanhang-yao/semantic-prior.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning with Semantic Priors: Stabilizing Point-Supervised Infrared Small Target Detection via Hierarchical Knowledge Distillation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：红外小目标检测（ISTD）在海事搜救、预警等领域至关重要。虽然全监督学习效果好，但像素级标注成本极高。
*   **核心问题**：点监督（Point Supervision）虽能降低成本，但现有的轻量化CNN检测器由于缺乏深层语义信息，在“在线标签演化”过程中容易受到背景杂波干扰，产生多噪的伪掩码（Pseudo-masks），导致训练过程不稳定且性能受限。
*   **整体含义**：本文旨在利用视觉基础模型（VFM，如DINOv2/v3）强大的语义先验知识，通过知识蒸馏的方式引导轻量化CNN学习，在不增加推理成本的前提下，稳定点监督下的训练过程并提升检测精度。

### 2. 方法论
*   **核心思想**：将点监督学习建模为一个**双层优化（Bilevel Optimization）**问题，利用冻结的VFM作为教师提供语义指导，并通过验证集的反馈来纠正训练集中的伪标签偏差。
*   **关键技术细节**：
    *   **双层优化框架**：内层循环在训练集上优化嵌入VFM的教师模型；外层循环利用验证集的泛化表现来指导学生模型（CNN）的蒸馏过程，确保知识迁移的有效性。
    *   **语义条件仿射调制（SCAM）**：设计了一个轻量级接口，通过Token感知注意力池化（TAP）聚合VFM的特征，生成仿射变换参数（缩放和偏移），动态注入到CNN的多层特征中，增强其对目标的感知力。
    *   **动态协作学习策略**：引入聚类级样本重加权机制，根据图像的强度、纹理等先验对样本进行聚类，自动降低噪声伪标签样本的权重，缓解数据不平衡问题。
*   **算法流程**：每个Epoch中，先根据教师预测更新伪掩码，然后进行内层步骤更新教师侧参数，最后进行外层步骤利用Gauss-Newton超梯度近似更新学生网络和聚类权重。

### 3. 实验设计
*   **数据集**：使用综合数据集 **SIRST3**（包含 SIRST-v1, NUDT-SIRST, IRSTD-1k）。
*   **测试场景**：为了深入分析，将测试集细分为 **Salient（显著）**、**Filamentary（丝状）**、**Faint（微弱）** 和 **Camouflaged（伪装）** 四种挑战性场景。
*   **Benchmark与对比方法**：
    *   **基准**：LESPS 和 PAL（当前最强的点监督方法）。
    *   **骨干网络**：在 ALCLNet, GGLNet, MLCLNet, MSHNet, RepISDNet, DNANet, SCTransNet 等7种主流模型上进行了验证。
    *   **指标**：IoU（交并比）、nIoU、Pd（检测概率）、Fa（虚警率）。

### 4. 资源与算力
*   **算力设备**：单张 **NVIDIA A40 GPU**。
*   **技术细节**：使用了自动混合精度（AMP）训练。
*   **训练配置**：AdamW 优化器，基础学习率 $1 \times 10^{-3}$，训练 300 个 Epoch，Batch Size 为 16。
*   **VFM模型**：采用冻结的 DINOv3 ViT-S+/16（约29M参数）。

### 5. 实验数量与充分性
*   **实验规模**：在7种不同的CNN骨干网络上进行了广泛测试，证明了方法的通用性。
*   **消融实验**：非常充分。分别验证了VFM的作用、SCAM模块的设计（如TAP vs GAP）、双层优化的各个组件（内/外层蒸馏、验证集反馈）、以及超参数（如MLP宽度、验证集比例）的影响。
*   **客观性**：通过可视化特征图（SCAM前后的对比）和训练稳定性曲线（IoU随Epoch的变化）直观展示了改进效果，实验设计遵循了学术界的标准协议，对比公平。

### 6. 主要结论与发现
*   **性能提升**：在所有骨干网络上，该方法均显著优于现有的点监督方法（如PAL），尤其在微弱和伪装目标上提升明显。
*   **训练稳定性**：引入VFM语义先验后，训练过程中的IoU曲线更加平滑，有效抑制了伪标签噪声导致的震荡。
*   **推理效率**：由于VFM仅在训练阶段参与，推理时学生模型保持了原始轻量化CNN的效率，实现了性能与速度的平衡。

### 7. 优点
*   **重训练轻推理**：巧妙地利用大模型的知识来增强小模型，而不在部署阶段引入额外负担。
*   **双层优化机制**：通过验证集反馈解决伪标签噪声问题，比传统的直接蒸馏更具鲁棒性。
*   **即插即用**：SCAM模块设计简洁，能够适配多种不同的CNN架构。

### 8. 不足与局限
*   **训练开销**：虽然推理快，但训练阶段由于需要运行VFM分支和计算超梯度，显存占用和训练时间会有所增加（文中提到延迟增加约1.04x至1.63x）。
*   **验证集依赖**：双层优化高度依赖验证集的质量和分布，如果验证集过小或存在严重偏差，可能会影响泛化效果。
*   **超参数调优**：尽管核心组件稳健，但双层优化涉及的学习率、更新频率等超参数可能仍需针对特定数据集进行微调。

（完）
