---
title: "Combee: Scaling Prompt Learning for Self-Improving Language Model Agents"
title_zh: Combee：面向自我改进型语言模型智能体的可扩展提示学习
authors: "Hanchen Li, Runyuan He, Qizheng Zhang, Changxiu Ji, Qiuyang Mang, Xiaokun Chen, Lakshya A Agrawal, Wei-Liang Liao, Eric Yang, Alvin Cheung, James Zou, Kunle Olukotun, Ion Stoica, Joseph E. Gonzalez"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04247v1"
tags: ["query:bopl"]
score: 9.0
evidence: 为语言模型智能体扩展提示学习
tldr: 本研究针对自改进大模型智能体在提示词学习中的扩展性难题，提出了 Combee 框架。现有方法在处理大规模并行智能体轨迹时往往面临效率瓶颈和质量退化。Combee 通过引入并行扫描、增强洗牌机制以及动态批大小控制器，实现了从海量智能体运行轨迹中高效学习系统提示词。实验表明，Combee 在保持或提升准确率的同时，比现有方法提速高达 17 倍，为大规模智能体集群的自我进化提供了高效的解决方案。
selection_source: fresh_fetch
motivation: 现有提示词学习方法在处理大规模并行智能体轨迹时存在效率低下和质量下降的问题，限制了其从海量数据中学习的能力。
method: 提出 Combee 框架，利用并行扫描、增强洗牌机制和动态批大小控制器来优化并行提示词学习过程。
result: 在 AppWorld 和 Terminal-Bench 等基准测试中，Combee 实现了高达 17 倍的加速，且准确率与现有方法相当或更高。
conclusion: Combee 有效解决了自改进智能体在大规模并行环境下的提示词学习扩展性难题，实现了效率与质量的双重提升。
---

## 摘要
提示学习的最新进展使得大语言模型智能体能够在不改变参数的情况下，从推理时的上下文中获取任务相关知识。例如，现有方法（如 ACE 或 GEPA）可以根据之前的智能体运行记录学习系统提示，从而提高准确率。然而，这些方法主要关注单智能体或低并行度的设置。这从根本上限制了它们从大量收集的智能体轨迹中高效学习的能力。并行运行提示学习以适应从大量智能体轨迹或并行智能体执行中学习的日益增长的趋势，将是高效且有益的。然而，由于缺乏原则性的扩展策略，现有方法在高并行度下会出现质量下降。为了同时提高提示学习的效率和质量，我们提出了 Combee，这是一个为自我改进型智能体扩展并行提示学习的新型框架。Combee 加速了学习过程，并支持在并行运行多个智能体的同时，从它们的聚合轨迹中学习，且不会造成质量下降。为实现这一目标，Combee 利用了并行扫描并采用了增强的洗牌机制；Combee 还引入了动态批大小控制器来平衡质量和延迟。在 AppWorld、Terminal-Bench、Formula 和 FiNER 上的评估表明，Combee 与之前的方法相比，在准确率相当或更高且成本相同的情况下，实现了高达 17 倍的加速。

## Abstract
Recent advances in prompt learning allow large language model agents to acquire task-relevant knowledge from inference-time context without parameter changes. For example, existing methods (like ACE or GEPA) can learn system prompts to improve accuracy based on previous agent runs. However, these methods primarily focus on single-agent or low-parallelism settings. This fundamentally limits their ability to efficiently learn from a large set of collected agentic traces. It would be efficient and beneficial to run prompt learning in parallel to accommodate the growing trend of learning from many agentic traces or parallel agent executions. Yet without a principled strategy for scaling, current methods suffer from quality degradation with high parallelism. To improve both the efficiency and quality of prompt learning, we propose Combee, a novel framework to scale parallel prompt learning for self-improving agents. Combee speeds up learning and enables running many agents in parallel while learning from their aggregate traces without quality degradation. To achieve this, Combee leverages parallel scans and employs an augmented shuffle mechanism; Combee also introduces a dynamic batch size controller to balance quality and delay. Evaluations on AppWorld, Terminal-Bench, Formula, and FiNER demonstrate that Combee achieves up to 17x speedup over previous methods with comparable or better accuracy and equivalent cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Combee: Scaling Prompt Learning for Self-Improving Language Model Agents》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的提示学习（Prompt Learning）方法（如 ACE、GEPA）在单智能体或低并行度环境下表现良好，但在**大规模并行扩展**时会遭遇“**上下文过载（Context Overload）**”问题。
*   **研究动机**：当尝试从成百上千个智能体运行轨迹中同时学习时，负责聚合信息的 LLM 会因为输入过长而进行“有损压缩”，丢弃具体的、高价值的策略，只保留通用的、无用的模式。这导致学习效率低下且最终任务准确率大幅下降。

### 2. 方法论：Combee 框架
Combee 采用了 **Map-Shuffle-Reduce** 的分布式学习范式，核心包含三个关键技术：
*   **并行扫描聚合（Parallel Scan Aggregation）**：
    *   不再一次性将所有反射（Reflections）喂给 LLM。
    *   采用多级树状结构，将 $n$ 个轨迹分成 $k$ 个子组（默认 $k = \sqrt{n}$）。先在子组内聚合，再对聚合结果进行二次聚合，确保每一级 LLM 处理的上下文都在其高效处理范围内。
*   **增强洗牌机制（Augmented Shuffling）**：
    *   在分发到聚合树之前，将每个反射复制 $p$ 次（默认 2 次）并随机打乱。
    *   这种“自一致性”思路增加了关键信息被聚合器采纳的概率，提高了并行学习的鲁棒性。
*   **动态批大小控制器（Dynamic Batch Size Controller）**：
    *   通过拟合幂律延迟曲线 $T_{epoch}(bs) = A \cdot bs^{-\alpha}$ 来自动寻找最优批大小。
    *   在保证训练速度（减少延迟）的同时，避免因批大小过大而导致的潜在质量退化，实现效率与质量的自动平衡。

### 3. 实验设计
*   **数据集/场景**：
    *   **智能体基准**：AppWorld（多步 API 任务）、Terminal-Bench 2.0（命令行软件工程任务）。
    *   **领域特定任务**：FiNER（金融实体识别）、Formula（金融数值推理）。
*   **对比方法**：
    *   **Base LLM**：无学习的基准。
    *   **ACE / GEPA**：原始的顺序学习方法。
    *   **Naive Scaling**：简单增加批大小的并行方法。
    *   **Top-K Retrieval / Summarization**：基于检索或摘要的传统上下文压缩方法。

### 4. 资源与算力
*   **模型使用**：主要使用 **DeepSeek-V3.1**（通过 Together AI 接口），并在扩展性分析中使用了 **GPT-OSS 120B**。
*   **算力说明**：文中未明确提及具体的 GPU 物理型号和数量（因为主要使用 API 接口），但详细记录了**训练时长**（从几分钟到几小时不等）和**经济成本**（单次实验约 $1.4 - $1.7 美元）。
*   **加速比**：实验记录了最高 **17 倍** 的墙钟时间加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同性质的基准测试，并在 ACE 和 GEPA 两个不同的提示学习框架上进行了验证。
*   **消融实验**：针对“增强洗牌”和“动态控制器”分别做了对比实验，验证了各组件的必要性。
*   **客观性**：实验对比了不同批大小（1 到 100）下的性能曲线，清晰展示了“上下文过载”现象及 Combee 的修复效果。使用了多个模型家族进行交叉验证，实验设计较为全面、公平。

### 6. 主要结论与发现
*   **打破扩展瓶颈**：Combee 在高并行度下（如 Batch Size 40+）依然能保持甚至超过顺序学习（Batch Size 1）的准确率。
*   **显著提速**：在 AppWorld 上实现 12 倍加速，在 Terminal-Bench 上实现 17 倍加速。
*   **信息保留**：定性分析显示，Combee 学习到的 Playbook（策略手册）长度远超朴素并行方法，保留了大量处理边缘情况的具体公式和规则。

### 7. 优点
*   **通用性强**：作为一种框架无关（Framework-agnostic）的设计，可以轻松集成到任何“生成-反射-更新”循环的系统中。
*   **系统性解决过载**：通过算法结构（并行扫描）而非简单的提示词工程解决了 LLM 的长文本处理局限。
*   **自动化程度高**：动态批大小控制器减少了人工调参的负担。

### 8. 不足与局限
*   **同步限制**：目前的 Combee 采用同步并行执行，如果某些智能体运行极慢，会产生“木桶效应”拖慢整体进度。
*   **延迟模型假设**：动态控制器依赖于延迟符合幂律分布的假设，在某些极端异构的计算环境下可能失效。
*   **工件类型限制**：目前主要针对文本形式的提示词/手册进行学习，尚未验证其在代码库或复杂多模态记忆库上的扩展效果。

（完）
