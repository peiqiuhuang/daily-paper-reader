---
title: "Bilevel MPC for Linear Systems: A Tractable Reduction and Continuous Connection to Hierarchical MPC"
title_zh: 线性系统的双层模型预测控制：一种易处理的简化及其与分层模型预测控制的连续连接
authors: "Ryuta Moriyasu, Carmen Amo Alonso, Marco Pavone"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29265v1"
tags: ["query:bopl"]
score: 8.0
evidence: 包含上下层的双层模型预测控制架构
tldr: 针对分层模型预测控制（MPC）中双层优化带来的非凸和非平滑难题，本文提出了一种针对线性系统的平滑单层简化方法。该方法在满足特定矩阵非奇异条件下，能将双层MPC等效转化为集中式MPC，并引入插值框架连接双层与标准分层MPC，实现了性能与计算效率的权衡。
selection_source: fresh_fetch
motivation: 传统基于KKT条件的分层MPC重构往往引入非凸和非平滑结构，不利于实时验证和控制。
method: 提出一种平滑的单层简化方案，并利用移位阻塞技术构建连接双层与分层MPC的插值框架。
result: 证明了在凸性条件下该方案与集中式MPC等效，并提供了后验性能退化证明以平衡计算效率。
conclusion: 该研究为分层控制系统提供了一种计算高效且理论完备的优化框架，实现了从分层到集中式控制的平滑过渡。
---

## 摘要
模型预测控制（MPC）已广泛应用于许多领域，通常采用将不同层级的控制器和决策层相结合的分层架构。然而，当此类架构被建模为双层优化问题时，基于 KKT 条件的标准重构往往会引入非凸且可能非光滑的结构，这对于实时可验证控制是不利的。在本文中，我们研究了一种双层 MPC 架构，它由（i）选择参考序列的上层和（ii）跟踪该参考序列的下层线性 MPC 组成。我们提出了一种平滑的单层简化方法，在可验证的分块矩阵非奇异条件下，该方法不会降低性能。此外，当问题为凸时，其解是唯一的，且等效于相应的集中式 MPC，从而使其能够继承闭环特性。我们进一步证明了双层 MPC 是标准分层 MPC 的自然扩展，并引入了一个通过动作阻塞（move-blocking）将两者连续连接的插值框架。该框架揭示了所得公式之间的最优值排序，并提供了低成本的后验退化证明，从而实现了性能与计算效率之间有原则的权衡。

## Abstract
Model predictive control (MPC) has been widely used in many fields, often in hierarchical architectures that combine controllers and decision-making layers at different levels. However, when such architectures are cast as bilevel optimization problems, standard KKT-based reformulations often introduce nonconvex and potentially nonsmooth structures that are undesirable for real-time verifiable control. In this paper, we study a bilevel MPC architecture composed of (i) an upper layer that selects the reference sequence and (ii) a lower-level linear MPC that tracks such reference sequence. We propose a smooth single-level reduction that does not degrade performance under a verifiable block-matrix nonsingularity condition. In addition, when the problem is convex, its solution is unique and equivalent to a corresponding centralized MPC, enabling the inheritance of closed-loop properties. We further show that bilevel MPC is a natural extension of standard hierarchical MPC, and introduce an interpolation framework that continuously connects the two via move-blocking. This framework reveals optimal-value ordering among the resulting formulations and provides inexpensive a posteriori degradation certificates, thereby enabling a principled performance-computational efficiency trade-off.

---

## 论文详细总结（自动生成）

### 论文结构化总结：线性系统的双层 MPC 简化与连接

#### 1. 核心问题与整体含义（研究动机和背景）
在工业控制（如航空航天、汽车、电力系统）中，**分层控制架构**（上层决策+下层执行）非常普遍。然而，将这种架构建模为**双层优化问题**（Bilevel Optimization）时，标准做法是利用 KKT 条件将其转化为单层数学规划（MPCC）。
*   **痛点**：MPCC 具有固有的**非凸性**和**非光滑性**，容易导致局部最优、数值不稳定以及解的不连续性，这对于需要实时验证和高可靠性的控制系统（如无人机、自动驾驶）是不可接受的。
*   **目标**：寻找一种能够保持凸性、确保解的唯一性，且在计算上易于处理的双层 MPC 简化方案，并建立其与传统分层 MPC（HMPC）之间的理论联系。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**平滑的单层简化方法（P2）**，其核心逻辑如下：
*   **核心思想**：取消复杂的互补性条件（Complementarity），通过将下层不等式约束“提升”到上层，并仅强制执行下层问题的**一阶平稳性（Stationarity）方程**。
*   **关键技术细节**：
    *   **稳态参数化**：利用零空间基矩阵 $Z$ 对参考序列进行最小维度参数化，使其能处理包含积分器或病态条件的系统。
    *   **精确简化条件**：证明了在**分块矩阵 $\bar{\Gamma}$ 非奇异**（可验证条件）的情况下，简化后的单层问题与原始双层问题在最优值和输入轨迹上是等效的。
    *   **等效集中式 MPC**：证明在凸性假设下，该双层架构在输入层面等效于一个**集中式 MPC（P3）**，从而直接继承了集中式控制的闭环稳定性和唯一性。
    *   **动作阻塞（Move-blocking）插值**：引入动作阻塞矩阵 $M$，通过调整阻塞块的大小，实现了从“单块参考”（标准 HMPC）到“全自由度参考”（双层 MPC）的连续过渡。
    *   **后验退化证书**：推导出了计算成本极低的性能差距上界（$\Delta$ 边界），允许开发者在不求解复杂问题的情况下，评估简化模型带来的性能损失。

#### 3. 实验设计
论文通过两个主要场景验证了方法的有效性：
*   **场景 A：二阶线性系统（玩具模型）**：一个带有积分器的位置-速度控制系统，用于验证理论的正确性。
*   **场景 B：四旋翼无人机控制（复杂模型）**：利用 **Koopman EDMD** 技术将非线性动力学线性化，在存在位置约束和输入限制的情况下进行闭环仿真。
*   **Benchmark（基准方法）**：
    *   **P0 (HMPC)**：标准分层 MPC，仅优化稳态目标。
    *   **P1 (BMPC)**：原始非凸双层 MPC（使用 SQP 求解）。
    *   **P3 (Centralized)**：集中式 MPC，作为性能上限。
*   **对比维度**：轨迹跟踪精度、约束违反情况、计算时间、不同动作阻塞阶数下的性能退化。

#### 4. 资源与算力
*   **软件环境**：使用 MATLAB 环境，调用了 `fmincon`（求解非凸 P1）和 `quadprog`（求解凸优化 P2/P3）。
*   **硬件/算力**：文中**未明确说明**具体的 GPU 或 CPU 型号及详细训练时长。但从算法性质来看，该方法旨在降低计算复杂度，实验在标准工作站上即可完成实时仿真。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对二阶系统进行了不同初始化（随机 vs 零初始化）的对比实验。
    *   针对动作阻塞阶数（1 到 10 阶）进行了完整的消融实验，验证了性能排序理论。
    *   针对四旋翼无人机进行了 50 组不同初始状态的采样，用于构建低秩阻塞矩阵。
*   **充分性与客观性**：实验设计较为全面，不仅验证了性能提升，还通过对比 P1 的失败案例（随机初始化导致局部最优）客观展示了所提方法 P2 的鲁棒性。

#### 6. 主要结论与发现
*   **等效性验证**：提出的简化版 P2 能够完美复现集中式 MPC 的最优轨迹，而原始双层 P1 极易受到初始化影响而陷入差解。
*   **性能排序**：实验证实了 $V_{Centralized} = V_{Bilevel} \leq V_{Blocked} \leq V_{HMPC}$ 的理论排序。
*   **约束处理**：相比于 HMPC 在瞬态过程中可能违反上层约束，双层 MPC（及简化版）能够严格保证瞬态安全性。
*   **计算效率**：P2 的计算耗时远低于非凸的 P1，且通过动作阻塞技术，可以在极小的计算增量下获得接近集中式控制的性能。

#### 7. 优点
*   **理论完备**：提供了从双层到单层简化的精确等效性证明，并解决了稳定性继承问题。
*   **实用性强**：通过动作阻塞和退化证书，为工程实践中“计算 vs 性能”的权衡提供了量化工具。
*   **鲁棒性高**：消除了互补性约束，使得双层控制问题可以用标准的凸二次规划（QP）求解器高效解决。

#### 8. 不足与局限
*   **系统限制**：目前主要针对**线性系统**（或通过 Koopman 等技术线性化的系统），对于强非线性且无法有效线性化的系统适用性受限。
*   **目标函数限制**：假设上层目标函数不直接依赖于参考序列本身，而是通过下层产生的输入轨迹间接依赖。
*   **参数敏感性**：虽然 $\bar{\Gamma}$ 的非奇异性是可验证的，但在某些极端参数设置下，该条件可能不满足，导致简化失效。

（完）
