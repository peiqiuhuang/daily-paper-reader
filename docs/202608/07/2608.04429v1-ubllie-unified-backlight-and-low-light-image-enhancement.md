---
title: "UBLLIE: Unified Backlight and Low-Light Image Enhancement"
title_zh: UBLLIE：统一的逆光与低光图像增强
authors: "Yasmin Yasin, Muhammad Usman, Ibrahim Radwan, Saeed Anwar"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04429v1"
tags: ["query:bopl"]
score: 8.0
evidence: CLIP引导的提示学习用于语义监督增强
tldr: 针对背光和低光图像存在的曝光不平衡及欠曝光问题，本文提出了一种统一的无监督增强框架UBLLIE。该方法利用CLIP引导的提示学习进行语义监督，并设计了带有空洞空间金字塔池化（ASPP）的对称残差U-Net架构，以捕捉多尺度上下文信息。通过迭代提示优化机制，该框架在无需成对数据的情况下，实现了优于现有监督和无监督方法的增强效果，显著提升了图像的保真度与感知质量。
selection_source: fresh_fetch
motivation: 旨在解决背光和低光图像中严重的曝光不平衡或全局欠曝光对视觉感知及下游计算机视觉任务带来的挑战。
method: 提出一种基于CLIP引导提示学习的无监督框架，结合带有ASPP模块的对称残差U-Net进行多尺度特征提取与自适应光照校正。
result: 在BAID、Backlit300、LOL等多个数据集上的实验证明，该方法在保真度、感知质量和泛化能力上均优于现有的先进算法。
conclusion: 该框架为处理复杂光照条件下的图像增强提供了一种鲁棒且可扩展的无监督解决方案，并填补了背光增强领域基准测试的不足。
---

## 摘要
逆光和低光图像通常面临严重的曝光不平衡或全局欠曝光问题，这对视觉感知和下游计算机视觉任务都构成了重大挑战。在本文中，我们提出了一个统一的无监督增强框架，在不依赖成对真值（ground-truth）数据的情况下处理这两类退化。我们的方法基于 CLIP 引导的提示学习（prompt learning），利用学习到的正向和负向文本提示对增强过程进行语义监督。为了提升增强质量，我们设计了一个对称残差 U-Net 骨干网络，并引入了空洞空间金字塔池化（Atrous Spatial Pyramid Pooling）模块。该架构能够捕获多尺度上下文信息，从而在空间异构光照下实现自适应校正。在训练过程中，增强网络由基于 CLIP 的语义相似度损失引导，并通过迭代提示优化机制进行细化。在成对和非成对数据集（包括 BAID、Backlit300、LOL 和 VE-LOL-L）上的广泛实验表明，我们的框架在保真度、感知质量和泛化性方面始终优于最先进的有监督和无监督方法。此外，我们的工作强调了为逆光增强（一个相对未被充分探索的领域）建立更强基准测试协议的必要性。所提出的框架为各种光照条件下的真实世界光照增强提供了一个鲁棒且可扩展的解决方案。

## Abstract
Backlit and low-light images often suffer from severe exposure imbalance or global underexposure, presenting significant challenges for both visual perception and downstream computer vision tasks. In this paper, we propose a unified, unsupervised enhancement framework that addresses both types of degradation without relying on paired ground-truth data. Our approach builds on CLIP-guided prompt learning to semantically supervise enhancement using learned positive and negative textual prompts. To improve the quality of our improvements over prior work, we design a symmetric residual U-Net backbone augmented with an Atrous Spatial Pyramid Pooling module. This architecture captures multi-scale contextual information, enabling adaptive correction under spatially heterogeneous illumination. During training, the enhancement network is guided by CLIP-based semantic similarity losses and refined via an iterative prompt optimization mechanism. Extensive experiments on both paired and unpaired datasets, including BAID, Backlit300, LOL, and VE-LOL-L, demonstrate that our framework consistently outperforms state-of-the-art supervised and unsupervised methods in terms of fidelity, perceptual quality, and generalization. Furthermore, our work emphasizes the need for stronger benchmarking protocols for backlit enhancement, a relatively underexplored area. The proposed framework provides a robust, scalable solution for real-world illumination enhancement across diverse lighting conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《UBLLIE: Unified Backlight and Low-Light Image Enhancement》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：图像在逆光（Backlit，局部曝光不均）和低光（Low-light，全局欠曝光）条件下，视觉质量会严重下降，影响人类感知及下游计算机视觉任务（如目标检测）。
*   **研究背景**：
    *   **数据稀缺**：获取真实场景下的成对（退化图像与真值图像）数据极其困难且成本高昂。
    *   **任务差异**：逆光增强需要处理空间异构的光照，而低光增强侧重全局亮度提升，现有方法往往难以在单一框架内同时有效处理这两种退化。
    *   **语义缺失**：传统无监督方法缺乏对图像内容的语义理解，容易导致色彩失真或伪影。

### 2. 论文提出的方法论
UBLLIE 提出了一种**统一的无监督增强框架**，核心思想是利用预训练的多模态模型（CLIP）来引导图像增强过程。
*   **核心架构**：
    *   **对称残差 U-Net (Symmetric Residual U-Net)**：作为骨干网络，通过残差连接保留空间细节。
    *   **空洞空间金字塔池化 (ASPP)**：在编码器和解码器之间引入 ASPP 模块，通过不同采样率的空洞卷积捕获多尺度上下文信息，以应对逆光场景中复杂的亮度分布。
*   **关键技术细节**：
    *   **CLIP 引导的提示学习 (Prompt Learning)**：定义正向提示（如 "High-quality photo"）和负向提示（如 "Low-quality, dark photo"）。通过计算增强图像与这些提示在 CLIP 潜在空间中的余弦相似度，引导网络向高质量方向优化。
    *   **迭代提示优化 (Iterative Prompt Optimization)**：在训练过程中动态优化文本提示的嵌入（Embedding），使其能更精准地描述“增强后”和“退化”的状态。
    *   **损失函数**：结合了 CLIP 语义相似度损失、全变分（TV）正则化损失（保持平滑）以及空间一致性损失。

### 3. 实验设计
*   **数据集**：
    *   **逆光场景**：BAID（大规模逆光数据集）、Backlit300。
    *   **低光场景**：LOL (v1 & v2)、VE-LOL-L。
*   **Benchmark（基准测试）**：
    *   **有监督方法**：RetinexNet, KinD, KinD++, MIRNet, HWMNet 等。
    *   **无监督/零样本方法**：EnlightenGAN, Zero-DCE, Zero-DCE++, CLIP-LIT, SCI 等。
*   **评价指标**：峰值信噪比 (PSNR)、结构相似性 (SSIM)、感知损失 (LPIPS) 以及非参考指标 (NIQE)。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA GeForce RTX 3090 GPU** (24GB VRAM) 上运行。
*   **软件环境**：基于 PyTorch 框架。
*   **训练细节**：使用 Adam 优化器，初始学习率为 $1 \times 10^{-4}$，batch size 设置为 8，输入图像尺寸裁剪为 $256 \times 256$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个主流数据集上进行了定量对比。
    *   提供了大量的定性视觉对比图（针对不同光照条件）。
    *   **消融实验**：验证了 ASPP 模块、不同损失函数组件以及迭代提示优化机制的有效性。
*   **充分性评价**：实验设计较为全面，涵盖了从合成到真实、从局部到全局光照不足的多种场景。通过与最新的有监督和无监督方法对比，证明了其在无须成对数据情况下的竞争优势。

### 6. 论文的主要结论与发现
*   **统一性**：UBLLIE 能够使用同一套架构和训练策略，同时解决逆光和低光增强问题。
*   **性能领先**：在无监督方法中，UBLLIE 在多个指标上达到了 SOTA（最先进）水平，甚至在某些指标上超过了需要成对数据训练的有监督模型。
*   **语义引导的有效性**：证明了 CLIP 的跨模态先验知识可以有效弥补无监督学习中缺乏真值监督的缺陷，使增强后的图像在色彩和对比度上更符合人类视觉偏好。

### 7. 优点
*   **无需成对数据**：极大地降低了数据收集成本，增强了在现实世界未见场景中的适用性。
*   **多尺度感知**：ASPP 模块的引入有效解决了逆光图像中明暗区域剧烈变化导致的增强不均问题。
*   **语义一致性**：利用 CLIP 确保了增强过程不仅仅是像素值的改变，而是保持了图像的语义逻辑。

### 8. 不足与局限
*   **噪声处理**：论文主要关注光照校正，对于极端低光环境下伴随的强随机噪声，文中未深入探讨专门的去噪机制。
*   **计算开销**：虽然推理速度较快，但在训练阶段，CLIP 模型的引入和迭代提示优化会增加额外的显存占用和计算时间。
*   **极端场景限制**：在完全无光或语义信息极度模糊的图像中，CLIP 引导可能会失效或产生错误的语义联想。

（完）
