---
title: PPO guided Agentic Pipeline for Adaptive Prompt Selection and Test Case Generation
title_zh: PPO 引导的自适应提示词选择与测试用例生成智能体流水线
authors: "Gourisetty Venkata Sai Koushik, Dama Aditya, Mahankali Harish Sai, Peddi Siddarhta, Shadab Ahmad, Vivek Yelleti"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00942v1"
tags: ["query:bopl"]
score: 8.0
evidence: PPO引导的自适应提示选择代理流水线
tldr: 本研究针对大规模复杂软件系统的测试用例生成难题，提出了一种基于强化学习驱动的智能体框架。该框架结合了近端策略优化（PPO）算法与大语言模型（LLM），通过两阶段流程实现：首先利用ToT引导的智能体精简源代码，随后利用PPO策略网络根据代码复杂度和覆盖率指标动态选择最优提示词策略。实验表明，该方法在分支和行覆盖率上显著优于CBMC等传统工具及现有LLM方法，展现了自适应提示词选择在复杂程序测试中的优越性。
selection_source: fresh_fetch
motivation: 针对具有海量且嵌套复杂的源代码的大规模软件系统，传统方法难以生成能够充分覆盖程序的测试用例。
method: 提出一种两阶段框架，先通过ToT优化智能体精简代码，再利用PPO策略网络根据实时覆盖率动态选择八种不同的LLM提示词技术。
result: 在20个基准程序上的实验显示，PPO-LLM在分支和行覆盖率上全面超越了CBMC、kS-LLM和kS-LLM++。
conclusion: 实验证明，由PPO驱动的自适应提示词选择策略在提升复杂程序测试覆盖率方面比静态提示词策略更具优势。
---

## 摘要
开发能够彻底测试大规模软件系统的有效测试用例本质上具有很大难度，特别是当这些系统拥有庞大、复杂且深度嵌套的源代码时。在这项工作中，我们提出了一种利用强化学习驱动的智能体框架生成测试用例的新方法，该框架将近端策略优化（PPO）与大语言模型（LLM）引擎相结合，以在测试生成过程中引导提示词的选择。我们的方法分为两个阶段。在第一阶段，受思维树（ToT）引导的优化智能体在不改变源代码功能行为的前提下，通过消除冗余来对源代码进行划分和最小化。在第二阶段，训练一个基于 PPO 的策略网络，根据代表源代码复杂度指标和实时覆盖率指标的 11 维输入状态向量，从八种不同的提示技术（如边界值分析、随机模糊测试等）中选择提示词，从而引导 LLM 引擎探索程序中未访问的路径。PPO 智能体根据行覆盖率和分支覆盖率的提升、对未探索分支的惩罚以及对缩减源代码长度的奖励的综合评分来获取奖励。对 20 个基准程序进行的实验表明，在从 BOUND~1 到 BOUND~2000 的各种循环边界值下，所提出的 PPO-LLM 方法在几乎所有情况下的分支和行覆盖率方面都优于 CBMC、kS-LLM 和 kS-LLM++。在 BOUND~1 时，PPO-LLM 在 PALS 套件上的分支覆盖率达到了 100%，而 kS-LLM++ 仅为 86.8% 左右。这证实了由 PPO 驱动的自适应提示词选择在 PALS 类程序上的表现显著优于静态提示策略。

## Abstract
Developing effective test cases capable of thoroughly exercising large-scale software systems is inherently difficult, especially if such systems have voluminous, complex, and deeply nested source codes. In this work, we present a novel approach for generating test cases using a reinforcement learning-driven agentic framework where Proximal Policy Optimization (PPO) is coupled with an LLM engine to guide prompt selection during test generation. Our approach consists of two phases. In Phase I, the ToT-guided optimization agent partitions and minimizes the source code by removing redundancies without changing the functional behavior of the source code. In Phase II, a PPO-based policy network is trained to solve the problem of selecting prompts among eight different prompting techniques, such as Boundary Value Analysis, Random Fuzzing, etc., based on the inputted 11-dimensional state vector representing the source code complexity metrics and live coverage metrics to direct the LLM engine towards exploring unvisited paths in the program. The PPO agent receives rewards based on a combination of increases in line and branch coverages, penalties for unexplored branches, and rewards for reducing source code length. From experiments conducted on twenty benchmark programs, it is evident that the proposed approach, PPO-LLM, outperforms CBMC, kS-LLM, and kS-LLM++ in terms of branch and line coverage in almost all cases, for various loop bound values ranging from BOUND~1 to BOUND~2000. While at BOUND~1, the coverage of branches is 100\% using PPO-LLM on the PALS suite, in comparison, it is around 86.8\% using kS-LLM++. This confirms that adaptive prompt selection driven by PPO substantially outperforms static prompting strategies on PALS type programs.

---

## 论文详细总结（自动生成）

这是一份关于论文《PPO guided Agentic Pipeline for Adaptive Prompt Selection and Test Case Generation》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何为具有大规模、复杂且深度嵌套源代码的软件系统生成高覆盖率的自动化测试用例（ATCG）。
*   **研究背景**：
    *   **传统方法局限**：符号执行和模型检测（如 CBMC）在处理大型程序时面临“状态爆炸”问题，难以扩展。
    *   **LLM 方法局限**：虽然大语言模型（LLM）在代码理解上表现出色，但现有的 LLM 测试生成方法多采用**静态提示词（Static Prompting）**。当测试覆盖率进入瓶颈期时，静态提示词无法根据当前的覆盖反馈调整策略，导致生成效率低下和资源浪费。
*   **研究动机**：通过引入强化学习（RL），使系统能够根据代码特征和实时覆盖率动态选择最合适的提示词策略，从而突破覆盖率平台期。

### 2. 论文提出的方法论
论文提出了一个名为 **PPO-LLM** 的两阶段智能体流水线框架：

*   **阶段 I：代码优化（Stage I: Code Optimization）**
    *   利用 **思维树（Tree-of-Thought, ToT）** 引导的优化智能体对原始 C 代码进行精简。
    *   **流程**：将代码碎片化（Fragmentation） $\rightarrow$ ToT 重构（去除死代码、简化逻辑） $\rightarrow$ 等效性验证。
    *   **目的**：降低 LLM 处理的 Token 数量，减少幻觉，提高语义一致性。

*   **阶段 II：PPO 引导的自适应测试生成（Stage II: PPO-LLM Generation）**
    *   **MDP 建模**：将提示词选择建模为马尔可夫决策过程（MDP）。
    *   **状态空间（11维向量）**：包括代码行数（LOC）、函数/分支/循环数量、循环复杂度、实时行覆盖率/分支覆盖率、编程语言标识及生成进度。
    *   **动作空间（8种提示词策略）**：包括边界值分析（BVA）、分支覆盖探索（BCE）、边缘案例猎人（ECH）、错误路径练习（EPE）、循环边界测试（LBT）、数据类型压力测试（DTS）、路径覆盖最大化（PCM）和随机模糊测试（FUZZ）。
    *   **奖励函数（Reward Shaping）**：综合考虑行覆盖率增量（$\Delta LC$）、分支覆盖率增量（$\Delta BC$）、测试用例唯一性、未测试分支惩罚以及代码压缩奖励。
    *   **核心算法**：使用 **近端策略优化（PPO）** 算法训练一个两层多层感知机（MLP）作为策略网络，实时决定每一轮生成应使用哪种提示词模板。

### 3. 实验设计
*   **数据集/场景**：选取了 20 个工业级 C 语言基准程序，分别来自 **PALS**（并行过程代数系统）和 **RERS**（反应式系统严格检查）基准套件。
*   **Benchmark 设置**：针对每个程序设置了 8 个不同的循环边界（Bound 1, 10, 100, 250, 500, 1000, 1500, 2000），共计 **160 个实验实例**。
*   **对比方法**：
    1.  **CBMC**：传统的有界模型检测工具。
    2.  **kS-LLM**：早期的 LLM 测试生成框架。
    3.  **kS-LLM++**：引入 ToT 优化但仍使用静态提示词的改进版 LLM 框架。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **模型细节**：提到策略网络是一个非常轻量级的 MLP（输入11维，隐藏层32维，输出8维），并指出该架构设计旨在确保在单次生成运行中即可收敛，无需大规模预训练。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 160 组实验（20 程序 $\times$ 8 边界），涵盖了从几百行到近万行（9111 LOC）的不同规模代码。
*   **充分性评价**：实验设计较为充分，通过多维度的 Bound 设置展示了方法在不同计算深度下的稳定性。
*   **公平性**：对比了传统工具和最新的 LLM SOTA 方法，并使用了标准的行覆盖率和分支覆盖率作为评价指标，实验过程具有客观性。

### 6. 论文的主要结论与发现
*   **PPO-LLM 显著优于基线**：在 PALS 基准测试中，PPO-LLM 在几乎所有 Bound 下的分支覆盖率均达到 95%-100%，远超 CBMC 和 kS-LLM++。
*   **自适应策略的价值**：实验证明，当简单分支被覆盖后，PPO 智能体会自动切换到“错误路径练习”或“路径覆盖最大化”等更深层的策略，这种动态调整是提升覆盖率的关键。
*   **代码优化的重要性**：当代码行数（LOC）通过阶段 I 减少 60% 以上时，测试生成的覆盖率提升最为显著，证实了减少 LLM “认知负荷”的有效性。
*   **局限性发现**：在 RERS（PS-系列）程序中，PPO-LLM 在某些高 Bound 场景下不如 kS-LLM++。这是因为 PS 程序依赖长序列环境输入，而当前的 PPO 奖励信号在处理这种长程依赖时存在延迟或失真。

### 7. 优点（亮点）
*   **动态闭环**：打破了 LLM 测试生成的静态提示词范式，引入 RL 实现“覆盖率反馈-策略调整”的闭环。
*   **两阶段协同**：结合了 ToT 代码优化和 PPO 策略选择，既精简了输入，又优化了生成逻辑。
*   **策略库丰富**：设计了 8 种针对性极强的测试专家提示词模板，覆盖了软件测试的主要维度。

### 8. 不足与局限
*   **长程依赖问题**：对于反应式系统（Reactive Systems），分支的触发往往依赖于极长的输入序列，当前基于单步/短时奖励的 PPO 难以学习到这种复杂的时序策略。
*   **算力信息缺失**：未提供具体的硬件环境和推理成本分析，难以评估其在工业环境下的经济性。
*   **奖励函数敏感性**：奖励函数中的权重（如 0.4, 0.5 等）多为经验设定，可能需要针对不同类型的软件进行微调。

（完）
