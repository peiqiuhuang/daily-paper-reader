---
title: When Do Explanations Help In-Context Learning? A Comparative Study of Natural Language Explanation Types and Faithfulness
title_zh: 解释何时有助于上下文学习？自然语言解释类型与忠实度的比较研究
authors: "Mahdi Dhaini, Adam Dejl, Juraj Vladika, Volkan Özer, Barbara Plank, Gjergji Kasneci"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16627v1"
tags: ["query:bopl"]
score: 8.0
evidence: 解释增强提示和语境学习评估
tldr: 本研究深入探讨了自然语言解释（NLE）在上下文学习（ICL）中对模型性能的影响。通过在6个基准测试和4个指令微调模型上进行对比评估，研究了不同来源（人工、自生成、外部模型生成）和筛选策略（随机、基于忠实度）的解释效果。结果显示，在分类任务中加入解释通常能提升准确率，且外部生成的解释表现强劲，而基于忠实度的筛选效果因任务和模型而异。该研究为优化提示词工程中的解释选择提供了重要见解。
selection_source: fresh_fetch
motivation: 尽管自然语言解释在上下文学习中被广泛应用，但不同来源和筛选策略对模型下游性能的具体影响仍不明确。
method: 在多个基准和模型上对比了人工编写、自生成及外部模型生成的解释，并评估了基于忠实度过滤的筛选策略。
result: 分类任务中加入解释普遍有效，外部模型生成的解释与人工解释相当，而基于忠实度的筛选仅带来微小且不稳定的收益。
conclusion: 解释的语义一致性对性能提升至关重要，研究结果为在实际提示词流程中选择和报告解释提供了实践指导。
---

## 摘要
自然语言解释 (NLEs) 越来越多地被用作输入，例如作为少样本推理过程 (few-shot rationales) 来影响上下文学习 (ICL) 中的模型行为。然而，在解释增强提示 (explanation-augmented prompting) 中，不同类型的 NLE 对下游模型性能的影响如何进行比较仍不清楚。因此，我们在六个基准测试和四个指令微调模型上进行了比较评估，研究了 NLE 来源（可用时的手写解释、自生成解释、外部大语言模型生成的解释）和 NLE 选择（随机过滤与基于忠实度的过滤）在 ICL 设置下如何影响 NLE 的下游效用。我们广泛的评估表明，在分类式基准测试中，在少样本提示中添加 NLE 通常比不带解释的少样本提示更能提高准确率；在 NLE 来源中，外部生成的 LLM-NLE 通常提供强大的下游效用，并且在两者都可用时与人类推理过程保持竞争力，而自生成 NLE (self-NLEs) 对选择策略更为敏感。在数学推理方面，其效果更多地取决于模型和来源。我们进一步表明，基于忠实度的自生成 NLE 选择在总体上产生了较小的平均收益，但根据指标、任务和模型的不同，可能会提高或降低性能。不同的忠实度指标可能存在显著分歧，从而影响所选的自生成 NLE 示例及其下游预测效用。使用随机交换和分布外推理过程进行的鲁棒性测试表明了部分鲁棒性，这表明语义对齐有助于性能提升。总之，我们的结果为在实际提示流水线中选择和报告影响模型行为的解释提供了见解。

## Abstract
Natural language explanations (NLEs) are increasingly used as inputs, for example, as few-shot rationales that influence model behavior in in-context learning (ICL). However, it remains unclear how different types of NLEs compare in their effects on downstream model performance in explanation-augmented prompting. Therefore, we provide a comparative evaluation across six benchmarks and four instruction-tuned models, studying how NLE source (human-written when available, self-generated explanations, generated by an external LLM) and NLE selection (random vs faithfulness-based filtering) affect downstream utility of NLEs when used in ICL settings. Our extensive evaluation shows that, on classification-style benchmarks, adding NLEs to few-shot prompts often improves accuracy over few-shot prompting without explanations; among NLE sources, externally generated LLM-NLEs often provide strong downstream utility and remain competitive with human rationales where both are available, whereas self-NLEs are more sensitive to the selection strategy. On math reasoning, the effects are more model- and source-dependent. We further show that faithfulness-based selection of self-NLEs yields small average gains overall, but can improve or reduce performance depending on the metric, task, and model. Different faithfulness metrics can disagree substantially, affecting which self-NLE examples are selected and their downstream predictive utility. Robustness tests with randomly swapped and out-of-distribution rationales indicate partial robustness, suggesting that semantic alignment contributes to performance gains. Overall, our results provide insights for selecting and reporting explanations that influence model behavior in practical prompting pipelines.

---

## 论文详细总结（自动生成）

这篇论文对自然语言解释（NLE）在上下文学习（ICL）中的作用进行了系统且深入的比较研究。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在少样本提示（Few-shot Prompting）中，不同来源（人工、模型自生成、外部模型生成）和不同质量（尤其是“忠实度”）的自然语言解释如何影响大语言模型（LLM）的下游任务性能？
*   **研究动机**：虽然 NLE 被广泛用于提高模型的可解释性，但其作为“行为塑造输入”的功能性作用（即能否提高预测准确率）尚不明确。目前缺乏关于解释来源、筛选策略以及解释质量对 ICL 性能影响的系统性证据。

### 2. 方法论
研究者提出了一个模块化的评估框架，包含四个关键步骤：
1.  **少样本样本选择**：采用“错误优先”策略，选择模型在零样本设置下预测错误的样本作为少样本示例，以提供纠错信号。
2.  **NLE 生成**：
    *   **人工解释 (Human-NLEs)**：来自现有的可解释数据集。
    *   **自生成解释 (Self-NLEs)**：由被评估模型自身生成（采用 Ph-CoT 流程，并针对正确标签进行重构）。
    *   **外部 LLM 解释 (LLM-NLEs)**：由外部模型（如 GPT-4o-mini 或 o3-mini）生成的解释。
3.  **解释筛选策略**：
    *   **随机选择**：不考虑质量。
    *   **基于忠实度的筛选**：使用两种指标（$fm_1$：综合多维度得分；$fm_2$：基于反事实干预的二元一致性）筛选出最高或最低忠实度的解释。
4.  **提示词设计**：构建包含 $n=6$ 个示例的提示词，对比不同解释类型对模型最终预测的影响。
5.  **鲁棒性测试**：设计了“随机交换解释”（同领域内错配）和“分布外解释”（跨领域错配）的压力测试。

### 3. 实验设计
*   **数据集**：涵盖 6 个基准测试，包括常识推理（ECQA）、自然语言推理（e-SNLI）、讽刺检测（SNARKS）、因果判断（CJ）、布尔表达式（Bool）和数学推理（GSM8K）。
*   **评估模型**：选取了不同规模和家族的 4 个指令微调模型：GPT-4o-mini、Llama-3.1-8B、Llama-3.3-70B 和 Mistral-7B-Instruct-v0.3。
*   **基准方法 (Baselines)**：零样本（ZS）、不带解释的少样本（FS-R）、零样本思维链（CoT）。
*   **实验设置**：固定为 6-shot，每组配置运行 5 次取平均值。

### 4. 资源与算力
*   **算力说明**：论文未明确列出具体的 GPU 型号或训练时长。
*   **实现方式**：实验主要通过 API 调用完成，包括 OpenAI API（用于 GPT 系列模型）和 Together AI API（用于 Llama 和 Mistral 模型）。由于是推理侧的 ICL 研究，不涉及大规模的模型参数微调。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 6 个任务、4 个模型、3 种解释来源、多种筛选策略以及 2 种压力测试。
*   **充分性**：实验设计非常详尽。通过跨模型、跨任务的对比，以及对忠实度指标分歧的深入探讨，研究展现了极高的客观性。每组实验运行 5 次并提供标准差，确保了结果的统计可靠性。

### 6. 主要结论与发现
*   **解释的普遍益处**：在分类任务中，加入 NLE 通常能显著提升少样本学习的准确率。
*   **来源对比**：**外部 LLM 生成的解释**在分类任务中表现最强且最稳定，甚至能与人工解释媲美；而**自生成解释**的效果波动较大，且高度依赖于筛选策略。
*   **忠实度指标的分歧**：不同的忠实度度量指标（$fm_1$ 与 $fm_2$）之间存在严重分歧（约 50% 的样本结论不一致），这直接影响了筛选效果。
*   **数学推理的特殊性**：在 GSM8K 等数学任务上，解释的效果高度依赖于模型能力和来源，且基于忠实度的筛选在某些模型上可能导致性能剧降。
*   **语义对齐的重要性**：压力测试显示，虽然模型对错误解释有一定容忍度，但语义不匹配（尤其是跨领域）会显著损害性能，证明了解释与示例之间的“语义对齐”是性能提升的关键。

### 7. 优点
*   **系统性强**：首次全面对比了人工、自生成和外部 LLM 解释在 ICL 中的预测效用。
*   **现实指导意义**：为开发者提供了实用的建议（如：优先使用外部强模型生成解释，而非耗时耗力的人工标注）。
*   **批判性视角**：深入揭示了现有自动忠实度评估指标的不稳定性，对可解释性研究具有警示作用。

### 8. 不足与局限
*   **人工数据覆盖有限**：仅在两个数据集（ECQA, e-SNLI）上有高质量人工解释，限制了人工 vs. 机器解释对比的普适性。
*   **固定参数限制**：实验固定在 6-shot 和特定的提示词模板下，未探讨示例数量或模板变体对结论的影响。
*   **指标依赖性**：研究高度依赖自动化的忠实度指标，而这些指标本身可能无法完全捕捉模型内部真实的因果推理过程。

（完）
