---
title: Hierarchical Prototype-Memory Adaptation of SAM for Surgical Instrument Segmentation
title_zh: 面向手术器械分割的 SAM 分层原型记忆适配
authors: "Xinning Yao, Jingjing Wang, Jinghua Yue, Xiaoyan Luo, Fugen Zhou, Bo Liu"
date: 2026-08-25
pdf: "https://arxiv.org/pdf/2608.24541v1"
tags: ["query:bopl"]
score: 8.0
evidence: 通过提示学习适配基础模型
tldr: 针对手术器械分割中SAM模型适配鲁棒性不足及多尺度特征耦合效率低的问题，本文提出HPMA框架。该框架通过构建冻结的多尺度视觉原型记忆库，并利用轻量级适配器将其集成到SAM中，以保留稳定的类别特征。通过引入尺度匹配耦合机制，在全局、结构和局部层面优化特征对齐，显著提升了模型在复杂手术场景下的分割精度，在EndoVis数据集上达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的SAM适配方法在复杂手术环境下容易导致提示词退化为任务特定参数，且难以有效处理多尺度视觉线索。
method: 提出分层原型记忆适配框架（HPMA），利用冻结的多尺度原型库和尺度匹配耦合机制来增强SAM的类别记忆与特征对齐。
result: 在EndoVis2017和EndoVis2018公开数据集上的实验表明，该方法超越了现有的基础模型适配技术，取得了领先的分割性能。
conclusion: HPMA通过引入稳定的分层原型记忆，有效解决了手术器械分割中的鲁棒性和多尺度融合难题，为医疗基础模型适配提供了新思路。
---

## 摘要
手术器械分割（SIS）是计算机辅助手术的基础，可靠的器械掩码能够实现精确的场景理解和临床辅助。最近，通过提示学习（prompt-learning）将 Segment Anything Model (SAM) 等基础模型适配到手术领域已取得了令人鼓舞的结果。然而，这些适配模型在具有挑战性的手术条件下的性能受到次优适配机制的限制。具体而言，纯粹通过下游分割损失优化提示或原型往往会导致它们退化为特定任务的参数，而不是作为持久、稳定的类别记忆，从而降低了它们应对复杂术中变化的鲁棒性。此外，通过单一提示路径路由多尺度视觉线索会产生瓶颈，阻碍有效的尺度匹配耦合。为了解决这些局限性，我们提出了 HPMA，一种用于 SAM 的分层原型记忆适配框架。具体来说，HPMA 从标注的手术场景中构建了一个冻结的多尺度视觉原型记忆库，并使用轻量级适配器将其集成到 SAM 的特征空间中，以保留稳定的类别证据。为了最大化多尺度线索的效用，我们引入了一种尺度匹配耦合机制，其中全局原型校准类级提示特征，结构原型引导解码器对象查询，而局部原型通过局部对齐目标对齐高分辨率特征图。在公开的 EndoVis2017 和 EndoVis2018 数据集上进行的大量实验表明，我们的方法达到了最先进的性能，优于现有的基础模型适配方法。

## Abstract
Surgical instrument segmentation (SIS) is fundamental for computer-assisted surgery, where reliable instrument masks enable precise scene understanding and clinical assistance. Recently, adapting foundation models like the Segment Anything Model (SAM) to the surgical domain via prompt-learning has shown encouraging results. However, the performance of these adapted models under challenging surgical conditions is constrained by suboptimal adaptation mechanisms. Specifically, optimizing prompts or prototypes purely via downstream segmentation loss tends to cause them to degenerate into task-specific parameters rather than serving as persistent, stable category memory, thereby degrading their robustness against complex intraoperative variations. Moreover, routing multi-scale visual cues through a single prompt pathway creates a bottleneck that hinders effective scale-matched coupling. To address these limitations, we propose HPMA, a Hierarchical Prototype-Memory Adaptation framework for SAM. Specifically, HPMA constructs a frozen, multi-scale visual prototype memory bank from annotated surgical scenes and integrates it into SAM's feature space using lightweight adapters to preserve stable category evidence. To maximize the utility of multi-scale cues, we introduce a scale-matched coupling mechanism where global prototypes calibrate class-level prompt features, structural prototypes guide decoder object queries, and local prototypes align high-resolution feature maps through a local alignment objective. Extensive experiments on the public EndoVis2017 and EndoVis2018 datasets demonstrate that our approach achieves state-of-the-art performance, outperforming existing foundation model adaptation methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HPMA（Hierarchical Prototype-Memory Adaptation）** 的框架，旨在将基础模型 SAM（Segment Anything Model）更有效地适配到手术器械分割（SIS）任务中。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：手术器械分割面临器械结构精细、术中环境复杂（如强反光、遮挡、边界模糊）以及手术领域与自然图像之间巨大的领域鸿沟（Domain Gap）。
*   **研究动机**：
    *   **提示词退化（Prompt Degeneration）**：现有的适配方法（如提示学习）在优化过程中，提示词容易退化为特定任务的参数，失去作为稳定类别记忆的能力，导致模型在复杂多变的术中场景下鲁棒性不足。
    *   **表征瓶颈（Representational Bottleneck）**：现有方法通常通过单一路径传递多尺度视觉线索，无法实现视觉特征与模型组件之间的有效尺度匹配耦合。

### 2. 方法论：HPMA 框架
HPMA 的核心思想是**解耦领域知识存储与下游特征适配**，通过构建冻结的视觉原型记忆库来保持类别的稳定性。
*   **多尺度原型记忆库构建**：
    *   从标注的手术图像中提取多尺度特征（FPN 特征）。
    *   利用 K-Means 聚类生成每个类别的 $K$ 个原型向量，并将其**冻结**，作为持久的统计记忆。
*   **分层表示级别到模块的耦合机制**：
    *   **全局级（Global Level）**：利用全局原型通过交叉注意力机制校准原始文本提示特征，确保文本嵌入锚定在手术视觉流形上。
    *   **结构级（Structural Level）**：将结构原型注入 SAM 解码器的对象查询（Object Queries）中，为模型提供器械几何结构的先验偏置（如刀柄、关节、尖端）。
    *   **局部级（Local Level）**：在训练阶段引入**局部对齐目标（Local Alignment Loss）**，通过余弦相似度将高分辨率特征图与局部原型对齐，强化边缘和纹理细节，且不增加推理开销。
*   **轻量级适配器**：使用带有可学习缩放因子的轻量级映射函数（如 MLP），将冻结的记忆库信息注入到 SAM 的特征空间中。

### 3. 实验设计
*   **数据集**：
    *   **EndoVis2017**：包含 8 个视频，7 类器械，采用 4 折交叉验证。
    *   **EndoVis2018**：包含 11 个训练视频和 4 个验证视频，7 类器械。
*   **评估指标**：Challenge IoU、IoU、mean class IoU (mc IoU) 和 mean Dice (mDice)。
*   **对比方法**：
    *   **专用手术分割模型**：ISINet, MATIS, S3Net, SCI-Net 等。
    *   **基于 SAM 的适配方法**：TrackAnything, PerSAM, SurgicalSAM, MA-SAM2, Distillation-SAM 等。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA A100 GPU** 进行训练。
*   **训练细节**：
    *   优化器：AdamW。
    *   输入尺寸：1008 × 1008。
    *   训练轮数：20 Epochs。
    *   学习率：基础学习率 $8 \times 10^{-5}$，预训练骨干网络采用更小的学习率。
*   **效率对比**：文中明确提到 HPMA 在使用 ViT-H 骨干时，计算量为 4917.91 GFLOPs（低于 SurgicalSAM 的 5989.78），推理速度为 **2.69 FPS**（高于 SurgicalSAM 的 1.98 FPS）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流 Benchmark 上进行了全面测试，涵盖了多类器械的详细 IoU 数据。
*   **消融实验**：
    *   验证了全局、结构、局部三个层级的增量贡献。
    *   对比了不同的原型注入策略（如注入位置的排列组合）。
    *   分析了原型数量 $K$ 值对性能的影响（发现 $K=4$ 时效果最佳）。
*   **客观性**：通过可视化注意力图（Attention Map）直观展示了模型如何聚焦于器械的物理组件，证明了方法的有效性。实验对比中，若复现结果低于原论文，则直接引用原论文数据，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：HPMA 在 EndoVis2017 和 2018 上均取得了 SOTA 性能，显著优于现有的专用模型和基于 SAM 的适配模型。
*   **分层耦合的必要性**：实验证明，将不同尺度的原型注入到对应的模型组件（如全局对文本、结构对解码器）比单一路径或盲目融合更有效。
*   **鲁棒性增强**：通过冻结原型记忆，模型在面对强反光和组织重叠等复杂术中变化时表现出更强的语义稳定性。

### 7. 优点与亮点
*   **创新性设计**：提出了“分层原型记忆”的概念，解决了提示词在微调过程中的退化问题。
*   **效率与精度平衡**：在提升分割精度的同时，通过轻量级适配器和训练辅助损失（局部对齐）降低了推理负担，提高了 FPS。
*   **可解释性**：通过注意力图可视化，证明了模型确实学习到了器械的结构化知识，而非简单的像素匹配。

### 8. 不足与局限
*   **实时性挑战**：虽然 FPS 有所提升，但 2.69 FPS 距离真正的实时手术导航（通常需 20+ FPS）仍有较大差距。
*   **原型依赖性**：原型库的构建依赖于离线的 K-Means 聚类，对于从未见过的全新器械类别，可能需要重新构建记忆库。
*   **应用限制**：目前主要在 2D 图像/视频上验证，尚未扩展到 3D 医疗影像或更复杂的机器人手术力反馈结合场景。

（完）
