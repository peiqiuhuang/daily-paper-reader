---
title: Verbalizing Subliminal Learning Effects Using Text Optimization
title_zh: 利用文本优化实现潜意识学习效应的语言化
authors: "Nathan Hu, Sanmi Koyejo, Christopher Potts"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.16927v1"
tags: ["query:bopl"]
score: 8.0
evidence: 通过文本优化从潜意识学习中恢复并语言化提示词
tldr: 本文研究了蒸馏数据集中的“潜意识学习”现象，即模型会习得数据集中未显式编码的教师模型特征。作者提出了一种名为SALVE的方法，通过文本优化将这些隐性特征转化为可读的提示词。该研究揭示了潜意识学习与上下文蒸馏的联系，并证明了SALVE在检测数据中毒、偏见注入及偏好数据筛选等多种场景下的有效性，为理解和防御模型蒸馏风险提供了新视角。
selection_source: fresh_fetch
motivation: 潜意识学习导致模型习得数据集中未显式编码的特征，带来了模型开发挑战和数据中毒风险。
method: 提出SALVE方法，通过优化软提示词并结合束搜索（beam search）将其转化为可读的文本描述。
result: SALVE能可靠地从数据集中提取教师模型的隐性特征，在多种复杂场景下均优于传统文本优化方法。
conclusion: SALVE为检测和理解潜意识学习效应提供了一种主动防御手段，深化了对模型蒸馏机制的认识。
---

## 摘要
潜意识学习是一种现象，即蒸馏数据集会传递教师模型中并未在数据集本身显式编码的特征。这为模型开发带来了新的挑战，并产生了数据投毒带来的新风险。在这项工作中，我们利用文本优化来检测潜意识学习效应，并将其描述为可读的提示词。受提示词引导的教师模型所产生的潜意识学习启发，我们观察到这是上下文蒸馏的一种特殊情况，并利用这一观察结果证明，在理论上，经过提示词引导的潜意识学习数据集能够识别出教师模型的提示词。我们将恢复该提示词的过程简化为一个文本优化问题，并提出了一种近似求解的方法。我们的方法 SALVE（搜索辅助的潜在语言化）通过优化软提示词，查询同一模型将其语言化为文本，并利用束搜索（beam search）来确保语言化的可靠性。在标准的潜意识学习设置中，SALVE 能够可靠地恢复出描述教师模型特征的可读提示词，而常见的文本优化方法则无法做到这一点。此外，我们发现即使在潜意识学习失效的情况下，SALVE 在某些设置中仍能从数据集中恢复出教师模型的特征，而修改学生模型的训练以改进上下文蒸馏则可能产生潜意识学习效应。最后，我们展示了 SALVE 在另外三种设置中也能检测到潜意识学习效应：（1）潜意识学习数据与无关数据的混合；（2）当教师模型通过激活转向（activation steering）产生偏见时生成的数据；以及（3）通过 Logit-Linear 选择筛选出的真实偏好数据子集。总的来说，我们的研究加深了对潜意识学习的理解，并提出 SALVE 作为一种主动检测潜意识学习效应的方法。

## Abstract
Subliminal learning is a phenomenon in which a distillation dataset transmits traits from the teacher model that are not legibly encoded in the dataset itself. This introduces a new challenge for model development and creates new risks from data poisoning. In this work, we use text optimization to detect subliminal learning effects and describe them as legible prompts. Subliminal learning from a prompted teacher motivates our approach. We observe that this is a special case of context distillation and leverage this observation to show that, in theory, the prompted subliminal learning dataset identifies the teacher's prompt. We reduce recovering this prompt to a text optimization problem and present a method to approximately solve it. Our method, SALVE (Search-Aided Latent Verbalization), optimizes a soft prompt, queries the same model to verbalize it as text, and uses beam search to make the verbalization reliable. In the standard subliminal learning setting, SALVE reliably recovers legible prompts that name the teacher's trait, while common text optimization methods fail to do so. In addition, we find that there are settings in which SALVE recovers the teacher's trait from a dataset even when subliminal learning fails, but that modifying student training to improve context distillation can create subliminal learning effects. We lastly show that SALVE detects subliminal learning effects in three additional settings: (1) mixtures of subliminal learning data and unrelated data, (2) data generated when the teacher is biased via activation steering, and (3) subsets of real preference data selected via Logit-Linear Selection. Overall, our results deepen our understanding of subliminal learning and present SALVE as a method to proactively detect subliminal learning effects.

---

## 论文详细总结（自动生成）

以下是对论文《利用文本优化实现潜意识学习效应的语言化》（*Verbalizing Subliminal Learning Effects Using Text Optimization*）的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：潜意识学习（Subliminal Learning）是指学生模型在蒸馏过程中，习得了教师模型中并未在数据集中显式编码的特征（如特定的偏好或偏见）。
*   **动机**：这种现象带来了严重的安全隐患，包括数据投毒和模型行为的不可预测性。由于这些特征在数据集中是“隐性”的，传统的审计方法难以检测。
*   **核心目标**：通过文本优化技术，将数据集中的隐性特征“翻译”为人类可读的提示词（Prompts），从而实现对潜意识学习效应的主动检测与审计。

### 2. 方法论：SALVE (Search-Aided Latent Verbalization)
*   **核心思想**：将潜意识学习视为上下文蒸馏的特例，证明了数据集在理论上唯一标识了生成该数据的提示词。通过优化一个软提示词（Soft Prompt）来最小化模型在数据集上的损失，再将其转化为自然语言。
*   **关键步骤**：
    1.  **软提示词优化**：在冻结模型参数的情况下，通过梯度下降优化一个长度为 $k$ 的软提示词向量，使其在数据集上达到最小损失。
    2.  **语言化（Verbalization）**：将优化后的软提示词作为输入，查询语言模型将其“翻译”为自然语言描述。
    3.  **可靠性增强（束搜索）**：为了解决语言化过程中的不确定性，引入了句子级的束搜索（Beam Search）。通过将候选提示词作为系统提示词，在小批量数据上计算损失，引导解码过程，确保生成的提示词既符合语义又具有低损失。

### 3. 实验设计
*   **场景与数据集**：
    *   **标准潜意识学习**：通过提示词引导教师模型（Qwen2.5-7B）生成无关的数字序列，观察学生模型是否习得动物偏好。
    *   **数据稀释**：将潜意识数据与无关数据混合。
    *   **激活转向（Activation Steering）**：通过激活向量而非提示词引导教师模型。
    *   **偏好数据筛选**：使用 Logit-Linear Selection (LLS) 筛选出的偏好数据。
    *   **防御审计**：检测针对大模型的“加密微调攻击”（Ciphered Fine-tuning Attacks）。
*   **对比方法**：对比了 OPRO（LLM 作为优化器）、GCG（梯度引导搜索）、AutoDAN、PGD、GBDA 以及 LARGO 等多种文本优化和对抗攻击方法。

### 4. 资源与算力
*   **算力消耗**：论文明确记录了各优化方法的 GPU 耗时。SALVE 在 A100-80G GPU 上平均每轮运行耗时约 1.5 小时，属于计算效率较高的审计方法。
*   **模型规模**：实验涵盖了从 1B 到 31B 参数规模的多种模型（如 Qwen2.5, OLMo, Llama-3.1/3.2, Gemma-4 等）。

### 5. 实验数量与充分性
*   **充分性**：实验设计非常扎实。不仅在标准场景下进行了对比，还通过消融实验（如 Best-of-N 对比）验证了束搜索的必要性。
*   **客观性**：通过多模型、多动物偏好、多攻击类型（如加密攻击）进行了广泛验证，且使用了 LLM 审计员进行客观评分，实验覆盖面广，结论具有较强的说服力。

### 6. 主要结论与发现
*   **可恢复性**：SALVE 能够可靠地从数据集中恢复出描述教师模型特征的提示词，即使在学生模型未表现出明显行为改变的情况下，也能检测到数据中潜藏的特征。
*   **机制洞察**：潜意识学习的失效往往归因于学生模型训练方式（如 LoRA 训练位置）的限制，而非数据中缺乏特征。通过修改训练策略（如微调嵌入层），可以显著放大潜意识学习效应。
*   **通用性**：SALVE 不仅适用于提示词引导，还能有效检测激活转向和偏好数据筛选带来的隐性偏见。

### 7. 优点
*   **可解释性强**：将复杂的模型行为差异转化为人类可读的自然语言提示词，极大地降低了审计门槛。
*   **主动防御**：提供了一种在模型训练前即可审计数据集风险的工具，符合“数据到行为”的预测需求。
*   **鲁棒性**：通过束搜索解决了传统文本优化方法容易陷入局部最优或生成乱码的问题。

### 8. 不足与局限
*   **语言化依赖**：SALVE 依赖于语言模型对软提示词的“理解”和“翻译”能力，如果模型本身无法理解某些隐性特征，语言化结果可能存在幻觉。
*   **表达能力限制**：单个提示词的表达能力有限，难以完全捕捉复杂的全参数微调带来的所有行为变化。
*   **应用门槛**：虽然比全参数审计简单，但仍需进行梯度优化，对于超大规模数据集的实时审计可能存在计算压力。

（完）
