---
title: "COMPAS: Difficulty-Aware Joint Search for Optimizing Code Generation"
title_zh: COMPAS：用于优化代码生成的难度感知联合搜索
authors: "Jingzhi Gong, Jie M. Zhang, Gunel Jahangirova, Dong Huang, Mohammad Reza Mousavi, Mark Harman"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04336v1"
tags: ["query:bopl"]
score: 8.0
evidence: 联合搜索优化提示和配置
tldr: 本研究针对现有代码生成系统优化维度单一的问题，提出了COMPAS框架。该框架首次实现了模型选择、提示词优化与解码设置的联合搜索，并发现最佳配置与任务难度密切相关。通过学习不同难度组的质量-成本前沿，COMPAS能在推理时为任务匹配最优配置。实验表明，该方法在LiveCodeBench上显著提升了代码生成准确率，同时大幅降低了计算成本，并在仓库级任务中表现出优异的迁移能力。
selection_source: fresh_fetch
motivation: 现有优化方法往往只针对模型、提示词或解码设置中的某一方面，忽略了这些因素间的相互作用以及任务难度对最优配置的影响。
method: 提出一种难度感知的联合搜索方法，通过低成本模型选择和提示词-解码联合优化，为不同难度的任务构建质量-成本最优前沿并进行在线路由。
result: "在LiveCodeBench上，COMPAS将pass@1从45.9%提升至52.8%，且成本从36.57美元骤降至4.92美元，在SWE-bench上也取得了领先性能。"
conclusion: 通过综合考虑模型、提示词、解码设置与任务难度的协同效应，可以显著提升代码生成的效率与性价比。
---

## 摘要
代码生成系统通过模型、提示词和解码设置进行大语言模型（LLM）调用。然而，现有的优化方法通常仅调整这些选择的一部分，或对所有任务使用单一的固定配置：全局优化器为所有任务搜索一种配置，路由仅选择模型，而提示词优化器则保持模型和解码设置固定。这使得它们之间的联合、特定于组的交互作用尚不明确。因此，我们研究了这些选择如何相互作用，并观察到提示词与解码设置存在交互，调优效果因模型而异，且最佳配置随任务难度而变化。在这些观察的指导下，我们引入了 COMPAS（针对模型、提示词和解码设置的代码生成优化），这是一种难度感知的方法，通过低成本的模型选择和联合提示词-解码搜索来学习特定于组的质量-成本前沿，然后在无需进一步搜索的情况下，在线将每个测试任务路由到其匹配的前沿。在 LiveCodeBench 上相同的搜索预算下，COMPAS 将 pass@1 从最佳基准线的 45.9% 提升至 52.8%，同时将成本从 36.57 美元降低至 4.92 美元。该方法还可迁移至 SWE-bench 上的仓库级代码生成，解决了 76.0% 的任务，而最佳基准线为 70.0%。代码和可复现性构件可在 https://github.com/gjz78910/COMPAS 获取。

## Abstract
Code generation systems make each LLM call with a model, a prompt, and decoding settings. However, existing optimization methods usually tune only part of these choices or use one fixed configuration for all tasks: global optimizers search one configuration for all tasks, routers choose only a model, and prompt optimizers keep the model and decoding settings fixed. This leaves their joint, group-specific interactions unclear. We therefore examine how these choices interact and observe that prompts and decoding settings interact, tuning effects vary by model, and the best configuration varies by task difficulty. Guided by these observations, we introduce COMPAS (Code-generation Optimization over Models, Prompts, And Decoding Settings), a difficulty-aware method that learns group-specific quality-cost fronts through low-cost model selection and joint prompt-decoding search, then routes each test task to its matching front online without further search. Under a matched search budget on LiveCodeBench, COMPAS improves pass@1 from 45.9% for the best baseline to 52.8% while reducing cost from $36.57 to $4.92. This also transfers to repository-level code generation on SWE-bench, resolving 76.0% of tasks versus 70.0% for the best baseline. Code and the reproducibility artifact are available at https://github.com/gjz78910/COMPAS.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **COMPAS** 的框架，旨在通过联合优化模型选择、提示词（Prompt）和解码设置（Decoding Settings），提升代码生成的质量并降低成本。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在代码生成任务中，调用大语言模型（LLM）涉及三个关键变量：模型选择、提示词工程和解码参数（如温度、top-p等）。
*   **研究动机**：
    *   **现有方法局限性**：目前的优化工具通常只关注单一维度（如只选模型、只优提示词或只调参数），忽略了三者之间的**联合交互作用**。
    *   **任务难度差异**：不同难度的代码任务对配置的需求不同。简单任务用廉价模型和简单提示词即可，而困难任务则需要更强的模型和复杂的搜索策略。
    *   **搜索空间巨大**：联合搜索所有变量会导致组合爆炸，直接暴力搜索在经济和时间上不可行。

### 2. 方法论（COMPAS 框架）
COMPAS 采用“离线搜索、在线路由”的策略，核心流程如下：
*   **任务分组（Difficulty-Aware Grouping）**：根据基准测试提供的难度标签（如 Easy, Medium, Hard）将任务分组。
*   **离线搜索阶段（两阶段）**：
    1.  **快速模型选择**：使用极小的预算（Probe）为每个难度组筛选出性价比最高的模型（通常在同系列的大小模型中选择）。
    2.  **联合提示词-解码调优**：在选定模型的基础上，利用 LLM 反思（Reflection）机制，同时变异提示词和解码设置。通过维护一个**帕累托前沿（Pareto Front）**，记录不同质量-成本权衡下的最优配置组合。
*   **在线路由阶段**：
    *   当新任务到来时，系统根据其难度标签，直接从离线学习到的对应难度组的帕累托前沿中选择一个最优配置进行调用，无需实时搜索。

### 3. 实验设计
*   **数据集/场景**：
    *   **LiveCodeBench (LCB)**：用于函数级代码生成，包含不同时间发布的版本以防止数据污染。
    *   **SWE-bench (Verified-mini)**：用于仓库级软件工程任务，评估智能体（Agent）解决实际 GitHub Issue 的能力。
*   **对比基准（Baselines）**：
    *   **Default**：固定使用最强模型和默认配置。
    *   **RouteLLM**：主流的模型路由方法。
    *   **EcoTune**：针对解码参数的超参数优化方法。
    *   **PromiseTune**：基于因果分析的配置调优方法。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号和数量，因为实验主要基于 API 调用（如 DeepSeek, Mistral, Qwen 系列）。
*   **预算控制**：实验通过**Token 预算**来公平对比。在 LiveCodeBench 上限制为 1M Tokens，在 SWE-bench 上限制为 4M Tokens。
*   **耗时**：COMPAS 在 LCB 上的离线搜索耗时约 9.9 小时（API 响应时间），显著低于 EcoTune 等需要大量采样的策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LCB 上进行了 5 个独立随机种子的重复实验，并使用了 Scott–Knott ESD 统计检验来验证显著性。
    *   进行了**消融实验**（6 种变体），验证了提示词搜索、解码搜索、任务分组等组件的必要性。
    *   进行了**敏感性分析**，测试了不同预算、不同难度预测器（如 XGBoost）对结果的影响。
*   **充分性评价**：实验覆盖了从函数级到仓库级的任务，跨越了多个模型家族（DeepSeek, Devstral, Qwen），实验设计较为客观、公平，且考虑了统计显著性。

### 6. 主要结论与发现
*   **性能提升**：在 LiveCodeBench 上，COMPAS 将 Pass@1 准确率从 45.9% 提升至 **52.8%**。
*   **成本大幅降低**：在提升性能的同时，将成本从 36.57 美元削减至 **4.92 美元**（降低了约 7.4 倍）。
*   **交互效应**：实验证实提示词和解码设置之间存在强烈的交互作用，联合搜索的效果优于单独搜索。
*   **难度敏感性**：任务分组是性能提升的关键因素，针对不同难度定制配置比使用单一全局最优配置更有效。

### 7. 优点
*   **多维度协同**：首次系统性地联合优化模型、提示词和解码参数。
*   **高性价比**：通过离线帕累托前沿搜索，实现了在线推理时的“零额外开销”优化。
*   **泛化性强**：不仅适用于简单的代码补全，也适用于复杂的软件工程智能体（SWE-agent）。
*   **实用性**：提供了处理难度标签缺失的方案（使用 XGBoost 预测难度），增加了落地可能性。

### 8. 不足与局限
*   **模型家族限制**：实验主要在同家族模型（如 DeepSeek-V4-Flash 和 Pro）之间选择，尚未充分探讨跨厂商模型（如 GPT-4 与 Claude 3）的联合搜索表现。
*   **依赖难度标签**：虽然测试了难度预测器，但其性能仍略低于使用官方标签的效果，在完全未知的真实场景中，难度评估的准确性会成为瓶颈。
*   **搜索空间仍有扩展余地**：目前仅涵盖了 3 个解码参数，未涉及更复杂的采样策略（如多路径采样或思维链长度控制）。

（完）
