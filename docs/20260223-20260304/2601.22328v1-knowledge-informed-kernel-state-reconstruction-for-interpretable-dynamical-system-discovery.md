---
title: Knowledge-Informed Kernel State Reconstruction for Interpretable Dynamical System Discovery
title_zh: 融合知识的核状态重构用于可解释动力系统发现
authors: "Luca Muscarnera, Silas Ruhrberg Estévez, Samuel Holt, Evgeny Saveliev, Mihaela van der Schaar"
date: 2026-01-29
pdf: "https://arxiv.org/pdf/2601.22328v1"
tags: ["query:sr"]
score: 9.0
evidence: 将守恒定律等结构和语义先验引入符号发现
tldr: 针对从噪声和部分观测数据中发现动力系统控制方程的难题，本文提出MAAT框架。该框架利用知识感知的核状态重构技术，在再生核希尔伯特空间中结合物理先验（如守恒律、非负性）进行状态估计。MAAT能处理异构采样数据，生成物理一致的平滑轨迹及其解析导数，为符号回归提供可靠接口。在12个科学基准测试中，其状态估计精度显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的动力系统发现方法在面对高噪声、部分观测或黑盒模型时，难以保证物理机制的可解释性和重构的准确性。
method: 提出MAAT框架，通过在再生核希尔伯特空间中整合非负性、守恒律等物理先验，实现对异构采样数据的平滑状态重构。
result: 在12个科学基准测试和多种噪声环境下，MAAT显著降低了用于符号回归的轨迹及导数估计的均方误差。
conclusion: MAAT为碎片化传感器数据与符号回归之间建立了物理一致的接口，有效提升了复杂动力系统发现的鲁棒性与可解释性。
---

## 摘要
从数据中恢复控制方程是科学发现的核心，然而现有方法在噪声、部分观测下往往失效，或者依赖于掩盖物理机制的黑盒潜动力学。我们提出了 MAAT（Model Aware Approximation of Trajectories，模型感知轨迹近似），这是一个基于知识告知核状态重构的符号发现框架。MAAT 在再生核希尔伯特空间中构建状态重构，并将结构和语义先验（如非负性、守恒律和特定领域的观测模型）直接纳入重构目标，同时能够处理异构采样和测量粒度。这产生了具有解析时间导数的平滑且物理一致的状态估计，为碎片化的传感器数据与符号回归之间提供了一个原则性的接口。在十二个不同的科学基准和多种噪声环境下，相比于强基准模型，MAAT 显著降低了下游符号回归所使用的轨迹和导数的状态估计均方误差（MSE）。

## Abstract
Recovering governing equations from data is central to scientific discovery, yet existing methods often break down under noisy, partial observations, or rely on black-box latent dynamics that obscure mechanism. We introduce MAAT (Model Aware Approximation of Trajectories), a framework for symbolic discovery built on knowledge-informed Kernel State Reconstruction. MAAT formulates state reconstruction in a reproducing kernel Hilbert space and directly incorporates structural and semantic priors such as non-negativity, conservation laws, and domain-specific observation models into the reconstruction objective, while accommodating heterogeneous sampling and measurement granularity. This yields smooth, physically consistent state estimates with analytic time derivatives, providing a principled interface between fragmented sensor data and symbolic regression. Across twelve diverse scientific benchmarks and multiple noise regimes, MAAT substantially reduces state-estimation MSE for trajectories and derivatives used by downstream symbolic regression relative to strong baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Knowledge-Informed Kernel State Reconstruction for Interpretable Dynamical System Discovery》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从现实世界中**高噪声、稀疏采样且仅能部分观测**的数据中，准确地恢复出动力系统的控制方程（如常微分方程 ODE）。
*   **研究背景**：
    *   科学发现依赖于可解释的数学方程，但现有的符号发现方法（如 SINDy）对噪声极其敏感，且通常假设所有状态变量均可直接观测。
    *   现有的预处理方法存在两极分化：统计平滑器（如样条曲线）忽略了物理约束；黑盒模型（如 Neural ODEs）虽然鲁棒但缺乏透明度，无法直接转化为符号方程。
*   **研究动机**：建立一个物理一致的“接口”，将碎片化的传感器数据转化为高质量、带解析导数的平滑轨迹，从而支持下游的符号回归。

### 2. 方法论（MAAT 框架）
*   **核心思想**：提出 **MAAT (Model Aware Approximation of Trajectories)**，将状态重构定义在**再生核希尔伯特空间 (RKHS)** 中，通过最小化一个融合了数据拟合、平滑度和物理先验的正则化风险函数来实现。
*   **关键技术细节**：
    *   **核状态重构 (KSR)**：利用高斯核等平滑核函数对轨迹建模。由于核函数是解析可微的，可以直接获得状态的时间导数 $\dot{x}$，避免了数值微分带来的噪声放大。
    *   **异构观测融合**：显式引入观测算子 $H$，能够同时处理“稀疏但直接”的测量（锚点）和“密集但聚合”的测量（如多个变量的总和）。
    *   **知识注入 (Knowledge-Informed)**：在优化目标中直接加入语义先验（如变量非负性）和结构先验（如质量守恒、单调性约束）。
    *   **优化流程**：
        1.  在 RKHS 中求解系数矩阵 $U$，平衡数据保真度与物理约束。
        2.  生成平滑轨迹 $\hat{x}(t)$ 及其解析导数 $\dot{\hat{x}}(t)$。
        3.  将重构数据输入符号回归后端（如 SINDy 或 PySR）发现方程。

### 3. 实验设计
*   **数据集/场景**：涵盖 12 个科学基准，包括流行病学（SEIR/SEIRH）、药理学（葡萄糖-胰岛素模型、肿瘤生长、EPO 动力学）、生物系统（中性粒细胞生命周期）和经济学模型。
*   **噪声环境**：测试了三种噪声机制：各向同性高斯噪声、相关高斯噪声、重尾 Student-t 噪声。
*   **对比方法 (Benchmarks)**：
    *   数值平滑：三次样条 (Cubic Spline)、Savitzky-Golay 滤波、TVRegDiff。
    *   概率/滤波：高斯过程 (GP)、卡尔曼滤波 (RTS Smoother)。
    *   深度学习：Neural ODE、PINNs、Symbolic Neural ODE。
*   **评估指标**：状态估计的均方误差 (MSE) 以及下游符号方程恢复的准确性。

### 4. 资源与算力
*   **硬件环境**：使用了 **AMD Ryzen Threadripper PRO 3995WX (64核)** CPU 和 **NVIDIA RTX A4000 (16GB)** GPU。
*   **计算效率**：MAAT 避免了 $O(N^3)$ 的直接矩阵求逆，采用基于 Adam 的一阶优化，计算耗时（约 44.7 秒）远低于 Neural ODE（约 630 秒），但高于简单的样条插值。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个不同的动力系统上进行了测试，每个实验运行 10 个随机种子并提供 95% 置信区间。
*   **消融实验**：专门验证了非负性约束、导数惩罚以及结构化先验对重构质量的影响。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了多种噪声类型，还跨越了多个学科领域，证明了方法在不同观测算子 $H$ 下的通用性。

### 6. 主要结论与发现
*   **重构精度**：在所有噪声环境下，MAAT 的状态估计 MSE 显著低于强基准方法（通常低 1-2 个数量级）。
*   **导数鲁棒性**：相比数值微分，MAAT 提供的解析导数在极高噪声下仍能保持稳定，这是成功进行符号回归的关键。
*   **先验价值**：引入简单的物理约束（如非负性）能显著提升稀疏数据下的重构质量，使原本无法识别的方程变得可识别。
*   **符号发现**：MAAT 作为预处理前端，显著提升了 SINDy 和 PySR 恢复正确方程结构的能力。

### 7. 优点
*   **物理一致性**：通过正则化项强制执行物理定律，确保重构轨迹不违反基本常识（如浓度不为负）。
*   **处理异构数据**：能够优雅地融合不同频率、不同观测维度的传感器数据。
*   **解析性**：利用核方法的特性获得闭式导数，解决了动力系统发现中最核心的“导数估计”难题。
*   **数学完备性**：提供了关于复合损失函数作为 L2 误差替代方案的理论证明。

### 8. 不足与局限
*   **先验依赖**：性能提升高度依赖于注入先验的准确性；若先验设定错误，可能会引入偏差。
*   **超参数敏感性**：虽然文中提到对核长度缩放有自适应调整，但在极端情况下，正则化权重的选择仍需专家经验。
*   **扩展性限制**：尽管比 Neural ODE 快，但对于超大规模数据集（$N$ 极大的情况），核矩阵的存储和计算开销（$O(N^2)$）仍可能成为瓶颈。
*   **结构已知性**：目前假设观测算子 $H$ 和系统的分层结构是预先已知的，但在某些完全未知的系统中，这可能难以实现。

（完）
