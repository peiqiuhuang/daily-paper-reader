---
title: Compiling Deterministic Structure into SLM Harnesses
title_zh: 将确定性结构编译进 SLM 治理框架
authors: "Zan Kai Chong, Hiroyuki Ohsaki, Bryan Ng"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17450v1"
tags: ["query:bopl"]
score: 8.0
evidence: 通过语义梯度迭代优化系统提示
tldr: 本研究针对企业部署小语言模型（SLM）时推理能力不足及大模型成本高昂的问题，提出了语义梯度下降（SGDe）框架。该框架利用大模型作为教师，通过自然语言批评引导SLM编译生成包含DAG拓扑、系统提示词和确定性代码的执行计划。SGDe在PAC学习框架下优化了任务分发，显著提升了SLM在复杂推理任务上的表现，为SLM的工程化应用提供了高效的自动化优化方案。
selection_source: fresh_fetch
motivation: 旨在解决小语言模型推理能力有限且大模型部署成本高、数据隐私受限的挑战。
method: 提出SGDe框架，利用教师模型生成的自然语言批评作为梯度，迭代优化SLM的工作流拓扑、提示词及确定性代码。
result: "在GSM-Hard衍生测试集上，该方法使SLM准确率达到91.3%至99.3%，较现有提示词优化器提升了26.3%至34.3%。"
conclusion: SGDe通过将确定性结构编译进SLM框架，实现了复杂推理任务的高效分发与自动化优化，显著增强了小模型的实用性。
---

## 摘要
企业级小语言模型（SLM）的部署受限于认知不对称性：SLM 无法自我修正推理错误，而前沿大语言模型（LLM）成本极高，且在大规模使用时面临数据主权限制。我们提出了语义梯度下降（SGDe），这是一种教师-学生框架，可将智能体工作流编译为由 DAG 拓扑、系统提示词和确定性可执行代码组成的离散执行计划。结尾的“e”将 SGDe 与随机梯度下降（SGD）区分开来。SGDe 在离散语义空间中运行，由前沿教师模型生成自然语言批评作为方向性梯度，以迭代优化 SLM 的工作流构件。我们在 PAC 学习框架内对 SGDe 进行了形式化，建立了样本复杂度界限，通过利用教师模型作为统计先验，使其在目标合成任务上仅需三个训练样本即可收敛。在通过对抗性合成构建的 GSM-Hard 衍生测试集上，编译后的工作流在 Corollary 1 启发的 small-m 机制下，m=5 时达到 91.3% 的准确率，m=3 时达到 99.3%，相比最先进的提示词优化器实现了 +26.3% 至 +34.3% 的绝对提升。在治理框架工程（harness engineering）这一新兴范式中，SGDe 将确定性代码的放置（即哪些子任务委托给 Python 运行时，哪些保留为 LLM 调用）视为追踪驱动的逐节点优化目标，推广了 PAL 和 PoT 的全问题卸载模式。教师模型编译出两种互补的确定性结构：能力卸载（当 SLM 无法可靠执行子任务时将其委托给 Python）和结构性共识（将方差受限的推理步骤封装在通过确定性投票聚合的扇出/扇入子图中）。

## Abstract
Enterprise deployment of small language models (SLMs) is constrained by epistemic asymmetry: SLMs cannot self-correct reasoning errors, while frontier LLMs are prohibitively costly and face data sovereignty limits for high-volume use. We propose Semantic Gradient Descent (SGDe), a teacher-student framework that compiles agentic workflows into discrete execution plans comprising DAG topologies, system prompts, and deterministic executable code. The trailing "e" distinguishes SGDe from stochastic gradient descent. SGDe operates in a discrete semantic space where a frontier teacher generates natural-language critiques acting as directional gradients to iteratively refine the SLM's workflow artefacts. We formalise SGDe within a PAC learning framework, establishing sample-complexity bounds that enable convergence with as few as three training examples on targeted synthetic tasks by leveraging the teacher as a statistical prior. On a GSM-Hard-derived test set built via adversarial synthesis, compiled workflows reach 91.3% accuracy at m=5 and 99.3% at m=3 within the small-m regime motivated by Corollary 1, a +26.3% to +34.3% absolute improvement over state-of-the-art prompt optimisers. In the emerging paradigm of harness engineering, SGDe treats placement of deterministic code (which subtasks to delegate to a Python runtime versus retain as LLM calls) as a trace-driven, per-node optimisation target, generalising the whole-problem offloading of PAL and PoT. The teacher compiles two complementary deterministic structures: capability offloading, which delegates subtasks to Python when the SLM cannot execute them reliably, and structural consensus, which wraps variance-limited reasoning steps in fan-out/fan-in subgraphs aggregated by deterministic voting.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SGDe（Semantic Gradient Descent，语义梯度下降）** 的框架，旨在通过将确定性结构（如 Python 代码和投票机制）编译进小语言模型（SLM）的治理框架（Harness）中，解决 SLM 在复杂推理任务中的局限性。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：企业在部署 AI 时面临“认知不对称”难题。前沿大模型（LLM）推理能力强但成本高昂且存在数据隐私风险；小模型（SLM）成本低、可本地部署，但缺乏自我修正能力，容易产生幻觉且逻辑推理（尤其是数学运算）不可靠。
*   **研究动机**：现有的蒸馏技术或提示词优化工具（如 DSPy）仅优化文本或权重，无法改变工作流的底层结构。论文提出通过“治理框架工程（Harness Engineering）”，自动将任务中的确定性部分（如计算）交给代码，将不确定部分交给模型，从而提升 SLM 的表现。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用“教师-学生”架构。由高能力的大模型（教师）作为优化器，分析 SLM（学生）的执行轨迹，生成自然语言形式的“语义梯度”（批评），并据此迭代修改工作流的 DAG 拓扑、提示词和可执行代码。
*   **关键技术细节**：
    *   **能力卸载（Capability Offloading）**：教师模型识别出 SLM 无法胜任的子任务（如复杂算术），将其重写为确定性的 Python 代码节点。
    *   **结构性共识（Structural Consensus）**：对于 SLM 能够处理但波动较大的推理步骤，教师会构建“扇出/扇入”结构，即并行运行多个带扰动提示词的 SLM 实例，并通过确定性投票节点汇总结果。
    *   **粒度调整（Granularity Adjustment）**：根据认知负荷自动拆分复杂节点或合并冗余节点。
    *   **PAC 学习框架**：在理论上证明了该方法的样本复杂度。通过将搜索空间限制在有限的子任务类型中，使得模型在极小样本（m=3）下即可收敛。

### 3. 实验设计
*   **数据集**：基于 **GSM-Hard**（数学推理任务）通过对抗性合成生成的测试集，专门针对 SLM 容易失败的场景。
*   **Benchmark（对比方法）**：
    *   **Baseline**：Zero-shot SLM（直接调用模型）。
    *   **DSPy**：目前最先进的提示词优化框架（属于“子系统内优化”，不改变拓扑结构）。
*   **实验对象**：学生模型为 **Qwen-2.5-1.5B**（量化版），教师模型为 **Kimi 2.5**（通过 API 调用）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **资源消耗特征**：强调了“离线优化、在线推理”的模式。优化阶段使用昂贵的教师模型 API，但一旦编译完成，生产环境仅需运行极小规模的 SLM（1.5B 参数）和 Python 脚本，推理成本极低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同样本量（m=3, 5, 10）进行了多次迭代优化实验。
    *   每组配置运行了 **n=3** 次独立实验以获取均值和标准差。
    *   进行了**边界条件验证实验**，测试了在混合任务（结构不一致）下的表现。
*   **充分性评价**：实验设计较为科学，通过对比 DSPy 证明了“结构编译”优于单纯的“提示词优化”。引入 PAC 理论界限为小样本下的高准确率提供了理论支撑，实验结果与理论推导一致。

### 6. 主要结论与发现
*   **性能飞跃**：在 m=3 的极小样本下，SGDe 使 1.5B 参数的 SLM 达到了 **99.3%** 的准确率，相比 DSPy 提升了 **34.3%**。
*   **样本效率**：由于教师模型提供了强大的统计先验，SGDe 表现出极高的样本效率，仅需 3-5 个例子即可完成工作流编译。
*   **确定性的重要性**：成功的优化方案无一例外都结合了“代码卸载”和“共识子图”，证明了混合架构是 SLM 落地生产的关键。

### 7. 优点
*   **理论与工程结合**：不仅提出了工程框架，还利用 PAC 学习理论解释了为什么离散语义空间的优化是可行的。
*   **突破模型极限**：通过“卸载”思想，让小模型能完成其参数量级本无法完成的复杂任务。
*   **高度可解释性**：生成的执行计划（DAG + 代码 + 提示词）是完全透明且可调试的。

### 8. 不足与局限
*   **领域局限性**：目前主要在结构化推理（数学、逻辑）任务上验证，对于开放式创作或感性对话任务的适用性尚待研究。
*   **拓扑过拟合风险**：实验发现，如果训练集包含多种异构任务，单一的 DAG 拓扑会产生干扰（Topological Overfitting），需要引入更复杂的路由层。
*   **教师模型依赖**：优化过程高度依赖顶级 LLM 的批评能力，若教师模型水平不足，生成的语义梯度可能失效。

（完）
