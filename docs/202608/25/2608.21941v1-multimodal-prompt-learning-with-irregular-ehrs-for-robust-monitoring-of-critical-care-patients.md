---
title: Multimodal Prompt Learning with Irregular EHRs for Robust Monitoring of Critical Care Patients
title_zh: 基于不规则电子健康记录的多模态提示学习，用于重症监护患者的鲁棒监测
authors: "Yixin Yang, Yueyang Sun, Weichen Liu, Xianbing Zhao, Sicen Liu"
date: 2026-08-22
pdf: "https://arxiv.org/pdf/2608.21941v1"
tags: ["query:bopl"]
score: 8.0
evidence: 多模态提示学习框架
tldr: 针对重症监护室（ICU）中多模态电子健康档案（EHR）数据缺失导致的预测性能下降问题，本文提出了一种多模态提示学习框架。该框架通过引入生成式、缺失信号、缺失类型和时间提示，在统一架构中捕捉模态内依赖和跨模态交互。实验证明，该方法在多种缺失场景下均优于现有模型，显著提升了临床预测的鲁棒性。
selection_source: fresh_fetch
motivation: 现实临床场景中多模态EHR数据常存在部分或完全缺失，导致现有模型性能大幅下降。
method: 提出包含生成式、缺失信号、缺失类型和时间提示的多模态提示学习框架，以应对不同模态缺失组合。
result: 在两种缺失设置下的实验结果显示，该方法在各项评估指标上均优于现有先进方法。
conclusion: 该框架通过提示学习机制有效解决了不完整多模态EHR数据的临床预测难题，具有较强的鲁棒性。
---

## 摘要
对重症监护室（ICU）患者进行准确评估，对于及时的临床干预和改善患者预后至关重要。多模态电子健康记录（EHRs），包括结构化生理时间序列和纵向临床笔记，为重症监护预测提供了互补信息。然而，在现实世界的临床环境中，单个模态可能仅被部分观测或完全不可用，导致现有多模态模型的性能大幅下降。为了应对这一挑战，我们提出了一种多模态提示学习框架，用于在多种模态缺失场景下进行鲁棒的临床预测。该框架引入了四种互补类型的提示：生成式提示（generative prompts）、缺失信号提示（missing-signal prompts）、缺失类型提示（missing-type prompts）和时间提示（temporal prompts）。生成式提示为不可用模态构建替代潜表征，而缺失信号提示则将观测到的表征与生成的表征区分开来。缺失类型提示使模型能够适应不同的模态可用性配置，而时间提示则对经过时间编码的临床序列执行特定条件的聚合。这些提示共同使模型能够在统一的架构中捕获感知缺失的模态内依赖关系和跨模态交互。大量实验表明，在两种缺失设置下，我们的方法在各项评估指标上均优于现有方法。消融实验和鲁棒性分析进一步验证了四种提示类型的互补贡献，以及所提框架在处理不完整多模态 EHR 数据进行临床预测时的有效性。

## Abstract
Accurate assessment of patients in intensive care units (ICUs) is essential for timely clinical intervention and improved patient outcomes. Multimodal electronic health records (EHRs), including structured physiological time series and longitudinal clinical notes, provide complementary information for critical care prediction. However, in real-world clinical settings, individual modalities may be partially observed or entirely unavailable, resulting in substantial performance degradation for existing multimodal models. To address this challenge, we propose a multimodal prompt-learning framework for robust clinical prediction under diverse missing-modality scenarios. The proposed framework introduces four complementary types of prompts: generative prompts, missing-signal prompts, missing-type prompts, and temporal prompts. Generative prompts construct surrogate latent representations for unavailable modalities, while missing-signal prompts distinguish observed representations from generated ones. Missing-type prompts condition the model on different modality-availability configurations, whereas temporal prompts perform condition-specific aggregation over temporally encoded clinical sequences. Together, these prompts enable the model to capture missingness-aware intramodal dependencies and cross-modal interactions within a unified architecture. Extensive experiments demonstrate that our method outperforms existing approaches across evaluation metrics on two missingness settings. Ablation and robustness analyses further verify the complementary contributions of the four prompt types and the effectiveness of the proposed framework for clinical prediction from incomplete multimodal EHR data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multimodal Prompt Learning with Irregular EHRs for Robust Monitoring of Critical Care Patients》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**重症监护室（ICU）患者的实时监测与死亡风险预测**。
*   **背景**：ICU 产生的电子健康记录（EHR）包含两种关键模态：结构化的**多变量不规则采样时间序列（MISTS）**（如生命体征、实验室检查）和非结构化的**纵向临床笔记**（如医生诊断、病程记录）。
*   **核心问题**：在现实临床场景中，由于设备故障、记录延迟、隐私限制或工作流差异，多模态数据往往是不完整的（某一种模态完全缺失或部分缺失）。现有的多模态模型在面对模态缺失时性能会剧烈下降。
*   **研究目标**：开发一种能够处理不规则采样且对模态缺失具有鲁棒性的临床预测框架。

### 2. 论文提出的方法论
论文提出了一种基于**多模态提示学习（Multimodal Prompt Learning）**的统一框架，核心思想是通过四种功能互补的“提示（Prompts）”来弥补缺失信息并引导模型决策：

1.  **生成式提示（Generative Prompts, $P_G$）**：当某一模态缺失时，利用现有的另一模态作为 Key 和 Value，通过交叉注意力机制（Cross-Attention）和学习到的生成提示作为 Query，在潜空间中重建缺失模态的替代表示。
2.  **缺失信号提示（Missing-Signal Prompts, $P_S$）**：为每个模态的 Token 添加来源标记，明确区分该特征是“真实观测到的”还是“由提示生成的”，使后续融合模块能非对称地加权处理。
3.  **缺失类型提示（Missing-Type Prompts, $P_T$）**：提供全局的模态可用性状态信息（如“仅笔记可用”、“仅生理指标可用”或“全模态可用”），注入到 Transformer 的自注意力层中，调节跨模态交互。
4.  **时间提示（Temporal Prompts, $P_\tau$）**：针对不同的缺失场景，学习特定的时间聚合权重，从不规则的时间序列中提取对当前缺失状态最敏感的特征，用于最终的分类预测。

**算法流程**：输入数据 -> 模态特定编码器（UTDE 处理生理指标，Clinical-Longformer 处理文本） -> 缺失模态生成 -> 信号与类型提示注入 -> 跨模态融合 -> 时间提示聚合 -> 预测输出。

### 3. 实验设计
*   **数据集**：使用了两个大型公开基准数据集：**MIMIC-III** 和 **MIMIC-IV**。
*   **任务场景**：基于入院后前 48 小时的数据预测患者的**院内死亡率（48-IHM）**。
*   **Benchmark 与对比方法**：
    *   **单模态基准**：包括处理时间序列的 GRU-D、mTAND、RAINDROP、UTDE 等；处理文本的 T-LSTM、HierTrans、Flat 等。
    *   **多模态基准**：包括 MMIMIC、CTPD、MedFuse、DrFuse 等先进的多模态融合模型。
*   **评估指标**：AUPRC（主要指标，针对类别不平衡）、AUROC 和 F1 分数。

### 4. 资源与算力
*   **论文提及情况**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **已知细节**：提到了文本编码器使用了预训练的 **Clinical-Longformer**，潜空间维度 $d=128$，统一时间步长 $L=48$。由于涉及大型预训练语言模型的微调和多模态 Transformer 的训练，可以推断该实验需要中等规模的算力支持（如 NVIDIA A100 或 V100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个独立的大型数据集上进行了验证。
    *   涵盖了三种模态场景：仅 MISTS 可用、仅笔记可用、全模态可用。
    *   进行了详尽的**消融实验**，逐一验证了四种提示（$P_G, P_S, P_T, P_\tau$）的有效性。
    *   探讨了**训练时模态丢弃率（Dropout Rate）**对模型鲁棒性的影响。
    *   提供了 **t-SNE 可视化**和**个案研究（Case Study）**。
*   **充分性评价**：实验设计较为充分且客观。通过在不同缺失配置下与多种 SOTA 方法对比，证明了其优越性。个案研究展示了模型如何通过提示修正错误预测，增强了结果的可解释性。

### 6. 主要结论与发现
*   **性能提升**：在模态缺失的情况下，该方法在 AUPRC 指标上显著优于现有的单模态和多模态模型。
*   **鲁棒性**：即使在全模态可用的情况下，该模型依然保持了与最强多模态基准相当的性能，证明了提示学习不会损害完整数据的处理能力。
*   **模态不对称性**：实验发现从“临床笔记”重建“生理指标”比反向重建更稳定，这反映了临床笔记中蕴含了更丰富的患者状态描述。
*   **提示的协同作用**：消融实验显示，四种提示共同作用时效果最好，其中生成式提示对性能提升贡献最大。

### 7. 优点
*   **创新性**：首次将四种不同功能的提示学习引入到不规则 EHR 的多模态缺失处理中。
*   **处理不规则性**：结合了时间感知编码器（UTDE/mTAND），有效解决了医疗数据采样频率不一致的问题。
*   **端到端统一**：无需复杂的预训练或多阶段对齐，在统一的 Transformer 架构下实现了缺失补偿与分类预测。

### 8. 不足与局限
*   **任务单一性**：实验仅集中在死亡率预测这一二分类任务上，未验证在多任务（如并发症预测、住院时长预测）上的泛化能力。
*   **模态局限**：目前仅考虑了生理指标和文本笔记，未涉及医学影像（如 X 光、CT）等其他重要临床模态。
*   **生成质量风险**：虽然生成式提示在潜空间重建了表征，但这种“想象”出来的特征在极端缺失情况下可能引入偏见或虚假信号，临床应用需谨慎。
*   **算力说明缺失**：缺乏训练效率和硬件需求的详细说明，不利于其他研究者评估部署成本。

（完）
