---
title: Derivative-Free Bilevel Optimization with Inexact Lower-Level Solutions
title_zh: 具有不精确下层解的无导数双层优化
authors: "Edoardo Cesaroni, Giampaolo Liuzzi, Stefano Lucidi"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20759v1"
tags: ["query:bopl"]
score: 9.0
evidence: 双层优化的无导数框架
tldr: 本研究针对一阶信息不可用或难以获取的双层优化问题，提出了一种无导数优化框架。该框架支持变量边界及一般非线性约束，其核心在于对下层问题采用逐步精细化的自适应精度求解策略。通过理论分析，证明了该方法在不同约束条件下对Clarke-Jahn或Goldstein平稳点的收敛性。实验表明，相比固定精度方法，该策略在处理复杂约束和高精度要求时表现更优。
selection_source: fresh_fetch
motivation: 旨在解决双层优化中一阶导数信息缺失或难以获取，且下层问题求解精度对整体收敛性影响显著的问题。
method: 提出一种无导数框架，通过逐步提升下层问题的求解精度，并结合精确惩罚函数处理复杂的非线性约束。
result: 在160个BOLIB基准问题上的测试证明，自适应精度策略在性能上始终优于固定精度求解方法。
conclusion: 该框架为缺乏导数信息的双层优化提供了有效的理论支撑和数值解法，特别是在高精度需求场景下具有显著优势。
---

## 摘要
在这项工作中，我们提出了一种用于双层优化的无导数框架。我们考虑了变量具有边界约束以及一般非线性约束的上层和下层问题，并假设（上层）一阶信息不可用或难以获取。下层问题的求解精度在整个优化过程中逐步细化。我们首先分析了上层问题仅受边界约束的情况，并证明了当细化过程达到最大精度时，算法收敛至 Clarke-Jahn 驻点。当对该细化过程施加限制时，我们利用扩展的 Goldstein 驻点概念证明了算法收敛至近似驻点。最后，我们通过精确罚函数方法扩展了所提框架以处理更复杂的约束，并在适当的假设下证明了其对驻点的收敛性。对来自 BOLIB 集合的 160 个问题进行的全面数值研究表明，自适应精度策略始终优于固定精度求解，且随着所需下层精度要求的提高，其优势变得更加显著。

## Abstract
In this work, we propose derivative-free framework for bilevel optimization. We consider both the upper and lower-level problems with bound constraints on the variables, as well as general nonlinear constraints, assuming that first-order information (in the upper-level) is not available or it is impractical to obtain. The lower-level problem is solved with an accuracy that is progressively refined throughout the optimization process. We first analyze the case in which the upper-level problem is subject only to bound constraints, establishing convergence to Clarke-Jahn stationary points when the refinement process is allowed to reach its maximum precision. When a limitation is imposed on this refinement process, we prove convergence to approximate stationary points using an extended notion of Goldstein stationarity. Finally, we extend the proposed framework to handle more complex constraints via an exact penalty function approach, proving convergence to stationary points under suitable assumptions. A comprehensive numerical study on 160 problems from the BOLIB collection shows that the adaptive accuracy strategy consistently yields better results than fixed-precision solves, with its benefits becoming more pronounced as the required lower-level accuracy becomes more stringent.

---

## 论文详细总结（自动生成）

这篇论文由罗马第一大学的研究人员撰写，提出了一种处理双层优化（Bilevel Optimization）问题的无导数算法框架。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
双层优化涉及两个层次的决策：上层（领导者）和下层（跟随者）。在许多实际应用中，上层问题的目标函数或约束条件往往是“黑箱”形式，即无法获得其一阶导数信息（梯度），或者获取成本极高。
*   **核心挑战**：下层问题的精确解通常难以获得，且在优化初期追求高精度下层解会造成巨大的计算浪费。
*   **研究动机**：开发一种不需要导数信息，且能通过**自适应调整下层求解精度**来平衡计算效率与收敛质量的通用框架。

### 2. 论文提出的方法论
论文提出了 **DFN-LLA**（Derivative-Free for Nonsmooth bilevel with Lower-Level Adaptive accuracy）算法：
*   **核心思想**：将下层问题的求解容差 $\zeta$ 与上层的搜索步长 $\alpha$ 动态耦合。当上层步长较大（处于全局搜索阶段）时，允许下层解有较大的误差；随着步长缩小（接近驻点），逐步提高下层求解精度。
*   **关键技术细节**：
    *   **单层转化**：利用下层问题的最优反应映射，将双层问题转化为含扰动的单层非光滑优化问题。
    *   **投影外推法（Projected Extrapolation）**：在搜索方向上寻找满足充分下降条件的步长，若成功则尝试扩大步长，若失败则缩小步长并触发下层精度更新。
    *   **精确惩罚函数**：针对上层的一般非线性约束，引入精确惩罚项，将约束问题转化为边界约束问题处理。
    *   **收敛性理论**：证明了当最终精度趋于零时，算法收敛至 Clarke-Jahn 驻点；当最终精度固定为正数时，收敛至近似的 Goldstein 驻点。

### 3. 实验设计
*   **数据集**：从 **BOLIB**（Bilevel Optimization LIBrary）测试库中选取了 **160 个**小规模双层优化问题。
*   **问题类型**：涵盖无约束、边界约束和一般非线性约束，变量总数（$n_x + n_y$）从 2 到 21 不等。
*   **对比方法（变体实验）**：
    1.  **MS-DFN-LLA**：多步长变体（为每个方向维护独立步长）。
    2.  **MS-DFN-NL-LLA**：无外推阶段的变体。
    3.  **MS-DFN-LLF**：**固定精度**变体（下层始终使用最高精度求解）。
*   **评价指标**：采用改进的性能剖面图（Performance Profiles）和数据剖面图（Data Profiles），以**下层函数调用次数**作为主要计算成本衡量标准。

### 4. 资源与算力
*   **硬件环境**：服务器配备两颗 Intel Xeon Gold 6252N CPU @ 2.30GHz，512 GB RAM。
*   **软件环境**：Python 3.12.3，使用 NumPy 进行数值计算，下层问题调用 **Uno** 求解器。
*   **算力消耗**：未明确单次训练时长，但设定了每个问题的截止时间为 9000 秒或上层函数评估上限 1500 次。实验不涉及 GPU 加速。

### 5. 实验数量与充分性
*   **实验规模**：使用了 160 个问题，这在双层优化领域属于较大规模的基准测试。
*   **充分性**：实验设计非常充分。作者不仅对比了自适应精度与固定精度的差异，还通过消融实验验证了“多步长策略”和“外推阶段”对算法性能的贡献。
*   **客观性**：通过引入 KKT 残差作为有效解的判定标准，确保了不同精度设置下的对比是公平且符合双层优化定义的。

### 6. 论文的主要结论与发现
*   **自适应精度的优越性**：自适应精度策略（LLA）在所有精度要求下均优于固定精度策略（LLF）。在要求极高精度（如 $10^{-9}$）时，LLF 往往因初期计算量过大而失败，而 LLA 仍能稳健收敛。
*   **计算效率提升**：以一个 72 维的控制问题为例，LLA 相比 LLF 减少了约 50% 的下层函数调用次数，并显著缩短了 CPU 时间。
*   **组件有效性**：多步长策略能更好地捕捉目标函数的局部几何特征；外推阶段有助于保持较大的搜索步长，从而延迟进入高精度的昂贵计算阶段。

### 7. 优点
*   **理论完备**：为非光滑、无导数环境下的双层优化提供了严谨的收敛性证明，特别是引入了 Goldstein 驻点概念来描述不精确解的情况。
*   **实用性强**：自适应精度逻辑简单有效，能显著降低求解黑箱双层问题的计算门槛。
*   **基准贡献**：扩展了性能剖面图方法，使其适用于双层优化这一特殊领域，为后续研究提供了评估标准。

### 8. 不足与局限
*   **规模限制**：测试问题主要集中在小规模（变量数 < 30），对于超大规模变量的双层优化问题，无导数方法的搜索效率可能会面临“维度灾难”。
*   **下层求解器依赖**：算法假设存在一个可靠的确定性下层求解器。如果下层问题本身极度非凸或存在多个局部最优，算法的性能可能受限。
*   **参数敏感性**：虽然论文给出了推荐参数，但惩罚参数 $\rho$ 和步长缩减因子 $\theta$ 等可能仍需针对特定复杂问题进行微调。

（完）
