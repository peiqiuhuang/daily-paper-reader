---
title: Optimal Control-Based Falsification of Learnt Dynamics via Neural ODEs and Symbolic Regression
title_zh: 基于最优控制的、通过神经常微分方程和符号回归实现的学习动力学证伪
authors: "Lasse Kötz, Jonas Sjöberg, Knut Åkesson"
date: 2026-01-18
pdf: "https://arxiv.org/pdf/2602.00031v1"
tags: ["query:sr"]
score: 9.0
evidence: 通过符号回归将神经ODE转换为解析形式
tldr: 本研究提出一种集成学习代理动力学与最优控制的证伪框架，用于高效生成网络物理系统的反例。该方法利用神经常微分方程识别动力学，并通过符号回归将其转化为解析形式，从而实现快速的轨迹优化。通过最小化STL鲁棒性寻找反例，并利用实验数据迭代改进模型。在ARCH-COMP 2024基准测试中，该方法显著减少了实验次数，提升了复杂系统验证的效率。
selection_source: fresh_fetch
motivation: 针对网络物理系统在STL规范下的证伪问题，解决传统优化方法实验成本高且缺乏系统动力学建模的挑战。
method: 结合神经常微分方程与符号回归构建解析代理模型，并将其转化为最优控制问题以最小化STL鲁棒性。
result: 在ARCH-COMP 2024基准测试中，该方法比不建模动力学的传统方法减少了数个数量级的系统实验次数。
conclusion: 通过集成学习模型与轨迹优化，该框架为复杂系统的安全性验证提供了一种高效、可解释且数据驱动的新方案。
---

## 摘要
我们提出了一种证伪框架，该框架将学习到的代理动力学与最优控制相结合，以高效地为信号时序逻辑（STL）规范下的信息物理系统（CPS）生成反例。未知的系统动力学通过神经常微分方程（Neural ODEs）进行识别，同时将已知的先验结构直接嵌入模型中，从而降低了数据需求。学习到的神经常微分方程通过符号回归转换为解析形式，实现了快速且可解释的轨迹优化。证伪过程被建模为在输入轨迹上最小化 STL 鲁棒性；负鲁棒性产生候选反例，并在原始系统上进行验证。虚假轨迹被迭代地用于改进代理模型，而真实的反例则作为最终结果返回。在 ARCH-COMP 2024 基准测试上的实验表明，与不建模系统动力学的基于优化的方法相比，该方法对被测系统的实验次数需求降低了几个数量级。

## Abstract
We present a falsification framework that integrates learned surrogate dynamics with optimal control to efficiently generate counterexamples for cyber-physical systems specified in signal temporal logic (STL). The unknown system dynamics are identified using neural ODEs, while known a-priori structure is embedded directly into the model, reducing data requirements. The learned neural ODE is converted into an analytical form via symbolic regression, enabling fast and interpretable trajectory optimization. Falsification is cast as minimizing STL robustness over input trajectories; negative robustness yields candidate counterexamples, which are validated on the original system. Spurious traces are iteratively used to refine the surrogate, while true counterexamples are returned as final results. Experiments on ARCH-COMP 2024 benchmarks show that this method requires orders of magnitude fewer experiments of the system under test than optimization-based approaches that do not model system dynamics.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FalConN**（Falsification using optimal Control and Neural surrogates）的框架，旨在通过结合深度学习、符号回归和最优控制理论，高效地为网络物理系统（CPS）寻找违反安全规范的反例。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何减少在复杂网络物理系统（SUT）上进行安全验证（证伪）时的实验次数。
*   **研究动机**：
    *   **形式化验证局限性**：对于复杂的非线性系统，传统的模型检测或演绎验证往往不可行。
    *   **黑盒证伪效率低**：现有的黑盒证伪方法（如随机搜索、贝叶斯优化）通常不建模系统动力学，导致需要大量的系统实验（模拟或物理测试），成本极高。
    *   **缺乏梯度信息**：黑盒模型无法提供用于引导搜索的梯度，使得寻找极值（最不鲁棒的轨迹）变得困难。

### 2. 方法论：FalConN 框架
该方法的核心思想是“学习代理模型 -> 符号化提取 -> 最优控制搜索 -> 真实系统验证”的迭代循环。

*   **神经常微分方程 (Neural ODEs)**：
    *   使用 Neural ODE 学习系统的动力学 $\dot{x} = f(x, u, t)$。
    *   **关键点**：支持嵌入先验物理知识（已知动力学部分），仅让神经网络学习未知部分，从而提高数据效率。
    *   使用伴随灵敏度方法（Adjoint Sensitivity Method）高效训练。
*   **符号回归 (Symbolic Regression, SR)**：
    *   将训练好的 Neural ODE “蒸馏”为解析数学表达式。
    *   **目的**：Neural ODE 虽然精确但计算密集且难以直接求导；符号表达式提供了**解析梯度**，极大地方便了后续的非线性优化。
*   **最优控制问题 (OCP) 建模**：
    *   将证伪任务转化为最小化信号时序逻辑（STL）鲁棒性值的优化问题。
    *   使用 **Log-Sum-Exponential (LSE)** 平滑近似 STL 的 `min/max` 算子，使其完全可微。
    *   利用 **SNOPT**（稀疏非线性优化器）求解最优输入序列 $u(t)$。
*   **迭代验证**：
    *   在代理模型上找到的候选反例会在真实 SUT 上运行。
    *   如果是真实反例，则任务完成；如果是“虚假反例”（代理模型不准），则将该轨迹加入数据集重新训练代理模型。

### 3. 实验设计
*   **实验场景**：使用 ARCH-COMP 2024 竞赛中的基准测试——**神经网络控制的磁悬浮系统 (Levitating Magnet)**。
*   **规范 (Specifications)**：测试了三个 STL 规范（NN, NN $\beta=0.04$, NNx），涉及设定点跟踪、收敛行为和结算过程。
*   **对比方法**：与 8 种最先进的工具进行对比，包括 ARIsTEO, ATheNA, EXAM-Net, ForeSee, FReaK, Moonlight, OD, $\Psi$-TaLiRo。
*   **评估指标**：证伪成功率 (FR)、平均实验次数 ($\bar{S}$)、中位数实验次数 ($\tilde{S}$)。

### 4. 资源与算力
*   **硬件环境**：第 13 代 Intel Core i7-13700H 处理器，32GB 内存。
*   **软件实现**：使用 Julia 语言开发，调用了 `SymbolicRegression.jl`、`Drake` (用于 OCP 建模) 和 `Breach` (用于鲁棒性评估)。
*   **训练细节**：Neural ODE 训练使用 ADAM 优化器（300 epoch）后接 L-BFGS 细化；符号回归运行 100 次迭代，种群大小为 50。
*   **算力说明**：文中未提及 GPU 使用，主要依赖 CPU 进行数值积分和符号搜索。

### 5. 实验数量与充分性
*   **实验规模**：针对每个规范独立运行 **10 次** 实验，以消除随机性影响。
*   **充分性**：实验涵盖了不同难度的 STL 规范（从简单的边界检查到复杂的三层嵌套时间算子）。
*   **客观性**：直接引用了 ARCH-COMP 2024 官方报告中的其他工具数据进行对比，确保了基准测试的公平性。

### 6. 主要结论与发现
*   **极高的数据效率**：在多个任务中，FalConN 仅需 **3 到 5 次** 真实系统实验即可找到反例，而传统方法往往需要几十甚至几百次。
*   **符号化的优势**：符号回归提取的解析模型在保持 Neural ODE 预测精度的同时，显著提升了优化求解器的收敛速度。
*   **鲁棒性**：即使在没有先验动力学知识的情况下，该框架依然能通过少量随机采样快速构建有效的代理模型。

### 7. 优点与亮点
*   **混合建模**：允许将物理定律与神经网络结合，减少了纯黑盒学习的盲目性。
*   **解析梯度**：通过符号回归解决了 Neural ODE 在大规模轨迹优化中计算梯度慢的问题。
*   **闭环迭代**：通过将虚假反例反馈给训练集，实现了代理模型的自动精化。

### 8. 不足与局限
*   **系统类型限制**：目前仅适用于连续动力学系统，尚未支持包含模式切换或离散状态的**混合系统 (Hybrid Systems)**。
*   **符号回归开销**：符号搜索过程本身可能比较耗时，且对于极高维度的状态空间，寻找合适的数学表达式可能变得困难。
*   **局部最优风险**：最优控制求解器（SNOPT）可能会陷入局部最优，导致无法找到某些极隐蔽的反例。
*   **超参数敏感性**：平滑参数 $k$ 和符号复杂度阈值需要根据具体问题进行调整。

（完）
