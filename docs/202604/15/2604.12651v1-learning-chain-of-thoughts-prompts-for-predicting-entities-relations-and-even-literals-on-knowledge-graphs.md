---
title: "Learning Chain Of Thoughts Prompts for Predicting Entities, Relations, and even Literals on Knowledge Graphs"
title_zh: 学习用于预测知识图谱中实体、关系乃至字面量的思维链提示
authors: "Alkid Baci, Luke Friedrichs, Caglar Demir, N'Dah Jean Kouagou, Axel-Cyrille Ngonga Ngomo"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12651v1"
tags: ["query:bopl"]
score: 9.0
evidence: 使用贝叶斯优化学习思维链提示
tldr: 针对知识图谱嵌入模型在处理未知实体、关系及字面量方面的局限性，本文提出RALP框架。该框架将链接预测重新定义为提示学习问题，利用贝叶斯优化在极少样本下学习思维链提示词作为评分函数。RALP不仅能预测缺失实体和关系，还能处理复杂的OWL逻辑推理任务，在多个基准测试中显著超越了现有模型，为动态异构图谱推理提供了灵活的新方案。
selection_source: fresh_fetch
motivation: 传统知识图谱嵌入模型难以应对动态图谱中的未知元素和字面量，而大语言模型具备强大的泛化与推理潜力。
method: 提出RALP框架，通过贝叶斯优化算法在少量样本上学习字符串形式的思维链提示词，并将其用于三元组评分与预测。
result: "RALP在多个基准数据集上将MRR提升了5%以上，并在复杂的OWL类表达式推理任务中实现了超过88%的Jaccard相似度。"
conclusion: 基于提示学习的LLM推理是传统嵌入方法的有效替代方案，能够更灵活地处理知识图谱中的复杂预测和逻辑推理任务。
---

## 摘要
知识图谱嵌入（KGE）模型在链接预测方面表现良好，但在处理未见实体、关系，尤其是字面量（literals）时面临挑战，这限制了它们在动态异构图中的应用。相比之下，预训练大语言模型（LLMs）通过提示（prompting）能够有效地进行泛化。我们将链接预测重新定义为提示学习问题，并引入了 RALP，它学习基于字符串的思维链（CoT）提示，作为三元组的评分函数。通过 MIPRO 算法利用贝叶斯优化，RALP 在无需梯度访问的情况下，仅需不到 30 个训练样本即可识别出有效的提示。在推理阶段，RALP 预测缺失的实体、关系或整个三元组，并根据学习到的提示分配置信度分数。我们在转导式（transductive）、数值型以及 OWL 实例检索基准上进行了评估。RALP 在各数据集上的平均倒数排名（MRR）比最先进的 KGE 模型提升了 5% 以上，并通过高质量的推断三元组增强了泛化能力。在具有复杂类表达式（例如 $\exists hasChild.Female$、$\geq 5 \; hasChild.Female$）的 OWL 推理任务中，它实现了超过 88% 的 Jaccard 相似度。这些结果凸显了基于提示的 LLM 推理是嵌入式方法的一种灵活替代方案。我们将实现代码、训练和评估流程开源：https://github.com/dice-group/RALP 。

## Abstract
Knowledge graph embedding (KGE) models perform well on link prediction but struggle with unseen entities, relations, and especially literals, limiting their use in dynamic, heterogeneous graphs. In contrast, pretrained large language models (LLMs) generalize effectively through prompting. We reformulate link prediction as a prompt learning problem and introduce RALP, which learns string-based chain-of-thought (CoT) prompts as scoring functions for triples. Using Bayesian Optimization through MIPRO algorithm, RALP identifies effective prompts from fewer than 30 training examples without gradient access. At inference, RALP predicts missing entities, relations or whole triples and assigns confidence scores based on the learned prompt. We evaluate on transductive, numerical, and OWL instance retrieval benchmarks. RALP improves state-of-the-art KGE models by over 5% MRR across datasets and enhances generalization via high-quality inferred triples. On OWL reasoning tasks with complex class expressions (e.g., $\exists hasChild.Female$, $\geq 5 \; hasChild.Female$), it achieves over 88% Jaccard similarity. These results highlight prompt-based LLM reasoning as a flexible alternative to embedding-based methods. We release our implementation, training, and evaluation pipeline as open source: https://github.com/dice-group/RALP .

---

## 论文详细总结（自动生成）

以下是对论文《Learning Chain Of Thoughts Prompts for Predicting Entities, Relations, and even Literals on Knowledge Graphs》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的知识图谱嵌入（KGE）模型（如 TransE, DistMult）依赖于将实体和关系映射为连续向量。这种方法在处理**未见实体（Out-of-Vocabulary）**、**数值型字面量（Literals）**以及**复杂逻辑推理**时表现较差。
*   **核心思想**：本文提出 **RALP** 框架，将链接预测任务从“学习连续向量评分函数”转变为“学习离散的字符串提示（Prompt）评分函数”。利用大语言模型（LLM）的上下文学习和推理能力，通过自动化提示优化技术，实现对实体、关系及数值的统一预测。

### 2. 方法论
*   **核心架构**：RALP 不再训练 Embedding，而是学习一个最优的思维链（CoT）提示词 $\beta$。
*   **关键技术细节**：
    *   **提示优化（MIPRO 算法）**：采用 DSPy 框架中的 MIPRO 算法，利用**贝叶斯优化**在离散的字符串空间内搜索最优提示词。这种方法无需梯度下降，仅需极少量（<30个）示例即可完成优化。
    *   **评分函数 $\phi_{\beta \le c}$**：将三元组 $(s, p, o)$ 转化为字符串输入 LLM，LLM 根据学习到的 CoT 提示输出推理过程及置信度分数。
    *   **数值预测**：针对数值字面量，RALP 引导 LLM 进行回归推理，输出点预测值及置信区间（Interval Range）。
    *   **OWL 推理**：将复杂的描述逻辑（DL）表达式转化为曼彻斯特语法（Manchester Syntax），利用 LLM 进行实例检索。
*   **算法流程**：
    1.  **初始化**：使用少量示例构建初始提示。
    2.  **迭代优化**：MIPRO 根据验证集上的表现（如 MRR）不断更新提示字符串。
    3.  **推理/增强**：使用优化后的提示进行预测，或通过 **Algorithm 1** 发现缺失三元组并反馈给传统 KGE 模型进行数据增强。

### 3. 实验设计
*   **数据集**：
    *   **Countries (S1, S2, S3)**：标准的转导式链接预测基准。
    *   **LitWD1K**：从 Wikidata 提取的包含数值字面量的数据集。
    *   **Father**：包含 130 个复杂 OWL 类表达式（ALCQHI 逻辑）的推理数据集。
*   **对比方法**：
    *   **传统 KGE 模型**：DistMult, Keci, DeCaL, MuRE, ComplEx, QMult, QuatE 等。
    *   **评估指标**：MRR, Hits@1/3/10（链接预测）；ICR, IW（数值区间预测）；Jaccard 相似度（OWL 推理）。

### 4. 资源与算力
*   **硬件设施**：使用 Ubuntu 服务器，配备 Intel Xeon Platinum 处理器和 **4 台 NVIDIA H100 80GB GPU**。
*   **模型设置**：本地部署了 **Qwen2.5-32B-Instruct**，通过 vLLM 引擎进行 4 卡张量并行推理。
*   **训练时长**：文中未明确给出具体的总训练时长，但强调了其高效性（仅需极少量的 LLM 调用即可完成提示优化）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个 Countries 子集上进行了完整的链接预测对比。
    *   对 7 种主流 KGE 模型进行了 RALP 增强前后的对比实验。
    *   对 10 种不同类型的数值属性进行了回归预测实验。
    *   对 130 个 OWL 概念表达式进行了 4 种不同配置（语法/命名空间）的消融研究。
*   **充分性评价**：实验设计较为全面，涵盖了从简单链接预测到复杂逻辑推理的多个维度。通过与多种 SOTA KGE 模型的对比，证明了其在小样本和泛化场景下的优越性。

### 6. 主要结论与发现
*   **性能突破**：RALP† 在 Countries-S1 上达到了 **1.000 的 MRR**，打破了传统 KGE 模型的瓶颈。
*   **数据增强有效性**：将 RALP 发现的缺失三元组加入训练集后，传统 KGE 模型的 MRR 普遍提升了 **5% 以上**。
*   **数值与逻辑能力**：在数值预测中实现了高精度的区间覆盖（ICR > 75%）；在 OWL 推理中，使用**曼彻斯特语法**的效果显著优于传统的描述逻辑语法，Jaccard 相似度超过 88%。

### 7. 优点
*   **零/少样本泛化**：能够处理训练集中从未出现过的实体和关系，这是传统 Embedding 方法的硬伤。
*   **可解释性**：通过思维链（CoT），模型能够给出预测背后的逻辑依据，而非仅仅是一个分数。
*   **多模态统一**：在一个框架下同时解决了实体预测、数值回归和逻辑推理问题。
*   **非梯度优化**：对 LLM 权重无要求，适用于闭源或仅 API 可用的模型。

### 8. 不足与局限
*   **可扩展性限制**：受限于 LLM 的上下文窗口长度，处理超大规模图谱时需要复杂的采样策略。
*   **语义依赖**：高度依赖实体和关系的字符串标签是否具有语义含义（如果标签是无意义的 ID，模型将失效）。
*   **推理成本**：相比于毫秒级的向量计算，LLM 的推理速度较慢且计算资源消耗更高。
*   **异常值敏感**：在数值预测中，上下文中的极端异常值（Outliers）容易干扰 LLM 的判断。

（完）
