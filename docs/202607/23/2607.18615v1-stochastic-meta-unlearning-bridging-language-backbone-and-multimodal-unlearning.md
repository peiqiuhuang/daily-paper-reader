---
title: "Stochastic Meta-Unlearning: Bridging Language Backbone and Multimodal Unlearning"
title_zh: 随机元遗忘：桥接语言主干与多模态遗忘
authors: "Zijie Liu, Jinhao Duan, Gaowen Liu, Sijia Liu, Tianlong Chen"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18615v1"
tags: ["query:bopl"]
score: 8.0
evidence: 元脱退学习的双层优化框架
tldr: 针对视觉语言模型（VLM）中仅靠文本反馈无法彻底消除记忆的问题，本文提出了随机元遗忘（SMU）框架。该框架采用双层优化结构，在内层对语言主干进行文本遗忘，在外层利用VLM层面的多模态反馈来指导更新。实验证明，SMU在遗忘效果与性能保持之间取得了最佳平衡，并具有良好的迁移性。
selection_source: fresh_fetch
motivation: 发现仅在语言主干上进行的文本遗忘在面对多模态输入时容易失效，导致被遗忘的信息被重新激活。
method: 提出一种双层元学习框架，通过VLM层面的多模态反馈来优化语言主干的遗忘初始化，使其感知最终的多模态行为。
result: 在多个模型和数据集上，SMU显著降低了遗忘准确率，同时大幅提升了保留数据和测试集的准确率。
conclusion: 引入多模态反馈能使语言主干的遗忘过程在VLM中更加可靠且具备更强的迁移能力。
---

## 摘要
视觉语言模型（VLM）的机器遗忘（Machine unlearning）仍有待深入探索。与语言模型不同，VLM 将语言主干与视觉组件相结合，这使得遗忘过程更加复杂。从单模态遗忘转向 VLM 遗忘时存在一个令人惊讶的现象：被独立语言主干遗忘的目标，在向完整 VLM 提供图像信息时仍能被恢复。这表明仅靠文本反馈不足以实现可靠的 VLM 遗忘。受此观察启发，我们提出了随机元遗忘（SMU），这是一个利用 VLM 级反馈来学习“遗忘就绪”初始化的双层框架。在内循环中，SMU 使用文本数据对语言主干执行若干遗忘步骤。在外循环中，SMU 将更新后的主干与冻结的 VLM 重新组合，并在 VLM 级别评估遗忘效果和效用。这种设计使遗忘更新能够感知最终的多模态行为，同时仍保持更新局限于语言主干。在两个 VLM、两个多模态模因数据集和三个基准模型上的实验表明，SMU 实现了最佳的遗忘-保留权衡。与各项指标中最强的基准模型相比，SMU 将平均遗忘准确率降低了 10.52 个百分点，并将平均保留准确率和测试准确率分别提高了 20.10 和 17.01 个百分点。更重要的是，SMU 还可以迁移到新的遗忘目标和不同的元测试遗忘方法中。这些结果表明，VLM 级反馈可以使语言主干遗忘对 VLM 而言更加可靠且更具迁移性。

## Abstract
Machine unlearning for vision-language models (VLMs) remains underexplored. Unlike language models, VLMs combine a language backbone with visual components, which makes unlearning more complex. There is a surprising phenomenon when moving from single-modality unlearning to VLM unlearning: a target forgotten by the standalone language backbone can still be recovered when image information is given to the full VLM. This shows that text-only feedback is not enough for reliable VLM unlearning. Motivated by this observation, we propose Stochastic Meta-Unlearning (SMU), a bilevel framework that uses VLM-level feedback to learn an unlearning-ready initialization. In the inner loop, SMU applies a few unlearning steps to the language backbone using text data. In the outer loop, SMU recomposes the updated backbone with the frozen VLM and evaluates forgetting and utility at the VLM level. This design makes the unlearning update aware of the final multimodal behavior, while still keeping the update local to the language backbone. Experiments on two VLMs, two multimodal meme datasets, and three baselines show that SMU achieves the best overall forget-retain trade-off. Compared with the strongest baseline for each metric, SMU reduces average Forget accuracy by 10.52 points and improves average Retain and Test accuracy by 20.10 and 17.01 points, respectively. More importantly, SMU also transfers to new forgetting targets and to different meta-test unlearning methods. These results suggest that VLM-level feedback can make language-backbone unlearning more reliable and more transferable for VLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stochastic Meta-Unlearning: Bridging Language Backbone and Multimodal Unlearning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（VLM）在执行“机器遗忘”任务时存在**组件与系统的不匹配（Component-System Mismatch）**。
*   **研究背景**：现有的 VLM 遗忘方法要么直接更新整个多模态模型（容易破坏通用能力），要么仅在语言主干上进行文本遗忘。
*   **关键发现**：作者观察到一个“多模态恢复”现象——即使语言主干在纯文本查询下已经“忘记”了目标知识，但当重新组合进 VLM 并提供图像信息时，该知识仍能被模型找回。这说明仅靠文本反馈不足以实现可靠的 VLM 遗忘。

### 2. 方法论：随机元遗忘（SMU）
*   **核心思想**：采用**双层优化（Bilevel Optimization）**框架。通过在元训练阶段引入 VLM 层面的多模态反馈，学习一个“遗忘就绪”的语言主干初始化参数，使得该主干在后续仅需少量文本遗忘步骤，就能在多模态环境下实现彻底遗忘。
*   **关键技术细节**：
    *   **内循环（Inner Loop）**：在语言主干上执行 $K$ 步基于文本的遗忘操作（如梯度差异法）。此过程不涉及图像，计算开销较小。
    *   **外循环（Outer Loop）**：将内循环更新后的主干重新插入冻结的视觉组件中，组成完整的 VLM。使用多模态数据（图像+文本）评估遗忘效果和效用保留情况。
    *   **元更新**：通过外循环的损失函数计算元梯度，反向传播穿透内循环的遗忘步骤，更新主干的初始参数 $\theta_T$。
    *   **随机性（Stochastic）**：在元训练中随机采样不同的遗忘目标、遗忘算子和查询条件，以增强模型对未知遗忘任务的泛化能力。

### 3. 实验设计
*   **数据集/场景**：
    *   **Hateful Memes**：多模态仇恨言论检测。
    *   **COVID Memes**：关于新冠疫情的有害模因检测。
*   **Benchmark 模型**：Qwen2.5-VL-7B-Instruct 和 InternVL3-8B-hf。
*   **对比方法**：
    *   **GradDiff**（梯度差异法）
    *   **NPO**（负偏好优化）
    *   **RMU**（随机映射遗忘）
    *   注：这些基准方法被直接应用于 VLM 层面进行对比。

### 4. 资源与算力
*   **硬件设备**：使用 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练细节**：
    *   微调阶段：LoRA（Rank 8），3 个 Epoch。
    *   元训练阶段：内循环 4 步，外循环 5 个 Epoch。
*   **算力说明**：文中明确指出 SMU 的元训练过程比单次遗忘方法更昂贵，因为涉及二阶导数或展开的梯度计算，但未给出具体的总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个主流 VLM 模型、2 个多模态数据集、3 种主流遗忘算法作为基准。
*   **充分性评价**：
    *   **多维度评估**：不仅测试了“文本→文本”的遗忘，还重点测试了“文本→多模态”的遗忘恢复情况。
    *   **迁移性实验**：验证了模型在面对**新遗忘目标**和**不同遗忘算子**时的表现，这在元学习论文中至关重要。
    *   **消融与对比**：对比了图像端遗忘的局限性，证明了选择语言主干作为干预点的合理性。实验设计较为客观、公平。

### 6. 主要结论与发现
*   **最佳权衡**：SMU 在“彻底遗忘”与“保留通用能力”之间取得了最佳平衡。相比最强基准，平均遗忘准确率降低了 10.52%，而保留和测试准确率分别提升了 20.10% 和 17.01%。
*   **解决恢复问题**：SMU 有效解决了图像信息诱导下的知识恢复问题，使遗忘在多模态环境下更加稳健。
*   **强泛化性**：元学习得到的初始化参数具有通用性，即使在元测试阶段更换遗忘算法（如从 GradDiff 换成 RMU），依然有效。

### 7. 优点（亮点）
*   **视角独特**：首次识别并形式化了 VLM 遗忘中的“组件-系统不匹配”问题。
*   **架构优雅**：通过双层优化，既保持了对语言主干更新的局部性（不破坏视觉编码器），又获得了全局多模态性能的保证。
*   **实用性强**：元训练完成后，实际部署时的遗忘操作仅需在文本端进行，非常高效。

### 8. 不足与局限
*   **计算成本**：元训练（Meta-training）阶段的计算开销显著高于普通遗忘方法，对于超大规模模型可能存在扩展性挑战。
*   **冻结组件的限制**：由于视觉编码器和投影层被冻结，如果某些敏感知识主要存储在视觉特征空间中，仅更新语言主干可能存在遗忘上限。
*   **安全性保证**：虽然实验表现优异，但缺乏在极端对抗性攻击（Adversarial Probing）下知识是否绝对不可恢复的理论证明。

（完）
