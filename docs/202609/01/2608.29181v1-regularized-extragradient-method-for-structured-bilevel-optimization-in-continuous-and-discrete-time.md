---
title: Regularized extragradient method for structured bilevel optimization in continuous and discrete time
title_zh: 连续和离散时间下结构化双层优化的正则化外梯度法
authors: "Radu Ioan Bot, Enis Chenchene, David Alexander Hulett"
date: 2026-08-29
pdf: "https://arxiv.org/pdf/2608.29181v1"
tags: ["query:bopl"]
score: 9.0
evidence: 希尔伯特空间中的结构化双层优化
tldr: 本研究针对希尔伯特空间中的结构化双层优化问题，提出了一种正则化外梯度方法。研究涵盖了平滑与非平滑两种情形，分别设计了连续时间动力系统和离散时间近端外梯度算法。通过引入动态正则化技术，在弱可积性或锐度条件下，证明了算法在外部目标函数和内部算子残差上的最后迭代收敛率，并实现了轨迹或序列向双层优化问题最优解的弱收敛。
selection_source: fresh_fetch
motivation: 旨在解决在最大单调算子零点集上最小化凸函数的复杂双层优化问题，并寻求具有收敛率保证的算法。
method: 结合动态正则化技术，在连续时间下构建单调流动力系统，在离散时间下提出正则化近端外梯度算法。
result: 在特定几何条件下，建立了外部残差和内部残差的最后迭代收敛率，并证明了算法的弱收敛性。
conclusion: 该研究为处理包含非平滑项的结构化双层优化问题提供了有效的理论框架和收敛性保障。
---

## 摘要
在实希尔伯特空间中，我们研究了一个双层优化问题，该问题旨在最大单调算子的零点集上最小化一个外部凸函数。在平滑设置下，即外部目标函数是凸的且 Fréchet 可微，而内部算子是单值的、连续且单调的，我们将该问题与一个一阶动力系统联系起来，该系统可被视为应用于动态正则化算子的单调流。在内部问题的适当几何条件下——即弱 Attouch-Czarnecki 型可积性条件或更强的锐度（sharpness）假设——我们建立了外部和内部残差的最后迭代收敛率，以及轨迹向双层问题最优解的弱收敛性。在“平滑+非平滑”设置下，我们为外部目标函数增加了一个真凸且下半连续的函数，同时内部算子通过具有相同性质的函数的次微分进行增强。我们提出了一种正则化近端外梯度算法，其中前向和后向步骤分别针对动态正则化的算子和函数执行。在与平滑设置类似的内部问题几何假设下，我们建立了外部和内部残差的最后迭代收敛率，以及迭代序列向双层问题最优解的弱收敛性。

## Abstract
In a real Hilbert space, we study a bilevel optimization problem that consists in minimizing an outer convex function over the zero set of a maximally monotone operator. In the smooth setting, where the outer objective is convex and Fréchet differentiable and the inner operator is single-valued, continuous and monotone, we associate with the problem a first-order dynamical system that can be viewed as a monotone flow applied to a dynamically regularized operator. Under suitable geometric conditions on the inner problem --- either a weak Attouch-Czarnecki-type integrability condition or the stronger assumption of sharpness --- we establish last-iterate convergence rates for both the outer and inner residuals, together with weak convergence of the trajectories to optimal solutions of the bilevel problem. In the smooth+nonsmooth setting, we enrich the outer objective with a proper, convex, and lower semicontinuous function, while the inner operator is augmented by the subdifferential of a function with the same properties. We propose a regularized proximal-extragradient algorithm in which both the forward and backward steps are performed with respect to dynamically regularized operators and functions, respectively. Under geometric assumptions on the inner problem analogous to those in the smooth setting, we establish last-iterate convergence rates for both the outer and inner residuals, together with weak convergence of the iterates to optimal solutions of the bilevel problem.

---

## 论文详细总结（自动生成）

### 论文总结：连续和离散时间下结构化双层优化的正则化外梯度法

#### 1. 核心问题与整体含义
本论文研究的是希尔伯特空间中的**结构化双层优化问题**。其核心任务是在一个由最大单调算子定义的零点集（内部问题）上，最小化一个凸目标函数（外部问题）。
*   **研究动机**：双层优化在纳什均衡选择、平衡约束数学规划（MPEC）和最优控制中具有重要应用。
*   **背景**：传统方法通常只能保证“遍历收敛”（所有迭代的平均值收敛），而难以保证“最后迭代收敛”（当前迭代点直接收敛）。此外，当内部问题涉及非势能（non-potential）算子时，现有算法的收敛率分析非常有限。

#### 2. 方法论
论文通过引入**动态 Tikhonov 正则化**技术，分别在连续和离散时间框架下提出了解决方案：
*   **核心思想**：将内部算子 $V$ 替换为动态正则化算子 $\Phi_t(x) = V(x) + \epsilon(t)\nabla h(x)$，其中正则化参数 $\epsilon(t)$ 随时间缓慢衰减。
*   **连续时间系统**：构建了一个非自治单调流动力系统，并利用 Lyapunov 分析证明了轨迹的弱收敛性。
*   **离散时间算法（Bi-EG）**：提出了一种**正则化近端外梯度算法**。
    *   **流程**：每轮迭代包含两个步骤。首先在当前点执行一次前向-后向预测（预测步），得到中间点 $y_k$；然后在 $y_k$ 处再次执行前向-后向操作（校正步）来更新 $x_{k+1}$。
    *   **关键技术**：利用 Fitzpatrick 函数定义了 Attouch-Czarnecki 型可积性条件，或在更强的“锐度”（Sharpness）假设下，推导出外部目标和内部残差的具体收敛率。

#### 3. 实验设计
论文设计了两个主要的数值实验场景来验证算法性能：
*   **场景 1：说明性示例**。在 $R^d$ 空间中最小化全变分（Total Variation）函数，约束条件由一个非线性单调算子定义。
*   **场景 2：最优传输（OT）中的次级变分问题**。在多个最优传输方案中，通过外部目标函数选择一个特定的 Monge 映射方案。
*   **Benchmark**：对比了 **OEG-H**（用于层次优化的乐观外梯度法），这是目前处理此类通用双层问题的主要基准方法。

#### 4. 资源与算力
*   **硬件环境**：实验在一台搭载第 12 代 Intel Core i7-1255U 处理器（1.70-4.70 GHz）和 16 GB RAM 的笔记本电脑上完成。
*   **软件环境**：使用 Python 编写。
*   **算力说明**：文中未提及 GPU 使用，该研究属于数学优化领域，主要依赖 CPU 进行数值迭代计算，不涉及大规模深度学习训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对说明性示例，进行了 20 种不同随机初始化的测试。
    *   对正则化衰减指数 $\delta$ 进行了敏感性分析（取 20 个均匀分布在 0.1 到 0.5 之间的值）。
*   **充分性**：实验设计较为客观。通过对比不同 $\delta$ 值下的收敛表现，验证了理论推导的收敛率。与 OEG-H 的对比涵盖了内部残差、内部间隙和外部目标间隙等多个维度，实验结果具有说服力。

#### 6. 主要结论与发现
*   **收敛率保证**：在特定几何条件下，Bi-EG 算法能够实现外部残差 $O(1/k^\delta)$ 和内部残差 $o(1/k^{1/2-\delta})$ 的最后迭代收敛率。
*   **性能优势**：实验表明，Bi-EG 在达到高精度方面比 OEG-H 快约一个数量级。
*   **理论验证**：数值观察到的收敛斜率与论文提出的理论界限高度吻合，证明了动态正则化参数选择的有效性。

#### 7. 优点
*   **理论突破**：首次在包含非势能单调算子的复合双层优化中建立了最后迭代收敛率。
*   **结构通用**：算法能够同时处理平滑和非平滑项，适用于更广泛的变分不等式和平衡选择问题。
*   **内存友好**：相比于需要存储多个辅助变量的 Popov 型方法，Bi-EG 仅需存储一个迭代变量，且步长限制较小。

#### 8. 不足与局限
*   **计算成本**：作为外梯度法，Bi-EG 每轮迭代需要两次算子评估（Forward 步骤），如果算子 $V$ 的计算代价极高，单次评估的方法可能更具吸引力。
*   **收敛强度**：论文主要证明了弱收敛性，在某些无限维应用场景下，可能需要更强的强收敛保证。
*   **应用深度**：实验主要集中在合成数据和经典优化模型上，尚未在复杂的大规模工业级数据集（如深度学习中的双层架构）上进行压力测试。

（完）
