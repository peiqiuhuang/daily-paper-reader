---
title: "RRSI: Regularized Recursive Self-Improvement of Agent Harnesses"
title_zh: RRSI：代理框架的正则化递归自我改进
authors: "Peng Xia, Rujun Han, Zifeng Wang, Yanfei Chen, Yufan Zhuang, Yoonho Lee, Chengsong Huang, Han Yu, Zhongying CuiZhu, Yifei Ming, Huaxiu Yao, Burak Gokturk, Tomas Pfister, Chen-Yu Lee"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24972v2"
tags: ["query:bopl"]
score: 8.0
evidence: 通过递归自我改进自动执行提示和框架编辑
tldr: 本文提出了一种名为RRSI的正则化递归自我改进框架，旨在解决LLM智能体在迭代优化过程中出现的过拟合问题。通过引入时间退火预算、演化历史引导、以及包含评估器和剪枝器的选择机制，RRSI能够有效约束智能体组件的演化，在提升任务性能的同时增强了泛化能力，并显著降低了策略令牌的消耗。
selection_source: fresh_fetch
motivation: 现有的智能体递归自我改进方法容易在训练任务上过拟合，导致在分布外任务上性能下降。
method: 引入正则化机制，通过限制演化候选方案的编辑数量、鼓励探索新路径，并利用评估器和剪枝器筛选出可复用的通用机制。
result: "在八项基准测试中，RRSI在训练集上提升了14.1分，在分布外任务上提升了4.7分，且策略令牌使用量减少了30%。"
conclusion: RRSI通过正则化手段成功平衡了智能体演化的性能提升与泛化能力，为构建高效、鲁棒的智能体系统提供了新范式。
---

## 摘要
大语言模型（LLM）代理的能力在很大程度上取决于其框架，即围绕冻结骨干模型所构建的提示词、控制流、工具使用、记忆及上下文管理。近期研究越来越多地通过迭代提出并选择代理框架的组件级编辑来自动化这一过程，这实际上在代理系统层面建立了一种递归自我改进（RSI）形式。然而，这种递归演化可能会因记忆训练任务而产生过拟合，导致在分布内（in-distribution）基准测试中表现出显著提升，但在分布外（out-of-distribution）基准测试中增益缩减甚至消失。我们提出了代理框架的正则化递归自我改进（RRSI），通过约束演化候选方案的提出与选择，将正则化原则纳入框架的自我改进过程。提出器（proposer）采用时间退火预算，限制候选方案可捆绑的编辑数量，并基于演化历史鼓励探索未知的轨迹。选择器（selector）配备了评估器（critic）和剪枝器（pruner）：评估器负责筛选特定于基准测试的方案，而剪枝器则移除过于微小、成本过高或不再有用的变更。这些约束共同促进了可复用代理机制的形成，而非仅限于特定基准测试的机制或噪声。在涵盖编码、代理工作空间和工程设计任务的八项基准测试中，RRSI 在其演化所针对的数据集上提升了高达 14.1 个百分点，在五个分布外基准测试中提升了高达 4.7 个百分点，同时生成的框架比未经正则化的演化方案减少了 30% 的策略令牌消耗。代码已在 https://github.com/google-research/rrsi 公开，项目主页为 https://regularized-rsi.com/。

## Abstract
An LLM agent's capability is largely magnified by its harness, namely the prompts, control flow, tooling, memory, and context management surrounding the frozen backbone model. Recent methods increasingly automate this process by iteratively proposing and selecting component-wise edits of an agent harness, practically establishing a form of recursive self-improvement (RSI) at the agent-system level. However, such recursive evolution may overfit by memorizing the training tasks, showing large in-distribution gains that shrink or even vanish on out-of-distribution benchmarks. We introduce Regularized Recursive Self-Improvement of Agent Harnesses (RRSI), which incorporates the principles of regularizations into harness self-improvement by constraining the evolution candidate proposal and selection. The proposer operates with a temporally annealed budget, limiting how many edits a candidate can bundle, and it encourages unexplored trajectories based on evolution history. The selector is equipped with a critic and a pruner: the critic screens benchmark-specific proposals, while the pruner, removes changes that are too small, too expensive, or no longer useful. Together these constraints favor reusable agent mechanisms over benchmark-specific ones or even noises. Across eight benchmarks spanning coding, agentic workspace and engineering design tasks, RRSI gains up to 14.1 points on the split it evolves against and up to 4.7 points on the five out-of-distribution benchmarks, while producing a harness that runs on 30% fewer policy tokens than the unregularized evolution. Code is available at https://github.com/google-research/rrsi and project page is https://regularized-rsi.com/.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《RRSI: Regularized Recursive Self-Improvement of Agent Harnesses》** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：现代 LLM 智能体的性能高度依赖于其“框架”（Harness），即提示词、控制流、工具接口和记忆管理等外围组件。
*   **问题**：现有的自动化框架演化方法（递归自我改进，RSI）存在严重的**过拟合风险**。智能体在演化集（Evolve set）上表现优异，但由于记忆了特定任务的模式或追逐评估噪声，导致在分布外（OOD）任务上泛化能力极差，甚至不如初始框架。

### 2. 方法论：RRSI 框架
RRSI 的核心思想是将机器学习中的正则化原则引入到智能体框架的演化过程中，在保持框架完全可编辑的前提下，约束搜索轨迹。
*   **提出侧正则化（Proposal-side）**：
    *   **退火更新预算（Annealed Update Sparsity）**：随演化轮次增加，限制单次候选方案中可捆绑的编辑数量，由粗到细进行优化。
    *   **证据感知信用分配（Evidence-Aware Credit Assignment）**：记录演化历史，避免重复测试已证伪的假设，并明确成功机制的贡献。
    *   **结构化探索（Structured Exploration）**：当演化停滞时，强制将预算分配给未探索的组件，防止陷入局部最优。
*   **选择侧正则化（Selection-side）**：
    *   **泄漏筛选（Leakage Screening）**：利用评估器（Critic）在评估前剔除包含特定任务名称或答案的“作弊”代码。
    *   **噪声调整性能底线（Noise-adjusted Performance Floor）**：要求候选方案必须显著优于噪声阈值，防止因评估随机性导致的性能回退。
    *   **复杂度感知接受（Ridge/L2-style）**：要求性能增益必须能抵消推理成本的增加。
    *   **结构化剪枝（Lasso/L1-style）**：定期移除长期无贡献的组件，保持框架的简洁性。

### 3. 实验设计
*   **场景与数据集**：涵盖三大领域共八项基准测试：
    *   **编码**：Terminal-Bench 2.1（演化）、SWE-bench Verified（OOD）。
    *   **代理工作空间**：Harvey LAB（演化/ID）、JobBench、GDPval、APEX-Agents（OOD）。
    *   **工程设计**：EngDesign（演化）、Frontier-Eng（OOD）。
*   **对比方法**：Meta-Harness、AHE、TTHE、HarnessX 以及未演化的初始框架 $H_0$。

### 4. 资源与算力
*   **算力说明**：论文未明确列出 GPU 的具体型号、数量或总训练时长。
*   **推理成本**：文中以“策略令牌（Policy tokens）”作为衡量成本的指标，指出 RRSI 相比未正则化的演化方法，在提升性能的同时，平均减少了约 30% 的推理令牌消耗。

### 5. 实验数量与充分性
*   **实验规模**：进行了多领域、多基准测试的对比，并包含详细的消融实验（分别验证提出侧和选择侧正则化的贡献）。
*   **客观性**：通过跨模型（Claude Opus 4.8 与 Gemini 3.5 Flash）验证了方法的鲁棒性，并使用确定性模拟器（而非仅依赖 LLM 判分）来消除评估偏差，实验设计非常严谨且具有说服力。

### 6. 主要结论与发现
*   **泛化性**：RRSI 成功解决了递归演化中的过拟合问题，在所有 OOD 测试集上均实现了性能提升。
*   **效率**：正则化不仅提升了泛化能力，还通过剪枝机制使最终框架更加轻量，推理成本更低。
*   **通用性**：RRSI 的改进机制不依赖于特定的骨干模型，即使在较弱的模型上也能观察到显著的性能增益。

### 7. 优点
*   **正则化视角创新**：将经典的机器学习正则化理论（L0/L1/L2）巧妙地映射到智能体框架的演化搜索中。
*   **机制可解释性**：通过剪枝和成本控制，确保了演化出的框架是“机制性”的改进，而非对特定任务的“死记硬背”。
*   **实用性强**：在保持框架灵活性的同时，显著降低了部署成本。

### 8. 不足与局限
*   **超参数依赖**：RRSI 引入了若干超参数（如噪声阈值、剪枝窗口等），其效果可能受这些参数设置的影响。
*   **演化集依赖**：尽管提升了泛化性，但方法依然依赖于一个有限的演化集，若演化集质量极差，效果可能受限。
*   **架构局限**：目前仅针对冻结骨干模型的框架演化，未涉及模型权重更新（Post-training）的场景。

（完）
