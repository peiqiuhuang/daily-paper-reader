---
title: Bayesian Model Merging
title_zh: 贝叶斯模型合并
authors: "Kaiyang Li, Shaobo Han, Qing Su, Shihao Ji"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.12843v1"
tags: ["query:bopl"]
score: 9.0
evidence: 即插即用的双层优化框架
tldr: 本研究提出贝叶斯模型合并（BMM）框架，旨在解决现有模型合并方法忽视锚点模型归纳偏置及缺乏全局超参数优化的问题。BMM采用双层优化：内层通过基于激活值的贝叶斯回归实现闭式解合并，外层利用贝叶斯优化搜索模块级超参数。此外，该方法还包含无需辅助数据的无数据变体。实验表明，BMM在多项视觉和语言任务中表现优异，性能接近多个专家模型的平均水平。
selection_source: fresh_fetch
motivation: 现有模型合并方法未充分利用强锚点模型的先验知识，且在网络不同模块间共用超参数，限制了合并效果。
method: 提出一种双层优化框架，内层利用锚点模型先验进行贝叶斯回归求闭式解，外层通过贝叶斯优化进行全局超参数搜索。
result: 在视觉和语言任务的广泛基准测试中，BMM一致优于现有主流基准，在8任务合并中性能极度接近专家模型。
conclusion: BMM为多任务模型合并提供了一种高效、即插即用且性能强大的贝叶斯优化方案。
---

## 摘要
模型合并旨在将多个特定任务的专家模型合并为一个单一模型，而无需进行联合重训练。在数据获取受限或计算预算有限的情况下，它为多任务学习提供了一种实用的替代方案。然而，现有方法面临两个关键局限性：(1) 它们忽视了强锚点模型中宝贵的归纳偏置，而是从头开始估计合并后的权重；(2) 它们依赖于网络不同模块间共享的超参数设置，缺乏全局优化策略。本文提出了贝叶斯模型合并 (BMM)，这是一个即插即用的双层优化框架。其中，内层将模型合并建模为在锚点模型诱导的强先验下的基于激活值的贝叶斯回归，从而得到高效的闭式解；外层则利用贝叶斯优化程序，基于小型验证集在全局范围内搜索特定于模块的超参数。此外，我们揭示了激活统计量与任务向量之间的一种关键对齐关系，使我们能够推导出 BMM 的无数据变体，该变体无需任何辅助数据即可估计用于回归的 Gram 矩阵。在广泛的基准测试中（包括视觉领域的 20 任务合并和语言领域的 5 任务合并），BMM 始终优于所有即插即用的锚点基准方法（例如 TA、WUDI-Merging 和 TSV）。特别是在 ViT-L/14 的 8 任务合并基准测试中，单个合并模型的性能达到了 95.1，与八个特定任务专家的平均性能 (95.8) 非常接近。

## Abstract
Model merging aims to combine multiple task-specific expert models into a single model without joint retraining, offering a practical alternative to multi-task learning when data access or computational budget is limited. Existing methods, however, face two key limitations: (1) they overlook the valuable inductive bias of strong anchor models and estimate the merged weights from scratch, and (2) they rely on a shared hyperparameter setting across different modules of the network, lacking a global optimization strategy. This paper introduces Bayesian Model Merging (BMM), a plug-and-play bi-level optimization framework, where the inner level formulates the model merging as an activation-based Bayesian regression under a strong prior induced by an anchor model, yielding an efficient closed-form solution; and the outer level leverages a Bayesian optimization procedure to search module-specific hyperparameters globally based on a small validation set. Furthermore, we reveal a key alignment between activation statistics and task vectors, enabling us to derive a data-free variant of BMM that estimates the Gram matrix for regression without any auxiliary data. Across extensive benchmarks, including up to 20-task merging in vision and 5-task merging in language, BMM consistently outperforms all plug-and-play anchor baselines (e.g., TA, WUDI-Merging, and TSV). In particular, on the ViT-L/14 benchmark for 8-task merging, a single merged model reaches 95.1, closely matching the average performance of eight task-specific experts (95.8).

---

## 论文详细总结（自动生成）

以下是对论文《Bayesian Model Merging》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在不访问原始训练数据且不进行联合重训练的情况下，将多个针对特定任务的专家模型（Expert Models）高效合并为一个统一模型。
*   **研究动机**：
    *   **存储与部署压力**：为每个任务维护独立模型开销巨大。
    *   **现有方法局限性**：
        1.  **忽视先验**：现有方法通常从零开始估计合并权重，未能利用强锚点模型（Anchor Models）提供的归纳偏置。
        2.  **超参数单一化**：现有方法在所有网络模块中采用统一的超参数设置，忽视了模块间的异质性，缺乏全局优化策略。
*   **整体含义**：提出一种贝叶斯框架，通过引入先验知识和全局超参数搜索，提升模型合并的性能，使其接近独立专家模型的水平。

### 2. 方法论：核心思想与关键技术
BMM 是一个**即插即用的双层优化框架**：
*   **内层优化（基于激活值的贝叶斯回归）**：
    *   将模型合并建模为线性回归问题。利用锚点模型（如 Task Arithmetic 或 TSV 的结果）作为高斯先验。
    *   **关键公式**：推导出模块化任务向量的**最大后验估计（MAP）闭式解**。该解平衡了对任务特定激活值的拟合与对锚点模型先验的遵循。
*   **外层优化（基于贝叶斯优化的全局搜索）**：
    *   利用高斯过程（GP）作为代理模型的**贝叶斯优化（BO）**，在验证集上搜索每个模块组的最优正则化强度（$\lambda$）和缩放因子（$s$）。
    *   **参数共享策略**：为提高搜索效率，将网络层划分为块，并在块内对功能相似的模块（如 Q/K/V 矩阵）进行参数绑定。
*   **无数据变体（Data-Free BMM）**：
    *   **理论发现（定理1）**：揭示了输入激活值的 Gram 矩阵与任务向量的 Gram 矩阵之间存在正相关对齐关系。
    *   **技术实现**：利用任务向量自身的统计特性替代真实激活值，从而在完全无辅助数据的情况下实现闭式解合并。

### 3. 实验设计
*   **数据集与场景**：
    *   **视觉任务**：涵盖 8 任务、14 任务及 20 任务合并场景（包括 MNIST、CIFAR、SUN397、Cars 等多样化数据集）。
    *   **语言任务**：5 任务合并（指令遵循、数学、多语言、代码、安全）。
*   **基准模型（Backbones）**：ViT-B/32, ViT-L/14, Llama-3.2-3B, Llama-3.1-8B。
*   **对比方法（Baselines）**：
    *   数据辅助类：RegMean。
    *   无数据类：Task Arithmetic (TA), TIES, TSV, WUDI-Merging, ISO-CTS。
    *   BMM 将上述方法作为“锚点”进行改进对比。

### 4. 资源与算力
*   **硬件环境**：服务器配备 **8 台 NVIDIA RTX-6000 (48GB) GPU**。
*   **运行耗时**：
    *   **视觉**：ViT-B/32 在 20 任务下约需 50 分钟；ViT-L/14 约需 9 小时。
    *   **语言**：Llama-3.1-8B 在 5 任务下约需 4 小时（8 GPU 并行）。
    *   **效率**：由于内层是闭式解，主要耗时在于外层 BO 循环中的验证集前向推理。

### 5. 实验数量与充分性
*   **实验规模**：进行了跨视觉和语言两大领域、四种主流架构、多达 20 个任务的大规模合并实验。
*   **充分性**：
    *   每组实验均包含 5 个随机种子（0-4）并报告均值与标准差。
    *   包含详尽的消融实验：验证了 BO 搜索 vs 随机搜索、模块化超参数 vs 统一超参数的有效性。
    *   **公平性**：BMM 使用的验证集与其他基准方法（如 TA、TSV）用于调节超参数的验证集完全一致，未引入额外数据。

### 6. 主要结论与发现
*   **性能卓越**：BMM 在所有基准测试中均一致优于其对应的锚点模型。在 ViT-L/14 的 8 任务合并中，准确率达到 95.1%，极度接近专家模型平均值（95.8%）。
*   **扩展性强**：随着合并任务数量增加（从 8 到 20），BMM 的领先优势依然稳固，有效缓解了任务间的干扰。
*   **无数据变体有效**：理论推导的无数据版本在多数情况下性能接近甚至超过某些数据辅助方法。
*   **不确定性校准**：通过采样版本的 BMM（非 MAP 点估计），可以显著改善合并模型的预测校准度（ECE）。

### 7. 优点（亮点）
*   **理论与实证结合**：不仅提出了算法，还通过定理证明了任务向量与激活统计量的对齐关系，为无数据合并提供了理论支撑。
*   **即插即用**：可以作为现有任何合并方法的后处理增强步骤。
*   **架构感知**：通过双层优化解决了深度网络中不同层、不同功能模块对正则化需求不同的问题。

### 8. 不足与局限
*   **模型规模限制**：实验目前最高仅覆盖到 8B 参数规模的模型，尚未在超大规模模型（如 70B+）上验证。
*   **依赖验证集**：虽然是“训练无关”，但外层 BO 优化仍依赖于一个小型且高质量的验证集来引导搜索。
*   **计算开销**：相比于 TA 等一次性合并方法，BMM 需要多次验证集推理，对于算力极度受限的场景可能存在压力。

（完）
