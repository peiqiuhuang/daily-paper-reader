---
title: "The Optimizer Is the Agent: Reasoning-Driven Search across Prompts, Programs, and ML Workflows"
title_zh: 优化器即智能体：跨提示词、程序和机器学习工作流的推理驱动搜索
authors: "Junbo Li, Boyi Liu, Canwen Xu, Yite Wang, Yuxiong He, Zhangyang Wang, Qiang Liu, Zhewei Yao"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.06714v1"
tags: ["query:bopl"]
score: 8.0
evidence: 提示和工作流的推理驱动搜索
tldr: 本研究提出ReASearch框架，挑战了传统依赖外部控制器（如进化算法）进行优化的范式，将搜索策略内化为具备推理能力的智能体。该智能体能自主诊断失败、编辑方案并管理搜索预算。在提示词、程序和机器学习工作流等14项任务中，ReASearch不仅优于专门的优化系统，还展现出从推理中自然涌现的复杂搜索行为，显著提升了自动化优化的灵活性与性能。
selection_source: fresh_fetch
motivation: 探索能否通过单一的工具使用智能体替代传统的手工设计外部搜索控制器，以实现更通用的自动化优化。
method: 提出ReASearch框架，利用具备持久记忆的智能体自主进行方案评估、故障诊断、策略修正及资源分配。
result: "在14个多样化任务中，该方法比强领域基准提升了2%至40%，甚至发现了超越人类已知最佳结果的方案。"
conclusion: 复杂的搜索行为可以从智能体的推理过程中自然涌现，证明了推理驱动的智能体在跨领域优化中的强大潜力。
---

## 摘要
最近用于优化提示词、程序和机器学习工作流的系统通常依赖于显式的外环控制器，如进化搜索、多臂老虎机（bandits）或文本梯度方法。我们提出了一个根本不同的问题：这种搜索策略中有多少可以被单个使用工具的智能体内化？我们提出了 ReASearch，这是一个统一的推理驱动优化框架，其中智能体自主决定评估内容、如何诊断故障、进行哪些修改以及何时验证或重启。智能体不再仅仅是受手工设计启发式方法引导的提案生成器，而是通过持久化记忆主动分析结果、分配预算并在长周期内优化其策略。通过共享的智能体循环和特定领域的工具，ReASearch 实例化了完全相同的脚手架来优化提示词、程序和机器学习工作流。在 14 个不同的任务中，它的表现与专门的优化系统相当，且大多优于后者，在强领域特定基准上实现了 2% 到 40% 的提升，并在某些情况下发现了优于先前人类已知最佳结果的解决方案。至关重要的是，我们观察到通常由显式控制器实现的复杂搜索行为，会从智能体的推理过程中自然涌现。

## Abstract
Recent systems for optimizing prompts, programs, and ML workflows typically rely on explicit outer-loop controllers such as evolutionary search, bandits, or textual-gradient methods. We ask a fundamentally different question: how much of this search policy can be internalized by a single tool-using agent? We present ReASearch, a unified framework for reasoning-driven optimization in which the agent autonomously decides what to evaluate, how to diagnose failures, which edits to make, and when to verify or restart. Rather than serving only as a proposal generator guided by hand-designed heuristics, the agent actively analyzes outcomes, allocates budget, and refines its strategy over long horizons through persistent memory. With a shared agent loop and domain-specific tools, ReASearch instantiates the exact same scaffold to optimize prompts, programs, and ML workflows. Across 14 diverse tasks, it is competitive with and mostly better than specialized optimization systems, achieving gains of 2% to 40% over strong domain-specific baselines, and in some cases discovering solutions that improve on prior human best-known results. Crucially, we observe that complex search behaviors, which are typically implemented by explicit controllers, emerge naturally from the agent's reasoning process.

---

## 论文详细总结（自动生成）

这篇论文由德克萨斯大学奥斯汀分校和 Snowflake 的研究团队合作完成，探讨了如何利用大语言模型（LLM）智能体替代传统的手工设计优化算法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的文本工件（如提示词、程序、机器学习配置）优化通常依赖于外部的“硬编码”控制器，如进化算法、多臂老虎机或贝叶斯优化。在这些系统中，LLM 仅充当被动的“变异器”，而搜索策略（如预算分配、分支选择、失败恢复）由外部算法决定。
*   **核心问题**：这种搜索策略中有多少可以被单个具备推理能力、能使用工具的智能体“内化”？
*   **整体含义**：论文提出了 **ReASearch** 框架，证明了复杂的搜索行为可以从智能体的推理过程中自然涌现，从而实现跨领域的通用自动化优化。

### 2. 方法论
*   **核心思想**：将优化任务建模为一个顺序推理问题。智能体不再受限于固定的外环逻辑，而是通过一组工具自主控制整个优化循环。
*   **关键技术细节**：
    *   **统一脚手架**：使用相同的智能体设计（代码智能体）来处理提示词、程序和机器学习（ML）工作流。
    *   **工具集（Tools）**：包括 `python_exec`（用于数据分析和统计诊断）、`edit_code`（修改工件）、`evaluate`（执行评估）和 `validate`（验证候选方案）。
    *   **持久化记忆**：维护一个 `lessons.md` 文件，记录“什么有效、什么失败、下一步尝试什么”，并在上下文超出限制时进行自动压缩和总结。
    *   **推理驱动搜索**：智能体自主决定何时进行小批量测试、何时进行大规模验证、何时回滚到之前的版本以及何时根据失败日志调整假设。

### 3. 实验设计
*   **实验场景与数据集**（共 14 个任务）：
    *   **提示词优化**：AIME 2025、GSM8K（数学）、HotpotQA（多跳问答）、Terminal-Bench 2.0（软件工程）。
    *   **程序演化**：圆堆积（Circle Packing）、Heilbronn 三角形（几何优化）、事务调度（TXN）、专家并行负载均衡（EPLB）、ARC-AGI-2（视觉推理）。
    *   **ML 工作流优化**：NanoGPT、图像分类（IMG-100）、强化学习（Atari, MuJoCo）、加密货币市场预测（Kaggle 竞赛）。
*   **对比基准（Benchmarks）**：
    *   提示词：对比 **GEPA**。
    *   程序：对比 **AdaEvolve**、**Shinka**、**AlphaEvolve** 等。
    *   ML 工作流：对比 **Claude Code**（官方 AutoResearch 设置）。
*   **模型底座**：主要使用 Claude 3.5 Sonnet 和 GPT-5，同时也测试了开源模型 GLM-5 和 Kimi-2.5。

### 4. 资源与算力
*   **算力说明**：文中未详细列出具体的 GPU 型号和数量，但提到了实验的成本和时间限制。
*   **成本与时长**：
    *   提示词优化：单次运行的 API 成本低于 **20 美元**。
    *   ML 工作流：每个实验设有固定的训练时长（5 到 30 分钟不等），总时长与基准测试对齐。
    *   程序演化：设定了固定的评估次数上限（如 500 次）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个大类、14 个迥异的任务，每个设置进行 **3 次独立运行**并取平均值。
*   **消融实验**：系统地消融了“记忆模块”和“Python 执行工具”，验证了两者在不同任务中的必要性。
*   **充分性评价**：实验设计非常充分且具有挑战性。通过跨领域的对比（从纯数学到 Kaggle 竞赛），证明了该框架的通用性。对比实验在预算（API 调用次数或训练时间）一致的情况下进行，保证了公平性。

### 6. 主要结论与发现
*   **性能卓越**：ReASearch 在所有任务中均达到或超过了专门的优化系统，性能提升在 **2% 到 40%** 之间。
*   **突破人类极限**：在圆堆积（Circle Packing）任务中，智能体发现了优于先前人类已知最佳结果的方案。
*   **涌现行为**：智能体自发表现出类似人类专家的优化行为，如：在昂贵验证前进行双重检查、根据失败日志进行因果诊断、在陷入局部最优时主动回滚。
*   **知识迁移**：通过 `lessons.md`，优化经验可以在不同的运行之间迁移。

### 7. 优点
*   **通用性极强**：用一套代码逻辑解决了提示词、算法发现和超参数调节三种完全不同的问题。
*   **诊断深度**：相比于只看分数的黑盒优化，ReASearch 能通过 Python 脚本分析错误模式，实现“外科手术式”的精准修改。
*   **低门槛**：减少了对复杂数学启发式算法（如贝叶斯优化）的依赖，更符合自然语言交互的直觉。

### 8. 不足与局限
*   **对底座模型依赖高**：性能高度依赖于底层 LLM 的推理能力，使用较弱的模型时效果会显著下降。
*   **Token 消耗**：长程优化任务会产生大量的对话历史，尽管有压缩机制，但在极端长任务中仍可能面临上下文丢失或成本上升的问题。
*   **随机性风险**：智能体的决策具有随机性，虽然平均表现优异，但单次运行可能因推理偏差陷入死循环（尽管文中提到有停滞警告机制）。

（完）
