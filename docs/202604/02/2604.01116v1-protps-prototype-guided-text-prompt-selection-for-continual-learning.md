---
title: "ProTPS: Prototype-Guided Text Prompt Selection for Continual Learning"
title_zh: ProTPS：面向持续学习的原型引导文本提示选择
authors: "Jie Mei, Li-Leng Peng, Keith Fuller, Jenq-Neng Hwang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01116v1"
tags: ["query:bopl"]
score: 8.0
evidence: 基于文本提示的方法和用于持续学习的可学习提示
tldr: 本文针对持续学习中由于文本提示语义重叠导致的灾难性遗忘问题，提出了ProTPS方法。该方法通过学习类别特定的视觉原型来引导文本提示的选择与学习，从而确保不同类别的语义特征具有唯一性。研究在类增量和跨数据集设置下均取得优异性能，并贡献了一个真实的海洋生物长尾数据集Marine112，为类别和领域增量学习提供了新的挑战与验证。
selection_source: fresh_fetch
motivation: 现有基于文本提示的持续学习方法难以学习到唯一的语义特征，导致新旧类别间语义重叠并引发灾难性遗忘。
method: 提出ProTPS方法，利用类别特定的视觉原型来指导文本提示的选择，从而增加训练灵活性并确保提示的唯一性。
result: 在类增量、跨数据集以及新提出的Marine112数据集上，ProTPS的性能均优于当前最先进的方法。
conclusion: ProTPS通过视觉原型引导机制有效缓解了持续学习中的遗忘问题，并为处理真实世界长尾分布的增量学习任务提供了有效方案。
---

## 摘要
在持续学习中，基于文本提示的方法利用文本编码器和可学习提示，为随时间顺序到达的类别编码语义特征。现有工作面临的一个共同挑战是如何学习唯一的文本提示，这些提示隐式地携带新类别的语义信息，从而使新到达类别的语义特征与已训练类别的语义特征不重叠，进而缓解灾难性遗忘问题。为了应对这一挑战，我们提出了一种名为“原型引导文本提示选择（ProTPS）”的新方法，旨在有意识地增加训练灵活性，从而鼓励学习唯一的文本提示。具体而言，我们的 ProTPS 学习特定类别的视觉原型和文本提示。视觉原型引导每个类别的文本提示的选择和学习。我们首先在类增量（CI）设置和跨数据集持续（CDC）学习设置中评估了 ProTPS。由于我们的 ProTPS 实现了接近上限的性能，我们进一步收集了一个包含 112 种海洋物种、跨度六年的真实世界数据集，命名为 Marine112，以为社区带来新的挑战。Marine112 真实地适用于类和域增量（CDI）学习设置，并且处于自然的长尾分布下。三种设置下的结果表明，我们的 ProTPS 表现优于近期的最先进方法。实现代码和 Marine112 数据集将在论文被接收后发布。

## Abstract
For continual learning, text-prompt-based methods leverage text encoders and learnable prompts to encode semantic features for sequentially arrived classes over time. A common challenge encountered by existing works is how to learn unique text prompts, which implicitly carry semantic information of new classes, so that the semantic features of newly arrived classes do not overlap with those of trained classes, thereby mitigating the catastrophic forgetting problem. To address this challenge, we propose a novel approach Prototype-guided Text Prompt Selection (ProTPS)'' to intentionally increase the training flexibility thus encouraging the learning of unique text prompts. Specifically, our ProTPS learns class-specific vision prototypes and text prompts. Vision prototypes guide the selection and learning of text prompts for each class. We first evaluate our ProTPS in both class incremental (CI) setting and cross-datasets continual (CDC) learning setting. Because our ProTPS achieves performance close to the upper bounds, we further collect a real-world dataset with 112 marine species collected over a span of six years, named Marine112, to bring new challenges to the community. Marine112 is authentically suited for the class and domain incremental (CDI) learning setting and is under natural long-tail distribution. The results under three settings show that our ProTPS performs favorably against the recent state-of-the-art methods. The implementation code and Marine112 dataset will be released upon the acceptance of our paper.

---

## 论文详细总结（自动生成）

### 论文总结：ProTPS —— 面向持续学习的原型引导文本提示选择

#### 1. 核心问题与研究动机
在持续学习（Continual Learning, CL）领域，基于文本提示（Text Prompt）的方法利用预训练模型（如 CLIP）的语义能力来处理不断增加的新类别。然而，现有方法面临的核心挑战是：**如何学习具有唯一性的文本提示**，以确保新旧类别的语义特征在特征空间中不发生重叠。如果提示学习不够独特，模型在学习新任务时会干扰旧知识，导致严重的“灾难性遗忘”。

#### 2. 方法论
论文提出了 **ProTPS (Prototype-guided Text Prompt Selection)**，其核心思想是利用视觉原型来引导文本提示的选择与优化，增加训练的灵活性。
*   **视觉原型（Vision Prototypes）**：为每个类别学习一个视觉原型向量，初始化为图像编码器提取的特征均值。在训练过程中进行微调（Refinement），并在后续任务中冻结已学到的原型以保持稳定性。
*   **原型引导的选择机制**：对于输入图像，模型计算其与所有视觉原型的余弦相似度，选择得分最高的原型所对应的文本提示。该提示随后与类别名称拼接，输入 CLIP 文本编码器。
*   **双分类器架构**：
    *   **视觉分类器**：基于视觉原型进行初步预测。
    *   **文本分类器**：基于学习到的文本提示生成语义特征作为分类权重。
*   **优化目标**：
    *   **分类损失 ($Loss_{c1}, Loss_{c2}$)**：分别针对视觉和文本分类器的交叉熵损失。
    *   **原型-提示对比损失 ($Loss_{pp}$)**：旨在对齐并增强视觉原型与文本提示之间的关联，使两者互补（视觉关注全局，文本关注局部细节）。
*   **采样策略**：在学习新任务时，随机采样旧类别的名称参与文本分类器的构建，在不增加显存负担的前提下，鼓励新提示学习到区分于旧类别的独特特征。

#### 3. 实验设计
*   **实验场景**：
    1.  **类增量学习 (CI)**：在 CIFAR100 和 ImageNet100 上进行 10 个任务的拆分。
    2.  **跨数据集持续学习 (CDC)**：评估模型在 ImageNet100 到 CIFAR100 之间的知识迁移能力。
    3.  **类与领域增量学习 (CDI)**：在作者新收集的 **Marine112** 数据集上测试。
*   **对比方法**：包括视觉提示方法（L2P, DualPrompt, CODA-P, HiDe-Prompt）和文本提示方法（CoOp, Continual-CLIP, AttriCLIP）。
*   **Benchmark**：以全量数据训练的 Linear Probe CLIP 作为性能上限（Upper Bound）。

#### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA V100 GPU**。
*   **训练配置**：每个任务训练 10 个 epoch，Batch Size 为 128。
*   **优化器**：采用 SGD，学习率从 0.001 按余弦退火降至 1e-4。
*   **模型骨干**：统一使用 CLIP 预训练的 ViT-L/14 作为图像编码器。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **多场景验证**：涵盖了 CI、CDC 和 CDI 三种主流持续学习设置。
*   **消融实验**：详细分析了原型学习策略（精炼 vs 冻结 vs 从零开始）、提示选择方法、采样策略以及对比损失函数对性能的影响。
*   **可视化分析**：通过 t-SNE 展现提示分布，通过 Grad-CAM 展示提示关注的图像区域，证明了方法能捕捉到局部细节。
*   **客观性**：实验结果取 3 次运行的平均值，对比基线均采用官方代码和推荐配置，实验设计较为公平、充分。

#### 6. 主要结论与发现
*   **性能领先**：ProTPS 在 ImageNet100 上比之前的 SOTA (AttriCLIP) 提升了 **10.6%**，在 CIFAR100 上提升了 **1.9%**。
*   **接近上限**：在多个任务中，ProTPS 的表现非常接近全量数据训练的上限，有效缓解了灾难性遗忘。
*   **真实场景适应性**：在具有长尾分布和领域偏移的 Marine112 数据集上，ProTPS 展现了极强的鲁棒性，领先幅度高达 **33.1%**。
*   **互补性**：视觉原型（全局特征）能有效引导文本提示（局部语义特征）的学习，双分类器融合效果最佳。

#### 7. 优点
*   **创新性**：引入视觉原型引导文本提示选择，打破了以往提示与类别一一对应的僵化关系，增加了训练灵活性。
*   **数据集贡献**：贡献了 **Marine112** 真实世界数据集，填补了社区在自然长尾、跨年度领域偏移增量学习数据上的空白。
*   **高效性**：无需存储大量旧样本（Rehearsal-free），额外参数量极小（仅增加约 105 KB）。

#### 8. 不足与局限
*   **骨干网络依赖**：高度依赖预训练的 CLIP 模型，若预训练模型本身对某些特定领域（如医学、卫星图像）表征不足，ProTPS 的效果可能会受限。
*   **选择偏差风险**：提示的选择依赖于视觉分类器的 Top-1 预测，如果视觉分类器在初始阶段预测错误，可能会引导错误的提示学习，虽然文中通过缩放因子 $\lambda$ 进行了缓解，但风险依然存在。
*   **计算开销**：虽然推理时参数量小，但在训练阶段需要通过文本编码器处理拼接后的提示，对于类别数量极大的场景，计算成本会有所增加。

（完）
