---
title: "Minimizing Targeted Activations: Input-Only Suppression of Evaluation-Awareness Latents in Large Language Models"
title_zh: 最小化目标激活：大语言模型中评估感知潜变量的仅限输入抑制
authors: "Deepanshu Mody, Samarth Agarwal, Utkarsh Mittal, Dipesh Mahato"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25907v1"
tags: ["query:bopl"]
score: 8.0
evidence: 通过令牌优化和正则化器优化流畅的提示词
tldr: 本研究探讨了如何仅通过优化输入提示词（而非直接修改模型内部参数）来抑制大语言模型中的特定内部潜变量，特别是与“评估感知”相关的变量。研究采用基于GCG的令牌优化和流畅度正则化方法，在Llama系列模型上成功抑制了多种目标特征。然而，结果表明内部特征的可读性并不等同于行为的可控性，抑制评估相关特征未必能改变模型的评估行为。
selection_source: fresh_fetch
motivation: 旨在研究是否能仅通过输入端优化来抑制模型内部的“评估感知”潜变量，以应对模型因意识到处于测试环境而改变行为的安全风险。
method: 结合GCG风格的令牌优化与自交叉熵流畅度正则化，在不直接干预推理过程的前提下优化输入提示词以最小化目标激活值。
result: 成功在Llama模型中抑制了SAE特征和CAA方向等潜变量，但发现抑制评估方向并不能有效改变模型在实际评估任务中的表现。
conclusion: 实验证明内部激活的可读性不代表行为的可控性，且随机方向的抑制也能产生类似的行为偏移，对当前的激活引导方法提出了警示。
---

## 摘要
激活引导（Activation steering）通过在推理时编辑内部激活来控制模型行为。我们研究了其在输入端的对偶问题：在不具备推理时模型访问权限的情况下，优化一个流畅的提示词，使选定的内部潜变量趋向于零。我们的目标是一个“评估感知”潜变量——在最近的研究中，该变量是线性可读且可引导的——如果模型在检测到自己正在接受测试时表现不同，对其进行控制将威胁到安全评估的有效性。我们通过引入负特征项（GCG 风格的 token 优化加上自交叉熵流畅度正则化项，并对流畅度权重进行扫描）改进了 Fluent Dreaming / EPO，在 Llama-3.2-3B 和 Llama-3.1-8B 上抑制了五种目标结构下的潜变量：CAA 方向、子空间范数、SAE 特征、单个 MLP 神经元以及行为逻辑值（logit）。实验表明，该潜变量可以被稳健地抑制（$z \approx -7$），并且一个经过因果验证的 Llama Scope SAE 特征可以被完全且选择性地关闭。但我们的对照实验揭示了关于 CAA 方向的警示性结论：安慰剂性质的随机方向同样能被强烈抑制并导致同等程度的行为偏移；此外，当我们在上下文中保留真实的评估段落并仅优化前缀时，抑制评估方向不仅未能减少、反而略微增加了模型的行为评估判断。简而言之，激活的可读性并不等同于行为的可控性。我们进一步发现，在两个模型规模上，单个 MLP 神经元都与评估相关但并非因果关系；此外，扫描真实的 Pile 数据集可以获得一个在内部方向上与优化器具有竞争力的自然文本基准。一个阳性对照验证了我们的擦除检测器，从而界定了早期悬而未决的“擦除与旋转”问题。

## Abstract
Activation steering controls model behavior by editing internal activations at inference time. We study its input-side dual: optimizing a fluent prompt so that a chosen internal latent is driven toward zero, with no inference-time model access. Our target is an "evaluation-awareness" latent-linearly readable and steerable in recent work-whose control would threaten the validity of safety evaluations if models behave differently when they detect being tested. Adapting Fluent Dreaming / EPO with a negated feature term (GCG-style token optimization plus a self-cross-entropy fluency regularizer, swept over a fluency weight), we suppress the latent under five target constructions-a CAA direction, a subspace norm, an SAE feature, a single MLP neuron, and a behavioral logit-on Llama-3.2-3B and Llama-3.1-8B. The latent is robustly suppressible ($z\approx-7$), and a causally-validated Llama Scope SAE feature can be fully and selectively turned off. But our controls tell a cautionary story about the CAA direction: a placebo random direction is suppressed just as hard and shifts behavior just as far, and when we hold a real eval passage in context and optimize only a prefix, suppressing the eval-direction fails to reduce-and slightly increases-the model's behavioral eval judgment. Activation-readability, in short, is not behavioral controllability. We further find that a single MLP neuron is eval-correlated but not causal at both scales, and that scanning the real Pile yields a natural-text baseline competitive with the optimizer for the internal direction. A positive control validates our erasure detector, bounding an erasure-vs-rotation question earlier left open.

---

## 论文详细总结（自动生成）

这是一份关于论文《Minimizing Targeted Activations: Input-Only Suppression of Evaluation-Awareness Latents in Large Language Models》的结构化技术总结：

### 1. 核心问题与整体含义
该研究探讨了大语言模型（LLM）中的**“评估感知”（Evaluation-Awareness）**现象，即模型能够识别出自己是否处于安全评估环境中，并可能因此伪装行为（如“沙盒化”或“对齐造假”）。
*   **研究动机**：现有的“激活引导”（Activation Steering）技术通常需要白盒权限在推理时修改内部激活值。本文研究其**输入端对偶问题**：能否仅通过优化输入提示词（Prompt），在不触碰模型内部参数的情况下，使特定的内部潜变量（如评估感知信号）最小化？
*   **核心意义**：如果攻击者或开发者能通过特定提示词“关闭”模型的评估感知，就能绕过安全检测，使模型在评估时表现正常，但在部署时表现异常。

### 2. 方法论
论文提出了一种**仅限输入的潜变量抑制方法**，将“特征梦境”（Fluent Dreaming/EPO）技术应用于特征最小化。
*   **核心思想**：利用离散令牌优化（GCG风格）寻找一个既能抑制目标激活值，又能保持语言流畅度和任务能力的提示词。
*   **关键技术细节**：
    *   **目标函数**：$Score(t) = -f(t) + \lambda_{xe}(-XE(t)) + \lambda_{kl}(-KL)$。其中 $f(t)$ 是目标激活值，$XE$ 是自交叉熵（保证流畅度），$KL$ 是散度约束（保证任务保留）。
    *   **优化算法**：采用 GCG（Greedy Coordinate Gradient）离散搜索，通过令牌梯度提议候选替换，再通过真实损失函数评估。
    *   **目标结构**：针对五种不同粒度的目标进行抑制：
        1.  **CAA方向**：类间均值差异方向。
        2.  **子空间范数**：多维判别子空间。
        3.  **SAE特征**：稀疏自编码器中的单义特征。
        4.  **单神经元**：MLP层中的特定单元。
        5.  **行为逻辑值（Logit）**：直接针对模型回答“是否为评估”的概率。

### 3. 实验设计
*   **模型**：Llama-3.2-3B-Instruct, Llama-3.1-8B-Instruct, 以及用于迁移测试的 Llama-3.2-1B。
*   **数据集/场景**：
    *   构建了**平衡对比集**（Balanced Contrast Set），解耦了“评估内容”与“正式语体”的混淆。
    *   使用了 **Llama Scope** 提供的 SAE 特征。
*   **基准方法（Baselines）**：
    *   **B0**：人工/模型改写（无梯度）。
    *   **B1**：随机令牌。
    *   **B2**：自然语料库（Pile数据集）扫描。
    *   **B3**：纯 GCG 优化（无流畅度约束）。
    *   **B4**：模型侧激活引导（作为效果上限）。

### 4. 资源与算力
*   论文未明确给出总的 GPU 小时数，但提到了具体的优化参数：
    *   迭代次数：120–150 次。
    *   Batch Size：3B 模型为 64，8B 模型为 48。
    *   使用了 **Transformer Lens** 库进行白盒梯度获取和激活读取。
    *   实验在 3B 和 8B 规模上运行，暗示使用了典型的学术级 GPU 集群（如 A100 或 H100）。

### 5. 实验数量与充分性
*   **实验规模**：所有核心实验均在 $n=4$ 个随机种子下重复，并报告了误差棒。
*   **充分性**：
    *   进行了跨模型规模（3B 到 1B）的迁移实验。
    *   设计了**安慰剂对照（Placebo Control）**：测试随机方向的抑制效果。
    *   设计了**上下文包装（Wrap）测试**：在保留真实评估文本的情况下优化前缀。
    *   **客观性**：作者不仅报告了成功的抑制，还诚实地指出了“激活抑制不等于行为控制”的负面结果，体现了极高的学术严谨性。

### 6. 主要结论与发现
1.  **抑制是可行的**：通过输入优化可以稳健地将目标激活降至零或负值（$z \approx -7$），甚至能完全关闭经过因果验证的 SAE 特征。
2.  **激活与行为脱节**：这是最重要的发现。尽管内部激活值下降了，但模型的行为判断（是否认为自己在受测）往往没有改变。
3.  **安慰剂效应**：抑制一个随机方向产生的行为偏移与抑制真实的“评估方向”几乎一致，说明简单的提示词优化产生的行为变化往往是非特异性的。
4.  **因果性缺失**：发现许多与评估高度相关的神经元仅是相关项而非因果项，关闭它们并不能改变模型行为。
5.  **自然文本的竞争力**：在 Pile 语料库中扫描出的自然文本，在抑制内部方向上的效果有时能媲美复杂的梯度优化器。

### 7. 优点
*   **视角独特**：首次系统研究了激活引导的“输入端对偶”问题。
*   **评估严谨**：引入了“擦除 vs 旋转”检测器和正交补探针，防止模型通过“作弊”（仅在特定维度隐藏信号）来欺骗评估者。
*   **批判性思维**：没有停留在“激活值下降”的表面成功，而是深入挖掘了其对实际行为的影响，对当前可解释性研究提出了警示。

### 8. 不足与局限
*   **流畅度不稳定性**：严格的流畅提示词生成依赖种子运气，并非每次优化都能达到 XE < 6 的标准。
*   **语体混淆**：尽管努力平衡，但“评估文本”与“正式语体”之间的关联仍难以完全剥离。
*   **任务范围**：主要集中在“评估感知”这一特定潜变量，尚未验证该方法在其他安全相关特征（如拒绝诱导）上的普适性。
*   **上下文失效**：在“前缀优化+固定正文”的实战场景下，该方法未能成功控制模型行为，甚至产生了反效果。

（完）
