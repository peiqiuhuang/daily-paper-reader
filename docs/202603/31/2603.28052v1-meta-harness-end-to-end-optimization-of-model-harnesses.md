---
title: "Meta-Harness: End-to-End Optimization of Model Harnesses"
title_zh: Meta-Harness：模型 Harness 的端到端优化
authors: "Yoonho Lee, Roshen Nair, Qizheng Zhang, Kangwook Lee, Omar Khattab, Chelsea Finn"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28052v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于自动化Harness和提示优化的外环系统
tldr: 大语言模型系统的性能不仅取决于权重，还取决于处理信息的“harness”代码。目前这类代码多为人工设计，且现有优化器效果有限。本文提出 Meta-Harness，一个通过代理提议者在文件系统中搜索和优化 harness 代码的端到端系统。它能访问历史候选代码、得分和执行轨迹，在文本分类、数学推理和智能体编程等任务中显著提升了性能并降低了成本，实现了自动化的 harness 工程。
selection_source: fresh_fetch
motivation: 现有的 LLM 系统 harness 代码主要依靠人工设计，且传统文本优化器因反馈压缩过度而难以胜任复杂的代码优化任务。
method: 提出一种名为 Meta-Harness 的外环优化系统，利用代理提议者通过访问文件系统中的源代码、评分和执行轨迹来搜索最优 harness 代码。
result: 在文本分类中提升 7.7 分且节省 4 倍 Token，在数学推理中提升 4.7 分，并在智能体编程任务中超越了最佳人工设计基准。
conclusion: 实验证明，通过赋予优化系统更丰富的历史经验访问权限，可以实现高效且自动化的 LLM 应用 harness 工程。
---

## 摘要
大语言模型（LLM）系统的性能不仅取决于模型权重，还取决于其 Harness：即决定存储、检索以及向模型呈现哪些信息的代码。然而，Harness 目前仍主要由人工设计，且现有的文本优化器与该场景匹配度较差，因为它们对反馈的压缩过于激进。我们推出了 Meta-Harness，这是一个为 LLM 应用搜索 Harness 代码的外环系统。它使用一个代理式提案器（agentic proposer），通过文件系统访问所有先前候选方案的源代码、评分和执行轨迹。在在线文本分类任务中，Meta-Harness 相比最先进的上下文管理系统提升了 7.7 个百分点，同时使用的上下文 Token 减少了 4 倍。在检索增强的数学推理任务中，单个发现的 Harness 在五个留出模型上的 200 道 IMO 级别题目上的平均准确率提升了 4.7 个百分点。在代理式编程任务中，发现的 Harness 在 TerminalBench-2 上超过了最佳的人工设计基准。综上所述，这些结果表明，更丰富地利用先验经验可以实现自动化的 Harness 工程。

## Abstract
The performance of large language model (LLM) systems depends not only on model weights, but also on their harness: the code that determines what information to store, retrieve, and present to the model. Yet harnesses are still designed largely by hand, and existing text optimizers are poorly matched to this setting because they compress feedback too aggressively. We introduce Meta-Harness, an outer-loop system that searches over harness code for LLM applications. It uses an agentic proposer that accesses the source code, scores, and execution traces of all prior candidates through a filesystem. On online text classification, Meta-Harness improves over a state-of-the-art context management system by 7.7 points while using 4x fewer context tokens. On retrieval-augmented math reasoning, a single discovered harness improves accuracy on 200 IMO-level problems by 4.7 points on average across five held-out models. On agentic coding, discovered harnesses surpass the best hand-engineered baselines on TerminalBench-2. Together, these results show that richer access to prior experience can enable automated harness engineering.

---

## 论文详细总结（自动生成）

以下是对论文《Meta-Harness: End-to-End Optimization of Model Harnesses》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机：** 大语言模型（LLM）系统的性能不仅取决于模型权重，还高度依赖于其“Harness”（即围绕模型的代码，负责检索、存储、上下文管理和提示词构建）。
*   **背景：** 目前 Harness 的设计主要依靠人工迭代（观察失败、调整启发式方法），效率低下。现有的自动化文本优化器（如 OPRO, TextGrad）在处理 Harness 优化时效果不佳，因为它们通常只利用标量分数或简短摘要作为反馈，忽略了长程推理中的关键诊断信息（执行轨迹）。
*   **核心目标：** 实现 Harness 工程的自动化，通过一个能够理解代码和执行日志的代理系统，搜索最优的端到端 Harness 实现。

### 2. 方法论
*   **核心思想：** 提出 **Meta-Harness**，一个“优化 Harness 的 Harness”。它将优化过程建模为一个外环搜索任务，由一个具备编程能力的代理（Coding Agent）担任提议者（Proposer）。
*   **关键技术细节：**
    *   **文件系统经验库：** 系统维护一个文件系统，存储所有历史候选 Harness 的源代码、评估得分以及详尽的**执行轨迹（Execution Traces）**（包括提示词、模型输出、工具调用等）。
    *   **代理提议者（Proposer）：** 使用 Claude Code (Opus-4.6)。它不只是生成代码，而是作为一个智能体，利用 `grep`、`cat` 等终端工具主动检索文件系统中的历史经验，分析失败原因并提出改进。
    *   **搜索流程：** 
        1. 提议者阅读历史记录，形成失败诊断假设。
        2. 提议者编写/修改 Python 格式的 Harness 代码。
        3. 系统在搜索集上评估该 Harness。
        4. 将代码、得分和轨迹存回文件系统，更新 Pareto 前沿。
*   **因果推理：** 提议者能够通过对比不同版本的执行轨迹，识别出哪些改动是有效的，哪些是由于提示词冲突导致的“混淆因素”。

### 3. 实验设计
*   **实验场景与数据集：**
    1.  **在线文本分类：** 使用 LawBench、Symptom2Disease、USPTO-50k。
    2.  **检索增强数学推理：** 针对 200 道 IMO 级别的难题，从 53.5 万道题目的语料库中检索。
    3.  **智能体编程：** 使用 TerminalBench-2 基准测试。
*   **对比方法（Baselines）：**
    *   **人工设计：** ACE (Agentic Context Engineering)、MCE、Terminus-KIRA 等。
    *   **自动化优化器：** OpenEvolve、TTT-Discover、GEPA、Best-of-N。
    *   **基础方案：** Zero-shot、Few-shot。
*   **跨模型验证：** 在数学任务中，使用一个模型发现的 Harness 在五个未见过的模型（如 GPT-5.4, Gemini-3）上进行测试。

### 4. 资源与算力
*   **算力消耗：** 文中未明确给出具体的 GPU 显存或核心小时数。
*   **评估成本：** 提到一次典型的搜索运行包含约 20 次迭代，共评估约 60 个候选 Harness。在文本分类任务中，单次运行通常在“几小时墙钟时间”内完成。
*   **模型使用：** 提议者固定为 Claude Code (Opus-4.6)，评估模型包括 GPT-OSS-120B/20B 和 Claude Haiku 4.5 等。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了分类、数学、编程三个完全不同的领域，证明了方法的通用性。
*   **消融实验：** 专门对比了“仅分数”、“分数+摘要”与“Meta-Harness（全量轨迹）”的效果，有力证明了原始执行轨迹在诊断中的不可替代性。
*   **泛化性测试：** 在 9 个分布外（OOD）分类数据集上进行了测试，并进行了跨模型迁移实验。
*   **客观性：** 采用了 Pareto 前沿分析（准确率 vs. Token 成本），而非单一指标，评估视角全面。

### 6. 主要结论与发现
*   **性能超越人工：** 在文本分类中比最强人工基准 ACE 提升 7.7 分，且 Token 消耗降低 4 倍；在 TerminalBench-2 上排名第一（Haiku 4.5 级别）。
*   **搜索效率极高：** 在达到相同性能的情况下，Meta-Harness 的评估次数仅为现有优化器（如 OpenEvolve）的 1/10。
*   **轨迹的重要性：** 访问原始执行轨迹是性能飞跃的核心，压缩反馈（如摘要）会丢失 10 点以上的准确率。
*   **代码空间的优势：** 在代码空间搜索比在提示词空间搜索更具鲁棒性，生成的算法具有更好的可解释性和迁移性。

### 7. 优点
*   **非马尔可夫优化：** 提议者能回顾整个搜索历史，而非仅基于上一个版本，这使其能进行复杂的因果分析。
*   **端到端灵活性：** 能够自动发现复杂的控制流（如数学任务中的四路径路由检索逻辑）。
*   **可解释的产出：** 最终产出的是人类可读的 Python 代码，便于部署和进一步微调。
*   **高效的 Pareto 优化：** 能够根据需求自动权衡性能与推理成本。

### 8. 不足与局限
*   **提议者依赖：** 该方法高度依赖于顶尖编程代理（如 Claude Code）的能力，较弱的开源模型可能无法有效执行复杂的文件系统诊断。
*   **API 成本：** 虽然评估次数较少，但由于提议者需要读取大量历史文件，单次提议的 Token 输入量较大（可达 10M Token 级别）。
*   **过拟合风险：** 在 TerminalBench-2 等小型基准上，虽然进行了审计，但仍存在针对特定测试环境进行过度优化的潜在风险。
*   **实时性限制：** 搜索过程仍需数小时，无法做到瞬时的在线自适应。

（完）
