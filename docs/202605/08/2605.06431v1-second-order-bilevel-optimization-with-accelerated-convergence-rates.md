---
title: Second-Order Bilevel Optimization with Accelerated Convergence Rates
title_zh: 具有加速收敛率的二阶双层优化
authors: "Sheng Yang, Chengchang Liu, Lesi Chen, John C. S. Lui"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06431v1"
tags: ["query:bopl"]
score: 10.0
evidence: 非凸-强凸双层优化的二阶方法
tldr: "本文研究非凸-强凸双层优化问题，提出了一种全新的全二阶双层近似方法（FSBA）。该方法在寻找超目标函数的二阶驻点时，实现了 $\\tilde{\\mathcal{O}}(\\epsilon^{-1.5})$ 的迭代复杂度，证明了二阶方法比一阶方法具有更快的收敛速度。为降低计算开销，作者进一步提出了延迟更新变体 LFSBA，通过复用二阶信息将计算复杂度降低了 $\\sqrt{d}$ 倍，并将其扩展至极小极大优化问题。"
selection_source: fresh_fetch
motivation: 旨在解决双层优化中一阶方法收敛速度较慢以及现有二阶方法计算开销过大的挑战。
method: 提出 FSBA 方法及其延迟更新版本 LFSBA，利用二阶导数信息和二阶信息重用策略来加速收敛并降低计算成本。
result: "FSBA 实现了 $\\tilde{\\mathcal{O}}(\\epsilon^{-1.5})$ 的迭代复杂度，且 LFSBA 在计算复杂度上比 FSBA 提升了 $\\sqrt{d}$ 倍。"
conclusion: 研究证明了二阶方法在双层优化中具有更优的收敛率，且通过延迟更新策略可以显著提高计算效率。
---

## 摘要
本文研究了针对非凸-强凸双层优化的二阶方法。我们提出了一种新颖的全二阶双层逼近方法（FSBA），在寻找超目标函数的 $(ε, \mathcal{O}(\sqrtε))$ 二阶驻点时，实现了 $\tilde{\mathcal{O}}(ε^{-1.5})$ 的迭代复杂度。我们的结果表明，在双层优化中，二阶方法可以实现比一阶方法更快的加速收敛率。为了解决与二阶算子相关的沉重计算开销，我们引入了 FSBA 的一种延迟变体，称为 LFSBA，它在多次迭代中重用二阶信息。我们证明了 LFSBA 的计算复杂度优于 FSBA，提升因子为 $\sqrt{d}$，其中 $d$ 是问题的维度。我们还将类似的思想应用于非凸-强凹极大极小优化，并提出了延迟极大极小三次正则化牛顿（LMCN）方法，与现有的二阶方法相比，该方法具有更好的计算复杂度。

## Abstract
This paper studies second-order methods for nonconvex-strongly-convex bilevel optimization. We propose a novel fully second-order bilevel approximation method (FSBA) that achieves an iteration complexity of $\tilde{\mathcal{O}}(ε^{-1.5})$ for finding the $(ε, \mathcal{O}(\sqrtε))$ second-order stationary point of the hyper-objective function. Our results demonstrate that second-order methods can achieve an accelerated convergence rate than first-order methods in bilevel optimization. To address the heavy computational cost associated with the second-order oracle, we introduce a lazy variant of FSBA, called LFSBA, which reuses second-order information across several iterations. We prove that LFSBA exhibits better computational complexity than FSBA by a factor of $\sqrt{d}$, where $d$ is the dimension of the problem. We also apply a similar idea to nonconvex strongly-concave minimax optimization and propose the lazy minimax cubic-regularized Newton (LMCN) method with better computational complexity compared to existing second-order methods.

---

## 论文详细总结（自动生成）

这篇论文《Second-Order Bilevel Optimization with Accelerated Convergence Rates》深入探讨了非凸-强凸双层优化问题的二阶求解方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：双层优化（Bilevel Optimization）在超参数调优、神经架构搜索、元学习和强化学习中应用广泛。
*   **核心问题**：现有一阶方法（如 AID, ITD）在处理非凸超目标函数时，迭代复杂度通常为 $\mathcal{O}(\epsilon^{-2})$ 或 $\mathcal{O}(\epsilon^{-1.75})$。尽管二阶信息理论上能加速收敛，但由于计算超海森矩阵（Hyper-Hessian）涉及三阶导数，计算成本极高。
*   **研究动机**：探索二阶方法是否能在双层优化中实现更优的收敛率（如接近 $\epsilon^{-1.5}$ 的理论下界），并解决二阶算子带来的沉重计算负担。

### 2. 论文提出的方法论
论文提出了三种主要算法：
*   **FSBA (Fully Second-Order Bilevel Approximation)**：
    *   **核心思想**：利用拉格朗日惩罚函数 $L^*_\lambda(x)$ 作为超目标函数的代理。
    *   **技术细节**：通过加速梯度下降（AGD）求解下层变量的近似值，进而估计超梯度和超海森矩阵。它采用**三次正则化牛顿法（Cubic-Regularized Newton）**进行更新。
    *   **复杂度**：实现了 $\tilde{\mathcal{O}}(\epsilon^{-1.5})$ 的迭代复杂度，达到了非凸优化下的近乎最优速率。
*   **LFSBA (Lazy FSBA)**：
    *   **核心思想**：引入“延迟海森矩阵（Lazy Hessian）”策略。
    *   **技术细节**：算法仅在特定的“快照”点计算海森矩阵，并在随后的 $m$ 次迭代中复用该信息。
    *   **优势**：在保持收敛率的同时，将计算复杂度降低了 $\sqrt{d}$ 倍（$d$ 为维度）。
*   **IFSBA (Inexact FSBA)**：
    *   **技术细节**：针对大规模问题，利用**切比雪夫多项式（Chebyshev Polynomials）**近似海森矩阵的逆，并结合海森矩阵-向量乘积（HVP）避免显式构建海森矩阵。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **合成极大极小问题**：验证算法在处理鞍点时的基本性能。
    2.  **数据超清洗（Data Hypercleaning）**：使用 Breast-cancer, Australian, MNIST 数据集。
    3.  **超参数调优**：在 20 Newsgroups 数据集上进行逻辑回归超参数优化。
    4.  **小样本元学习（Few-Shot Meta-Learning）**：在 miniImageNet 和 FC100 数据集上进行 5-way 5-shot 实验。
*   **对比方法（Benchmarks）**：包括 AID-BiO, ITD-BiO, F2BA（最强一阶基准）, PZOBO, qNBO, RAHGD 等。

### 4. 资源与算力
*   **硬件配置**：服务器配备 Intel Xeon Platinum 8352V CPU (16 vCPUs, 120GB RAM) 和 **NVIDIA RTX 4090 GPU (24GB VRAM)**。
*   **软件环境**：PyTorch 2.5.1, Python 3.12, CUDA 12.4。
*   **训练时长**：文中未明确给出总训练时长，但通过实验图表的横轴（Running Time）可以看出，对比实验通常在数百秒内完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成数据到真实世界复杂任务（元学习）的四类实验。
*   **消融实验**：针对延迟更新频率 $m$、惩罚因子 $\lambda$ 和正则化参数 $M$ 进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分，不仅对比了收敛精度，还对比了运行时间（Wall-clock time）和算子调用次数，客观地展示了二阶方法在计算效率上的优势。

### 6. 主要结论与发现
*   **收敛加速**：FSBA 确实实现了比一阶方法更快的 $\tilde{\mathcal{O}}(\epsilon^{-1.5})$ 迭代复杂度。
*   **延迟策略有效性**：LFSBA 通过复用海森矩阵，显著降低了高维问题的计算开销，且在实验中表现出比频繁更新海森矩阵更好的时间效率。
*   **二阶驻点**：该方法能有效找到二阶驻点（SOSP），在逃离鞍点方面优于传统一阶双层优化算法。
*   **通用性**：提出的延迟更新思想成功扩展到了极大极小优化问题（LMCN 算法），并取得了更好的复杂度。

### 7. 优点
*   **理论突破**：首次在非凸-强凸双层优化中证明了二阶方法可以达到 $\epsilon^{-1.5}$ 的加速收敛率。
*   **计算优化**：通过 Lazy Hessian 和切比雪夫近似，解决了二阶方法在实际应用中“理论快、计算慢”的痛点。
*   **实用性强**：IFSBA 变体不需要显式计算海森矩阵逆，使其能够处理大规模深度学习任务。

### 8. 不足与局限
*   **假设限制**：理论分析仍依赖于下层问题的**强凸性**假设，对于下层是非凸或一般凸的问题，该方法的适用性尚未证明。
*   **超参数敏感性**：虽然消融实验显示了一定的鲁棒性，但三次正则化参数 $M$ 和惩罚项 $\lambda$ 的选择仍可能影响不同任务下的表现。
*   **高阶导数依赖**：尽管避免了三阶导数，但仍需二阶导数（或 HVP），在某些无法获取二阶信息的黑盒优化场景中无法使用。

（完）
