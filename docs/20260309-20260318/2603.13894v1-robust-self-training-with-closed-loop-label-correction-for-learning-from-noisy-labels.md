---
title: Robust Self-Training with Closed-loop Label Correction for Learning from Noisy Labels
title_zh: 针对噪声标签学习的闭环标签纠正鲁棒自训练
authors: "Zhanhui Lin, Yanlin Liu, Sanping Zhou"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13894v1"
tags: ["query:bopl"]
score: 9.0
evidence: 用于标签纠错的解耦双层优化
tldr: 针对深度学习中噪声标签导致性能下降的问题，本文提出一种基于解耦双层优化的自训练标签校正框架。该方法通过分类器与神经校正函数的协同演化，利用少量干净数据和中间特征进行知识迁移，构建闭环反馈系统以防止误差放大。实验证明该方法在保持高效率的同时，在多个基准数据集上达到了SOTA性能，具有较强的实用性。
selection_source: fresh_fetch
motivation: 现有处理噪声标签的方法往往存在样本利用效率低和计算开销过大的局限性。
method: 提出一种解耦双层优化框架，利用神经校正函数与分类器的协同演化及闭环反馈机制来修正噪声标签。
result: 在CIFAR和Clothing1M等基准数据集上，该方法在显著减少训练时间的同时取得了当前最先进的分类精度。
conclusion: 该研究通过理论支撑和实验验证，证明了闭环自训练机制在处理大规模噪声数据时的稳健性和高效性。
---

## 摘要
使用噪声标签训练深度神经网络仍然是一个重大挑战，通常会导致性能下降。现有的处理标签噪声的方法通常依赖于转移矩阵、噪声检测或元学习技术，但它们往往表现出噪声样本利用效率低且计算成本高的问题。在本文中，我们提出了一种使用解耦双层优化的自训练标签纠正框架，其中分类器和神经纠正函数共同演化。利用一个小型干净数据集，我们的方法采用噪声后验模拟和中间特征来传递真实标签（ground-truth）知识，形成一个防止误差放大的闭环反馈系统。理论保证支撑了我们方法的稳定性，在 CIFAR 和 Clothing1M 等基准数据集上的广泛实验证实了其在缩短训练时间的同时达到了最先进的性能，突显了其在噪声标签学习中的实际应用价值。

## Abstract
Training deep neural networks with noisy labels remains a significant challenge, often leading to degraded performance. Existing methods for handling label noise typically rely on either transition matrix, noise detection, or meta-learning techniques, but they often exhibit low utilization efficiency of noisy samples and incur high computational costs. In this paper, we propose a self-training label correction framework using decoupled bilevel optimization, where a classifier and neural correction function co-evolve. Leveraging a small clean dataset, our method employs noisy posterior simulation and intermediate features to transfer ground-truth knowledge, forming a closed-loop feedback system that prevents error amplification. Theoretical guarantees underpin the stability of our approach, and extensive experiments on benchmark datasets like CIFAR and Clothing1M confirm state-of-the-art performance with reduced training time, highlighting its practical applicability for learning from noisy labels.

---

## 论文详细总结（自动生成）

以下是对论文《Robust Self-Training with Closed-loop Label Correction for Learning from Noisy Labels》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度神经网络（DNN）在含有噪声标签的大规模数据集上训练时，容易过度拟合噪声而非学习潜在模式，导致泛化性能下降。
*   **研究动机**：
    *   **现有方法局限**：转移矩阵法难以处理复杂的实例相关噪声；元学习方法（如双层优化）计算开销巨大且内存占用高；样本选择法（如丢弃噪声样本）导致数据利用率低。
    *   **研究背景**：虽然完全干净的数据集稀缺，但获取少量（如1%-10%）经过验证的干净数据是可行的。本文旨在利用这部分少量干净数据，构建一个高效、稳定且能处理复杂噪声的标签纠正框架。

### 2. 方法论：核心思想与技术细节
本文提出了一种基于**解耦双层优化**的闭环自训练框架，核心流程如下：
*   **协同演化机制**：框架包含一个主分类器 $f$ 和一个神经纠正函数 $r$（通常为浅层 MLP）。两者交替优化：纠正函数利用干净数据学习如何修复噪声标签，分类器则在修复后的标签上进行训练。
*   **噪声后验模拟**：为了在没有“噪声-干净”对的情况下训练纠正函数，引入一个辅助的“噪声预测头” $f_n$，在噪声数据集上模拟噪声分布 $p(\tilde{y}|x)$，并将其应用于干净数据集以生成合成的噪声标签。
*   **中间特征利用**：纠正函数 $r$ 不仅输入预测概率，还输入分类器的**中间特征表示**。这利用了“早期学习”现象，即中间特征比最终输出包含更稳定的语义信息。
*   **鲁棒凸组合（Robust Convex Combination）**：为了防止误差放大，最终的纠正标签是历史预测值的加权组合。权重通过在干净验证集上最小化风险来动态优化。
*   **闭环反馈**：干净数据作为“锚点”提供反馈，引导标签纠正，防止自训练过程中的漂移。

### 3. 实验设计
*   **数据集**：
    *   **CIFAR-10 / CIFAR-100**：人工注入对称噪声（Symmetric）、非对称噪声（Asymmetric）和实例相关噪声（Instance-dependent），噪声比例从 0.2 到 0.6。
    *   **Clothing1M**：包含 100 万张图片的真实世界大规模噪声数据集。
*   **对比基准（Benchmarks）**：
    *   转移矩阵类：FasTEN。
    *   元学习类：MW-Net、L2B、EMLC。
    *   其他：标准交叉熵（CE）、DMLP。
*   **实验设置**：在 CIFAR 中通常使用 5000 个干净样本（9:1 比例），并额外测试了仅 1000 个干净样本（49:1 比例）的极端情况。

### 4. 资源与算力
*   **硬件环境**：实验主要在单张 **NVIDIA RTX 4090 GPU** 上完成。
*   **训练时长**：
    *   **CIFAR-100**：总训练时间约为 **0.35 小时**，相比元学习方法 L2B（1.61 小时）快了约 4.6 倍。
    *   **Clothing1M**：每轮（Epoch）训练耗时约 **0.55 小时**，内存占用（16.9GB）远低于 EMLC（需两张显卡共约 44GB）。
*   **效率优势**：由于采用了解耦优化而非复杂的二阶导数计算，该方法在计算和内存效率上具有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 2 个合成噪声数据集和 1 个大型真实噪声数据集。
    *   针对 3 种噪声类型、3 种噪声水平进行了详尽测试。
    *   **消融实验**：验证了中间特征输入的有效性、凸组合机制的必要性、损失平衡因子 $\lambda$ 的敏感性以及纠正频率的影响。
*   **充分性评价**：实验设计非常充分，不仅对比了 SOTA 方法，还探讨了在极少量干净数据下的鲁棒性，证明了其在不同场景下的普适性。

### 6. 主要结论与发现
*   **性能领先**：在所有测试场景下，该方法均达到了最先进（SOTA）的准确率，尤其在复杂的实例相关噪声下优势明显。
*   **稳定性强**：通过理论证明（Proposition 1），凸组合机制保证了预期风险是非增的，有效防止了自训练中的模型崩溃。
*   **特征价值**：实验证明，使用中间特征作为纠正函数的输入远优于仅使用最终层的 Logits 或伪标签。
*   **高效性**：该方法在保持高精度的同时，大幅降低了训练时间和显存需求，更适合大规模工业应用。

### 7. 亮点与优点
*   **解耦优化**：巧妙地避开了元学习中昂贵的内循环更新，实现了高性能与高效率的平衡。
*   **理论支撑**：为标签纠正的稳定性提供了数学证明，而非纯启发式设计。
*   **闭环设计**：通过干净数据的持续反馈，解决了传统自训练方法中常见的误差累积问题。
*   **轻量级模块**：纠正函数仅为 2 层 MLP，计算开销几乎可以忽略不计（仅占总时间的 1%）。

### 8. 不足与局限
*   **依赖干净子集**：虽然需求量小，但仍必须依赖一个完全干净且具有代表性的验证集。在某些极端领域，获取绝对干净的数据可能依然困难。
*   **超参数敏感性**：尽管消融实验显示 $\lambda$ 影响较小，但在不同数据集上，标签纠正的频率和学习率等超参数仍需精细调优。
*   **类别平衡假设**：论文未深入讨论在长尾分布（类别极度不平衡）且含有噪声的情况下的表现。

（完）
