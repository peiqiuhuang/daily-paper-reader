---
title: Plug-and-play Class-aware Knowledge Injection for Prompt Learning with Visual-Language Model
title_zh: 视觉语言模型提示学习中的即插即用类感知知识注入
authors: "Junhui Yin, Nan Pu, Xinyu Zhang, Lingfeng Yang, Lin Wu, Xiaojie Wang, Zhun Zhong"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05910v1"
tags: ["query:bopl"]
score: 8.0
evidence: 提示学习中的类别感知知识注入
tldr: 针对视觉语言模型提示学习中忽略类别特定知识的问题，本文提出了一种即插即用的类别感知知识注入（CAKI）框架。该框架通过构建类别级知识库并利用查询-键匹配机制，为测试实例注入细粒度的类别知识，有效弥补了现有方法在处理类别间区分度和类别内一致性方面的不足，显著提升了模型在基础和新类别上的分类性能。
selection_source: fresh_fetch
motivation: 现有的提示学习方法因缺乏细粒度的类别特定知识，导致在区分不同类别或维持同类一致性时表现欠佳。
method: 提出CAKI框架，通过从少样本中编码类别知识构建知识库，并利用查询-键匹配机制为测试实例检索并注入相关知识。
result: 大量实验表明，CAKI作为即插即用组件，能有效增强现有提示学习方法在基础类和新类上的识别准确率。
conclusion: 通过即插即用的方式注入类别感知知识，是优化视觉语言模型下游任务性能的一种高效且通用的策略。
---

## 摘要
提示学习已成为增强 CLIP 等视觉语言模型（VLM）以应对各种下游任务的有效且广泛使用的技术，特别是在特定领域的零样本分类中。现有方法通常侧重于为给定领域学习类共享（class-shared）提示，或通过条件提示学习生成实例特定（instance-specific）提示。虽然这些方法取得了显著的性能，但它们在提示设计中往往忽略了类特定（class-specific）知识，导致结果不尽如人意。其根本原因在于：1）与粗粒度的类共享提示相比，类特定提示提供了更细粒度的监督，有助于防止将不同类别的数据误分类为同一类；2）与类特定提示相比，实例特定提示忽略了跨多个实例的更丰富的类级信息，可能导致同一类别的数据被划分为多个类别。为了有效地将类特定知识补充到现有方法中，我们提出了一种即插即用的类感知知识注入（CAKI）框架。CAKI 包含两个关键组件，即类特定提示生成和查询-键（query-key）提示匹配。前者将属于同一类别的少样本（few-shot）示例中的类特定知识编码到提示中，并将学习到的提示存储在类级知识库中。后者为每个测试实例提供了一种即插即用机制，用于从知识库中检索相关的类级知识，并注入此类知识以优化模型预测。大量实验表明，我们的 CAKI 有效地提高了现有方法在基础类和新类上的性能。代码已在 https://github.com/yjh576/CAKI 公开。

## Abstract
Prompt learning has become an effective and widely used technique in enhancing vision-language models (VLMs) such as CLIP for various downstream tasks, particularly in zero-shot classification within specific domains. Existing methods typically focus on either learning class-shared prompts for a given domain or generating instance-specific prompts through conditional prompt learning. While these methods have achieved promising performance, they often overlook class-specific knowledge in prompt design, leading to suboptimal outcomes. The underlying reasons are: 1) class-specific prompts offer more fine-grained supervision compared to coarse class-shared prompts, which helps prevent misclassification of data from different classes into a single class; 2) compared to class-specific prompts, instance-specific prompts neglect the richer class-level information across multiple instances, potentially causing data from the same class to be divided into multiple classes. To effectively supplement the class-specific knowledge into existing methods, we propose a plug-and-play Class-Aware Knowledge Injection (CAKI) framework. CAKI comprises two key components, i.e., class-specific prompt generation and query-key prompt matching. The former encodes class-specific knowledge into prompts from few-shot samples that belong to the same class and stores the learned prompts in a class-level knowledge bank. The latter provides a plug-and-play mechanism for each test instance to retrieve relevant class-level knowledge from the knowledge bank and inject such knowledge to refine model predictions. Extensive experiments demonstrate that our CAKI effectively improves the performance of existing methods on base and novel classes. Code is publicly available at \href{https://github.com/yjh576/CAKI}{this https URL}.

---

## 论文详细总结（自动生成）

### 论文详细总结：CAKI 框架

#### 1. 论文的核心问题与整体含义
*   **研究背景**：视觉语言模型（如 CLIP）在下游任务中表现优异，但全量微调成本高。提示学习（Prompt Learning）成为一种高效的适配方案。
*   **核心问题**：现有的提示学习方法存在两极分化：
    1.  **类共享提示（Class-shared）**：粒度太粗，容易将不同类别的数据混淆。
    2.  **实例特定提示（Instance-specific）**：缺乏类级别的全局信息，容易导致同类数据被误分为多类。
*   **研究动机**：论文旨在引入“类特定知识”（Class-specific knowledge），通过即插即用的方式，在不改变现有模型架构的前提下，提升模型对细粒度类别的区分能力和泛化性能。

#### 2. 论文提出的方法论
CAKI（Class-Aware Knowledge Injection）框架主要由两个核心组件构成：
*   **类特定提示生成（CSPG）**：
    *   利用少样本（Few-shot）训练数据，为每个类别单独学习一个提示向量（Value）。
    *   将该类别的文本特征作为键（Key），构建一个“键-值”对知识库。
    *   该过程将类级别的先验知识编码进提示中。
*   **查询-键提示匹配（QKPM）**：
    *   **粗粒度预测**：在测试阶段，先利用现有方法（如 CoOp）得到初步预测。
    *   **知识检索**：将测试图像特征作为查询（Query），从知识库中检索 Top-K 个最相关的类特定提示。
    *   **细粒度修正**：将检索到的提示注入模型，生成多个细粒度预测，并根据匹配分数进行加权融合，最终修正原始的粗粒度预测。
*   **即插即用**：该框架可以无缝集成到 CoOp、CoCoOp、MaPLe 等多种主流提示学习方法中。

#### 3. 实验设计
*   **数据集**：涵盖 10 个主流视觉识别数据集，包括 Caltech-101（物体）、Oxford-Pets（宠物）、Stanford Cars（汽车）、Oxford-Flowers102（花卉）、Food-101（食物）、FGVC Aircraft（飞机）、EuroSAT（卫星图）、SUN-397（场景）、DTD（纹理）和 UCF-101（动作）。
*   **Benchmark 设置**：
    1.  **基础类到新类泛化**：在基础类训练，测试对未见类别的泛化能力。
    2.  **少样本学习**：评估在极少标注数据下的学习能力。
    3.  **领域泛化**：在 ImageNet 训练，在具有领域偏移的数据集上测试。
    4.  **跨任务迁移**：验证在语义分割和目标检测任务上的有效性。
*   **对比方法**：对比了 CoOp, CoCoOp, MaPLe, PromptSRC, TCP, GalLoP, TPT 等多种 SOTA 方法。

#### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 3090 GPU (24 GB)**。
*   **训练细节**：使用 AdamW 优化器，学习率 0.005，训练 5 个 epoch，batch size 为 1。
*   **效率表现**：论文明确给出了时间开销，例如在 Flower102 数据集上，102 个类别的训练仅需约 1.5 分钟，单张图像推理时间（K=3 时）约为 0.146 秒。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 10 个数据集上进行了详尽测试，涵盖了 1-shot、4-shot 和 16-shot 多种设置。
*   **消融实验**：针对 CSPG 和 QKPM 的有效性、提示匹配策略、超参数（权重 $\beta$、检索数 $K$、温度系数 $\tau$）、缓存大小等进行了多组消融实验。
*   **客观性**：所有实验均运行 3 次并报告均值与标准差，对比基准统一，实验设计严谨，充分验证了方法在不同场景下的鲁棒性。

#### 6. 主要结论与发现
*   **性能提升**：CAKI 作为即插即用组件，显著提升了所有基准方法的性能（例如在 CoOp 基础上，多个数据集的谐波平均值提升了 1%-3% 以上）。
*   **知识互补**：类特定知识能有效弥补类共享提示和实例特定提示的不足，增强了模型对视觉相似类别的区分度。
*   **通用性强**：不仅在分类任务上有效，在语义分割和目标检测等下游任务中也表现出良好的迁移能力。

#### 7. 优点
*   **即插即用**：无需重新训练原始 VLM，也不改变现有提示学习方法的训练流程，适配性极高。
*   **高效简洁**：训练极快，存储开销随类别数线性增长，处于可接受范围。
*   **细粒度修正**：通过检索机制实现了“粗到细”的预测优化，逻辑清晰且符合直觉。

#### 8. 不足与局限
*   **依赖初始预测**：如果粗粒度模型（用于匹配的 Query）预测偏差过大，检索到的类特定提示可能引入噪声。
*   **推理延迟**：由于需要对 Top-K 个提示进行多次前向传播，推理时间会随 $K$ 值的增加而增加。
*   **新类语义鸿沟**：对于与基础类语义完全无关的新类别，知识库的增益可能受限（论文提到未来可引入 LLM 进行语义扩展来解决）。

（完）
