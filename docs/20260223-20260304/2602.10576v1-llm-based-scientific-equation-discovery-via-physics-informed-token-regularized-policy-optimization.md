---
title: LLM-Based Scientific Equation Discovery via Physics-Informed Token-Regularized Policy Optimization
title_zh: 基于物理信息令牌正则化策略优化的 LLM 科学方程发现
authors: "Boxiao Wang, Kai Li, Tianyi Liu, Chen Li, Junzhe Wang, Yifan Zhang, Jian Cheng"
date: 2026-02-11
pdf: "https://arxiv.org/pdf/2602.10576v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于大语言模型和策略优化的科学方程发现
tldr: 本研究针对符号回归中大语言模型作为静态生成器导致的物理不一致和方程冗余问题，提出了PiT-PO框架。该框架利用强化学习将大模型演进为自适应生成器，通过物理信息约束确保科学一致性，并利用词元级正则化抑制冗余结构。实验证明，PiT-PO在标准基准上达到领先水平，成功发现了新型湍流模型，并证明了小模型在科学发现领域超越闭源巨头的潜力。
selection_source: fresh_fetch
motivation: 现有基于大语言模型的符号回归方法缺乏基于反馈的内部表示更新，导致生成的方程常存在物理不一致或数学冗余。
method: 提出PiT-PO框架，通过强化学习引入物理有效性双重约束和细粒度词元惩罚，将大模型转化为自适应的方程生成器。
result: 该方法在标准基准测试中取得最优性能，并成功应用于复杂流体力学问题中新型湍流模型的发现。
conclusion: PiT-PO通过对齐物理规律与结构精简性，显著提升了模型在科学方程发现中的表现，并降低了高性能科学发现的门槛。
---

## 摘要
符号回归旨在从观测数据中提取数学方程。近期的研究方法已成功利用大语言模型（LLMs）生成方程假设，充分利用了其庞大的预训练科学先验知识。然而，现有框架主要将 LLM 视为静态生成器，依赖提示词层面的引导来驱动探索。这种范式无法根据搜索反馈更新模型的内部表示，往往会产生物理上不一致或数学上冗余的表达式。在这项工作中，我们提出了 PiT-PO（物理信息令牌正则化策略优化），这是一个通过强化学习将 LLM 演化为自适应生成器的统一框架。PiT-PO 的核心是一种双重约束机制，它在严格执行分层物理有效性的同时，应用细粒度的令牌级惩罚来抑制冗余结构。因此，PiT-PO 使 LLM 能够生成既符合科学一致性又具有结构简洁性的方程。实验表明，PiT-PO 在标准基准测试中达到了最先进的性能，并成功为具有挑战性的流体动力学问题发现了新型湍流模型。我们还证明了 PiT-PO 能够使小规模模型超越闭源巨头模型，从而推动了高性能科学发现的普及。

## Abstract
Symbolic regression aims to distill mathematical equations from observational data. Recent approaches have successfully leveraged Large Language Models (LLMs) to generate equation hypotheses, capitalizing on their vast pre-trained scientific priors. However, existing frameworks predominantly treat the LLM as a static generator, relying on prompt-level guidance to steer exploration. This paradigm fails to update the model's internal representations based on search feedback, often yielding physically inconsistent or mathematically redundant expressions. In this work, we propose PiT-PO (Physics-informed Token-regularized Policy Optimization), a unified framework that evolves the LLM into an adaptive generator via reinforcement learning. Central to PiT-PO is a dual-constraint mechanism that rigorously enforces hierarchical physical validity while simultaneously applying fine-grained, token-level penalties to suppress redundant structures. Consequently, PiT-PO aligns LLM to produce equations that are both scientifically consistent and structurally parsimonious. Empirically, PiT-PO achieves state-of-the-art performance on standard benchmarks and successfully discovers novel turbulence models for challenging fluid dynamics problems. We also demonstrate that PiT-PO empowers small-scale models to outperform closed-source giants, democratizing access to high-performance scientific discovery.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM-Based Scientific Equation Discovery via Physics-Informed Token-Regularized Policy Optimization》（基于物理信息令牌正则化策略优化的 LLM 科学方程发现）的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：符号回归（Symbolic Regression, SR）旨在从观测数据中发现简洁、可解释的数学方程。虽然现有方法开始利用大语言模型（LLM）的科学先验知识，但存在两个主要瓶颈：
    1.  **静态生成限制**：现有框架将 LLM 视为“冻结”的生成器，仅通过提示词（Prompt）引导，无法根据搜索反馈动态更新模型内部参数。
    2.  **物理与数学失真**：生成的方程往往只追求数值拟合，忽略了物理一致性（如单位统一、边界条件）和数学简洁性（存在大量冗余项）。
*   **研究背景**：作者试图将 LLM 从一个“静态提议者”转变为“自适应生成器”，通过强化学习（RL）使模型在搜索过程中不断进化。

### 2. 方法论：PiT-PO 框架
PiT-PO 结合了进化搜索与参数化学习，核心技术包括：
*   **搜索中 LLM 演化（In-Search Evolution）**：采用 **GRPO（组相对策略优化）** 算法。在搜索过程中，通过 LoRA 动态微调 LLM 参数，使其生成分布逐渐向目标系统的结构特征对齐。
*   **双重约束学习信号**：
    *   **分层物理约束**：分为通用层（量纲齐次性、可微性）和领域特定层（如湍流建模中的实现性、边界条件、渐近缩放等）。这些约束作为“门控惩罚”，引导模型进入物理有效的搜索区域。
    *   **定理引导的数学约束**：提出 **支持排除定理（Support Exclusion Theorem）**。该定理通过分析拟合系数的量级，识别出数学上的冗余项（False Discoveries）。
*   **细粒度令牌级正则化（Token-Regularized）**：不同于标准 GRPO 的序列级奖励，PiT-PO 将冗余项转化为**词元级（Token-level）惩罚**。如果某个词元属于被识别出的冗余项，则对其施加对数尺度的惩罚，从而精确抑制冗余结构的生成。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **LLM-SR Suite**：包含非线性振荡器、大肠杆菌生长模型、应力-应变数据等 4 个跨学科任务。
    *   **LLM-SRBench**：包含 239 个任务，分为 LSR-Transform（物理定律的变体）和 LSR-Synth（合成的复杂方程）。
    *   **实际应用案例**：周期性山丘流（Flow over periodic hills）的湍流建模，目标是发现非线性雷诺应力各向异性张量的符号表达式。
*   **对比方法**：包括传统遗传算法（GPlearn）、符号回归工具（PySR）、深度符号回归（uDSR）、检索增强方法（RAG-SR）以及基于 LLM 的前沿方法（LLM-SR, LaSR, SGA）。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA RTX 3090 GPU** 上完成。
*   **模型配置**：使用了 4 位量化（4-bit quantized）的开源模型，包括 **Llama-3.1-8B-Instruct**、**Llama-3.2-3B** 和 **1B** 版本。
*   **训练时长**：每个任务运行 2500 次搜索迭代。论文通过时间成本分析证明，尽管引入了 RL 微调，但在相同墙钟时间（Wall-clock time）下，PiT-PO 的收敛效率仍远高于静态 LLM 方法。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础物理方程到复杂生物、材料科学任务，再到 239 个基准任务的大规模测试。
*   **消融实验**：针对物理约束（w/o Phy）和令牌正则化（w/o TokenReg）进行了专门的消融研究，验证了各组件的必要性。
*   **客观性**：通过多随机种子运行并报告中位数及范围，对比了不同参数规模的 LLM（1B 到 8B），并与闭源模型（GPT-4o-mini）进行了跨量级对比，实验设计严谨且公平。

### 6. 主要结论与发现
*   **性能领先**：PiT-PO 在所有基准测试中均达到了 SOTA 水平，尤其在恢复方程的符号准确性（Symbolic Accuracy）上表现卓越。
*   **小模型逆袭**：通过 PiT-PO 优化的 **8B 甚至 1B 开源模型**，在方程发现能力上超越了依赖提示词工程的闭源巨头模型（如 GPT-4o-mini）。
*   **突破停滞**：实验观察到 PiT-PO 能在搜索后期通过物理/数学约束打破数值拟合的平台期，实现 NMSE（归一化均方误差）的数量级下降。
*   **实际价值**：在湍流建模中，发现的方程在 CFD（计算流体力学）后验评估中比传统 RANS 模型更接近 DNS（直接数值模拟）参考值。

### 7. 优点与亮点
*   **自适应性**：首次在符号回归中实现了 LLM 参数的在线闭环进化，解决了静态模型无法内化搜索反馈的问题。
*   **理论支撑**：提出的“支持排除定理”为识别符号冗余提供了严谨的数学依据，而非仅凭启发式规则。
*   **民主化科学发现**：证明了在消费级显卡（RTX 3090）上利用小模型也能进行高水平的科学发现，降低了技术门槛。

### 8. 不足与局限
*   **领域知识依赖**：对于复杂的实际问题（如湍流），仍需要专家预先定义领域特定的物理约束（如实现性、渐近缩放），这限制了其在完全未知领域的“零样本”发现能力。
*   **计算开销**：虽然时间效率可接受，但相比纯提示词方法，RL 微调确实增加了额外的计算负担。
*   **算子库限制**：实验中为了稳定性和物理意义，对某些算子（如三角函数）进行了人工限制，可能影响更广泛数学形式的探索。

（完）
