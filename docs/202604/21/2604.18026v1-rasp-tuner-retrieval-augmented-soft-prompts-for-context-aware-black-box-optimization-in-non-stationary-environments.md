---
title: "RASP-Tuner: Retrieval-Augmented Soft Prompts for Context-Aware Black-Box Optimization in Non-Stationary Environments"
title_zh: RASP-Tuner：用于非平稳环境中上下文感知黑盒优化的检索增强软提示
authors: Enze Pan
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18026v1"
tags: ["query:bopl"]
score: 9.0
evidence: 用于黑盒优化的检索增强软提示
tldr: 针对非平稳环境下的黑盒优化问题，本文提出 RASP-Tuner。该框架通过检索相似的历史上下文来识别潜在状态，并利用检索到的软提示（Soft Prompt）在低维子空间中进行快速自适应。它结合了专家混合代理模型和 RealErrorComposer 指标映射技术，在保持低计算开销的同时显著降低了累积遗憾值。实验表明，其在多个基准测试中表现优于传统 GP-UCB，且运行速度提升了 8-12 倍。
selection_source: fresh_fetch
motivation: 传统的黑盒优化在处理随上下文变化的非平稳环境时，面临重复适应成本高以及高斯过程在大规模观测下计算开销过大的挑战。
method: 提出一种检索增强的软提示调优方法，通过检索相似上下文、在低维提示空间进行自适应，并仅在误差激增时触发全局代理模型更新。
result: 在 9 个合成基准和 3 个真实世界流数据上，RASP-Tuner 的累积遗憾值优于或持平于 GP-UCB，且每步运行时间缩短了 8 到 12 倍。
conclusion: RASP-Tuner 证明了结合检索机制与低维提示空间自适应是解决非平稳、上下文感知黑盒优化问题的高效且低延迟的有效方案。
---

## 摘要
许多已部署的系统呈现出黑盒目标函数，其最小化配置随外部观测到的上下文而变化。当上下文重新访问一小组潜在状态（latent regimes）时，丢弃历史信息的优化器会付出重复的自适应代价；当每一步必须保持低开销时，在高观测计数下进行完整的高斯过程（GP）重新拟合难以持续。我们将在线调优建模为以上下文为条件的遗憾最小化（regret minimization），并提出了 RASP-Tuner，它实例化了一个基于第一性原理的分解方案：(i) 通过检索相似的历史上下文来识别状态代理（regime proxy）；(ii) 使用混合专家（mixture-of-experts）代理模型预测短时域损失，其输入拼接了参数、上下文和检索到的软提示；(iii) 主要在低维提示子空间中进行自适应，仅在标量化误差或分歧激增时才调用完整的代理模型更新。RealErrorComposer 通过 EMA 稳定的逻辑分数将异构流式指标映射到 [0,1]，从而提供单一的可微训练目标。在九个合成非平稳基准测试、一个对抗性上下文完整性检查以及三个表格化真实世界流（见真实世界实验章节）中，在时域 T=100 的配对测试下，RASP-Tuner 在九个合成任务中的七个上，其累积遗憾优于或等同于我们的 GP-UCB 和 CMA-ES 实现，同时在相同硬件上，每步的实际运行时间比滑动窗口 GP-UCB 低 8-12 倍。在聚类分离且强凸的状态模型（RA-GD）中的理想化分析为有界动态遗憾提供了充分条件；部署的流水线违反了其中几个前提，我们阐明了哪些差距仍有待解决。

## Abstract
Many deployed systems expose black-box objectives whose minimizing configuration shifts with an externally observed context. When contexts revisit a small set of latent regimes, an optimizer that discards history pays repeated adaptation cost; when each step must remain inexpensive, full Gaussian-process (GP) refits at high observation counts are difficult to sustain. We cast online tuning as context-conditioned regret minimization and present RASP-Tuner, which instantiates a decomposition motivated by first principles: (i) identify a regime proxy by retrieving similar past contexts; (ii) predict short-horizon loss with a mixture-of-experts surrogate whose input concatenates parameters, context, and a retrieved soft prompt; (iii) adapt chiefly in a low-dimensional prompt subspace, invoking full surrogate updates only when scalarized error or disagreement spikes. A RealErrorComposer maps heterogeneous streaming metrics to [0,1] via EMA-stabilized logistic scores, supplying a single differentiable training target. On nine synthetic non-stationary benchmarks, an adversarial-context sanity check, and three tabular real-world streams (Section on real-world experiments), RASP-Tuner improves or matches cumulative regret relative to our GP-UCB and CMA-ES implementations on seven of nine synthetic tasks under paired tests at horizon T=100, while recording 8-12 times lower wall-clock per step than sliding-window GP-UCB on identical hardware. Idealized analysis in a cluster-separated, strongly convex regime model (RA-GD) supplies sufficient conditions for bounded dynamic regret; the deployed pipeline violates several of these premises, and we articulate which gaps remain open.

---

## 论文详细总结（自动生成）

# RASP-Tuner: 针对非平稳环境的检索增强软提示黑盒优化论文总结

## 1. 核心问题与整体含义（研究动机和背景）
在现实世界的黑盒优化（BBO）场景中（如控制器调优、数据库参数配置等），目标函数往往是非平稳的，即最优配置会随外部上下文（Context）的变化而漂移。
*   **现有挑战：** 
    *   **重复适应成本：** 传统的进化策略（如 CMA-ES）缺乏对历史上下文的显式记忆，当环境回到之前的状态时，需要重新消耗样本进行适应。
    *   **计算开销：** 基于高斯过程（GP）的方法虽然能处理上下文，但其计算复杂度随观测数呈立方增长（$O(N^3)$），难以满足高频调优的实时性要求。
*   **核心目标：** 论文旨在设计一种既能利用历史经验（减少重复适应），又能保持极低计算延迟（$O(1)$ 推理深度）的在线优化框架。

## 2. 方法论：核心思想与关键技术
RASP-Tuner 借鉴了自然语言处理（NLP）中的检索增强生成（RAG）和软提示（Soft Prompt）思想，将其转化为优化任务中的“检索-条件-适应”流程。

### 关键技术组件：
*   **PromptMemory（检索系统）：** 存储上下文键值、历史最优参数和可学习的“软提示”向量。通过 k-NN 检索相似的历史上下文，生成当前状态的代理（Regime Proxy）和参数空间的“提示”（Hint）。
*   **Prompt-MoE Surrogate（代理模型）：** 采用混合专家模型（MoE）预测标量化误差。输入是参数、上下文和检索到的软提示的拼接。通过门控机制分配计算容量，利用软提示在不改变全局模型的情况下进行局部微调。
*   **混合自适应机制（Hybrid Adaptation）：**
    *   **快速路径（Fast Path）：** 几乎每一步都仅对检索到的软提示进行梯度更新，保持共享的 MoE 骨干网络稳定。
    *   **慢速路径（Slow Path）：** 仅在误差异常或专家分歧过大时，触发完整的模型更新（包含回放缓冲区和锚点正则化以防止遗忘）。
*   **RealErrorComposer（指标标量化）：** 使用指数移动平均（EMA）稳定的逻辑分数（Logistic Scores），将异构、多目标的原始指标映射到 $[0, 1]$ 区间的单一可微目标 $e_t$。
*   **候选策略：** 结合代理模型的梯度下降、检索到的历史提示以及随机扰动生成候选参数，通过下置信界（LCB）准则进行选择。

## 3. 实验设计
*   **实验场景：**
    *   **9 个合成非平稳基准：** 涵盖 LLM 推理服务、AutoML 流、机器人 ISP 调优、晶圆漂移、切换 LQR 控制、服务器突发流量等。
    *   **1 个对抗性上下文任务：** 上下文纯为噪声，用于测试模型在信号缺失时的鲁棒性。
    *   **3 个真实世界表格流：** 基于 California Housing、Boston Housing 和 Digits 数据集构建的超参数调优流。
*   **对比基准（Benchmarks）：**
    *   **Random Search (RS)：** 随机搜索。
    *   **CMA-ES：** 经典的进化策略。
    *   **GP-UCB：** 带有滑动窗口的高斯过程上限置信界方法。
    *   **NoMemory-RASP：** 屏蔽上下文检索的消融版本。

## 4. 资源与算力
*   **硬件环境：** 实验在 Apple M 系列芯片等级的 CPU 工作站上运行。
*   **算力消耗：** 论文未强调大规模 GPU 集群训练，因为其核心卖点是**在线调优的轻量化**。
*   **效率指标：** RASP-Tuner 的每步延迟保持在几十毫秒量级，比滑动窗口 GP-UCB 快 8-12 倍。

## 5. 实验数量与充分性
*   **实验规模：** 每个任务运行 5 个随机种子，时域长度 $T=100$。
*   **充分性：** 
    *   涵盖了漂移（Drift）、状态切换（Switching）和轨迹回放（Trace Replay）等多种非平稳模式。
    *   通过消融实验验证了“记忆检索”模块的必要性。
    *   通过对抗性实验客观地展示了方法的失效边界（即上下文无信息时表现下降）。
*   **公平性：** 所有任务使用统一的超参数设置（Table 2），避免了针对特定任务的过度调优。

## 6. 主要结论与发现
*   **性能优异：** 在 9 个合成任务中的 7 个上，RASP-Tuner 的累积遗憾显著低于或持平于最强的基线方法（GP-UCB 或 CMA-ES）。
*   **高效率：** 在相同硬件下，其运行速度比 GP 方法提升了一个数量级，且内存增长受限。
*   **快速适应：** 在状态切换场景下，由于能检索历史提示，RASP-Tuner 能在极短步数内恢复到最优性能。
*   **理论支撑：** 在理想化的聚类分离和强凸假设下，证明了该方法具有有界的动态遗憾。

## 7. 优点
*   **架构创新：** 首次将 NLP 中的“软提示”和“检索增强”概念成功移植到黑盒优化领域，解决了非平稳环境下的记忆与适应难题。
*   **低延迟：** 避开了 GP 的高额计算开销，适合对实时性要求高的在线系统。
*   **通用性：** RealErrorComposer 解决了多指标、不同量纲下的自动标量化问题，减少了人工设计奖励函数的负担。

## 8. 不足与局限
*   **上下文依赖：** 实验证明，如果上下文（Context）与目标函数无关（如对抗性噪声），检索机制会引入有害方差，导致性能不如上下文无关的方法。
*   **理论与实践的差距：** 理论证明基于强凸和精确梯度假设，而实际部署的 MoE 模型是非凸的，理论保证在复杂神经网络上的适用性有限。
*   **时域限制：** 实验主要集中在 $T=100$ 的短中时域，在极长时域下的内存管理和模型稳定性仍需进一步验证。

（完）
