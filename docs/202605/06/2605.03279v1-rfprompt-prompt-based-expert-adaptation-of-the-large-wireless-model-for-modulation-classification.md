---
title: "RFPrompt: Prompt-Based Expert Adaptation of the Large Wireless Model for Modulation Classification"
title_zh: RFPrompt：基于提示的大型无线模型专家适配，用于调制分类
authors: "Md Raihan Uddin, Tolunay Seyfi, Fatemeh Afghah"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03279v1"
tags: ["query:bopl"]
score: 9.0
evidence: 参数高效的基于提示的适配
tldr: 本研究针对无线通信中自动调制分类（AMC）面临的分布偏移挑战，提出了RFPrompt框架。该框架基于大型无线模型（LWM），通过引入可学习的深度提示词进行参数高效的专家适配，同时保持预训练骨干网络冻结。实验证明，RFPrompt在处理分布外数据和有限监督场景下表现优异，显著提升了模型在真实射频环境中的鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决无线基础模型在应对硬件损伤和未知传播环境等分布偏移任务时，如何高效适配且不破坏预训练结构的问题。
method: 提出了RFPrompt框架，在冻结的大型无线模型中引入可学习的深度提示词令牌，实现低参数量的任务特定适配。
result: 实验结果显示，该方法在标准及分布外调制分类任务中均提升了鲁棒性，尤其在真实空口IQ数据和有限样本下表现突出。
conclusion: 提示词学习是适配无线基础模型以应对复杂下游射频环境的一种实用且高效的策略。
---

## 摘要
实际部署中的自动调制分类 (AMC) 要求对由硬件损伤、未见过的传播环境以及训练期间从未遇到的记录条件引起的分布偏移具有鲁棒性。尽管无线基础模型为鲁棒的射频 (RF) 表征学习提供了一个有前景的起点，但一个重要的开放性问题是如何高效地将它们适配到分布外 (OOD) 的下游任务，而不覆盖大规模预训练期间学到的结构。在本文中，我们研究了基于提示的适配作为无线基础模型中 OOD 迁移的一种通用机制。我们提出了 RFPrompt，这是一个参数高效的框架，它在保持预训练主干网络冻结的同时引入了可学习的深度提示标记 (prompt tokens)，从而能够以极少的训练参数实现特定任务的适配。我们在大型无线模型 (LWM)（一种混合专家无线基础模型）上实例化并评估了该方法，并研究了其在标准和 OOD 调制分类设置下的表现。结果表明，基于提示的适配在分布偏移和有限监督下持续提高了鲁棒性，特别是在真实的空中 (over-the-air) IQ 数据上，同时保持了极高的参数效率。这些发现表明，提示学习是将无线基础模型适配到具有挑战性的下游射频环境的一种实用且有效的策略。

## Abstract
Automatic modulation classification (AMC) in real-world deployments demands robustness to distribution shifts arising from hardware impairments, unseen propagation environments, and recording conditions never encountered during training. Although wireless foundation models offer a promising starting point for robust RF representation learning, an important open question is how to adapt them efficiently to out-of-distribution (OOD) downstream tasks without overwriting the structure learned during large-scale pre-training. In this paper, we investigate prompt-based adaptation as a general mechanism for OOD transfer in wireless foundation models. We propose RFPrompt, a parameter-efficient framework that introduces learnable deep prompt tokens while keeping the pretrained backbone frozen, enabling task-specific adaptation with minimal trainable parameters. We instantiate and evaluate this approach on the Large Wireless Model (LWM), a mixture-of-experts wireless foundation model, and study its behavior under both standard and OOD modulation-classification settings. Results show that prompt-based adaptation consistently improves robustness under distribution shift and limited supervision, particularly on real-world over-the-air IQ data, while preserving strong parameter efficiency. These findings suggest that prompt learning is a practical and effective strategy for adapting wireless foundation models to challenging downstream RF environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《RFPrompt: Prompt-Based Expert Adaptation of the Large Wireless Model for Modulation Classification》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自动调制分类（AMC）在实际部署中面临严重的**分布外（OOD）泛化问题**，如硬件损伤、未知的传播环境和多变的信噪比。
*   **研究动机**：虽然现有的无线基础模型（如 LWM）在大规模合成数据上表现优异，但如何高效地将这些庞大的模型适配到真实的、标签稀缺的下游射频（RF）环境，同时又不破坏预训练学到的知识结构，是一个亟待解决的挑战。
*   **整体含义**：本文旨在探索一种**参数高效的适配机制**，通过“提示学习（Prompt Learning）”而非全量微调，使无线基础模型能够快速、鲁棒地迁移到复杂的真实世界任务中。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **RFPrompt** 框架。在保持大型无线模型（LWM）主干网络完全冻结的前提下，为其中的“混合专家（MoE）”结构引入可学习的**深度提示标记（Deep Prompt Tokens）**。
*   **关键技术细节**：
    *   **输入处理**：将 1024 个复数 IQ 样本通过短时傅里叶变换（STFT）转换为 128×128 的幅度谱图。
    *   **主干网络**：基于 LWM，包含三个协议专用专家（LTE、WiFi、5G）和一个路由网络（Router）。
    *   **深度提示注入**：在每个专家的每一层 Transformer（共 12 层）中，插入 $M$ 个可学习的提示向量。这些向量与输入标记一起参与自注意力计算，从而“引导”冻结的注意力机制关注调制相关的特征（如符号率谐波、星座密度模式）。
    *   **训练目标**：仅训练提示标记、路由网络和分类头（MLP），主干参数保持不变。
    *   **参数效率**：可训练参数仅占总参数量的 **0.34%**（约 255K / 4.8M）。

### 3. 实验设计
*   **数据集**：
    1.  **IEEE Dataport IQ**：包含 LTE、5G 和 WiFi 信号，属于分布内（In-distribution）基准。
    2.  **Real-World IQ**：真实的空口捕获数据，包含多径衰落、同频干扰和硬件非理想性，作为主要的 **OOD 基准**。
*   **对比方法（Baselines）**：
    *   **基础模型变体**：冻结专家（Frozen Expert）、部分微调（PFT）、SpectrumFM。
    *   **传统深度学习**：ResNet18、EfficientNet-B0、MobileNetV3、专用卷积神经网络（CNN）。
    *   **元学习/迁移学习**：MAML、FOMAML、Reptile、基于 RadioML 2016 的迁移学习。
*   **评估场景**：
    *   **Stage A（数据规模扫描）**：研究随着训练样本增加（N=100 到 1600）的性能变化。
    *   **Stage B（少样本学习）**：研究在极低样本（K=2 到 128 shot）下的适配能力。

### 4. 资源与算力
*   **硬件环境**：论文提到使用了 **12 GB 显存的 GPU**（未指明具体型号，推测为消费级或入门级工作站显卡）。
*   **训练细节**：使用 AdamW 优化器，采用余弦退火调度和 5 个 epoch 的线性预热。
*   **效率对比**：论文提供了训练效率对比表（表 VI），显示 RFPrompt 的训练时间与部分微调（PFT）相当，但远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个性质截然不同的数据集（合成 vs 真实）。
    *   进行了多维度的消融实验，包括提示长度 $P$ 的影响（$P=8$ 到 32）。
    *   提供了 t-SNE 可视化分析，从表征空间解释了提示学习的效果。
*   **充分性评价**：实验设计较为充分，特别是在 **少样本（Few-shot）** 和 **OOD 鲁棒性** 方面的对比非常详尽。通过与多种元学习和传统 CNN 的对比，客观地证明了基础模型在射频领域的优势。

### 6. 主要结论与发现
*   **OOD 表现卓越**：在真实世界 OOD 数据集上，RFPrompt 在样本量 $N \ge 200$ 时显著优于所有基准方法。
*   **少样本优势**：在极低样本（如 2-shot）下，RFPrompt 的准确率比传统模型高出 **30% 以上**，证明了预训练表征的强大基础。
*   **参数高效性**：仅用 0.34% 的参数更新，就弥补了冻结模型与微调模型之间 79% 的性能差距。
*   **注意力引导**：通过深度提示，模型能够有效地在冻结的权重中“导航”，提取出对调制分类关键的特征，同时抑制信道噪声。

### 7. 优点（亮点）
*   **架构创新**：首次将深度提示学习应用于无线 MoE 基础模型，解决了专家适配的难题。
*   **实用性强**：极低的参数更新量非常适合计算资源受限的边缘无线设备。
*   **鲁棒性高**：在面对真实环境中的硬件损伤和多径效应时，表现出比元学习更稳定的迁移能力。

### 8. 不足与局限
*   **提示长度敏感性**：实验显示提示长度 $P$ 对性能有非单调影响，过长会导致优化噪声，需要针对不同任务手动调优。
*   **性能天花板**：在数据量非常充足（Stage A 高端）且分布一致的情况下，RFPrompt 的表现有时会被 SpectrumFM 或全量微调超越，说明提示学习在“大数据”模式下可能存在容量瓶颈。
*   **协议依赖**：目前的专家结构（LTE/WiFi/5G）仍带有较强的先验假设，对于完全未知的非标准协议信号，其适配效果尚待验证。

（完）
