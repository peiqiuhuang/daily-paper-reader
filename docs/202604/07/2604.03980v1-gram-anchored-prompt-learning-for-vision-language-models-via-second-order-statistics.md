---
title: Gram-Anchored Prompt Learning for Vision-Language Models via Second-Order Statistics
title_zh: 基于二阶统计量的视觉-语言模型 Gram 矩阵锚定提示学习
authors: "Minglei Chen, Weilong Wang, Jiang Duan, Ye Deng"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.03980v1"
tags: ["query:bopl"]
score: 9.0
evidence: 视觉语言模型的参数高效提示学习
tldr: 本研究针对视觉语言模型（VLM）的参数高效提示学习，指出仅依赖一阶空间特征易受领域偏移和局部噪声影响。为此，提出了 Gram 锚定提示学习（GAPL）框架，通过引入基于 Gram 矩阵的二阶统计流，将局部语义对齐与全局结构一致性相结合。该方法使语言表示能动态适应不同领域的统计分布变化，在多个基准测试中显著提升了模型的鲁棒性和下游任务表现。
selection_source: fresh_fetch
motivation: 传统方法仅利用一阶视觉特征进行对齐，难以应对复杂的领域偏移和空间噪声干扰。
method: 引入 Gram 矩阵提取二阶统计信息，并将其作为锚点辅助提示学习，实现一阶与二阶特征的协同。
result: 实验证明二阶特征能有效增强模型性能，GAPL 在多个视觉任务基准上均取得了领先结果。
conclusion: 结合二阶统计量的提示学习为提升视觉语言模型的跨领域适应能力提供了一种稳健且高效的新方案。
---

## 摘要
参数高效的提示学习已成为将视觉-语言模型（VLMs）适配到下游任务的事实标准。现有方法主要侧重于将文本提示与一阶视觉特征（即空间特征图）进行对齐。尽管这对于细粒度语义辨别是有效的，但我们认为仅依赖一阶信息不足以实现鲁棒的适配，因为这些空间纠缠的特征极易受到领域偏移和局部噪声的影响。在这项工作中，我们提出了基于二阶统计量的视觉-语言模型 Gram 矩阵锚定提示学习（GAPL），这是一个协同局部语义对齐与全局结构一致性的框架。在方法论上，我们通过 Gram 矩阵引入了一个额外的二阶统计流，以增强标准的一阶空间交互。通过将提示锚定到这些二阶先验，我们的方法使语言表示能够动态地适应跨不同领域的统计分布偏移。大量的实验表明了二阶特征的有效性，并展示了 GAPL 在各种基准测试中极具竞争力的性能。

## Abstract
Parameter-efficient prompt learning has become the de facto standard for adapting Vision-Language Models (VLMs) to downstream tasks. Existing approaches predominantly focus on aligning text prompts with first-order visual features (i.e., spatial feature maps). While effective for fine-grained semantic discrimination, we argue that relying solely on first-order information is insufficient for robust adaptation, as these spatially entangled features are highly susceptible to domain shifts and local noise. In this work, we propose \textbf{Gram-Anchored Prompt Learning (GAPL)} for Vision-Language Models via Second-Order Statistics, a framework that synergizes local semantic alignment with global structural consistency. Methodologically, we introduce an additional second-order statistical stream via \textbf{Gram matrices} that augments the standard first-order spatial interaction. By anchoring prompts to these second-order priors, our approach enables language representations to dynamically adapt to statistical distribution shifts across diverse domains. Extensive experiments indicate the effectiveness of the second-order features, and show compelling performances of GAPL on various benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Gram-Anchored Prompt Learning for Vision-Language Models via Second-Order Statistics》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言模型（VLM）提示学习方法（如 CoOp, CoCoOp）主要依赖**一阶视觉特征**（如全局池化向量或空间特征图）来生成或调整提示词。
*   **研究动机**：一阶特征与局部外观和空间细节紧密耦合，在面对**领域偏移（Domain Shift）**（例如从照片变为素描）时，同一语义概念在特征空间中的位置会发生剧烈偏移，导致提示词生成对风格变化过于敏感，缺乏鲁棒性。
*   **整体含义**：本文提出引入**二阶统计量（Gram 矩阵）**作为提示学习的“锚点”。二阶统计量在风格迁移领域已被证明能捕捉稳定的全局纹理和风格信息，通过将其引入提示学习，可以为模型提供一个不随空间布局剧烈变化的结构化参考，从而提升跨领域的泛化能力。

### 2. 论文提出的方法论：Gram-Anchored Prompt Learning (GAPL)
GAPL 框架由三个互补的流组成，旨在协同全局语义、风格统计和局部细节：
*   **核心思想**：利用 Gram 矩阵提取图像的二阶统计特征，并用其调制（Modulate）文本提示的表示，使文本分支能感知并适应视觉分布的变化。
*   **关键技术细节**：
    1.  **Gram-Anchored Stream（核心）**：
        *   从视觉编码器的 Patch Tokens 计算通道维度的 **Gram 矩阵** $G = \frac{1}{N} F^\top F$。
        *   为了计算效率，仅提取 Gram 矩阵的**对角线元素**（代表通道能量）作为紧凑描述符。
        *   通过轻量级 MLP 生成门控向量 $\Gamma$，对提示后的文本特征进行元素级调制，生成“风格文本锚点”（Style Text Anchor）。
    2.  **Global Invariant Stream**：使用预定义的文本模板（如 ImageNet 模板）融合学习到的提示，以保留 VLM 原有的通用语义知识，防止过拟合。
    3.  **Contextual-Anchored Stream**：利用一组可学习的局部信号作为 Key/Value，通过注意力机制与文本特征交互，实现细粒度的局部对齐。
*   **融合机制**：采用**输入自适应的分支融合**策略，通过一个 MLP 根据当前图像的视觉特征和风格门控动态预测三个分支的权重，最后加权融合 Logits 进行预测。

### 3. 实验设计
*   **数据集与场景**：
    *   **领域泛化**：在 ImageNet 上训练，在四个具有挑战性的变体（ImageNet-V2, -Sketch, -A, -R）上直接测试。
    *   **基类到新类泛化**：在 11 个下游任务数据集（包括 Caltech101, OxfordPets, StanfordCars, Flowers102, Food101, FGVCAircraft, SUN397, DTD, EuroSAT, UCF101）上评估对未见类别的处理能力。
*   **Benchmark 与对比方法**：
    *   对比了包括 CoOp, CoCoOp, MaPLe, PromptSRC, TCP, MMRL, HiCroPL 等在内的多种近年来的 SOTA 提示学习方法。

### 4. 资源与算力
*   **硬件环境**：论文提到使用 PyTorch 实现，骨干网络为 CLIP ViT-B/16。
*   **训练细节**：
    *   领域泛化：训练 20 个 epoch，Batch Size 为 16。
    *   基类到新类泛化：训练 5 个 epoch，Batch Size 为 4。
    *   优化器：使用 SGD。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和数量，但从其参数高效（PEFT）的性质和较小的 Batch Size 来看，该方法对算力的需求相对较低，属于典型的轻量级适配方案。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 个公开数据集，涵盖了细粒度分类、场景识别、纹理识别、动作识别及遥感等多种视觉任务。
*   **消融实验**：
    *   验证了三个流（全局、Gram、上下文）各自的贡献。
    *   对比了不同视觉提示深度（V0 vs V12）的影响。
    *   探讨了二阶统计量的设计（仅对角线 vs 全矩阵 vs 均值+方差）。
    *   分析了融合权重 $\alpha$ 的敏感性。
*   **充分性评价**：实验设计遵循了该领域的标准协议（CoCoOp 协议），对比方法全面且包含最新研究，通过 t-SNE 可视化和定量距离分析进一步验证了领域对齐效果，实验结果具有较高的说服力和客观性。

### 6. 论文的主要结论与发现
*   **二阶统计量的有效性**：实验证明 Gram 矩阵的对角线元素足以提供有效的风格锚点，显著提升了模型在 ImageNet-A 等极端偏移数据集上的表现。
*   **性能领先**：GAPL 在领域泛化（平均 61.12%）和基类到新类泛化（平均调和均值 HM 81.77%）上均取得了 SOTA 或极具竞争力的结果。
*   **特征对齐**：可视化分析显示，引入 Gram 锚点后，不同领域的同一类别特征在潜在空间中更加聚集，减少了领域诱导的方差。

### 7. 优点
*   **视角新颖**：首次将二阶统计量（Gram 矩阵）引入 VLM 的动态提示学习，突破了传统仅依赖一阶特征的局限。
*   **鲁棒性强**：通过统计锚定，有效缓解了提示学习在面对分布外（OOD）数据时的性能退化问题。
*   **效率高**：仅使用 Gram 矩阵对角线元素，在保持低计算开销的同时获得了显著的性能提升。

### 8. 不足与局限
*   **推理模式固定**：目前的 GAPL 在离线训练后参数固定，无法在推理阶段根据流式数据进行实时在线适配（Test-time Adaptation）。
*   **任务覆盖**：主要集中在图像分类任务，尚未在目标检测、分割等需要极高空间精度的下游任务中验证二阶统计量的影响。
*   **对角线简化**：虽然对角线元素高效，但可能丢失了通道间的相关性信息，对于某些极度依赖复杂纹理关联的任务可能存在信息损失。

（完）
