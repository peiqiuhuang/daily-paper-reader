---
title: "CAP-CoT: Cycle Adversarial Prompt for Improving Chain of Thoughts in LLM Reasoning"
title_zh: CAP-CoT：用于改进大语言模型推理中思维链的循环对抗提示
authors: "Shuxu Chen, Yitian Zhou, Jiaquan Zhang, Haoyu Bian, Aming Wu, Sungyoung Lee, Chaoning Zhang, Hyundong Shin"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23270v1"
tags: ["query:bopl"]
score: 8.0
evidence: 循环对抗提示词优化框架
tldr: 针对大语言模型在复杂推理中思维链（CoT）不稳定的问题，本文提出CAP-CoT框架。该框架通过循环对抗提示优化，利用前向求解器生成答案，对抗挑战者构造针对性错误，并由反馈代理对比两者生成结构化反馈。通过闭环迭代更新提示词，该方法在提升推理准确性的同时，显著增强了模型在多步推理任务中的稳定性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的CoT推理在处理长链多步问题时表现不稳定，且缺乏有效的迭代对比纠错机制来提升推理质量。
method: 提出一种循环对抗提示优化框架，通过求解器生成答案、挑战者制造逻辑漏洞、反馈代理对比纠错的闭环机制来优化提示词。
result: 在六个基准测试和四种主流模型上的实验证明，CAP-CoT能有效减少推理波动并显著提高推理准确率。
conclusion: CAP-CoT通过任务语义层面的对抗优化，成功暴露并修复了推理中的逻辑漏洞，为增强LLM推理稳定性提供了新思路。
---

## 摘要
思维链（CoT）提示已成为一种简单且有效的方法，用于引导大语言模型（LLM）生成逐步解决方案。然而，在处理长且多步骤的问题时，CoT 推理在多次运行中可能表现得不稳定，导致针对相同任务产生不一致的答案。大多数先前的工作集中在单次推理中改进前向推理链，而较少关注迭代和对比修正。为了填补这一空白，我们提出了 CAP-CoT，这是一个循环对抗提示优化框架，旨在提高单个已部署求解器的 CoT 推理准确性和稳定性。在每个循环中，前向求解器生成候选推理链，对抗挑战者利用针对性的错误策略构建看似合理但刻意存在缺陷的推理链，反馈代理则对比这两条链并生成步骤对齐的结构化反馈。这种反馈从两个方向闭合了优化循环，包括根据挑战者暴露的错误更新求解器提示，以及更新挑战者提示以在后续循环中生成更具针对性的错误。与越狱或提示注入攻击等面向安全的对抗提示不同，我们的对抗组件是任务语义化的，旨在暴露推理链中的逻辑漏洞。在六个基准测试和四个 LLM 骨干模型上的实验表明，在两到三个对抗提示优化循环内，CAP-CoT 能够持续减少多次运行之间的变异性，同时提高推理准确性和对提示扰动的鲁棒性。

## Abstract
Chain-of-Thought (CoT) prompting has emerged as a simple and effective way to elicit step-by-step solutions from large language models (LLMs). However, CoT reasoning can be unstable across runs on long, multi-step problems, leading to inconsistent answers for unchanged task. Most prior work focuses on improving the forward reasoning chain within a single pass, with less attention to iterative and contrastive correction. To address this gap, we propose CAP-CoT, a Cycle Adversarial Prompt optimization framework designed to improve both CoT reasoning accuracy and stability of a single deployed solver. In each cycle, a forward solver generates candidate reasoning chains, an adversarial challenger constructs plausible but deliberately flawed chains using targeted error strategies, and a feedback agent contrasts the two chains and produces step-aligned structured feedback. This feedback closes the optimization loop in two directions, including updating the solver prompt based on errors exposed by the challenger, and updating the challenger prompt to generate increasingly targeted errors in subsequent cycles. Unlike safety-oriented adversarial prompting such as jailbreak or prompt-injection attacks, our adversarial component is task-semantic and aims to expose logical vulnerabilities in reasoning chains. Experiments across six benchmarks and four LLM backbones demonstrate that within two to three adversarial prompt optimization cycles, CAP-CoT consistently reduces variability across runs while improving reasoning accuracy and robustness to prompt perturbations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CAP-CoT**（Cycle Adversarial Prompt for Chain-of-Thought）的框架，旨在通过循环对抗提示优化来增强大语言模型（LLM）在复杂任务中的推理准确性和稳定性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管思维链（CoT）能显著提升 LLM 的推理能力，但在处理长链、多步骤问题时表现出极大的**不稳定性**。模型在多次运行中可能对同一问题给出不一致的答案，且容易受到提示词微小扰动或无关上下文的影响。
*   **研究动机**：现有的改进方法（如 Self-Consistency, ToT, GoT）大多关注于增加前向推理的采样或搜索空间，而忽略了**迭代式对比纠错**的重要性。作者认为，通过让模型学习“什么是错的”以及“为什么错”，可以比单纯提供正确示例更有效地加固推理逻辑。

### 2. 方法论：核心思想与关键技术
CAP-CoT 采用了一个包含三个角色的循环优化框架，通过“对比”产生学习信号：
*   **核心思想**：建立一个“求解器-挑战者-反馈者”的闭环。通过构造看似合理但逻辑有误的“对抗链”，迫使求解器识别并修复自身的逻辑漏洞。
*   **关键组件**：
    1.  **求解器代理 ($G_S$)**：生成标准的前向推理链。
    2.  **对抗挑战者代理 ($G_C$)**：利用特定的错误策略（如逻辑跳跃、概念混淆、模糊语言、错误包装）构造具有误导性的错误推理链。
    3.  **反馈代理 ($F$)**：对比求解器和挑战者的输出，生成**步骤对齐（Step-aligned）**的结构化反馈。
*   **优化流程（SFPR）**：
    *   **双向更新**：反馈不仅用于更新求解器的提示词（修复漏洞），还用于更新挑战者的提示词（使其在下一轮生成更具针对性的“陷阱”）。
    *   **任务语义对抗**：强调这种对抗是逻辑层面的（Task-semantic），而非安全层面的（如越狱攻击）。

### 3. 实验设计
*   **数据集**：涵盖了 6 个主流推理基准，包括数学（MATH, GSM8K）、逻辑与算法（BBH）、知识理解（MMLU-CF）、多跳问答（HotpotQA）和长文本理解（LongBench）。
*   **骨干模型（Backbones）**：GPT-4o-mini, Qwen-turbo, DeepSeek-V3, GPT-4o。
*   **对比方法（Baselines）**：共 13 种，包括基础 CoT、采样类（CoT-SC）、精炼类（Self-Refine）、搜索类（ToT, GoT, FoT）、代理流类（AFlow）以及辩论类（MAD, ECON, AoT）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **实现细节**：由于该方法属于**提示词级别（Prompt-level）的优化**，不涉及模型参数的微调（Fine-tuning）。实验通过 LangChain 框架调用官方 API 实现。其成本主要体现在推理时的 Token 消耗，而非离线训练算力。

### 5. 实验数量与充分性
*   **实验规模**：在 4 种不同架构的骨干模型上进行了跨 6 个数据集的测试，每组实验运行 3-5 次取平均值，并提供了标准差以衡量稳定性。
*   **充分性与客观性**：
    *   进行了详尽的**消融实验**，验证了挑战者和反馈代理的必要性。
    *   探讨了**优化轮数**的影响（发现 2-3 轮即可达到饱和）。
    *   进行了**温度敏感性测试**（Temperature Sweep），证明了该方法在不同随机性设置下均能保持高稳定性。
    *   对比了 **Token 效率**，客观展示了该方法在性能与成本之间的平衡点。

### 6. 主要结论与发现
*   **性能提升**：CAP-CoT 在所有基准测试和骨干模型上均优于现有方法，尤其在复杂数学和长文本推理中表现突出。
*   **稳定性增强**：通过对抗优化，模型对提示词扰动和采样噪声的鲁棒性显著提高，多次运行的变异性（Variation）大幅降低。
*   **快速收敛**：通常只需 2-3 个循环，提示词的质量就能达到最优，后续收益递减。
*   **成本效益**：虽然 Token 消耗高于单次 CoT，但远低于复杂的树状或图状搜索方法（如 ToT, GoT），具有较好的实用性。

### 7. 优点（亮点）
*   **自适应对抗**：挑战者不是使用固定的错误模板，而是根据求解器的表现动态演化，能够精准打击模型的“盲区”。
*   **结构化反馈**：步骤对齐的反馈机制使得提示词的更新具有高度的可解释性和针对性。
*   **即插即用**：作为一种提示优化框架，它可以直接应用于任何现有的 LLM，无需重新训练模型。

### 8. 不足与局限
*   **依赖反馈质量**：整个循环的有效性高度依赖于反馈代理（$F$）的判别能力。如果骨干模型本身逻辑能力极弱，可能无法生成高质量的反馈。
*   **推理延迟**：虽然比搜索类方法快，但由于需要多个代理交互和多次循环，其实时推理延迟仍高于标准 CoT。
*   **错误分类局限**：初始的错误分类法（Jump, Confusion 等）虽然有效，但可能仍未覆盖所有潜在的推理错误类型。

（完）
