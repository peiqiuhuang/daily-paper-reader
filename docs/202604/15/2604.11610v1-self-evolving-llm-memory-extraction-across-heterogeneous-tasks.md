---
title: Self-Evolving LLM Memory Extraction Across Heterogeneous Tasks
title_zh: 跨异构任务的大语言模型自演化记忆提取
authors: "Yuqing Yang, Tengxiao Liu, Wang Bill Zhu, Taiwei Shi, Linxin Song, Robin Jia"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11610v1"
tags: ["query:bopl"]
score: 8.0
evidence: 针对异构任务的自进化提示词优化框架
tldr: 针对大语言模型在处理多样化任务时记忆提取需求不一的问题，本文提出了异构记忆提取任务及BEHEMOTH基准测试。研究发现现有静态提示词和自演化框架在异构任务下表现不佳，为此提出了CluE策略，通过聚类分析不同场景并合成跨类见解来优化提取提示词，显著提升了模型在复杂任务中的泛化能力。
selection_source: fresh_fetch
motivation: 现有的LLM记忆提取方法难以适应不同任务（如个性化与问题解决）对信息提取类型的多样化需求。
method: 提出CluE策略，通过对训练样本进行场景聚类、独立分析各簇特征并合成全局见解，实现提示词的自演化优化。
result: "在包含18个数据集的BEHEMOTH基准上，CluE相比现有自演化框架实现了9.04%的相对性能提升。"
conclusion: 针对异构任务采用基于聚类的自演化策略，能有效解决单一提示词在多场景记忆提取中的局限性。
---

## 摘要
随着基于大语言模型（LLM）的助手变得持久化和个性化，它们必须从过去的对话中提取并保留有用信息作为记忆。然而，值得记住的信息类型在不同任务之间存在显著差异。我们形式化了“异构记忆提取”任务，并引入了 BEHEMOTH，这是一个基准测试，它重新利用了涵盖个性化、问题解决和智能体任务的 18 个现有数据集，并使用下游效用驱动的指标进行系统评估。我们的实证分析证实，没有单一的静态提取提示词能在所有任务类别中占据主导地位，且现有的自演化提示词优化框架（最初为同质分布设计）在训练任务具有异构性时性能会下降。为了解决这一问题，我们提出了 CluE，这是一种基于聚类的自演化策略，它根据提取场景将训练样本分组为簇，独立分析每个簇，并综合跨簇的见解来更新提取提示词。在 BEHEMOTH 上的实验表明，CluE 在异构任务中具有有效的泛化能力（相对增益达 +9.04%），始终优于先前的自演化框架。

## Abstract
As LLM-based assistants become persistent and personalized, they must extract and retain useful information from past conversations as memory. However, the types of information worth remembering vary considerably across tasks. We formalize the \textit{heterogeneous memory extraction} task and introduce \textbf{BEHEMOTH}, a benchmark that repurposes 18 existing datasets spanning personalization, problem-solving, and agentic tasks, using a downstream utility-driven metric for systematic evaluation. Our empirical analysis confirms that no single static extraction prompt dominates across all task categories, and that existing self-evolving prompt optimization frameworks, originally designed for homogeneous distributions, degrade when training tasks are heterogeneous. To address this, we propose \textbf{CluE}, a cluster-based self-evolving strategy that groups training examples into clusters by extraction scenarios, analyzes each cluster independently, and synthesizes cross-cluster insights to update the extraction prompt. Experiments on BEHEMOTH show that CluE generalizes effectively across heterogeneous tasks ($+$9.04\% relative gain), consistently outperforming prior self-evolving frameworks.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在长期交互中的记忆提取问题，提出了一个全新的基准测试和一种自演化的提示词优化方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 助手向个性化和持久化发展，模型需要从历史对话中提取有用信息存入记忆。然而，**“什么信息值得记住”**在不同任务中差异巨大：对话助手关注用户偏好，而智能体（Agent）系统关注解题策略。
*   **研究动机**：现有的记忆系统通常依赖预定义的静态规则，难以兼顾多样化的现实场景。
*   **核心挑战**：在异构任务（Heterogeneous Tasks）分布下，现有的自演化提示词优化框架容易陷入“更新过快导致对近期样本过拟合”或“更新过慢导致反馈信号被稀释”的困境。

### 2. 方法论：CluE（基于聚类的演化策略）
为了在异构分布下实现稳定的提示词演化，论文提出了 **CluE (Cluster-based Evolution)**。其核心思想是将训练样本按“提取场景”进行聚类，独立分析后再综合。
*   **核心流程**：
    1.  **总结（Summarization）**：对每个训练样本的日志进行抽象，描述其提取场景（如：需要提取什么类型的信息、提取的难点在哪），而非关注具体业务领域。
    2.  **聚类（Clustering）**：由“聚类管理器”根据场景总结将样本分组。例如，将“从长对话中提取操作步骤”的样本归为一类，无论它们来自数学题还是智能体轨迹。
    3.  **簇内分析（Cluster-based Analysis）**：针对每个簇独立分析成功和失败案例，生成针对该场景的改进建议。
    4.  **跨簇综合提案（Cross-cluster Proposal）**：由“提案器”整合所有簇的分析，识别通用原则，组织特定场景的指南，并解决冲突，最终生成更新的提取提示词。

### 3. 实验设计
*   **基准测试 BEHEMOTH**：整合了 18 个现有数据集，涵盖三大类：
    *   **个性化（Personalization）**：如 MemBench, PrefEval。
    *   **问题解决（Problem-Solving）**：如 AIME, MMLU Pro, BigCodeBench。
    *   **智能体任务（Agentic）**：如 AlfWorld, ToolBench。
*   **评估指标**：采用**下游效用驱动指标**。即：提取的记忆是否能帮助模型在后续的“目标查询”中获得更高的奖励（Reward），而非依赖主观的 LLM 评分。
*   **对比方法**：
    *   **静态提示词**：Simple（极简）、Mem0（侧重事实）、ReasoningBank（侧重策略）、Survey、OpenMemory。
    *   **自演化框架**：GEPA、ACE、MemEvolve。

### 4. 资源与算力
*   **模型使用**：主要使用 **Qwen3-32B** 作为生成模型（$LLM_g$）和提取模型（$LLM_e$）。此外，还使用了 **Gemini-3-Flash** 进行跨模型泛化验证。
*   **算力消耗**：论文提供了各框架的运行效率对比（表 8）。
    *   **CluE**：总耗时约 **5.5 小时**，包含 221 次优化模型调用和 1150 次评估调用。
    *   **对比**：ACE 最慢（约 12.4 小时），MemEvolve 最快（约 5.0 小时）。
*   **硬件细节**：文中未明确说明具体的 GPU 型号和数量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖 18 个数据集，训练集包含 330 个样本，测试集对每个数据集采样（最高 200 个）。
*   **充分性**：
    *   **分布内与分布外（OOD）测试**：预留了 3 个数据集作为 OOD 测试，验证了提示词的泛化能力。
    *   **跨模型验证**：在 Qwen 和 Gemini 两个不同家族的模型上均验证了有效性。
    *   **持续记忆测试**：从单步提取扩展到持续记忆流场景，证明了提升的稳定性。
    *   **消融与定性分析**：展示了聚类如何演化以及最终生成的提示词结构。
*   **客观性**：通过固定模型配置和使用自动化奖励函数（如准确率、成功率），减少了人为偏见。

### 6. 主要结论与发现
*   **无“银弹”提示词**：没有任何一个静态提示词能在所有异构任务中表现最优。
*   **现有框架的局限**：GEPA、ACE 等在异构任务下往往“拆东墙补西墙”，在某一类任务上的提升常以另一类任务的下降为代价。
*   **CluE 的优越性**：CluE 实现了 **+9.04% 的整体相对增益**，且在所有任务类别上均有正向提升。
*   **结构化优势**：CluE 演化出的提示词具有清晰的分类体系（Taxonomy）和针对性指南，比其他框架生成的冗长或偏向性提示词更有效。

### 7. 优点
*   **系统性基准**：BEHEMOTH 填补了异构记忆提取评估的空白。
*   **聚类思想巧妙**：通过“场景聚类”而非“任务聚类”，抓住了记忆提取的本质矛盾，解决了异构分布下的优化不稳定性。
*   **效用导向**：坚持使用下游任务的实际表现作为反馈信号，比单纯的语义相似度或 LLM 裁判更具说服力。

### 8. 不足与局限
*   **实时性挑战**：虽然 CluE 演化出的提示词效果好，但其演化过程（聚类、多步分析）计算开销相对较高，不适合极高频的在线实时更新。
*   **单步与持续的鸿沟**：虽然实验验证了单步演化的提示词在持续场景有效，但演化过程本身仍是在单步环境下进行的，未考虑记忆累积带来的长期干扰。
*   **任务复杂度**：BEHEMOTH 虽然包含 18 个数据集，但每个数据集的交互轮数相对有限，对于超长对话或极复杂环境的覆盖仍有提升空间。

（完）
