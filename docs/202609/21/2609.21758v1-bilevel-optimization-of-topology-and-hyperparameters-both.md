---
title: Bilevel Optimization of Topology and Hyperparameters (BOTH)
title_zh: 拓扑与超参数的双层优化 (BOTH)
authors: "Suryanarayanan Manoj Sanu, Miguel Anibal Bessa, Alejandro Marcos Aragón"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21758v1"
tags: ["query:bopl"]
score: 9.0
evidence: 拓扑与超参数的双层优化
tldr: 该论文提出了一种名为BOTH的拓扑优化方法，通过自动微分技术实现拓扑优化过程与超参数的联合优化。针对传统拓扑优化中依赖人工调参（“魔法数字”）的痛点，该方法利用超梯度在优化过程中动态调整超参数，不仅显著降低了调参难度，还能高效处理数千个超参数，在应力约束和柔度最小化问题中展现了优异的性能。
selection_source: fresh_fetch
motivation: 传统拓扑优化高度依赖人工调参，且现有超参数优化方法难以处理大规模参数。
method: 利用自动微分技术对拓扑优化过程进行微分，计算超梯度以实现超参数与拓扑设计的同步优化。
result: 该方法在应力约束和柔度最小化任务中表现出色，且计算成本仅相当于少量标准拓扑优化运行。
conclusion: 通过将超参数纳入微分优化框架，BOTH实现了拓扑优化中超参数调优的自动化与高效化。
---

## 摘要
拓扑优化 (TO) 是迈向设计过程自动化的一大步：给定一个有效的仿真模型，TO 可以通过对仿真过程进行微分并迭代改进设计，从而一键生成可行的原型。然而在实践中，TO 充斥着所谓的“魔法数字”——即那些对结果有显著影响的超参数。寻找合适的参数值通常不仅需要深厚的领域知识，还需要大量的试错。虽然从业者可以使用基于代理模型的超参数优化作为替代方案，但这种方法要求通过严谨的问题建模来严格限制超参数的数量。在此，我们提出利用自动微分对 TO 本身进行微分。这产生了“超梯度”，使我们能够将这些超参数与主优化过程同步进行调整。我们证明，仅评估一到两步的 TO 就足以提供充分的信息，且该方法能够良好地扩展至数千个超参数，其计算成本仅相当于几次标准的 TO 运行。我们在应力约束和柔度问题上验证了该方法，其中后者利用了密度场的神经参数化。

## Abstract
Topology optimization (TO) represents a significant step towards automating the design process: given a working simulation, TO can produce a viable prototype at the press of a button by differentiating the simulation and iteratively improving the design. In practice, however, TO is riddled with ``magic numbers''---hyperparameters whose tuning significantly affects the outcome. Finding the right values typically requires not only deep problem-specific knowledge but also extensive trial-and-error. While practitioners can use surrogate-assisted hyperparameter optimization as an alternative, this approach requires strictly limiting the number of hyperparameters through careful problem formulation. Here, we propose differentiating TO itself using automatic differentiation. This yields ``hypergradients'' that allow us to tune these hyperparameters in tandem with the primary optimization. We show that evaluating just one or two steps of TO is sufficiently informative and that the method scales favorably to thousands of hyperparameters at an expense comparable to only a few standard TO runs. We demonstrate this approach on stress-constrained and compliance problems, with the latter utilizing a neural parameterization of the density field.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bilevel Optimization of Topology and Hyperparameters (BOTH)》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：拓扑优化（TO）虽能实现设计自动化，但其算法中充斥着大量“魔法数字”（超参数），如惩罚因子、过滤半径、投影阈值等。
*   **痛点**：这些超参数对最终拓扑结构影响巨大，但目前主要依赖专家经验和繁琐的试错（Trial-and-error）。现有的黑盒优化方法（如贝叶斯优化）在处理大规模超参数时计算成本极高，且难以扩展。
*   **目标**：提出一种基于梯度的双层优化框架（BOTH），实现设计变量与超参数的同步自动优化，从而消除对启发式规则的依赖。

### 2. 方法论：BOTH 框架
*   **核心思想**：将超参数优化建模为双层优化问题，利用自动微分（AD）技术计算“超梯度”，通过微分优化路径来更新超参数。
*   **关键技术**：
    *   **伪一阶超梯度**：为了降低计算复杂度和数值不稳定性，通过 `stop-gradient` 操作忽略 Hessian 矩阵项，仅保留混合偏导数项，使计算变得高效且稳定。
    *   **自适应退火（Annealing）**：初始阶段仅进行 1-2 步内层优化（LL），随着外层优化（UL）的成熟，逐步增加内层迭代步数，以平衡计算成本与梯度准确性。
    *   **热启动（Warmstarting）**：利用上一轮 UL 迭代的终点作为下一轮 LL 优化的起点，确保优化轨迹的平滑性，并显著降低收敛所需的迭代次数。
    *   **二次惩罚法（QPM）**：将带约束的拓扑优化问题转化为无约束问题，便于利用 Adam 等一阶优化器进行微分。

### 3. 实验设计
*   **场景**：
    *   **基准测试**：SMD-1 双层优化基准问题（具有解析解）。
    *   **柔度最小化**：MBB 梁问题，包含标准密度参数化和神经参数化（CNN）。
    *   **应力约束优化**：两杆结构和 L 型支架问题。
*   **对比方法**：主要与贝叶斯优化（BO）进行对比，BO 被视为低维超参数调优的强基准。

### 4. 资源与算力
*   **说明**：论文未明确列出具体的 GPU 型号、数量或总训练时长。
*   **计算效率**：论文强调该方法在计算成本上与几次标准 TO 运行相当，且其计算开销主要由线性求解器（Linear Solves）决定，与超参数的数量维度解耦，具有良好的扩展性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单的解析基准到复杂的神经参数化拓扑优化，并进行了高维（6915 个超参数）扩展实验。
*   **客观性**：进行了多组消融实验（如不同权重系数 $k$、不同初始化方案、不同学习率等），并对比了冷启动与热启动的效果。实验设计严谨，通过多次随机初始化运行来统计中位数和标准差，具有较好的公平性和说服力。

### 6. 主要结论与发现
*   **有效性**：即使仅进行 1-2 步内层优化，所产生的超梯度也足以驱动外层优化，且效果优于传统的黑盒调参。
*   **扩展性**：该方法能够轻松处理数千个超参数（如每个单元独立的投影锐度），这是传统 derivative-free 方法无法实现的。
*   **鲁棒性**：热启动策略不仅加速了收敛，还通过平滑优化景观显著降低了非凸性带来的负面影响。

### 7. 优点
*   **创新性**：首次将微分双层优化引入拓扑优化领域，打破了超参数数量的限制。
*   **高效性**：通过伪一阶近似和热启动，将昂贵的超参数调优转化为与标准 TO 成本相当的过程。
*   **通用性**：框架不仅适用于密度法，还可扩展至神经参数化等现代 TO 变体。

### 8. 不足与局限
*   **侵入性**：该方法要求整个优化流程（包括物理仿真）必须在支持自动微分的框架（如 JAX）中实现，对现有代码库的重构要求较高。
*   **贪婪搜索**：本质上是局部优化，对超参数的初始值仍有一定依赖。
*   **应用限制**：目前主要针对连续超参数，对于离散的算法选择（如选择哪种滤波器）仍需手动设计或进行连续化松弛。
*   **设计依赖**：框架并未完全消除人工判断，用户仍需定义双层问题的目标函数、归一化方式及初始启发式策略。

（完）
