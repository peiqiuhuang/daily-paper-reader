---
title: Degradation-Aware Prompt Learning with Cross-Modal Compensation for Adverse Weather Removal
title_zh: 基于跨模态补偿的退化感知提示学习用于恶劣天气去除
authors: "Wanshu Fan, Yunzhe Zhang, Yue Shen, Liyan Wang, Jing Qin, Kin-Man Lam, Cong Wang, Jinshan Pan"
date: 2026-08-07
pdf: "https://arxiv.org/pdf/2608.06939v1"
tags: ["query:bopl"]
score: 8.0
evidence: 视觉语言模型的退化感知提示学习
tldr: 针对恶劣天气导致的复杂图像退化，本文提出DCMPC-Net，一种利用预训练视觉语言模型提供跨模态退化线索的统一修复框架。该方法通过跨模态提示生成器、注意力对齐模块和双特征补偿模块，实现了退化语义与空间信息的有效结合，显著提升了模型在多种天气条件下的修复精度和视觉一致性。
selection_source: fresh_fetch
motivation: 现有全能修复模型缺乏对退化特征的显式空间和语义建模，难以适应多变的恶劣天气条件。
method: 提出DCMPC-Net，利用跨模态提示生成器融合文本与视觉特征，并通过注意力对齐和双特征补偿模块引导图像修复。
result: 实验表明，DCMPC-Net在特定任务和统一天气修复设置下均优于现有最先进方法，具有更高的准确性和视觉保真度。
conclusion: 该研究证明了结合跨模态语义引导与空间结构增强能有效提升恶劣天气下的图像恢复性能。
---

## 摘要
恶劣天气会导致多样且复杂的图像退化，严重损害了计算机视觉系统的可靠性。现有的通用修复模型尝试在统一框架内处理多种退化类型，但往往缺乏对退化特征的显式空间和语义建模，限制了它们对多样化天气条件的适应性。为了解决这一局限性，我们提出了一种退化感知跨模态提示补偿网络（DCMPC-Net），该网络利用预训练视觉-语言模型中的跨模态退化线索，在统一的主干网络中调节修复特征。具体而言，我们的 DCMPC-Net 主要由跨模态提示生成器（CMPG）、提示引导注意力对齐模块（PGAAM）和双特征补偿模块（DFCM）组成。CMPG 将文本嵌入与视觉特征相结合，生成编码了退化相关语义和上下文线索的退化感知提示。这些提示通过 PGAAM 注入解码器，该模块自适应地将语义信息与退化区域对齐，以促进上下文感知的修复。为了进一步增强结构保真度，引入了 DFCM，它将退化伪影与场景结构解耦，从而改善了精细纹理和细节内容的重建。通过将跨模态语义引导与空间对齐和结构增强相结合，DCMPC-Net 在多种天气条件下实现了鲁棒且感知一致的修复。大量实验表明，DCMPC-Net 在特定任务和统一设置下均优于现有最先进的方法，实现了卓越的准确性和视觉保真度。

## Abstract
Adverse weather causes diverse and complex image degradations, severely compromising the reliability of computer vision systems. Existing all-in-one restoration models attempt to address multiple degradation types within a unified framework, but often lack explicit spatial and semantic modeling of degradation characteristics, limiting their adaptability to diverse weather conditions. To address this limitation, we propose a Degradation-Aware Cross-Modal Prompt Compensation Network (DCMPC-Net) that leverages cross-modal degradation cues from a pretrained vision-language model to condition restoration features within a unified backbone. Specifically, our DCMPC-Net mainly consists of the Cross-Modal Prompt Generator (CMPG), Prompt-Guided Attention Alignment Module (PGAAM), and Dual Feature Compensation Module (DFCM). The CMPG integrates textual embeddings with visual features to produce degradation-aware prompts that encode degradation-related semantic and contextual cues. These prompts are injected into the decoder via a PGAAM, which adaptively aligns semantic information with degraded regions to facilitate context-aware restoration. To further enhance structural fidelity, DFCM is introduced that disentangles degradation artifacts from scene structures, thereby improving the reconstruction of fine textures and detailed content. By integrating cross-modal semantic guidance with spatial alignment and structural enhancement, DCMPC-Net achieves robust and perceptually consistent restoration across diverse weather conditions. Extensive experiments show that DCMPC-Net outperforms state-of-the-art methods in both task-specific and unified settings, achieving superior accuracy and visual fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Degradation-Aware Prompt Learning with Cross-Modal Compensation for Adverse Weather Removal》（基于跨模态补偿的退化感知提示学习用于恶劣天气去除）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：恶劣天气（如雨、雪、雾）会导致复杂的图像退化，影响计算机视觉系统的可靠性。现有的“全能型”（All-in-one）修复模型虽然试图在统一框架下处理多种天气，但往往缺乏对退化特征的**显式空间建模**和**语义理解**，导致在处理多样化且空间分布不均的退化时效果不佳。
*   **研究背景**：传统的深度学习方法主要依赖视觉特征，而忽略了文本语义（如“大雨”、“薄雾”）能提供的宏观退化线索。本文旨在通过引入预训练视觉-语言模型（VLM）的跨模态信息，增强模型对不同天气类型的感知和修复能力。

### 2. 方法论
论文提出了 **DCMPC-Net**，其核心思想是利用跨模态提示来引导图像修复过程。主要包含三个关键组件：
*   **跨模态提示生成器 (CMPG)**：利用预训练的 CLIP 模型提取文本嵌入（描述天气类型），并将其与图像的视觉特征融合，生成包含退化语义和上下文线索的“退化感知提示”。
*   **提示引导注意力对齐模块 (PGAAM)**：这是一个空间对齐机制。它将生成的提示信息注入解码器，通过注意力机制自适应地将语义信息与图像中的退化区域（如雨丝位置）进行对齐，确保修复过程具有上下文感知性。
*   **双特征补偿模块 (DFCM)**：旨在增强结构保真度。该模块将退化伪影与场景结构进行解耦，通过双路径处理分别强化精细纹理和全局结构，从而改善重建图像的细节。
*   **算法流程**：输入退化图像 -> CMPG 生成提示 -> 编码器提取多尺度特征 -> 解码器结合 PGAAM 和 DFCM 进行特征对齐与补偿 -> 输出清晰图像。

### 3. 实验设计
*   **数据集**：
    *   **统一设置（All-in-one）**：在包含雨、雪、雾的混合数据集（如 WeatherStream）上进行训练和测试。
    *   **特定任务设置**：分别在 Rain100L/H（去雨）、Snow100K（去雪）、RESIDE-OTS（去雾）等标准基准上验证。
*   **Benchmark 与对比方法**：
    *   对比了多种 SOTA（最先进）方法，包括通用修复模型（如 Restormer, MPRNet）、全能型模型（如 AirNet, TransWeather, PromptIR）以及特定任务的领先模型。
    *   评价指标采用峰值信噪比（PSNR）和结构相似性（SSIM）。

### 4. 资源与算力
*   **硬件环境**：论文提到实验是在 **NVIDIA GeForce RTX 3090 GPU** 上进行的。
*   **训练细节**：使用了 AdamW 优化器，初始学习率为 $2 \times 10^{-4}$，并采用余弦退火策略。具体的训练时长未在摘要和核心部分详细说明，但通常此类 Transformer 架构在 3090 上需要数天的训练时间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比实验，覆盖了 5 种以上的不同天气退化场景。
    *   **消融实验**：系统地验证了 CMPG、PGAAM 和 DFCM 每个组件的有效性，证明了跨模态提示和空间对齐的必要性。
    *   **定性分析**：提供了大量的视觉对比图，展示了模型在处理密集雨条和厚雪时的视觉优势。
*   **充分性评价**：实验设计较为全面，不仅对比了同类全能模型，还跨越了多个独立任务的数据集，客观地证明了该方法的泛化能力。

### 6. 主要结论与发现
*   **跨模态引导的有效性**：引入文本语义提示能显著帮助模型区分不同类型的退化，尤其是在多种天气混合的复杂场景下。
*   **性能领先**：DCMPC-Net 在 PSNR 和 SSIM 指标上均优于现有的全能修复模型（如比 PromptIR 在某些数据集上提升了约 0.5-1.0 dB）。
*   **结构保持**：通过 DFCM 模块，模型在去除退化的同时，能更好地保留图像的边缘和纹理，减少了过度平滑的现象。

### 7. 优点
*   **创新性地结合跨模态信息**：将 VLM 的语义能力引入底层视觉修复任务，解决了纯视觉模型难以理解复杂退化语义的问题。
*   **空间对齐机制**：PGAAM 解决了提示信息与图像局部特征不匹配的问题，提高了修复的精确度。
*   **端到端统一框架**：无需针对每种天气单独训练模型，降低了部署成本。

### 8. 不足与局限
*   **计算开销**：引入预训练 VLM 和复杂的注意力对齐模块会增加推理时的计算负担和内存占用。
*   **对文本提示的依赖**：模型性能可能受限于预定义文本描述的准确性，对于未见过的极端复合天气，提示的有效性可能下降。
*   **实时性**：论文未详细讨论在移动端或嵌入式设备上的实时运行表现，这对于自动驾驶等应用至关重要。

（完）
