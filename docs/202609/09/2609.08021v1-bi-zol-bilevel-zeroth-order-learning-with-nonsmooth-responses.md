---
title: "Bi-ZOL: Bilevel Zeroth-Order Learning with Nonsmooth Responses"
title_zh: Bi-ZOL：具有非光滑响应的双层零阶学习
authors: "Zhisen Jiang, Saverio Bolognani"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.08021v1"
tags: ["query:bopl"]
score: 10.0
evidence: 针对非平滑问题的双层零阶学习
tldr: 本文针对下层模型信息不可知且响应映射非光滑的双层优化问题，提出了Bi-ZOL算法。该方法通过分离双层链式法则结构，利用零阶采样估计响应雅可比矩阵，而非对整个超目标函数进行平滑处理。研究证明了该方法在非光滑环境下的收敛性，并有效降低了结构偏差，在激励跟踪任务中表现优于传统零阶平滑方法。
selection_source: fresh_fetch
motivation: 现有双层优化方法在下层响应映射非光滑且模型信息缺失时，因无法计算响应雅可比矩阵而失效。
method: 提出Bi-ZOL算法，通过保留上层精确偏导数并仅对响应雅可比矩阵进行零阶采样估计，实现对Clarke链式法则次微分的有效逼近。
result: "理论证明了该算法在有限时间内收敛至平稳点，且在分段C1,1响应下具有O(δ)的结构偏差。"
conclusion: Bi-ZOL在处理非光滑双层优化问题时，比传统零阶平滑方法具有更高的计算效率和更小的平稳性间隙。
---

## 摘要
本文研究了响应预言机设置下的下层约束双层优化问题，其中下层模型信息不可用，且诱导的响应映射局部 Lipschitz 连续但可能非光滑。在这种设置下，经典的响应雅可比矩阵和约简超梯度可能不存在。我们提出了双层零阶学习（Bi-ZOL），这是一种结构引导的零阶方法，用于寻找非光滑约简问题的平稳点。Bi-ZOL 没有对完全平滑的约简超目标函数进行梯度估计，而是分离了双层链式法则结构：它在查询响应处保留精确的上层偏导数，并仅使用零阶采样来估计响应雅可比矩阵。这种构造产生了一个与 Clarke 链式法则次微分更直接对齐的近似超梯度。我们证明了 Bi-ZOL 方向允许部分平滑解释，量化了其逐点结构偏差，并证明了其在有限时间内收敛到 (δ,ε)-Bi-ZOL Frank-Wolfe 平稳点。在局部正则性条件下，对于分段 C^{1,1} 响应，偏差为 O(δ)；对于活动单元邻域上的分段仿射响应，偏差消失。在基于激励的跟踪问题上的实验表明，在相当的响应预言机预算下，Bi-ZOL 比传统的零阶平滑方法实现了更小的平稳性间隙和更低的超目标函数值。

## Abstract
This paper studies lower-level-constrained bilevel optimization in a response-oracle setting, where lower-level model information is unavailable and the induced response mapping is locally Lipschitz but potentially nonsmooth. In this setting, the classical response Jacobian and reduced hypergradient may fail to exist. We propose Bilevel Zeroth-Order Learning (Bi-ZOL), a structure-guided zeroth-order method for finding stationary points of the nonsmooth reduced problem. Instead of estimating the gradient of a fully smoothed reduced hyperobjective, Bi-ZOL separates the bilevel chain-rule structure: it keeps the exact upper-level partial gradients at the queried response and uses zeroth-order sampling only to estimate the response Jacobian. This construction yields an approximate hypergradient that is more directly aligned with the Clarke chain-rule subdifferential. We show that the Bi-ZOL direction admits a partial-smoothing interpretation, quantify its pointwise structural bias, and prove finite-time convergence to a $(δ,ε)$-Bi-ZOL Frank--Wolfe stationary point. The bias is $O(δ)$ for piecewise $C^{1,1}$ responses under local regularity and vanishes for piecewise affine responses on active-cell neighborhoods. Experiments on incentive-based tracking problems show that Bi-ZOL achieves smaller stationarity gaps and lower hyperobjective values than vanilla zeroth-order smoothing under comparable response-oracle budgets.

---

## 论文详细总结（自动生成）

以下是对论文《Bi-ZOL: Bilevel Zeroth-Order Learning with Nonsmooth Responses》的结构化总结：

### 1. 核心问题与研究动机
在许多分层决策系统（如交通网络、能源调度、超参数调优）中，双层优化问题普遍存在。现有方法通常依赖于下层问题的显式模型信息（如梯度或 Hessian 矩阵）来计算响应雅可比矩阵（Response Jacobian）。然而，在**响应预言机（Response-Oracle）**设置下，下层模型是黑盒，且由于下层约束的存在，响应映射往往是**非光滑（Nonsmooth）**的，导致经典的响应雅可比矩阵可能不存在。传统的“完全零阶平滑”（VZO）方法通过对整个超目标函数进行平滑处理，不仅引入了不必要的偏差，还破坏了双层优化固有的链式法则结构。

### 2. 方法论：Bi-ZOL
*   **核心思想**：采用“部分平滑”策略。保留上层目标函数在当前观测响应处的精确偏导数，仅对缺失的、可能非光滑的响应雅可比矩阵进行零阶采样估计。
*   **关键技术**：
    *   **结构引导的近似超梯度**：利用 Clarke 链式法则次微分结构，构造近似超梯度 $g_\delta(x)$。
    *   **零阶雅可比估计**：通过两点随机扰动（Two-point random perturbations）估计响应雅可比矩阵。
    *   **Frank-Wolfe 算法框架**：将估计出的近似超梯度嵌入 Frank-Wolfe 迭代中，确保更新始终在可行域内。
    *   **理论支撑**：证明了该方法在分段 $C^{1,1}$ 响应下具有 $O(\delta)$ 的结构偏差，且在分段仿射（PWA）响应下偏差可降为零。

### 3. 实验设计
*   **场景**：基于激励的跟踪问题（Incentive-based tracking problems），包含两种下层约束：盒约束（Box constraints，产生 PWA 响应）和欧几里得球约束（Euclidean ball constraints，产生非 PWA 响应）。
*   **Benchmark 与对比**：对比了 Bi-ZOL 与传统的“完全零阶平滑”（VZO）方法。
*   **评价指标**：Bi-ZOL Frank-Wolfe 平稳性间隙（Stationarity gap）、Clarke Frank-Wolfe 平稳性间隙、超目标函数值（Hyperobjective value）。

### 4. 资源与算力
*   **说明**：论文中未明确提及具体的硬件型号（如 GPU 型号、数量）或具体的训练时长。实验主要侧重于算法的收敛性分析和不同参数（如 Batch size、维度）下的性能对比。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组实验，包括不同 Batch size 下的收敛性对比、不同维度（$n, m$）下的可扩展性测试。
*   **客观性与公平性**：实验设计较为严谨，所有对比方法在相同问题实例、相同初始点、相同平滑半径和相同响应预言机预算下进行，并进行了 10 次独立运行以报告均值和标准差，结果具有统计学意义。

### 6. 主要结论与发现
*   **性能优势**：在相同响应预言机预算下，Bi-ZOL 相比 VZO 实现了更小的平稳性间隙和更低的超目标函数值。
*   **结构对齐**：Bi-ZOL 更好地对齐了非光滑问题的 Clarke 链式法则几何结构，在处理非光滑响应时表现出更强的鲁棒性。
*   **可扩展性**：Bi-ZOL 在不同维度规模下均表现出比 VZO 更稳定的收敛性能，且对响应维度 $m$ 的依赖性较小。

### 7. 优点
*   **结构利用率高**：巧妙地利用了双层优化的链式法则结构，避免了对已知信息的盲目平滑，减少了方差。
*   **理论严谨**：提供了清晰的结构偏差量化分析和从 Bi-ZOL 平稳性到 Clarke 平稳性的转换证明。
*   **通用性**：不仅适用于分段仿射（PWA）响应，在具有弯曲边界的非 PWA 响应下同样有效。

### 8. 不足与局限
*   **收敛速度依赖**：虽然证明了有限时间收敛，但对于极高精度的要求，零阶方法的查询复杂度依然较高。
*   **参数敏感性**：平滑半径 $\delta$ 和步长 $\gamma$ 的选择对实际收敛效果有显著影响，文中虽给出了理论指导，但在复杂黑盒场景下调参可能具有挑战性。
*   **应用限制**：该方法主要针对下层响应映射为单值映射（Single-valued mapping）的情况，对于多值映射（Set-valued mapping）的复杂情况未做深入探讨。

（完）
