---
title: "Control-Data Flow Separation: Stable Prompt Optimization in Multi-Agent LLMs"
title_zh: 控制流与数据流分离：多智能体大语言模型中的稳定提示词优化
authors: "Wentao Zhang, Syed Shariyar Murtaza, Junaid Ahmad Bhatti, Utkarsh Soni, Yifan Nie, Eugene Wen, Yuntian Deng"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.00621v1"
tags: ["query:bopl"]
score: 9.0
evidence: 通过控制-数据流分离实现稳定的提示词优化
tldr: "针对多智能体大模型系统中提示词优化容易破坏执行协议（如路由、格式）的问题，本文提出“控制-数据流分离”框架。该方法将执行关键的控制逻辑表示为类型化的程序对象，而将任务相关内容保留为可优化的语言数据流。这种设计防止了提示词优化导致的系统崩溃，在合成推理、协作评论生成等任务中实现了100%的协议有效性，并显著提升了任务性能。"
selection_source: fresh_fetch
motivation: 提示词中任务内容与执行协议的耦合导致优化过程中容易出现协议失效，进而引发整个智能体流水线的崩溃。
method: 提出将执行控制表示为结构化的程序对象，而将任务语言作为独立的可优化数据流进行分离管理。
result: "在多种复杂工作流中实现了100%的协议有效性，并持续提升了多智能体系统的任务表现。"
conclusion: 通过控制与数据的解耦，可以实现更稳定、高效且不破坏系统结构的提示词自动优化。
---

## 摘要
提示词优化可以提升多智能体大语言模型（LLM）系统的性能，但被优化的提示词通常承担着两个交织在一起的角色：生成任务相关内容以及指定执行关键协议（如消息路由、输出格式化和终止信号），而底层代码正是依赖于这些协议。因此，旨在改进内容生成的提示词编辑可能会无意中破坏协议，导致整个智能体流水线失效。我们的核心观察是，这两个角色具有不同的表示形式：执行协议通常是结构化的，而任务相关内容通常以非结构化语言表达。基于此，我们提出了控制流与数据流分离，将执行关键控制表示为有类型的、经过验证的程序对象，而任务相关语言则保持为用于智能体通信的可优化数据流。这种设计允许优化器在不将路由或格式化接口暴露给提示词漂移（prompt drift）的情况下改进多智能体行为。在合成推理、协作评论生成和保险评级工作流中，实验表明我们的框架实现了 100% 的最终协议有效性，同时持续提升了任务性能。

## Abstract
Prompt optimization can improve multi-agent LLM systems, but the prompts being optimized often serve two entangled roles: generating task-relevant content and specifying execution-critical protocols, such as message routing, output formatting, and termination signals, on which the underlying code relies. As a result, a prompt edit intended to improve content generation can inadvertently corrupt the protocol and cause the entire agent pipeline to fail. Our key observation is that these two roles have different representations: execution protocols are typically structured, while task-relevant content is usually expressed in unstructured language. Based on this, we propose control-data flow separation, where execution-critical control is represented as typed, validated program objects, while task-relevant language remains the optimizable data flow for agent communication. This design allows optimizers to improve multi-agent behavior without exposing the routing or formatting interface to prompt drift. Across synthetic reasoning, collaborative review generation, and insurance rating workflows, our framework empirically achieves 100% eventual protocol validity while consistently improving task performance.

---

## 论文详细总结（自动生成）

这篇论文由滑铁卢大学和宏利金融（Manulife）的研究团队合作完成，旨在解决多智能体大语言模型（LLM）系统在提示词优化过程中面临的“协议脆弱性”问题。

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在多智能体系统中，提示词（Prompt）通常承担双重角色：一是生成任务内容（如摘要、评论），二是规定执行协议（如JSON格式、路由指令、终止信号）。
*   **核心问题**：现有的提示词优化方法（如 TextGrad, DSPy）通常将整个提示词视为可编辑参数。在优化过程中，为了提升内容质量，优化器往往会无意中修改或删除格式指令，导致解析失败、路由错误或系统崩溃。这种“内容”与“控制”的交织使得多智能体流水线的自动化优化变得极其不稳定。

### 2. 方法论：核心思想与技术细节
论文提出了**控制-数据流分离（Control-Data Flow Separation）**框架，其核心思想是将执行逻辑与任务语言解耦：
*   **双通道输出**：每个智能体的输出被划分为两个通道：
    *   **控制通道（Control Channel）**：有类型的、结构化的程序对象（如 Python dataclass 或 Pydantic 模型），用于驱动路由和终止逻辑。
    *   **数据通道（Data Channel）**：非结构化的自然语言消息，用于智能体间的实质性通信。
*   **关键技术细节**：
    *   **冻结架构（Frozen Scaffolding）**：将控制协议的格式指令放置在优化器不可触及的“冻结槽”中，确保优化器只能修改数据通道的提示词。
    *   **类型化验证**：程序控制器仅根据经过验证的控制对象做出决策，而不解析非结构化数据。
    *   **重试机制**：如果 LLM 输出的控制块不符合 Schema，系统会触发有限次数的重试或回退到默认安全操作，从而保证流水线不中断。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **BBH (Big-Bench Hard)**：单智能体推理任务，涵盖逻辑推理、对象追踪等。
    2.  **MARG 评论生成**：多智能体协作任务（1个领导者 + 3个工作者），模拟科学论文评审。
    3.  **保险核保（合成）**：模拟人寿保险核保流程，根据医疗摘要进行风险评级。
    4.  **保险核保（行业验证）**：使用由专业核保师标注的合成医疗数据，涉及更复杂的知识库检索。
*   **对比方法（Baselines）**：
    *   **Fixed**：手动编写的固定提示词。
    *   **Naive TextGrad**：直接对原始提示词进行文本梯度优化（不分离控制流）。
    *   **DSPy**：包括未编译版本、BootstrapFewShot 和 MIPROv2 优化器。
*   **评估指标**：任务准确率（Acc）、Jaccard 相似度（用于评论生成）以及**稳定性（Stability）**（即协议未失效的比例）。

### 4. 资源与算力
*   **算力使用**：论文未提及使用本地 GPU 进行训练，所有实验均通过 API 调用完成。
*   **模型选择**：使用了 OpenAI 的 `gpt-4o-mini` 和 `gpt-4o-nano`（推测为 mini 的变体）作为智能体模型，`gpt-4o-mini` 作为优化器。
*   **成本与环境**：在单台 Linux 服务器上运行。MARG 任务的一次完整优化运行成本约为 3 美元。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单智能体到复杂多智能体路由的 4 种不同任务。
*   **充分性**：
    *   每项实验均运行了 3 个随机种子并取平均值。
    *   进行了详细的**消融实验**，分别验证了 Schema 架构、重试机制和样本反馈对性能的影响。
    *   **跨模型验证**：在 OpenAI 之外，还测试了 Anthropic (Claude) 和 Google (Gemini) 模型，证明了该框架的通用性。
*   **客观性**：对比了当前最先进的 DSPy 框架，并在相同的算力预算和基础模型下进行公平竞争。

### 6. 主要结论与发现
*   **100% 稳定性**：在所有测试场景中，该框架均实现了 100% 的最终协议有效性，而 Naive TextGrad 在复杂任务中稳定性常跌至 0%。
*   **性能提升**：在保证不崩溃的前提下，该方法在所有任务上的表现均优于基准线。例如在评论生成任务中，Jaccard 分数从 31.0 提升至 44.4。
*   **防止提示词漂移**：分析显示，Naive 方法在优化过程中会频繁修改 JSON 字段名或格式指令，而分离框架有效地将优化集中在任务逻辑上。

### 7. 优点
*   **工程实用性极强**：解决了多智能体系统落地的核心痛点——优化过程中的系统崩溃问题。
*   **轻量化实现**：通过 Python 库 `cdsep` 实现，开发者只需不到 40 行代码即可构建稳定的多智能体流水线。
*   **理论保证**：论文形式化证明了该框架在满足特定条件下具有“协议稳定性”保证。

### 8. 不足与局限
*   **语义正确性不等于协议正确性**：该框架保证了系统“不崩溃”，但不能保证智能体生成的业务内容在语义上是完全正确的。
*   **Schema 固定**：目前要求预先定义智能体角色和控制 Schema，不支持在运行过程中动态创建智能体或演化协议。
*   **依赖 LLM 裁判**：在 MARG 任务中使用了 LLM 作为裁判进行评估，这可能引入 LLM 固有的偏见（如偏好长文本）。

（完）
