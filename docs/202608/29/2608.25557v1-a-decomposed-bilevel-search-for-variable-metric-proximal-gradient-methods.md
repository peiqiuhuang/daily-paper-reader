---
title: A Decomposed Bilevel Search for Variable-Metric Proximal Gradient Methods
title_zh: 变度量近端梯度法的分解双层搜索
authors: "Xinpeng Li, Ya-xiang Yuan"
date: 2026-08-26
pdf: "https://arxiv.org/pdf/2608.25557v1"
tags: ["query:bopl"]
score: 9.0
evidence: 用于优化的分解双层搜索方法
tldr: 本文针对复合凸优化中的变度量近端梯度法，解决了缩放近端映射缺乏闭式解的难题。提出了一种名为分解双层搜索（DBS）的新方法，通过对角加秩一因子构造拟牛顿度量，将复杂的缩放近端步骤简化为二维单调残差系统的求解。该方法在强凸条件下具有线性收敛性，且内层求解复杂度低。实验证明，DBS在处理SLOPE和群组Lasso等高维问题时，相比FISTA等传统方法，能以更少的梯度评估次数达到更高精度，显著提升了计算效率。
selection_source: fresh_fetch
motivation: 变度量近端梯度法虽能加速优化，但其诱导的缩放近端映射通常缺乏闭式解，导致计算开销巨大。
method: 提出分解双层搜索（DBS）算法，利用对角加秩一因子构造度量，将缩放近端步骤转化为高效的二维单调残差系统求解。
result: 实验显示DBS在处理高维SLOPE和群组Lasso问题时，梯度评估次数远少于FISTA，且在大规模数据集上具有明显的时间优势。
conclusion: DBS为变度量近端方法提供了一种高效、可靠且具有理论收敛保证的内层求解方案，有效提升了复杂凸优化的求解性能。
---

## 摘要
变度量近端方法可加速复合凸优化，但由拟牛顿度量诱导的缩放近端映射（scaled proximal map）很少具有闭式解。我们开发了分解双层搜索（DBS），该方法基于对角加秩一因子 \(X=D+uv^\top\)，其对角缩放满足弱割线方程。所诱导的逆度量 \(B^{-1}=XX^\top\) 涵盖了无记忆 DFP/BFGS 型 Broyden 族成员，而因子形式将每个缩放近端步骤简化为一个二维单调残差系统。每次残差评估需要一次对角度量近端映射，且经过认证的双层求解能在 \(\mathcal O((d+T_p)\log^2(1/ε))\) 的工作量内达到目标精度 \(ε\)，其中 \(T_p\) 是该近端映射的开销。在强凸条件下，外层方法在精确和非精确内层求解下均呈线性收敛。在标量特化 \(D=αI\) 中，该算子（oracle）仅使用正则化项的普通近端评估，无需广义雅可比或活动集信息。在有序加权 \(\ell_1\)（SLOPE/OWL）和 group-lasso 逻辑回归上的实验评估了内层算子和完整的外层方法。该算子仅需几百次普通近端评估即可解决条件数高达 \(4\times10^6\) 的高维 SLOPE 缩放近端子问题。在 SLOPE 外层基准测试中，热启动算子使缩放近端开销保持可控，DBS 以远少于 Lipschitz 归一化 FISTA 的梯度评估次数达到严格目标；在 \texttt{real-sim} 数据集上，这表现出明显的时间优势。在 group-lasso 逻辑回归中，DBS 在合成相关实例上表现可靠，并在真实数据分组复制实例上速度最快。

## Abstract
Variable-metric proximal methods accelerate composite convex optimization,   but the scaled proximal map induced by a quasi-Newton metric rarely   has a closed form. We develop \emph{Decomposed Bilevel Search} (DBS), based   on a diagonal-plus-rank-one factor \(X=D+uv^\top\) whose diagonal scaling   satisfies a weak secant equation. The induced inverse metric   \(B^{-1}=XX^\top\) recovers zero-memory DFP/BFGS-type Broyden members, while   the factor form reduces each scaled proximal step to a two-dimensional   monotone residual system. Each residual evaluation requires one   diagonal-metric proximal map, and a certified bilevel solve reaches target   accuracy \(ε\) in   \(\mathcal O((d+T_p)\log^2(1/ε))\) work, where \(T_p\) is the cost of   that proximal map. Under strong convexity, the outer method converges   linearly with exact and inexact inner solves. In the scalar   specialization \(D=αI\), the oracle uses only ordinary proximal   evaluations of the regularizer and no generalized Jacobian or active-set   information.   Experiments on ordered-weighted \(\ell_1\) (SLOPE/OWL) and group-lasso   logistic regression evaluate both the inner oracle and the full outer   method. The oracle solves   high-dimensional SLOPE scaled proximal subproblems of   condition number up to \(4\times10^6\) using a few hundred ordinary   proximal evaluations. In SLOPE outer benchmarks, the   warm-started oracle keeps the scaled-proximal overhead controlled and DBS   reaches stringent targets with far fewer gradient evaluations than   Lipschitz-normalized FISTA; on   \texttt{real-sim} this becomes a clear target-time advantage. On   group-lasso logistic regression, DBS is reliable on synthetic correlated   instances and fastest on a real-data grouped-copy instance.

---

## 论文详细总结（自动生成）

这篇论文由中国科学院数学与系统科学研究院的 Xinpeng Li 和袁亚湘院士撰写，提出了一种名为“分解双层搜索”（Decomposed Bilevel Search, DBS）的新算法，旨在解决复合凸优化中变度量近端梯度法的计算瓶颈。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在复合凸优化问题 $\min f(x) + h(x)$ 中，变度量近端梯度法（如近端拟牛顿法）虽然比一阶方法收敛更快，但其核心步骤——**缩放近端映射（Scaled Proximal Mapping）**通常没有闭式解。
*   **研究动机**：现有的解决方法要么限制度量矩阵的结构（如仅使用对角阵），导致无法充分利用二阶曲率信息；要么需要复杂的内部迭代求解器（如半光滑牛顿法），这依赖于正则化项 $h(x)$ 的特定结构（如广义雅可比信息）。
*   **整体含义**：本文旨在开发一种通用的、高效的内部求解算子，使得在仅能访问 $h(x)$ 普通近端算子的情况下，也能高效计算秩一修正度量下的缩放近端映射。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用**对角加秩一（Diagonal-plus-rank-one）**因子 $X = D + uv^\top$ 来构造逆度量矩阵 $B^{-1} = XX^\top$。这种结构能够涵盖无记忆的 DFP 和 BFGS 等 Broyden 族拟牛顿度量。
*   **关键技术细节**：
    *   **降维转化**：通过因子分解，将 $d$ 维的缩放近端子问题转化为一个**二维单调残差系统**的寻根问题。
    *   **双层搜索（DBS）**：
        *   **外层**：采用二维剪切平面法（如椭圆法）进行单调定位。
        *   **内层**：利用残差系统的单调性，通过二分法进行认证求解。
    *   **算子评估**：每次残差评估仅需调用一次对角度量下的近端映射（在标量缩放时即为普通近端映射），计算复杂度为 $O(d)$。
    *   **热启动与安全策略**：引入预测机制利用前一轮迭代信息，并设有“认证回退”机制，确保在最坏情况下也能达到 $O(\log^2(1/\epsilon))$ 的复杂度。
*   **算法流程**：在外层迭代中，根据梯度和位移更新 $X$ 因子，然后调用 DBS 算子求解近端步骤，最后更新变量。

### 3. 实验设计
*   **应用场景**：
    1.  **SLOPE (Sorted $\ell_1$) 逻辑回归**：一种非平滑、非对称且结构复杂的正则化问题。
    2.  **Group-Lasso 逻辑回归**：具有块稀疏结构的正则化问题。
*   **数据集**：
    *   **合成数据**：使用 AR(1) 模型生成具有高度相关性（$\rho$ 高达 0.995）的特征矩阵，模拟病态条件。
    *   **真实数据**：使用 `real-sim` 数据集（经过特征增强处理以增加相关性）。
*   **Benchmark（对比方法）**：
    *   **FISTA-L / MFISTA-L**：经典的加速近端梯度法。
    *   **Prox-BB**：基于 Barzilai-Borwein 步长的谱近端梯度法。
    *   **0SR1**：无记忆对称秩一（SR1）变度量方法。

### 4. 资源与算力
*   **硬件环境**：8 核 Arm Neoverse-N1 (AWS Graviton2) 实例，32 GB RAM。
*   **软件环境**：Python 3.12.3，使用 4 个 BLAS 线程。
*   **算力说明**：论文未提供总训练时长，但详细记录了每个子问题的评估次数和达到目标精度所需的墙钟时间（Wall-clock time）。实验结果显示，DBS 在处理高维问题（$d=5000$）时，单次缩放近端计算仅需约 0.2 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对内部算子（Oracle）的独立性能测试（对比了 FISTA、APG-SC、Prox-BB 作为子问题求解器的表现）。
    *   针对外层优化算法的端到端性能基准测试。
    *   包含不同相关系数（$\rho$）、不同目标精度（$10^{-4}$ 到 $5\times 10^{-7}$）的多组实验。
*   **充分性与公平性**：实验涵盖了从合成数据到真实数据的过渡，并进行了消融实验（如对比有无热启动的 DBS）。所有方法在相同的硬件和步长搜索网格下运行，使用了中位数统计以排除随机性干扰，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **高效性**：DBS 算子能处理条件数高达 $4\times 10^6$ 的子问题，仅需几百次普通近端评估。
*   **收敛速度**：在高度相关的病态问题上，DBS 达到高精度目标所需的梯度评估次数远少于 FISTA。
*   **时间优势**：尽管 DBS 的单次迭代成本高于一阶方法，但在真实数据集 `real-sim` 上，其减少的迭代次数足以转化为显著的总运行时间优势。
*   **理论验证**：实验证实了强凸条件下的线性收敛理论，且热启动机制能将内部开销降低 3-5 倍。

### 7. 优点
*   **通用性强**：不需要正则化项 $h(x)$ 的二阶导数或活动集信息，只需普通近端算子即可。
*   **理论完备**：提供了最坏情况下的复杂度分析和强凸下的线性收敛证明。
*   **鲁棒性高**：通过二维单调搜索和认证回退机制，解决了拟牛顿度量在近端法中难以精确求解的顽疾。

### 8. 不足与局限
*   **秩的限制**：目前仅限于秩一（Rank-one）修正。虽然可以通过多次秩一修正模拟高秩，但本文未深入探讨更高秩因子的分解效率。
*   **计算开销**：对于那些普通近端映射极其廉价（如 $\ell_1$）且问题条件良好的情况，DBS 的内部搜索开销可能超过其带来的收敛加速收益。
*   **应用范围**：主要针对凸优化。虽然拟牛顿法常用于非凸问题，但本文的单调性分析和收敛保证均基于凸性假设。

（完）
