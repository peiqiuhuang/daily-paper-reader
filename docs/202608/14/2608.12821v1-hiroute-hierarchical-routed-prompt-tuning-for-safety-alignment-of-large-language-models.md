---
title: "HiRoute: Hierarchical Routed Prompt Tuning for Safety Alignment of Large Language Models"
title_zh: HiRoute：面向大语言模型安全对齐的分层路由提示微调
authors: "Fangzhou Chen, Shiji Zhao, Mengyang Wang, Qihui Zhu, Ranjie Duan, Maoxun Yuan, Xingxing Wei"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.12821v1"
tags: ["query:bopl"]
score: 9.0
evidence: 输入自适应的分层提示词微调框架
tldr: 针对大语言模型在安全对齐中难以平衡安全性与有用性、易过度拒绝的问题，本文提出 HiRoute 框架。该方法采用分层路由机制，将通用安全控制与特定类别响应引导分离。通过轻量级路由预测风险并动态组合提示专家，HiRoute 在提升多类别安全性的同时，显著降低了对良性输入的过度拒绝，并保持了模型的通用能力。
selection_source: fresh_fetch
motivation: 现有的静态提示微调方法难以在维持跨类别安全边界的同时，针对特定风险生成建设性响应并避免过度拒绝。
method: 提出一种输入自适应的分层路由框架，利用路由预测风险得分，并动态组合共享的粗粒度提示与风险特定的细粒度提示专家。
result: 实验表明 HiRoute 在多个安全基准上实现了极高的安全率，同时显著降低了过度拒绝率并保持了通用任务的竞争力。
conclusion: HiRoute 证明了通过分层路由和动态提示组合，可以有效地在不损害模型通用性能的前提下实现精细化的安全对齐。
---

## 摘要
大语言模型（LLMs）在面对有害请求和越狱攻击时仍然十分脆弱。基于提示微调（prompt tuning）的参数高效安全对齐方法通常依赖于单一的全局提示或外部选择的提示模块。这种静态设计难以在维持跨类别安全边界的同时，针对特定风险生成建设性的响应，并避免对良性输入的过度拒绝。为了解决这些局限性，我们提出了 HiRoute，这是一种输入自适应的分层提示微调框架，它将类别无关的安全控制与类别特定的响应引导相分离。HiRoute 首先在从冻结的 LLM 中提取的表示上训练一个轻量级分层路由器，以共同检测有害意图并预测多标签风险评分。随后，它冻结主干模型和路由器，并利用带有交替梯度更新的偏好优化来学习一个共享的粗粒度提示和一组作为连续嵌入的细粒度提示专家。在推理阶段，良性输入绕过安全分支，而风险输入则使用共享提示以及由路由器加权的风险特定提示专家混合进行处理。在三个指令微调模型上的实验表明，HiRoute 在多个安全基准测试中实现了极高的安全率，同时保持了安全响应的帮助性，减少了过度拒绝，并在通用任务上保持了具有竞争力的性能。

## Abstract
Large language models (LLMs) remain vulnerable to harmful requests and jailbreak attacks. Parameter-efficient safety alignment methods based on prompt tuning typically rely on a single global prompt or externally selected prompt modules. Such static designs struggle to maintain a cross-category safety boundary while generating constructive responses tailored to specific risks and avoiding over-refusal of benign inputs. To address these limitations, we propose HiRoute, an input-adaptive hierarchical prompt-tuning framework that separates category-agnostic safety control from category-specific response guidance. HiRoute first trains a lightweight hierarchical router on representations extracted from a frozen LLM to jointly detect harmful intent and predict multi-label risk scores. It then freezes both the backbone model and the router and uses preference optimization with alternating gradient updates to learn a shared coarse-grained prompt and a set of fine-grained prompt experts as continuous embeddings. At inference time, benign inputs bypass the safety branch, whereas risky inputs are processed using the shared prompt together with a router-weighted mixture of risk-specific prompt experts. Experiments across three instruction-tuned models show that HiRoute achieves high safety rates across multiple safety benchmarks while preserving safe-response helpfulness, reducing over-refusal, and maintaining competitive performance on general-purpose tasks.

---

## 论文详细总结（自动生成）

### 论文总结：HiRoute —— 面向大语言模型安全对齐的分层路由提示微调

#### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）通过 SFT 和 RLHF 进行了安全对齐，但仍易受越狱攻击和有害请求的影响。现有的参数高效微调（PEFT）方法（如提示微调）通常采用静态的全局提示，这导致了两个极端：要么安全约束不足，要么过度拒绝（Over-refusal）良性请求，且无法针对不同风险提供建设性的引导。
*   **核心问题**：如何在不改变模型参数的前提下，实现一种既能维持稳定安全边界，又能根据输入风险类型动态调整响应策略的自适应安全对齐机制？

#### 2. 方法论
HiRoute 提出了一种**输入自适应的分层提示微调框架**，其核心流程分为两个阶段：
*   **分层路由（Hierarchical Routing）**：
    *   在冻结的 LLM 表示层之上训练一个轻量级 Transformer 路由器。
    *   **粗粒度头**：判断输入是否属于风险类别（二分类）。
    *   **细粒度头**：预测具体风险类别（多标签分类，如网络犯罪、隐私侵犯等）。
*   **分层提示组合（Hierarchical Prompt Composition）**：
    *   **共享粗粒度提示 ($P_c$)**：为所有风险输入提供统一的安全底线约束。
    *   **细粒度提示专家 ($P_k$)**：针对特定风险类别学习独立的连续嵌入，通过路由器输出的权重进行加权混合 ($P_f$)。
*   **训练与推理**：
    *   **训练**：采用解耦的两阶段法。先训练路由器；再冻结主干和路由器，利用 DPO（直接偏好优化）并配合**交替梯度掩码更新策略**来优化提示词，减少粗/细粒度提示间的干扰。
    *   **推理**：引入安全门控机制。良性输入直接由原模型处理；风险输入则挂载“共享提示 + 路由加权专家提示”的组合进行生成。

#### 3. 实验设计
*   **数据集**：
    *   路由训练：WildGuardMix（粗粒度）、PKU-SafeRLHF（细粒度）。
    *   提示优化：1200 条粗粒度偏好数据和 1000 条细粒度类别偏好数据。
*   **测试基准（Benchmark）**：
    *   **安全性**：StrongReject, AdvBench, JailbreakBench。
    *   **通用能力**：GSM8K（数学）、MT-Bench（对话质量）、TruthfulQA（事实性）。
    *   **过度拒绝**：XSTest。
*   **对比方法**：Base（原始模型）、RPO（鲁棒提示优化）、DRO（防御响应优化）、ACD（对比解码）。
*   **实验模型**：Mistral-7B-Instruct-v0.3, Vicuna-7B-v1.5, Zephyr-7B-Beta。

#### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   路由器：训练 8 个 epoch（前 4 个仅粗粒度，后 4 个联合训练）。
    *   提示词：训练 6 个 epoch。
    *   具体总时长未明确标注，但基于 4090 和 7B 模型规模，预计在数小时至十数小时量级。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流 7B 模型上进行了全面测试，并扩展验证了 Vicuna-13B 的可扩展性。
*   **消融实验**：非常充分。涵盖了提示词长度分配比例（Coarse vs Fine）、推理门控阈值 $\tau_s$、训练策略（交替更新 vs 顺序更新）以及路由有效性的对比。
*   **客观性**：引入了 GPT-5 作为裁判进行“安全响应帮助性”评分，并进行了人工一致性校验，评估维度涵盖了安全率、帮助性、通用能力和过度拒绝，较为客观全面。

#### 6. 主要结论与发现
*   **安全与帮助性的平衡**：HiRoute 在维持极高安全率（平均 93.2% - 97.7%）的同时，其安全响应的帮助性得分显著高于基准方法。
*   **有效降低过度拒绝**：通过安全门控，HiRoute 在 XSTest 上的过度拒绝率极低（如 Mistral 上仅 2.5%），远优于 RPO (40.5%)。
*   **通用能力保持**：在 GSM8K 和 MT-Bench 等任务上，HiRoute 几乎没有性能损失，证明了提示微调对主干模型能力的保护作用。

#### 7. 优点
*   **结构设计巧妙**：分层结构成功解决了“通用安全边界”与“特定风险引导”之间的矛盾。
*   **参数高效且非侵入式**：无需修改 LLM 权重，易于部署和切换。
*   **建设性对齐**：模型不再只是简单说“不”，而是能提供合规的替代方案和风险解释。

#### 8. 不足与局限
*   **路由器依赖性**：系统的安全性高度依赖路由器的分类准确率，对于分布外（OOD）或极其隐蔽的复合风险，路由失效可能导致防御降级。
*   **场景覆盖**：实验主要集中在单轮文本对话，对于多轮对话中的上下文攻击或多模态输入的安全性尚未验证。
*   **白盒攻击风险**：虽然对迁移攻击（GCG）表现良好，但未讨论针对路由器和提示词组合机制的专门自适应攻击。

（完）
