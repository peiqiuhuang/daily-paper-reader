---
title: "FlowBot: Inducing LLM Workflows with Bilevel Optimization and Textual Gradients"
title_zh: FlowBot：利用双层优化和文本梯度诱导大语言模型工作流
authors: "Hongyeon Yu, Young-Bum Kim, Yoon Kim"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26258v1"
tags: ["query:bopl"]
score: 10.0
evidence: 将工作流归纳建模为双层优化问题
tldr: 针对LLM工作流依赖人工设计的瓶颈，本文提出FlowBot框架。该方法采用双层优化策略：外层优化工作流结构，内层通过逐层反向传播“文本梯度”优化具体调用。FlowBot实现了数据驱动的工作流自动诱导，在性能上可媲美人工设计的强基准，为自动化构建复杂LLM系统提供了高效的新途径。
selection_source: fresh_fetch
motivation: 现有的LLM工作流构建高度依赖人工设计和提示词工程，限制了其在大规模实际应用中的部署效率。
method: 提出FlowBot框架，利用双层优化结构和逐层反向传播的“文本梯度”来自动诱导和优化工作流。
result: 实验证明FlowBot自动生成的工作流在性能上达到了与人工精心设计或自动生成的强基准相当的水平。
conclusion: 该研究展示了通过数据驱动方法自动化诱导复杂LLM工作流的潜力，有效降低了系统构建的门槛。
---

## 摘要
LLM 工作流通过协调对单个 LLM（每个 LLM 都配备了不同的指令和工具）的结构化调用来实现特定目标，为扩展 LLM 的能力和构建能够处理多样化任务的强大系统提供了一条充满前景的路径。然而，现有的构建此类工作流的方法通常依赖于人工设计的流水线和提示词，这在实际部署中构成了巨大的瓶颈。如何以数据驱动的方式自动诱导和优化此类工作流？本文描述了一种用于自动诱导 LLM 工作流的简单数据驱动方法。我们将工作流诱导公式化为一个双层优化问题：外层循环优化工作流的高层草图（特别是 LLM 调用应如何结构化），内层循环则逐一优化每个单独的 LLM 调用。两个循环都使用“文本梯度”进行优化，其中对于内层循环，我们通过逐层“反向传播”文本梯度，以模块化的方式优化每个组件。我们发现，通过我们的 FlowBot（通过双层优化和文本梯度进行工作流诱导）方法发现的 LLM 工作流，其性能与使用人工设计或自动生成工作流的强基准模型相比具有竞争力。

## Abstract
LLM workflows, which coordinate structured calls to individual LLMs (each augmented with varying instructions and tools) to achieve a particular goal, offer a promising path towards extending the capabilities of LLMs and building powerful systems that can tackle diverse tasks. However, existing approaches for building such workflows generally rely on human-crafted pipelines and prompts, which presents a substantial bottleneck in real world deployment. How can automatically induce and optimize such workflows in a data-driven way? This paper describes a simple data-driven approach for automatically inducing LLM workflows. We formulate workflow induction as a bilevel optimization problem: an outer loop which optimizes a high-level sketch of the workflow (in particular how the LLM calls should be structured), and an inner loop which optimizes each individual LLM call one-by one. Both loops are optimized with ``textual gradients'' where for the inner loop we optimize each component in a modular way through ``backpropagating'' textual gradients layer-by-layer. We find that LLM workflows discovered through our \textsc{FlowBot} (work\textbf{flow} induction through \textbf{b}ilevel \textbf{o}ptimization and \textbf{t}extual gradients) approach performs competitively against strong baselines that make use of human-crafted or automatically-generated workflows.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **FlowBot**，一个旨在自动诱导和优化大语言模型（LLM）工作流的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的 LLM 应用已从单次调用转向复杂的工作流（即“复合 AI 系统”），通过多步调用、工具使用和推理步骤解决复杂任务。然而，这些工作流的结构（Sketch）和每个步骤的提示词（Prompts）目前高度依赖人工设计，难以跨领域扩展。
*   **核心问题**：如何以数据驱动的方式，同时自动诱导工作流的**结构**和优化其**组件行为**？
*   **整体含义**：FlowBot 将工作流构建视为一个优化问题，借鉴了神经网络中“架构搜索”和“梯度下降”的思想，实现了 LLM 系统设计的全自动化。

### 2. 方法论
FlowBot 将工作流诱导公式化为一个**双层优化（Bilevel Optimization）**问题：
*   **外层循环（结构优化）**：优化工作流的“草图”（W），即决定需要哪些步骤、步骤的顺序以及是否使用工具。
*   **内层循环（提示词优化）**：在固定结构下，优化每个步骤的具体提示词（$\theta_i$）。
*   **关键技术：文本梯度（Textual Gradients）与反向传播**：
    *   **前向传播**：输入数据经过一系列 LLM 调用层，产生最终输出。
    *   **文本反向传播**：利用 LLM 作为“梯度计算器”，根据最终损失（Loss）生成自然语言反馈（即文本梯度 $g_K$）。该反馈逐层向前传递，每一层都会根据下游的反馈和本层的输入输出，生成针对本层改进的文本建议。
    *   **更新机制**：收集一个 Batch 的文本梯度后，利用 LLM 总结这些反馈并更新提示词（内层）或修改工作流结构（外层，如增加实体消歧步骤）。

### 3. 实验设计
*   **数据集**：涵盖了 10 个主流基准测试，包括多跳问答（HotpotQA, HoVer）、指令遵循（IFBench）、隐私委派（PUPA）、数学推理（GSM8K, MATH）、代码生成（HumanEval, MBPP）和阅读理解（DROP）。
*   **对比基准（Benchmarks）**：
    *   **提示词优化方法**：Trace, MIPROv2, TextGrad, GEPA。
    *   **自动工作流生成方法**：ADAS, AFlow。
    *   **基础提示词工程**：CoT, MedPrompt, Self-Refine 等。
*   **实验设置**：主要使用 **GPT-4o mini** 作为执行器（Executor）和优化器（Optimizer）。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长，因为该研究主要基于 OpenAI 的 API 调用。
*   **成本与效率**：论文详细记录了 API 调用次数和费用。在 6 个任务中，FlowBot 的总成本约为 **$47.67**，而对比方法 AFlow 的成本约为 $130.95。FlowBot 在达到最佳性能时所需的 API 调用次数显著少于 AFlow（例如在 HotpotQA 上，FlowBot 约 1.88 万次，AFlow 约 18.7 万次）。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同性质的数据集上进行了测试，实验覆盖面广。
*   **消融实验**：非常充分。作者分别验证了“仅优化提示词”、“无双层优化（单层）”以及“无逐层反向传播”的情况，证明了双层结构和逐层反馈的必要性。
*   **模型鲁棒性**：测试了不同规模的开源模型（如 Qwen3.5 系列，从 4B 到 397B），验证了方法在不同模型能力下的有效性。
*   **客观性**：通过重新实现 AFlow 并使用相同的模型（GPT-4o mini）进行对比，确保了实验的公平性。

### 6. 主要结论与发现
*   **性能领先**：FlowBot 在多个任务上优于或持平于人工设计的强基准（如 GEPA）和自动生成基准（如 AFlow）。
*   **结构合理性**：诱导出的工作流结构（如先消歧、再检索、后验证）与人类专家的设计逻辑高度一致。
*   **模块化优势**：逐层文本反向传播能精准定位错误（如某一步骤的幻觉），并针对性地修复提示词。
*   **通用性**：FlowBot 甚至可以诱导出一个“统一工作流”，通过路由机制同时处理数学、代码和问答等多种任务。

### 7. 优点
*   **全自动化**：无需人工干预即可从零开始构建复杂的工作流。
*   **高效率**：相比于基于蒙特卡洛树搜索（MCTS）的方法（如 AFlow），FlowBot 的双层优化路径更直接，API 成本更低。
*   **可解释性**：优化的过程基于自然语言反馈，人类可以直观地看到系统是如何根据错误进行“反思”和改进的。

### 8. 不足与局限
*   **优化开销**：虽然比同类方法便宜，但相比于直接推理，优化过程仍需大量的 API 调用和额外成本。
*   **收敛瓶颈**：在某些任务上，性能会迅速达到平台期，增加训练数据不再带来显著提升，这反映了当前提示词优化机制的固有上限。
*   **延迟问题**：诱导出的复杂工作流虽然准确率高，但步骤较多，会导致推理延迟增加。

（完）
