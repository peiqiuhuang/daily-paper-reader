---
title: "FeedbackLLM: Metadata driven Multi-Agentic Language Agnostic Test Case Generator with Evolving prompt and Coverage Feedback"
title_zh: FeedbackLLM：一种基于元数据驱动、具有演化提示词和覆盖率反馈的多智能体语言无关测试用例生成器
authors: "Kushal Jasti, Tejamani Prashanth Sahu, Rishitha Pentyala, Muvvala Mohit, Vivek Yelleti"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01264v1"
tags: ["query:bopl"]
score: 8.0
evidence: 具有演化提示和覆盖率反馈的自动测试用例生成器
tldr: FeedbackLLM是一个自动化的、跨语言的测试用例生成框架，旨在解决传统方法效率低及现有LLM方法冗余度高的问题。它采用两阶段多智能体架构：首先解析源码提取约束并生成初始用例，随后通过行反馈和分支反馈智能体提取未覆盖元数据，驱动提示词演化并进行迭代优化。该框架引入冗余预防缓存以降低开销，在C和Python基准测试中显著提升了代码覆盖率。
selection_source: fresh_fetch
motivation: 传统测试生成方法耗时且难以扩展，而现有LLM方法易产生幻觉和冗余用例，导致分支覆盖率不足。
method: 提出一种两阶段多智能体框架，利用行反馈和分支反馈智能体提取未覆盖元数据，通过k步迭代演化提示词并结合缓存机制减少重复。
result: 在C和Python标准基准程序上的评估显示，FeedbackLLM在行覆盖率和分支覆盖率上均优于基准工具，且执行时间呈线性扩展。
conclusion: FeedbackLLM通过紧耦合的反馈循环和多智能体协作，有效提升了自动化测试生成的覆盖质量和计算效率。
---

## 摘要
传统的测试用例生成方法通常需要大量人工参与并产生显著的计算开销。此外，这些方法缺乏可扩展性，因此不适用于复杂的软件系统。最近，大语言模型（LLMs）已被应用于软件测试。然而，基于单次提示词工程的方法往往会产生幻觉并生成冗余的测试用例，导致分支覆盖率较低。为了解决上述局限性，本文提出了 FeedbackLLM，这是一种基于紧耦合两阶段方法的新型自动化语言无关测试用例生成框架。在第一阶段，FeedbackLLM 通过解析源代码提取输入约束并生成可能的测试用例。测试用例的质量在第二阶段由以下两个专门的 LLM 反馈智能体进行评估：(i) 行反馈智能体（Line Feedback Agent）：提取与未执行行相关的元数据；(ii) 分支反馈智能体（Branch Feedback Agent）：提取未执行分支条件的元数据。上述智能体在两阶段过程中运行，协同通信，且该过程重复执行 k 步。此外，我们还引入了冗余预防缓存，以避免重复的 API 请求并减少不必要的执行周期。该架构的性能在 C 和 Python 程序的标准基准测试上进行了评估。实验结果表明，FeedbackLLM 比基准工具具有更高的行覆盖率和分支覆盖率，同时其执行时间呈线性扩展。

## Abstract
Traditional approaches to test case generation often involve manual effort and incur significant computational overhead. Additionally, these approaches are not scalable, and hence, unsuitable for complex software systems. Recently, Large Language Models (LLMs) have been applied to software testing. However, single-shot prompt engineering-based approaches tend to hallucinate and generate redundant test cases, resulting in fewer branches. To handle the above-mentioned limitations, in this paper, we propose FeedbackLLM, a novel automated language-agnostic test case generation framework based on tightly coupled two-stage approach. In the first stage, FeedbackLLM extracts the input constraints by parsing source code and generates the possible test cases. The quality of the test cases is evaluated in the second stage by the following two specialized LLM feedback agents: (i) Line Feedback Agent: extracts the metadata related to missed line executions and (ii) Branch Feedback Agent: extracts the metadata of the unexecuted branch conditions. The above agents operate in a two-stage process, communicating in tandem, and this procedure is repeated for k-steps. Further, we also introduced a redundancy prevention cache to avoid duplicate API requests and avoid unnecessary execution cycles. The performance of the proposed architecture is evaluated on the standard benchmark programs related to C and Python programs. FeedbackLLM demonstrated more line and branch coverage than baseline tools while scaling linearly in execution time.

---

## 论文详细总结（自动生成）

以下是对论文《FeedbackLLM: Metadata driven Multi-Agentic Language Agnostic Test Case Generator with Evolving prompt and Coverage Feedback》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：软件测试占开发预算的10-20%，但传统方法（如基于随机变异的 Fuzzing 或基于符号执行的 Bounded Model Checking）面临复杂约束求解难、状态空间爆炸和计算开销大等问题。
*   **核心挑战**：虽然大语言模型（LLM）已被用于测试生成，但现有的单次提示词（Single-shot）方法容易产生“幻觉”，生成冗余用例，且难以覆盖深层逻辑分支。
*   **论文目标**：提出一种名为 **FeedbackLLM** 的自动化框架，通过多智能体协作和迭代反馈机制，实现跨语言（Language Agnostic）的高覆盖率测试用例生成。

### 2. 方法论
FeedbackLLM 采用了一种紧耦合的两阶段多智能体架构，核心流程如下：
*   **第一阶段：测试生成（Generation）**
    *   **测试用例生成智能体**：解析源代码提取输入约束，根据基础提示词（Base Prompt）生成 JSON 格式的初始测试输入。
*   **第二阶段：多智能体反馈循环（Multi-Agent Feedback Loop）**
    *   **行反馈智能体（Line Feedback Agent）**：分析 `coverage.py` (Python) 或 `gcov` (C) 提供的未执行行号，推导执行这些行所需的逻辑条件，并生成提示词修正建议。
    *   **分支反馈智能体（Branch Feedback Agent）**：专门针对仅部分覆盖的条件语句（如只走过 True 分支），推导翻转分支结果所需的输入约束。
*   **关键技术细节**：
    *   **演化提示词（Evolving Prompt）**：将两个反馈智能体的建议合并，在下一次迭代（共 $k$ 步）中追加到原始提示词中，引导模型探索未覆盖区域。
    *   **冗余预防缓存（RPC）**：利用 $O(1)$ 查询效率的哈希表（Python Set）存储已生成的输入，过滤重复用例，从而节省 API 配额并减少无效的编译执行周期。
    *   **算法流程**：初始化缓存 $\rightarrow$ 合并提示词 $\rightarrow$ 调用 LLM $\rightarrow$ 过滤并执行 $\rightarrow$ 评估覆盖率 $\rightarrow$ 若未达标则触发反馈智能体进入下一轮迭代。

### 3. 实验设计
*   **数据集/场景**：
    *   **C 语言**：选取了 20 个来自 PALS 和 RERS 基准测试的标准程序，包含复杂的嵌套逻辑和边界条件。
    *   **Python 语言**：选取了 20 个包含嵌套条件、循环和数学约束的程序。
*   **基准测试（Benchmark）**：
    *   对比了 **kS-LLM**（一种现有的迭代式 LLM 测试生成方法）。
    *   对比了 **Zero-shot LLM**（单次生成基准）。
*   **评估指标**：行覆盖率（Line Coverage）、分支覆盖率（Branch Coverage）以及执行时间（Execution Time）。

### 4. 资源与算力
*   **硬件环境**：运行在 macOS 系统的机器上。
*   **软件环境**：Python 3.9，GCC 编译器，Gcov 和 Coverage.py 工具。
*   **模型使用**：使用了 **Google Gemini-2.5-Flash** 模型，通过官方 GenAI SDK 调用。
*   **训练时长**：文中未提及模型训练，因为该方法属于 Prompt Engineering 和 Agentic AI 范畴，主要开销在于 API 调用和本地代码编译执行。

### 5. 实验数量与充分性
*   **实验规模**：共针对 40 个程序进行了测试，并设置了不同的“边界值（Bounds）”（从 1 到 2000）来观察覆盖率随迭代次数和复杂度的变化趋势。
*   **充分性评价**：实验涵盖了静态语言（C）和动态语言（Python），证明了框架的语言无关性。通过与 kS-LLM 的对比，展示了多智能体反馈在处理复杂逻辑上的优势。实验设计较为客观，通过多次迭代展示了覆盖率的收敛过程。

### 6. 主要结论与发现
*   **覆盖率提升**：FeedbackLLM 在行覆盖和分支覆盖上均显著优于基准工具。在早期迭代中覆盖率增长最快，随后趋于稳定。
*   **多智能体优势**：将“行反馈”与“分支反馈”职责分离，有效降低了单个模型的认知负荷，减少了幻觉，提高了生成针对性输入的准确率。
*   **语言差异**：Python 程序的平均覆盖率普遍高于 C 程序，显示出 LLM 在处理动态语言逻辑时可能具有更强的探索能力。
*   **可扩展性**：得益于冗余预防缓存（RPC），执行时间随程序复杂度的增加呈线性增长，而非指数级增长。

### 7. 优点
*   **反馈粒度细**：通过专门的分支反馈智能体解决“顽固”的布尔标志翻转问题。
*   **效率优化**：引入缓存机制，极大地降低了 LLM API 的 Token 消耗和重复计算。
*   **跨语言支持**：架构设计不依赖特定语言特性，具有良好的通用性。
*   **提示词演化**：通过闭环反馈使提示词能够根据测试结果动态调整，具备一定的自适应能力。

### 8. 不足与局限
*   **非确定性风险**：尽管有温度控制，LLM 的输出仍存在随机性，可能导致测试结果在不同运行间波动。
*   **输入类型限制**：目前的静态分析模块基于正则表达式和 AST 解析，对于极其复杂的结构化输入（如深度嵌套的 JSON、SQL 数据库或二进制图像数据）支持有限。
*   **缺乏测试断言（Oracle）**：目前主要关注路径探索和崩溃检测（Crash Detection），尚不能自动生成用于验证业务逻辑正确性的断言语句。
*   **静态分析深度**：对于用户自定义的复杂数据结构，目前的解析能力尚待加强。

（完）
