---
title: Projection-Free Evolution Strategies for Continuous Prompt Search
title_zh: 面向连续提示搜索的无投影演化策略
authors: "Yu Cai, Canxi Huang, Xiaoyu He"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13786v1"
tags: ["query:bopl"]
score: 9.0
evidence: 连续提示词搜索与优化
tldr: 针对连续提示词搜索中高维黑盒优化的挑战，本文发现传统的随机投影方法无法有效捕捉提示词空间的低维结构。为此，提出一种无需投影的进化策略方法，通过在全空间直接优化并结合内在维度自适应机制，在不增加计算开销的情况下提升搜索效率。此外，引入置信度正则化增强少样本泛化能力，在GLUE基准测试中显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的随机投影方法难以有效捕捉连续提示词空间中本质的低维结构，限制了黑盒优化的性能。
method: 提出一种基于进化策略的无投影搜索方法，利用自适应机制在全空间优化，并结合置信度正则化提升泛化性。
result: 在GLUE基准的七个自然语言理解任务上，该方法显著优于现有的黑盒提示词搜索基准。
conclusion: 无需投影的进化策略能更有效地利用提示词空间的内在结构，是解决高维连续提示词搜索问题的有效途径。
---

## 摘要
连续提示搜索为自然语言处理任务中传统的参数微调提供了一种计算高效的替代方案。然而，其在实际应用中的有效性往往受到黑盒性质以及目标函数景观固有高维性的显著阻碍。现有方法通常通过将搜索限制在随机投影的低维子空间中来缓解这些挑战。然而，投影机制的有效性及其潜在动机仍然模糊不清。在本文中，我们首先通过实证研究证明，尽管提示空间具有低维结构，但随机投影无法充分捕捉这一核心结构。受此发现启发，我们提出了一种基于演化策略的无投影提示搜索方法。通过在全提示空间中直接优化，并结合针对本征维度校准的自适应机制，我们的方法在不增加额外计算开销的情况下实现了具有竞争力的搜索能力。此外，为了弥合少样本场景下的泛化差距，我们引入了一种基于置信度的正则化机制，系统地增强了模型对目标词映射器（verbalizers）的置信度。在 GLUE 基准测试的七个自然语言理解任务上的实验结果表明，我们提出的方法显著优于现有的基准模型。

## Abstract
Continuous prompt search offers a computationally efficient alternative to conventional parameter tuning in natural language processing tasks. Nevertheless, its practical effectiveness can be significantly hindered by the black-box nature and the inherent high-dimensionality of the objective landscapes. Existing methods typically mitigate these challenges by restricting the search to a randomly projected low-dimensional subspace. However, the effectiveness and underlying motivation of the projection mechanism remain ambiguous. In this paper, we first empirically demonstrate that despite the prompt space possessing a low-dimensional structure, random projections fail to adequately capture this essential structure. Motivated by this finding, we propose a projection-free prompt search method based on evolutionary strategies. By directly optimizing in the full prompt space with an adaptation mechanism calibrated to the intrinsic dimension, our method achieves competitive search capabilities without additional computational overhead. Furthermore, to bridge the generalization gap in few-shot scenarios, we introduce a confidence-based regularization mechanism that systematically enhances the model's confidence in the target verbalizers. Experimental results on seven natural language understanding tasks from the GLUE benchmark demonstrate that our proposed approach significantly outperforms existing baselines.

---

## 论文详细总结（自动生成）

这篇论文针对预训练语言模型（PLM）的连续提示词（Continuous Prompt）搜索问题，提出了一种无需子空间投影的演化策略方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖梯度（黑盒场景）且面对极高维度（如 RoBERTa-Large 的提示词空间可达 50,000 维以上）的情况下，高效搜索最优的连续提示词。
*   **研究动机**：
    *   现有主流方法（如 BBT）假设提示词空间具有低维特性，通过**随机投影**将搜索限制在低维子空间。
    *   作者通过实证发现：虽然提示词空间确实存在低本征维度（Intrinsic Dimension, ID），但**随机投影矩阵无法有效捕捉这一结构**，反而限制了搜索上限。
    *   在高维全空间直接搜索通常面临收敛极慢和严重的少样本过拟合问题。

### 2. 方法论
论文提出了 **ES-ID-CR** 框架，包含两个核心技术：
*   **本征维度感知自适应机制（ID-Aware Adaptation）**：
    *   **核心思想**：直接在全空间优化，但修改演化策略（如 (1+1)-ES 或 SaES）中的步长自适应参数。
    *   **技术细节**：标准演化策略的阻尼因子 $\tau$ 通常与环境维度 $d$ 相关（$\tau = \sqrt{2d}$）。作者将其校准为与本征维度 $\tilde{d}$ 相关（$\tau = \sqrt{2\tilde{d}}$）。
    *   **效果**：这使得算法在全空间搜索时，步长更新速度能匹配景观的实际几何特征，从而显著加速收敛。
*   **置信度正则化（Confidence-Based Regularization, CR）**：
    *   **核心思想**：解决全空间搜索在少样本下的过拟合。
    *   **公式**：$\ell_{CR} = \ell_{CE} - \beta \cdot \log \frac{\sum_{u \in W} \exp(a_u)}{\sum_{v \in V} \exp(a_v)}$。
    *   **逻辑**：除了最小化目标标签的交叉熵损失（$\ell_{CE}$），还惩罚模型对目标词集 $W$ 相对于整个词表 $V$ 的低置信度。这强制模型将提示词引导至预训练分布的高置信区域，提升泛化性。

### 3. 实验设计
*   **数据集**：GLUE 基准测试中的 7 个任务（MRPC, MNLI, QQP, SST-2, RTE, CoLA, QNLI）。
*   **模型（Backbone）**：RoBERTa-Large（掩码语言模型）和 OPT-6.7B（因果语言模型）。
*   **对比方法（Baselines）**：
    *   零样本：手工提示词（ManualPrompt）、上下文学习（ICL）。
    *   黑盒优化：BBT（基于投影的 CMA-ES）、ZOSGD（零阶随机梯度下降）。
    *   离散提示词优化：RLPrompt（强化学习）、BDPL（变分策略梯度）。
*   **实验设置**：统一采用 16-shot（少样本）设置，每个任务运行 5 个随机种子取平均。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA RTX 4090 GPU**。
*   **算力开销**：
    *   每个任务限制在 **5,000 次函数评测（FEs）** 以内。
    *   文中给出了 Time/FE（每次评测耗时），约为 0.15 秒左右。
    *   作者强调，该方法在全空间搜索的计算开销与基于投影的方法（BBT）相当，并未引入额外的显存或计算负担。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个不同性质的 NLP 任务，并在两种完全不同的模型架构（RoBERTa 和 OPT）上验证。
*   **消融与分析**：
    *   **本征维度测量**：通过 MLE 估计验证了提示词空间的 ID 约为 250 左右，且不随提示词长度线性增长。
    *   **投影局限性分析**：对比了全空间 CMA-ES 与子空间 BBT 的训练损失和优化轨迹。
    *   **超参数敏感性**：对本征维度 $\tilde{d}$ 和正则化系数 $\beta$ 进行了详细的网格搜索和敏感性分析。
*   **评价**：实验设计严谨，通过 5 个随机种子和多维度对比，证明了结果的客观性和公平性。

### 6. 主要结论与发现
*   **提示词空间确实是低维的**，其本征维度存在一个不随长度增加的经验上限（约 250）。
*   **随机投影是性能瓶颈**：在相同初始点下，全空间搜索总能找到比随机子空间更好的解。
*   **无投影搜索可行**：通过校准演化策略的自适应机制，可以在不降维的情况下实现快速收敛。
*   **置信度是泛化的关键**：全空间搜索容易产生“投机取巧”的解，CR 正则化能有效引导模型建立稳健的语义连接。

### 7. 优点
*   **理论洞察深**：不仅提出了方法，还深入探讨了为什么之前的投影方法不是最优的。
*   **通用性强**：CR 正则化被证明是一个“即插即用”模块，同样能提升 ZOSGD 等其他算法的性能。
*   **架构无关**：在掩码 LM 和超大规模因果 LM（OPT-6.7B）上均表现出色。

### 8. 不足与局限
*   **超参数依赖**：虽然 $\tilde{d}$ 相对稳健，但正则化系数 $\beta$ 在不同任务间仍需粗粒度的搜索（0.01 到 100 之间）。
*   **搜索预算限制**：实验主要集中在 5,000 次评测内，对于更复杂的推理任务，可能需要更大的搜索预算。
*   **提示词长度**：虽然验证了 ID 不随长度增长，但在超长提示词下的内存占用（虽然是线性增长）仍是黑盒优化的一大挑战。

（完）
