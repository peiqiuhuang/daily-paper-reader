---
title: "RASPRef: Retrieval-Augmented Self-Supervised Prompt Refinement for Large Reasoning Models"
title_zh: RASPRef：大语言推理模型的检索增强自监督提示词优化
authors: Rahul Soni
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27008v1"
tags: ["query:bopl"]
score: 9.0
evidence: 自监督提示词精炼框架
tldr: 针对推理型大模型对提示词高度敏感且人工设计成本高的问题，本文提出RASPRef框架。该方法通过检索相关示例和推理轨迹，结合多样本一致性、验证器反馈及模型自我批判等自监督信号，实现提示词的自动化迭代优化。实验证明，该方法在数学推理任务上显著优于静态提示词，为提升大模型推理能力提供了一种可扩展的自动化方案。
selection_source: fresh_fetch
motivation: 现有的推理大模型性能受提示词影响极大，且人工设计提示词的过程难以跨领域扩展。
method: 提出RASPRef框架，利用检索增强技术结合多样本一致性和模型自我批判等自监督信号来迭代优化提示词。
result: 在GSM8K等数学推理任务上的实验表明，检索引导的提示词优化相比静态提示词基准显著提升了模型性能。
conclusion: 提示词设计仍是推理模型的关键因素，自监督提示词优化为提升推理性能提供了一种实用且可扩展的策略。
---

## 摘要
最近的推理导向型语言模型（如 DeepSeek R1 和 OpenAI o1）在 GSM8K、MATH 和多跳问答任务等结构化推理基准测试中表现出了强大的性能。然而，它们的性能对提示词的表述高度敏感，而设计有效的提示词通常是一个手动且迭代的过程，难以在不同任务或领域间扩展。为了解决这一局限性，我们引入了检索增强自监督提示词优化（RASPRef），这是一个无需人工标注或特定任务监督即可改进提示词的框架。该方法检索相关示例和先前生成的推理轨迹，并利用多样本一致性、验证器反馈和模型生成的批判等信号来迭代优化提示词。与以往主要关注改进模型输出的方法不同，RASPRef 直接将提示词视为优化目标，并通过迭代的检索引导优化过程对其进行改进。在 GSM8K 风格的数学推理任务上的实验表明，与静态提示词基准相比，检索引导的提示词提高了性能。我们进一步讨论了检索质量、轨迹选择和自监督反馈信号如何影响提示词优化的有效性。这些发现表明，提示词设计仍然是推理导向型语言模型的关键因素，而自改进提示词为提高推理性能提供了一种实用且可扩展的策略。

## Abstract
Recent reasoning-focused language models such as DeepSeek R1 and OpenAI o1 have demonstrated strong performance on structured reasoning benchmarks including GSM8K, MATH, and multi-hop question answering tasks. However, their performance remains highly sensitive to prompt formulation, and designing effective prompts is typically a manual and iterative process that does not scale well across tasks or domains. To address this limitation, we introduce Retrieval-Augmented Self-Supervised Prompt Refinement (RASPRef), a framework that improves prompts without requiring human annotations or task-specific supervision. The approach retrieves relevant examples and previously generated reasoning trajectories, and leverages signals such as multi-sample consistency, verifier feedback, and model-generated critiques to iteratively refine the prompt. Unlike prior approaches that focus primarily on improving model outputs, RASPRef directly treats the prompt as the optimization target and improves it through an iterative retrieval-guided refinement process. Experiments on GSM8K-style mathematical reasoning tasks show that retrieval-guided prompting improves performance compared with a static prompting baseline. We further discuss how retrieval quality, trajectory selection, and self-supervised feedback signals may influence the effectiveness of prompt refinement. These findings suggest that prompt design remains a critical factor for reasoning-oriented language models, and that self-improving prompts offer a practical and scalable strategy for improving reasoning performance.

---

## 论文详细总结（自动生成）

### RASPRef：大语言推理模型的检索增强自监督提示词优化论文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
尽管 DeepSeek R1 和 OpenAI o1 等推理型大模型（LRMs）在复杂任务上表现出色，但它们对**提示词（Prompt）的表述极其敏感**。微小的措辞变化或示例选择都会显著影响推理质量。目前，设计高效提示词主要依赖人工迭代，这在跨领域应用时难以扩展。论文提出的 **RASPRef** 旨在实现提示词的**自动化、自监督优化**，无需人工标注，通过结合历史推理轨迹的检索与模型自我反馈，使提示词能够随经验积累而进化。

#### 2. 论文提出的方法论
RASPRef 的核心思想是将提示词视为一个可优化的离散目标，通过“检索-评估-修改”的闭环进行迭代。其关键流程包括：
*   **轨迹检索（Retrieval）**：针对新问题，从存储库中检索任务结构或领域相似的历史推理轨迹（包含输入、提示词、推理链及反馈）。
*   **初始提示词构建**：利用检索到的高质量轨迹提取通用指令、提炼推理准则，并选取代表性示例。
*   **自监督评估（Quality Objective）**：定义了一个综合评分函数 $Q(p; x)$，包含四个维度：
    1.  **多样本一致性 ($C_{cons}$)**：多个推理结果的答案是否趋同。
    2.  **验证器评分 ($C_{ver}$)**：轻量级验证模型对推理结构和逻辑合理性的评分。
    3.  **自我批判 ($C_{crit}$)**：模型对自身推理过程的分析质量。
    4.  **检索对齐 ($C_{ret}$)**：推理过程是否有效利用了检索到的参考资产。
*   **迭代精炼**：模型扮演“编辑”角色，根据评估信号和批判意见修改提示词，直至评分稳定或达到计算预算。

#### 3. 实验设计
*   **数据集**：主要使用 **GSM8K**（小学数学应用题）的子集进行验证。
*   **Benchmark**：从 GSM8K 中抽取 500 个测试样本，并使用 200 个训练样本构建检索索引。
*   **对比方法**：
    *   **静态提示词（Static Prompting）**：固定的思维链（CoT）模板，不随输入变化。
    *   **检索增强提示词（Retrieval-Augmented Prompting）**：根据输入动态检索相似示例加入提示词（即 RASPRef 的核心组件之一）。

#### 4. 资源与算力
*   **模型使用**：通过 API 调用 **gpt-4o-mini** 作为推理引擎和编辑模型。
*   **硬件环境**：检索、提示词构建和评估逻辑运行在**本地 CPU 环境**（Python）下。
*   **算力说明**：由于模型是冻结的且通过 API 访问，论文**未涉及 GPU 训练算力**。检索部分使用了 `text-embedding-3-small` 模型生成向量索引。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 500 个 GSM8K 样本上进行了测试。
*   **充分性评价**：
    *   **初步验证有效**：实验展示了从 85.6% 到 95.0% 的显著提升，证明了检索引导的有效性。
    *   **局限性**：实验数量相对较少，且仅限于数学推理单一领域。论文目前更多是一个**原型验证（Prototype）**，缺乏大规模消融实验（如不同 $R$ 轮迭代的影响、不同自监督信号的权重贡献等）以及在多跳问答、代码生成等其他推理任务上的表现。

#### 6. 论文的主要结论与发现
*   **检索显著增强推理**：通过引入相似问题的推理轨迹，模型能更好地组织中间步骤，准确率从 85.6% 提升至 95.0%。
*   **提示词是可进化的**：将提示词作为优化目标而非固定指令，可以有效利用模型产生的历史数据（Interaction Logs）。
*   **自监督信号可行性**：无需人工标签，仅靠一致性和验证器反馈即可引导提示词向更高质量的方向演进。

#### 7. 优点
*   **无需人工标注**：完全依赖自监督信号，具有极强的可扩展性和低成本特性。
*   **黑盒优化**：不需要访问模型权重或梯度，适用于所有闭源 API 模型。
*   **可解释性强**：优化的结果是自然语言提示词，人类可以直观理解模型学到了哪些推理启发式规则。
*   **动态适配**：相比静态提示词，能更好地处理长尾（罕见）任务。

#### 8. 不足与局限
*   **计算开销**：每一轮迭代都需要多次采样和验证，增加了推理阶段的延迟和 API 成本。
*   **过拟合风险**：过度针对特定邻域的样本进行提示词精炼，可能导致提示词变得过于具体，从而丧失在其他任务上的泛化能力。
*   **轨迹质量依赖**：如果检索库中包含错误或逻辑混乱的推理链，可能会误导提示词的优化方向。
*   **实验覆盖面窄**：目前仅在数学任务上验证，尚未在更复杂的长链推理或非结构化任务中证明其普适性。

（完）
