---
title: "SCHK-HTC: Sibling Contrastive Learning with Hierarchical Knowledge-Aware Prompt Tuning for Hierarchical Text Classification"
title_zh: SCHK-HTC：融合层级知识感知提示微调与兄弟节点对比学习的层级文本分类
authors: "Ke Xiong, Qian Wu, Wangjie Gan, Yuke Li, Xuhong Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15998v1"
tags: ["query:bopl"]
score: 8.0
evidence: 层次化知识感知提示微调
tldr: 针对少样本层次文本分类中兄弟节点类别难以区分的问题，本文提出了SCHK-HTC模型。该方法结合了层次知识感知提示微调与兄弟对比学习机制，通过提取层次化领域知识并引导模型学习更具辨别力的特征，有效增强了对深层相似类别的区分能力。实验证明，该方法在多个基准数据集上均达到了领先水平。
selection_source: fresh_fetch
motivation: 现有方法在少样本场景下难以区分语义相近的兄弟节点类别，且缺乏对深层级领域知识的有效利用。
method: 提出了一种结合层次知识提取模块与兄弟对比学习机制的框架，利用提示微调引导模型编码各层级的判别性特征。
result: 在三个基准数据集上的实验结果显示，该方法在多数指标上均超越了现有的最先进模型。
conclusion: 通过整合层次知识与对比学习，SCHK-HTC显著提升了模型对易混淆兄弟类别的辨别力，有效解决了少样本层次分类难题。
---

## 摘要
少样本层级文本分类（few-shot HTC）是一项具有挑战性的任务，旨在数据稀缺的条件下将文本映射到预定义的树状结构标签层级中。虽然现有方法利用标签层级的结构约束来维持父子预测的一致性，但由于领域知识不足，它们在区分语义相似的兄弟类别时面临关键瓶颈。我们提出了一种名为 SCHK-HTC 的创新方法，即融合层级知识感知提示微调与兄弟节点对比学习的少样本层级文本分类。我们的工作增强了模型对深层兄弟类别间细微差异的感知能力，而不仅仅是强制执行层级规则。具体而言，我们提出了一个包含两个核心组件的新颖框架：层级知识提取模块和兄弟节点对比学习机制。该设计引导模型在每个层级编码判别性特征，从而提高了易混淆类别的可分离性。我们的方法在三个基准数据集上均取得了优异性能，在大多数情况下超越了现有的最先进方法。我们的代码已开源至 https://github.com/happywinder/SCHK-HTC。

## Abstract
Few-shot Hierarchical Text Classification (few-shot HTC) is a challenging task that involves mapping texts to a predefined tree-structured label hierarchy under data-scarce conditions. While current approaches utilize structural constraints from the label hierarchy to maintain parent-child prediction consistency, they face a critical bottleneck, the difficulty in distinguishing semantically similar sibling classes due to insufficient domain knowledge. We introduce an innovative method named Sibling Contrastive Learning with Hierarchical Knowledge-aware Prompt Tuning for few-shot HTC tasks (SCHK-HTC). Our work enhances the model's perception of subtle differences between sibling classes at deeper levels, rather than just enforcing hierarchical rules. Specifically, we propose a novel framework featuring two core components: a hierarchical knowledge extraction module and a sibling contrastive learning mechanism. This design guides model to encode discriminative features at each hierarchy level, thus improving the separability of confusable classes. Our approach achieves superior performance across three benchmark datasets, surpassing existing state-of-the-art methods in most cases. Our code is available at https://github.com/happywinder/SCHK-HTC.

---

## 论文详细总结（自动生成）

这是一份关于论文《SCHK-HTC: Sibling Contrastive Learning with Hierarchical Knowledge-Aware Prompt Tuning for Hierarchical Text Classification》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在少样本层级文本分类（Few-shot HTC）任务中，模型难以区分层级结构中深层的、语义高度相似的“兄弟节点”（Sibling Classes）。
*   **研究背景**：
    *   现有的 HTC 方法多关注父子节点的结构约束，但在数据稀缺时，仅靠文本信息难以捕捉细微的类别差异。
    *   现有的引入外部知识的方法（如 K-HTC 或基于 LLM 的 DCL）存在知识融合不充分、参数量过大或对标注数据依赖过高等问题。
*   **动机**：通过引入外部知识图谱（KG）的结构化信息，并结合针对兄弟节点的对比学习机制，提升模型在深层级分类中的判别能力。

### 2. 方法论
SCHK-HTC 框架主要由两个核心编码器和三部分损失函数组成：
*   **层级知识感知编码器 (HK-Encoder)**：
    *   **知识提取**：通过实体链接从 Wikidata 提取输入文本相关的子图。
    *   **多模态融合**：利用 BERT 提取实体的语义特征，同时利用 Node2Vec 和邻居聚合（AGG）提取子图的结构特征，两者融合生成知识感知的表示。
*   **层级上下文编码器**：
    *   采用基于提示（Prompt）的编码策略，设计双 [MASK] 模板（例如：“第L层是 [MASK]pos 而不是 [MASK]neg”），强制模型学习正确标签与易混淆兄弟标签之间的差异。
*   **训练目标（损失函数）**：
    *   **知识感知层级 InfoNCE 损失**：在知识空间内，拉近同类样本的距离，推开异类样本。
    *   **兄弟对比学习 (SCL) 损失**：利用硬负采样（从预测概率最高的非正确兄弟标签中选取）和 LLM 生成的标签详细解释，强化对相似兄弟类别的区分。
    *   **分类损失与 MLM 损失**：结合线性 Verbalizer 的分类损失和 BERT 原有的掩码语言模型损失进行联合优化。

### 3. 实验设计
*   **数据集**：
    *   **WOS**：单路径层级数据集（2层）。
    *   **DBpedia**：单路径层级数据集（3层）。
    *   **RCV1-V2**：多路径层级数据集（4层）。
*   **实验设置**：采用 1, 2, 4, 8, 16-shot 的少样本设置。
*   **对比方法 (Baselines)**：
    *   基础模型：Vanilla-BERT。
    *   对比学习/层级模型：HGCLR, HPT, HierVerb（之前的 SOTA）。
    *   检索增强模型：DCL（最新的 SOTA）。
*   **评价指标**：Micro-F1 和 Macro-F1。

### 4. 资源与算力
*   **硬件环境**：使用了 2 个 Intel Xeon Gold 6430 CPU 和 **1 个 NVIDIA RTX A6000 GPU**。
*   **训练细节**：Batch size 为 8，学习率为 4e-5，采用 Adam 优化器，并使用了早停（Early Stopping）策略（耐心值为 10 epochs）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流数据集上进行了 5 种不同 shot 程度的测试，且每组实验均运行了 **5 个随机种子**并报告均值，保证了结果的统计可靠性。
*   **消融实验**：分别移除了 HK-Encoder、InfoNCE 损失和 SCL 损失，验证了各组件对性能的贡献，实验设计较为完整。
*   **深度分析**：专门针对最深层级的分类准确率进行了对比分析（见图1），直接回应了研究动机。
*   **客观性**：对比了包括最新 SOTA 在内的多种方法，并对多路径任务中表现稍逊的原因进行了客观分析。

### 6. 主要结论与发现
*   **性能领先**：SCHK-HTC 在大多数少样本场景下均优于现有 SOTA 方法，尤其在 WOS 和 DBpedia 数据集上提升显著。
*   **深层区分力**：实验证明该方法在处理层级结构中最底层的易混淆类别时，比 HierVerb 等方法具有更强的判别力。
*   **知识的重要性**：引入外部知识图谱并进行层级化的对比约束，能有效弥补少样本场景下领域知识的匮乏。

### 7. 优点
*   **针对性强**：精准捕捉了 HTC 任务中“兄弟节点混淆”的核心痛点。
*   **知识融合深入**：不仅引入了实体的语义，还通过图算法引入了知识图谱的结构信息。
*   **提示词设计巧妙**：双 [MASK] 模板（Positive vs. Negative）直观地引导模型进行判别式学习。

### 8. 不足与局限
*   **多路径任务限制**：在多路径数据集（如 RCV1-V2）上，随着样本量增加，其优势相比单路径任务有所收敛。作者认为这是因为对比学习在处理共存标签时可能产生优化冲突。
*   **外部依赖**：模型性能高度依赖于实体链接（Entity Linking）的质量以及外部知识图谱的覆盖范围。
*   **计算开销**：虽然比 LLM 方法轻量，但相比纯文本 BERT，增加了子图提取和知识编码的预处理步骤。

（完）
