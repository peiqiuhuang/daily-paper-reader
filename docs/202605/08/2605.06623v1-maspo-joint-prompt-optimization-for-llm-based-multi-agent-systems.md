---
title: "MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems"
title_zh: MASPO：基于大语言模型的多智能体系统联合提示词优化
authors: "Zhexuan Wang, Xuebo Liu, Li Wang, Zifei Shan, Yutong Wang, Zhenxi Song, Min Zhang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06623v1"
tags: ["query:bopl"]
score: 9.0
evidence: 自动且迭代地优化提示
tldr: 针对基于大语言模型的多智能体系统（MAS）中提示词优化困难的问题，本文提出MASPO框架。该框架通过联合评估机制，不仅关注单个智能体的局部表现，更强调其对下游智能体及全局目标的贡献。结合数据驱动的进化束搜索算法，MASPO能自动且迭代地优化整个系统的提示词，有效解决了局部与全局目标不一致的挑战，显著提升了复杂协作任务的性能。
selection_source: fresh_fetch
motivation: 现有的多智能体系统提示词优化往往忽略了局部智能体行为与全局系统目标之间的对齐问题。
method: 提出一种联合评估机制，结合进化束搜索算法，根据下游智能体的成功率和全局结果迭代优化所有智能体的提示词。
result: "在6个多样化任务上的实验表明，MASPO优于现有最先进方法，平均准确率提升了2.9%。"
conclusion: MASPO通过跨智能体的联合优化，为提升多智能体协作系统的整体效能提供了一种高效且无需真值标签的自动化方案。
---

## 摘要
基于大语言模型（LLM）的多智能体系统（MAS）在处理复杂协作任务方面展现出巨大潜力，其中智能体通常通过特定角色的提示词进行编排。虽然这些提示词的质量至关重要，但在相互协作的智能体之间进行联合优化仍是一项艰巨的挑战，这主要是由于局部智能体目标与整体系统目标之间的不一致。为解决这一问题，我们提出了 MASPO，这是一个旨在自动且迭代地优化整个系统提示词的新型框架。MASPO 的核心创新在于其联合评估机制，该机制不仅根据提示词的局部有效性进行评估，还评估其促进后续智能体下游任务成功的能力。这在不依赖地面真值（ground-truth）标签的情况下，有效地弥合了局部交互与全局结果之间的差距。此外，MASPO 采用数据驱动的进化束搜索（evolutionary beam search）以高效地在多维提示词空间中进行搜索。在 6 个不同任务上的广泛实证评估表明，MASPO 始终优于最先进的提示词优化方法，平均准确率提升了 2.9。我们在 https://github.com/wangzx1219/MASPO 发布了代码。

## Abstract
Large language model (LLM)-based Multi-agent systems (MAS) have shown promise in tackling complex collaborative tasks, where agents are typically orchestrated via role-specific prompts. While the quality of these prompts is pivotal, jointly optimizing them across interacting agents remains a non-trivial challenge, primarily due to the misalignment between local agent objectives and holistic system goals. To address this, we introduce MASPO, a novel framework designed to automatically and iteratively refine prompts across the entire system. A core innovation of MASPO is its joint evaluation mechanism, which assesses prompts not merely by their local validity, but by their capacity to facilitate downstream success for successor agents. This effectively bridges the gap between local interactions and global outcomes without relying on ground-truth labels. Furthermore, MASPO employs a data-driven evolutionary beam search to efficiently navigate the high-dimensional prompt space. Extensive empirical evaluations across 6 diverse tasks demonstrate that MASPO consistently outperforms state-of-the-art prompt optimization methods, achieving an average accuracy improvement of 2.9. We release our code at https://github.com/wangzx1219/MASPO.

---

## 论文详细总结（自动生成）

这是一份关于论文《MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：基于大语言模型（LLM）的多智能体系统（MAS）通过不同角色的协作处理复杂任务，而每个智能体的“提示词（Prompt）”设计至关重要。
*   **核心问题**：
    1.  **局部与全局失配（Local-Global Misalignment）**：中间智能体可能完美执行了局部指令，但其输出却误导了下游智能体，导致系统整体失败。
    2.  **信用分配难题（Credit Assignment）**：在多步协作中，很难确定最终的失败是由哪个环节的提示词不当引起的。
    3.  **非平稳性（Non-stationarity）**：当一个智能体的提示词改变时，下游智能体面临的输入分布也会随之改变，导致联合优化极其困难。
*   **整体含义**：MASPO 旨在通过一种自动化的、迭代的联合优化框架，解决多智能体协作中的提示词协同进化问题。

### 2. 方法论
MASPO 框架包含三个关键技术组件：
*   **多粒度联合评估机制（Joint Evaluation）**：
    *   不再只看最终结果，而是结合三个维度评分：**局部有效性**（是否符合角色约束）、**前瞻潜力**（Lookahead Potential，是否对直接下游智能体有帮助）和**全局对齐**（对最终输出的贡献）。
    *   该机制不依赖地面真值（Ground-truth）标签，而是利用 LLM 作为评估器进行偏好判断。
*   **失配驱动的生成式搜索（Misalignment-Aware Sampling）**：
    *   专门挖掘“局部成功但全局失败”的案例（失配案例），将其存入内存缓冲区。
    *   在生成新提示词候选时，强制优化器分析这些失败案例，从而针对性地修复协作断裂点。
*   **自适应优化动力学（Evolutionary Beam Search & Beam Refresh）**：
    *   采用**坐标上升法**：按拓扑顺序逐个优化智能体。
    *   **束搜索（Beam Search）**：维护 Top-K 个提示词候选。
    *   **束刷新机制（Beam Refresh）**：当上游智能体更新后，下游智能体旧的评分会失效。该机制通过重新评估来校准候选评分，解决非平稳性问题。

### 3. 实验设计
*   **数据集/场景**：涵盖三大领域共 6 个基准测试：
    *   **数学**：MATH-500, AGIEval-MATH, AQuA。
    *   **复杂推理**：GPQA-Diamond。
    *   **代码生成**：MBPP, HumanEval-ET。
*   **智能体架构**：测试了**顺序结构（Sequential）**和**分层结构（Hierarchical）**两种 MAS 拓扑。
*   **对比方法（Baselines）**：
    *   **基础方法**：Vanilla, CoT, Self-Refine。
    *   **MAS 启发式方法**：AgentDropout, SC (CoT)。
    *   **提示词优化算法**：TPE (用于 MIPRO/MASS), SPO (单智能体优化器适配版)。
*   **模型配置**：使用 **Qwen3-8B** 作为智能体骨干模型，**Gemini-2.5-Pro** 作为优化器和评估器。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **成本控制**：作者提到通过限制样本池大小（仅 50 个无标签样本）和每轮迭代的 Batch Size（10 个）来降低计算开销和 API 调用成本。
*   **模型调用**：主要开销在于调用 Gemini API 进行提示词生成和评估。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集上进行了主实验，并针对 Sequential 和 Hierarchical 两种拓扑分别验证。
*   **消融实验**：非常充分。分别验证了束搜索、束刷新、联合评估、失配采样、前瞻深度、计算预算等对结果的影响。
*   **鲁棒性测试**：测试了在“极简初始化”和“错误领域初始化”下的恢复能力，以及跨模型（DeepSeek-V3, GLM-4.6, Claude 等）的提示词迁移效果。
*   **客观性**：通过控制计算预算（Same Budget）与基线方法对比，确保了性能提升并非单纯来自更多的计算资源。

### 6. 主要结论与发现
*   **性能提升**：MASPO 在所有任务上均优于现有方法，相比最强的优化基线平均准确率提升了 **2.9%**。
*   **解决失配**：实验证明，显式注入失配案例能显著降低系统在优化过程中的失配率。
*   **迁移性**：在 Qwen3-8B 上优化出的提示词，直接迁移到 DeepSeek 或 Claude 等模型上依然有效，说明其捕捉到了通用的协作逻辑。
*   **中间信号的重要性**：消融实验显示，局部和前瞻性评分的权重对最终性能的影响大于全局评分，验证了密集中间反馈的必要性。

### 7. 优点
*   **无需标签**：采用自监督评估，极大扩展了在缺乏真值答案的复杂场景下的适用性。
*   **拓扑无关性**：能够适应各种复杂的智能体通信图结构。
*   **针对性强**：通过“失配采样”精准定位协作瓶颈，比盲目的进化算法效率更高。
*   **稳定性高**：束刷新机制有效缓解了多智能体优化中常见的协变量偏移问题。

### 8. 不足与局限
*   **依赖强模型**：优化过程高度依赖高性能的“教师模型”（如 Gemini-2.5-Pro），虽然测试了 8B 模型的自优化能力，但效果会有所下降。
*   **计算延迟**：迭代式的提示词优化过程在部署前需要大量的预计算时间。
*   **搜索空间限制**：尽管使用了束搜索，但在极大规模智能体网络（如数百个智能体）中的扩展性仍有待验证。
*   **偏差风险**：评估器 LLM 可能存在主观偏见（如偏好更长的回答），这可能会影响提示词优化的方向。

（完）
