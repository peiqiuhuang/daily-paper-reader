---
title: "When Prompts Interact: Assessing Prompt Arithmetic for Deconfounding under Distribution Shift"
title_zh: 当提示词相互作用时：评估分布偏移下用于去混杂的提示词算术
authors: "Zhecheng Sheng, Yongsen Tan, Xiruo Ding, Trevor Cohen, Serguei Pakhomov"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03096v1"
tags: ["query:bopl"]
score: 8.0
evidence: 提示微调作为任务算术的参数高效替代方案
tldr: 本研究针对分类模型在分布偏移下易受虚假特征（混淆变量）影响的问题，探讨了提示词算术（Prompt Arithmetic）的有效性。由于全量微调的任务算术成本高昂，作者提出了混合提示词算术（HyPA），通过将任务提示词与线性化的混淆提示词相结合来抵消虚假相关性。实验证明，HyPA在多个基准测试中显著提升了模型在分布偏移下的鲁棒性，并能有效抑制表征中的混淆信号，为解决分布偏移问题提供了一种参数高效的新方案。
selection_source: fresh_fetch
motivation: 模型在分类任务中常依赖虚假特征导致分布偏移下性能下降，而传统的任务算术虽能去噪但全量微调计算成本过高。
method: 提出混合提示词算术（HyPA），通过将任务提示词与线性化的混淆提示词进行组合运算来消除虚假相关性。
result: HyPA在多个基准测试中均改善了鲁棒性与性能的权衡，并被证实能有效减少预测中混淆信号的影响或抑制其表征。
conclusion: HyPA是一种参数高效且极具前景的方法，能够显著提升模型在混淆偏移场景下的鲁棒性。
---

## 摘要
在分类任务中，模型可能会依赖混杂变量来获得强大的分布内性能，从而捕捉到在分布偏移下失效的伪特征。这种捷径行为会导致在分布外设置下的性能大幅下降。任务算术通过减去次要模型更新来移除不需要的信号，提供了一种潜在的解决方案，但它通常需要全量微调，计算成本高昂。提示词微调通过一小组可训练的虚拟标记来适配模型，提供了一种参数高效的替代方案。对生成的提示词进行任务算术是针对整个模型操作的一种极具吸引力的替代方案，但这种方法在多大程度上能限制对伪特征的依赖仍有待确定。在这项工作中，我们研究了通过任务算术组合软提示词是否能提高对混杂偏移的鲁棒性。我们提出了混合提示词算术（HyPA），它将任务提示词与线性化的混杂因素提示词相结合，以抵消伪相关。在多个基准测试中，相对于分布偏移下的提示词算术基准，HyPA 一致地改善了鲁棒性与性能之间的权衡。我们进一步分析了 HyPA 如何影响隐藏表示，并发现证据表明它通过减少混杂信号对预测的影响或在表示中抑制它们来减轻混杂。这些结果表明，在评估的设置中，HyPA 是一种参数高效且极具前景的方法，可用于提高混杂偏移下的鲁棒性。

## Abstract
In classification tasks, models may rely on confounding variables to achieve strong in-distribution performance, capturing spurious features that fail under distribution shift. This shortcut behavior leads to substantial degradation in out-of-distribution settings. Task arithmetic offers a potential solution by removing unwanted signals via subtraction of secondary model updates, but it typically requires full fine-tuning, which is computationally expensive. Prompt tuning provides a parameter-efficient alternative by adapting models through a small set of trainable virtual tokens. Task arithmetic on the resulting prompts presents an appealing alternative to operations on entire models, but the extent to which this approach can limit reliance on spurious features remains to be established. In this work, we study whether composing soft prompts through task arithmetic improves robustness to confounding shifts. We propose Hybrid Prompt Arithmetic (HyPA), which combines task prompts with linearized confounder prompts to counteract spurious correlations. Across multiple benchmarks, HyPA consistently improves the robustness-performance trade-off relative to prompt-arithmetic baselines under distribution shift. We further analyze how HyPA affects hidden representations and find evidence consistent with it mitigating confounding either by reducing the influence of confounder signals on predictions or by suppressing them in the representation. These results establish HyPA as a parameter-efficient and promising approach for improving robustness under confounding shifts in the evaluated setting.

---

## 论文详细总结（自动生成）

这篇论文探讨了在分布偏移（Distribution Shift）背景下，如何利用参数高效的“提示词算术”（Prompt Arithmetic）来消除模型对虚假特征（混淆变量）的依赖。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：机器学习模型在训练时常会捕捉到与标签相关的“虚假相关性”（如背景、机构来源等混淆变量），这种现象被称为“捷径学习”。当测试集分布发生偏移（混淆偏移）时，模型性能会剧烈下降。
*   **核心挑战**：现有的去混杂方法（如任务算术）通常需要对整个模型进行全量微调，计算成本极高。而“提示词微调”（Prompt Tuning）虽然高效，但其在提示词空间进行算术运算（加减向量）能否有效实现去混杂，目前尚不明确。
*   **整体含义**：论文旨在提出一种既能保持模型性能，又能通过简单的提示词向量运算提高鲁棒性的参数高效方案。

### 2. 方法论：混合提示词算术 (HyPA)
*   **核心思想**：结合“非线性微调”的高表达能力和“线性化微调”的解耦特性。
*   **关键技术流程**：
    1.  **第一阶段（非线性任务微调）**：在原始预训练模型上，通过标准的非线性提示词微调获得任务提示词向量 $\tau_t$。这一步保证了模型在目标任务上的基础性能。
    2.  **第二阶段（线性化混淆微调）**：在任务提示词 $P_t$ 的基础上，利用一阶泰勒展开对模型进行局部线性化近似，在切空间（Tangent Space）内训练一个专门捕捉混淆变量特征的向量 $\tau_{lin,c}$。
    3.  **提示词算术组合**：将两者结合，得到去混杂后的提示词 $P_{debias} = P_0 + \tau_t + \lambda \cdot \tau_{lin,c}$。通过调整缩放因子 $\lambda$（通常为负值），从任务表示中“减去”混淆信号。
*   **公式逻辑**：利用线性化微调产生的向量具有更好的参数解耦性，从而使算术减法在提示词空间更加精确有效。

### 3. 实验设计
*   **数据集**：涵盖了临床文本、社交媒体和电商评论等5个基准：
    *   **SHAC / MIMIC**：临床笔记，混淆变量为医疗机构或患者性别。
    *   **Hate Speech**：仇恨言论检测，混淆变量为数据来源。
    *   **Civil Comments**：评论毒性预测，混淆变量为种族提及。
    *   **Amazon Reviews**：情感分类，混淆变量为产品类别。
*   **对比基线 (Benchmarks)**：
    *   **PA (Prompt Arithmetic)**：全非线性微调的提示词算术。
    *   **LinPA (Linearized PA)**：全线性化微调的提示词算术。
    *   **传统方法**：GroupDRO、IRM（不变风险最小化）以及全参数任务算术。
*   **评估指标**：OOD AUPRC（分布外性能）、Absolute Slope（鲁棒性斜率）、Adjusted Integral（综合性能与鲁棒性的指标）。

### 4. 资源与算力
*   **模型规模**：主要使用 GPT-2 (124M)，消融实验中使用了 Qwen3-0.6B 和 Qwen3-1.7B。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到提示词长度为 100，Batch Size 为 8/16，且强调该方法是“参数高效”的（相比全量微调，可训练参数量减少了几个数量级）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域的数据集上进行了测试，每个实验均运行了 5 个随机种子并取平均值。
*   **消融实验**：
    *   测试了不同的锚点选择（初始化点 vs 任务微调点）。
    *   测试了不同规模的基座模型（GPT-2 到 Qwen3）。
    *   测试了不同长度的提示词（50, 100, 150）。
*   **充分性评价**：实验设计较为全面，不仅对比了性能，还通过 t-SNE 可视化和线性探测（Linear Probing）深入分析了隐藏层表征的变化，客观地展示了方法在不同场景下的差异。

### 6. 主要结论与发现
*   **性能优势**：HyPA 在所有数据集上均优于纯非线性或纯线性化的提示词算术，在保持分布内（ID）性能的同时，显著提升了分布外（OOD）的鲁棒性。
*   **去混杂机制**：HyPA 的作用机制具有数据集依赖性。在某些任务中，它通过“阻断”混淆信号对预测的影响起作用；在另一些任务中，它则直接从表征中“移除”了混淆信息。
*   **线性化的重要性**：实验证实，在切空间（线性化）提取混淆向量能显著减少任务特征与混淆特征的纠缠。

### 7. 优点
*   **参数高效**：无需改动预训练模型参数，仅通过微调极少量的虚拟 Token 即可实现去混杂。
*   **即插即用**：作为一种后处理/组合策略，不需要修改原始训练流水线。
*   **平衡性好**：通过混合策略解决了线性化微调表达力不足和非线性微调特征纠缠的矛盾。

### 8. 不足与局限
*   **超参数敏感性**：缩放因子 $\lambda$ 的选择依赖于验证集，在完全未知的测试分布下，如何确定最优的 $\lambda$ 仍是一个挑战。
*   **模型扩展性**：在更大规模的模型（如 Qwen3-1.7B）上，去混杂效果并未表现出明显的缩放效应（Scaling Law），可能与提示词更新在深层网络中的衰减有关。
*   **理论深度**：虽然实验结果扎实，但对于提示词更新如何通过冻结的骨干网络传播并精确影响输出层的理论解释仍有待加强。

（完）
