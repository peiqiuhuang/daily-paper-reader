---
title: Point-Selection Fine-Tuning Framework for Robust Point Cloud Classification
title_zh: 面向鲁棒点云分类的点选择微调框架
authors: "Da Li, Chang Ma, Dongfu Yin"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19711v1"
tags: ["query:bopl"]
score: 8.0
evidence: 参数高效提示词生成
tldr: 针对点云分类中噪声和损坏点导致的性能下降问题，本文提出了一种名为PSFT的点选择微调框架。该框架通过评估点的影响力来剔除离群点，并利用轻量级提示生成分支和特征过滤器在冻结的预训练模型上进行高效微调。PSFT在保持参数效率的同时，显著提升了模型在多种损坏数据集上的鲁棒性，为3D预训练模型的下游适配提供了新思路。
selection_source: fresh_fetch
motivation: 传统的全量微调或参数高效微调在面对点云噪声和损坏时，容易放大离群点影响或对损坏的Token过于敏感，导致鲁棒性下降。
method: 提出PSFT框架，通过估计点影响力自适应保留关键点，并结合层级提示生成与Beta门控残差混合的特征过滤器进行鲁棒微调。
result: 在ModelNet-C、ModelNet40-C和ScanObjectNN-C等多个基准测试中，PSFT在多种预训练骨干网络上均取得了领先的抗损坏性能。
conclusion: PSFT是一种兼顾参数效率与鲁棒性的点云分类微调方案，能有效抑制噪声干扰并提升预训练模型在复杂环境下的泛化能力。
---

## 摘要
噪声和损坏的点会显著降低点云识别性能，尤其是在具有挑战性的损坏设置下。特别是，对 3D 预训练模型进行全量微调可能会放大离群值的影响，并覆盖预训练期间学习到的鲁棒性先验，而朴素的参数高效适配对损坏的 token 仍然敏感。为了解决这个问题，我们提出了 PSFT，一种在保持参数高效的同时提高鲁棒性的点选择微调框架。PSFT 首先从池化前特征中估计逐点影响，并自适应地保留影响最小的点以抑制离群值。基于选定的子集，提示生成分支预测逐层提示 token，并将其注入冻结的主干网络中，以进行轻量级下游适配。为了进一步减轻选择后的残余噪声，我们附加了一个带有瓶颈 MLP 变换和 Beta 门控残差混合的轻量级特征过滤器，以在预测前细化 patch-token 表示。大量实验表明，在所有测试的 3D 预训练主干网络上，PSFT 一致地降低了 ModelNet-C 和 ModelNet40-C 上的损坏误差，同时在评估的微调策略中，使用 ULIP-2 和 Uni3D-B 取得了最强的 ScanObjectNN-C 结果。我们的实现可以在 https://github.com/CVChMA/PSFT/tree/master 找到。

## Abstract
Noisy and corrupted points can substantially degrade point cloud recognition performance, especially under challenging corruption settings. In particular, full fine-tuning of 3D pre-trained models may amplify the influence of outliers and overwrite robustness priors learned during pre-training, while naive parameter-efficient adaptation remains sensitive to corrupted tokens. To address this issue, we propose PSFT, a point-selection fine-tuning framework that improves robustness while remaining parameter-efficient. PSFT first estimates point-wise influence from pre-pooling features and adaptively retains minimally influential points to suppress outliers. Based on the selected subset, a prompt generation branch predicts layer-wise prompt tokens and injects them into a frozen backbone for lightweight downstream adaptation. To further mitigate residual noise after selection, we append a lightweight feature filter with bottleneck MLP transformation and Beta-gated residual blending to refine patch-token representations before prediction. Extensive experiments show that PSFT consistently reduces corruption error on ModelNet-C and ModelNet40-C across all tested 3D pre-trained backbones, while achieving the strongest ScanObjectNN-C results with ULIP-2 and Uni3D-B among the evaluated tuning strategies. Our implementation can be found at https://github.com/CVChMA/PSFT/tree/master.

---

## 论文详细总结（自动生成）

这是一份关于论文《Point-Selection Fine-Tuning Framework for Robust Point Cloud Classification》（面向鲁棒点云分类的点选择微调框架）的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
在 3D 点云理解领域，预训练模型在干净数据上表现优异，但在面对噪声、遮挡或传感器故障导致的**数据损坏（Corruptions）**时，性能会大幅下降。
*   **研究动机**：作者发现，传统的**全量微调（Full Fine-tuning）**虽然在训练初期能提升鲁棒性，但后期会覆盖预训练模型中宝贵的鲁棒先验知识；而仅微调分类头（Head-only）则缺乏足够的适配能力。
*   **核心挑战**：现有的参数高效微调（PEFT）方法对损坏的输入 Token 依然敏感，噪声点会干扰轻量化适配模块的预测。
*   **整体含义**：本文旨在提出一种既能保持参数高效，又能通过主动筛选输入点和细化特征来增强模型抗干扰能力的微调框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **PSFT (Point-Selection Fine-Tuning)** 框架，主要包含三个核心模块：

*   **点选择模块 (Point Selection Module)**：
    *   **核心思想**：基于“离群点通常在全局池化中具有极高影响力”的观察，反其道而行之，保留影响力较小的点。
    *   **技术细节**：通过计算每个点在最大池化（Max-pooling）中贡献最大值的频率来评估其影响力。利用**熵引导的焦点估计**（公式 1-5），自适应地计算需要保留的点数 $S$，从而剔除潜在的噪声离群点。
*   **提示生成模块 (Prompt Generation Module)**：
    *   **核心思想**：在冻结的主干网络中注入任务相关的提示信息。
    *   **技术细节**：使用基于 EdgeConv 的轻量级分支，从筛选后的点集中提取局部几何特征，生成逐层的提示 Token（Prompt Tokens），并将其插入 Transformer 的每一层。
*   **特征过滤器模块 (Feature Filter Module, FFM)**：
    *   **核心思想**：处理筛选后可能残余的微小噪声（如抖动）。
    *   **技术细节**：在编码器之后添加一个瓶颈结构的 MLP。引入了**Beta 门控残差混合**（公式 7-8），在训练时通过 $Beta(\lambda, \lambda)$ 分布随机采样混合比例 $\alpha$，平衡去噪特征与原始特征，增强模型的正则化效果。

### 3. 实验设计
*   **数据集与基准 (Benchmarks)**：
    *   **ModelNet-C**：包含 7 种合成损坏类型（缩放、抖动、旋转等）。
    *   **ModelNet40-C**：更广泛的损坏基准，包含密度、噪声、变换等 15 种损坏。
    *   **ScanObjectNN-C**：基于真实扫描数据的损坏基准，更具挑战性。
*   **骨干网络 (Backbones)**：测试了 Point-BERT、Point-MAE、Uni3D-B 和 ULIP-2 四种主流预训练模型。
*   **对比方法**：包括全量微调 (FT)、仅微调分类头 (Head)、IDPT、VPT、AdaptFormer 等多种微调策略。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，学习率设为 $5 \times 10^{-4}$，训练周期为 300 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量以及总训练时长。但从其参数量对比来看（仅 2.4M 可调参数，约为全量微调的 10.9%），该方法对显存和计算资源的需求远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流基准上进行了详尽的对比实验（表 1-3）。
    *   针对 4 种不同的预训练架构验证了通用性。
    *   进行了**消融实验**（表 5），分别验证了点选择（PS）、提示生成（PG）和特征过滤（FFM）的独立贡献。
    *   进行了**敏感性分析**（图 4），探讨了 Beta 分布参数 $\lambda$ 和测试时混合系数 $\alpha$ 对结果的影响。
*   **充分性评价**：实验设计较为充分，涵盖了从合成到真实的多种场景，且对比了多种 PEFT 基准，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：PSFT 在 ModelNet-C 和 ModelNet40-C 上一致性地降低了平均损坏误差（mCE/mER）。
*   **强模型增益更明显**：在 ScanObjectNN-C 真实基准上，PSFT 配合 ULIP-2 和 Uni3D-B 等强骨干网络取得了最佳效果。
*   **参数效率高**：相比全量微调，PSFT 减少了约 89.1% 的可训练参数，同时获得了更好的鲁棒性。
*   **发现**：简单的 PEFT（如 IDPT）在面对损坏数据时依然脆弱，必须结合输入端的点筛选才能真正实现鲁棒适配。

### 7. 优点（亮点）
*   **自适应性**：通过熵计算自适应确定保留点数，而非固定比例，增强了对不同损坏程度的灵活性。
*   **耦合设计**：将“点选择”与“提示微调”结合，既保护了提示生成的质量，又保留了冻结主干的鲁棒先验。
*   **随机残差混合**：FFM 模块中的 Beta 门控设计是一种巧妙的正则化手段，有效平衡了信号保留与去噪。

### 8. 不足与局限
*   **真实场景的局限性**：在 ScanObjectNN-C 上，对于较弱的骨干网络（如 Point-BERT），PSFT 的提升并不稳健，甚至在某些指标上不如全量微调，说明该方法对基础特征的质量有一定依赖。
*   **计算开销**：虽然训练参数少，但在推理时增加了点选择和提示生成的额外计算步骤（尽管是轻量级的）。
*   **数据增强依赖**：补充材料显示，该方法在某些真实数据集上与 WOLFMix 等强增强手段结合时会出现不一致的表现，存在一定的适配风险。

（完）
