Title: L2O-CCG: Adversarial Learning with Set Generalization for Adaptive Robust Optimization

URL Source: https://arxiv.org/pdf/2603.22548v1

Published Time: Wed, 25 Mar 2026 00:15:55 GMT

Number of Pages: 8

Markdown Content:
# L2O-CCG: Adversarial Learning with Set Generalization for Adaptive Robust Optimization 

Zhiyi Zhou, J´ an Drgoˇ na, Yury Dvorkin 

Abstract — The adversarial subproblem in two-stage adaptive robust optimization (ARO), which identifies the worst-case uncertainty realization, is a major computational bottleneck. This difficulty is exacerbated when the recourse value function is non-concave and the uncertainty set shifts across applications. Existing approaches typically exploit specific structural assump-tions on the value function or the uncertainty set geometry to re-formulate this subproblem, but degrade when these assumptions are violated or the geometry changes at deployment. To address this challenge, we propose L2O-CCG, a bi-level framework that enables the integration of structure-aware adversarial solvers within the constraint-and-column generation (CCG) algorithm. As one instantiation, we develop a generalizable adversarial learning method, which replaces solver-based adversarial search with a learned proximal gradient optimizer that can generalize across uncertainty set geometries without retraining. Here, an inner-level neural network approximates the recourse value function from offline data, while an outer-level pre-trained mapping generates iteration-dependent step sizes for a proximal gradient scheme. We also establish out-of-distribution conver-gence bounds under uncertainty set parameter shifts, showing how the trajectory deviation of the learned optimizer is bounded by the uncertainty set shift. We illustrate performance of the L2O-CCG method on a building HVAC management task. 1

I. I NTRODUCTION 

Two-stage adaptive robust optimization (ARO) is a frame-work for sequential decision-making under uncertainty. By decomposing decisions into here-and-now choices and wait-and-see recourse, ARO represents the adaptive nature of many real-world systems, including power system opera-tions [1], model predictive control (MPC) [2], supply chain design [3], and capital budgeting [4]. The resulting min-max-min optimization, however, leads to NP-hardness in practical cases, e.g., [5]. For example, even when the inner-most recourse problem is convex, the adversarial max-min subproblem can be NP-hard when the inner-most value function is non-concave with respect to the uncertainty vector [5]. Classical methods rely on decomposition algorithms such as column-and-constraint generation (CCG) [5], which iteratively solve adversarial subproblems to identify worst-case scenarios. In large-scale applications, these adversarial                              

> This work was supported in part by the US Department of En-ergy (DOE) Advanced Research Projects Agency–Energy under Grant DEAR000130010.13039/100000001, by National Science Foundation under Grant OISE 2330450, and by the US DOE, Office of Science, ASCR program under the Scientific Discovery through Advanced Computing (SciDAC) Institute “LEADS: LEarning-Accelerated Domain Science”. Zhiyi Zhou, J´ an Drgoˇ na, and Yury Dvorkin are with the Johns Hopkins University, Baltimore MD, U.S. (zzhou124, jdrgona1, ydvorki1@jh.edu).
> 1Codes are available in: https://github.com/Zhiyi-miyo/ L2O-CCG

solves dominate the computational cost, motivating methods which can accelerate the verifiable worst-case search. Recent machine learning approaches aim to reduce this computational burden in ARO. Dumouchell et al [6] train a neural network (NN) to approximate the inner-most value function and embed it into the max-min subproblem as an MILP via ReLU reformulation [7], [8]. While this approach avoids repeatedly solving the inner recourse optimization problem, the resulting adversarial problem remains a MILP whose size grows with the network and must be re-solved at every CCG iteration. Bertsimas and Kim [9] bypass iterative adversarial search by solving many ARO instances offline and learning a direct mapping from problem parameters to optimal solutions. This strategy removes online adversarial solves but requires massive offline computation, and its solution quality degrades when the tested uncertainty set differs from the training. Brenner et al [10] uses varia-tional autoencoders to generate realistic adversarial scenarios through projected gradient ascent in a latent space, reducing conservatism in high-dimensional settings. But they assume specific uncertainty sets and may require retraining when the uncertainty set changes. These limitations highlight the need for learning-based methods that both scale with problem size and remain robust under uncertainty set shifts. In parallel, the learning-to-optimize (L2O) literature has developed parameterized optimizers that learn update rules from data rather than the exact estimation value. Broadly, there exsit two categories: supervised approaches [11], [12] learn from datasets of solved instances to warm-start or accelerate classical solvers; and self-supervised approaches [13], [14], [15] train parameterized update rules via algorithm unrolling without requiring pre-solved solutions. Recent work [15], [14] have established convergence guarantees for the self-supervised one. Martin and Furieri [16] develop convergent-by-design architectures. By learning parameter-ized update rules rather than repeatedly solving optimization subproblems, these approaches can alleviate the computa-tional burden while reducing reliance on large offline datasets of solved instances. Building on this direction, [17] proposes an L2O framework for solving parametric constrained bi-level problems; however, the method does not provide the-oretical guarantees on convergence or robustness under out-of-distribution (OOD) instances. In this paper, we introduce L2O-CCG, a learning-to-optimize framework that accelerates the adversarial search within CCG while generalizing across unseen uncertainty sets without retraining. The main contributions are:  

> •

A bi-level learning framework for ARO : We propose                                     

> arXiv:2603.22548v1 [eess.SY] 23 Mar 2026 TABLE I COMPARISON OF UNCERTAINTY SETS
> Property Polyhedral Smooth (Ellipsoid) Data-driven (W-bell)
> Typical form {ξ:Aξ ≤b, ∥ξ∥1≤Γ}{(ξ−ξ0)⊤Σ−1(ξ−ξ0)≤1}Learned or implicit sets (VAE, GAN, samples) Geometric structure Polytope, axis-aligned, sparse ex-treme points Convex, smooth boundary Implicit, possibly nonconvex Tractability in classical RO Exact reformulation (CCG, Ben-ders) Tractable under strong assumptions Often intractable Typical power system applications Load and renewable forecast errors in OPF; contingency modeling; re-serve sizing Correlated renewable generation uncertainty; chance-constrained OPF; frequency regulation Line rating uncertainty; data-center loads; price-responsive demand; ramping events

a modular architecture where the inner level approx-imates the recourse value function with a trainable surrogate, and the outer level solves the adversarial subproblem via a configurable optimizer that can be adapted to the structural properties of the surrogate and the uncertainty set. As a concrete instantiation, we pair a set-encoder NN with an LSTM-based learned optimizer.  

> •

Set-agnostic generalization via learned operators :We design the learned optimizer so that uncertainty set parameters enter only through analytical proximal operators, not the learned parameters. This decoupling enables generalization across box, polyhedral, ellip-soidal, and GMM uncertainty sets without retraining.  

> •

Out-of-distribution convergence guarantees : We es-tablish convergence bounds showing that the trajectory deviation of the learned optimizer under set parameter shifts is controlled by the proximal operator discrepancy between training and test-time sets, independent of the learned parameters. By testing on a HVAC management problem, we demonstrate that L2O-CCG achieves 10 –100 × speedup over classical methods with comparable solution quality across four un-certainty geometries. II. P RELIMINARIES 

Consider the general form of a two-stage ARO problem: 

min  

> x∈X

h1(x) + max  

> ξ∈U (Γ ,ω )

Q(x, ξ ), (1) where the inner-most (recourse) value function is defined as 

Q(x, ξ ) = min  

> y∈Y (x,ξ )

h2(y) (2) and where x denotes first-stage (here-and-now) decisions made prior to the realization of uncertainty, y represents second-stage (recourse) decisions that adapt after observing uncertainty realization ξ. The uncertainty vector ξ belongs to a prescribed uncertainty set U(Γ , ω ) with budget Γ and shape parameter ω that characterizes the geometry of the set (e.g., covariance in ellipsoidal sets or component-wise bounds in box sets). The feasible recourse region is given by 

Y(x, ξ ) = {y | g1(y, x, ξ ) ≤ 0, g 2(y, x, ξ ) = 0 }. (3) Classical approaches typically rely on decomposition methods to solve (1), including Benders-type and cutting-plane decompositions [1]. Among these, CCG [5] has emerged as one of the most effective methods in practice, which iterates between a master problem and a worst-case subproblem. At iteration k, the master problem 

min  

> x,θ

h1(x) + θ (4) s.t. θ ≥ Q(x, ξ i), i = 1 , . . . , k, x ∈ X ,

is solved over accumulated uncertainty scenarios {ξi}ki=1 .Given the master solution xk, the adversarial subproblem 

ξk+1 ∈ arg max  

> ξ∈U

Q(x, ξ ). (5) identifies the worst-case uncertainty. If no scenario increases the objective beyond tolerance ϵ, the algorithm terminates. Otherwise, ξk+1 is added and the procedure repeats. The CCG performance and convergence depend on the tractabil-ity of (5), which in turn is impacted by the structure of 

Q(x, ξ ) and the geometry of U.

Structure of Q(x, ξ ). In general, Q(x, ξ ) is neither convex nor concave in ξ. This occurs when uncertainty affects system dynamics or constraint coefficients, which is common in control, energy, and resource allocation problems. In such cases, max ξ∈U Q(x, ξ ) may contain multiple local maxima, and the adversarial subproblem becomes NP-hard even when the inner recourse problem is convex. Table II summarizes typical Q-structures arising in common models. 

Geometry of U. The uncertainty set determines the feasibility region for (5). Polyhedral sets allow for vertex enumeration and are compatible with MILP-based CCG. Ellipsoidal sets capture correlations but require quadratic constraints in the adversarial problem, making each CCG iteration substantially more expensive than for linear uncer-tainty sets. Data-driven sets (e.g., GMM density level sets) can represent multi-modal uncertainty but are non-convex, introducing binary variables or big-M disjunctions. Table I summarizes common uncertainty set geometries. 

Joint effect. Table III shows that when Q is concave in ξ, classical methods solve (5) efficiently regardless of set geometry. However, for non-concave Q, solver-based reformulations become intractable or lose global optimality guarantees, regardless of U. Moreover, methods tailored to a specific uncertainty geometry degrade under distributional shift [9], [10]. These limitations motivate L2O-based ap-proaches that approximate the worst-case search, without relying on specific assumptions for Q or U structures. TABLE II COMPARISON OF Q STRUCTURE FOR RECOURSE PROBLEMS                                              

> Inner Problem Q structure
> LP with ξin RHS piecewise affine and convex QP with ξin RHS piecewise affine and convex QCQP with ξin RHS piecewise affine and convex QP with ξin linear coefficients nonconvex and nonconcave QCQP with ξin quadratic coefficients nonconvex and nonconcave TABLE III OPTIMIZER SELECTION FOR max ξ∈U Q(x, ξ )UNDER DIFFERENT UNCERTAINTY GEOMETRIES AND Q-STRUCTURES .
> Set UQ-structure Optimizer
> Poly-hedral Concave, affine LP [18]; exact Concave, quadratic QP [19]; exact Concave, ICNN Gradient-based: projected as-cent / Frank–Wolfe [20]; con-vergent Concave non-smooth, ReLU MILP reformulation [21]; exact but expensive Non-concave, general NN L2O / multi-start [13]; heuristic Ellip-soidal Concave, affine Closed-form [22]; exact Concave, quadratic QCQP [23]; exact Concave, ICNN Projected ascent [24]; efficient Concave non-smooth, ReLU MILP /QCQP reformula-tion [21]; scales poorly Non-concave, general NN L2O / gradient [13]; heuristic Data-driven Concave, affine Cutting-plane [25]; reliable Concave, quadratic Frank–Wolfe; moderate Concave, ICNN Gradient-based with learned projection [26]; scalable Concave non-smooth, ReLU Oracle-based search / Learning optimizer [27]; approximate Non-concave, general NN L2O [28]; scalable, heuristic

III. P ROPOSED L2O-CCG F RAMEWORK 

A. Bi-Level Learning Structure 

We propose a generalizable bi-level learning framework that replaces the computationally expensive adversarial sub-problem of CCG with a learned update rule, as shown in Fig. 1. Inner-level NNs ˆQobj (x, ξ ) and ˆQfea (x, ξ ) approximate the recourse value function and feasibility, respectively, from offline data, while an outer-level learned optimizer d(·)

solves the adversarial subproblem via gradient-based search based on the learned ˆQobj (x, ξ ) and ˆQfea (x, ξ ) values. At each iteration t, the modified procedure is: 1) Master problem : Solve (4) to obtain xt.2) Learned adversarial : Starting from multiple initial points {ξ(m)0 }Mm=1 sampled from U, run K steps of the learned optimizer: 

ξk+1 = ξk − d(zk), (6) where d(zk) is the learned optimizer that generates iteration-dependent step sizes from gradient features 

zk, and the proximal operator enforces ξ ∈ U . Return 

ξt+1 ← arg min m F (ξ(m) 

> K

). The feature vector zk

includes the gradient of the value function ˆQobj (x, ξ )

and ˆQfea (x, ξ ) with respect to ξ. The full design is detailed in Section III-B.  

> Fig. 1. Diagram for the proposed L2O-CCG

Algorithm 1 L2O-CCG: CCG with Learned Adversarial 

Input: Uncertainty set U, tolerance ϵ, multi-starts M , L2O steps K

Output: First-stage decision x∗, robust objective θ∗ 

> 1:

Offline:  

> 2:

Train value network ˆQ(·; ϕ) =  ˆQobj , ˆQfea 

 from data  

> 3:

Train learned optimizer d(·) via (14)  

> 4:

Online:  

> 5:

Ξ ← { 0} 

> 6:

for t = 1 , 2, . . . , T do  

> 7:

(xt, θ t) ← solve master (4) over Ξ 

> 8:

for m = 1 , . . . , M do ▷ multi-start L2O  

> 9:

Sample ξ(m)0 from U 

> 10:

for k = 0 , 1, . . . , K − 1 do  

> 11:

ξ(m) 

> k+1

← update via (6) using d(z(m) 

> k

) 

> 12:

end for  

> 13:

end for  

> 14:

ξt+1 ← arg min m∈{ 1,...,M } F



ξ(m)

> K

 

> 15:

Compute true Q(xt, ξ t+1 ) via (2) ▷ verification  

> 16:

if gap ≤ ϵ then  

> 17:

return (xt, θ t) 

> 18:

end if  

> 19:

Ξ ← Ξ ∪ { ξt+1 } 

> 20:

end for 

3) Exact verification : Evaluate the true recourse 

Q(xt, ξ t+1 ) by solving (2). 4) Convergence check : If the true gap (Q(xt, ξ t+1 ) −

θt)/(|θt| + ϵ) ≤ ϵ, terminate. Otherwise add ξt+1 to the master problem and repeat. Algorithm 1 summarizes the complete procedure. Since the master problem and verification use exact solvers, every scenario added is valid. If the learned adversarial misses the global worst case, CCG continues to iterate and terminates finitely under standard assumptions, e.g., Zeng et al [5] point out that polyhedral uncertainty set should contain finitely many extreme points and put into a convex recourse problem. L2O replaces the MILP-based solution in [29] with M × K

forward passes, typically 2–3 orders of magnitude faster per CCG iteration. The two learning modules are trained sequentially. First, the value network ˆQ(ξ; ϕ) is trained from a dataset of solved recourse problems. Then, the adversarial optimizer d(·) is trained for recourse value function ˆQ over U.B. Adversarial Learning 1) Inner-Level Surrogate: The inner level of L2O-CCG requires a differentiable approximation of Q(x, ξ ). The choice of surrogate architecture should reflect the known or expected Q-structure, e.g., according to Table. III, input-convex NN when Q is known to be concave, kernel-based models for low-dimensional settings, or general feedforward networks when Q is non-concave. In this work, we adopt a general NN to handle the non-concave case without structural assumptions. We first train a NN ˆQ(x, ξ ; ϕ) to approximate the re-course value function from a dataset of solved second-stage problems {(x(i), ξ (i), Q (i)

> obj

, Q (i) 

> fea

)}Ni=1 . Motivated by [6], the architecture uses a set-encoder with element-wise MLPs and sum-pooling aggregation: 

Φx(x) = σpost 

Pnx 

> j=1

σelem (xj )



(7a) 

Φξ (ξ) = σpost 

 P nξ 

> j=1

σelem (ξj ) , (7b) and the concatenated embedding is passed through a dual-head value network: 

[ ˆQobj , ˆQfea ] = MLP val 

 [Φ x(x); Φ ξ (ξ)] , (8) where ˆQobj and ˆQfea predict the recourse cost and constraint violation. Both heads are trained jointly with mean-squared-error loss on min-max normalized targets. The dual-head design enables the adversarial optimizer to seek scenarios that maximize cost while maintaining a feasible recourse. 

2) Outer-Level Optimizer: Given the trained value 

ˆQ(·; ϕ), the adversarial subproblem becomes min ξ∈U F (ξ)

with the augmented objective 

F (ξ) = − ˆQobj (ξ; x)

| {z } 

> f(ξ)

+ α d (ξ; U) + β max  0, ˆQfea (ξ; x)| {z }

> r(ξ)

,

(9) decomposed into a smooth component f (differentiable through the NN) and a non-smooth component r handled by the proximal operator. The outer-level optimizer can likewise be chosen based on the surrogate structure, e.g, according to Table. III, projected gradient ascent for concave surrogates, Frank–Wolfe for polyhedral sets with linear surrogates, or MILP reformulation for ReLU networks. For the non-concave general-NN setting targeted here, we adopt a self-supervised L2O approach based on [30], where the optimizer is trained via algorithm unrolling without requiring pre-solved adversarial instances. Specifically, the update rule (6) expands as: 

vk = Qk vk−1 + ( I − Qk)∇f (ξk), (10a) 

¯ξk+1 = ξk − Rk∇f (ξk) − Bk vk, (10b) 

ξk+1 = prox r,R k ( ¯ξk+1 ), (10c) where Rk, Q k, B k ∈ (0 , 1) nξ are diagonal step-size and mo-mentum matrices generated by d(zk), and vk is a momentum variable. The proximal operator 

prox r,R k ( ¯ξ) = arg min 

> ξ

n

r(ξ) + 12 ∥ξ − ¯ξ∥2

> R−1
> k

o

(11) enforces set membership while accounting for the non-smooth penalty in r. The learned optimizer d(·) is parame-terized as a mapping ϕ:

(Rk, Q k, B k), s k = ϕψ (zk, s k−1), (12) where sk−1 is the hidden state and the feature vector is 

zk = [ ∇f (ξk), g k, v k−1], (13) consisting of the current gradient ∇f (ξk), subgradient gk ∈

∂r (ξk), and the previous momentum vk−1. Outputs are passed through sigmoid activations to ensure bounded step sizes, as required by the convergence theory in [30]. We opt for zk to include only gradient-based features, that is the uncertainty set parameters (Γ , ω ) are excluded from the 

ϕψ (·) input. Instead, these set parameters enter exclusively through the proximal operator (10c). This decoupling yields a geometry-agnostic step-size policy: the mapping ϕψ (·)

generalizes across uncertainty sets and parameters without retraining, while the proximal operator handles set-specific geometry. We provide formal OOD guarantees in Section IV. The proximal step (10c) is the only component that de-pends on the uncertainty set geometry. We derive closed-form or efficient proximal operators for common set geometries:  

> •

Box Ubox : Clamping to [−θj , θ j ] followed by ℓ1-ball projection for budget ∥ξ∥1 ≤ Γ. 

> •

Polyhedral Upoly : Box projection followed by iterative halfplane projection (Dykstra-style) for Hξ ≤ h. 

> •

Ellipsoidal Uellip : Scaling projection ξ ← ξ ·

min(1 , Γ/∥ξ∥Σ−1 ). 

> •

GMM level set Ugmm : Projection onto the nearest component ellipsoid via Mahalanobis distance. All operators are differentiable almost everywhere, enabling end-to-end training of ϕψ (·) through backpropagation. The parameters ψ are trained by unrolling K adversarial steps and minimizing the cumulative augmented objective: 

min  

> ψ

Ex∼D x

" KX

> k=1

F (ξk; x, U)

#

, (14) where the trajectory {ξk}Kk=0 is generated by the update rule (10) with parameters from (12). Gradients are prop-agated via truncated backpropagation through time. At in-ference, the trained optimizer runs from M random initial points sampled from U, and the best solution across restarts is returned as the adversarial scenario. IV. F ORMAL OOD G UARANTEES FOR 

UNCERTAINTY SETS 

This section establishes theoretical guarantees for the pro-posed L2O-CCG when the uncertainty-set parameters differ from those used during training. In particular, we analyze how changes in the uncertainty-set parameters (Γ , ω ) affect the convergence behavior of the learned optimizer. Our analysis proceeds in three steps. First, we establish convergence of the learned optimizer in the in-distribution (InD) setting where the uncertainty-set parameters belong to the training domain. Second, we analyze how deviations in the uncertainty-set parameters induce feature shifts in the learned optimizer and quantify their impact on the optimization trajectory. Finally, we derive bounds on the resulting performance degradation and show that the analysis applies to a broad class of uncertainty sets through a unified penalty representation. Let F (ξ) = f (ξ) + r(ξ) where f (ξ) = − ˆQobj (ξ; x)

represents the piecewise-smooth objective component, while 

r(ξ) = αd (ξ; Γ , θ )+ β max(0 , ˆQfea (ξ, x )) represents the non-smooth penalty components. 

Assumption 1. ( Piecewise L-smoothness ). The learned in-ner recourse f (ξ) = ˆQobj (x, ξ ) is a piecewise L-smooth function. There exists a finite partition {P 1, ..., PM } of the domain such that on each piece Pi, there exists Li ∈ R:

||∇ f (ξ1) − ∇ f (ξ2)|| ≤ Li|| ξ1 − ξ2|| , ∀ξ1, ξ 2 ∈ P i. (15) Let L ≜ max i Li be the maximum local Lipschitz constant. 

Definition 1. ( InD Domain and OOD Domain ). Let SP ⊆

R+ × Ω be a compact set of uncertainty parameters used during training. Then the in-distribution (InD) L2O problem corresponds to (Γ in , ω in ) ∈ S . The OOD domain is SO ≜

R+ × Ω\S . OOD scenarios are (Γ out , ω out ) ∈ S O .We first establish convergence of L2O-CCG in the InD setting, where the configurations coincide during training and testing. Proposition 1 shows that under the piecewise 

L-smoothness assumption, the learned update rule achieves the standard proximal-gradient descent guarantee up to addi-tional terms that appear when the trajectory crosses piecewise boundaries of the value function. 

Proposition 1. ( InD Convergence with Boundary Transition [30] ). For the composite F (ξ) = f (ξ)+ r(ξ) with piecewise 

L-smooth f (·), the optimal L2O configuration is: 

N1 = 1

L 1, N2 = 0 (16) Under these optimal settings, when crossing from piece 

Pi to Pj at boundary ξb, define gradient jump as ∆ξb ≜

||∇ fj (ξb) − ∇ fi(ξb)|| . The per-iteration bound is given as: 

F (ξk) − F (ξk−1) ≤ − 12L || ξk − ξk−1|| 2 + 12L ∆ξ, (17) which can be accumulated across multiple steps: 

F (ξK ) − F (ξ∗) ≤ L

2K || ξ0 − ξ∗|| 2 + 12LK 

X

> k

∆ξk (18) where ∆ξk = 0 is no crossing. Then motivated by [30], we consider two trajectories produced by the same learned update rule d(·): InD trajec-tory {ξk, in , z k, in }k≥0 generated using (Γ in , ω in ), and OOD trajectory {ξk, out , z k, out }k≥0 generated using (Γ out , ω out ).Here we apply gradient-only features: 

z = [ ∇f (ξ)⊤, g ⊤]⊤, g ∈ ∂r (ξ; Γ , ω ) (19) 

Definition 2. (Virtual Feature ). Consider an OOD un-certainty variable ξout ∈ U(Γ out , ω out ) with parameters 

(Γ out , ω out ) /∈ S and a corresponding InD variable ξin ∈U(Γ in , ω in ) with (Γ in , ω in ) ∈ S . Define the virtual feature as the difference between input features: 

δ = zout −zin = [( ∇f (ξout )−∇ f (ξin )) ⊤, (gout −gin )⊤]⊤ (20) where the first component is the gradient difference due to different iterations as f (·) doesn’t depend on Γ and ω, and the second component refers to the subgradient difference where gin ∈ ∂r (ξin ; Γ in , ω in ), g out ∈ ∂r (ξout ; Γ out , ω out ).Building upon the InD guarantee in Proposition 1, we now consider OOD scenarios where only the uncertainty set parameters Γ, ω change while the piecewise smooth recourse component f (ξ) remains identical. This constraint-only shift simplifies the virtual-feature structure and makes the deterioration explicitly traceable to boundary violations. The OOD deterioration can be quantified through feature shift δk, and then transferred to the iterate shift sk via a virtual Jacobian. Similar to [30], the difference between two updates d(zk, out ) and d(zk, in ) is represented by a linear map applied to the feature shift. We assume that the learned update rule d(·) is differen-tiable almost everywhere and Lipschitz on the feature domain of interest. In particular, for each iteration k, there exists a virtual Jacobian matrix Jk ∈ Rd×p such that 

d(zk, out ) = d(zk, in ) + Jk(zk, out − zk, in )= d(zk, in ) + Jkδk, (21) and ∥Jk∥ ≤ CJ

√n for some constant CJ > 0, where n is the dimension of ξ.Under this assumption, the variable shift satisfies 

sk+1 = sk + Jkδk (22a) 

∥sk+1 ∥ ≤ ∥ sk∥ + CJ ∥δk∥ (22b) which shows that the key is to quantify ∥δk∥, which in our setting is induced by (Γ , ω ) shift through the penalty term. 

Theorem 1. ( Per-Iteration OOD Descent Bound ). Under the piecewise L-smoothness assumption and InD-optimal param-eters ( N1 = 1 

> L

1, N2 = 0 ), gradient-only features and the virtual-feature construction, for any OOD pair (Γ out , ω out ) ∈PO , the per-iteration ascent satisfies: 

F (ξk + sk) − F (ξk−1 + sk−1)

≤ − ||∇ f (ξk−1 + sk−1) + gk, out || 

2L

+ L|| diag (J1,k −1δk)( ∇f + gout )|| 2 (23) 

+ L|| ∇f (ξk−1 + sk−1) + gk, out − ∇ f (ξk−1) − gk, in 

2L

− J2,k −1δk|| 2

where the virtual Jacobians satisfy || Ji|| F ≤ Ci

√n

In Theorem 1, the first term is exactly the ideal InD proximal-gradient improvement. The two extra quadratic penalties vanish identically whenever both trajectories stay strictly inside their respective budgeted regions. The subgradient difference decomposes as: 

∆gk = || gk, out − gk, in || ≤ ∥ ∆Γ 

> r

(ξ)∥ + ∥∆ωr (ξ)∥, (24) where 

∆Γ 

> r

(ξ) = ∇ξ r(ξ; Γ out , ω in ) − ∇ ξ r(ξ; Γ in , ω in ), (25) 

∆ωr (ξ) = ∇ξ r(ξ; Γ in , ω out ) − ∇ ξ r(ξ; Γ in , ω in ). (26) Here we quantify the Γ-shift and ω-shift independently. The Γ-shift affects only the budget component of the penalty. The ω-shift changes both (i) the violation level and the chain-rule scaling, producing an anisotropic preconditioning mismatch. 

Corollary 1. ( Subgradient Difference ). Let S(ξ; ω) denote the smoothed budget statistic, and let the budget penalty be a smooth function of t(ξ; Γ , ω ) ≜ S(ξ; ω)−Γ. The subgradient difference is: 

∆gk = ( α(tout ) − α(tin )) ∇ξ S(ξ; ωin ) + ∆ ωr (ξ), (27) where ∆ωr (ξ) follows the Lipschitz split with factors 1

> ω2min

.The OOD penalty terms in Theorem 1 are bounded by: 

O(Lα(ξ)2|∆Γ |2||∇ ξ S|| 2 + || ∆ω ⊙ (ξ − ξ0)|| 2 + || ∆ω|| 2

ω4min 

)

(28) The penalty is exactly zero on all iterations where nei-ther trajectory touches its boundary. The per-iteration local bound naturally extends to the global multi-iteration rate via telescoping. 

Theorem 2. ( Multi-Iteration OOD Rate ). The K iterations’ convergence rate in the OOD scenarios is upper bounded by: 

min 

> 1≤k≤K

[F (ξk + sk) − F (ξ∗ + sk−1)] 

≤ L

2 || ξ0 + s0 − ξ∗ − s∗|| 2

− L

2 || ξK + sK − ξ∗ − s∗|| 2 (29) 

+ L

> K

X

> k=1

(ξk + sk − (∇f + gk, out )/L )⊤

(ξk + sk − ξ∗ − s∗)

The summation term is bounded by 

O(P 

> k

|| ∆g(∆Γ , ∆ω)|| 2) and is identically zero on all iterations that remain interior to both budgeted regions. The proposed OOD analysis does not rely on any combi-natorial or linear structure of the uncertainty set. Instead, it only requires that the uncertainty set be encoded through a soft penalty function r(ξ; ω) with parameters ω. This representation can cover a broad family of uncertainty sets. Table IV summarizes several examples and shows how parameter shifts correspond to the feature shifts analyzed above. For the polyhedral set, the budget statistic S(ξ; ω)

represents a weighted ℓ1 deviation from the nominal point 

ξ0. A change in Γ shifts the threshold defining the feasible region, while changes in ω rescale individual coordinates and thus alter the anisotropy of the uncertainty region. For the ellipsoidal set, the budget statistic is a quadratic form defined by the covariance matrix Σ. A change in Γ modifies the confidence radius of the ellipsoid, while changes in Σ

alter its orientation and axis lengths, effectively introducing                              

> TABLE IV UNCERTAINTY -SET PARAMETERIZATION AND CORRESPONDENCE WITH THE PROPOSED OOD ANALYSIS
> Set Budget statistic S(ξ;ω)OOD shift
> Polyhedral S(ξ;ω) = X
> i
> |ξi−ξ0
> i|
> ωi
> Γ-shift: threshold shift at budget boundary;
> ω-shift: anisotropic coordi-nate scaling Ellipsoidal S(ξ; Σ) =(ξ−
> ξ0)⊤Σ−1(ξ−ξ0)Γ-shift: radius/confidence level change;
> Σ-shift: quadratic preconditioning mismatch Data-driven
> S(ξ;ω) = ρ(ξ;ω)Γ-shift: ambiguity radius change;
> ω-shift: learned representa-tion geometry shift

a quadratic preconditioning mismatch in the penalty gradient. For data-driven sets, the budget statistic is defined through a learned representation ρ(ξ; ω), such as those arising from generative or distributionally robust models. In this case, Γ

controls the ambiguity radius, while the parameters ω define the geometry of the learned uncertainty representation. So in all cases listed above, the penalty takes the generic form r(ξ; ω) = ϕ(S(ξ; ω) − Γ) , where S(·) is the corre-sponding budget statistic and ϕ is a smooth hinge-type func-tion. Consequently, the penalty gradient admits the unified representation ∇ξ r(ξ; ω) = α(S(ξ; ω) − Γ) ∇ξ S(ξ; ω), with 

α = ϕ′. This structure allows the OOD mismatch ∆r (ξ) to be decomposed into a threshold-induced Γ-shift term and a geometry-induced parameter-shift term, both of which can be quantified using the same analysis framework. V. E XPERIMENTS 

All experiments are conducted on an Intel Core i7-10875H CPU and an NVIDIA GeForce RTX 2060 GPU (6 GB VRAM). The algorithms were implemented in Python 3.10 using PyTorch 2.2 [31] and Gurobi 13.0 [32]. NNs were trained using GPU acceleration, whereas optimization sub-problems were solved on CPU. 

A. Problem Settings 

We consider a two-stage ARO problem arising in a HVAC thermal dynamics model of buildings: 

min 

> u0

max  

> ξ∈U (Γ ,θ )

min 

> xt,u t

x⊤ 

> N

Pf xN + 

> N−1

X

> t=1

(x⊤ 

> t

P x t + u⊤ 

> t

Ru t)

s.t. xt+1 = At(ξ)xt + Bt(ξ)ut, ∀t (30a) 

At(ξ) = At, 0 + atξ, ∀t (30b) 

Bt(ξ) = Bt, 0 + btξ, ∀t (30c) 

x ≤ xt ≤ x, u ≤ ut ≤ u, ∀t (30d) 

∆u ≤ ut − u0 ≤ ∆u, ∀t (30e) where the temperature state dimension nx = 4 in coupled building zones, input dimension nu = 2 , corresponding to HVAC control signals. The planning horizon is N = 10 ,and uncertainty dimension is nξ = 5 . The uncertainty vector ξ captures uncertainty in heat transfer coefficients between zones, ambient heat exchange, thermal damping, and HVAC actuation efficiency. Constraint (30e) bounds the deviation of recourse actions from the first-stage decision, modeling limited recourse flexibility. We apply a LSTM-based mapping for the adversarial learning. The optimizer runs K = 40 –60 adversarial steps with 10 –15 random restarts at inference time. We evaluate performance across four uncertainty sets: Box with budget: Ubox = {ξ : |ξj | ≤ θj , Pnξ 

> j=1

|ξj | ≤ Γ}.

Polyhedral with budget: Upoly = {ξ : Hξ ≤ h, |ξj | ≤ 

θj , P 

> j

|ξj | ≤ Γ} where H ∈ Rm×nξ encodes coupling constraints between related uncertainty components (i.e., joint limits on spring stiffness perturbations). Ellipsoid with budget: Uellip = ξ : ξ⊤Σ−1ξ ≤ Γ2 with 

Σ encoding correlations between uncertainties. GMM density level set (nonconvex): Ugmm =n

ξ : PCc=1 wc N (ξ | μc, Σc) ≥ ρ

o

.We compare the proposed L2O-CCG against two represen-tative baselines: classical CCG [5] and Neur2RO [29]. For classical CCG, since grid search over U ⊂ R5 is infeasible, the adversarial step uses multi-start random sampling ( 50 

candidates) with exact QP evaluation. This serves as the gold-standard reference for solution quality. For Neur2RO, the adversarial subproblem becomes a MILP over the uncertainty set. For ellipsoidal and GMM sets, the MILP includes quadratic and big-M constraints respectively. 

B. In-Distribution Results 

We evaluate all methods on the nominal problem instances where the training and test uncertainty sets are identical (in-distribution). For each uncertainty set type, we report: (i) Verified objective: the robust cost evaluated at each method’s 

u∗ 

> 0

against the same multi-start adversarial oracle ( 500 can-didates), ensuring fair comparison; (ii) computation time: total solve time including all CCG iterations; (iii) Number of scenarios: total scenarios generated before convergence; (iv) Optimality gap: relative gap to the CCG reference. Table. V compares the three methods under InD settings across four uncertainty set geometries. L2O-CCG consis-tently achieves the fastest solve times across all four ge-ometries. The solution quality remains competitive: on box and polyhedral sets, the optimality gap is within 0.2% and 3.7%, respectively; on ellipsoidal and GMM sets, L2O-CCG actually achieves smaller gaps (6.2% and 4.7%) than Neur2RO (7.4% and 8.3%), suggesting that the learned opti-mizer navigates the non-convex adversarial landscape more effectively than the MILP reformulation in these geometries. The iteration number required by L2O-CCG is also notably low (1–5 scenarios), indicating that the L2O-CCG identifies high-quality worst-case scenarios from the first iteration. 

C. Out-of-Distribution Results 

A key advantage of our bi-level L2O-CCG is robustness to distribution shift. We train the L2O optimizer on a nominal uncertainty set and evaluate on perturbed sets at test time:                                                      

> TABLE V IN-DISTRIBUTION COMPARISON ACROSS UNCERTAINTY SET TYPES .Set Metric CCG Neur2RO L2O-CCG Box Verified Obj. 25.60 25.64 25.66 Time (s) 87.24 10.99 0.78 # Scenarios 231Gap (%) 0.0 0.1% 0.2% Polyhedral Verified Obj. 25.60 25.63 24.64 Time (s) 83.86 81.28 1.37 # Scenarios 231Gap (%) 0.0 0.1% -3.7% Ellipsoidal Verified Obj. 22.88 21.19 21.46 Time (s) 753.53 59.32 0.74 # Scenarios 21 21Gap (%) 0.0 7.4% 6.2% GMM Verified Obj. 25.82 27.97 27.04 Time (s) 832.81 61.38 9.26 # Scenarios 21 25Gap (%) 0.0 8.3% 4.7%
> •

Box : Train with (θ, Γ) = (0 .3, 1.0) ; test with θ ∈{0.2, 0.4, 0.5} and Γ ∈ { 0.8, 1.2, 1.5}. 

> •

Polyhedral : Train with nominal ( θ, Γ, H, h ); text with scaled θ, Γ and tightened/relaxed halfplance constraints 

h′ = sh · h for sh ∈ { 0.8, 1.2}. 

> •

Ellipsoid : Train with nominal Σ, Γ; test with Γ′ = s · Γ

for s ∈ { 0.7, 1.3, 1.5} and rotated Σ. 

> •

GMM : Train with nominal {(wc, μ c, Σc)}3

> c=1

; test with shifted component means μ′ 

> c

= μc + δ for 

δ ∈ { 0.05 , 0.1} and modified density threshold ρ′ ∈{0.5ρ, 2ρ}.Fig. 2 and Fig. 3 compare generalization performance when the uncertainty set parameters shift from those used during training. Across all four geometries, L2O-CCG main-tains smaller optimality gaps than Neur2RO. This is because since the LSTM never sees set parameters during training, its step-size policy is less affected by parameter shifts, and the proximal operator automatically adapts to the new geometry. Notably, the gap improvement of L2O-CCG over Neur2RO is more pronounced under OOD conditions than InD. VI. C ONCLUSIONS 

The adversarial subproblem in two-stage ARO is akey computational bottleneck that limits the scalability of decomposition-based algorithms. We proposes L2O-CCG, a modular bi-level framework that integrates learning-to-optimize into the CCG algorithm. By combining a NN surro-gate of the recourse value function with a learned optimizer trained via algorithm unrolling, L2O-CCG eliminates the need for MILP-based adversarial solves while preserving the convergence guarantee of the CCG algorithm through exact verification. The set-agnostic design, where uncertainty set parameters enter only through proximal operators and are decoupled from the learned optimizer parameters, enables a single trained model to generalize across different ge-ometry without retraining. We establish OOD convergence bounds showing how the optimizer’s trajectory deviation is controlled by the distribution shift. Experiments on a Fig. 2. Relative error (compared with benchmark CCG) across out-of-distribution scenarios for different uncertainty sets. We evaluate four different uncertainty sets: box, polyhedral, elliposid, and GMM. L2O-CCG consistently achieves comparable or lower error than Neur2RO across all settings, while maintaining tighter distributions and fewer extreme deviations. The results indicate strong generalization performance of the proposed method under distributional shifts. 

Fig. 3. Computation time across out-of-distribution scenarios for different uncertainty sets. We evaluate four different uncertainty sets: box, polyhedral, elliposid, and GMM. L2O-CCG achieves orders-of-magnitude speedup compared to CCG and outperforms Neur2RO in most cases. 

robust MPC benchmark confirm that L2O-CCG achieves significant speedups over classical CCG and MILP-based learning approaches (Neur2RO) with comparable solution quality across four uncertainty geometries, under both in-distribution and out-of-distribution cases. REFERENCES [1] D. Bertsimas, E. Litvinov, X. A. Sun, J. Zhao, and T. Zheng, “Adaptive robust optimization for the security constrained unit commitment problem,” IEEE Trans. Power Syst. , vol. 28, no. 1, pp. 52–63, 2013. [2] A. Bemporad and M. Morari, “Robust model predictive control: A survey,” in Robustness in Identification and Control , vol. 245 of 

Lecture Notes in Control and Information Sciences , pp. 207–226, Springer, 1999. [3] A. Ben-Tal, A. Goryashko, E. Guslitzer, and A. Nemirovski, “Ad-justable robust solutions of uncertain linear programs,” Math. Pro-gram. , vol. 99, no. 2, pp. 351–376, 2004. [4] G. A. Hanasusanto et al. , “ k-adaptability in two-stage robust binary programming,” vol. 63, no. 4, pp. 877–891, 2015. [5] B. Zeng and L. Zhao, “Solving two-stage robust optimization problems using a column-and-constraint generation method,” Oper. Res. Lett. ,vol. 41, no. 5, pp. 457–461, 2013. [6] J. Dumouchelle, E. B. Khalil, and M. Bodur, “Neur2RO: Neural two-stage robust optimization,” in Proc. Int. Conf. Learn. Represent. (ICLR) , 2024. [7] M. Fischetti et al. , “Deep neural networks and mixed integer linear optimization,” Constraints , vol. 23, no. 3, pp. 296–309, 2018. [8] B. Grimstad et al. , “ReLU networks as surrogate models in mixed-integer linear programs,” Computers & Chemical Engineering ,vol. 131, p. 106580, 2019. [9] D. Bertsimas, N. Kallus, C. McCord, and B. Sturt, “A machine learning approach to two-stage adaptive robust optimization,” Eur. J. Oper. Res. ,vol. 319, no. 1, pp. 16–30, 2024. [10] A. Brenner, A. Sidford, and Y. Dvorkin, “A deep generative learning approach for two-stage adaptive robust optimization,” 2024. [11] R. Sambharya, H. Chen, R. Sun, and M. Liu, “Learning to warm-start fixed-point optimization algorithms,” J. Mach. Learn. Res. , vol. 25, no. 166, pp. 1–46, 2024. [12] E. King et al. , “Metric learning to accelerate convergence of operator splitting methods,” in IEEE 63rd Conference on Decision and Control (CDC) , pp. 1553–1560, 2024. [13] M. Andrychowicz, M. Denil, S. Gomez, M. W. Hoffman, D. Pfau, T. Schaul, B. Shillingford, and N. de Freitas, “Learning to learn by gradient descent by gradient descent,” in Adv. Neural Inf. Process. Syst. (NeurIPS) , vol. 29, 2016. [14] K. Li and J. Malik, “Learning to optimize,” in Proc. Int. Conf. Learn. Represent. (ICLR) , 2017. [15] B. Tang et al. , “Learning to optimize for mixed-integer non-linear programming with feasibility guarantees,” 2025. [16] A. Martin et al. , “Learning to optimize with convergence guarantees using nonlinear system theory,” IEEE Control Systems Letters , 2024. [17] J. Kotary, H. Sharma, E. King, D. Vrabie, F. Fioretto, and J. Drgona, “Learning to solve constrained bilevel control co-design problems,” 

arXiv preprint arXiv:2507.09050 , 2025. [18] D. Bertsimas et al. , “The price of robustness,” Operations research ,vol. 52, no. 1, pp. 35–53, 2004. [19] A. Ben-Tal et al. , “Robust solutions of uncertain linear programs,” 

Operations Research Letters , 1999. [20] B. Amos, L. Xu, and J. Z. Kolter, “Input convex neural networks,” in 

Proc. Int. Conf. Mach. Learn. (ICML) , pp. 146–155, 2017. [21] R. Anderson, J. Huchette, W. Ma, C. Tjandraatmadja, and J. P. Vielma, “Strong mixed-integer programming formulations for trained neural networks,” Mathematical Programming , vol. 183, no. 1, 2020. [22] A. Ben-Tal et al. , Robust Optimization . 2009. [23] A. R. Conn, N. I. Gould, and P. L. Toint, Trust region methods . SIAM, 2000. [24] S. Boyd and L. Vandenberghe, Convex optimization . Cambridge university press, 2004. [25] A. Nemirovskii, “Interior-point polynomial algorithms in convex pro-gramming,” Sort , vol. 20, no. 50, p. 100, 2004. [26] B. Amos and J. Z. Kolter, “Optnet: Differentiable optimization as a layer in neural networks,” in International conference on machine learning , pp. 136–145, PMLR, 2017. [27] P. Mohajerin Esfahani and D. Kuhn, “Data-driven distributionally robust optimization using the wasserstein metric: Performance guar-antees and tractable reformulations,” Mathematical Programming ,vol. 171, no. 1, pp. 115–166, 2018. [28] C. et al., “Learning to optimize for resource allocation,” NeurIPS ,2017. [29] J. Dumouchelle, E. Julien, J. Kurtz, and E. B. Khalil, “Deep learning for two-stage robust integer optimization,” arXiv preprint arXiv:2310.04345 , 2023. [30] Q. Song, W. Lin, J. Wang, and H. Xu, “Towards robust learning to optimize with theoretical guarantees,” in Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR) , pp. 27498–27506, 2024. [31] A. Paszke et al. , “Pytorch: An imperative style, high-performance deep learning library,” Advances in Neural Information Processing Systems ,vol. 32, 2019. [32] Gurobi Optimization, LLC, “Gurobi optimizer reference manual,” 2024.