---
title: "SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation"
title_zh: SCMAPR：面向复杂场景文本生成视频的自纠错多智能体提示词优化
authors: "Chengyi Yang, Pengzhen Li, Jiayin Qi, Aimin Zhou, Ji Wu, Ji Liu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05489v1"
tags: ["query:bopl"]
score: 8.0
evidence: 多智能体提示词精炼与优化
tldr: 针对文本生成视频（T2V）在复杂场景下因提示词模糊导致的生成质量差问题，本文提出了SCMAPR框架。该框架采用多智能体协作模式，通过场景感知路由、策略驱动优化及结构化语义校验实现提示词的自我修正。此外，研究者还推出了T2V-Complexity基准测试集。实验证明，SCMAPR在多个主流榜单上显著提升了文本与视频的对齐度及生成质量。
selection_source: fresh_fetch
motivation: 现有的T2V系统在处理复杂场景时，常因文本提示词的歧义和描述不足而难以生成高质量视频。
method: 提出一种多智能体提示词优化框架，通过场景分类路由、策略化重写及带反馈的自我修正机制来增强提示词。
result: 在VBench、EvalCrafter和T2V-CompBench等多个基准测试中，SCMAPR均显著优于现有的SOTA基准模型。
conclusion: SCMAPR通过多智能体协同优化，有效解决了复杂场景下的T2V对齐难题，并为该领域的评估提供了新的基准。
---

## 摘要
文本生成视频（T2V）生成受益于扩散模型的最新进展，但现有系统在复杂场景下仍面临挑战，这通常由于文本提示词的歧义性和描述不足而加剧。在这项工作中，我们将复杂场景下的提示词优化建模为一个阶段性的多智能体优化过程，并提出了 SCMAPR，即一种用于 T2V 提示词的场景感知且自纠错的多智能体提示词优化框架。SCMAPR 协调专门的智能体来执行以下操作：(i) 将每个提示词路由到基于分类法的场景以进行策略选择；(ii) 合成场景感知的重写策略并执行受策略约束的优化；(iii) 进行结构化语义验证，并在检测到违规时触发条件修正。为了明确 T2V 提示词中复杂场景的构成，提供代表性示例，并在这些挑战性条件下进行严格评估，我们进一步引入了 {T2V-Complexity}，这是一个专门由复杂场景提示词组成的复杂场景 T2V 基准测试。在 3 个现有基准测试和我们的 T2V-Complexity 基准测试上进行的广泛实验表明，SCMAPR 在复杂场景下持续提升了文本-视频对齐度和整体生成质量，在 VBench 和 EvalCrafter 上的平均得分分别提升了高达 2.67% 和 3.28，在 T2V-CompBench 上相比 3 个最先进的基准模型提升了高达 0.028。

## Abstract
Text-to-Video (T2V) generation has benefited from recent advances in diffusion models, yet current systems still struggle under complex scenarios, which are generally exacerbated by the ambiguity and underspecification of text prompts. In this work, we formulate complex-scenario prompt refinement as a stage-wise multi-agent refinement process and propose SCMAPR, i.e., a scenario-aware and Self-Correcting Multi-Agent Prompt Refinement framework for T2V prompting. SCMAPR coordinates specialized agents to (i) route each prompt to a taxonomy-grounded scenario for strategy selection, (ii) synthesize scenario-aware rewriting policies and perform policy-conditioned refinement, and (iii) conduct structured semantic verification that triggers conditional revision when violations are detected. To clarify what constitutes complex scenarios in T2V prompting, provide representative examples, and enable rigorous evaluation under such challenging conditions, we further introduce {T2V-Complexity}, which is a complex-scenario T2V benchmark consisting exclusively of complex-scenario prompts. Extensive experiments on 3 existing benchmarks and our T2V-Complexity benchmark demonstrate that SCMAPR consistently improves text-video alignment and overall generation quality under complex scenarios, achieving up to 2.67\% and 3.28 gains in average score on VBench and EvalCrafter, and up to 0.028 improvement on T2V-CompBench over 3 State-Of-The-Art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的文本生成视频（T2V）扩散模型在处理“复杂场景”时表现不佳。这主要是因为用户输入的提示词（Prompts）往往存在歧义、描述不足或缺乏时空逻辑约束。
*   **研究背景**：虽然现有的提示词优化方法在文本生成图像（T2I）领域较为成熟，但 T2V 要求更高（需兼顾时间一致性、运动动力学和物理规律）。现有的 T2V 优化方法（如 RAPO）往往只关注对象关系，忽略了抽象语义和长程时间一致性等复杂挑战。

### 2. 方法论：SCMAPR 框架
SCMAPR 将提示词优化建模为一个**阶段性的多智能体协作过程**，包含 6 个专门的智能体和 5 个功能阶段：
*   **阶段 I：场景路由（Scenario Routing）**：由 *Scenario Router* 根据预定义的 10 类复杂场景分类法（如抽象描述、复杂空间关系、多元素场景等）对原始提示词进行打标。
*   **阶段 II：策略合成（Policy Synthesis）**：*Policy Generator* 根据场景标签生成针对性的重写策略，明确哪些隐式约束需要显式化。
*   **阶段 III：策略约束优化（Policy-Conditioned Refinement）**：*Prompt Refiner* 在策略指导下重写提示词，丰富细节并消除歧义。
*   **阶段 IV：语义验证（Semantic Verification）**：
    *   **原子化提取**：*Semantic Atomizer* 将原始提示词分解为原子语义单元（角色、对象、动作等）。
    *   **蕴含校验**：*Entailment Validator* 检查优化后的提示词是否完整保留或违背了这些原子语义。
*   **阶段 V：条件修正（Conditional Revision）**：若验证发现语义缺失或矛盾，*Content Reviser* 根据反馈进行针对性修正，形成自纠错闭环。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **现有基准**：VBench、EvalCrafter、T2V-CompBench。
    *   **新基准（T2V-Complexity）**：论文引入了一个包含 1000 个提示词的新基准，涵盖 10 类复杂场景，每类 100 个，旨在填补现有基准在复杂场景覆盖上的不足。
*   **对比方法**：直接提示（Direct Prompting）、Open-Sora 提示词优化器、RAPO（SOTA 方法）。
*   **底层模型（Backbones）**：使用了 LaVie 和 Wan2.1 两种主流 T2V 模型作为生成引擎。
*   **LLM 驱动**：使用 DeepSeek-V3 作为多智能体系统的核心推理引擎。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA H100 GPU** 的服务器上运行。
*   **算力细节**：文中未明确给出具体的训练时长（因为该框架主要是基于 LLM 的推理过程，属于 Training-free 框架），但提到了嵌入模型使用了 BGE-M3。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的基准测试上进行了大规模定量评估，涵盖了美学质量、背景一致性、成像质量、运动平滑度、主体一致性等多个维度。
*   **消融实验**：针对“场景路由”、“策略生成”和“验证与自纠错”三个核心组件进行了消融实验，证明了每个模块对最终性能的贡献。
*   **客观性**：通过多模型（LaVie/Wan）验证，确保了方法的通用性；引入新基准 T2V-Complexity 解决了现有数据集类别不平衡的问题，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：SCMAPR 在所有基准测试中均取得 SOTA 结果。在 VBench 上平均得分提升高达 2.67%，在 EvalCrafter 上提升 3.28，在 T2V-CompBench 上提升 0.028。
*   **语义对齐**：通过原子级验证，SCMAPR 显著减少了生成过程中的“幻觉”现象（如生成未提及的对象或忽略关键动作）。
*   **复杂场景处理**：在处理抽象语义（如“希望”）和复杂空间布局（如“在...之间”）时，SCMAPR 的策略化重写能有效引导模型生成更符合逻辑的视频。

### 7. 优点与亮点
*   **自纠错机制**：引入了结构化的语义验证闭环，不仅是简单的“扩写”，更保证了“忠实度”。
*   **场景感知**：通过分类法（Taxonomy）进行路由，实现了“因材施教”的优化策略。
*   **新基准贡献**：T2V-Complexity 为社区提供了评估复杂 T2V 任务的细粒度工具。

### 8. 不足与局限
*   **推理开销**：多智能体协作（尤其是多次 LLM 调用和自纠错循环）会显著增加提示词处理阶段的延迟和 API 成本。
*   **LLM 依赖**：系统的有效性高度依赖于底层 LLM（如 DeepSeek-V3）的推理能力，若 LLM 无法准确理解复杂逻辑，优化效果会打折扣。
*   **分类法局限**：虽然定义了 10 类场景，但现实中的复杂场景可能超出此分类，且场景之间可能存在重叠。

（完）
