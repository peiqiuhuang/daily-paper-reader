---
title: "RobustSGPO: Search-Space Control for Agent Harness Evolution"
title_zh: RobustSGPO：用于智能体框架演进的搜索空间控制
authors: "Zibo Zhao, Jijun Shi, Mo Zhou, Zhongyuan Wang, Shifu Bie, Yunfei Zhang, Xuanting Zhou, Xiangyu Wu, Bin Liu, Ruiming Tang, Wenwu Ou, Kun Gai"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.09646v1"
tags: ["query:bopl"]
score: 9.0
evidence: 基于语义梯度的提示优化
tldr: RobustSGPO旨在解决语义梯度提示优化（SGPO）中编辑范围和操作选择不明确的问题。该方法通过引入搜索空间控制，包括明确的编辑请求、补丁验证及基于快照的搜索策略，显著提升了智能体框架的演进效率。在AgentX工作流的实验中，RobustSGPO在任务完成率和测试质量上均表现优异，证明了其在复杂任务迁移和资源受限环境下的鲁棒性与有效性。
selection_source: fresh_fetch
motivation: 现有SGPO方法缺乏对编辑范围和操作的有效约束，导致搜索过程不稳定且效率低下。
method: 引入RobustSGPO框架，通过明确编辑请求、构建并验证补丁，以及基于快照的搜索策略来优化智能体框架。
result: "在120项任务的测试中，RobustSGPO将任务完成率从60.0%提升至80.0%，并显著提高了测试质量得分。"
conclusion: 通过搜索空间控制和合理的快照保留策略，能够有效提升智能体在复杂任务中的演进质量与迁移能力。
---

## 摘要
基于语义梯度的提示优化（SGPO）利用执行反馈来改进智能体框架，但其局部更新规则并未解决编辑范围和操作选择的问题。我们引入了 RobustSGPO，它能够指定所需的编辑、构建并检查补丁，并从当前最优解或保留的快照中继续搜索。我们在 AgentX 头脑风暴工作流中，通过 120 个任务、95 次运行和 7,350 次候选尝试，评估了权限调度、累积控制和任务族迁移。周期性的 1→2→3 调度比固定最大权限的测试得分高出 0.28 分。在 2000 万 token 的预算下，RobustSGPO 将 30 个留出任务的完成率从 60.0% 提高到 80.0%，并将测试质量从 3.77 提升至 4.14。类别保留减少了任务迁移后的源任务性能衰减，而随机保留则达到了更高的目标终点。搜索空间控制通过可执行的编辑和备选起点提升了质量，并伴有可衡量的保留开销。

## Abstract
Semantic-gradient-based prompt optimization (SGPO) improves agent harnesses using execution feedback, but its local update rule leaves the choice of edit scope and operation unresolved. We introduce RobustSGPO, which specifies the requested edit, constructs and checks the patch, and continues search from either the incumbent or retained snapshots. We evaluate permission scheduling, cumulative controls, and task-family transfer in the AgentX brainstorming workflow using 120 tasks, 95 runs, and 7,350 candidate attempts. Periodic $1\to2\to3$ scheduling exceeds fixed maximum permission by 0.28 test-score points. RobustSGPO increases completion on 30 held-out tasks from 60.0% to 80.0% and improves test quality from 3.77 to 4.14 under a 20-million-token budget. Category retention reduces source-task degradation after a shift, whereas random retention reaches a higher destination endpoint. Search-space control benefits quality through executable edits and alternative starting points, with measurable retention overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《RobustSGPO: Search-Space Control for Agent Harness Evolution》的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：AgentX 框架引入了基于语义梯度的提示优化（SGPO），通过执行反馈自动演进智能体框架。
*   **问题**：原有的 SGPO 缺乏对“编辑范围”和“操作类型”的有效约束。模型在进行框架修改时，往往倾向于简单的指令重写，难以处理复杂的跨智能体交互故障，且搜索过程容易陷入局部最优或无效的探索空间。
*   **目标**：通过引入显式的搜索空间控制，提升智能体框架演进的有效性、结构化程度及在任务迁移中的鲁棒性。

### 2. 方法论：RobustSGPO
*   **核心思想**：将自由形式的更新转化为受控的“编辑请求”，通过类型化编译器（Typed Compiler）确保补丁的可执行性，并利用基于类别的快照保留机制优化搜索起点。
*   **关键技术细节**：
    *   **编辑选择（Search-Space Control）**：通过定义范围（Scope）、操作（Operation）和目标（Target）三元组，将搜索空间从无约束的文本生成限制为可行的结构化编辑。
    *   **补丁构建与检查**：引入“类型化编译器”，对模型生成的补丁进行预执行检查，确保结构性修改（如添加/删除智能体）符合规范，过滤无效编辑。
    *   **快照保留（Category Retention）**：借鉴 MAP-Elites 思想，按编辑类别存储最优快照，在后续搜索中交替使用当前最优解和历史保留快照作为起点，以平衡探索与利用。

### 3. 实验设计
*   **场景**：基于 AgentX 的工业推荐系统头脑风暴工作流。
*   **数据集**：120 个任务，分为“局部指令故障”和“跨智能体交互故障”两类，按 30/15/15 比例划分为优化、验证和测试集。
*   **对比方法**：对比了固定权限（$\alpha_1, \alpha_2, \alpha_3$）、周期性调度、随机保留策略以及原始 SGPO 方法。

### 4. 资源与算力
*   **算力消耗**：论文未明确提及具体的 GPU 型号或集群规模。
*   **预算限制**：实验以“Token 消耗”作为核心成本度量，设定了 2000 万 Token 的总预算上限，用于衡量不同方法在相同算力成本下的性能表现。

### 5. 实验数量与充分性
*   **规模**：共进行了 95 次运行，累计 7,350 次候选尝试。
*   **充分性**：实验设计涵盖了权限调度（E1）、消融实验（E2）和任务迁移（E3），并进行了严格的对比测试。实验通过固定初始条件、多次随机种子运行，保证了结果的客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：RobustSGPO 在 2000 万 Token 预算下，将测试质量从 3.77 提升至 4.14，任务完成率从 60.0% 提升至 80.0%。
*   **调度策略**：周期性权限调度（1→2→3）优于固定权限，能更有效地覆盖搜索空间。
*   **迁移能力**：类别保留策略能显著减少任务迁移后的性能衰减，而随机保留虽能达到更高的目标终点，但会牺牲源任务的性能。

### 7. 优点
*   **结构化演进**：将模糊的提示优化转化为可控的结构化编辑，显著提高了演进的成功率。
*   **资源效率**：在同等 Token 预算下，RobustSGPO 表现出更强的收敛能力。
*   **可解释性**：通过分类保留快照，使得演进过程中的搜索路径更具可追溯性。

### 8. 不足与局限
*   **应用范围**：目前主要在 AgentX 的特定工作流中验证，对其他复杂 Agent 系统的通用性有待进一步证明。
*   **保留开销**：虽然提升了质量，但维护快照库和进行重评分（Rescoring）带来了额外的计算开销。
*   **自动化程度**：尽管引入了编译器，但对于复杂的跨智能体逻辑，仍依赖于预定义的编辑规则，缺乏完全的端到端自动化。

（完）
