---
title: Local-Global Prompt Learning via Sparse Optimal Transport
title_zh: 基于稀疏最优传输的局部-全局提示学习
authors: "Deniz Kizaroğlu, Ülku Tuncer Küçüktas, Emre Çakmakyurdu, Alptekin Temizel"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08347v1"
tags: ["query:bopl"]
score: 9.0
evidence: 通过最优传输进行的可微提示学习
tldr: 本研究针对视觉语言模型在少样本学习中局部对齐冗余和提示词重叠的问题，提出了SOT-GLP方法。该方法结合了全局对齐分支与基于稀疏最优传输（Sparse OT）的局部对齐分支，通过显式划分显著视觉区域并将其分配给类别特定的局部提示词，实现了更精细的特征捕捉。实验表明，SOT-GLP在11个基准数据集的少样本分类和分布外（OOD）检测任务中均取得了领先性能，并揭示了保持原始特征空间几何对提升模型鲁棒性的重要性。
selection_source: fresh_fetch
motivation: 现有局部提示词学习方法在选择图像区域时相互独立，导致局部特征使用冗余且提示词之间存在重叠。
method: 引入共享的稀疏补丁支持和平衡最优传输分配机制，将显著视觉区域显式划分给类别特定的局部提示词，并结合全局对齐分支。
result: "在16-shot ViT-B/16设置下，该方法在11个数据集上达到85.1%的平均准确率，且在OOD检测中实现94.2%的AUC。"
conclusion: 无投影的局部对齐能够保留CLIP原始流形的几何特性，在优化分类性能的同时显著增强了模型的分布外检测能力。
---

## 摘要
视觉语言模型（VLM，如 CLIP）的少样本适配通常依赖于学习与全局图像嵌入相匹配的文本提示。近期的研究通过引入局部图文对齐来捕捉细粒度视觉线索，从而扩展了这一范式。然而，这些方法通常为每个提示独立选择局部区域，导致局部特征使用冗余以及提示重叠。我们提出了 SOT-GLP，它引入了共享的稀疏补丁（patch）支撑和平衡最优传输分配，在保持全局对齐的同时，显式地在类别特定的局部提示之间划分显著视觉区域。我们的方法学习共享的全局提示和类别特定的局部提示。全局分支维持标准的图文匹配，以实现稳健的类别级对齐。局部分支利用 V-V 注意力构建类别条件的稀疏补丁集，并通过平衡熵最优传输将其与多个类别特定的提示对齐，从而产生补丁的软划分，防止提示重叠和坍缩。我们在两个互补的目标上评估了我们的方法：(i) 在 11 个标准基准上的少样本分类准确率，以及 (ii) 分布外（OOD）检测。在使用 16-shot ViT-B/16 的标准 11 数据集基准上，SOT-GLP 达到了 85.1% 的平均准确率，优于之前的提示学习方法。我们在提示学习中发现了一个明显的准确率-鲁棒性权衡：虽然可学习的投影优化了分布内拟合，但它们改变了基础特征空间。我们证明了无投影的局部对齐保留了 CLIP 流形的原始几何结构，从而实现了超越全适配模型的先进 OOD 检测性能（94.2% AUC）。代码实现见：https://github.com/Deniz2304988/SOT-GLP

## Abstract
Few-shot adaptation of vision-language models (VLMs) like CLIP typically relies on learning textual prompts matched to global image embeddings. Recent works extend this paradigm by incorporating local image-text alignment to capture fine-grained visual cues, yet these approaches often select local regions independently for each prompt, leading to redundant local feature usage and prompt overlap. We propose SOT-GLP, which introduces a shared sparse patch support and balanced optimal transport allocation to explicitly partition salient visual regions among class-specific local prompts while preserving global alignment. Our method learns shared global prompts and class-specific local prompts. The global branch maintains standard image-text matching for robust category-level alignment. The local branch constructs a class-conditioned sparse patch set using V-V attention and aligns it to multiple class-specific prompts via balanced entropic optimal transport, yielding a soft partition of patches that prevents prompt overlap and collapse. We evaluate our method on two complementary objectives: (i) few-shot classification accuracy on 11 standard benchmarks and (ii) out-of-distribution (OOD) detection. On the standard 11-dataset benchmark with 16-shot ViT-B/16, SOT-GLP achieves 85.1% average accuracy, outperforming prior prompt-learning methods. We identify a distinct accuracy-robustness trade-off in prompt learning: while learnable projections optimize in-distribution fit, they alter the foundational feature space. We demonstrate that a projection-free local alignment preserves the native geometry of the CLIP manifold, yielding state-of-the-art OOD detection performance (94.2% AUC) that surpasses fully adapted models. Implementation available at: https://github.com/Deniz2304988/SOT-GLP

---

## 论文详细总结（自动生成）

以下是对论文《Local-Global Prompt Learning via Sparse Optimal Transport》（基于稀疏最优传输的局部-全局提示学习）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（如 CLIP）在少样本适配时，通常只匹配全局图像嵌入，忽略了细粒度的局部视觉特征。
*   **研究背景**：现有的局部提示学习方法（如 GalLoP）通常为每个提示词独立选择局部区域，这导致多个提示词可能关注相同的冗余区域，缺乏对视觉证据的有效划分和专业化分工。
*   **动机**：提出一种能够显式划分显著视觉区域、并将其分配给不同局部提示词的机制，以捕捉更丰富的细粒度信息，同时保持全局语义的稳健性。

### 2. 方法论
SOT-GLP 采用双分支架构，结合了全局对齐和基于稀疏最优传输（OT）的局部对齐：
*   **双分支参数化**：
    *   **全局分支**：学习一组共享的全局提示词，与图像的 `[CLS]` 标记对齐，确保类别级的稳健性。
    *   **局部分支**：为每个类别学习多个特定的局部提示词，用于捕捉纹理、部件等细节。
*   **局部感知特征提取**：引入 **V-V（Value-Value）注意力** 流。不同于标准的 Q-K 注意力，V-V 注意力直接关联值表示，能增强补丁间的交互并抑制背景干扰。
*   **显著性引导的稀疏化**：计算补丁与局部提示词集的平均相似度，仅保留前 $K$ 个（Top-K）显著补丁，构建稀疏支持集，过滤背景噪声。
*   **平衡熵最优传输（Balanced OT）**：
    *   将筛选出的 $K$ 个补丁与 $N_l$ 个局部提示词进行匹配。
    *   通过 **Sinkhorn 迭代** 求解具有均匀边际约束的最优传输问题，强制每个提示词分配到等量的补丁质量。
    *   **核心作用**：防止“提示词坍缩”（即所有提示词都关注同一个最显著补丁），实现补丁在提示词间的软划分。
*   **训练与推理**：联合优化全局和局部交叉熵损失。推理时，综合两个分支的相似度得分进行分类。

### 3. 实验设计
*   **数据集**：在 11 个标准基准数据集上进行评估，涵盖通用对象（ImageNet, Caltech101）、细粒度分类（Aircraft, Cars, Pets, Flowers, Food）、纹理（DTD）、场景（SUN397, EuroSAT）和动作识别（UCF101）。
*   **任务场景**：
    1.  **少样本分类**：分别测试 1, 2, 4, 8, 16-shot 性能。
    2.  **分布外（OOD）检测**：以 ImageNet 为分布内（ID），iNaturalist、SUN、Places、Textures 为 OOD 数据集。
*   **对比方法**：包括 CLIP (Zero-shot)、CoOp、CoCoOp、MaPLe、PLOT、PromptSRC、LoCoOp 和 GalLoP 等主流提示学习方法。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA RTX 3090 GPU**。
*   **训练细节**：采用混合精度训练和数据并行执行；有效 Batch Size 为 32；使用 SGD 优化器，运行 50 个 Epoch（含 5 个 Epoch 预热）。
*   **推理开销**：由于引入了双流 V-V 注意力，推理成本增加了约 24%（从 44.9 GFLOPs 增至 58.8 GFLOPs），但 OT 匹配部分的计算开销极小。

### 5. 实验数量与充分性
*   **实验规模**：在 11 个数据集上进行了全方位的少样本实验，并针对 16-shot 设置报告了 3 次随机初始化的平均值。
*   **消融实验**：针对 V-V 注意力、局部投影层、类别特定提示词等核心组件进行了详细消融，验证了各部分的贡献。
*   **参数敏感性**：分析了损失权重 $\lambda$ 和稀疏补丁数量 $K$ 的影响。
*   **评价**：实验设计全面，涵盖了从分类准确率到鲁棒性（OOD）的多个维度，对比基准线清晰且具有代表性，实验结果客观公正。

### 6. 主要结论与发现
*   **性能领先**：SOT-GLP 在 16-shot 设置下达到 **85.1%** 的平均准确率，在 11 个数据集中的 9 个上取得了 SOTA 结果。
*   **准确率-鲁棒性权衡**：研究发现，虽然可学习的局部投影层能提升 0.9% 的分类准确率，但会破坏 CLIP 原始特征空间的几何结构。
*   **OOD 突破**：**无投影版本的 SOT-GLP** 在 OOD 检测上表现惊人（94.2% AUC），证明了保留预训练流形对识别分布外样本至关重要。
*   **可视化验证**：通过热力图证明了 OT 机制确实促使不同的局部提示词关注图像的不同部位（如鸟的头部、翅膀、尾巴）。

### 7. 优点与亮点
*   **机制创新**：首次将平衡最优传输引入局部提示学习，解决了提示词冗余和重叠的顽疾。
*   **特征增强**：巧妙利用 V-V 注意力提取更具判别力的局部特征，而非仅仅依赖原始 CLIP 的全局偏置特征。
*   **双重优势**：不仅在分类任务上表现优异，还为 OOD 检测提供了一个高性能的变体，具有很强的实用部署价值。

### 8. 不足与局限
*   **计算成本**：双流视觉编码器增加了推理时的计算负担（GFLOPs 提升 24%），在资源受限的实时场景中可能受限。
*   **特定领域限制**：在极度专业化的领域（如 EuroSAT 遥感图像或 Aircraft 飞机型号）提升有限，这可能是因为 CLIP 预训练特征在这些领域的补丁表示不够精确。
*   **融合策略**：目前采用简单的加权求和融合全局与局部得分，未来可能需要更复杂的门控机制来处理分支间的冲突。

（完）
