---
title: "Stochastic Nonconvex Bilevel Optimization: Improved Rates Without Rare-Visit Assumption"
title_zh: 随机非凸双层优化：无需“稀疏访问”假设的改进收敛率
authors: "Daniel Cortild, Mathias Staudigl, Juan Peypouquet, Coralia Cartis"
date: 2026-09-06
pdf: "https://arxiv.org/pdf/2609.06580v1"
tags: ["query:bopl"]
score: 10.0
evidence: 随机非凸双层优化的收敛率
tldr: 本文研究了随机非凸双层优化问题，针对现有动态障碍梯度下降（DBGD）算法在处理非凸目标时依赖难以验证的“稀疏访问”假设或计算成本过高的问题，提出了一种简单的分母正则化方法。该方法在无需该假设的前提下，实现了更优的收敛速率，并提供了任意时刻的参数调度方案，显著提升了算法的实用性和理论性能。
selection_source: fresh_fetch
motivation: 现有随机双层优化算法在处理非凸问题时，往往被迫在难以验证的“稀疏访问”假设与极高的计算成本之间进行权衡。
method: 通过对DBGD乘子进行简单的分母正则化处理，消除了对稀疏访问假设的依赖。
result: "在达到(ε, ε)-平稳性时，实现了O(ε^-2)的迭代复杂度，以及O(ε^-4)的上层和O(ε^-7)的下层随机梯度复杂度，优于现有无假设方法。"
conclusion: 该研究为随机非凸双层优化提供了一种更高效、更具鲁棒性的算法框架，并支持灵活的参数调度。
---

## 摘要
我们研究了具有平滑且可能非凸的上层和下层目标的随机简单双层优化问题。现有的动态障碍梯度下降（DBGD）的随机扩展，要么在难以验证的、依赖于轨迹的“稀疏访问”（rare-visit）假设下获得快速收敛，要么在去除该假设后付出了极高的预言机（oracle）成本。我们证明，对 DBGD 乘子进行简单的仅分母正则化处理，无需此类假设即可保持快速收敛率。具体而言，我们的方法在 $O(\varepsilon^{-2})$ 次迭代中实现了 $(\varepsilon, \varepsilon)$-平稳性，且仅需 $O(\varepsilon^{-4})$ 次上层和 $O(\varepsilon^{-7})$ 次下层随机梯度计算，这优于目前已知的最佳无假设复杂度。此外，我们还推导了任意时刻的参数调度方案。

## Abstract
We investigate stochastic simple bilevel optimization with smooth and possibly nonconvex upper- and lower-level objectives. Existing stochastic extensions of dynamic barrier gradient descent (DBGD) either obtain fast convergence under an unverifiable trajectory-dependent ``rare-visit'' assumption, or remove this assumption at a substantially higher oracle cost. We show that a simple denominator-only regularization of the DBGD multiplier eliminates the need for such an assumption while preserving fast convergence rates. Specifically, our method achieves $(\varepsilon, \varepsilon)$-stationarity in $O(\varepsilon^{-2})$ iterations using $O(\varepsilon^{-4})$ upper-level and $O(\varepsilon^{-7})$ lower-level stochastic gradients, which improves upon the best assumption-free complexities. We additionally derive anytime parameter schedules.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stochastic Nonconvex Bilevel Optimization: Improved Rates Without Rare-Visit Assumption》的结构化总结：

### 1. 核心问题与研究动机
在随机非凸双层优化问题中，下层目标函数通常是非凸的，这使得寻找全局最优解变得不可行。现有的动态障碍梯度下降（DBGD）算法在随机环境下存在两难困境：要么依赖一个难以预先验证的“稀疏访问”（rare-visit）假设来获得快速收敛，要么在去除该假设后导致预言机（oracle）调用成本大幅增加。本文旨在寻找一种无需该假设且能保持高效收敛率的算法，以解决实际应用（如超参数优化、机器遗忘等）中的计算瓶颈。

### 2. 方法论：SDBGD
*   **核心思想**：通过对 DBGD 乘子定义中的分母进行正则化处理，消除随机梯度在远离平稳点时可能出现的奇异性。
*   **关键技术**：
    *   **分母正则化**：将乘子定义中的分母从 $\|\nabla g_k\|^2$ 修改为 $\|\nabla g_k\|^2 + \rho_k$（其中 $\rho_k > 0$ 为正则化参数）。
    *   **乘子设计**：与现有方法（如 Ahmadi 等人）不同，作者仅对分母进行扰动，而不对分子进行相应的惩罚项修正，这种微小的定义差异带来了本质上的收敛性提升。
    *   **算法流程**：SDBGD 算法通过迭代更新 $x_{k+1} = x_k - \eta_k \hat{d}_k$，其中 $\hat{d}_k$ 是基于正则化乘子计算的随机方向。
    *   **参数调度**：提供了“视界依赖”（horizon-dependent）和“任意时刻”（anytime）两种参数调度方案，无需预知总迭代次数即可实现收敛。

### 3. 实验设计
*   **场景**：构建了一个包含非凸下层目标的玩具问题（Toy Problem），目标函数包含余弦和指数项，具有多个局部极小值。
*   **Benchmark 与对比方法**：对比了 SDBPG、PR-SDBPG 和 VR-PR-SDBPG（Ahmadi 等人提出的方法）。
*   **评估指标**：在相同的随机预言机预算（$10^8$ 次调用）下，对比上层和下层的平稳性度量指标。

### 4. 资源与算力
*   **硬件环境**：实验在配备 M5 芯片和 16GB 内存的 Apple Silicon MacBook Pro 上运行。
*   **软件环境**：Python 3.13。
*   **说明**：文中未提及具体的训练时长，但明确了算力资源为个人电脑，且代码已开源。

### 5. 实验数量与充分性
*   **实验规模**：进行了 10 次独立运行以获取统计意义上的均值。
*   **客观性**：实验设计较为公平，所有对比方法均在相同的预算下使用“任意时刻”调度方案，且使用了相同的初始点。
*   **充分性**：虽然仅展示了一个玩具问题，但对于验证理论收敛率的正确性而言是足够的。

### 6. 主要结论与发现
*   **理论突破**：SDBGD 在无需“稀疏访问”假设的前提下，实现了 $O(\varepsilon^{-2})$ 的迭代复杂度，以及 $O(\varepsilon^{-4})$ 的上层和 $O(\varepsilon^{-7})$ 的下层随机梯度复杂度，优于现有的无假设方法。
*   **乘子敏感性**：研究表明，双层优化中乘子定义的微小变化会对算法的理论收敛保证产生深远影响。

### 7. 优点
*   **理论严谨**：消除了难以验证的假设，使算法在实际应用中更具可信度。
*   **灵活性**：支持“任意时刻”参数调度，增加了算法在未知总迭代次数场景下的实用性。
*   **简洁性**：通过简单的分母正则化即实现了性能提升，易于实现。

### 8. 不足与局限
*   **实验单一**：仅在简单的玩具问题上进行了数值验证，缺乏在大规模机器学习任务（如深度神经网络超参数优化）中的实证表现。
*   **下层复杂度**：虽然优于现有方法，但 $O(\varepsilon^{-7})$ 的下层梯度复杂度在处理极大规模问题时仍可能面临挑战。
*   **假设限制**：依然保留了随机梯度方差有界的标准假设，未探讨更弱的方差假设条件。

（完）
