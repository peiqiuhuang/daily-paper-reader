---
title: "VaSST: Variational Inference for Symbolic Regression using Soft Symbolic Trees"
title_zh: VaSST：基于软符号树的符号回归变分推理
authors: "Somjit Roy, Pritam Dey, Bani K. Mallick"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23561v1"
tags: ["query:sr"]
score: 10.0
evidence: 符号回归的变分推理
tldr: 符号回归旨在从数据中恢复显式物理定律，但现有方法在处理噪声和不确定性量化方面存在局限。本文提出 VaSST，一种基于变分推理的可扩展概率框架。通过引入“软符号树”将离散的表达式搜索转化为连续的梯度优化问题，实现了高效的结构搜索和原则性的不确定性量化。在 Feynman 数据库等实验中，VaSST 在结构恢复和预测精度上均优于现有最先进方法。
selection_source: fresh_fetch
motivation: 现有的符号回归方法多依赖启发式搜索或缺乏不确定性量化，难以在复杂的组合空间中高效探索。
method: 提出 VaSST 框架，利用软符号树对表达式进行连续松弛，将组合搜索转化为基于梯度的变分推理优化。
result: 在模拟实验和 Feynman 符号回归数据库中，VaSST 在结构恢复准确性和预测性能上均超越了现有技术。
conclusion: VaSST 为符号回归提供了一种高效且具备概率解释性的新途径，有效解决了离散空间搜索与不确定性评估的难题。
---

## 摘要
符号回归近年来在人工智能驱动的科学发现中备受关注，旨在从数据中恢复显式的闭式表达式，从而揭示潜在的物理定律。尽管取得了近期进展，现有方法仍主要由启发式搜索算法或假设低噪声环境且缺乏原则性不确定性量化的数据密集型方法主导。完全概率化的表述非常稀缺，而现有的基于马尔可夫链蒙特卡罗（MCMC）的贝叶斯方法通常难以有效探索符号表达式高度多模态的组合空间。我们提出了 VaSST，这是一种基于变分推理的可扩展符号回归概率框架。VaSST 采用了符号表达式树的连续松弛，称为软符号树（soft symbolic trees），其中离散的算子和特征分配被允许组件上的软分布所取代。这种松弛将天文数字级符号空间上的组合搜索转化为高效的基于梯度的优化问题，同时保留了连贯的概率解释。学习到的软表示诱导出符号结构的后验分布，从而实现原则性的不确定性量化。在模拟实验和 SRBench 中的 Feynman 符号回归数据库上，与最先进的符号回归方法相比，VaSST 在结构恢复和预测精度方面均取得了优异的性能。

## Abstract
Symbolic regression has recently gained traction in AI-driven scientific discovery, aiming to recover explicit closed-form expressions from data that reveal underlying physical laws. Despite recent advances, existing methods remain dominated by heuristic search algorithms or data-intensive approaches that assume low-noise regimes and lack principled uncertainty quantification. Fully probabilistic formulations are scarce, and existing Markov chain Monte Carlo-based Bayesian methods often struggle to efficiently explore the highly multimodal combinatorial space of symbolic expressions. We introduce VaSST, a scalable probabilistic framework for symbolic regression based on variational inference. VaSST employs a continuous relaxation of symbolic expression trees, termed soft symbolic trees, where discrete operator and feature assignments are replaced by soft distributions over allowable components. This relaxation transforms the combinatorial search over an astronomically large symbolic space into an efficient gradient-based optimization problem while preserving a coherent probabilistic interpretation. The learned soft representations induce posterior distributions over symbolic structures, enabling principled uncertainty quantification. Across simulated experiments and Feynman Symbolic Regression Database within SRBench, VaSST achieves superior performance in both structural recovery and predictive accuracy compared to state-of-the-art symbolic regression methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《VaSST: Variational Inference for Symbolic Regression using Soft Symbolic Trees》的结构化深入总结：

### 1. 核心问题与整体含义
符号回归（Symbolic Regression, SR）的核心目标是从观测数据中直接发现简洁、可解释的数学表达式（如物理定律）。
*   **研究动机**：现有的 SR 方法存在三大痛点：
    1.  **搜索效率低**：遗传算法（GP）等启发式搜索在天文数字级的组合空间中极慢且易陷入局部最优。
    2.  **缺乏不确定性量化**：大多数方法只给出单一公式，无法评估该公式的可靠性。
    3.  **贝叶斯推断困难**：现有的贝叶斯方法（如基于 MCMC 的方法）在处理高度多模态的离散空间时收敛极慢，难以扩展到大数据集。
*   **核心价值**：VaSST 旨在通过变分推理（Variational Inference）将离散的公式搜索转化为连续的梯度优化问题，从而实现高效、可扩展且具备概率解释性的符号回归。

### 2. 方法论：软符号树与变分推理
VaSST 的核心思想是**“连续松弛”**，即将离散的树结构映射到连续空间。
*   **软符号树（Soft Symbolic Trees）**：
    *   传统的符号树节点是确定的算子（如 +）或特征（如 $x_1$）。
    *   VaSST 将每个节点表示为所有可能算子和特征的**加权组合（软分布）**。
    *   利用 **Binary Concrete 变换** 处理节点的扩展/剪枝决策，利用 **Gumbel-Softmax 变换** 处理算子和特征的选择。这使得整个树的评估过程变得可微。
*   **概率建模**：
    *   模型被表述为一个由 $K$ 棵符号树组成的线性集成模型。
    *   引入了**深度依赖的先验（Depth-dependent prior）**：随着树深度的增加，节点分裂的概率降低，这在数学上体现了“奥卡姆剃刀”原则，倾向于生成简洁的公式。
*   **算法流程**：
    1.  **初始化**：设置变分参数（控制节点分布的参数）。
    2.  **黑盒变分推理（BBVI）**：通过最大化证据下界（ELBO）来优化变分参数。
    3.  **温度退火**：在优化过程中逐渐降低松弛分布的“温度”，使软分布逐渐向离散的硬决策靠拢。
    4.  **后验采样**：优化完成后，从学习到的分布中采样出多个“硬”符号树，并根据拟合精度进行排序。

### 3. 实验设计
*   **数据集/场景**：
    1.  **模拟实验**：包括多项式（$x_0^2 - x_1 + 0.5x_2^2$）和三角函数（$6\sin(x_0)\cos(x_1)$）的恢复。
    2.  **Feynman 数据库（FSReD）**：从经典的《费曼物理学讲义》中提取的真实物理公式，如库仑定律、引力势能、洛伦兹力、傅里叶热传导定律。
*   **Benchmark**：使用符号回归权威基准测试集 **SRBench**。
*   **对比方法**：
    *   **机器学习类**：QLattice。
    *   **遗传算法类**：gplearn, DEAP。
    *   **贝叶斯类**：BMS (Bayesian Machine Scientist), BSR (Bayesian Symbolic Regression)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 RTX 4090）或具体的训练总时长。
*   **效率对比**：文中通过实验展示了 VaSST 在不同样本量（$n=2000$ 到 $5000$）下的运行时间。结果表明，VaSST 的运行速度显著快于同为贝叶斯框架的 BMS 和 BSR，证明了变分推理在计算效率上的优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个公式都设置了**三种噪声水平**（无噪声、$\sigma^2=0.12, 0.22$），以测试鲁棒性。
    *   每个实验重复了 **10 次**以获取均值和标准差。
    *   在后验采样阶段生成了 **2000 个**候选公式进行评估。
*   **充分性评价**：实验设计较为充分。它不仅对比了预测精度（RMSE），还重点评估了**结构恢复（Structural Recovery）**的准确性（即是否找回了正确的数学形式）。对比方法涵盖了当前主流的各类技术路径，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **结构恢复能力强**：VaSST 在面对复杂公式（如带有交叉项和非线性函数的公式）时，比遗传算法和神经网络方法更容易找回真实的物理结构。
*   **噪声鲁棒性**：在加入高斯噪声的情况下，VaSST 依然能保持稳定的结构发现能力，而其他方法（如 QLattice）往往会生成极其复杂的过拟合公式。
*   **不确定性量化**：VaSST 能够输出前 N 个最可能的候选公式，为科学家提供了模型选择的概率依据。
*   **扩展性**：相比于传统的贝叶斯 MCMC 方法，VaSST 能够更高效地处理大规模数据集。

### 7. 优点（亮点）
*   **巧妙的松弛技术**：通过软符号树将不可导的树搜索问题转化为可导的参数优化问题，这是对符号回归领域的重要贡献。
*   **原则性的简洁性控制**：通过深度依赖先验自动平衡公式的复杂度和准确度，避免了传统方法中常见的“公式膨胀（Bloat）”问题。
*   **概率解释性**：不仅给出一个公式，还给出了公式空间的分布，符合科学发现对严谨性的要求。

### 8. 不足与局限
*   **最大深度限制**：VaSST 需要预设一个最大树深度 $D$，如果真实公式的嵌套层级超过此深度，模型将无法捕捉。
*   **超参数敏感性**：变分推理的效果往往依赖于温度退火策略和学习率的设置，这可能需要针对不同问题进行调优。
*   **算子库依赖**：虽然可以自定义算子，但如果预设的算子库（Operator Set）不包含真实公式所需的算子，模型无法自发创造新算子。
*   **计算开销**：虽然比 MCMC 快，但相比于简单的线性回归或预训练的 Transformer 模型，其优化过程仍涉及大量的自动微分计算。

（完）
