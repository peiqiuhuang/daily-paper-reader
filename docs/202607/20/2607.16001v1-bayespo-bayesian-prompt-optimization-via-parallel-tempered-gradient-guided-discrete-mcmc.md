---
title: "BayesPO: Bayesian Prompt Optimization via Parallel-Tempered Gradient-Guided Discrete MCMC"
title_zh: BayesPO：通过并行回火梯度引导离散 MCMC 实现的贝叶斯提示优化
authors: "Junjie Zhou, Zhijian Ou"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16001v1"
tags: ["query:bopl"]
score: 9.0
evidence: 通过梯度引导的离散MCMC进行贝叶斯提示词优化
tldr: 本研究提出BayesPO框架，将提示词优化视为离散Token上的贝叶斯后验采样问题。通过结合任务似然项与语言模型先验，构建基于能量的后验分布，并利用并行回火和梯度引导的离散MCMC进行全局搜索。实验表明，该方法能有效发现语义合理的提示词，在指令诱导任务中显著提升了准确率，为概率化提示词优化提供了严谨的理论框架和实用工具。
selection_source: fresh_fetch
motivation: 现有的自动提示词优化多依赖启发式搜索，缺乏严谨的概率框架来平衡任务表现与指令流畅度。
method: 提出BayesPO框架，利用梯度引导的Gibbs-with-Langevin采样和并行回火技术，在离散Token空间内进行高效的后验分布采样。
result: "在Qwen2.5模型上的实验显示，该方法在24项指令诱导任务中将平均准确率从60.04%提升至63.23%，并能有效跳出局部最优。"
conclusion: 贝叶斯提示词采样是一种原则性的后优化工具，尽管存在计算开销大和易过拟合的局限，但为概率化提示词优化指明了重要方向。
---

## 摘要
提示优化在不更新模型参数的情况下适配大语言模型（LLMs），但许多自动提示优化器仍是对候选指令的启发式搜索过程。本文将提示优化视为离散提示标记（tokens）上的贝叶斯后验采样。我们通过结合任务似然项（奖励能解释输入输出示例的提示）和语言模型先验（偏好流畅的指令）来定义后验分布。这把提示优化转化为一个基于能量的后验采样问题，其中梯度可用于引导词表标记上的离散马尔可夫链蒙特卡洛（MCMC）建议。我们将该框架称为 BayesPO，即贝叶斯提示优化的缩写。在本文中，BayesPO 使用马尔可夫链蒙特卡洛进行实例化：它采用经过 Metropolis-Hastings 修正的 Gibbs-with-Langevin (GwL) 建议，并集成了并行回火，以便在 LLM 诱导的崎岖能量景观中进行全局探索。具体的采样器进一步使 GwL 采样器适配非权重共享 LLM 嵌入的实际约束。使用 Qwen2.5 模型的实验表明，该采样器在诊断任务中发现了具有语义意义的提示，并行回火有助于在诗歌补全任务中跳出局部最优解，并且在 24 个指令归纳子任务上对 APE 提示进行后优化，将平均准确率从 60.04% 提高到 63.23%。研究还揭示了两个主要局限性：能量最小化可能会在小型优化集上过拟合，且当前的采样器计算成本仍然较高。这些发现将贝叶斯提示采样定位为一种有原则的后优化工具，并为概率提示优化指明了一个有前景的方向。

## Abstract
Prompt optimization adapts large language models (LLMs) without updating model parameters, but many automatic prompt optimizers remain heuristic search procedures over candidate instructions. This paper studies prompt optimization as Bayesian posterior sampling over discrete prompt tokens. We define a posterior distribution by combining a task likelihood term, which rewards prompts that explain input-output examples, with a language-model prior, which favors fluent instructions. This converts prompt optimization into an energy-based posterior sampling problem, for which gradients can be used to guide discrete Markov chain Monte Carlo (MCMC) proposals over vocabulary tokens. We refer to our framework as BayesPO, short for Bayesian Prompt Optimization. In this paper, BayesPO is instantiated with Markov chain Monte Carlo: it uses a Metropolis-Hastings corrected Gibbs-with-Langevin (GwL) proposal and integrates parallel tempering for global exploration of rugged LLM-induced energy landscapes. The concrete sampler further adapts the GwL sampler to the practical constraints of non-weight-tied LLM embeddings. Experiments with Qwen2.5 models show that the sampler discovers semantically meaningful prompts on diagnostic tasks, that parallel tempering helps escape a local optimum in a poetry completion task, and that post-optimizing APE prompts on 24 instruction-induction subtasks improves average accuracy from 60.04% to 63.23%. The study also reveals two main limitations: energy minimization may overfit small optimization sets, and the current sampler remains computationally expensive. These findings position Bayesian prompt sampling as a principled post-optimization tool and point to a promising direction for probabilistic prompt optimization.

---

## 论文详细总结（自动生成）

这篇论文提出了 **BayesPO**，一个将大语言模型（LLM）提示词（Prompt）优化建模为离散 Token 空间上的贝叶斯后验采样框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自动提示词优化方法（如 APE、AutoPrompt 等）大多属于启发式搜索、局部贪婪编辑或依赖 LLM 生成候选方案，缺乏严谨的概率框架，且难以在复杂的能量景观中进行全局探索。
*   **研究动机**：作者希望将提示词优化转化为一个原则性的**贝叶斯后验采样问题**。通过定义一个结合“任务似然”（提示词对示例的解释力）和“语言模型先验”（提示词的流畅度）的后验分布，利用梯度信息引导离散空间的马尔可夫链蒙特卡洛（MCMC）采样，从而找到既符合任务需求又自然流畅的提示词。

### 2. 方法论：BayesPO 框架
*   **核心思想**：将提示词视为离散随机变量，构建基于能量的模型（EBM）。
*   **能量函数定义**：
    *   $U(\rho) = -\log P(\rho) - \sum_{i=1}^K \log P(y_i | \rho, x_i)$
    *   其中第一项是提示词的先验（保证流畅性），第二项是任务似然（保证任务准确性）。
*   **关键技术细节**：
    *   **梯度引导的离散建议（GwL）**：计算能量函数相对于提示词嵌入（Embedding）的梯度，利用梯度方向在词表中寻找能降低能量的 Token 替换方案。
    *   **MH 修正**：使用 Metropolis-Hastings 算法对建议进行接受/拒绝判定，确保采样收敛至目标后验分布。
    *   **并行回火（Parallel Tempering, PT）**：同时运行多个不同温度的马尔可夫链。高温度链负责在大范围内跨越能量障碍进行探索，低温度链负责在局部精细优化，通过链间的状态交换实现全局搜索。
    *   **非权重共享适配**：针对 Qwen2.5 等输入输出嵌入不共享的模型，专门推导了结合输入侧和输出侧梯度的建议分布公式。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **诊断性任务**：文言文翻译、反义词生成（验证采样器能否从随机初始化中发现语义）。
    2.  **诗歌补全任务**：补全李白的《静夜思》（验证并行回火跳出局部最优解的能力）。
    3.  **APE 指令诱导基准**：包含 24 个子任务（如助动词变换、同义词、情感分析等）。
*   **对比基准（Benchmark）**：主要对比 APE（Automatic Prompt Engineering）生成的提示词。
*   **模型使用**：使用了 Qwen2.5-0.5B-Instruct（诊断任务）和 Qwen2.5-7B-Instruct（核心实验）。

### 4. 资源与算力
*   **硬件环境**：使用了配备 **NVIDIA RTX 3090 GPU** 的多 GPU 服务器。
*   **实现细节**：基于 PyTorch 混合精度训练，推理部分使用了 vLLM 加速。
*   **算力开销**：论文明确指出该方法计算成本较高。由于涉及多次反向传播和多链并行，其运行速度比 APE 慢约一个数量级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 APE 基准上测试了全部 24 个子任务，每个任务使用 6 个训练示例。
    *   针对并行回火（PT）进行了消融对比（单链 vs 多链）。
    *   提供了详细的能量轨迹图和优化前后的提示词对比。
*   **充分性评价**：实验设计较为全面，涵盖了从简单语义发现到复杂指令优化的多个维度。通过 24 个子任务的平均表现来评估，具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：在 APE 基准上，BayesPO 将平均测试准确率从 **60.04% 提升至 63.23%**。在 APE 初始提示词语义漂移的任务中（如“第二个字母”提取），提升尤为显著。
*   **全局探索有效性**：并行回火（PT）被证明能有效帮助模型跳出局部最优（如在诗歌任务中纠正错误的补全）。
*   **能量-准确率失配**：研究发现，在极小数据集上，训练能量的降低并不总是导致测试准确率的提高，存在**过拟合**风险。

### 7. 优点
*   **理论严谨**：为提示词优化提供了坚实的贝叶斯概率基础，而非单纯的启发式搜索。
*   **离散空间优化**：成功将连续梯度信息应用于离散 Token 的采样，且通过 MH 修正保证了采样的有效性。
*   **强大的探索能力**：引入并行回火解决了 LLM 能量景观崎岖、易陷入局部解的问题。

### 8. 不足与局限
*   **计算开销大**：由于需要频繁计算梯度和进行多链采样，实时性较差，难以在大规模实时场景下应用。
*   **过拟合风险**：在优化集非常小时，模型倾向于寻找能完美解释这几个例子但泛化性差的提示词。
*   **固定长度限制**：目前的采样器假设提示词长度固定，无法自动进行 Token 的增加或删除（即不支持跨维度采样）。

（完）
