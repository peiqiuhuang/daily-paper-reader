---
title: Bi-Level Optimization for Single Domain Generalization
title_zh: 单域泛化的双层优化
authors: "Marzi Heidari, Hanping Zhang, Hao Yan, Yuhong Guo"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06349v1"
tags: ["query:bopl"]
score: 9.0
evidence: 用于领域提示编码器的双层优化
tldr: 针对单域泛化（SDG）中仅有单一源域数据的挑战，本文提出BiSDG双层优化框架。该方法通过标签保持转换构建代理域来模拟分布偏移，并利用领域提示编码器生成调制信号以增强特征。通过双层优化机制，内层优化任务性能，外层提升跨域泛化能力，并采用高效梯度近似方案。实验证明BiSDG在多个基准上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 旨在解决在仅有单一标记源域且无法获取目标域数据的情况下，如何提高模型对未知领域泛化能力的问题。
method: 提出一种双层优化框架，通过领域提示编码器生成调制信号模拟分布偏移，并结合高效的梯度近似算法进行训练。
result: 在多个单域泛化基准测试中，BiSDG 始终优于现有方法，刷新了该领域的最佳性能记录。
conclusion: BiSDG 通过显式解耦任务学习与领域建模，为单域泛化提供了一种高效且性能卓越的优化方案。
---

## 摘要
在训练期间无法访问任何目标数据的情况下，从单个有标签的源域泛化到未见过的目标域，仍然是鲁棒机器学习中的一个根本挑战。针对这一尚未得到充分探索的设定（即单域泛化，SDG），我们提出了 BiSDG，这是一个双层优化框架，显式地将任务学习与领域建模解耦。BiSDG 通过对源数据进行保持标签的变换来构建代理域，从而模拟分布偏移。为了捕捉特定领域的上下文，我们提出了一个领域提示编码器，该编码器生成轻量级调制信号，通过特征线性调制（feature-wise linear modulation）产生增强特征。学习过程被表述为一个双层优化问题：内层目标在固定提示下优化任务性能，而外层目标通过更新领域提示编码器来最大化在代理域上的泛化能力。我们进一步开发了一种实用的梯度近似方案，能够在无需二阶导数的情况下实现高效的双层训练。在各种 SDG 基准测试上的广泛实验表明，BiSDG 一贯优于现有方法，在 SDG 设定下达到了新的最先进性能。

## Abstract
Generalizing from a single labeled source domain to unseen target domains, without access to any target data during training, remains a fundamental challenge in robust machine learning. We address this underexplored setting, known as Single Domain Generalization (SDG), by proposing BiSDG, a bi-level optimization framework that explicitly decouples task learning from domain modeling. BiSDG simulates distribution shifts through surrogate domains constructed via label-preserving transformations of the source data. To capture domain-specific context, we propose a domain prompt encoder that generates lightweight modulation signals to produce augmenting features via feature-wise linear modulation. The learning process is formulated as a bi-level optimization problem: the inner objective optimizes task performance under fixed prompts, while the outer objective maximizes generalization across the surrogate domains by updating the domain prompt encoder. We further develop a practical gradient approximation scheme that enables efficient bi-level training without second-order derivatives. Extensive experiments on various SGD benchmarks demonstrate that BiSDG consistently outperforms prior methods, setting new state-of-the-art performance in the SDG setting.

---

## 论文详细总结（自动生成）

以下是对论文《Bi-Level Optimization for Single Domain Generalization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：单域泛化（Single Domain Generalization, SDG）。即如何在仅有一个标记源域且训练期间完全无法访问目标域数据的情况下，提升模型对未知目标域的泛化能力。
*   **研究背景与动机**：
    *   传统的领域泛化（DG）依赖多个源域来学习不变性，而 SDG 场景更符合数据稀缺的现实。
    *   现有 SDG 方法多采用数据增强或对抗训练，但存在“彩票困境”：模型仅在与增强数据相似的目标域上表现良好，缺乏自适应能力。
    *   **研究目标**：提出一种“领域感知”的方法，显式地将任务学习与领域建模解耦，使模型能够根据领域上下文进行自适应。

### 2. 方法论
BiSDG 框架的核心思想是通过双层优化（Bi-Level Optimization）来协调任务模型和领域提示编码器的学习。
*   **代理域合成（Surrogate-Domain Synthesis）**：通过 $K$ 组语义一致的图像变换（如色彩偏移、几何畸变、光度退化等）构建代理域，模拟潜在的分布偏移。
*   **领域提示编码器（Domain Prompt Encoder）**：
    *   使用 **Set Transformer** 作为编码器，从输入批次中提取领域特征。
    *   生成轻量级的调制信号（缩放因子 $\gamma$ 和偏移因子 $\beta$）。
    *   利用 **FiLM（特征线性调制）** 技术对特征图进行标准化和调制，实现领域感知的特征增强。
*   **双层优化机制**：
    *   **内层优化（Inner Objective）**：在固定领域提示的情况下，利用源域数据优化预测模型（特征提取器+分类器）。损失函数结合了交叉熵损失和对抗一致性损失（$L_{adv}$），以增强局部平滑性。
    *   **外层优化（Outer Objective）**：更新领域提示编码器的参数，目标是最大化模型在所有合成代理域上的泛化性能（交叉熵损失）。
*   **梯度近似方案**：为了避免计算昂贵的高阶导数（Hessian 矩阵），提出了一种基于**有限差分近似**的梯度估计方法，使得双层训练在计算上更加高效。

### 3. 实验设计
*   **数据集与场景**：
    *   **Digits**：以 MNIST 为源域，测试 SVHN、MNIST-M、SYN、USPS。
    *   **PACS**：以 Photo 为源域，测试 Art、Cartoon、Sketch。
    *   **DomainNet**：以 Real 为源域，测试其余 5 个极具挑战性的多样化领域。
*   **基准方法（Benchmarks）**：对比了包括传统增强（MixUp, CutOut, RandAugment）、对抗增强（ADA, ME-ADA, AdvST）以及生成式/元学习方法（PDEN, L2D, MCL）在内的十余种 SOTA 方法。

### 4. 资源与算力
*   **硬件设备**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练配置**：
    *   Digits 使用 LeNet，训练 50 轮。
    *   PACS 和 DomainNet 使用预训练的 ResNet-18，分别训练 50 轮和 200 轮。
    *   批次大小（Batch Size）在 32 到 128 之间。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准数据集上进行了详尽测试，涵盖了从简单数字到复杂物体识别的多种场景。
*   **消融实验**：针对对抗损失（$L_{adv}$）和特征标准化步骤进行了消融，验证了各组件的必要性。
*   **敏感性分析**：对代理域数量 $K$、损失权重 $\lambda$、梯度近似步长 $\epsilon_\Theta$ 和变换数量 $m$ 进行了参数敏感性分析。
*   **客观性**：所有实验均重复 5 次并报告均值与标准差，对比方法涵盖了近年来的顶级会议成果，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：BiSDG 在所有三个基准测试中均刷新了 SOTA 记录。在 PACS 上平均精度达到 65.3%，在 Digits 上达到 82.7%，在最具挑战性的 DomainNet 上达到 28.3%。
*   **解耦的有效性**：通过双层优化将任务特征学习与领域调制信号生成分开，能有效防止模型坍缩为简单的增强模型。
*   **鲁棒性**：实验证明，即使在极大的领域偏移下（如 DomainNet），BiSDG 依然能通过学习到的领域提示捕捉到关键的泛化特征。

### 7. 优点
*   **架构创新**：引入双层优化框架解决 SDG 问题，提供了一种比单纯数据增强更具理论支撑的优化路径。
*   **高效性**：通过一阶梯度近似解决了双层优化计算量大的痛点，使其具有实际应用价值。
*   **领域感知**：FiLM 调制与领域提示编码器的结合，赋予了模型在推理时根据输入数据分布进行微调的能力。

### 8. 不足与局限
*   **代理域依赖**：方法的成功在很大程度上取决于预定义的变换算子。如果目标域的偏移类型完全不在代理域模拟的范围内，性能提升可能会受限。
*   **算力细节缺失**：未提供具体的训练耗时和硬件需求，难以评估其相对于单层优化方法的额外计算开销。
*   **超参数敏感度**：敏感性分析显示，损失权重 $\lambda$ 和梯度步长 $\epsilon_\Theta$ 对性能有一定影响，可能需要针对不同数据集进行精细调优。

（完）
