---
title: Recursive Harness Self-Improvement
title_zh: 递归式框架自我提升
authors: "Hyunin Lee, Jinglue Xu, Jeffrey Seely, Donghyun Lee, Matei Zaharia, Yujin Tang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15524v1"
tags: ["query:bopl"]
score: 8.0
evidence: 将用户构建的测试框架优化为提示级规范
tldr: "本研究探讨了模型与脚手架（harness）的协同进化，提出“递归脚手架自我改进”（RHI）方法。RHI将脚手架视为提示词级别的代理循环规范，利用修订历史的成对反馈进行迭代优化。在30个机器学习研究任务中，RHI仅需少量迭代即可显著提升低推理成本代理的性能，甚至超越高推理基准，同时降低60%的推理成本。该方法通过优化任务特定的上下文管理而非延长推理链条，为模型-脚手架协同进化范式下的持续学习提供了实用路径。"
selection_source: fresh_fetch
motivation: 针对特定任务手动更新和优化复杂的模型脚手架既昂贵又费力，亟需一种轻量级且自动化的优化方案。
method: 引入递归脚手架自我改进（RHI），通过对脚手架提示词规范进行迭代修订，并利用成对反馈机制进行自我优化。
result: "RHI在多项任务中使低推理成本代理的表现超过了高推理基准，并成功削减了高达60%的推理开销。"
conclusion: RHI证明了通过优化任务特定的信息流和上下文管理，可以在不增加推理负担的情况下显著提升代理性能。
---

## 摘要
在模型-框架协同演化的背景下，框架不仅是推理时的脚手架，更是数据生成组件，其执行轨迹可以塑造未来的基础模型。这催生了“框架在环学习”：同时针对即时智能体性能和用于未来模型训练的轨迹质量来优化框架。然而，持续更新由提供商构建的脚手架成本高昂且耗费人力。因此，我们研究了以任务特定的方式优化用户构建的框架，是否能在保持计算轻量且仅需少量更新迭代的情况下，提高执行轨迹的质量。为此，我们引入了递归式框架自我提升（RHI），它将框架表示为智能体循环的提示级规范，并利用其自身修订历史的成对反馈进行迭代优化。在涵盖量化金融、机器人技术和制药等领域的 30 个合成机器学习研究任务中，仅需几次 RHI 迭代就足以显著提高低推理努力智能体的性能上限，超过了相应的最大推理努力设置，同时将推理成本降低了高达 60%。我们表明，这些收益主要源于通过更有效的智能体间信息流改进了任务特定的上下文管理，而非更长的推理轨迹。最后，我们将这种行为形式化为 RHI 隐式优化目标的信息论假设，表明 RHI 是模型-框架协同演化范式下进行持续学习的一种实用算法。

## Abstract
Under model--harness co-evolution, harnesses are not merely inference-time scaffolds but data-generating components whose execution traces can shape future foundation models. This motivates harness-in-the-loop learning: optimizing harnesses for both immediate agent performance and the quality of traces used for future model training. However, continually updating provider-built scaffolds is costly and labor-intensive. We therefore investigate whether optimizing user-constructed harnesses in a task-specific manner can improve execution-trace quality while remaining computationally lightweight and requiring only a few update iterations. To this end, we introduce Recursive Harness Self-Improvement (RHI), which represents the harness as a prompt-level specification of the agent loop and iteratively refines it using pairwise feedback over its own revision history. Across 30 synthetic machine-learning research tasks spanning quantitative finance, robotics, and pharmacy, a few RHI iterations suffice to substantially raise the performance ceiling of low-reasoning-effort agents, exceeding the corresponding maximum-reasoning-effort setting while reducing inference cost by up to 60%. We show that these gains arise primarily from improved task-specific context management through more effective inter-agent information flow rather than longer reasoning traces. Finally, we formalize this behavior as an information-theoretic hypothesis for RHI's implicit optimization objective, suggesting RHI as a practical algorithm for continual learning within the paradigm of model--harness co-evolution.

---

## 论文详细总结（自动生成）

这篇论文由 Sakana AI 和加州大学伯克利分校的研究团队合作完成，探讨了在“模型-框架协同演化”（Model-Harness Co-evolution）范式下，如何通过递归自我改进来优化智能体（Agent）的执行框架。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代 AI 系统的进步不仅取决于基础模型，还取决于围绕模型的“框架”（Harness，即提示词、工作流、工具调用逻辑等）。高质量的框架能生成高质量的执行轨迹，而这些轨迹又是训练下一代模型的重要数据。
*   **研究动机**：现有的框架优化方法（如 Meta-Harness）通常依赖大规模的人工工程或昂贵的群体搜索（Population-based search），难以在用户端针对特定任务进行轻量化、快速的迭代。
*   **整体含义**：论文旨在探索一种**轻量级、少样本迭代**的方法，通过优化用户构建的特定任务框架，在不增加模型推理负担的前提下，提升智能体的性能上限并降低成本。

### 2. 方法论：递归式框架自我提升（RHI）
*   **核心思想**：将框架（Harness）定义为提示词级别的智能体循环规范，通过比较当前版本与前一版本的输出质量（成对反馈），递归地修订框架。
*   **关键技术细节**：
    *   **框架表示**：将框架拆解为**角色（Roles）**、**指令（Instructions）**、**契约（Contracts，智能体间的信息交换规范）**和**步骤（Hops，工作流结构）**。
    *   **轨迹局部优化**：不同于搜索多个候选方案，RHI 仅将当前输出与上一次迭代的输出进行对比。
    *   **反馈循环**：使用 LLM 作为裁判（LLM-as-a-judge）生成偏好反馈，框架优化器（Lharness）根据反馈历史修订提示词规范。
*   **算法流程**：
    1. 初始化框架 $H^{(0)}$。
    2. 智能体执行任务并生成输出。
    3. 裁判比较当前输出与前一版本，生成偏好反馈并存入历史。
    4. 优化器根据历史反馈更新框架 $H^{(i+1)}$。
    5. 重复上述过程，直至性能达标或达到迭代上限。

### 3. 实验设计
*   **场景与数据集**：构建了 30 个合成的、开放式机器学习研究任务，涵盖**量化金融**（如 Wikipedia 页面浏览量预测股价）、**机器人**（如视觉语言模型微调）和**制药**（如蛋白质序列-结构多模态预训练）三个领域。
*   **Benchmark**：采用成对胜率（Pairwise Win Rate）作为主要指标，由高级 LLM（如 GPT-5.5, Opus 4.8 等）担任裁判。
*   **对比方法**：
    *   **测试时缩放（Test-time Scaling）基准**：对比了不同推理努力程度（high, xhigh, max, ultracode）的原始模型。
    *   **内置框架**：对比了提供商内置的多智能体工作流（如 Claude 的 ultracode 模式）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **原因**：RHI 属于**提示词级别的优化**，不涉及模型参数的微调（Fine-tuning），其主要开销在于 LLM API 的调用（推理成本）。实验中使用了 Claude 系列模型（Sonnet 4.6, Opus 4.7/4.8）和 GPT-5.5 进行推理和评估。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个领域、30 个复杂任务上进行了测试，每个任务运行 3 个随机种子，并使用多种 LLM 裁判进行交叉验证。
*   **消融实验**：
    *   验证了 RHI 是否优于单纯增加推理步数（测试时缩放）。
    *   分析了框架中哪些组件（契约 vs 步骤）对性能贡献最大。
    *   探讨了 RHI 是否能弥补模型本身能力的不足（训练时缩放 vs RHI）。
*   **充分性评价**：实验设计较为全面，通过信息论分析（互信息、总相关性）深入探讨了 RHI 的内在机制，客观地展示了性能提升与成本降低的权衡。

### 6. 主要结论与发现
*   **突破性能天花板**：仅需 2 次迭代，低推理努力的智能体（high）配合 RHI 优化的框架，性能即可超过最大推理努力（max/ultracode）的基准。
*   **成本大幅降低**：在 Opus 4.8 上，RHI 在提升性能的同时，将推理成本降低了高达 **60%**。
*   **机制发现**：性能提升并非源于生成了更长的推理链条，而是源于**更高效的上下文管理**。RHI 优化了智能体间的通信契约，减少了冗余信息流。
*   **信息论验证**：RHI 隐式地增加了框架组件与任务之间的互信息，同时降低了组件间的冗余度（Functional Specialization）。

### 7. 优点
*   **轻量且实用**：不需要微调模型，仅通过提示词迭代即可实现显著提升，适合实际生产环境。
*   **效率极高**：在极少的迭代次数（1-4 次）内即可收敛，搜索开销远低于传统的群体进化算法。
*   **深度洞察**：不仅展示了结果，还通过信息论假设解释了 LLM 如何通过优化“契约”来提升协作效率。

### 8. 不足与局限
*   **合成任务偏差**：实验基于合成的 ML 研究任务，在真实世界复杂、模糊任务中的泛化能力尚待验证。
*   **裁判偏见风险**：高度依赖 LLM-as-a-judge，可能存在自我偏好或对特定格式的偏见。
*   **应用限制**：目前仅限于提示词级别的框架优化，尚未涉及代码级脚手架（如工具 API 的自动重写）的深度优化。
*   **模型依赖**：RHI 的效果高度依赖于底层模型遵循复杂指令和自我反思的能力。

（完）
