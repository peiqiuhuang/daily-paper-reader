---
title: Regularized extragradient method for structured bilevel optimization in continuous and discrete time
title_zh: 连续与离散时间下结构化双层优化的正则化外梯度法
authors: "Radu Ioan Bot, Enis Chenchene, David Alexander Hulett"
date: 2026-08-29
pdf: "https://arxiv.org/pdf/2608.29181v2"
tags: ["query:bopl"]
score: 9.0
evidence: 结构化双层优化的正则化额外梯度方法
tldr: 本文研究了实Hilbert空间中约束在极大单调算子零点集上的结构化双层优化问题。针对光滑与非光滑设置，分别提出了基于动态正则化的连续一阶动力系统与离散正则化近端-外梯度算法。在适当的几何条件下，建立了内外层残差的末代收敛速率及迭代序列向最优解的弱收敛性，为结构化双层优化提供了高效的求解理论。
selection_source: fresh_fetch
motivation: 旨在解决约束在极大单调算子零点集上的结构化双层优化问题，填补缺乏末代收敛速率与理论保障的空白。
method: 针对光滑与非光滑场景，分别设计了基于动态正则化算子的连续一阶动力系统和离散正则化近端-外梯度算法。
result: 在特定几何条件下，成功建立了内外层残差的末代（last-iterate）收敛速率，并证明了轨迹与迭代序列对最优解的弱收敛性。
conclusion: 该研究为连续和离散时间框架下的结构化双层单调优化问题提供了系统的正则化算法设计与完整的收敛性理论支撑。
---

## 摘要
在实希尔伯特空间中，我们研究了一个双层优化问题，该问题旨在极大单调算子的零点集上最小化一个外层凸函数。在光滑设定下（其中外层目标函数为凸且 Fréchet 可微，内层算子为单值、连续且单调），我们将该问题与一个一阶动力系统关联起来，该系统可视为应用于动态正则化算子的单调流。在内层问题的适当几何条件（弱 Attouch-Czarnecki 型可积性条件或更强的尖锐性假设）下，我们建立了外层和内层残差的末代迭代收敛速率，以及轨迹到双层优化问题最优解的弱收敛性。在光滑+非光滑设定下，我们使用真凸且下半连续的函数扩展了外层目标，同时通过具有相同性质函数的次微分增强了内层算子。我们提出了一种正则化近端外梯度算法，其中前向和后向步骤分别针对动态正则化的算子和函数进行。在与光滑设定类似的内层问题几何假设下，我们建立了外层和内层残差的末代迭代收敛速率，以及迭代序列到双层优化问题最优解的弱收敛性。

## Abstract
In a real Hilbert space, we study a bilevel optimization problem that consists in minimizing an outer convex function over the zero set of a maximally monotone operator. In the smooth setting, where the outer objective is convex and Fréchet differentiable and the inner operator is single-valued, continuous and monotone, we associate with the problem a first-order dynamical system that can be viewed as a monotone flow applied to a dynamically regularized operator. Under suitable geometric conditions on the inner problem --- either a weak Attouch-Czarnecki-type integrability condition or the stronger assumption of sharpness --- we establish last-iterate convergence rates for both the outer and inner residuals, together with weak convergence of the trajectories to optimal solutions of the bilevel problem. In the smooth+nonsmooth setting, we enrich the outer objective with a proper, convex, and lower semicontinuous function, while the inner operator is augmented by the subdifferential of a function with the same properties. We propose a regularized proximal-extragradient algorithm in which both the forward and backward steps are performed with respect to dynamically regularized operators and functions, respectively. Under geometric assumptions on the inner problem analogous to those in the smooth setting, we establish last-iterate convergence rates for both the outer and inner residuals, together with weak convergence of the iterates to optimal solutions of the bilevel problem.

---

## 论文详细总结（自动生成）

以下是对该论文的结构化、深入且客观的中文总结：

---

### 1. 核心问题与研究动机（背景）

- **研究背景**：双层优化（Bilevel Optimization）广泛应用于均衡选择、逆问题和层次控制等领域。传统的双层优化方法大多要求内层问题为凸函数的极小化（势函数/梯度流 setting）。然而，当内层约束表现为一般的单调算子（如变分不等式或单调包含问题）时，传统基于函数值的度量不再适用。
- **核心问题**：论文研究实希尔伯特空间中外层目标为凸函数、内层约束为极大单调算子零点集的结构化双层优化问题。旨在为连续时间动力系统与离散近端-外梯度算法建立动态 Tikhonov 正则化机制，并证明内/外层残差的末代（last-iterate）收敛速率及轨迹/迭代序列对最优解的弱收敛性。

---

### 2. 方法论：核心思想与技术细节

- **核心思想**：通过引入随时间 $t$ 或迭代步 $k$ 缓慢衰减的动态正则化参数 $\varepsilon(t) \propto t^{-\delta}$ 或 $\varepsilon_k \propto k^{-\delta}$（其中 $\delta > 0$），将外层函数的梯度/次微分动态融合到内层单调算子中。这种机制在渐进逼近内层可行域的同时，保留了外层优化的引导作用。
- **连续时间动力系统（光滑设定）**：
  - 将内层算子与动态正则化项组合为动态正则化算子，构造非自主单调流动力学方程，并引入微分校正项以保障轨迹稳定性。
  - 使用 Fitzpatrick 函数对内层算子进行凸分析表征，并基于 Lyapunov 能级函数建立连续轨迹的衰减估计。
- **离散正则化近端-外梯度算法（Bi-EG，光滑+非光滑设定）**：
  - 允许外层包含非光滑真凸下半连续函数，内层算子包含次微分算子。
  - 在每个迭代步中，前向步骤（算子评估）与后向步骤（近端算子计算）均在当前的动态正则化算子和正则化函数下同步执行。
- **几何条件与收敛保障**：
  - 提出了弱 Attouch–Czarnecki 型可积分条件以及更强的尖锐性假设（Sharpness / Hölder 误差界）。
  - 在上述条件下，推导出了内层残差、外层目标残差以及内层间隙的末代收敛速率（如 $O(k^{-\delta})$、$o(k^{-1/2})$ 以及 $o(k^{-(1/2-\delta)})$ 等），并证明了迭代序列弱收敛到双层优化的最优解。

---

### 3. 实验设计

- **测试场景与数据集**：
  1. **合成高维双层优化问题**：在 $\mathbb{R}^d$ 空间中，外层目标为一阶全变分（TV1）正则项，内层约束包含偏对称矩阵算子与凸集投影构成的非线性单调算子。
  2. **最优传输（OT）次级变分问题**：正 $N$ 边形顶点上的离散概率测度之间的 OT 问题，旨在从多个内层最优传输方案中遴选出使指定连接传输质量最小化的唯一 Monge 映射。
- **对比 Baseline**：层次变分不等式领域的代表性算法——层次优化乐观外梯度法（OEG-H）。
- **评估指标**：内层间隙（Inner gap）、内层残差（Inner residual）及外层目标间隙（Outer objective gap）。

---

### 4. 资源与算力

- **硬件配置**：实验运行于配备 12代 Intel Core i7-1255U 处理器（1.70–4.70 GHz）、16 GB 内存的笔记本电脑上。
- **算力需求**：全套实验基于 Python 开发，未依赖 GPU 加速，在普通 CPU 环境下即可完成测试。

---

### 5. 实验数量与充分性

- **实验设计与数量**：
  - 进行了 20 组随机初始化的对比收敛测试。
  - 针对衰减指数 $\delta \in (0.1, 0.5)$ 进行了 20 组不同取值的敏感性与消融实验。
  - 迭代步数上限设定为 $10^4$ 步。
- **充分性与公平性**：实验覆盖了非线性变分不等式与离散最优传输两种代表性应用，基线方法设置了各自理论允许上限附近的步长。实验设计客观公正，能够有效地验证理论推导的收敛速率。

---

### 6. 主要结论与发现

- **理论速率验证**：数值实验观察到的残差衰减斜率与理论推断的末代收敛速率完全吻合。
- **参数 $\delta$ 的权衡效应**：参数 $\delta$ 决定了内层残差衰减与外层目标收敛之间的 Trade-off：较小的 $\delta$ 显著促进内层问题的快速收敛，而较大的 $\delta$ 则有利于外层目标的优化。
- **算法性能比较**：虽然 Bi-EG 每次迭代需要两次算子评估（相比 OEG-H 的单次评估），但 Bi-EG 在初期能快约一个数量级达到高精度，且仅需存储一个迭代变量（OEG-H 需要存储两个）。

---

### 7. 优点与亮点

- **理论拓展**：打破了传统双层优化要求内层为凸优化问题的限制，成功将求解范式扩展至包含非光滑项的极大单调算子框架。
- **末代收敛保障**：打破了以往层次变分不等式方法仅能保证遍历（Ergodic）平均收敛的局限，建立了强有力的末代收敛速率。
- **分析工具创新**：在连续时间分析中巧妙借助 Fitzpatrick 函数表示 Attouch-Czarnecki 条件，离散分析中引入了无需依赖具体解集的 Lyapunov 函数分析方法。

---

### 8. 不足与局限

- **算子评估开销**：作为 Korpelevich 型外梯度方法的扩展，Bi-EG 相比 Popov 型方法（如 OEG-H）在每步迭代中需要多进行一次前向算子评估。
- **应用验证局限**：实验部分主要基于中小型合成示例和离散 OT 场景，尚未在大规模机器学习（如超参数优化、元学习或对抗训练）等超高维实际任务中进行大规模验证。
- **假设条件依赖**：收敛速率的建立高度依赖于内层算子的 Lipschitz 连续性以及特定的 Attouch-Czarnecki 型条件或尖锐性假设。

---

（完）
