---
title: "DiffuMask: Diffusion Language Model for Token-level Prompt Pruning"
title_zh: DiffuMask：用于词元级提示词剪枝的扩散语言模型
authors: "Caleb Zheng, Jyotika Singh, Fang Tu, Weiyi Sun, Sujeeth Bharadwaj, Yassine Benajiba, Sujith Ravi, Eli Shlizerman, Dan Roth"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06627v1"
tags: ["query:bopl"]
score: 8.0
evidence: 自动提示词剪枝与压缩
tldr: "针对大语言模型提示词冗长且计算成本高的问题，本文提出DiffuMask，一种基于扩散模型的Token级剪枝框架。该方法结合层级化的示例级与Token级信号，通过迭代掩码预测实现快速并行剪枝。DiffuMask在大幅缩短提示词长度（最高80%）的同时，能有效保留核心推理上下文，在多种任务设置下维持甚至提升了模型准确率，为高效上下文学习提供了可控且通用的解决方案。"
selection_source: fresh_fetch
motivation: 现有的提示词剪枝方法通常依赖于计算密集型的序列化Token移除，导致压缩过程缓慢且效率低下。
method: 提出一种基于扩散语言模型的框架，通过集成层级化信号并利用迭代掩码预测实现并行的提示词剪枝。
result: "实验表明DiffuMask可减少高达80%的提示词长度，且在域内、跨域及跨模型设置下均能保持或提升推理准确率。"
conclusion: DiffuMask为提示词压缩提供了一个高效、可控且泛化性强的框架，显著提升了大语言模型的推理效率。
---

## 摘要
上下文学习（In-Context Learning）和思维链（Chain-of-Thought）提示提高了大语言模型（LLM）的推理能力。这些方法通常以更长、更昂贵的提示词为代价，且其中可能包含冗余信息。基于剪枝的提示词压缩提供了一种实用的解决方案，但现有方法依赖于顺序词元移除，计算开销较大。我们提出了 DiffuMask，这是一个基于扩散的框架，集成了分层的示例级（shot-level）和词元级（token-level）剪枝信号，通过迭代掩码预测实现快速且并行的提示词剪枝。DiffuMask 通过在每个去噪步骤中掩盖多个词元，显著加速了压缩过程。它提供了对保留内容的可调控性，在保留关键推理上下文的同时，实现了高达 80% 的提示词长度缩减。同时，它在域内、跨域和跨模型设置下均能保持或提高准确率。我们的结果表明，DiffuMask 为提示词压缩提供了一个通用且可控的框架，促进了大语言模型中更快速、更可靠的上下文推理。

## Abstract
In-Context Learning and Chain-of-Thought prompting improve reasoning in large language models (LLMs). These typically come at the cost of longer, more expensive prompts that may contain redundant information. Prompt compression based on pruning offers a practical solution, yet existing methods rely on sequential token removal which is computationally intensive. We present DiffuMask, a diffusion-based framework integrating hierarchical shot-level and token-level pruning signals, that enables rapid and parallel prompt pruning via iterative mask prediction. DiffuMask substantially accelerates the compression process via masking multiple tokens in each denoising step. It offers tunable control over retained content, preserving essential reasoning context and achieving up to 80\% prompt length reduction. Meanwhile, it maintains or improves accuracy across in-domain, out-of-domain, and cross-model settings. Our results show that DiffuMask provides a generalizable and controllable framework for prompt compression, facilitating faster and more reliable in-context reasoning in LLMs.

---

## 论文详细总结（自动生成）

以下是对论文《DiffuMask: Diffusion Language Model for Token-level Prompt Pruning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）依赖上下文学习（ICL）和思维链（CoT）来处理复杂任务，但这导致提示词（Prompt）变得极其冗长，增加了推理延迟和计算成本。
*   **核心问题**：现有的提示词剪枝方法（如基于强化学习或进化算法的方法）通常是**顺序执行**的，即逐个移除词元（Token）并评估效果。这种方式计算极其密集，处理单个提示词可能需要数小时甚至数天，难以在大规模实际应用中推广。
*   **研究目标**：提出一种高效、并行且可控的框架，利用扩散模型快速预测提示词的保留掩码，实现大幅度压缩而不损失性能。

### 2. 方法论
*   **核心思想**：将提示词剪枝建模为一个**迭代去噪过程**。模型不再是顺序删除Token，而是通过扩散语言模型（DLM）并行预测整个序列中哪些Token应该被保留（1）或移除（0）。
*   **关键技术细节**：
    *   **数据构建流水线**：首先通过层级剪枝生成训练数据。包括**示例级剪枝**（移除不相关的Few-shot例子）和**词元级剪枝**（使用TA-Pruning算法移除例子内部的冗余词）。
    *   **DiffuMask模型**：基于离散扩散模型（如LLaDA-8B）进行微调。
    *   **训练目标**：结合了二元交叉熵损失（BCE）和**抗掩码惩罚（Anti-mask penalty）**。后者专门用于惩罚错误移除关键Token的行为，以平衡压缩率和信息完整性。
    *   **推理策略**：采用64步扩散迭代。引入了 `Top-k` 掩码预测和置信度阈值 $\tau$，允许用户在推理时灵活调节压缩强度（更激进或更保守）。

### 3. 实验设计
*   **数据集**：
    *   **域内（In-domain）**：GSM8K（数学推理基准）。
    *   **跨域（Out-of-domain）**：Yelp5（情感分析）、Yahoo Answers（主题分类）、AG’s News（新闻分类）。
*   **对比基准（Benchmarks）**：
    *   原始全量提示词（Full Prompt）。
    *   **通用压缩工具**：LLMLingua、LLMLingua2。
    *   **专门剪枝方法**：CoT-Influx（仅示例级）、HP（层级剪枝基准）。
*   **跨模型验证**：将DiffuMask生成的剪枝提示词应用于 GPT-4o、Gemini 2.5 Pro、Grok-3、Llama-3.3-70B 等8个主流模型。

### 4. 资源与算力
*   **硬件**：实验明确提到使用了 **NVIDIA A100 GPU**。
*   **训练与推理时长**：
    *   **训练数据生成**：使用传统TA-Pruning生成一个样本需要 **10-48小时**。
    *   **DiffuMask推理**：完成一次提示词剪枝仅需 **约1分钟**，相比传统顺序剪枝方法实现了数个数量级的加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   构建了包含数百个高质量“完整-剪枝”对的训练集。
    *   在GSM8K上进行了详尽的测试（1319个查询）。
    *   跨域实验涵盖了从1-shot到20-shot的不同配置。
*   **充分性评价**：实验设计非常全面。不仅验证了压缩效率和准确率，还通过**跨模型实验**证明了剪枝策略的通用性（即在一个模型上学到的剪枝规律可以迁移到其他模型），并进行了词性（POS）分布分析，证明剪枝决策是非平凡的（不仅仅是删除了虚词）。

### 6. 主要结论与发现
*   **高效压缩**：DiffuMask最高可实现 **80%** 的提示词长度缩减。
*   **性能保持/提升**：在大幅压缩后，模型在GSM8K等任务上的准确率往往能持平甚至超过全量提示词，说明剪枝有效去除了干扰噪声。
*   **强泛化性**：尽管模型仅在数学推理数据上微调，但在完全不同的文本分类任务（跨域）和完全不同的模型架构（跨模型）上依然表现出色。

### 7. 优点
*   **并行加速**：打破了传统剪枝方法的效率瓶颈，使Token级剪枝在实际生产环境中变得可行。
*   **可控性强**：通过调节推理参数，用户可以精确控制压缩比与性能的权衡。
*   **架构无关性**：生成的剪枝提示词具有“即插即用”的特性，适用于多种下游LLM。

### 8. 不足与局限
*   **数据获取成本高**：虽然DiffuMask推理很快，但训练它所需的初始高质量标签（由TA-Pruning生成）极其耗时，这限制了训练数据的规模。
*   **非实时性**：虽然比顺序剪枝快，但64步扩散迭代对于极低延迟要求的实时对话场景可能仍显稍慢。
*   **任务覆盖面**：目前主要集中在推理和分类任务，尚未在长文本摘要、多轮对话或多模态任务中进行验证。

（完）
