---
title: "Seeing is Believing: Robust Vision-Guided Cross-Modal Prompt Learning under Label Noise"
title_zh: 眼见为实：标签噪声下鲁棒的视觉引导跨模态提示学习
authors: "Zibin Geng, Xuefeng Jiang, Jia Li, Zheng Li, Tian Wen, Lvhua Wu, Sheng Sun, Yuwei Wang, Min Liu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09532v1"
tags: ["query:bopl"]
score: 9.0
evidence: 视觉引导的提示学习框架
tldr: 针对视觉语言模型在标签噪声环境下提示学习鲁棒性不足的问题，本文提出VisPrompt框架。该方法利用视觉信息比噪声标签更可靠的特性，通过跨模态注意力机制将视觉语义反向注入提示表示中，并结合条件调制机制动态平衡文本先验与图像证据。VisPrompt在保持预训练模型冻结的同时，显著提升了模型对错误标注的抵抗力，在多个基准数据集上表现优于现有方法。
selection_source: fresh_fetch
motivation: 提示学习易受标签噪声干扰，而视觉内容包含更稳定可靠的语义信息，可用于引导更鲁棒的提示生成。
method: 提出VisPrompt框架，利用跨模态注意力机制将实例级视觉特征注入提示，并采用轻量级条件调制机制自适应控制注入强度。
result: 在7个合成及真实噪声数据集上的实验表明，VisPrompt在极少额外参数下显著提升了鲁棒性，优于现有基线方法。
conclusion: 通过视觉引导的跨模态提示学习，可以有效抑制噪声干扰并缓解对错误标注样本的记忆，是提升VLM鲁棒性的有效途径。
---

## 摘要
提示学习是视觉语言模型的一种参数高效方法，但其在标签噪声下的鲁棒性研究较少。视觉内容包含更丰富且更可靠的语义信息，在标签噪声下表现得更为鲁棒。然而，提示本身极易受到标签噪声的影响。基于这一直觉，我们提出了 VisPrompt，这是一个针对噪声标签场景的轻量级且鲁棒的视觉引导提示学习框架。具体而言，我们利用跨模态注意力机制，反向地将视觉语义注入到提示表示中。这使得提示标记（prompt tokens）能够选择性地聚合与当前样本相关的视觉信息，从而通过将提示学习锚定在稳定的实例级视觉证据上并减少噪声监督的影响，来提高鲁棒性。针对所有样本采用相同的视觉信息注入方式（尽管其视觉线索质量存在差异）所导致的稳定性问题，我们进一步引入了一种轻量级的条件调制机制，以自适应地控制视觉信息注入的强度，从而在文本侧语义先验与图像侧实例证据之间取得更鲁棒的平衡。所提出的框架有效地抑制了噪声引起的干扰，减少了提示更新中的不稳定性，并缓解了对错误标注样本的记忆。VisPrompt 在保持预训练视觉语言模型（VLM）主干冻结且仅引入少量额外可训练参数的情况下，显著提高了鲁棒性。在合成和真实世界标签噪声下的广泛实验表明，VisPrompt 在七个基准数据集上普遍优于现有基准方法，并表现出更强的鲁棒性。我们的代码已在 https://github.com/gezbww/Vis_Prompt 公开。

## Abstract
Prompt learning is a parameter-efficient approach for vision-language models, yet its robustness under label noise is less investigated. Visual content contains richer and more reliable semantic information, which remains more robust under label noise. However, the prompt itself is highly susceptible to label noise. Motivated by this intuition, we propose VisPrompt, a lightweight and robust vision-guided prompt learning framework for noisy-label settings. Specifically, we exploit a cross-modal attention mechanism to reversely inject visual semantics into prompt representations. This enables the prompt tokens to selectively aggregate visual information relevant to the current sample, thereby improving robustness by anchoring prompt learning to stable instance-level visual evidence and reducing the influence of noisy supervision. To address the instability caused by using the same way of injecting visual information for all samples, despite differences in the quality of their visual cues, we further introduce a lightweight conditional modulation mechanism to adaptively control the strength of visual information injection, which strikes a more robust balance between text-side semantic priors and image-side instance evidence. The proposed framework effectively suppresses the noise-induced disturbances, reduce instability in prompt updates, and alleviate memorization of mislabeled samples. VisPrompt significantly improves robustness while keeping the pretrained VLM backbone frozen and introducing only a small amount of additional trainable parameters. Extensive experiments under synthetic and real-world label noise demonstrate that VisPrompt generally outperforms existing baselines on seven benchmark datasets and achieves stronger robustness. Our code is publicly available at https://github.com/gezbww/Vis_Prompt.

---

## 论文详细总结（自动生成）

这篇论文针对视觉语言模型（VLM）在提示学习（Prompt Learning）中面临的标签噪声问题，提出了名为 **VisPrompt** 的鲁棒性框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在微调预训练视觉语言模型（如 CLIP）时，提示学习虽然参数高效，但极易受到**标签噪声（Label Noise）**的干扰。由于模型主干冻结，仅有的少量可学习提示标记（Prompt Tokens）会迅速拟合错误标签，导致模型性能大幅下降。
*   **研究动机**：作者观察到，尽管标签可能被错误标注，但**图像内容本身包含的实例级语义是相对稳定且可靠的**。因此，如果能利用视觉信息来引导提示的生成，就能将优化过程锚定在真实的视觉证据上，从而抵御噪声标签的误导。

### 2. 方法论：VisPrompt 框架
VisPrompt 的核心思想是“以视觉引导文本提示”，主要包含以下四个关键步骤：
1.  **视觉特征投影**：将图像编码器提取的局部视觉标记（Visual Tokens）通过线性层投影到提示嵌入空间，使其与文本表示对齐。
2.  **跨模态视觉提示调节（核心技术）**：利用**跨模态注意力机制（Cross-Modal Attention）**，以可学习的提示标记作为 Query，以视觉标记作为 Key 和 Value。通过这种方式，提示标记能够选择性地聚合图像中相关的视觉语义。
3.  **基于 FiLM 的鲁棒调制**：为了应对不同图像视觉质量的差异，引入了 **FiLM（特征线性调制）** 和**门控机制**。该机制根据当前视觉特征自适应地调整视觉信息注入提示的强度，防止低质量视觉线索引入额外干扰。
4.  **鲁棒损失选择**：利用**最优传输（Optimal Transport, OT）**算法估计样本的可靠性。将样本分为可靠和不可靠两部分：可靠样本使用标准交叉熵损失（CE），不可靠样本使用广义交叉熵损失（GCE），以减少错误标注的影响。

### 3. 实验设计
*   **数据集**：
    *   **合成噪声场景**：EuroSAT（卫星图）、Flowers102（花卉）、OxfordPets（宠物）、DTD（纹理）、UCF101（动作）、Caltech101（物体）。
    *   **真实噪声场景**：Food101N（来自互联网的真实噪声食品数据集）。
*   **噪声设置**：涵盖对称噪声（Symmetric）和非对称噪声（Asymmetric），噪声率从 12.5% 到 75% 不等。
*   **对比基准（Baselines）**：
    *   标准提示学习：CoOp。
    *   鲁棒损失/学习方法：GCE、JoAPR、NLPrompt。
*   **评估指标**：Top-1 分类准确率。

### 4. 资源与算力
*   **硬件**：实验在 **Nvidia RTX 4090 GPU** 上进行。
*   **软件/框架**：基于 PyTorch 框架。
*   **训练细节**：使用 SGD 优化器，学习率为 0.002，采用余弦退火调度，训练 200 个 epoch，batch size 为 16。
*   **参数量**：新增的可训练参数量极少，不到总参数量的 1%（FiLM 模块仅占 0.3%）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个数据集上进行了大规模测试，涵盖了多种噪声类型和极高的噪声比例（高达 75%）。
*   **消融实验**：对比了“无视觉引导”、“有视觉引导但无 FiLM 调制”以及“完整 VisPrompt”的效果，验证了各组件的必要性。
*   **敏感性分析**：测试了不同提示长度（4 到 32）、不同 Shot 数（1 到 16）以及不同视觉主干（ResNet 系列和 ViT 系列）的影响。
*   **充分性评价**：实验设计非常充分且客观，通过跨数据集、跨噪声率、跨模型主干的对比，有力证明了方法的普适性和鲁棒性。

### 6. 主要结论与发现
*   **视觉引导的有效性**：VisPrompt 在所有测试数据集和噪声率下均优于现有基线方法。
*   **极端噪声下的韧性**：在 75% 的极端噪声下，VisPrompt 的性能下降远小于传统方法，显示出极强的抗干扰能力。
*   **理论支撑**：通过理论分析证明了跨模态注意力能提供清洁语义的去噪近似，且 FiLM 调制能保证提示更新的稳定性。
*   **参数高效**：在几乎不增加推理负担的情况下，显著提升了 VLM 的下游任务适应能力。

### 7. 优点与亮点
*   **视角独特**：不同于以往侧重于“标签清洗”或“损失函数设计”的方法，本文从“跨模态语义锚定”的角度解决标签噪声问题。
*   **轻量级设计**：仅引入极少量参数，且保持 VLM 主干冻结，非常适合实际部署。
*   **理论与实验结合**：不仅有详尽的实验，还提供了 Lipschitz 连续性相关的理论证明，增强了说服力。

### 8. 不足与局限
*   **任务覆盖面**：目前主要集中在图像分类任务，尚未在目标检测、语义分割或视频理解等更复杂的任务上验证。
*   **计算开销**：虽然参数量小，但引入跨模态注意力机制会带来一定的额外计算开销，在资源极度受限的设备上可能存在挑战。
*   **噪声模式**：虽然测试了真实噪声数据集，但大部分实验仍基于合成噪声，对于更复杂的实例相关噪声（Instance-dependent noise）探讨较少。

（完）
