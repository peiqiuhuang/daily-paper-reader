---
title: Modality-Agnostic Prompt Learning for Multi-Modal Camouflaged Object Detection
title_zh: 面向多模态伪装目标检测的模态无关提示学习
authors: "Hao Wang, Jiqing Zhang, Xin Yang, Baocai Yin, Lu Jiang, Zetian Mi, Huibing Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12380v1"
tags: ["query:bopl"]
score: 9.0
evidence: 用于适配的模态无关提示学习
tldr: 针对多模态伪装目标检测中现有方法依赖特定模态架构、扩展性差的问题，本文提出一种模态无关的提示学习框架。该框架基于Segment Anything Model (SAM)，通过数据驱动的内容域与知识驱动的提示域交互，生成统一的多模态提示，并结合轻量级掩码细化模块提升边界精度。实验证明该方法在RGB-D、RGB-T和RGB-P等多种模态组合上均具有优异的性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有的多模态伪装目标检测方法通常依赖特定模态的架构或定制化融合策略，限制了模型的扩展性和跨模态泛化能力。
method: 提出一种基于SAM的模态无关提示学习框架，通过内容域与提示域的交互提取任务相关线索，并引入轻量级掩码细化模块校准预测边界。
result: 在RGB-深度、RGB-热成像和RGB-偏振等多个基准测试上，该框架均表现出显著的性能提升和强大的跨模态适应性。
conclusion: 本研究证明了通过模态无关的提示学习可以有效利用辅助模态信息增强SAM在复杂伪装场景下的分割能力。
---

## 摘要
伪装目标检测 (COD) 旨在分割与复杂背景无缝融合的目标，随着研究的深入，利用额外的视觉模态通过互补信息增强鲁棒性正受到越来越多的关注。然而，现有的大多数方法通常依赖于特定模态的架构或定制的融合策略，这限制了其可扩展性和跨模态泛化能力。为了解决这一问题，我们提出了一种新型框架，为 Segment Anything Model (SAM) 生成模态无关的多模态提示，从而实现对任意辅助模态的参数高效适配，并显著提升 COD 任务的整体性能。具体而言，我们通过数据驱动的内容域与知识驱动的提示域之间的交互来建模多模态学习，将任务相关的线索蒸馏为统一的提示，用于 SAM 解码。我们进一步引入了一个轻量级的掩码细化模块 (Mask Refine Module)，通过结合细粒度的提示线索来校准粗糙的预测，从而获得更准确的伪装目标边界。在 RGB-Depth、RGB-Thermal 和 RGB-Polarization 基准测试上的大量实验验证了我们模态无关框架的有效性和泛化性。

## Abstract
Camouflaged Object Detection (COD) aims to segment objects that blend seamlessly into complex backgrounds, with growing interest in exploiting additional visual modalities to enhance robustness through complementary information. However, most existing approaches generally rely on modality-specific architectures or customized fusion strategies, which limit scalability and cross-modal generalization. To address this, we propose a novel framework that generates modality-agnostic multi-modal prompts for the Segment Anything Model (SAM), enabling parameter-efficient adaptation to arbitrary auxiliary modalities and significantly improving overall performance on COD tasks. Specifically, we model multi-modal learning through interactions between a data-driven content domain and a knowledge-driven prompt domain, distilling task-relevant cues into unified prompts for SAM decoding. We further introduce a lightweight Mask Refine Module to calibrate coarse predictions by incorporating fine-grained prompt cues, leading to more accurate camouflaged object boundaries. Extensive experiments on RGB-Depth, RGB-Thermal, and RGB-Polarization benchmarks validate the effectiveness and generalization of our modality-agnostic framework.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Modality-Agnostic Prompt Learning** 的新框架，旨在解决多模态伪装目标检测（Multi-Modal COD）中模型对特定模态依赖性强、扩展性差的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：伪装目标检测（COD）要求识别与背景高度相似的目标。虽然引入深度（Depth）、热成像（Thermal）或偏振（Polarization）等辅助模态能提供互补信息，但现有方法通常为特定模态设计专门的融合架构，导致模型难以扩展到新模态，且跨模态泛化能力弱。
*   **研究背景**：Segment Anything Model (SAM) 虽具有强大的分割能力，但在伪装场景下表现不佳。现有的 SAM 微调方法（如 Adapter）往往也是模态相关的。
*   **整体含义**：论文试图构建一个通用的、模态无关的提示学习框架，使 SAM 能够以极低的参数代价（参数高效微调）适配任何辅助视觉模态。

### 2. 方法论
核心思想是建立**内容域（Content Domain）**与**提示域（Prompt Domain）**的交互，将多模态证据转化为 SAM 可理解的提示信号。
*   **内容域构建**：使用冻结的 SAM 编码器提取 RGB 特征，使用 PVT 编码器提取辅助模态特征，通过元素级加权融合（Weighted Fusion）构建包含空间和语义信息的“内容域”。
*   **提示域生成**：引入一组可学习的提示原型（Prompt Prototypes），通过内容域特征进行自适应调制，生成代表任务先验知识的“提示域”。
*   **双域协同交互（CPIM）**：采用对称的交叉注意力机制（Cross-Attention），实现数据驱动的内容证据与知识驱动的提示先验之间的双向蒸馏。
*   **掩码细化模块（MRM）**：针对 SAM 在模糊边界处表现不佳的问题，利用细粒度的提示域特征对 SAM 解码器的中间特征进行校准和重构，提升边界精度。
*   **Auxi-tokens**：在解码阶段引入额外的可学习辅助 Token，增强模型处理不同模态的灵活性。

### 3. 实验设计
*   **数据集**：
    *   **RGB-D (深度)**：COD10K, CAMO, NC4K, CHAMELEON。
    *   **RGB-P (偏振)**：PCOD-1200。
    *   **RGB-T (热成像)**：VIAC。
*   **Benchmark 与对比方法**：
    *   对比了 15 种通用 SOTA 方法（如 SINet, PFNet, FSPNet 等）。
    *   对比了 13 种基于 SAM 的微调方法（如 SAM-Adapter, HQSAM, DSAM, SAM-DSA 等）。
*   **评价指标**：$S_\alpha$ (结构度量), $F_\omega^\beta$ (加权 F 值), $E_\phi$ (增强对齐度量), $M$ (平均绝对误差)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **软件/参数**：PyTorch 实现，Adam 优化器，初始学习率 5e-4。
*   **训练细节**：训练 50 个 epoch，batch size 为 4。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同的多模态任务（RGB-D, RGB-P, RGB-T）上分别进行了独立验证，并进行了联合训练（Joint Training）实验。
*   **消融实验**：针对内容域融合、CPIM 模块、交叉注意力、MRM 模块及辅助 Token 均做了详细的消融对比。
*   **跨模态迁移**：专门设计了“在一个模态训练，在另一个模态测试”的迁移实验，验证了“模态无关”的特性。
*   **充分性评价**：实验设计非常充分且客观。通过跨模态、跨任务（扩展到显著性目标检测 SOD）以及属性分析（小物体、不规则物体等），全面证明了框架的鲁棒性和通用性。

### 6. 主要结论与发现
*   **性能领先**：在所有三个多模态 COD 基准测试中，该方法均达到了 SOTA 性能，尤其在边界粘连严重的复杂场景下优势明显。
*   **参数极度高效**：可训练参数量仅为 **2.73M**，远低于同类 SAM 微调方法（如 DSAM 的 121.93M），且性能更优。
*   **模态无关性**：实验证明模型不依赖于特定模态的结构设计，能够灵活处理各种辅助输入，且具有良好的跨模态泛化能力。

### 7. 优点（亮点）
*   **通用性强**：打破了“一种模态一个模型”的限制，提出了统一的多模态处理范式。
*   **双域建模**：将数据驱动的感知与知识驱动的提示相结合，逻辑清晰且符合认知直觉。
*   **轻量化**：在保持高性能的同时，极大地降低了微调大模型的计算成本。
*   **边界精度高**：通过 MRM 模块有效弥补了 SAM 在伪装目标边缘分割上的短板。

### 8. 不足与局限
*   **辅助模态质量依赖**：当辅助模态本身包含大量噪声或无法提供有效区分信息时（如热成像中目标与背景等温），模型性能会受到限制。
*   **数据规模不平衡**：在联合训练实验中，由于不同模态数据集规模差异较大（如 RGB-D 远多于 RGB-P），导致联合训练的效果略逊于独立训练。
*   **实时性未提及**：虽然参数量小，但未详细讨论推理速度（FPS），对于某些实时应用场景的适用性有待验证。

（完）
