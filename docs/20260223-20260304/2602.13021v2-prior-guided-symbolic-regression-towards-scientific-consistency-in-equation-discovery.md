---
title: "Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery"
title_zh: 先验引导的符号回归：迈向方程发现中的科学一致性
authors: "Jing Xiao, Xinhai Chen, Jiaming Peng, Qinglin Wang, Menghan Jia, Zhiquan Lai, Guangping Yu, Dongsheng Li, Tiejun Li, Jie Liu"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13021v2"
tags: ["query:sr"]
score: 10.0
evidence: 先验引导的符号回归以确保科学一致性
tldr: 符号回归旨在从数据中发现可解释方程，但现有方法常陷入“伪方程陷阱”，即拟合良好却违背科学原理。本文提出PG-SR框架，通过预热、进化和精炼三阶段流程，引入先验约束检查器和先验退火约束评估（PACE）机制，将领域先验转化为可执行约束。理论证明该方法降低了假设空间的复杂度，实验表明其在多领域优于现有基线，且对噪声和数据稀缺具有鲁棒性。
selection_source: fresh_fetch
motivation: 现有符号回归方法过度依赖经验风险最小化，导致生成的方程虽然拟合度高但缺乏科学一致性。
method: 提出PG-SR框架，利用先验约束检查器和先验退火约束评估机制，在三阶段演化过程中引导方程发现。
result: 实验证明PG-SR在多个领域显著超越了现有最先进基线，并在噪声和数据稀缺环境下表现出极强的鲁棒性。
conclusion: 通过引入显式的科学先验约束，PG-SR有效解决了伪方程问题，提升了符号回归在科学发现中的可靠性与泛化能力。
---

## 摘要
符号回归 (SR) 旨在从观测数据中发现可解释的方程，具有揭示自然现象背后潜在原理的潜力。然而，现有方法往往会陷入“伪方程陷阱”：生成的方程虽然能很好地拟合观测数据，但与基本科学原理不一致。一个关键原因是这些方法由经验风险最小化主导，缺乏确保科学一致性的显式约束。为了弥补这一差距，我们提出了 PG-SR，这是一个基于预热、进化和精炼三阶段流水线构建的先验引导符号回归框架。在整个流水线中，PG-SR 引入了一个先验约束检查器，将领域先验显式编码为可执行的约束程序，并在进化阶段采用先验退火约束评估 (PACE) 机制，逐步引导发现过程转向符合科学一致性的区域。在理论上，我们证明了 PG-SR 降低了假设空间的 Rademacher 复杂度，从而产生了更紧致的泛化界限，并为防止伪方程提供了保证。在实验上，PG-SR 在多个领域均优于最先进的基准方法，并对不同的先验质量、噪声数据和数据稀缺性保持了鲁棒性。

## Abstract
Symbolic Regression (SR) aims to discover interpretable equations from observational data, with the potential to reveal underlying principles behind natural phenomena. However, existing approaches often fall into the Pseudo-Equation Trap: producing equations that fit observations well but remain inconsistent with fundamental scientific principles. A key reason is that these approaches are dominated by empirical risk minimization, lacking explicit constraints to ensure scientific consistency. To bridge this gap, we propose PG-SR, a prior-guided SR framework built upon a three-stage pipeline consisting of warm-up, evolution, and refinement. Throughout the pipeline, PG-SR introduces a prior constraint checker that explicitly encodes domain priors as executable constraint programs, and employs a Prior Annealing Constrained Evaluation (PACE) mechanism during the evolution stage to progressively steer discovery toward scientifically consistent regions. Theoretically, we prove that PG-SR reduces the Rademacher complexity of the hypothesis space, yielding tighter generalization bounds and establishing a guarantee against pseudo-equations. Experimentally, PG-SR outperforms state-of-the-art baselines across diverse domains, maintaining robustness to varying prior quality, noisy data, and data scarcity.

---

## 论文详细总结（自动生成）

以下是对论文《Prior-Guided Symbolic Regression: Towards Scientific Consistency in Equation Discovery》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）的核心目标是从观测数据中提取可解释的数学方程。然而，现有方法（如遗传算法、Transformer或大模型驱动的方法）普遍面临**“伪方程陷阱”（Pseudo-Equation Trap）**：即生成的方程在训练集上拟合精度极高（经验风险极小），但其数学结构违背了基本的科学原理（如物理守恒、生物单峰响应等）。在数据稀疏或存在噪声的情况下，这种现象尤为严重，导致模型在分布外（OOD）数据上表现极差。论文的研究动机在于打破单纯依赖“经验风险最小化”的范式，通过引入显式的科学先验约束，确保方程发现过程的科学一致性。

### 2. 方法论：核心思想与技术细节
论文提出了 **PG-SR** 框架，其核心思想是将领域先验知识转化为可执行的约束程序，并引导搜索过程。
*   **三阶段流水线**：
    1.  **预热（Warm-up）**：利用数据统计分析引导 LLM 生成初步的方程骨架，初始化经验池。
    2.  **进化（Evolution）**：通过 LLM 作为进化算子，结合多岛模型（Islands）保持种群多样性，不断迭代生成新假设。
    3.  **精炼（Refinement）**：利用残差分析和反射机制，针对拟合不佳的区域进行结构化修正。
*   **关键技术：先验退火约束评估（PACE）**：
    *   为了避免搜索初期约束过严导致陷入局部最优，PACE 引入了动态评分机制。
    *   **公式逻辑**：对于违反约束的候选方程，其得分会根据进化进度 $t$ 进行“收缩和偏移”变换。初期（$t$ 小）惩罚较轻，允许探索；后期（$t$ 大）惩罚加重，强制符合科学一致性。
*   **先验约束检查器**：采用 LLM 辅助、人工校验的工作流，将科学原理（如“生物量为0时增长率为0”）编写为 Python 检查程序。
*   **理论保证**：论文证明了引入先验约束能有效降低假设空间的 **Rademacher 复杂度**，从而获得更紧致的泛化界限，从理论上解释了为何能规避伪方程。

### 3. 实验设计
*   **数据集/场景**：涵盖了生物（大肠杆菌生长）、材料（铝合金应力-应变实测数据）、化学（反应动力学）和物理（非线性振荡器）四个领域的 5 个复杂任务。
*   **基准方法（Baselines）**：对比了 13 种最先进的方法，包括：
    *   **搜索类**：PySR, GPLearn, DSR, uDSR, Operon, GP-GOMEA。
    *   **Transformer 类**：TPSR, E2E, NeSymReS, PhyE2E。
    *   **LLM 类**：LLM-SR, LaSR, DrSR。
*   **评估指标**：主要使用分布内（ID）和分布外（OOD）的归一化均方误差（NMSE）。

### 4. 资源与算力
*   **硬件设备**：使用了本地工作站，配置为 **Intel Core i9-14900HX CPU** 和 **NVIDIA GeForce RTX 4090 GPU**。
*   **推理成本**：
    *   单次完整实验运行时间约为 **10 小时**。
    *   总 Token 消耗约为 **1785 万**（GPT-4o-mini），单次运行成本约 **4.70 美元**。
*   **模型后端**：主要使用 GPT-4o-mini，同时也测试了 Llama-3.3-70B 以验证鲁棒性。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的客观性和说服力：
*   **性能对比**：在 5 个数据集上进行了 ID 和 OOD 的全面对比。
*   **消融实验**：分别验证了预热、PACE 机制和精炼阶段的必要性。
*   **鲁棒性测试**：
    *   **先验质量敏感性**：测试了无先验、弱先验、错误先验和全先验下的表现。
    *   **噪声鲁棒性**：在不同高斯噪声水平（0.01 到 0.1）下测试。
    *   **数据稀缺性**：测试了从 100% 到 5% 数据量下的性能变化。
*   **公平性**：对基线方法也尝试了加入先验增强进行对比，证明了 PG-SR 框架本身的优越性而非仅仅是先验的功劳。

### 6. 主要结论与发现
*   **规避陷阱**：PG-SR 能有效识别并剔除那些拟合度高但物理意义错误的“伪方程”。
*   **OOD 泛化极强**：在所有任务中，PG-SR 的 OOD 误差显著低于基线（有时低出数个数量级），证明了科学一致性是泛化的关键。
*   **PACE 的有效性**：动态退火机制比硬性过滤（Hard-filtering）更能维持搜索空间的连续性，避免过早收敛。
*   **对错误先验的容错性**：即使提供错误的先验，由于 PACE 允许数据驱动的成分存在，模型性能也不会崩溃，表现出良好的鲁棒性。

### 7. 优点
*   **理论与实践结合**：不仅有扎实的统计学习理论证明，还有跨学科的复杂实验验证。
*   **PACE 机制创新**：巧妙地解决了“探索”与“约束”之间的矛盾，是符号回归领域的一个重要改进。
*   **真实数据验证**：在材料科学的实测应力-应变数据上表现出色，证明了其处理现实噪声问题的潜力。

### 8. 不足与局限
*   **人工干预需求**：虽然使用了 LLM 辅助，但构建“可执行约束程序”仍需要人类专家进行最终校验，尚未实现完全自动化。
*   **计算成本**：相比于传统的遗传算法（如 PySR），基于 LLM 的迭代推理成本（时间和金钱）较高。
*   **先验依赖性**：虽然对错误先验有一定容错，但其核心优势仍高度依赖于高质量领域知识的输入。

（完）
