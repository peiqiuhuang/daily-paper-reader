---
title: "A Tale of Two Problems: Multi-Task Bilevel Learning Meets Equality Constrained Multi-Objective Optimization"
title_zh: 两个问题的故事：多任务双层学习遇上等式约束多目标优化
authors: "Zhiyao Zhang, Myeung Suk Oh, Zhen Qin, Jiaxiang Li, Xin Zhang, Jia Liu"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09094v1"
tags: ["query:bopl"]
score: 9.0
evidence: 多任务双层学习与双层优化方法
tldr: 本研究针对现有双层优化局限于单任务且依赖下层强凸性假设的问题，首次探讨了在下层一般凸性下的多任务双层学习（MTBL）。通过将MTBL重构为等式约束多目标优化（ECMO）问题，并提出基于加权切比雪夫惩罚的算法，实现了在确定性和随机设置下的收敛。该方法不仅建立了KKT帕累托平稳性准则，还通过调整偏好向量探索帕累托前沿，为复杂机器学习任务提供了新的优化框架。
selection_source: fresh_fetch
motivation: 现有的双层优化方法难以处理多任务场景及下层仅满足一般凸性的复杂机器学习问题。
method: 将多任务双层学习问题转化为等式约束多目标优化问题，并提出一种加权切比雪夫惩罚算法。
result: "算法在确定性和随机环境下均达到了O(ST^{-1/2})的有限时间收敛率，并能有效探索帕累托前沿。"
conclusion: 该研究通过建立ECMO与MTBL的联系，为解决具有一般凸性约束的多任务优化问题提供了理论基础和高效算法。
---

## 摘要
近年来，双层优化（BLO）因其在机器学习中的广泛应用而备受关注。然而，现有关于 BLO 的大多数工作仍局限于单任务设置，并依赖于下层强凸性假设，这极大地限制了它们在日益复杂的现代机器学习问题中的适用性。在本文中，我们首次尝试在松弛的下层一般凸性（LLGC）假设下，将 BLO 扩展到多任务设置。为此，我们将具有 LLGC 的多任务双层学习（MTBL）问题重新表述为一个等式约束多目标优化（ECMO）问题。然而，ECMO 本身是一个尚未在文献中研究过的新问题。为了填补这一空白，我们首先建立了一种新的基于 Karush-Kuhn-Tucker (KKT) 的 Pareto 平稳性，作为 ECMO 算法设计的收敛准则。在此基础上，我们提出了一种加权切比雪夫（WC）惩罚算法，该算法在确定性和随机设置下均能达到基于 KKT 的 Pareto 平稳性的 $O(ST^{-\frac{1}{2}})$ 有限时间收敛率，其中 $S$ 表示目标数量，$T$ 是总迭代次数。此外，通过在 $S$ 维单纯形上改变偏好向量，我们的 WC 惩罚方法能够系统地探索 Pareto 前沿。最后，ECMO 问题的解可以直接转化为原始 MTBL 问题的解，从而闭合了这两个基础优化框架之间的环路。

## Abstract
In recent years, bilevel optimization (BLO) has attracted significant attention for its broad applications in machine learning. However, most existing works on BLO remain confined to the single-task setting and rely on the lower-level strong convexity assumption, which significantly restricts their applicability to modern machine learning problems of growing complexity. In this paper, we make the first attempt to extend BLO to the multi-task setting under a relaxed lower-level general convexity (LLGC) assumption. To this end, we reformulate the multi-task bilevel learning (MTBL) problem with LLGC into an equality constrained multi-objective optimization (ECMO) problem. However, ECMO itself is a new problem that has not yet been studied in the literature. To address this gap, we first establish a new Karush-Kuhn-Tucker (KKT)-based Pareto stationarity as the convergence criterion for ECMO algorithm design. Based on this foundation, we propose a weighted Chebyshev (WC)-penalty algorithm that achieves a finite-time convergence rate of $O(ST^{-\frac{1}{2})$ to KKT-based Pareto stationarity in both deterministic and stochastic settings, where $S$ denotes the number of objectives, and $T$ is the total iterations. Moreover, by varying the preference vector over the $S$-dimensional simplex, our WC-penalty method systematically explores the Pareto front. Finally, solutions to the ECMO problem translate directly into solutions for the original MTBL problem, thereby closing the loop between these two foundational optimization frameworks.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Tale of Two Problems: Multi-Task Bilevel Learning Meets Equality Constrained Multi-Objective Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现代机器学习任务（如大语言模型 LLM 的对齐）通常具有嵌套结构（双层优化）和多个冲突目标（多任务学习）。
*   **现有局限**：
    1.  **强凸性依赖**：现有的双层优化（BLO）大多依赖下层问题的“强凸性”（LLSC）假设，以确保超梯度（hyper-gradient）有定义，但这在复杂模型中难以满足。
    2.  **单任务局限**：多数研究局限于单任务，难以处理多任务间的权衡。
*   **核心问题**：如何在**下层仅满足一般凸性（LLGC）**的松弛条件下，解决**多任务双层学习（MTBL）**问题。

### 2. 方法论
*   **核心思想**：将 MTBL 问题转化为一个等式约束的多目标优化（ECMO）问题，并利用加权切比雪夫（Weighted Chebyshev, WC）标量化技术进行求解。
*   **关键技术细节**：
    1.  **问题重构**：利用下层凸性，将下层最优性条件转化为一阶平稳条件 $\nabla_y g(x, y) = 0$，从而将双层结构转化为单层的等式约束。
    2.  **KKT 帕累托平稳性**：针对 ECMO 这一新领域，论文首次建立了基于 KKT 条件的帕累托平稳性准则，作为算法收敛的理论基础。
    3.  **WC-Penalty 算法**：
        *   使用加权切比雪夫标量化来处理多目标，通过改变偏好向量（Preference Vector）来探索帕累托前沿（Pareto Front）。
        *   引入惩罚函数（Penalty Method）处理等式约束，将约束违规项加入目标函数。
        *   采用单层更新策略（Single-loop），避免了传统双层优化中昂贵的 Hessian 矩阵逆运算。
*   **收敛性**：证明了在确定性和随机（Stochastic）环境下，算法均能达到 $O(ST^{-1/2})$ 的有限时间收敛率（其中 $S$ 为目标数，$T$ 为迭代次数）。

### 3. 实验设计
*   **实验场景**：
    1.  **RLHF 奖励模型训练**：在 HelpSteer 数据集上进行多目标数据加权。
    2.  **LLM 对齐**：使用 Llama-3.2（1B/3B）和 Llama-3.1（8B）模型进行多准则对齐。
    3.  **多任务元学习**：在 MNIST 异构数据集上验证表示学习能力。
*   **数据集**：HelpSteer（包含 5 个评估准则：帮助性、正确性、连贯性、复杂性、冗余性）、MNIST。
*   **对比方法（Benchmark）**：
    *   **MTBL 专用算法**：MOML、MoCo、FORUM。
    *   **双层优化基准**：ITD、SOBA（结合线性标量化）。

### 4. 资源与算力
*   **硬件环境**：使用了由 **4 张 NVIDIA H100 GPU (94GB)** 组成的集群。
*   **软件框架**：基于 PyTorch 的 DistributedDataParallel (DDP) 进行分布式训练。
*   **训练时长**：论文提到其算法效率极高，例如在奖励模型任务中，WC-Penalty 在 70 秒内即可收敛，而对比方法（如 FORUM）可能需要数小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单的 MLP 到 8B 参数的大规模语言模型，实验跨度大。
*   **充分性**：
    *   进行了帕累托前沿探索实验，验证了通过调整偏好向量获取不同权衡解的能力。
    *   使用了超体积（Hypervolume）和 $\epsilon$-metric 等多目标优化专业指标进行量化对比。
    *   每组实验重复 5 次并标注了标准差，确保了结果的统计客观性。
*   **公平性**：对比了当前最先进的（SOTA）多任务双层优化算法，并在相同算力条件下评估了运行时间与收敛性能。

### 6. 主要结论与发现
*   **理论闭环**：证明了 ECMO 问题的解可以直接转化为原始 LLGC-MTBL 问题的解。
*   **性能优越**：WC-Penalty 算法在收敛速度和帕累托前沿覆盖度上显著优于现有的双层优化和多任务学习基准。
*   **扩展性强**：该方法成功应用于 8B 规模的 LLM，证明了其处理现代大规模机器学习问题的潜力。
*   **单层优势**：相比于双层循环（Double-loop）算法，单层惩罚项方法大幅降低了计算开销。

### 7. 优点
*   **理论突破**：首次在下层一般凸性下探讨多任务双层优化，并填补了 ECMO 有限时间收敛性分析的空白。
*   **算法高效**：避免了计算超梯度时常见的 Hessian 逆矩阵运算，极大地提升了在大模型上的实用性。
*   **探索能力**：WC 标量化相比线性标量化能更有效地处理非凸帕累托前沿。

### 8. 不足与局限
*   **约束限制**：目前主要聚焦于等式约束，虽然文中提到可以扩展到不等式约束，但未进行深入的理论推导和大规模实验验证。
*   **超参数敏感性**：惩罚项系数（$u, v$）的选择对收敛稳定性和约束满足程度有较大影响，实际应用中可能需要调优。
*   **LICQ 假设**：理论证明依赖于线性独立约束准则（LICQ），在某些极端退化的神经网络参数空间中，这一假设可能难以严格保证。

（完）
