---
title: "POLCA: Stochastic Generative Optimization with LLM"
title_zh: POLCA：基于大语言模型的随机生成式优化
authors: "Xuanfei Ren, Allen Nie, Tengyang Xie, Ching-An Cheng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14769v1"
tags: ["query:bopl"]
score: 9.0
evidence: 大语言模型提示的随机生成优化
tldr: 本研究针对LLM提示词及多轮智能体等复杂系统优化中的人工迭代难题，将其形式化为随机生成式优化问题。提出POLCA框架，利用LLM作为优化器，通过优先级队列平衡探索与利用，并引入ε-Net机制与LLM总结器应对反馈噪声和随机性。该方法具有理论收敛性保证，在多个基准测试中展现出卓越的采样效率和鲁棒性，显著优于现有SOTA算法。
selection_source: fresh_fetch
motivation: 旨在解决复杂LLM系统优化中人工迭代成本高以及反馈噪声、随机行为带来的不确定性挑战。
method: 提出POLCA框架，结合优先级队列、ε-Net多样性管理和LLM元学习总结器，实现对解空间的系统化探索。
result: 实验证明POLCA在τ-bench和HotpotQA等任务中，无论是确定性还是随机性场景，均取得了领先的性能和效率。
conclusion: POLCA通过结合生成式模型与随机优化理论，为自动化构建高性能LLM系统提供了一个稳健且高效的通用框架。
---

## 摘要
优化复杂系统（从大语言模型提示词到多轮智能体）传统上需要耗费大量人力的手动迭代。我们将这一挑战形式化为一个随机生成式优化问题，其中生成式语言模型充当优化器，在数值奖励和文本反馈的引导下寻找最优系统。我们提出了基于局部上下文聚合的优先级优化（POLCA），这是一个可扩展的框架，旨在处理优化过程中的随机性（如噪声反馈、采样小批量和随机系统行为），同时有效管理解空间的无约束扩张。POLCA 维护一个优先级队列来管理探索与利用之间的权衡，系统地追踪候选解及其评估历史。为了提高效率，我们集成了 ε-Net 机制以维持参数多样性，并使用大语言模型总结器在历史试验中进行元学习。我们从理论上证明了 POLCA 在随机性条件下能够收敛至近优候选解。我们在多种基准测试上评估了该框架，包括 τ-bench、HotpotQA（智能体优化）、VeriBench（代码翻译）和 KernelBench（CUDA 内核生成）。实验结果表明，POLCA 实现了鲁棒、样本高效且时间高效的性能，在确定性和随机性问题上均持续优于现有最先进算法。本工作的代码库已在 https://github.com/rlx-lab/POLCA 公开。

## Abstract
Optimizing complex systems, ranging from LLM prompts to multi-turn agents, traditionally requires labor-intensive manual iteration. We formalize this challenge as a stochastic generative optimization problem where a generative language model acts as the optimizer, guided by numerical rewards and text feedback to discover the best system. We introduce Prioritized Optimization with Local Contextual Aggregation (POLCA), a scalable framework designed to handle stochasticity in optimization -- such as noisy feedback, sampling minibatches, and stochastic system behaviors -- while effectively managing the unconstrained expansion of solution space. POLCA maintains a priority queue to manage the exploration-exploitation tradeoff, systematically tracking candidate solutions and their evaluation histories. To enhance efficiency, we integrate an $\varepsilon$-Net mechanism to maintain parameter diversity and an LLM Summarizer to perform meta-learning across historical trials. We theoretically prove that POLCA converges to near-optimal candidate solutions under stochasticity. We evaluate our framework on diverse benchmarks, including $τ$-bench, HotpotQA (agent optimization), VeriBench (code translation) and KernelBench (CUDA kernel generation). Experimental results demonstrate that POLCA achieves robust, sample and time-efficient performance, consistently outperforming state-of-the-art algorithms in both deterministic and stochastic problems. The codebase for this work is publicly available at https://github.com/rlx-lab/POLCA.

---

## 论文详细总结（自动生成）

这是一份关于论文《POLCA: Stochastic Generative Optimization with LLM》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何自动化地优化复杂的大语言模型（LLM）系统（如提示词、多轮智能体工作流、代码生成等）。
*   **研究背景**：传统的优化方法（如手动迭代或简单的自动提示词优化）在面对**随机性**（Stochasticity）时表现不佳。这种随机性源于：1) 评估时的噪声反馈（如采样小批量数据导致的误差）；2) 系统本身的随机行为（如智能体的非确定性输出）。
*   **整体含义**：论文将该问题形式化为“随机生成式优化（SGO）”，旨在利用 LLM 作为优化器，在存在噪声和无约束解空间的情况下，系统化地寻找最优配置。

### 2. 论文提出的方法论：POLCA
POLCA（Prioritized Optimization with Local Contextual Aggregation）框架包含以下核心组件：
*   **优先级队列（Priority Queue, PQ）**：维护候选解及其评估历史。通过一种类似于“乐观估计”的机制，平衡对高潜力解的利用（Exploitation）和对未知区域的探索（Exploration）。
*   **$\epsilon$-Net 机制（多样性管理）**：为了防止 LLM 优化器陷入局部最优或生成过于相似的解，POLCA 引入了 $\epsilon$-Net 概念。它通过语义相似度过滤掉与已有解过于接近的候选者，确保解空间的覆盖面。
*   **LLM 总结器（Summarizer）**：针对特定候选解，聚合其多次评估的历史反馈（包括数值奖励和文本失败原因），生成简洁的元学习上下文，帮助优化器理解该解的优缺点。
*   **算法流程**：
    1. 从 PQ 中选取当前表现最好的解。
    2. 总结器聚合该解的历史表现。
    3. LLM 优化器根据总结和反馈生成新候选解。
    4. 通过 $\epsilon$-Net 筛选多样性。
    5. 在小批量数据上评估新解并更新 PQ。
*   **理论保证**：论文提供了收敛性证明，确保在随机环境下算法能以高概率收敛至近优解。

### 3. 实验设计
*   **数据集与场景**：
    *   **$\tau$-bench**：评估工具使用和多轮对话智能体在零售/航空领域的表现（高随机性）。
    *   **HotpotQA**：多跳推理任务，优化 DSPy 风格的提示词。
    *   **VeriBench**：代码翻译任务（C++ 到 Rust）。
    *   **KernelBench**：CUDA 内核性能优化（数值优化与代码生成的结合）。
*   **对比基准（Baselines）**：
    *   **OPRO**：经典的基于 LLM 的优化方法。
    *   **DSPy (MIPROv2)**：当前最先进的提示词优化框架。
    *   **Random Search**：随机搜索作为基准线。

### 4. 资源与算力
*   **模型使用**：主要使用 `gpt-4o-2024-08-06` 作为优化器和总结器，部分实验使用 `gpt-4o-mini`。
*   **算力细节**：论文**未明确说明**具体的 GPU 硬件型号或总训练时长。由于该方法是基于 API 的生成式优化，其成本主要体现在 API 调用次数（Token 消耗）而非本地显存占用。实验中提到每个任务通常运行 10-50 轮迭代。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从智能体对话、逻辑推理到底层代码优化的四个截然不同的领域，实验设置包括了确定性环境和高度随机的环境。
*   **消融实验**：进行了充分的消融研究，验证了优先级队列、$\epsilon$-Net 和总结器各自的贡献。
*   **客观性**：通过多次运行取平均值及标准差，并对比了不同预算（采样次数）下的性能曲线，证明了 POLCA 的采样效率。实验设计较为公平，涵盖了主流的 SOTA 方法。

### 6. 主要结论与发现
*   **鲁棒性强**：在存在噪声反馈的随机场景下（如 $\tau$-bench），POLCA 显著优于 DSPy 和 OPRO。
*   **采样效率高**：POLCA 能够用更少的评估次数（Sample-efficient）达到更高的性能指标。
*   **多样性至关重要**：$\epsilon$-Net 机制有效防止了 LLM 优化器在无效的微小改动上浪费预算。
*   **元学习效应**：通过总结历史失败案例，LLM 优化器能更精准地进行“自我修正”。

### 7. 优点（亮点）
*   **理论与实践结合**：不仅提出了启发式框架，还给出了随机优化背景下的收敛性理论证明。
*   **通用性**：框架不局限于提示词优化，同样适用于代码生成和智能体工作流优化。
*   **处理噪声的能力**：通过优先级队列和聚合反馈，解决了 LLM 系统评估中常见的“分数波动”问题。

### 8. 不足与局限
*   **API 成本**：频繁调用 GPT-4o 作为优化器和总结器可能产生较高的 Token 费用。
*   **依赖基础模型能力**：优化效果高度依赖于所使用的 LLM 优化器的推理能力，若使用较弱的开源模型，效果可能打折扣。
*   **超参数敏感性**：虽然引入了 $\epsilon$-Net，但相似度阈值 $\epsilon$ 的设定可能需要针对不同任务进行微调。

（完）
