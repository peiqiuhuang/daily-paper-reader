---
title: "Advanced Image Generation: Negative Prompt Optimization and Latent Classifier Guidance"
title_zh: 高级图像生成：负向提示词优化与潜空间分类器引导
authors: "Vaddi Charan Sai Nandan Reddy, Harini B, Chandana M S"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14580v1"
tags: ["query:bopl"]
score: 8.0
evidence: 通过微调的LLM进行负向提示词优化
tldr: 本研究针对Stable Diffusion生成图像质量不佳的问题，提出了一种结合负向提示词优化与潜空间分类器引导的新型系统。通过微调LLM自动生成负向提示词，并利用CNN-RNN混合分类器在扩散过程中实时评估并回滚低质量更新。该双重引导框架显著减少了图像伪影，提升了语义忠实度。
selection_source: fresh_fetch
motivation: 旨在解决扩散模型在图像生成过程中存在的伪影问题及语义表达不准确的挑战。
method: 结合了基于LLM的负向提示词自动优化技术和基于CNN-RNN混合分类器的潜空间实时引导回滚机制。
result: 实验证明该框架能有效减少生成图像中的伪影，并显著提高图像与提示词之间的语义一致性。
conclusion: 通过负向提示词与潜空间分类器的双重引导，可以显著提升预训练扩散模型的图像生成质量。
---

## 摘要
我们提出了一种新颖的系统，该系统通过微调的序列到序列（sequence-to-sequence）大语言模型（LLM）整合了负向提示词优化，并结合潜空间分类器引导，以提升 Stable Diffusion 生成图像的质量。我们的方法能够自动生成优化的负向提示词，并采用 CNN-RNN 混合分类器来评估和引导扩散步骤，回滚低质量的潜空间更新。实验结果表明，与基准扩散模型相比，我们的双重引导框架减少了伪影并提高了语义保真度。

## Abstract
We present a novel system that integrates negative prompt optimization via a fine-tuned sequence-to-sequence LLM and latent-space classifier guidance to improve the quality of images generated by Stable Diffusion. Our approach automatically generates optimized negative prompts, and employs a CNN-RNN hybrid classifier to evaluate and guide diffusion steps, rolling back low-quality latent updates. Experimental results demonstrate that our dual-guidance framework reduces artifacts and improves semantic fidelity compared to baseline diffusion.

---

## 论文详细总结（自动生成）

这是一份关于论文《Advanced Image Generation: Negative Prompt Optimization and Latent Classifier Guidance》的结构化技术总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 Stable Diffusion 等扩散模型在文生图领域取得了巨大成功，但仍面临图像伪影（如肢体畸形）、语义漂移以及无法完全遵循用户意图的问题。
*   **核心问题**：如何通过自动化手段优化“负向提示词（Negative Prompts）”，并结合实时监控扩散过程的“潜空间引导”，来减少生成错误并提升图像质量。
*   **整体含义**：本文提出了一个双重引导框架，将大语言模型（LLM）生成的负向提示词与一个能够识别并回滚低质量生成步骤的 CNN-RNN 分类器相结合。

### 2. 方法论
该系统由三个核心模块组成：
*   **负向提示词优化（LLM 模块）**：
    *   使用微调后的 **T5-base (Seq2Seq)** 模型。
    *   在 `mikeogezi/negopt_full` 数据集上进行训练，将正向提示词映射为高质量的负向提示词（如“deformed, blurry, bad anatomy”）。
    *   推理阶段采用 **束搜索（Beam Search）** 算法以生成最有效的负向词序列。
*   **扩散引擎与 DDIM 调度**：采用确定性的 DDIM 采样规则，结合正向和负向提示词的嵌入进行噪声预测。
*   **潜空间分类器引导（CNN-RNN 混合模型）**：
    *   **CNN 编码器**：从 4 通道的潜空间张量（Latent）中提取空间特征。
    *   **双向 GRU (RNN)**：聚合连续 $k$ 个扩散步骤的特征向量，捕捉生成过程中的动态变化。
    *   **回滚机制（Rollback）**：如果分类器判定当前步骤的潜空间质量低于阈值（$\tau=0.5$），系统将回滚至上一个“接受”的状态，最多允许 5 次回滚。

### 3. 实验设计
*   **数据集**：
    *   负向提示词训练：`mikeogezi/negopt_full`（包含约 25.6 万个样本）。
    *   分类器训练：自定义的合成潜空间序列数据集（1000 条序列，其中 20% 用于测试）。
*   **基准测试（Benchmarks）**：
    *   **分类器对比**：逻辑回归（Logistic Regression）、支持向量机（SVM）、原生 CNN。
    *   **生成对比**：预训练 Stable Diffusion 基准、仅使用负向提示词优化、以及本文提出的双重引导框架。
*   **评估指标**：分类准确率（Accuracy）、ROC AUC、Brier 分数，以及定性的视觉图像对比。

### 4. 资源与算力
*   **训练细节**：
    *   T5 模型微调：Adam 优化器，学习率 $1e-3$（SFT 阶段 16 轮）和 $1e-5$（RL 阶段 8 轮）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长。考虑到使用了 T5-base 和 Stable Diffusion，通常需要至少消费级高端 GPU（如 RTX 3090/4090）或服务器级显卡。

### 5. 实验数量与充分性
*   **实验规模**：
    *   定量实验基于 200 个标注的潜空间序列样本。
    *   定性实验展示了特定提示词（如“迷雾森林中的鹿”）下的生成效果。
*   **充分性评价**：实验规模**相对较小**。虽然对比了多种分类算法，但缺乏大规模的自动化图像质量评估（如 FID 或 CLIP Score 的大规模统计）。此外，分类器的性能表现（AUC 低于 0.5）显示实验结果并不理想，反映出该模块仍处于初步探索阶段。

### 6. 主要结论与发现
*   **双重引导的有效性**：定性结果显示，自动生成的负向提示词能显著减少图像中的肢体畸形和模糊。
*   **分类器的局限性**：实验发现 CNN-RNN 分类器在潜空间质量判定上的表现**差于随机猜测**（AUC 仅为 0.4261），这表明直接在潜空间进行质量分类极具挑战性。
*   **回滚机制的潜力**：尽管分类器精度有限，但提出的“评估-回滚”逻辑为解决扩散过程中的累积误差提供了一个可行的架构思路。

### 7. 优点
*   **创新集成**：首次尝试将 LLM 驱动的提示词工程与基于 RNN 的潜空间实时监控相结合。
*   **自动化程度高**：消除了人工调试负向提示词的繁琐过程。
*   **模块化设计**：系统架构清晰，易于将分类器或扩散模型替换为更先进的版本。

### 8. 不足与局限
*   **分类器性能不佳**：分类器的各项指标（Precision, Recall, AUC）均较低，未能证明其在引导回滚方面的实际可靠性。
*   **推理开销**：引入回滚机制和 RNN 评估会显著增加单张图像的生成时间。
*   **评估维度单一**：缺乏对生成速度、内存占用以及在更多样化数据集上表现的深入分析。
*   **数据偏差**：合成的潜空间序列数据集可能无法完全覆盖真实生成过程中的复杂错误模式。

（完）
