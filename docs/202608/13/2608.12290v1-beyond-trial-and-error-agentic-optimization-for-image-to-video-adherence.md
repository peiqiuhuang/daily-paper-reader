---
title: "Beyond Trial-and-Error: Agentic Optimization for Image-to-Video Adherence"
title_zh: 超越试错：图像到视频一致性的代理优化
authors: "Aman Tyagi, Hemanth Boinpally, Jonathan Chen, Douglas Gebert, Steven Hickson"
date: 2026-08-12
pdf: "https://arxiv.org/pdf/2608.12290v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于目标导向优化的迭代提示词优化循环
tldr: "针对图生视频（I2V）模型缺乏精细控制和可靠性的问题，本文提出了一种“智能体自我改进”框架。该框架将视频合成转化为闭环目标导向优化，通过多模态大模型进行两阶段优化：首先利用迭代提示词优化确保语义一致性并检测伪影，随后采用贝叶斯优化协同搜索最优随机种子和CFG比例。实验表明，该方法显著提升了视频生成的预测性和质量，在人工评测中胜率高达69%。"
selection_source: fresh_fetch
motivation: 现有的黑盒图生视频模型由于随机性强，导致用户往往需要通过低效的试错过程来获得理想结果。
method: 提出两阶段优化框架，结合多模态大模型的提示词迭代优化与基于贝叶斯优化的超参数搜索。
result: "该方法在人类偏好研究中显著优于无引导搜索，最高取得了69%的胜率。"
conclusion: 该研究为增强视频生成模型的可控性和可靠性提供了实用方法，推动了该领域向生产级工具的演进。
---

## 摘要
现代黑盒图像到视频（I2V）模型在自动化内容创作方面提供了强大的功能，但其缺乏细粒度的控制和可靠性，给专业工作流带来了重大挑战。其固有的随机性导致文本提示词或超参数的微小变化会产生截然不同的输出，通常需要低效的暴力试错过程。为了解决这些局限性，我们引入了“代理自我改进”（Agentic Self-Improvement）框架，将视频合成重新定义为一个闭环、目标导向的优化过程。我们的框架采用一种新颖的两阶段方法，系统地探索生成参数空间。在第一阶段，迭代提示词优化循环利用多模态大语言模型（mLLM）来精炼输入提示词。这种精炼过程实现了两种自动化评估：戴维森场景图（DSG）查询确保语义一致性，以及常见错误问题（CMQ）用于伪影检测。在第二阶段，我们使用贝叶斯优化来高效地协同优化随机种子和 CFG 比例。该搜索由一套质量指标引导，包括源自 DSG 和 CMQ 评估的新型视频-文本一致性（VTA）评分。我们的框架显著优于无引导的搜索方法：在人类偏好研究中，通过我们的代理方法生成的视频明显优于基准输出，胜率高达 69%。这项工作为增强最先进视频生成模型的可预测性和控制力提供了一种实用且可扩展的方法论，推动该领域从投机性的探索迈向可靠的、生产就绪的工具。

## Abstract
Modern black-box Image-to-Video (I2V) models offer powerful capabilities in automated content creation, yet their lack of fine-grained control and reliability presents significant challenges in professional workflows. Their inherent stochasticity causes minor variations in textual prompts or hyperparameters to yield drastically different outputs often necessitating inefficient, brute-force trial-and-error processes. To address these limitations, we introduce the ``Agentic Self-Improvement" framework, which reframes video synthesis into a closed-loop, goal-directed optimization. Our framework systematically navigates the generation parameter space using a novel two-stage approach. In the first stage, an iterative prompt optimization loop uses a multimodal Large Language Model (mLLM) to refine the input prompt. This refinement implements two automated evaluations: Davidsonian Scene Graph (DSG) queries ensure semantic adherence, and Common Mistake Questions (CMQ) for artifact detection. At the second stage, we use Bayesian optimization to efficiently co-optimize stochastic seeds and CFG scales. This search is guided by a suite of quality metrics, including the novel Video-Text Adherence (VTA) score derived from the DSG and CMQ evaluations. Our framework significantly outperforms unguided search methods: in human preference studies, videos generated via our agentic approach were strongly preferred over baseline outputs, achieving win rates up to 69\%. This work provides a practical and extensible methodology for enhancing the predictability and control of state-of-the-art video generation models, moving the field beyond speculative curiosities toward reliable, production-ready tools.

---

## 论文详细总结（自动生成）

### 论文总结：超越试错——图像到视频一致性的代理优化（Agentic Optimization for I2V）

#### 1. 核心问题与整体含义
*   **研究背景**：当前的图像到视频（I2V）生成模型（如 Veo, Sora 等）虽然能生成高质量视频，但本质上是“黑盒”且具有高度随机性。
*   **核心痛点**：微小的提示词变化或超参数（如随机种子、CFG 比例）波动会导致输出结果剧烈抖动。专业创作者为了获得符合预期的视频，往往需要进行大量低效的“暴力试错”（Trial-and-Error）。
*   **研究目标**：提出一种“代理自我改进”（Agentic Self-Improvement）框架，将视频生成从开环的投机过程转变为闭环的目标导向优化过程，以提高生成结果的可控性、一致性和可靠性。

#### 2. 方法论
该框架采用两阶段优化策略，系统性地处理提示词和超参数两大变量：

*   **第一阶段：基于结构化评估的提示词优化**
    *   **问题生成**：利用多模态大模型（mLLM，如 Gemini）将用户提示词分解为两类可验证的 Yes/No 问题：
        *   **DSG（戴维森场景图）**：拆解语义元素（如：是否有女人？是否在走廊？）。
        *   **CMQ（常见错误问题）**：针对视频特有缺陷（如：肢体是否畸形？物体是否闪烁？）。
    *   **迭代精炼**：生成视频 -> mLLM 作为 VQA 评估器打分 -> 根据未通过的问题重写提示词 -> 再次生成，直到得分最大化。
*   **第二阶段：基于贝叶斯搜索的超参数优化**
    *   **核心算法**：使用贝叶斯优化（Vizier 平台）协同搜索 **CFG 比例**（连续变量 [1, 15]）和 **随机种子**（分类变量）。
    *   **多目标奖励函数**：定义了一个综合评分，包含：
        *   **RAHF**：模拟人类偏好的质量模型。
        *   **UVQ**：通用视频质量指标。
        *   **VTA（视频-文本一致性）**：本文提出的新指标，基于第一阶段 DSG/CMQ 的通过率，采用递归掩码机制处理层级依赖。
    *   **过滤机制**：自动检测并剔除包含镜头切换（Shotcut）或异常循环（Loop）的失败样本。

#### 3. 实验设计
*   **数据集/场景**：使用 **V-Bench** 的图像到视频（I2V）任务作为测试基准。
*   **基准模型**：以 Google 的 **Veo 2.0** 作为生成引擎。
*   **对比方法**：
    1.  **随机搜索（Random Search）**：模拟用户无引导的多次尝试。
    2.  **Best-of-Random**：生成相同数量的随机视频，但使用本文提出的自动化指标选出最优者（用以证明贝叶斯搜索本身的引导价值）。
*   **评估指标**：以**专家人类偏好（Human Preference）**作为主要衡量标准，辅以 V-Bench++ 自动化评分。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **实现细节**：实验主要通过 API 调用完成（Veo 2.0 视频生成 API 和 Gemini 2.5 Pro 理解 API）。
*   **优化规模**：提示词优化迭代 10 次；超参数搜索分别测试了 10 次和 100 次运行（Runs）的计算预算。

#### 5. 实验数量与充分性
*   **实验规模**：随机抽取了 100 组图像-提示词对进行端到端测试。
*   **充分性验证**：
    *   **VQA 可靠性测试**：对比了 Gemini 2.5 Pro 与人类标注的一致性，整体准确率达 87%，证明了自动化评估的有效性。
    *   **消融实验**：分别验证了提示词优化模块和超参数搜索模块的增益。
    *   **统计显著性**：通过二项式符号检验（Binomial Sign Tests）确认了胜率的统计学意义。
*   **客观性**：采用了双盲人类评测，并引入了强基准（Best-of-Random）来排除“仅靠后验筛选”的可能性。

#### 6. 主要结论与发现
*   **显著提升**：在 100 次运行预算下，该框架生成的视频在人类偏好中以 **69% 的胜率** 碾压随机基准。
*   **搜索优于筛选**：贝叶斯优化不仅能选出好视频，还能引导模型进入更高质量的参数空间，表现优于单纯的“生成后筛选”。
*   **提示词的力量**：仅通过提示词优化，就能在 27% 的案例中显著改善视频质量。
*   **指标局限**：发现现有的自动化视频指标（如 V-Bench++）在区分高质量视频时灵敏度不足，得分过于趋同，而人类能感知到明显差异。

#### 7. 优点
*   **闭环自动化**：将原本依赖直觉的“提示词工程”和“调参”转化为系统性的数学优化问题。
*   **细粒度评估**：通过 DSG 和 CMQ 将抽象的“好坏”具象化为逻辑严密的查询树。
*   **通用性强**：作为一种“包装器”框架，它可以应用于任何黑盒视频生成模型，具有很强的工程落地价值。

#### 8. 不足与局限
*   **计算开销**：为了获得一个最优视频，需要进行数十次甚至上百次视频生成和 mLLM 推理，成本较高，实时性较差。
*   **理解瓶颈**：mLLM 在处理复杂物理规律和细微运动（CMQ 部分）时的准确率（82%）仍低于语义识别（92%）。
*   **解耦优化**：目前提示词和超参数是分阶段优化的，可能忽略了两者的联合最优解（Joint Optimization）。

（完）
