---
title: Generalizable Face Forgery Detection via Separable Prompt Learning
title_zh: 基于可分离提示学习的可泛化人脸伪造检测
authors: "Enrui Yang, Yuezun Li"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17307v1"
tags: ["query:bopl"]
score: 8.0
evidence: CLIP 的可分离提示学习策略
tldr: 本研究针对人脸伪造检测的泛化性问题，提出了一种基于CLIP的可分离提示学习策略（SePL）。不同于以往仅利用CLIP视觉编码器的方法，SePL通过文本模态引导检测，利用两种提示学习方式解耦图像中的伪造相关与无关信息。实验证明，该方法在跨数据集和跨方法评估中表现优异，显著提升了检测器的泛化能力。
selection_source: fresh_fetch
motivation: 现有基于CLIP的伪造检测方法过度依赖视觉模态，而忽视了文本模态在引导深度伪造检测方面的潜力。
method: 提出可分离提示学习策略（SePL），通过两种提示学习和跨模态对齐策略来解耦图像中的伪造相关与无关信息。
result: 实验结果显示，该方法在跨数据集和跨方法评估中均取得了具有竞争性甚至领先的检测性能。
conclusion: SePL证明了通过精心设计的文本提示学习，可以有效利用CLIP的先验知识提升人脸伪造检测的泛化性。
---

## 摘要
使用 CLIP 进行人脸伪造检测最近已成为一个充满前景且日益流行的研究方向。由于通过大规模预训练获得了丰富的视觉知识，大多数现有方法通常依赖于 CLIP 的视觉编码器，而对文本模态的关注有限。鉴于文本模态的指导性，我们认为通过精心设计，可以利用它来指导 Deepfake 检测。因此，我们将重点从视觉模态转向文本模态，并提出了一种新的可分离提示学习策略（SePL），使 CLIP 能够作为一个有效的人脸伪造检测器。SePL 的核心思想是通过两种类型的提示学习来解耦图像中与伪造相关和与伪造无关的信息，其中前者用于增强检测。为了实现这种解耦，我们描述了一种跨模态对齐策略和一组专门的目标函数。大量实验表明，通过这种简单的适配，我们的方法在跨数据集和跨方法评估下，与其它方法相比取得了具有竞争力甚至更优越的性能，突显了其强大的泛化能力。代码已发布在 https://github.com/OUC-YER/SePL-DeepfakeDetection。

## Abstract
Detecting face forgeries using CLIP has recently emerged as a promising and increasingly popular research direction. Owing to its rich visual knowledge acquired through large-scale pretraining, most existing methods typically rely on the visual encoder of CLIP, while paying limited attention to the text modality. Given the instructive nature of the text modality, we posit that it can be leveraged to instruct Deepfake detection with meticulous design. Accordingly, we shift the focus from the visual modality to the text modality and propose a new Separable Prompt Learning strategy (SePL) that enables CLIP to serve as an effective face forgery detector. The core idea of SePL is to disentangle forgery-specific and forgery-irrelevant information in images via two types of prompt learning, with the former enhancing detection. To achieve this disentangle, we describe a cross-modality alignment strategy and a set of dedicated objectives. Extensive experiments demonstrate that, with this simple adaptation, our method achieves competitive and even superior performance compared to other methods under both cross-dataset and cross-method evaluation, highlighting its strong generalizability. The codes have been released at https://github.com/OUC-YER/SePL-DeepfakeDetection

---

## 论文详细总结（自动生成）

这是一份关于论文《Generalizable Face Forgery Detection via Separable Prompt Learning》（基于可分离提示学习的可泛化人脸伪造检测）的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的人脸伪造检测（Deepfake Detection）方法在面对未见过的伪造技术时泛化能力较弱。
*   **研究动机**：
    *   虽然 CLIP 等视觉-语言模型拥有丰富的预训练知识，但现有基于 CLIP 的检测方法大多只利用其**视觉编码器**，忽视了**文本模态**的指导作用。
    *   伪造痕迹（如混合伪影）通常极其细微，且与图像本身的语义内容（如人脸身份、表情）高度耦合，难以提取纯粹的伪造特征。
    *   现有的文本提示（Prompt）多为手工设计或简单的前缀学习，难以精准捕捉隐蔽的伪造线索。

### 2. 方法论
论文提出了 **SePL（Separable Prompt Learning）** 策略，核心是通过文本提示引导视觉特征的解耦：
*   **可分离提示设计**：设计了两种类型的可学习提示：
    *   **伪造相关提示 ($P_A$)**：捕捉伪造痕迹。
    *   **伪造无关提示 ($P_B$)**：捕捉图像语义内容（如身份、背景）。
    *   每个提示由“实例级条件向量”（由轻量级 Meta-Network 生成）和“全局基准向量”组成，以兼顾图像特异性和通用性。
*   **跨模态对齐策略 (CMA)**：利用跨模态注意力机制（Cross-Attention），将视觉特征作为 Query，文本嵌入作为 Key 和 Value，从而将原始视觉特征分解为对齐后的伪造特征 ($f_A$) 和语义特征 ($f_B$)。
*   **两阶段训练流程**：
    *   **第一阶段**：仅训练语义提示 ($P_B$)，利用 CLIP 原有的视觉-语言对齐先验，使其锚定图像的语义内容。
    *   **第二阶段**：联合优化所有组件，引入**非对称对齐损失**（真图远离 $P_A$，假图靠近 $P_A$；所有图靠近 $P_B$）和**正交解耦损失**（确保 $f_A$ 和 $f_B$ 互不相关）。
*   **损失函数组合**：包括分类损失 ($L_{cls}$)、解耦损失 ($L_{dis}$)、提示多样性损失 ($L_{div}$)、跨模态对齐损失 ($L_{align}$) 和监督对比损失 ($L_{con}$)。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：FaceForensics++ (FF++) 的 c23 版本。
    *   **跨数据集测试**：Celeb-DF-v2, DFD, DFDC, DFDCP, WDF。
    *   **跨方法测试**：针对 8 种不同的伪造算法（如 UniFace, BleFace, SimSwap 等）进行评估。
    *   **通用伪造测试**：在 Universal-FakeDetect 数据集上验证对 GAN 和扩散模型的检测能力。
*   **Benchmark 与对比方法**：对比了 17 种主流方法，包括传统的频率域方法（F3Net, SPSL）、特征解耦方法（RECCE, CORE）以及最新的基于 CLIP 的方法（ForAda, Effort, RepDFD）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 3090 GPU**。
*   **软件环境**：CUDA 11.3, PyTorch 1.11.0。
*   **训练细节**：Batch size 为 24，使用 Adam 优化器，学习率为 $2 \times 10^{-4}$。
*   **时长**：文中未明确给出具体的训练总时长，但提到使用了 10% 的 Warm-up 比例。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个主流跨数据集测试和 8 种跨方法测试。
    *   进行了详尽的**消融实验**，验证了各个损失项、不同 CLIP 骨干网络（ViT-B/16, B/32, L/14）、提示长度、预训练阶段以及特征融合策略的影响。
    *   提供了 **t-SNE 可视化**、**Grad-CAM 激活图**以及在 6 种扰动（如噪声、压缩、模糊）下的**鲁棒性测试**。
*   **评价**：实验设计非常充分且客观。通过跨数据集和跨方法的双重验证，证明了该方法在不同分布下的稳定性。

### 6. 主要结论与发现
*   **性能领先**：SePL 在跨方法评估中取得了 **0.976 AUC** 的平均分，比之前的最优方法（ForAda）提升了 2.7%。
*   **解耦有效性**：可视化证明了 SePL 能有效将伪造线索从复杂的面部语义中分离出来，伪造分支的特征空间具有更好的类间可分性。
*   **文本模态潜力**：证明了通过精心设计的提示学习，文本模态可以显著增强视觉编码器捕捉细微伪造痕迹的能力。

### 7. 优点（亮点）
*   **新颖的视角**：将研究重点从单纯的视觉适配转向了“文本引导的视觉解耦”，填补了 CLIP 在该领域文本利用不足的空白。
*   **非对称对齐**：针对伪造检测任务的特殊性（真图无伪造痕迹，假图有），设计了非对称的对齐逻辑，符合物理逻辑。
*   **即插即用与轻量化**：基于 PEFT（LoRA）和轻量级 Meta-Network，无需全参数微调，训练效率高。

### 8. 不足与局限
*   **固定提示长度**：目前使用固定的提示长度（K=16），可能无法完美适应所有复杂程度不同的伪造类型。
*   **极端环境下的鲁棒性**：在 DFDC 等包含重度压缩和野外复杂环境的数据集上，虽然表现优秀，但相比专门针对视觉增强的方法（如 ForAda），领先优势有所缩小，说明文本引导在极端噪声下可能受到视觉特征质量的限制。
*   **应用限制**：主要针对人脸伪造，虽然在通用图像伪造上做了扩展实验，但在更广泛的 AIGC 内容检测上的表现仍需进一步验证。

（完）
