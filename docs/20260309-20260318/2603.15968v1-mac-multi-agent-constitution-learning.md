---
title: "MAC: Multi-Agent Constitution Learning"
title_zh: MAC：多智能体宪法学习
authors: "Rushil Thareja, Gautam Gupta, Francesco Pinto, Nils Lukas"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15968v1"
tags: ["query:bopl"]
score: 9.0
evidence: 使用代理网络优化结构化提示以学习宪法规则
tldr: "针对宪法AI中规则编写依赖人工且自动优化效果不佳的问题，本文提出MAC（多智能体宪法学习）。该方法通过多智能体协作网络，对结构化规则集进行自动迭代优化（接受、编辑或拒绝更新）。实验表明，MAC在PII识别和工具调用任务中表现优异，性能提升超50%，且无需参数微调即可达到与SFT相当的效果，兼具可解释性与高效性。"
selection_source: fresh_fetch
motivation: 现有的LLM提示词优化器在自动学习宪法规则时，存在对标注数据依赖性强且缺乏结构化管理导致优化效果受限的问题。
method: 提出一种多智能体框架，通过专门的智能体网络对结构化规则集进行动态更新，并利用MAC+通过成功轨迹强化高奖励的规则更新。
result: "在PII标记和工具调用任务中，MAC比现有提示词优化方法性能提升超过50%，且达到了与监督微调（SFT）和GRPO相当的水平。"
conclusion: MAC提供了一种可解释、可审计且高效的自动规则学习方案，证明了结构化提示词优化在无需更新模型参数的情况下具有巨大潜力。
---

## 摘要
宪法式 AI (Constitutional AI) 是一种基于自然语言编写的规则集来监督和控制大语言模型 (LLM) 的方法。这些规则通常由人类专家编写，但原则上，只要有足够的针对目标行为的训练数据，就可以自动学习。现有的基于 LLM 的提示词优化器尝试实现这一点，但在学习“宪法”方面效果不佳，原因在于：(i) 它们需要大量的标注样本；(ii) 优化后的提示词缺乏结构，导致随着提示词长度增加，改进效果逐渐减弱。为了解决这些局限性，我们提出了多智能体宪法学习 (MAC)，它通过一个由具有专门任务（接受、编辑或拒绝规则更新）的智能体组成的网络，对以规则集形式表示的结构化提示词进行优化。我们还提出了 MAC+，它通过在成功轨迹上训练智能体来强化能够带来更高奖励的更新，从而提升性能。我们在个人身份信息 (PII) 标记任务上评估了 MAC，这是一个标签有限且可解释性至关重要的分类任务，并证明了它可以泛化到诸如工具调用等其他智能体任务。MAC 的表现优于近期的提示词优化方法 50% 以上，生成了人类可读且可审计的规则集，并在无需参数更新的情况下，实现了与监督微调和 GRPO 相当的性能。

## Abstract
Constitutional AI is a method to oversee and control LLMs based on a set of rules written in natural language. These rules are typically written by human experts, but could in principle be learned automatically given sufficient training data for the desired behavior. Existing LLM-based prompt optimizers attempt this but are ineffective at learning constitutions since (i) they require many labeled examples and (ii) lack structure in the optimized prompts, leading to diminishing improvements as prompt size grows. To address these limitations, we propose Multi-Agent Constitutional Learning (MAC), which optimizes over structured prompts represented as sets of rules using a network of agents with specialized tasks to accept, edit, or reject rule updates. We also present MAC+, which improves performance by training agents on successful trajectories to reinforce updates leading to higher reward. We evaluate MAC on tagging Personally Identifiable Information (PII), a classification task with limited labels where interpretability is critical, and demonstrate that it generalizes to other agentic tasks such as tool calling. MAC outperforms recent prompt optimization methods by over 50%, produces human-readable and auditable rule sets, and achieves performance comparable to supervised fine-tuning and GRPO without requiring parameter updates.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAC: Multi-Agent Constitution Learning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：宪法式 AI（Constitutional AI）通过自然语言规则集（即“宪法”）来引导 LLM 行为，具有高透明度和可审计性。然而，手动编写这些规则既昂贵又耗时。
*   **现有局限**：现有的自动提示词优化（APO）方法在学习“宪法”时表现不佳，主要因为它们通常生成非结构化的长文本，且在标注数据有限的情况下难以收敛，随着提示词增长，改进效果会迅速递减。
*   **核心目标**：提出一种能够从少量示例中自动学习、迭代并优化**结构化规则集**的方法，使其既能达到高性能，又保持人类可读性。

### 2. 方法论
MAC 框架将宪法学习视为一个多智能体协作的优化过程，核心思想是将“宪法”模块化为一组独立的规则。
*   **核心智能体网络**：
    *   **ANNOTATOR（标注者）**：根据当前宪法执行下游任务（如 PII 标记）。
    *   **DECISION（决策者）**：分析标注错误（假阳性/假阴性），决定更新策略：添加（ADD）、编辑（EDIT）或删除（REMOVE）规则。
    *   **CREATOR（创建者）**：根据决策者的建议起草新规则。
    *   **EDITOR（编辑器）**：对现有特定规则进行针对性修改。
*   **算法流程**：
    1.  **初始化**：宪法初始为空。
    2.  **迭代优化**：在训练批次上运行标注者，计算 F1 分数并识别错误样本。
    3.  **提议与验证**：决策者提出更新建议，创建者或编辑器生成候选宪法。如果候选宪法在当前批次上的表现优于原宪法，则接受更新。
    4.  **变体增强**：
        *   **MAC+**：通过大模型（14B）生成的成功优化轨迹，对小模型（3B）智能体进行监督微调（SFT），提升其推理和规则编写能力。
        *   **reMAC**：在推理时引入检索增强，将训练集中的相似示例作为 Few-shot 加入提示词，辅助规则执行。

### 3. 实验设计
*   **数据集与场景**：
    *   **PII 标记（核心任务）**：涵盖法律（ECHR）、医疗（MACCROBAT）和金融（PUPA）三个领域。这些领域对隐私敏感且规则定义各异。
    *   **工具调用（泛化任务）**：使用 Berkeley Function Calling Leaderboard (BFCL) 评估其在智能体任务上的通用性。
*   **对比方法（Baselines）**：
    *   **预训练工具**：Microsoft Presidio (基于 BERT/SpaCy/Stanza), NVIDIA GLiNER-PII。
    *   **提示词优化器**：GEPA (基于进化的优化), MIPRO (基于提议的优化)。
    *   **训练方法**：监督微调 (SFT) 和 GRPO（强化学习），作为性能上限参考。

### 4. 资源与算力
*   **模型使用**：采用了 Qwen 2.5 Instruct 系列模型（3B, 7B, 14B）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **计算预算**：为了公平对比，所有优化方法（MAC, GEPA, MIPRO）均限制在相同的计算预算内（480 次前向传播/评估）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个领域、3 种模型规模上进行了交叉验证，共计 9 组主要对比实验。
*   **消融与变体实验**：
    *   对比了 reMAC 在不同检索数量（k=1 到 5）下的表现。
    *   对比了 MAC+ 在跨领域（用医疗/金融数据训练，在法律数据上测试）下的泛化能力。
    *   进行了“LLM-as-a-judge”实验来评估各子智能体的胜率。
*   **评价**：实验设计较为充分且客观。通过固定计算预算确保了与 APO 方法对比的公平性；通过跨领域测试验证了 MAC+ 学习的是“任务结构”而非“数据记忆”，有效规避了隐私数据泄露风险。

### 6. 主要结论与发现
*   **性能卓越**：MAC 在 PII 标记任务上比现有的提示词优化方法（GEPA/MIPRO）性能提升超过 **50%**。
*   **媲美微调**：在无需更新模型参数的情况下，MAC 在 14B 模型上的表现接近甚至超过了经过全量微调的 SFT 和 GRPO 模型。
*   **小模型友好**：通过 MAC+ 的智能体蒸馏，3B 模型的规则编写能力显著增强，胜率提升最高达 32 倍。
*   **可解释性**：生成的宪法规则（平均约 9 条）清晰易读，能够直接被人类审计和修改。

### 7. 优点
*   **结构化优势**：相比于生成一整段混乱的提示词，MAC 生成的规则集具有模块化特征，易于维护。
*   **隐私保护**：通过推理侧的规则引导而非权重微调，降低了模型记忆敏感训练数据的风险。
*   **多智能体解耦**：将复杂的优化任务分解为决策、创建、编辑三个子任务，降低了对单个 LLM 推理能力的门槛。

### 8. 不足与局限
*   **规则冲突风险**：随着规则数量增加，不同规则之间可能存在逻辑冲突，论文未深入探讨如何自动检测并解决规则冲突。
*   **指标依赖性**：MAC 的优化高度依赖于验证集指标（如 F1）。如果指标本身存在偏差或噪声，学习到的规则可能会过拟合于错误的标注。
*   **上下文压力**：虽然规则集比非结构化提示词更简洁，但当规则极其复杂时，仍可能触及小模型的上下文窗口限制或导致注意力分散。

（完）
