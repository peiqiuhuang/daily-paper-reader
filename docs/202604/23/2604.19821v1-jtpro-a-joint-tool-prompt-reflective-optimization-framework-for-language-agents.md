---
title: "JTPRO: A Joint Tool-Prompt Reflective Optimization Framework for Language Agents"
title_zh: JTPRO：一种面向语言智能体的联合工具-提示词反思优化框架
authors: "Sandip Ghoshal, Anshul Mittal, Jyotika Singh, Miguel Ballesteros, Weiyi Sun, Fang Tu, Shailender Singh, Yassine Benajiba, Fahad Shah, Sujeeth Bharadwaj, Sujith Ravi, Dan Roth"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.19821v1"
tags: ["query:bopl"]
score: 8.0
evidence: 提示词与工具模式的迭代协同优化
tldr: JTPRO是一个针对大语言模型智能体的联合工具-提示词反射优化框架。针对工具数量增加及领域特定化导致的工具误选和参数填充错误，该框架通过迭代反射机制，同时优化全局指令和单个工具的Schema描述。在多工具基准测试中，JTPRO在工具选择和参数填充准确率上显著优于现有基准，提升了智能体在复杂工具环境下的可靠性。
selection_source: fresh_fetch
motivation: 现有的LLM智能体在面对大量或专业工具时，常因提示词过于通用和工具描述不清晰而导致调用错误。
method: 提出JTPRO框架，利用基于运行轨迹的反射机制，协同优化全局指令与每个工具的Schema及参数描述。
result: "在多项基准测试中，JTPRO在综合成功率（OSR）上比CoT和GEPA等强基准提升了5%至20%。"
conclusion: 联合优化指令和工具Schema比单独优化其中之一更有效且更具鲁棒性，能显著提升工具调用的准确性。
---

## 摘要
增强了外部工具的大语言模型（LLM）智能体在工具数量增加且变得具有领域特定性时，往往表现不佳。在这种情况下，模糊的工具描述和定义不明确的智能体指令经常导致工具误选以及错误的槽位/值实例化。我们假设这归因于两个根本原因：一是忽略了工具特定细微差别的通用化、“一刀切”式提示词；二是定义不全的工具模式（schema），缺乏关于何时以及如何使用每个工具以及如何格式化其参数的明确指导。我们提出了联合工具-提示词反思优化（JTPRO），这是一个旨在提高轨迹监督（trace-supervised）设置下工具调用可靠性的框架。该框架通过迭代使用基于展开（rollout-driven）的反思，共同优化全局指令和每个工具的模式/参数描述，从而在大规模工具库中实现准确的工具选择和参数实例化。JTPRO 旨在仅保留正确消除歧义和槽位填充所需的工具局部线索。我们在多工具基准测试上评估了 JTPRO，这些测试涵盖了不同数量的工具，并使用了三个指标：工具选择准确率（TSA）、槽位填充准确率（SFA）和总成功率（OSR）（即正确的工具 + 正确的槽位 + 正确的值）。JTPRO 在 OSR 上始终优于强基准模型，包括 CoT 风格的智能体以及 GEPA 等反思性提示词优化器，相对提升达 5%-20%。消融实验表明，联合优化指令和工具模式比孤立地优化其中任何一个组件都更有效且更具鲁棒性。

## Abstract
Large language model (LLM) agents augmented with external tools often struggle as number of tools grow large and become domain-specific. In such settings, ambiguous tool descriptions and under-specified agent instructions frequently lead to tool mis-selection and incorrect slot/value instantiation. We hypothesize that this is due to two root causes: generic, one-size-fits-all prompts that ignore tool-specific nuances, and underspecified tool schemas that lack clear guidance on when and how to use each tool and how to format its parameters. We introduce Joint Tool-Prompt Reflective Optimization (JTPRO), a framework for improving tool-calling reliability in trace-supervised settings by iteratively using rollout-driven reflection to co-optimize global instructions and per-tool schema/argument descriptions for accurate tool selection and argument instantiation in large tool inventories. JTPRO is designed to preserve only tool-local cues needed for correct disambiguation and slot filling. We evaluate JTPRO across multi-tool benchmarks, which account for different number of tools using three metrics: Tool Selection Accuracy (TSA), Slot Filling Accuracy(SFA), and Overall Success Rate(OSR) (correct tool + correct slots + correct values). JTPRO consistently outperforms strong baselines, including CoT-style agents, and reflective prompt optimizers such as GEPA by 5%-20% (relative) on OSR. Ablations show that joint optimization of instructions and tool schemas is more effective and robust than optimizing either component in isolation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **JTPRO**（Joint Tool-Prompt Reflective Optimization）的框架，旨在通过联合优化全局指令和工具模式（Schema）描述，提升大语言模型（LLM）智能体在处理大规模、专业化工具库时的可靠性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当工具数量增加（如达到 1000 个）或涉及特定领域专业知识时，LLM 智能体常出现“工具误选”和“参数填充错误”。
*   **研究动机**：作者认为失败源于两个根本原因：
    1.  **提示词通用化**：全局提示词过于笼统，忽略了工具间的细微差别。
    2.  **工具 Schema 定义不全**：缺乏明确的调用时机指导和参数格式规范。
*   **背景**：现有的优化方法通常只针对全局提示词或仅针对工具文档，且在处理大规模工具库时的参数实例化（Slot Filling）准确率往往成为整体成功率的瓶颈。

### 2. 方法论
JTPRO 是一种**无需微调**的、基于上下文的反射优化框架，其核心流程如下：
*   **联合优化（Joint Optimization）**：同时迭代更新全局指令 $P$ 和每个工具的 Schema 描述 $\{T_i\}$。
*   **反思驱动的迭代循环**：
    1.  **帕累托采样（Pareto Selection）**：从候选池中选择表现最优的上下文组合。
    2.  **诊断与编辑**：在训练集上运行智能体，通过 `DIAGNOSE` 函数识别工具混淆、缺失参数或格式错误，由“反射器”（Reflector）提出针对性的局部修改建议。
    3.  **增量合并（Merge-with-Best）**：将新生成的草案与当前验证集表现最好的版本合并，形成“不断增长的行动手册”。
*   **槽位语义全局化（Slot-Semantics Globalization）**：这是该框架的关键技术。它识别跨工具重复出现的参数（如日期格式、数值边界、货币单位），将其提取为全局规则，并在具体工具描述中用简短的指针代替。这既减少了上下文冗余，又确保了参数处理的一致性。

### 3. 实验设计
*   **数据集**：
    1.  **ETID (Enterprise Tool Inventory Dataset)**：合成的领域特定数据集，包含 124 个工具，侧重于复杂的参数填充。
    2.  **ToolACE**：用于测试工具规模扩展性，工具数量从 300 扩展到 1000 个。
    3.  **SEAL-Tools**：测试并行多工具调用的场景。
*   **评估指标**：
    *   **TSA**：工具选择准确率。
    *   **SFA**：槽位填充准确率（在工具选择正确的前提下）。
    *   **OSR**：总成功率（工具、槽位、值全部正确）。
*   **对比方法**：基准 CoT 智能体、GEPA（反射式提示词优化器）、MIPRO 等。
*   **实验模型**：使用了 GPT-4o mini、o3-mini 和 GPT-5（注：文中提及 GPT-5，可能指代其测试时的前沿模型版本）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **模型调用**：提到使用 `o3-mini` 作为反射器模型，实验过程涉及多次迭代 rollouts，属于推理侧开销较大的优化方法，但由于无需模型微调，其资源消耗主要集中在 API 调用或推理算力上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从单工具到多工具并行调用的多种场景。
    *   在 ToolACE 上做了 500 和 1000 个工具的扩展性实验。
    *   在 ETID 上设置了低监督学习制度（每个意图仅 1/2/4 个示例）。
    *   进行了消融实验，验证了“联合优化”优于“单一组件优化”。
*   **充分性与公平性**：实验设置了相同的优化预算（rollouts 次数一致），并进行了 5-10 次独立运行取平均值，以减少随机性影响。整体实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能提升**：JTPRO 在总成功率（OSR）上比强基准提升了 **5%–20%**。
*   **参数填充是瓶颈**：实验证明，即使工具选择正确，错误的参数填充也会严重拖累 OSR。JTPRO 通过优化 Schema 显著提升了 SFA。
*   **规模鲁棒性**：在工具增加到 1000 个时，JTPRO 的领先优势更加明显，证明了其在处理工具混淆方面的有效性。
*   **联合优化的必要性**：消融实验显示，仅优化指令或仅优化 Schema 的效果均不如两者协同优化。

### 7. 优点与亮点
*   **协同进化**：打破了以往只改提示词不改工具定义的局限，实现了指令与 Schema 的同步优化。
*   **上下文效率**：通过“槽位语义全局化”巧妙地解决了大规模工具库导致的上下文窗口膨胀问题，提高了推理效率。
*   **可解释性**：生成的优化结果是纯文本的指令和描述，具有高度的可解释性和可维护性。

### 8. 不足与局限
*   **工作流限制**：目前仅评估了单步或并行工具调用，**未涉及长程序列化工具链**（即前一个工具的输出作为后一个工具的输入）。
*   **结构复杂度**：实验未深入测试深度嵌套的 JSON 参数结构。
*   **数据闭源**：核心数据集 ETID 为内部合成且未公开发布，限制了外部复现。
*   **评估范围**：主要关注“调用级”准确性，未涵盖工具执行后的响应解析及后续业务逻辑处理。

（完）
