---
title: "From Failing to Passing: Evolving Natural Language Prompt Optimization Rules for LLM Code Generation"
title_zh: 从失败到通过：为大语言模型代码生成演化自然语言提示词优化规则
authors: "Amal Akli, Melissa Akli, Cedric Richter, Mike Papadakis, Yves Le Traon"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05121v1"
tags: ["query:bopl"]
score: 9.0
evidence: 基于搜索的方法来演化自然语言提示优化规则
tldr: 针对大语言模型在代码生成中对提示词高度敏感的问题，本文提出了一种基于搜索的方法来演化自然语言转换规则。通过引入DUALFIX流水线，将演化的规则与执行反馈修复相结合，有效解决了规范和实现层面的错误。该方法具有通用性和跨模型迁移能力，在LiveCodeBench和APPS基准测试中显著提升了修复率，比Self-Fix效果好3-5倍。
selection_source: fresh_fetch
motivation: 旨在利用LLM对提示词表述的敏感性，通过优化自然语言提示词规则来提升代码生成的准确率。
method: 提出一种搜索算法来演化通用的提示词转换规则，并结合执行反馈构建了名为DUALFIX的两阶段修复流水线。
result: "实验表明DUALFIX能修复10-30%的失败案例，在多个基准测试上表现优于Self-Fix，且规则具有良好的跨模型迁移性。"
conclusion: 演化的自然语言提示词规则是提升LLM代码生成能力的有效手段，且具备错误无关性和模型通用性。
---

## 摘要
众所周知，大语言模型对提示词的表述方式非常敏感。即使是措辞上的微小变化也可能显著降低性能。这种敏感性揭示了一个机会：如果提示词的措辞会损害性能，那么它是否也能被用来提升性能？为了研究这个问题，我们引入了一种基于搜索的方法，用于识别并演化出一组对代码生成性能具有强大下游影响的自然语言转换规则。随后，我们提出了 DUALFIX，这是一个分阶段的修复流水线，它将演化出的转换规则与执行反馈修复相结合，同时解决规范级和实现级的失败。我们方法的一个关键优势在于其通用性：演化出的规则与具体错误无关，可在不同问题间重复使用，并能在不同模型间迁移。我们在两个具有挑战性的基准测试 LiveCodeBench 和 APPS 上，针对三个模型将 DUALFIX 与执行反馈修复基线进行了评估。结果表明，演化出的转换规则修复了 10-30% 的失败案例，其中包括 12-17% 仅靠基于执行的修复无法解决的失败。总体而言，在所有评估设置中，DualFix 恢复了高达 30% 的基线失败案例，修复的失败案例数量是 Self-Fix 的 3 到 5 倍。此外，我们还展示了在一个模型上演化出的规则可以零样本迁移到其他模型，在无需任何重新优化的前提下优于执行反馈修复。

## Abstract
Large language models are known to be sensitive to prompt formulation. Even minor variations in wording can substantially degrade performance. This sensitivity reveals an opportunity: if prompt phrasing can harm performance, can it be used to improve it? To investigate this question, we introduce a search-based approach that identifies and evolves a set of natural language transformation rules with strong downstream effects on coding performance. We then propose DUALFIX, a staged repair pipeline that combines the evolved transformation rules with execution-feedback repair, addressing both specification-level and implementation-level failures. A key strength of our approach lies in its generality: the evolved rules are error-agnostic, reusable across problems, and transferable across models. We evaluate DUALFIX against execution-feedback repair baselines across three models on two challenging benchmarks, LiveCodeBench and APPS. Our results show that the evolved transformations fix from 10-30% of failing cases, including 12-17% of failures that execution-based repair alone cannot resolve. Overall, DualFix recovers up to 30% of baseline failures and fixes 3-5 times more failing cases than Self-Fix across all evaluated settings. Furthermore, we also show that rules evolved on one model transfer zero-shot to other models, outperforming execution-feedback repair without any re-optimization.

---

## 论文详细总结（自动生成）

这是一份关于论文《From Failing to Passing: Evolving Natural Language Prompt Optimization Rules for LLM Code Generation》的深度结构化总结：

### 1. 核心问题与整体含义
该研究针对大语言模型（LLM）在代码生成任务中对提示词（Prompt）表述高度敏感的问题展开。研究动机在于：既然微小的措辞变化能导致模型生成错误代码，那么是否可以通过系统性地优化提示词表述来修复这些错误？论文指出，现有的代码修复方法（如 Self-Fix）主要依赖执行反馈（报错信息），但往往无法解决因任务描述（Specification）本身模糊或不符合模型偏好而导致的失败。因此，本文旨在通过演化出一套通用的自然语言转换规则，从“规范层面”和“实现层面”双管齐下提升代码生成成功率。

### 2. 方法论
论文提出了两个核心组件：**RuleEvol**（规则演化框架）和 **DualFix**（分阶段修复流水线）。

*   **RuleEvol（规则演化）：**
    *   **核心思想：** 利用遗传算法（Genetic Search）在离线阶段自动发现并优化提示词转换规则。
    *   **规则格式：** 采用 `IF-THEN-EXCEPT` 结构（例如：如果存在数学符号，则将其转换为平铺的自然语言描述，除非该符号是标准 API 的一部分）。
    *   **演化流程：** 包含初始化、父代选择（基于帕累托前沿以保持多样性）、变异操作（包括反射变异、删除变异和合并变异）以及变异门控（仅保留能提升性能的变异）。
*   **DualFix（修复流水线）：**
    *   **第一阶段（基于错误的修复）：** 当初始代码生成失败时，利用 LLM 根据执行报错信息重写任务描述并重新生成代码。
    *   **第二阶段（基于规则的修复）：** 如果第一阶段失败，则应用 RuleEvol 演化出的通用规则重写原始任务描述。这种方法不依赖具体的报错信息，旨在消除描述中的歧义或复杂格式。

### 3. 实验设计
*   **数据集/基准测试：**
    *   **LiveCodeBench (LCB)：** 包含来自 Codeforces、LeetCode 等平台的实时编程题目，有效防止数据污染。
    *   **APPS：** 选取其中的“面试（Interview）”难度级别题目。
*   **评估模型：**
    *   代码生成模型（Generator）：Qwen2.5-Coder-7B、Codestral-22B、Claude Haiku 4.5。
    *   重写与变异模型：GPT-4o-mini 和 GPT-5-mini（用于规则演化过程）。
*   **对比方法：**
    *   **Direct Generation：** 原始提示词直接生成。
    *   **Self-Fix：** 模型根据报错信息自行修复代码（业界常用基线）。
    *   **Iterative-Error：** 多轮基于错误的重写修复。

### 4. 资源与算力
*   **GPU 使用：** Qwen2.5-Coder-7B 在单张 GPU 上运行（Batch Size 16）；Codestral-22B 分布在两张 GPU 上运行（Batch Size 8）。
*   **演化参数：** 规则演化运行 10 个世代，每个世代包含 2 个子代，帕累托前沿大小为 7。
*   **外部 API：** 规则的重写和变异依赖于 OpenAI 的模型接口。论文未明确提及具体的训练总时长，但强调了规则一旦演化完成，在推理阶段是轻量级且可复用的。

### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度评估：** 涵盖了不同规模的模型（7B 到 22B）和不同类型的基准测试。
*   **消融实验：** 分别验证了“基于规则”和“基于错误”两个组件的独立贡献，证明了两者修复的错误类型具有互补性（重合度低）。
*   **迁移性实验：** 验证了在一个模型（如 Codestral）上演化出的规则，在不经过任何调整的情况下，直接应用于另一个模型（如 Claude）时的有效性。
*   **统计严谨：** 采用了固定的随机种子进行数据集划分，并设置了严格的门控机制防止规则过拟合。

### 6. 主要结论与发现
*   **性能飞跃：** DualFix 修复了 10-30% 的基线失败案例，修复能力是传统 Self-Fix 的 3 到 5 倍。
*   **规则的价值：** 演化出的规则即使在不参考报错信息的情况下，也能修复 12-17% 的错误，说明很多失败源于任务描述的表述方式。
*   **跨模型通用性：** 规则具有显著的零样本迁移能力，表明 RuleEvol 捕捉到了 LLM 处理自然语言规范时的通用偏好（如将复杂的 LaTeX 数学公式转换为简单的英文描述）。
*   **互补性：** 规范层面的重写（规则）与实现层面的修复（错误反馈）针对的是不同的失效模式，结合使用效果最佳。

### 7. 优点
*   **错误无关性：** 演化出的规则不需要知道代码哪里错了，具有很强的预处理潜力。
*   **可解释性：** 规则以自然语言形式存在（IF-THEN），人类开发者可以理解并学习这些优化技巧。
*   **高效复用：** 规则只需离线演化一次，即可在推理阶段低成本地应用于各种新问题。
*   **突破“自修复僵局”：** 解决了 LLM 在面对复杂错误时，仅靠报错信息无法跳出错误逻辑循环的问题。

### 8. 不足与局限
*   **语言限制：** 实验主要集中在 Python 语言和竞赛/面试类题目，在大型软件工程项目或多语言环境下的有效性尚待验证。
*   **重写成本：** 虽然规则应用是轻量的，但 DualFix 流水线涉及多次 LLM 调用（重写+生成），在对实时性要求极高的场景下可能存在延迟。
*   **语义漂移风险：** 尽管设有约束，但在复杂的重写过程中，仍存在极小概率改变原始题目含义的风险。
*   **依赖闭源模型：** 规则演化过程依赖于高性能的闭源模型（如 GPT-5-mini），对于完全开源环境的适配性未深入讨论。

（完）
