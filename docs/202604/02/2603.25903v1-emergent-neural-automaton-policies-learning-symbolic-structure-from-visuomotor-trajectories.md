---
title: "Emergent Neural Automaton Policies: Learning Symbolic Structure from Visuomotor Trajectories"
title_zh: 涌现神经自动机策略：从视觉运动轨迹中学习符号结构
authors: "Yiyuan Pan, Xusheng Luo, Hanjiang Hu, Peiqi Yu, Changliu Liu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25903v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于机器人学习的双层神经符号策略
tldr: "针对长程机器人任务中端到端模型缺乏结构先验及神经符号方法依赖手工设计的难题，本文提出ENAP框架。该框架通过自适应聚类和L*算法从视觉运动演示中自动推断出Mealy状态机作为高层规划器，并结合底层残差网络进行连续控制。ENAP无需任务特定标签，在低数据量下性能优于现有VLA模型，显著提升了任务的解释性和样本效率。"
selection_source: fresh_fetch
motivation: 旨在解决长程机器人学习中端到端策略缺乏结构化推理能力以及传统神经符号方法过度依赖手工先验的问题。
method: "提出一种双层架构，利用L*算法从数据中推断离散状态机作为高层规划器，并配合底层残差网络实现精确的连续控制。"
result: "在复杂操作和长程任务中，ENAP在低数据量下的表现比最先进的VLA策略高出多达27%，且具有更强的可解释性。"
conclusion: ENAP证明了从视觉运动轨迹中自动涌现符号结构的可行性，为高效、可解释的长程机器人任务学习提供了新路径。
---

## 摘要
将机器人学习扩展到长时程任务仍然是一个巨大的挑战。虽然端到端策略通常缺乏有效长期推理所需的结构先验，但传统的神经符号方法又严重依赖于手工设计的符号先验。为了解决这一问题，我们引入了 ENAP（涌现神经自动机策略），这是一个允许双层神经符号策略从视觉运动演示中自适应涌现的框架。具体而言，我们首先采用自适应聚类和 L* 算法的扩展，从视觉运动数据中推断出 Mealy 状态机，该状态机作为可解释的高层规划器，能够捕捉潜在的任务模式。然后，这种离散结构引导底层反应式残差网络通过行为克隆 (BC) 学习精确的连续控制。通过利用离散转移和连续残差显式建模任务结构，ENAP 在不需要任务特定标签的情况下，实现了高样本效率和可解释性。在复杂操作和长时程任务上的广泛实验表明，ENAP 在低数据量情况下的性能优于最先进的 (SoTA) 端到端 VLA 策略高达 27%，同时提供了机器人意图的结构化表示（图 1）。

## Abstract
Scaling robot learning to long-horizon tasks remains a formidable challenge. While end-to-end policies often lack the structural priors needed for effective long-term reasoning, traditional neuro-symbolic methods rely heavily on hand-crafted symbolic priors. To address the issue, we introduce ENAP (Emergent Neural Automaton Policy), a framework that allows a bi-level neuro-symbolic policy adaptively emerge from visuomotor demonstrations. Specifically, we first employ adaptive clustering and an extension of the L* algorithm to infer a Mealy state machine from visuomotor data, which serves as an interpretable high-level planner capturing latent task modes. Then, this discrete structure guides a low-level reactive residual network to learn precise continuous control via behavior cloning (BC). By explicitly modeling the task structure with discrete transitions and continuous residuals, ENAP achieves high sample efficiency and interpretability without requiring task-specific labels. Extensive experiments on complex manipulation and long-horizon tasks demonstrate that ENAP outperforms state-of-the-art (SoTA) end-to-end VLA policies by up to 27% in low-data regimes, while offering a structured representation of robotic intent (Fig. 1).

---

## 论文详细总结（自动生成）

以下是对论文《Emergent Neural Automaton Policies: Learning Symbolic Structure from Visuomotor Trajectories》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不需要手工设计符号先验（如谓词逻辑）的情况下，从原始的视觉运动演示数据中自动提取出具有推理能力的逻辑结构，并将其应用于长程（Long-horizon）机器人任务。
*   **研究背景**：
    *   **端到端策略（如VLA模型）**：虽然在短程任务中表现良好，但在长程任务中缺乏结构化推理能力，且属于“黑盒”模型，数据需求量极大。
    *   **传统神经符号方法**：虽然具有可解释性，但高度依赖专家手工设计的符号标签，难以自适应地从数据中发现任务内在逻辑。
*   **研究目标**：实现“系统2”（符号推理/长期规划）从“系统1”（直觉反应/传感器控制）的感知流中自然涌现。

### 2. 方法论
ENAP 提出了一种双层神经符号架构，将任务分解为离散的状态机规划和连续的残差控制：
*   **自适应符号抽象（Symbol Abstraction）**：
    *   利用视觉编码器（如DINOv2）提取特征，通过 **HDBSCAN** 聚类算法将连续观测空间离散化为符号字母表 $\Sigma$。
    *   训练一个带有**相位感知对比损失（Phase-aware contrastive loss）**的 RNN，将变长的历史轨迹映射为固定维度的嵌入 $h_t$，作为离散状态的连续代理。
*   **扩展 L* 算法的结构提取**：
    *   在无预定义标签的情况下，通过扩展经典的 **L* 自动机学习算法**，从静态演示数据库中推断出**概率 Mealy 机（PMM）**。
    *   **自监督成员查询（MQ）**：通过在数据集中挖掘相似的历史嵌入来构建假设状态机。
    *   **非确定性等价查询（EQ）**：通过在测试轨迹上进行路径搜索来验证并修正状态机拓扑。
*   **双层控制流水线**：
    *   **高层（PMM）**：根据当前状态和输入符号提供粗略的动作先验 $a_{base}$。
    *   **底层（残差网络）**：学习一个补偿项 $\Delta a$，结合视觉观测对动作进行精细修正。
*   **迭代优化（EM风格）**：交替进行结构提取（E步）和策略优化（M步），使特征空间与逻辑结构共同进化。

### 3. 实验设计
*   **实验场景**：
    *   **复杂操作（仿真）**：ManiSkill 环境下的 PegInsert（插拔）、DualStackCube（堆叠）、MultiGoalPushT（多目标推挤）。
    *   **长程 TAMP（仿真）**：CALVIN 基准测试，包含多达5个阶段的顺序和层次任务。
    *   **现实世界**：使用 Kinova Gen3 机械臂完成 StackLego（乐高堆叠）、MultiPickPlace（颜色匹配分拣）、Hanger（挂衣架）。
*   **对比基准（Benchmarks）**：
    *   **生成式/VLA策略**：OpenVLA、$\pi_0$、Diffusion Policy。
    *   **传统模仿学习**：Transformer BC、GMM。
    *   **神经符号基准**：HULC、MDT、FLOWER 等。

### 4. 资源与算力
*   **参数规模**：论文对比了模型大小，ENAP* (DINO) 约为 **23M** 参数，远小于 OpenVLA (7.6B) 和 $\pi_0$ (3.3B)。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到 ENAP 在推理速度上显著优于大型 VLA 模型（如在实机实验中，ENAP 推理延迟为 281ms，而 $\pi_0.5$ 为 6841ms）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单操作到复杂长程任务的多个维度，包括仿真和实机验证。
*   **消融实验**：针对相似性阈值 $\tau_{sim}$、RNN 损失函数项、对比损失的影响进行了详细消融。
*   **数据效率分析**：专门测试了在 25%、50%、100% 数据量下的表现，证明了其在低数据量下的鲁棒性。
*   **客观性**：引入了六种结构化指标（如 SRN、CSS、ASD 等）来量化评估自动机的质量，而非仅看成功率，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能卓越**：在低数据量情况下，ENAP 的成功率比最先进的 VLA 模型高出 **8% 到 27%**。
*   **涌现逻辑**：ENAP 能够自动发现任务中的循环依赖（如失败重试逻辑）、逻辑分支（处理多模态目标）和状态收敛。
*   **高效简洁**：仅需 VLA 模型约 **0.6%** 的参数量，即可在特定任务上达到甚至超过其性能。
*   **鲁棒性**：通过状态机中的“自循环”结构，机器人展现出了天然的错误恢复能力（如在乐高对齐失败时自动重试）。

### 7. 优点与亮点
*   **无需标签（Label-free）**：完全从原始视觉运动数据中学习，消除了对专家手工标注符号的依赖。
*   **可解释性强**：提取的状态机拓扑结构清晰，甚至可以利用 GPT 为自动机节点生成语义标签（如“抓取”、“对齐”）。
*   **双层解耦**：将全局任务逻辑与局部反应式控制分离，降低了神经网络的学习难度，提升了样本效率。

### 8. 不足与局限
*   **超参数敏感性**：自动机的粒度高度依赖于聚类阈值 $\tau_{sim}$，设置不当可能导致状态爆炸或过于简化。
*   **静态数据限制**：由于缺乏交互式 Oracle，结构提取受限于演示数据集的覆盖范围，无法发现数据中未包含的潜在转移。
*   **跨任务泛化**：虽然在单任务长程推理上表现优异，但如何将其扩展到跨任务、跨形态的大规模通用机器人学习仍需进一步探索。

（完）
