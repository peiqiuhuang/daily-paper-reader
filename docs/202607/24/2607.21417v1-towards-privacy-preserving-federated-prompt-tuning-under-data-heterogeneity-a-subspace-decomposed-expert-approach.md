---
title: "Towards Privacy-Preserving Federated Prompt Tuning under Data Heterogeneity: A Subspace-Decomposed Expert Approach"
title_zh: 迈向数据异构下的隐私保护联邦提示微调：一种子空间分解专家方法
authors: "Yuhua Wang, Xiaodong Li, Yihao Guo, Yuxiang Jia, Qinnan Zhang, Yifan Sun, Hainan Zhang, Yongxin Tong, Zhiming Zheng"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21417v1"
tags: ["query:bopl"]
score: 8.0
evidence: 视觉语言模型的联邦提示微调
tldr: 针对联邦提示微调（FPT）中数据异构与隐私保护的冲突，本文提出FedSEPT框架。该框架通过子空间分解专家建模（SEM）将多个专家提示参数化为共享低秩因子和私有残差，在降低通信开销与差分隐私（DP）噪声的同时，利用实例感知专家融合（IEF）实现语义互补。实验证明，FedSEPT在11个基准测试中实现了优于现有方法的本地适应与全局泛化平衡。
selection_source: fresh_fetch
motivation: 现有的联邦提示微调方法在处理异构数据时，单一共享提示容易导致知识过度平滑，而多专家模式又面临通信成本高和差分隐私噪声大的挑战。
method: 提出FedSEPT框架，利用子空间分解专家建模（SEM）压缩通信空间，并结合实例感知专家融合（IEF）实现高效的专家路由与逻辑层融合。
result: 在11个异构基准数据集上的实验表明，在相同隐私约束下，FedSEPT在本地适应性和全局泛化能力之间取得了最佳的权衡。
conclusion: 本研究通过子空间分解和专家融合技术，有效解决了联邦学习环境下视觉语言模型微调的隐私、通信与异构性难题。
---

## 摘要
联邦提示微调（FPT）能够利用轻量化提示实现视觉-语言模型（VLM）的协同适配。现有方法通常在本地差分隐私（DP）下通过拆分提示设计来应对异构性和隐私问题，即将用于全局迁移的共享提示与用于本地适配的私有提示相结合。然而，单一的共享提示可能会过度平滑多样化的可迁移知识，从而削弱个性化与泛化之间的平衡。多专家提示（MEP）可以更好地捕捉这种多样性，但会扩大通信空间，增加 DP 噪声和通信成本，同时使鲁棒的专家组合变得更加困难。我们提出了 FedSEPT，一种隐私保护的联邦子空间分解专家提示微调方法。具体而言，我们采用子空间分解专家建模（SEM），利用共享低秩因子、固定公共基和私有残差对多个提示专家进行参数化，从而将通信和 DP 扰动限制在紧凑的因子空间内，同时实现在统一坐标系下的直接服务器聚合。我们进一步设计了实例感知专家融合（IEF），通过设备端路由自适应地组合语义互补的专家，并利用缓存的专家特定文本特征执行高效的 logit 级融合。在 11 个异构基准测试上的广泛实验表明，在相同的隐私约束下，FedSEPT 比强基准方法在本地适配和全局泛化之间实现了更好的权衡。

## Abstract
Federated prompt tuning (FPT) enables collaborative adaptation of vision--language models (VLMs) using lightweight prompts. Existing methods often address heterogeneity and privacy through a split-prompt design under local differential privacy (DP), combining a shared prompt for global transfer with private prompts for local adaptation. However, a single shared prompt may over-smooth diverse transferable knowledge, weakening the balance between personalization and generalization. Multi-expert prompts (MEPs) can better capture this diversity, but enlarge the communicated space, increasing DP noise and communication cost while making robust expert composition more difficult. We propose FedSEPT, a privacy-preserving Fed}erated Subspace-decomposed Expert Prompt Tuning. Specifically, we employ Subspace-decomposed Expert Modeling (SEM) to parameterize multiple prompt experts with shared low-rank factors, a fixed public basis, and private residuals, thereby confining communication and DP perturbation to a compact factor space while enabling direct server aggregation in a common coordinate system. We further design Instance-aware Expert Fusion (IEF), which adaptively combines semantically complementary experts via on-device routing and performs efficient logit-level fusion using cached expert-specific text features. Extensive experiments on 11 heterogeneous benchmarks show that, under the same privacy constraints, FedSEPT achieves a better trade-off between local adaptation and global generalization than strong baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Privacy-Preserving Federated Prompt Tuning under Data Heterogeneity: A Subspace-Decomposed Expert Approach》的结构化深入总结：

### 1. 论文的核心问题与整体含义
论文聚焦于**联邦提示微调（Federated Prompt Tuning, FPT）**在实际应用中面临的两大核心挑战：**数据异构性（Data Heterogeneity）**与**隐私保护（Privacy Preservation）**。
*   **研究动机**：现有的 FPT 方法通常采用“共享-私有”提示拆分设计，并结合本地差分隐私（Local DP）。然而，单一的共享提示在面对高度异构的客户端数据时，容易产生“过度平滑”现象，无法捕捉多样化的可迁移知识。
*   **核心矛盾**：引入多专家提示（MEP）虽能提升表达力，但会导致：
    1.  **维度灾难**：DP 噪声随参数维度增加而增大，严重破坏语义结构。
    2.  **通信开销**：多专家参数量大，增加边缘设备负担。
    3.  **组合障碍**：在 DP 噪声干扰下，如何鲁棒地融合多个专家提示变得困难。

### 2. 论文提出的方法论：FedSEPT
为了解决上述问题，作者提出了 **FedSEPT** 框架，其核心由两个关键模块组成：

*   **子空间分解专家建模（SEM）**：
    *   **核心思想**：将每个专家提示 $P$ 分解为：$P = AB_0 + R$。
    *   **技术细节**：$A$ 是低秩共享因子，$B_0$ 是预先确定的固定公共基（不参与更新），$R$ 是客户端私有残差。
    *   **优势**：差分隐私（DP-SGD）仅作用于低维的共享因子 $A$，从而将噪声扰动限制在紧凑的子空间内，显著降低了维度对隐私效用的负面影响，并减少了通信量。
*   **实例感知专家融合（IEF）**：
    *   **核心思想**：根据输入图像特征动态调整各专家的权重。
    *   **技术细节**：在设备端训练一个轻量级路由（Router）。为了提高效率，系统会缓存各专家的文本特征，并在 **Logit 层（输出层）** 进行融合，而非在提示层融合。这避免了每次推理都要重复调用昂贵的文本编码器。
*   **正则化机制**：引入多样性损失（$L_{div}$）防止专家坍缩，以及负载均衡损失（$L_{gate}$）确保专家被充分利用。

### 3. 实验设计
*   **数据集与场景**：涵盖了 11 个异构基准数据集，分为三类场景：
    1.  **病理性标签偏斜**：Food101, Caltech101, Flowers102, DTD, OxfordPets。
    2.  **实际标签偏斜**：CIFAR-10, CIFAR-100（使用 Dirichlet 分布划分）。
    3.  **域与标签双重偏斜**：PACS, Office31, OfficeHome, DomainNet。
*   **Benchmark 与对比方法**：
    *   对比了 6 种代表性方法：PromptFL, FedPGP, FedOTP, FedPHA, pFedMoAP（多专家基准）, 以及隐私保护基准 DP-FPL。
*   **评估指标**：In-Client（本地准确率）、Cross-Client（跨客户端泛化准确率）以及两者的调和平均值（HM），HM 是衡量个性化与泛化平衡的关键指标。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **2 张 NVIDIA A100 (80GB)** GPU。
*   **训练细节**：通信轮数 $T=50$，本地 Epoch $E=1$，学习率 0.001。
*   **模型底座**：使用预训练的 CLIP (ViT-B/16) 作为冻结的骨干网络。
*   **效率表现**：FedSEPT 将通信开销从 64 KiB 降低到 8 KiB，且由于采用了 Logit 级融合，推理延迟和 FLOPs 均优于其他动态融合方案。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 11 个数据集的性能对比、针对不同 Shot 数量的鲁棒性分析、超参数敏感性分析（专家数 $M$、秩 $r$、损失权重等）。
*   **消融实验**：对比了单提示、全维度多专家、无私有残差、Top-1 路由等变体，验证了各组件的必要性。
*   **安全性评估**：专门针对成员推理攻击（MIA）和梯度反向攻击（GIA）进行了防御性测试。
*   **评价**：实验设计非常充分且严谨，涵盖了从效用到效率再到安全性的全方位评估，结果具有高度的客观性和说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：在所有 11 个基准测试中，FedSEPT 在隐私预算 $\epsilon=1$ 的约束下，HM 指标均显著优于现有方法。
*   **隐私-效用权衡**：通过子空间分解，FedSEPT 有效缓解了 DP 噪声对模型性能的破坏，证明了在低维空间进行联邦学习是提升隐私模型效用的有效途径。
*   **泛化与个性化**：多专家机制配合实例感知融合，使模型既能通过共享因子学习全局知识，又能通过私有残差和路由实现精准的本地适配。

### 7. 优点
*   **创新性强**：巧妙地将低秩分解（SEM）与多专家模型（MEP）结合，解决了联邦学习中高维参数加噪导致的效用下降问题。
*   **效率极高**：Logit 级融合的设计非常具有工程实践价值，大幅降低了多专家模型在边缘设备上的推理成本。
*   **理论与实践结合**：不仅提供了形式化的隐私证明，还通过多种攻击模型验证了实际的安全性。

### 8. 不足与局限
*   **公共基依赖**：SEM 依赖于一个固定的公共基 $B_0$，虽然文中提到其是数据无关的，但在极端分布偏移下，固定基是否会限制表达能力的上限仍有待商榷。
*   **本地训练开销**：虽然推理效率高，但在训练阶段，客户端仍需计算多个专家的梯度，对于计算资源极度匮乏的设备可能仍存在压力。
*   **超参数调节**：引入了专家数量、子空间维度及多个损失权重，增加了系统调优的复杂度。

（完）
