---
title: Parameter-efficient Prompt Tuning and Hierarchical Textual Guidance for Few-shot Whole Slide Image Classification
title_zh: 用于少样本全切片图像分类的参数高效提示微调与分层文本引导
authors: "Jayanie Bogahawatte, Sachith Seneviratne, Saman Halgamuge"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21504v1"
tags: ["query:bopl"]
score: 9.0
evidence: 专注于视觉语言模型的参数高效提示微调
tldr: 针对少样本弱监督全切片图像（WSI）分类中预训练视觉语言模型参数量大、推理开销高以及硬过滤导致信息丢失的问题，本文提出了一种高效的提示微调方法。通过在文本编码器中缩放和偏移特征，并引入软分层文本引导策略，该方法在显著减少可训练参数的同时，充分利用了WSI的层级结构信息。实验表明，该方法在多种癌症分类任务上显著优于现有技术，并能有效进行肿瘤定位。
selection_source: fresh_fetch
motivation: 现有的VLM微调方法在处理WSI时存在计算开销大和因硬性过滤低对齐实例而导致关键信息丢失的问题。
method: 提出了一种通过缩放和偏移文本编码器特征的参数高效提示微调方法，并结合了无需硬过滤的软分层文本引导策略。
result: "在乳腺癌、肺癌和卵巢癌数据集上，分类准确率分别提升了高达10.9%、7.8%和13.8%，同时显著减少了可训练参数量。"
conclusion: 该方法在降低计算成本的同时，通过更精细的文本引导提升了少样本WSI分类的性能和肿瘤定位能力。
---

## 摘要
全切片图像（WSIs）具有十亿像素级的规模，在 WSI 分类流程中通常被划分为小的实例，以实现计算可行性。然而，获取大量的实例级标注成本高昂，这使得少样本弱监督 WSI 分类（FSWC）对于从有限的切片级标签中进行学习至关重要。近年来，预训练视觉语言模型（VLMs）已被应用于 FSWC，但它们仍存在一些局限性。现有的 FSWC 提示微调方法显著增加了可训练参数的数量和推理开销。此外，当前的方法会丢弃与 VLM 文本嵌入对齐度较低的实例，这可能导致信息丢失。为了应对这些挑战，我们提出了两项核心贡献。首先，我们通过在文本编码器中缩放和偏移特征，引入了一种新的参数高效提示微调方法，从而显著降低了计算成本。其次，为了不仅利用 VLMs 的预训练知识，还利用 WSIs 固有的分层结构，我们引入了一种带有软分层文本引导策略的 WSI 表征学习方法，且无需使用硬实例过滤。在涵盖乳腺癌、肺癌和卵巢癌类型的病理数据集上进行的全面评估表明，与 FSWC 的现有最先进方法相比，本方法分别实现了高达 10.9%、7.8% 和 13.8% 的持续改进。我们的方法在乳腺癌和肺癌数据集上将可训练参数数量减少了 18.1%，在卵巢癌数据集上减少了 5.8%，同时在弱监督肿瘤定位方面也表现出色。

## Abstract
Whole Slide Images (WSIs) are giga-pixel in scale and are typically partitioned into small instances in WSI classification pipelines for computational feasibility. However, obtaining extensive instance level annotations is costly, making few-shot weakly supervised WSI classification (FSWC) crucial for learning from limited slide-level labels. Recently, pre-trained vision-language models (VLMs) have been adopted in FSWC, yet they exhibit several limitations. Existing prompt tuning methods in FSWC substantially increase both the number of trainable parameters and inference overhead. Moreover, current methods discard instances with low alignment to text embeddings from VLMs, potentially leading to information loss. To address these challenges, we propose two key contributions. First, we introduce a new parameter efficient prompt tuning method by scaling and shifting features in text encoder, which significantly reduces the computational cost. Second, to leverage not only the pre-trained knowledge of VLMs, but also the inherent hierarchical structure of WSIs, we introduce a WSI representation learning approach with a soft hierarchical textual guidance strategy without utilizing hard instance filtering. Comprehensive evaluations on pathology datasets covering breast, lung, and ovarian cancer types demonstrate consistent improvements up-to 10.9%, 7.8%, and 13.8% respectively, over the state-of-the-art methods in FSWC. Our method reduces the number of trainable parameters by 18.1% on both breast and lung cancer datasets, and 5.8% on the ovarian cancer dataset, while also excelling at weakly-supervised tumor localization. Code at https://github.com/Jayanie/HIPSS.

---

## 论文详细总结（自动生成）

以下是对论文《Parameter-efficient Prompt Tuning and Hierarchical Textual Guidance for Few-shot Whole Slide Image Classification》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在极少标注样本（Few-shot）的情况下，利用视觉语言模型（VLM）实现高精度的全切片图像（WSI）弱监督分类。
*   **研究背景**：
    *   WSI 具有十亿级像素，标注成本极高，因此少样本弱监督学习（FSWC）至关重要。
    *   **现有挑战**：
        1.  **参数冗余与开销**：现有的提示微调（Prompt Tuning）方法（如 CoOp）或交叉注意力机制增加了大量可训练参数和推理延迟。
        2.  **信息丢失**：现有方法通常采用“硬过滤”策略，直接丢弃与文本对齐度低的图像块（Instances），可能导致关键诊断信息丢失。
        3.  **结构忽视**：WSI 具有天然的层级空间结构（细胞-组织-器官），现有方法往往将其视为无序的图像块集合。

### 2. 方法论
论文提出了 **HIPSS** 框架，包含两个核心创新点：
*   **参数高效的提示微调（SSF-based Prompt Tuning）**：
    *   **核心思想**：受“缩放与偏移特征（SSF）”启发，在预训练文本编码器的特定层中插入线性变换。
    *   **技术细节**：公式为 $y = \gamma \cdot x + \beta$，其中 $\gamma$ 和 $\beta$ 是可学习的缩放和偏移参数。在训练时冻结 VLM 主干，仅更新这两个参数。
    *   **优势**：通过重参数化技术，这些参数在推理阶段可以合并入原始模型，实现**零额外推理开销**。
*   **软分层文本引导的表征学习（Hierarchical Textual Guidance）**：
    *   **层级结构**：将 WSI 划分为“区域（Region, 4096×4096）”和“实例（Instance, 256×256）”两个层级。
    *   **软引导机制**：在每一层级使用注意力池化，并根据图像特征与文本嵌入的余弦相似度动态调整注意力权重。
    *   **对齐策略**：不直接丢弃低相似度实例，而是通过相似度得分进行加权（抑制负相关、保留低正相关、放大高置信度），从而保留潜在的诊断信号。

### 3. 实验设计
*   **数据集**：
    *   **Camelyon16**：乳腺癌转移检测（二分类）。
    *   **TCGA-Lung**：肺癌亚型分类（二分类）。
    *   **UBC-OCEAN**：卵巢癌亚型分类（五分类）。
*   **实验设置**：测试了 1, 2, 4, 8, 16-shot（每类仅 1-16 个标注切片）的极端少样本场景。
*   **Benchmark 与对比方法**：
    *   传统 MIL 方法：ABMIL。
    *   SOTA 少样本方法：TOP, FOCUS, ViLa-MIL, FAST, MAPLE 等。
    *   所有对比方法均统一使用 **CONCH** 作为 VLM 主干以确保公平。

### 4. 资源与算力
*   **硬件**：使用了单张 **Nvidia A100 (80GB) GPU**。
*   **训练细节**：采用 Adam 优化器，学习率在 0.01 到 0.0001 之间，训练 100 个 epoch，并使用早停机制（Early Stopping）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同器官的数据集上进行了全面测试，涵盖了二分类和多分类任务。
*   **稳健性**：每个实验结果均基于 **3 折交叉验证**，且每折运行 **20 个随机种子**取平均值，有效降低了少样本场景下的随机性偏差。
*   **消融实验**：针对分层结构（H）、文本引导（T）和 SSF 微调（S）分别做了消融分析，验证了各组件的贡献。
*   **参数敏感性**：对微调深度（$d_s$）、引导因子（$\lambda$）和阈值（$\alpha$）进行了详细的参数分析。

### 6. 主要结论与发现
*   **性能领先**：在 1-shot 场景下，HIPSS 在卵巢癌数据集上比现有最先进方法提升了 **13.8%** 的 AUC。
*   **参数极简**：相比于其他 FSWC 方法，可训练参数量减少了 **5.8% - 18.1%**，且推理速度提升了约 **12.6%**。
*   **定位能力**：尽管仅使用切片级标签训练，HIPSS 在 Camelyon16 上的肿瘤定位 Dice 系数达到 **0.732**，远超其他少样本方法。

### 7. 优点
*   **高效性**：SSF 提示微调在保持极低参数量的同时，比 LoRA 等常见微调方法表现更好。
*   **空间感知**：通过分层建模捕捉了 WSI 的空间组织信息，符合病理学诊断逻辑。
*   **鲁棒性**：软引导策略避免了硬过滤带来的信息丢失，在极少样本（1-shot）下表现尤为稳健。

### 8. 不足与局限
*   **提示词依赖**：病理描述由 ChatGPT-4o 生成，虽然有效，但缺乏病理专家的直接验证和精细化调整。
*   **区域尺寸固定**：实验中统一使用 4096×4096 的区域划分，未探讨不同分辨率或多尺度区域划分对性能的影响。
*   **应用限制**：目前主要验证了分类和定位任务，尚未在更复杂的回归或生存分析任务中测试。

（完）
