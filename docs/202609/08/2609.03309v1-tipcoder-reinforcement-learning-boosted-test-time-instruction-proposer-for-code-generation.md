---
title: "TIPCODER: Reinforcement Learning Boosted Test-time Instruction Proposer for Code Generation"
title_zh: TIPCODER：强化学习增强的测试时代码生成指令生成器
authors: "Minyu Chen, Sihao Wu, Ling-I Wu, Song Qin, Jingyang Li, Lei Ning, Jianxin Xue, Guoqiang Li"
date: 2026-09-03
pdf: "https://arxiv.org/pdf/2609.03309v1"
tags: ["query:bopl"]
score: 8.0
evidence: 通过强化学习优化的自动指令建议器
tldr: TIPCODER 是一种针对代码生成的测试时指令优化框架。针对现有模型因提示词约束缺失或逻辑偏差导致的生成失败，该方法通过强化学习训练指令生成器，在代码合成前提供针对性的辅助提示（Tips）。通过结合基础方案与提示引导方案，并利用奖励模型进行筛选，TIPCODER 有效提升了代码生成的质量与鲁棒性，在多个基准测试中表现优于传统采样和提示优化方法。
selection_source: fresh_fetch
motivation: 许多代码生成失败源于原始提示词中缺失约束、忽略边界条件或存在误导性推理路径。
method: 利用强化学习训练一个指令生成器，将多轮调试轨迹转化为针对特定问题的辅助提示，并在推理时通过奖励模型筛选最优解。
result: 在多个代码生成基准测试中，TIPCODER 相比随机采样和通用提示优化方法展现出更优的性能和一致的测试时扩展能力。
conclusion: 通过实例级的指令空间探索，TIPCODER 为提升大语言模型代码生成能力提供了一种高效且可扩展的测试时优化策略。
---

## 摘要
代码生成的测试时扩展通常通过从固定指令中采样多个程序来探索解空间。我们研究了一个互补的方向：实例级的指令空间探索。我们观察到，许多编码失败源于原始提示词中缺失的约束、被忽视的边界情况或误导性的推理路径。为了解决这个问题，我们提出了 TipCoder，这是一种测试时指令生成器，它在代码合成之前生成针对特定问题的辅助提示（tips）。TipCoder 将多轮调试轨迹提炼为主动指导，并利用边际效用奖励通过强化学习进一步优化该生成器。在推理阶段，它同时生成基础解和提示引导解，并应用奖励模型进行事后选择。这种“探索-选择”设计使得提示能够发掘额外的候选潜力，同时减少因不必要指导带来的回归。在评估的代码生成基准和目标代码大模型上，TipCoder 提供了一种一致的指令级测试时扩展策略，在基于奖励模型的统一选择协议下，其表现优于随机采样和通用提示优化基准。

## Abstract
Test-time scaling for code generation typically explores the solution space by sampling multiple programs from a fixed instruction. We study a complementary direction: instance-level instruction-space exploration. Our observation is that many coding failures stem from missing constraints, overlooked edge cases, or misleading reasoning paths induced by the original prompt. To address this, we propose TipCoder, a test-time instruction proposer that generates problem-specific auxiliary tips before code synthesis. TipCoder distills multi-turn debugging trajectories into proactive guidance and further optimizes the Proposer with reinforcement learning using a marginal-utility reward. At inference time, it generates both a base solution and a tip-guided solution, and applies a Reward Model for post-hoc selection. This exploration-selection design allows tips to expose additional candidate potential while reducing regressions from unnecessary guidance. Across the evaluated code-generation benchmarks and target Code LLMs, TipCoder provides a consistent instruction-level test-time scaling strategy, comparing favorably with stochastic sampling and generic prompt optimization baselines under a shared reward-model-based selection protocol.

---

## 论文详细总结（自动生成）

这是一份关于论文 **TIPCODER: Reinforcement Learning Boosted Test-time Instruction Proposer for Code Generation** 的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：代码大模型（Code LLMs）对初始指令高度敏感。现有的测试时扩展（Test-time Scaling）方法（如 Best-of-N）主要通过在固定指令下进行多次采样来探索解空间。
*   **动机**：研究发现，许多代码生成失败并非模型能力不足，而是源于原始提示词（Prompt）中缺失隐式约束、忽略边界条件或存在误导性推理路径。
*   **核心含义**：论文提出了一种互补的扩展维度——**实例级指令空间探索**。通过在代码生成前动态生成针对性的辅助提示（Tips），引导模型探索更优的解空间，从而提升代码生成的成功率。

### 2. 方法论
*   **核心思想**：将“多轮调试”中获得的经验压缩为“单轮提示”。通过训练一个 Proposer 模型，使其在给定问题时生成辅助提示，从而在不改变模型参数的情况下优化生成结果。
*   **关键技术细节**：
    *   **数据构建**：利用 AceCoder-87K 数据集，通过 Gemini-3-Pro 进行多轮调试，提取修复轨迹，并使用“掩码蒸馏（Masked Distillation）”技术，在隐藏具体代码实现的情况下提取高层约束和边界条件。
    *   **训练流程**：
        1.  **SFT（监督微调）**：利用蒸馏出的高质量提示对 Proposer 进行初始化。
        2.  **RL（强化学习）**：使用 GRPO 算法，以“边际效用奖励（Marginal-Utility Reward）”为目标进行优化。奖励函数不仅考虑代码通过率，还引入了对基础方案的边际提升量，并惩罚过长的提示词。
    *   **推理流程**：采用“探索-选择”架构。模型同时生成“基础解”和“提示引导解”，最后通过一个预训练的奖励模型（Reward Model）进行事后筛选。

### 3. 实验设计
*   **数据集**：HumanEval+、MBPP+ 以及 BigCodeBench-Instruct。
*   **对比方法**：
    *   **基准**：Base（直接生成）、Best-of-N（随机采样）。
    *   **提示优化类**：OPRO、GEPA、CTRL。
    *   **零样本基准**：Self-Hint（使用未训练的 Proposer 生成提示）。
*   **目标模型**：Qwen2.5-Coder-7B、DeepSeek-Coder-7B、Qwen3-Coder-30B-A3B、DeepSeek-Coder-V2-Lite。

### 4. 资源与算力
*   **硬件配置**：所有实验均在配备 **4 张 NVIDIA A800 GPU** 的计算节点上进行。
*   **训练细节**：训练过程分为 SFT 和 RL 两个阶段，使用了 OpenRLHF 框架和 vLLM 进行加速。文中明确了超参数设置（如学习率、KL 散度系数等），并提到为了平衡数据并行，全局 Batch Size 设置为 3 的倍数。

### 5. 实验数量与充分性
*   **充分性**：实验覆盖了 4 种不同规模和架构的开源模型，并在 3 个主流代码基准上进行了测试。
*   **客观性**：通过“探索-选择”分解实验，明确了性能提升源于指令空间的有效探索，而非单纯依赖奖励模型的筛选能力。实验设计了成本归一化对比（Cost-Normalized），在相同计算预算下与 Best-of-N 进行了公平比较。

### 6. 主要结论与发现
*   **有效性**：TIPCODER 在所有测试模型上均实现了 Pass@1 的提升，证明了实例级指令优化是比通用提示优化更稳定的扩展策略。
*   **迁移性**：训练好的 Proposer 能够直接迁移到未参与训练的目标模型上，表现出良好的通用性。
*   **效率**：在达到相同通过率的情况下，TIPCODER 相比 Best-of-N 消耗的 Token 更少，推理效率更高。

### 7. 优点
*   **黑盒兼容性**：无需访问目标模型的参数、梯度或执行反馈，仅通过提示词增强即可工作。
*   **解耦设计**：将“指令探索”与“结果筛选”解耦，通过奖励模型有效降低了引入辅助提示可能带来的负面干扰（Regression）。
*   **针对性强**：生成的提示词聚焦于边界条件和约束，而非通用的编程建议，更具实用价值。

### 8. 不足与局限
*   **推理成本**：引入了额外的提示生成步骤和两次代码生成过程，增加了单次请求的推理延迟。
*   **依赖筛选器**：最终性能高度依赖于事后奖励模型（Reward Model）的准确性，若筛选器失效，则无法发挥提示词的潜力。
*   **训练偏差**：Proposer 的训练基于特定的环境和数据构建流程，可能继承了训练环境的错误模式，在处理极度复杂的跨库任务时仍有提升空间。

（完）
