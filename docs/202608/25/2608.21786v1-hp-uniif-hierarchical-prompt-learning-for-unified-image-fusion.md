---
title: "HP-UniIF: Hierarchical Prompt Learning for Unified Image Fusion"
title_zh: HP-UniIF：面向统一图像融合的分层提示学习
authors: "Xingxin Xu, Siqi Zhao, Xin Li, Xinjie Yao, Yiming Sun, Pengfei Zhu"
date: 2026-08-22
pdf: "https://arxiv.org/pdf/2608.21786v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于图像融合的分层提示学习
tldr: 针对现实场景中图像融合、退化修复及下游感知任务难以在单一系统中协同的问题，本文提出HP-UniIF框架。该框架利用扩散先验，通过深度分层条件调制策略（包括任务提示、退化提示和应用提示），在不同网络阶段解耦并处理异构融合、局部修复和语义对齐。实验证明其在多种融合任务和退化场景下均表现优异。
selection_source: fresh_fetch
motivation: 现有的统一图像融合框架难以同时处理异构融合、退化修复和任务感知，导致表示纠缠和性能下降。
method: 提出一种基于扩散先验的分层提示学习策略，在网络的不同深度分别注入任务、退化和应用相关的调制信息以实现目标解耦。
result: 在多项融合任务、多种退化类型及各类下游应用中，HP-UniIF均取得了超越现有方法的性能表现。
conclusion: HP-UniIF通过分层解耦设计，成功实现了图像融合、质量提升与语义感知的统一，为复杂场景下的视觉任务提供了高效方案。
---

## 摘要
通用图像融合旨在整合来自多个源图像的互补信息，然而实际应用通常要求单个系统同时支持异构融合、退化修复和面向任务的感知。现有的统一框架难以处理这些正交目标，导致表示纠缠以及各子任务性能下降。我们提出了 HP-UniIF，这是一个利用扩散先验来连接异构融合、视觉修复和下游感知的统一视觉框架。为了解决扩散模型在单一流水线中对领域级、退化级和任务级目标的适应性有限的问题，HP-UniIF 引入了一种深度分层条件调制策略，在不同的网络阶段解耦这些目标。瓶颈层的任务提示调制使骨干网络适应不同的融合范式，浅层的退化提示路由注入退化感知约束以进行局部修复，解码阶段的应用提示库则使生成结果与下游任务保持一致。这种分层设计使 HP-UniIF 能够产生视觉上真实的结果，同时保留与任务相关的语义。在多个融合任务、多种退化和各种下游应用上的广泛实验证明了 HP-UniIF 的卓越性能。

## Abstract
General image fusion seeks to integrate complementary information from multiple source images, yet real-world applications often require a single system to support heterogeneous fusion, degradation restoration, and task-oriented perception simultaneously. Existing unified frameworks struggle with these orthogonal objectives, resulting in entangled representations and degraded performance across subtasks. We propose HP-UniIF, a unified vision framework that leverages diffusion priors to bridge heterogeneous fusion, visual restoration, and downstream perception. To address the limited adaptability of diffusion models to domain-, degradation-, and task-level objectives within one pipeline, HP-UniIF introduces a depth-wise hierarchical conditional modulation strategy that decouples these objectives across network stages. Task prompt modulation at bottleneck layers adapts the backbone to different fusion paradigms, the degradation prompt router at shallow layers injects degradation-aware constraints for local restoration, and the application prompt bank at decoding stages aligns generation with downstream tasks. This hierarchical design enables HP-UniIF to produce visually faithful results while preserving task-relevant semantics. Extensive experiments across multiple fusion tasks, diverse degradations, and various downstream applications demonstrate the superior performance of HP-UniIF.

---

## 论文详细总结（自动生成）

### 论文总结：HP-UniIF —— 面向统一图像融合的分层提示学习

#### 1. 论文的核心问题与整体含义
论文针对现实场景中图像融合面临的 **M3 挑战**（Multi-task, Multi-degradation, Multi-application）提出了统一框架。现有的融合模型通常只能在单一任务（如红外-可见光融合）、单一环境（清洁图像）或单一目标（视觉质量）上表现良好。当需要一个系统同时处理**异构融合任务**、**复杂退化修复**（如噪声、模糊、低光）以及**下游感知任务**（如目标检测、语义分割）时，不同目标的梯度和信号会产生干扰，导致性能下降。HP-UniIF 的核心意义在于通过分层解耦的方式，在共享的扩散先验（Diffusion Prior）基础上协调这些正交目标。

#### 2. 方法论
HP-UniIF 基于预训练的潜在扩散模型（Latent Diffusion Model，如 SD-Turbo），引入了**深度分层条件调制策略**，将不同层级的目标解耦到网络的不同阶段：
*   **任务提示调制 (TPM)**：位于瓶颈层和解码层。利用可学习的任务提示库，根据当前特征状态动态更新提示，通过通道级仿射变换和门控机制调制特征，使骨干网络适应不同的融合范式（VIF, MEF, MFF）。
*   **退化提示路由 (DPR)**：位于浅层（跳跃连接处）。通过构建退化描述符，从预设的提示池中检索最相关的 $K$ 个退化提示，对源图像特征进行局部修复，防止退化伪影传播到融合结果中。
*   **应用提示库 (APB)**：位于解码阶段。在不改变融合路径的前提下，注入特定下游任务（如检测、分割）的语义引导，通过内容保留分支和提示注入分支的协作，使输出图像更利于机器感知。
*   **训练策略**：采用三阶段解耦训练：S1 学习多任务融合；S2 冻结融合路径训练 DPR 进行修复；S3 冻结前两者训练 APB 进行应用适配。

#### 3. 实验设计
*   **任务场景**：涵盖红外-可见光融合 (VIF)、多曝光融合 (MEF) 和多聚焦融合 (MFF)。
*   **数据集**：
    *   VIF: LLVIP, MSRS。
    *   MEF: SCIE, MEFB。
    *   MFF: RealMFF, MFI-WHU, MFIF。
*   **Benchmark 与对比方法**：
    *   通用融合方法：U2Fusion, DeFusion, CCF。
    *   退化感知方法：GIFNet, URFusion, OmniFuse。
    *   任务特定方法：DRMF, TG-ECNet, TSDM-Fusion, SAGE 等。
    *   下游任务评估：使用 YOLOv5 进行检测，DeepLabV3+ 进行分割。

#### 4. 资源与算力
论文提到使用了预训练的 **SD-Turbo** 模型作为初始化，训练时将输入图像随机裁剪为 **512 × 512**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量以及总训练时长。这在扩散模型相关的论文中是一个缺失的信息点，通常此类模型对显存和计算能力有较高要求。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度评估**：在三大融合任务上分别使用了 5-6 个定量指标（如 EN, MI, PSNR, VIF 等），总计对比了 17 项指标。
*   **退化鲁棒性**：在清洁和多种退化（噪声、模糊、低光、雾等）条件下均进行了对比。
*   **下游任务**：不仅评估视觉质量，还给出了检测（mAP）和分割（mIoU）的量化提升。
*   **消融实验**：系统验证了扩散先验、TPM、DPR 和 APB 各个组件对最终性能的贡献，证明了分层设计的必要性。

#### 6. 论文的主要结论与发现
*   **解耦的有效性**：通过分层注入提示，可以有效解决融合、修复和感知任务之间的表示纠缠问题。
*   **扩散先验的优势**：预训练的扩散模型提供了强大的结构和生成先验，显著提升了融合图像的细节丰富度和真实感。
*   **下游任务增益**：APB 模块能够在不破坏视觉保真度的前提下，显著提升机器视觉算法在融合图像上的表现（如 mAP@.5:.95 的提升）。
*   **统一性**：单一模型即可在多种异构任务和复杂环境下达到 SOTA 性能。

#### 7. 优点
*   **架构创新**：分层提示学习策略巧妙地利用了 U-Net 不同深度的特征特性（浅层修复、深层语义）。
*   **高度灵活**：APB 的设计使得模型可以轻松扩展到新的下游应用，只需学习极少量的参数。
*   **鲁棒性强**：DPR 的检索机制能够自适应处理多种复合退化。

#### 8. 不足与局限
*   **推理开销**：虽然使用了 SD-Turbo（单步或少步生成），但扩散模型的骨干网络参数量通常远大于传统的轻量化融合网络，实时性可能受限。
*   **算力透明度**：缺乏具体的训练资源消耗说明，不便于复现者评估成本。
*   **未知退化泛化性**：虽然 DPR 涵盖了多种退化，但对于训练集之外的极端或新型退化（Unseen Degradations）的泛化能力仍有待进一步验证。
*   **应用限制**：APB 目前仅验证了检测和分割，对于更复杂的视频融合或 3D 场景融合尚未涉及。

（完）
