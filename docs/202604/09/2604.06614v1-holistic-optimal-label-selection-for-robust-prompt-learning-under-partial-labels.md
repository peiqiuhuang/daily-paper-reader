---
title: Holistic Optimal Label Selection for Robust Prompt Learning under Partial Labels
title_zh: 部分标签下鲁棒提示学习的全局最优标签选择
authors: "Yaqi Zhao, Haoliang Sun, Yating Wang, Yongshun Gong, Yilong Yin"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06614v1"
tags: ["query:bopl"]
score: 8.0
evidence: 提示学习作为一种参数高效的视觉语言模型适配方法
tldr: 针对视觉语言模型在部分标签监督下提示学习性能受限的问题，本文提出了全局最优标签选择（HopS）框架。该框架结合了基于局部密度的过滤策略和基于最优传输的全局选择目标，分别从特征空间的局部结构和批次数据的全局分布出发，有效缓解了标签歧义。在8个基准数据集上的实验证明，HopS显著提升了弱监督下的提示学习效果，优于现有基准方法。
selection_source: fresh_fetch
motivation: 旨在解决提示学习在仅有部分候选标签时，因标签歧义和监督信息不足导致的性能下降问题。
method: 提出了HopS框架，通过局部密度过滤捕捉特征结构规律，并利用最优传输理论实现批次级别的全局标签分配。
result: 在八个主流基准数据集上的广泛实验表明，HopS在部分标签监督下一致性地优于所有对比基准。
conclusion: 本研究证明了结合局部与全局视角的整体标签选择策略在弱监督提示学习中的有效性和实用价值。
---

## 摘要
提示学习作为一种参数高效的方法，在将大型预训练视觉语言模型适配到下游任务方面受到了广泛关注。然而，当仅有部分标签可用时，其性能往往受限于标签歧义和监督信息不足。为解决这一问题，我们提出了全局最优标签选择（HopS），通过两种互补策略利用预训练特征编码器的泛化能力。首先，我们设计了一个基于局部密度的过滤器，从最近邻的候选集中选择频率最高的标签，并利用 softmax 分数识别最合理的标签，从而捕捉特征空间中的结构规律。其次，我们引入了一个基于最优传输的全局选择目标，将均匀采样分布映射到整个批次的候选标签分布。通过最小化期望传输成本，它可以确定最可能的标签分配。这两种策略协同工作，从局部和全局两个视角提供鲁棒的标签选择。在八个基准数据集上的广泛实验表明，HopS 在部分监督下持续提升性能，并优于所有基准方法。这些结果突显了全局标签选择的优点，并为弱监督设置下的提示学习提供了一种实用的解决方案。

## Abstract
Prompt learning has gained significant attention as a parameter-efficient approach for adapting large pre-trained vision-language models to downstream tasks. However, when only partial labels are available, its performance is often limited by label ambiguity and insufficient supervisory information. To address this issue, we propose Holistic Optimal Label Selection (HopS), leveraging the generalization ability of pre-trained feature encoders through two complementary strategies. First, we design a local density-based filter that selects the top frequent labels from the nearest neighbors' candidate sets and uses the softmax scores to identify the most plausible label, capturing structural regularities in the feature space. Second, we introduce a global selection objective based on optimal transport that maps the uniform sampling distribution to the candidate label distributions across a batch. By minimizing the expected transport cost, it can determine the most likely label assignments. These two strategies work together to provide robust label selection from both local and global perspectives. Extensive experiments on eight benchmark datasets show that HopS consistently improves performance under partial supervision and outperforms all baselines. Those results highlight the merit of holistic label selection and offer a practical solution for prompt learning in weakly supervised settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《Holistic Optimal Label Selection for Robust Prompt Learning under Partial Labels》（部分标签下鲁棒提示学习的全局最优标签选择）的结构化总结：

### 1. 核心问题与整体含义
论文关注的是**部分标签学习（Partial Label Learning, PLL）**场景下的**提示学习（Prompt Learning）**。在 PLL 中，每个训练样本被赋予一个候选标签集合，其中包含真实标签和若干干扰标签。
*   **研究动机**：现有的提示学习方法（如 CoOp）在全监督下表现优异，但在 PLL 场景下，由于标签歧义（Ambiguity）和监督信息不足，性能会大幅下降。
*   **核心挑战**：如何在不微调预训练视觉编码器（以保持其泛化能力）的前提下，从模糊的候选集中准确识别出真实标签，并引导提示向量的优化。

### 2. 方法论：HopS 框架
论文提出了 **HopS（Holistic Optimal Label Selection）** 框架，通过结合局部和全局两个视角的互补策略来消除标签歧义：

*   **局部密度过滤器 (Local Density-Based Filter, LDF)**：
    *   **核心思想**：利用特征空间中近邻样本的语义一致性。
    *   **技术细节**：首先预计算图像特征的相似度矩阵，为每个样本寻找 $k$ 个最近邻。统计这些邻居候选标签出现的频率，构建“共识掩码”，过滤掉频率低于阈值 $\tau$ 的标签。最后在缩减后的候选集中，根据模型当前的预测分数选出“局部共识”标签。
*   **全局最优传输规划器 (Global Optimal Transport Planner, GOP)**：
    *   **核心思想**：将标签选择建模为一个最优传输（OT）问题，实现样本与标签的全局最优匹配。
    *   **技术细节**：在每个 Batch 内，将样本的均匀分布（源）与基于候选集频率估计的标签分布（目标）进行对齐。通过构造基于图文相似度的成本矩阵，利用 **Sinkhorn-Knopp 算法** 迭代求解最优传输方案。该方案能确保标签分配在全局范围内符合语义逻辑。
*   **联合优化**：将 LDF 和 GOP 选出的伪标签作为监督信号，通过加权交叉熵损失函数共同优化可学习的提示向量（Prompt Tokens）。

### 3. 实验设计
*   **数据集**：涵盖 8 个主流视觉识别基准，包括通用分类（Caltech101, EuroSAT）和细粒度分类（DTD, FGVCAircraft, Food101, Flowers102, OxfordPets, UCF101）。
*   **混淆设置**：
    *   **随机混淆 (Rand)**：干扰标签随机选择。
    *   **实例相关混淆 (Insd)**：利用预训练模型寻找视觉上最相似的类别作为干扰项，更符合现实噪声。
*   **对比方法**：
    *   **基础模型**：Zero-shot CLIP, 16-shot CoOp（全监督）。
    *   **PLL 损失函数**：RC, CC, LWC, MSE, EXP。
    *   **鲁棒学习损失**：MAE, SCE, GCE。
    *   **最新 PLL 专门方法**：Papi, CroSel, SoLar。

### 4. 资源与算力
*   **硬件**：使用了 Linux 系统，配备 **8 张 NVIDIA RTX 4090 GPU**。
*   **训练时长**：每个模型训练约 **18–30 分钟**。
*   **内存占用**：显存峰值约为 **6696 MiB**。
*   **软件环境**：Python 3.8, PyTorch 1.13.1, CUDA 12.0。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 8 个数据集在不同混淆率（0.5 到 0.9）下的表现，涵盖了统一提示（uni-prompt）和类别特定提示（cls-prompts）两种模式。
*   **消融与分析**：
    *   验证了 LDF 和 GOP 的互补性（通过可视化两者识别真值的准确率曲线）。
    *   分析了 Batch Size、超参数 $k$ 和 $\tau$ 的敏感度。
    *   探讨了长尾分布、真值缺失（Noisy PLL）等极端情况。
*   **客观性**：所有实验均采用 3 个随机种子取平均值，并对比了从传统损失到最新 SOTA 的多种方法，实验设计严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：HopS 在所有 8 个数据集和各种混淆设置下均显著优于现有基准。在某些数据集（如 Caltech, Flowers）上，其性能甚至逼近了全监督的 16-shot 结果。
*   **互补效应**：LDF 在训练早期能快速提供稳定的局部约束，而 GOP 则通过全局对齐进一步提升了标签选择的精度，两者结合比单一模块更鲁棒。
*   **鲁棒性强**：在实例相关混淆（Insd）和长尾分布下，HopS 表现出比专门的 PLL 方法（如 SoLar）更强的适应性。

### 7. 优点（亮点）
*   **视角全面**：首次在提示学习中结合了非参数化的局部结构（k-NN）和参数化的全局规划（OT）。
*   **计算高效**：尽管引入了 OT 算法，但由于是在少样本（Few-shot）和 Batch 级别操作，计算开销极低，训练时间与普通交叉熵损失相当。
*   **即插即用**：该方法不改变 VLM 的骨干网络，仅优化提示向量，具有很强的实用价值。

### 8. 不足与局限
*   **极端噪声限制**：在“随机混淆”且“真值缺失”的极端场景下，HopS 单独使用的效果略逊于某些专门的鲁棒损失（如 SCE），需要结合 SCE 才能达到最优。
*   **超参数依赖**：虽然实验证明 $k$ 和 $\tau$ 相对稳定，但在极度不平衡或类别极多的数据集上，可能仍需微调 Batch Size 以匹配类别规模。
*   **应用范围**：目前主要验证了其在图像分类任务上的有效性，尚未扩展到目标检测或分割等更复杂的视觉任务。

（完）
