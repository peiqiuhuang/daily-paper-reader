---
title: Small Language Model Helps Resolve Semantic Ambiguity of LLM Prompt
title_zh: 小语言模型助力解决大语言模型提示词的语义歧义
authors: "Zhenzhen Huang, Chaoning Zhang, Fachrina Dewi Puspitasari, Jiaquan Zhang, Yitian Zhou, Shuxu Chen, Yang Yang"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23263v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于消除歧义的推理前提示词优化机制
tldr: 针对大语言模型（LLM）易受自然语言提示词语义歧义干扰的问题，本文提出一种基于小语言模型（SLM）的预推理提示词优化方法。该方法通过显式识别语义风险、检查多视角一致性并解决冲突，将模糊提示词转化为逻辑结构清晰的输入。实验表明，该方法在极低成本下显著提升了LLM的推理性能，为提示词工程提供了高效的新思路。
selection_source: fresh_fetch
motivation: 用户输入的自然语言提示词常存在语义歧义，导致大语言模型在推理过程中难以选择正确的路径。
method: 提出一种预推理优化机制，利用小语言模型识别并解决提示词中的语义冲突，生成结构化的清晰输入。
result: 在多个基准测试中，该方法将推理性能提升了2.5个百分点，且额外成本仅为0.02美元。
conclusion: 显式提示词消歧是一种高效且低成本的优化手段，能在不改变模型内部机制的情况下显著提升LLM的推理能力。
---

## 摘要
大语言模型（LLMs）因其卓越的指令遵循能力，正越来越多地被应用于各种复杂的推理任务。然而，模型的性能高度依赖于用户输入提示词的开放性特征。自然语言提示词通常不遵循严格的语法规则，从而产生具有多种解释的歧义查询。这种歧义提示词会干扰模型在回答问题时选择正确的推理路径。先前的研究通过在 LLM 推理过程中应用查询编辑来应对这一挑战，但并未显式地解决歧义的根本原因。为了弥补这一局限性，我们提出了一种通过显式提示词消歧实现的推理前提示词优化机制。具体而言，我们识别提示词中的语义风险，检查其多视角一致性，并解决出现的任何语义冲突。最后，我们将解决后的歧义以逻辑结构化的方式组织起来，作为 LLM 的清晰输入。通过显式地解决语义歧义，我们的方法可以使注意力分布更加集中于语义关键的标记（tokens）。我们还利用小语言模型（SLMs）作为提示词消歧的主要执行器，以受益于其高效的计算。通过在多个基准测试上的全面实验，我们证明了我们的方法在仅花费 0.02 美元的成本下，将推理性能提升了 2.5 个百分点。我们的研究推动了将显式提示词消歧作为一种有效的提示词优化方法，且无需干扰 LLM 推理的内部机制。

## Abstract
Large language models (LLMs) are increasingly utilized in various complex reasoning tasks due to their excellent instruction following capability. However, the model's performance is highly dependent on the open-ended characteristics of the users' input prompt. Natural prompts often do not follow proper syntactic rules, which creates ambiguous queries that yield multiple interpretations. Such ambiguous prompts confuse the model in choosing the correct reasoning paths to answer questions. Prior works address this challenge by applying query editing during the LLM inference process without explicitly solving the root cause of the ambiguity. To address this limitation, we propose a pre-inference prompt optimization mechanism via explicit prompt disambiguation. Particularly, we identify semantic risks in the prompt, check their multi-perspective consistency, and resolve any semantic conflicts that arise. Finally, we organize the resolved ambiguities in a logically structured manner as a clean input to the LLM. By explicitly resolving semantic ambiguity, our method can produce a more focused attention distribution to the semantically essential tokens. We also leverage small language models (SLMs) as the main executor of prompt disambiguation to benefit from their efficient computation. Through comprehensive experiments on multiple benchmarks, we demonstrate that our method improves reasoning performance by 2.5 points at a cost of only \$0.02. Our study promotes explicit prompt disambiguation as an effective prompt optimization method without disturbing the internal mechanism of LLM inference.

---

## 论文详细总结（自动生成）

这篇论文《Small Language Model Helps Resolve Semantic Ambiguity of LLM Prompt》由电子科技大学和庆熙大学的研究团队提出。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：用户输入的自然语言提示词（Prompt）通常具有非规范性、欠指定（underspecified）和内部不一致等特征，导致**语义歧义**。
*   **研究背景**：当提示词存在歧义时，大语言模型（LLM）在推理过程中会产生注意力分散、推理轨迹不稳定以及随机采样方差高等问题。
*   **动机**：现有的提示词优化方法（如 CoT、APE 等）大多依赖 LLM 在推理过程中自行解决歧义，这实际上是将歧义视为下游推理问题，而非上游输入质量问题。本文旨在通过“预推理（Pre-inference）”阶段的显式消歧来提升模型性能。

### 2. 方法论：DisambiguSLM
核心思想是利用**小语言模型（SLM）**作为高效的执行器，在 LLM 推理前对提示词进行扫描、验证和修复。
*   **语义风险建模**：定义语义风险 $R(Q)$ 为注意力熵（分散程度）与推理分支数量的函数。风险类型包括指代歧义、缺失/隐含假设、时间歧义。
*   **三层技术框架**：
    1.  **第一层：语义风险识别**：SLM 作为全局扫描仪，定位可能导致推理不稳定的文本片段（风险点）。
    2.  **第二层：一致性验证与冲突解决**：
        *   **双路径验证**：对每个风险点，调用两次独立的 SLM 生成两种解释。
        *   **一致性检查**：计算两种解释的语义嵌入相似度。若相似度高则融合；若相似度低（存在冲突），则由 SLM 结合上下文生成一个自洽的统一解释。
    3.  **第三层：语义整合与增强**：将所有修复后的语义解释整合为结构化的补充上下文，与原始查询一同输入 LLM。

### 3. 实验设计
*   **数据集/场景**：涵盖五大推理基准测试：
    *   **GPQA**（研究生水平科学问答）、**AGIEval-MATH**（数学推理）、**LIAR**（逻辑一致性）、**WSC**（指代消解）、**BBH-Navigate**（多步导航）。
*   **对比方法**：
    *   基础方法：Naive Prompting, CoT, Rephrase, Step-back。
    *   前沿优化方法：APE, OPRO, PromptAgent, PromptBreeder, TextGrad, SPO。
*   **目标模型**：GPT-4o-mini, Llama-3-70B, DeepSeek-V3。

### 4. 资源与算力
*   **模型使用**：使用 **Llama-3.2-1B** 作为主要的 SLM 执行器。
*   **成本**：论文强调了极高的性价比，单次优化成本仅为 **0.02 美元**，远低于基于 LLM 的优化方法（如 TextGrad 的 13.14 美元）。
*   **算力细节**：文中未明确给出具体的 GPU 型号、数量或训练时长，但提到 SLM 的推理开销极低，适合实时部署。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个 LLM 架构和 5 个基准数据集上进行了全面测试，每组实验运行 3 次取平均值。
*   **消融实验**：针对模块贡献（L1/L2/L3）、SLM 模型大小（1B vs 1.5B vs GPT-4o）、相似度阈值 $\delta$ 的敏感度进行了详细分析。
*   **鲁棒性测试**：专门构建了“歧义增强型”数据集，验证模型在极端模糊输入下的表现。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还通过注意力分布图（Attention Map）深入分析了模型内部机制的变化，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：DisambiguSLM 在所有基准测试中均优于现有方法，平均性能提升约 2.5-8 个百分点，尤其在对歧义敏感的 WSC 任务上表现突出。
*   **注意力优化**：分析显示，消歧后的提示词能显著降低 LLM 内部注意力的熵，使模型将注意力从“汇聚标记（Sink tokens）”和停用词转移到关键的语义锚点上。
*   **稳定性**：该方法显著降低了采样输出的不一致率（Disagreement Rate），提高了推理的可靠性。
*   **SLM 效能**：1B 规模的小模型已足以胜任消歧任务，进一步扩大 SLM 规模带来的收益边际递减。

### 7. 优点
*   **显式消歧**：不同于黑盒优化，该方法提供了可解释的语义修复过程。
*   **极低成本**：利用 SLM 替代 LLM 进行提示词工程，大幅降低了 API 调用成本。
*   **即插即用**：无需修改 LLM 内部参数或进行微调，具有很强的通用性。
*   **理论支撑**：通过信息论（熵）和注意力机制分析，从底层解释了为何消歧能帮助推理。

### 8. 不足与局限
*   **事实性限制**：该方法旨在解决“语义歧义”而非“事实错误”。如果任务瓶颈在于缺乏外部知识而非理解歧义，该方法效果有限。
*   **延迟增加**：虽然 SLM 很快，但在 LLM 推理前增加了额外的处理步骤，对于极高实时性要求的场景可能存在微小延迟。
*   **应用范围**：主要针对推理密集型任务，对于简单的创意写作或开放式对话，消歧的必要性可能较低。

（完）
