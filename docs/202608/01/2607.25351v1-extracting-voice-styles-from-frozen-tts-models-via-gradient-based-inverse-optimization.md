---
title: Extracting Voice Styles from Frozen TTS Models via Gradient-Based Inverse Optimization
title_zh: 通过基于梯度的逆向优化从冻结的 TTS 模型中提取语音风格
authors: Gyeongmin Kim
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.25351v1"
tags: ["query:bopl"]
score: 6.0
evidence: 基于梯度的风格向量优化
tldr: "本研究针对许多TTS系统仅提供预设风格向量而缺乏音频编码器的问题，提出了一种基于梯度下降的逆向优化方法。该方法在保持TTS模型权重冻结的情况下，通过匹配目标录音的WavLM统计特征来直接优化风格向量。由于该目标函数丢弃了时间轴信息，因此无需文本转录或对齐即可实现语音克隆。实验表明，该方法显著提升了合成语音与目标说话人的相似度，验证器接受率从1%大幅提高至53%。"
selection_source: fresh_fetch
motivation: 解决TTS系统因缺少参考编码器而导致用户无法将自有语音转换为模型可接受的风格向量的问题。
method: 利用梯度下降法直接优化风格向量，使其在冻结的TTS模型中生成的音频与目标录音的WavLM统计特征相匹配。
result: "实验显示ECAPA-TDNN相似度从0.132提升至0.413，语音验证器的接受率从1%显著提高到53%。"
conclusion: 通过逆向优化冻结模型，可以在无需文本转录的情况下有效提取语音风格，为语音克隆提供了一种新途径。
---

## 摘要
一些文本转语音（TTS）系统发布了合成模型和预设风格向量，但没有提供将音频转换为此类向量的参考编码器。虽然模型仍然接受风格向量，但拥有自己语音的用户却无法生成对应的向量。我们直接求解该输入，通过梯度下降对已发布的流水线进行逆向处理：所有权重保持冻结，仅针对一段录音的时间池化 WavLM 统计量优化风格向量。由于目标函数丢弃了时间轴，合成文本可以与录音内容不同，因此不需要文本转录和对齐。在来自两个语料库的 154 名说话人上，ECAPA-TDNN 相似度从 0.132 提高到 0.413，ResNet 相似度从 0.099 提高到 0.401，每位说话人的表现均有所提升；在等错误率点（EER）处，验证器将 53% 的恢复语音接受为目标语音，而初始预设向量的接受率仅为 1%。

## Abstract
Some text-to-speech systems ship a synthesis model and preset style vectors but not the reference encoder that turns audio into such a vector. The model still accepts a style vector; a user with a voice of their own cannot produce one. We solve for that input directly, inverting the released pipeline by gradient descent: every weight stays frozen and only the style vector is optimized, against time-pooled WavLM statistics of one recording. Because the objective discards the time axis, the synthesized text may differ from the recording, so no transcript and no alignment are needed. On 154 speakers from two corpora, ECAPA-TDNN similarity rises from 0.132 to 0.413 and ResNet from 0.099 to 0.401, improving for every speaker; a verifier at its equal-error point accepts 53% of the recovered voices as the target, against 1% for the presets they start from.