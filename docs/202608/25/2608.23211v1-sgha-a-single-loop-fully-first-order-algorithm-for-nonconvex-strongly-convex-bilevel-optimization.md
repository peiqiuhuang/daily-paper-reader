---
title: "SGHA: A Single-Loop Fully First-Order Algorithm for Nonconvex-Strongly-Convex Bilevel Optimization"
title_zh: SGHA：一种用于非凸-强凸双层优化的单圈全一阶算法
authors: "Zhihao Gu, Qilong Wu, Junchi Yang"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.23211v1"
tags: ["query:bopl"]
score: 9.0
evidence: 非凸-强凸双层优化的首阶算法
tldr: 本研究针对非凸-强凸（NC-SC）双层优化问题，提出了一种名为SGHA的单层全一阶算法。现有方法多依赖复杂的双重循环或惩罚机制，而SGHA通过将下层平稳性转化为约束，构建正则化拉格朗日函数，并利用有限差分近似海森向量积。该算法在确定性环境下实现了O(ε⁻²)的复杂度，在随机环境下通过随机平滑假设可达到O(ε⁻⁴)，成功匹配了复杂度下界，为高效解决大规模双层优化提供了理论支撑。
selection_source: fresh_fetch
motivation: 旨在克服现有双层优化算法中双重循环结构复杂及对二阶导数依赖性强导致的计算瓶颈。
method: 采用约束重构法将下层优化转化为约束，并结合平滑梯度下降上升法与有限差分技术实现全一阶单层迭代。
result: 算法在确定性与随机场景下均取得了最优的复杂度保证，其中随机设置下的复杂度在特定条件下匹配了理论下界。
conclusion: SGHA证明了单层一阶框架在双层优化中的可行性与高效性，显著提升了非凸-强凸问题的求解效率。
---

## 摘要
在这项工作中，我们研究了仅使用一阶算子（first-order oracles）寻找非凸-强凸（NC-SC）双层优化问题 $ε$-驻点的算子复杂度。现有的达到已知最佳复杂度保证的方法通常依赖于基于惩罚的双圈（double-loop）程序。我们提出了一种新颖的单圈（single-loop）算法，该算法基于一种约束重构，其中下层驻点性质被施加为约束。具体而言，我们通过引入二次正则化项并将对偶变量限制在有界域内来构造正则化拉格朗日函数，然后应用平滑梯度下降上升法（Smoothed Gradient Descent Ascent）[Zhang et al., 2020]，并利用梯度的有限差分来近似 Hessian-向量乘积。我们分别将所得的确定性算法和随机算法称为 SGHA 和 Stoc-SGHA。在确定性设置下，SGHA 实现了 $O(\barκ_y^{5}ε^{-2})$ 的算子复杂度，其中 $\barκ_y$ 表示相关的条件数。在随机设置下，对于任何 $ρ\in(0,1)$，Stoc-SGHA 以至少 $1-ρ$ 的概率实现了 $O\left(\barκ_y^{17}ε^{-6}ρ^{-3}\right)$ 的算子复杂度；在额外的迭代有界假设下，其期望算子复杂度为 $O\left(\barκ_y^{17}ε^{-6}\right)$。此外，在仅对下层目标施加额外的随机平滑性假设下，Stoc-SGHA 的随机算子复杂度在高概率下提升至 $O\left(\barκ_y^{11}ε^{-4}ρ^{-2}\right)$，在期望下提升至 $O\left(\barκ_y^{

## Abstract
In this work, we study the oracle complexity of finding an $ε$-stationary point for nonconvex-strongly-convex (NC-SC) bilevel optimization using only first-order oracles. Existing methods achieving the best-known complexity guarantees typically rely on double-loop, penalty-based procedures. We propose a novel single-loop algorithm based on a constrained reformulation in which lower-level stationarity is imposed as a constraint. Specifically, we construct a regularized Lagrangian by introducing a quadratic regularizer and restricting the dual variable to a bounded domain, and then apply Smoothed Gradient Descent Ascent [Zhang et al., 2020], with Hessian-vector products approximated via finite differences of gradients. We refer to the resulting deterministic and stochastic algorithms as SGHA and Stoc-SGHA, respectively. In the deterministic setting, SGHA achieves an oracle complexity of $O(\barκ_y^{5}ε^{-2})$, where $\barκ_y$ denotes the relevant condition number. In the stochastic setting, Stoc-SGHA achieves an oracle complexity of $O\left(\barκ_y^{17}ε^{-6}ρ^{-3}\right)$ with probability at least $1-ρ$ for any $ρ\in(0,1)$, and an oracle complexity of $O\left(\barκ_y^{17}ε^{-6}\right)$ in expectation under an additional bounded-iterate assumption. Moreover, under an additional stochastic smoothness assumption imposed only on the lower-level objective, the stochastic oracle complexity of Stoc-SGHA improves to $O\left(\barκ_y^{11}ε^{-4}ρ^{-2}\right)$ with high probability and $O\left(\barκ_y^{11}ε^{-4}\right)$ in expectation, matching the $ε$-dependence of the lower bounds.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SGHA**（Smoothed GDA with Hessian Approximation）的新型算法，旨在解决非凸-强凸（NC-SC）双层优化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：双层优化（Bilevel Optimization）在机器学习（如元学习、超参数优化）中应用广泛。然而，现有的高效算法通常依赖于**双重循环（Double-loop）**结构或需要计算高成本的**二阶信息（如 Hessian 矩阵）**。
*   **核心挑战**：如何在仅使用**一阶算子（Fully First-order）**且采用更简单的**单层循环（Single-loop）**框架下，达到理论上的最优收敛复杂度。
*   **整体含义**：论文证明了单层循环算法也可以在确定性设置下达到 $O(\epsilon^{-2})$ 的最优复杂度，并在随机设置下匹配已知的最佳复杂度，填补了单层全一阶算法在双层优化理论中的空白。

### 2. 方法论
*   **核心思想**：将双层优化问题重构为带等式约束的单层优化问题（利用下层问题的最优性条件 $\nabla_y g(x, y) = 0$），并基于此构造正则化拉格朗日函数。
*   **关键技术细节**：
    *   **约束重构**：将下层强凸问题的驻点条件作为约束，引入拉格朗日乘子 $\lambda$。
    *   **正则化拉格朗日函数**：为了处理对偶变量的无界性，将 $\lambda$ 限制在有界域内，并添加二次正则化项使其变为强凹。
    *   **平滑 GDA 框架**：引入辅助变量 $\hat{x}, \hat{y}$ 进行平滑处理，使非凸-强凹的极小极大问题更易于求解。
    *   **有限差分近似**：算法不直接计算 Hessian-向量乘积（HVP），而是利用两个一阶梯度的差分来近似 HVP，从而实现全一阶计算。
*   **算法流程**：在每一步迭代中，同时更新原始变量 $(x, y)$、辅助平滑变量 $(\hat{x}, \hat{y})$ 和对偶变量 $\lambda$。

### 3. 实验设计
*   **实验场景**：
    1.  **逻辑回归正则化学习（Learn-to-regularize）**：在 **20 Newsgroups** 数据集上学习每个特征的正则化参数（特征维度高达 101,631）。
    2.  **数据超清洗（Data Hyper-cleaning）**：在 **MNIST** 数据集上识别被污染的训练样本（标签噪声比例设为 0.5, 0.9, 0.99）。
*   **对比方法（Benchmarks）**：
    *   基于惩罚的方法：F2SA, F2BSA。
    *   基于 HVP 的随机方法：stocBiO, MRBO。
    *   方差缩减方法：VRBO。
    *   基准线：无超参数优化的模型。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）。
*   **训练规模**：实验涉及了大规模特征（>10万维）和完整的数据集（MNIST 7万张图像），迭代次数在 1,000 到 20,000 次之间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的任务（超参数优化与数据清洗）。
*   **充分性分析**：
    *   在数据清洗实验中，测试了三种不同的噪声比例（从 50% 到 99%），展示了算法在极端情况下的鲁棒性。
    *   对比了当前最先进的（SOTA）双层优化算法，实验结果显示 SGHA 在测试损失、准确率以及识别错误样本的能力上均优于基准方法。
    *   **评价**：实验设计较为客观、公平，参数调节遵循了统一的协议。

### 6. 主要结论与发现
*   **理论突破**：
    *   **确定性设置**：SGHA 复杂度为 $O(\epsilon^{-2})$，达到了理论下界。
    *   **随机设置**：Stoc-SGHA 在标准假设下达到 $O(\epsilon^{-6})$；若增加下层目标随机平滑假设，则提升至 $O(\epsilon^{-4})$，匹配了随机一阶算法的 $\epsilon$ 依赖性下界。
*   **性能表现**：SGHA 在处理高维特征和高噪声数据时表现出极强的竞争力，尤其在单层循环的简洁性下依然保持了高性能。

### 7. 优点
*   **简洁高效**：单层循环结构避免了复杂的内层迭代调整，易于工程实现。
*   **全一阶特性**：无需计算二阶导数，显著降低了单步迭代的计算开销和内存占用。
*   **理论完备**：提供了详尽的收敛性证明，且在多种设置下达到了最优或最先进的复杂度。

### 8. 不足与局限
*   **条件数依赖**：虽然对 $\epsilon$ 的依赖性最优，但算法对条件数 $\kappa_y$ 的依赖程度（如 $\kappa_y^{17}$）仍高于某些双圈惩罚类方法，这可能影响其在极度病态问题上的表现。
*   **强凸性限制**：目前理论分析仍局限于下层问题为强凸的情况，对于更一般的凸或非凸下层问题的扩展尚不明确。
*   **随机设置的差距**：在标准随机设置下，其复杂度与理论下界之间仍存在一定的差距（$\epsilon^{-6}$ vs $\epsilon^{-4}$）。

（完）
