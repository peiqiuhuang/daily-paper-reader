---
title: Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression
title_zh: 突破摊销神经符号回归中的简化瓶颈
authors: "Paul Saegert, Ullrich Köthe"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08885v4"
tags: ["query:sr"]
score: 10.0
evidence: 摊销神经符号回归与表达式简化
tldr: 符号回归旨在发现可解释的数学表达式，但摊销神经符号回归常受限于表达式简化的计算瓶颈。本文提出 SimpliPy 简化引擎，其速度比 SymPy 快 100 倍，并基于此构建了 Flash-ANSR 框架。该方法通过高效的规则化简化，实现了大规模训练集的扩展和系统性的数据去重，在保持高推理效率的同时，显著提升了复杂科学表达式的恢复精度和简洁度。
selection_source: fresh_fetch
motivation: 现有的摊销符号回归方法因依赖计算缓慢的通用代数系统进行表达式简化，导致训练和推理效率低下，难以处理复杂的科学问题。
method: 开发了高性能规则化简化引擎 SimpliPy，并将其集成到 Flash-ANSR 框架中，以实现更高效的训练数据处理和模型优化。
result: 实验表明 SimpliPy 实现了百倍加速，使 Flash-ANSR 在精度上显著超越现有摊销基准，并达到了与最先进的直接优化方法 PySR 相当的水平。
conclusion: 突破表达式简化的性能瓶颈是提升摊销符号回归扩展性的关键，该研究为实现高效且精准的科学发现提供了新路径。
---

## 摘要
符号回归 (SR) 旨在发现能够准确描述观测数据的可解释解析表达式。摊销 SR 有望比主流的遗传编程 SR 方法更高效，但目前在扩展到现实科学复杂度方面仍面临挑战。我们发现，一个关键障碍是缺乏将等效表达式快速归约为简洁规范形式的方法。摊销 SR 曾通过 SymPy 等通用计算机代数系统 (CAS) 来解决这一问题，但高昂的计算成本严重限制了训练和推理速度。我们提出了 SimpliPy，这是一个基于规则的简化引擎，在质量相当的情况下，其速度比 SymPy 快 100 倍。这使得摊销 SR 得到了实质性的改进，包括对更大训练集的扩展性、对每个表达式 token 预算的更高效利用，以及针对等效测试表达式的系统性训练集去污染。我们在 Flash-ANSR 框架中展示了这些优势，该框架在 FastSRB 基准测试上的准确率远高于摊销基准模型（NeSymReS、E2E）。此外，它的性能与最先进的直接优化方法 (PySR) 相当，同时随着推理预算的增加，它能恢复出更简洁而非更复杂的表达式。

## Abstract
Symbolic regression (SR) aims to discover interpretable analytical expressions that accurately describe observed data. Amortized SR promises to be much more efficient than the predominant genetic programming SR methods, but currently struggles to scale to realistic scientific complexity. We find that a key obstacle is the lack of a fast reduction of equivalent expressions to a concise normalized form. Amortized SR has addressed this by general-purpose Computer Algebra Systems (CAS) like SymPy, but the high computational cost severely limits training and inference speed. We propose SimpliPy, a rule-based simplification engine achieving a 100-fold speed-up over SymPy at comparable quality. This enables substantial improvements in amortized SR, including scalability to much larger training sets, more efficient use of the per-expression token budget, and systematic training set decontamination with respect to equivalent test expressions. We demonstrate these advantages in our Flash-ANSR framework, which achieves much better accuracy than amortized baselines (NeSymReS, E2E) on the FastSRB benchmark. Moreover, it performs on par with state-of-the-art direct optimization (PySR) while recovering more concise instead of more complex expressions with increasing inference budget.

---

## 论文详细总结（自动生成）

这是一份关于论文《Breaking the Simplification Bottleneck in Amortized Neural Symbolic Regression》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）旨在从观测数据中自动发现简洁、可解释的数学公式。目前主要有两类方法：以 **PySR** 为代表的遗传编程（GP）方法（精度高但每次都要从零搜索，耗时久）和**摊销神经符号回归（Amortized SR）**（通过预训练 Transformer 模型实现秒级推理）。

**核心瓶颈：** 摊销 SR 的性能受限于“表达式简化”。
*   **冗余问题：** 随机生成的公式存在大量等效形式（如 $x+x$ 与 $2x$），如果不简化，模型会浪费大量容量去学习这种“一对多”的映射。
*   **计算瓶颈：** 现有的简化工具（如 SymPy）基于复杂的树遍历，速度极慢。在训练过程中实时简化会导致数据生成速度比模型更新慢几个数量级，迫使研究者只能使用规模较小、多样性差的离线数据集。

### 2. 方法论：核心思想与关键技术
论文提出了 **Flash-ANSR** 框架，其核心是名为 **SimpliPy** 的高性能简化引擎。

*   **SimpliPy 简化引擎：**
    *   **离线规则发现：** 预先通过 Kruskal 算法变体在小规模表达式空间内搜索最优归约规则，构建一个最小生成森林。
    *   **在线模式匹配：** 将代数简化转化为极速的哈希查找和模式匹配。它直接操作 Token 序列，避免了对象解析开销。
    *   **性能：** 在保持与 SymPy 相当的简化质量前提下，实现了 **100 倍的速度提升**。
*   **Flash-ANSR 架构：**
    *   **编码器：** 使用改进的 **Set Transformer**，引入了 Masked RMSSetNorm 以处理变长数据集。
    *   **解码器：** 采用预归一化（Pre-norm）的 Transformer 架构，支持旋转位置编码（RoPE）和 FlashAttention。
    *   **在线数据生成：** 利用 SimpliPy 的速度优势，在训练期间实时生成并简化了 **5.12 亿**个表达式对，支持高达 10 维的输入。
*   **严格去污染（Decontamination）：** 
    *   通过数值和符号双重校验，确保训练集中不包含任何与测试集等效的表达式，保证了实验的严谨性。

### 3. 实验设计
*   **基准测试（Benchmark）：** 使用 **FastSRB**，这是一个基于物理规律（如 SRSD 数据集）的基准，比传统的 Feynman 或 Nguyen 基准更贴近真实的科学应用场景。
*   **对比方法：**
    *   **摊销基准：** NeSymReS、E2E（代表当前最强的神经符号回归模型）。
    *   **优化基准：** PySR（目前工业界和学术界的黄金标准）。
*   **评估维度：** 推理时间与恢复率的 Pareto 前沿、数据稀疏性（点数从 1 到 2048）、噪声鲁棒性（$10^{-3}$ 到 $10^{-1}$）。

### 4. 资源与算力
论文详细列出了不同规模模型的训练开销：
*   **模型系列：** 3M、20M、120M、1B（9.55 亿）参数。
*   **硬件与时长：**
    *   **3M 模型：** RTX 2080Ti，耗时 112 小时。
    *   **20M 模型：** RTX 4090，耗时 56 小时。
    *   **120M 模型：** A100，耗时 340 小时。
    *   **1B 模型：** **H200**，耗时 **657 小时**（约 27 天）。
*   **推理：** 在单张 RTX 4090 上进行，通过增加采样数量来扩展推理计算量。

### 5. 实验数量与充分性
*   **实验规模：** 评估了 115 个复杂的物理表达式，每个表达式配合 5-10 个不同的随机采样数据集。
*   **充分性：** 实验设计非常全面，不仅对比了最终精度，还深入分析了**推理时间缩放规律**（Scaling Laws）。
*   **客观性：** 论文特别强调了“去污染”和“机器精度恢复（$FVU < 10^{-7}$）”的严格标准，避免了以往论文中通过宽松阈值（如 $R^2 > 0.9$）虚标性能的问题。

### 6. 主要结论与发现
*   **性能飞跃：** Flash-ANSR 彻底碾压了之前的摊销模型（NeSymReS 和 E2E），在 FastSRB 上表现出极强的泛化力。
*   **比肩 PySR：** 120M 和 1B 规模的模型在推理时间超过 10 秒后，性能达到了与 PySR 相当的水平，甚至在某些高预算场景下实现超越。
*   **“简洁性反转”现象（亮点）：** 随着推理预算（采样数）增加，PySR 往往通过增加公式复杂度来拟合残差，而 Flash-ANSR 则倾向于找到更简洁、更接近真值的“正确”公式。
*   **双下降现象：** 在数据极度稀疏时，模型会出现类似深度学习的“复杂度峰值”，即在点数不足以约束公式时，模型会尝试用复杂的常数堆叠来插值。

### 7. 优点
*   **工程突破：** SimpliPy 解决了困扰该领域多年的训练效率瓶颈，使得大规模在线训练成为可能。
*   **科学严谨：** 建立了极高标准的评估协议，包括严格的去污染处理，为后续研究树立了标杆。
*   **可扩展性：** 证明了符号回归模型同样符合 Scaling Laws，模型越大、训练数据越多，性能越强。

### 8. 不足与局限
*   **噪声鲁棒性：** 在高噪声（$\eta \ge 0.01$）环境下，Flash-ANSR 的表现不如 PySR。这是因为模型训练时使用的是无噪声数据，噪声被编码器误认为是需要解释的“高频信号”，导致生成了错误的复杂结构。
*   **分布偏移：** 模型对训练先验（如常数的分布范围）依赖较强，如果测试数据的常数远超训练范围，性能会下降。
*   **算力门槛：** 1B 参数模型的训练成本极高，普通实验室难以复现其最高性能。

（完）
