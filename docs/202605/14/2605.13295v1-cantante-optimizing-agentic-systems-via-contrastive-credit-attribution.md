---
title: "CANTANTE: Optimizing Agentic Systems via Contrastive Credit Attribution"
title_zh: CANTANTE：通过对比式信用分配优化智能体系统
authors: Tom Zehle
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13295v1"
tags: ["query:bopl"]
score: 9.0
evidence: 将提示词作为可学习参数的自动提示词优化
tldr: 针对大模型多智能体系统优化中存在的“系统级评分与局部参数不匹配”的信用分配难题，本文提出CANTANTE框架。该框架通过对比同一查询下不同配置的运行结果，将系统级奖励分解为针对单个智能体的更新信号，并将其应用于提示词优化。实验表明，CANTANTE在编程、数学推理和问答任务上显著优于现有优化器，在大幅提升性能的同时降低了推理成本。
selection_source: fresh_fetch
motivation: 多智能体系统的优化面临信用分配难题，即全局系统评分难以有效指导局部智能体参数的调整。
method: 提出CANTANTE框架，通过对比同一查询下不同配置的执行轨迹，将系统级奖励分解为针对每个智能体的对比信用分配信号。
result: "在MBPP和GSM8K任务上分别比最强基准提升了18.9%和12.5%，且在保持高性能的同时降低了推理成本。"
conclusion: CANTANTE证明了对比信用分配能产生有效的局部优化信号，为自动化优化复杂多智能体系统提供了高效方案。
---

## 摘要
基于大语言模型（LLM）的多智能体系统在软件工程、预测建模和检索增强生成等复杂的现实任务中表现出了强大的性能。然而，由于评分仅在系统层面可用，而控制智能体行为的参数是局部的，因此其配置的自动化仍然是一个结构性挑战。我们认为，优化这些系统从根本上说是一个信用分配（credit-assignment）问题。因此，我们提出了 CANTANTE，这是一个通过对比同一查询下多个联合配置的运行结果，将系统级奖励分解为每个智能体更新信号的框架。我们将其应用于提示词优化，将智能体提示词视为可学习的系统参数。我们在编程（MBPP）、数学推理（GSM8K）和多跳问答（HotpotQA）任务上，将 CANTANTE 与 GEPA 和 MIPROv2 进行了对比评估。在这些基准测试中，CANTANTE 在所有评估的优化器中获得了最佳的平均排名，并始终优于未优化的提示词。在 MBPP 上，它比最强的基准模型提高了 18.9 个百分点，在 GSM8K 上提高了 12.5 个百分点，同时推理成本更低。在 HotpotQA 上，它的表现与最强基准模型的差距保持在一个标准差之内。至关重要的是，我们的信用相关性分析证实，归因器产生了有意义的单智能体信号，而不是简单地重复全局系统评分。

## Abstract
LLM-based multi-agent systems have demonstrated strong performance across complex real-world tasks, such as software engineering, predictive modeling, and retrieval-augmented generation. Yet automating their configuration remains a structural challenge, as scores are available only at the system level, whereas the parameters governing agent behavior are local. We argue that optimizing these systems is fundamentally a credit-assignment problem. We therefore introduce CANTANTE, a framework that decomposes system-level rewards into per-agent update signals by contrasting rollouts of multiple joint configurations on the same query. We instantiate it for prompt optimization, treating agent prompts as learnable system parameters. We evaluate CANTANTE against GEPA and MIPROv2 on programming (MBPP), mathematical reasoning (GSM8K), and multi-hop question answering (HotpotQA). Across these benchmarks, CANTANTE achieves the best average rank among all evaluated optimizers and consistently outperforms unoptimized prompts. It improves over the strongest baseline by +18.9 percentage points on MBPP and +12.5 percentage points on GSM8K, while incurring a lower inference cost. It remains within one standard deviation of the strongest baseline on HotpotQA. Crucially, our credit correlation analysis confirms that the attributer produces meaningful per-agent signals rather than echoing the global system score.

---

## 论文详细总结（自动生成）

这是一份关于论文《CANTANTE: Optimizing Agentic Systems via Contrastive Credit Attribution》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：大语言模型（LLM）多智能体系统（MAS）在处理复杂任务时表现优异，但其配置（如提示词、角色分配）主要依赖人工调试。自动优化的核心难点在于**信用分配（Credit Assignment）**：系统反馈通常是全局性的（如“任务是否成功”的标量评分），而参数是局部的（每个智能体有自己的提示词）。
*   **研究动机**：现有的优化方法（如 MIPROv2 或 GEPA）要么将系统视为黑盒整体优化，要么简单地将全局分数传播给所有智能体，这会导致“一人犯错，全员受罚”的误导性更新。
*   **论文目标**：提出一种名为 **CANTANTE** 的框架，通过对比不同配置下的执行轨迹，将全局奖励分解为针对单个智能体的精确更新信号。

### 2. 方法论：核心思想与技术细节
*   **核心思想——对比式组内归因（Contrastive In-Group Attribution）**：
    *   在同一次迭代中，针对同一个查询（Query），生成 $K$ 种不同的智能体联合配置并运行。
    *   利用一个**归因模型（Attributer LLM）**对比这些不同配置的执行轨迹（Trajectories）和最终得分。
    *   通过对比，归因模型能够识别出是哪个智能体的特定表现导致了结果的差异，从而为每个智能体分配 $[-1, +1]$ 范围内的信用分数。
*   **算法流程**：
    1.  **提议**：局部优化器（如 CAPO）为每个智能体提议 $K$ 个候选参数（提示词）。
    2.  **执行**：组合成 $K$ 个系统配置，在任务上运行并获取全局得分和执行轨迹。
    3.  **归因**：归因模型接收同一查询下的多个轨迹，通过对比分析，输出每个智能体在各配置下的信用评分。
    4.  **更新**：将这些局部信用信号反馈给各智能体的局部优化器，进行下一轮迭代。
*   **技术亮点**：该方法独立于具体的局部优化器，且在所有配置得分相同时（如全部失败），归因模型仍能通过分析中间推理过程给出有意义的反馈，避免优化停滞。

### 3. 实验设计
*   **数据集与场景**：
    *   **MBPP (编程)**：使用“计划者-执行者-验证者”循环拓扑。
    *   **GSM8K (数学推理)**：使用“并行执行-共识汇总”拓扑。
    *   **HotpotQA (多跳问答)**：使用“检索-阅读-总结-幻觉检测”的 RAG 拓扑。
*   **对比基准（Baselines）**：
    *   **Initial**：未经优化的初始提示词。
    *   **GEPA**：采用轮询方式顺序优化智能体的基准。
    *   **MIPROv2**：将联合配置视为整体进行贝叶斯优化的强力基准。
*   **评估指标**：测试集准确率、推理成本（Token 消耗）、平均排名。

### 4. 资源与算力
*   **硬件**：使用 **NVIDIA L40 GPU**。
*   **算力消耗**：
    *   主实验耗时约 **100 GPU 小时**。
    *   消融实验耗时约 **30 GPU 小时**。
    *   总计约 **130 GPU 小时**。
*   **模型**：下游智能体使用 Qwen3-30B，优化器和归因模型使用 GPT-OSS-120B。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同领域的基准测试上进行了评估，每个实验运行 3 个随机种子并计算标准差。
*   **消融实验**：非常充分。涵盖了归因组件（恒等归因对比）、提示词优化器选择（EvoPrompt）、少样本（Few-shot）影响、对比组大小（Group size）、数据集规模（低数据状态）等 8 组以上的对比实验。
*   **公平性**：所有优化器共享相同的 **10M Token 优化预算**，确保了资源消耗层面的公平对比。

### 6. 主要结论与发现
*   **性能卓越**：CANTANTE 在所有基准测试中获得了最佳平均排名。在 MBPP 上比最强基准提升 **18.9%**，在 GSM8K 上提升 **12.5%**。
*   **成本效益**：在提升准确率的同时，CANTANTE 优化出的提示词在推理时的 Token 消耗往往更低（例如在 MBPP 和 GSM8K 上比初始提示词更省 Token）。
*   **归因有效性**：信用相关性分析证明，归因模型产生的信号与智能体实际贡献高度相关，而非简单重复全局分数。
*   **鲁棒性**：即使在开发集样本极少（如仅 75 个）的情况下，该方法依然保持高效。

### 7. 优点与亮点
*   **解耦设计**：将“信用分配”与“局部优化”解耦，使其可以灵活适配各种现有的提示词优化算法。
*   **诊断功能**：信用评分的相关性分析可以作为诊断工具，帮助开发者识别多智能体拓扑中的结构性瓶颈（例如发现某个智能体对最终结果几乎没有贡献）。
*   **解决信号稀疏**：对比机制在全局奖励无差异时仍能提供梯度方向，显著提升了搜索效率。

### 8. 不足与局限
*   **拓扑固定**：目前仅优化智能体参数（提示词），不涉及工作流拓扑（图结构）的动态调整。
*   **归因模型依赖**：归因质量受限于归因模型的能力。如果归因模型弱于下游智能体，可能会提供错误的指导信号。
*   **可扩展性限制**：随着智能体数量增加，归因模型需要处理的轨迹长度会呈指数增长，可能超出 LLM 的上下文窗口或导致信息过载（实验目前最高测试了 4 个智能体）。
*   **额外开销**：引入归因模型会消耗额外的优化预算（Token），虽然实验证明这是值得的，但在极端预算受限的情况下可能存在挑战。

（完）
