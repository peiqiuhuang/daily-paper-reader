Title: Solving Stackelberg Vertex Cover on trees using split and join

URL Source: https://arxiv.org/pdf/2607.16078v1

Published Time: Mon, 20 Jul 2026 01:27:17 GMT

Number of Pages: 18

Markdown Content:
# Solving Stackelberg Vertex Cover on trees using split and join 

## Dominik Scheder #

TU Chemnitz, Germany 

## Johannes Tantow #

TU Chemnitz, Germany 

Abstract 

The Stackelberg Vertex Cover problem is a bilevel optimization problem with two players on a graph 

G = ( F ∪ P, E ) where each vertex from F has a weight and the first player selects a price for each vertex in P . Afterwards, the second player finds a minimum vertex cover X and the first player receives the set price for each vertex from X ∩ P . The goal is to maximize the revenue of the first player. This problem was recently shown to be NP-complete for bipartite graphs while being solvable in linear time on paths. We present three new algorithms for solving Stackelberg Vertex Cover on certain kinds of trees: (1) a pseudo-polynomial algorithm working on general trees when all weights are integer, i.e., it is FPT with the maximum weight as a parameter; (2) a strongly polynomial algorithm for trees having the property that the least common ancestor of any two vertices from P

is again in P (this case includes paths); and (3) an FPT-algorithm for trees, where the parameter is the maximum number P -vertices vi that an F -vertex u can reach while using no other P -vertices. These algorithms are based on a lemma that allows us to split instances at a vertex u into multiple sub-instances, which follows from LP duality and integrality of the vertex cover LP on bipartite graphs. The lemma requires that the minimum vertex covers of the sub-instances agree on 

u (either all include u or all don’t). For this we introduce the concept of commitments . Finally, we show that the Stackelberg Vertex Cover problem with commitments is weakly NP-complete. 

2012 ACM Subject Classification Theory of computation → Problems, reductions and complete-ness; Theory of computation → Mathematical optimization; Theory of computation → Dynamic programming 

Keywords and phrases Vertex Cover, Stackelberg Games, Bilevel optimization, Dynamic Program-ming 

Acknowledgements We want to thank Lennart Kauther for helpful discussions 

## 1 Introduction 

A Stackelberg Game is a multi-player game with one distinguished player, the leader, and all other players are called followers. The game consists of a set of items , some of which are owned by the leader. Those are called priceable . In a first phase the leader will select prices for the priceable items. In the second phase the followers will buy items to solve some goal. If an item belonging to the leader is bought, she will receive the price she set for it. The leader wants to set prices such as to maximize the revenue she earns in the second phase. This game is based on the economical model of von Stackelberg[ 16 ]. In computer science, these games are often studied as a bilevel optimization problem where the followers need to solve a combinatorial optimization problem. One of the first examples was the study of the Stackelberg shortest path problem where the leader owns a subset of the edges and sets prices which are interpreted as tolls. Afterwards, the follower needs to solve a shortest path problem and buys the edges on the path. Maximizing the leader’s revenue was shown to be NP -complete for general graphs by Labbé, Marcotte and Savard[ 14 ]. Further work on this problem was by Roch, Savard and Marcotte in [ 15 ], arXiv:2607.16078v1 [cs.DS] 17 Jul 2026 2 Stackelberg Vertex Cover on trees 

by Joret in [ 12 ] or by Briest, Chalermsook, Khanna, Laekhanukit and Nanongkai[ 5]. Other combinatorial problems studied were minimum spanning trees by Cardinal et al.[ 8 ], packing and scheduling problems by Böhnlein, Schaudt und Schauer[ 4] or shortest path trees by Bilo, Gualà, Proietti and Widmayer[2]. Recently, a general toolkit was shown that proves ΣP 

> 2

-completeness of the Stackelberg game for most NP -complete problems by Grüne, Henke, Rotenberg and Wulf[ 11 ]. Thus, there is now mainly an interest in problems where the follower needs to solve a problem from P.All problems from P admit FPT -algorithms for their Stackelberg game when parameterized by the number of priceable items as shown by Böhnlein, Kratsch and Schaudt[3]. 

## 1.1 Problem description 

We focus in this work on the vertex cover problem. This is in P when the given graph is bipartite. Stackelberg Vertex Cover was introduced in [ 6 , 7] by Briest, Hoefer and Krysta. We are given a graph G = ( V, E ) with V = P ˙∪ F and a weight function w : F → R. Vertices in P are called priceable and vertices in F are called fixed-price . There are two players, the leader and the follower. In the first phase the leader picks a pricing function p : P → R,so (G, p ∪ w) is just a usual graph with vertex weights. In the second phase the follower chooses a minimum weight vertex cover X. The revenue for the leader for this vertex cover is Rev (G, p, X ) := ∑  

> x∈P∩X

p(x). If multiple minimum weight vertex covers exists, the follower will pick a vertex cover that maximizes the revenue for the leader. 1 The leader wants to find a pricing function that maximizes their revenue. We focus on the on-follower problem on trees; the multi-follower problem on trees was already shown to be NP -complete [ 6, 7]; for general bipartite graphs, the one-follower problem is NP -complete as shown by Jungnitsch, Peis and Schröder [ 13 ] but admits a 2-approximation [6 , 7]. If all priceable vertices lie on the same side of the bipartition, the problem can be solved in polynomial time[ 6, 7]. Recently, Eickhoff, Kauther and Peis[ 10 ] gave a linear-time algorithm for paths . Although paths are among the simplest graphs imaginable, their algorithm is highly non-trivial. 

## 1.2 Results and ideas 

Consider the tree in Figure 1 where priceable vertices are the rectangular vertices. If we (the leader) were to set the price of π0 to ∞, the follower would be forced to buy all its neighbors (lest they pay an infinite price); this splits the instances into individual subtrees with only one priceable vertex, and we can solve the rest easily achieving a revenue of 15. Thus, if we decide to certainly not sell the priceable vertex π0, we can split the instance by removing π0 and all its neighbors. On the other hand, if we are certain that we want to follower to buy π0, there is no obvious way to split G into subgraphs. Our main technical contribution is a Split-Join lemma that shows that we can still split the tree into subtrees, each of which contain π0, and solve each individually; the catch is that this only works if the leader can incentivize the follower to buy π0 in every sub-instance. We call this requirement a commitment to sell or a Yes-commitment . The optimal solutions of the three sub-instances are, in order from left to right: 

1. Setting p(π0) = 8 and p(π1) = ∞, yielding a revenue of 8.

2. Setting p(π0) = 0 and p(π2) = 4 , earning 4.  

> 1This is a standard convention in the Stackelberg literature. If the follower does not follow it, the leader can enforce it by reducing the price of each priceable item by ϵ.

D. Scheder and J. Tantow 3         

> (a) The tree before (b) Splitting the tree after set-ting π0to ∞
> (c) Splitting the instance when
> π0is supposed to be sold.
> Figure 1 Reducing an instance by splitting.

3. Setting p(π0) = 10 and p(π3) = 3 , earning 13. We can now combine the pricing schemes of the three sub-instances of Stackelberg-VC with commitment by adding up the pricing functions, obtaining p(π0) = 18 , p(π2) = 4 ,

p(π3) = 3 , and p(π1) = ∞, achieving a revenue of 25 . Note that in the middle instance, we could earn 7 by setting p(π0) = ∞ and p(π2) = 7 ; however, this would violate the commitment to sell π0 in every instance, and indeed those three pricing schemes would not combine to a global one in a correct or meaningful way. In Section 2 we formally define Stackelberg VC with commitment and state the Split-Join lemma. Its proof is based on LP duality between and the integrality of the vertex cover LP on bipartite graphs. In the above example we split G into parts at a priceable vertex. If all priceable vertices are leaves, then we cannot split it further. Is it possible to split G into sub-instances at a fixed-price vertex in a meaningful way? Yes, as it turns out, but this operation is much more expensive because we have to decide how the weight w(u) should split into weights in the sub-instances. And we have found no better way than trying all possible ways. This is why our algorithm for general trees is only pseudo-polynomial. We first define a class of trees that can be solved by only splitting on priceable vertices. 

LCA trees and visibility. If G is a tree, select an arbitrary priceable vertex to be its root. We call G an LCA-tree if the least common ancestor of any two priceable vertices is again priceable. 

▶ Definition 1 (Visibility) . Let u, v ∈ V . We say u can see v if, on the path from u to v, all vertices except possibly u and v are fixed-price. The visibility of G is the smallest k such that every u ∈ F can see at most k priceable vertices. 

Note that LCA trees are exactly those trees of visibility at most 2.

▶ Theorem 2. The optimal value and the optimal pricing of the Stackelberg vertex cover game can be computed in linear time on LCA-trees. 

The previous technique allowed us to solve an instance by splitting it at every priceable vertex, solving the small instances that in LCA-trees have at most 2 priceable vertices with 4 Stackelberg Vertex Cover on trees 

commitments. For general trees this might have more than two priceable vertices in the individual components, namely up to k, the visibility number of the tree. If k is small, we can brute-force the individual components: 

▶ Corollary 3. Stackelberg-Vertex-Cover is in FPT , when parameterized by the visibility number. 

Both of these previous results are shown in Section 3. Somewhat orthogonally to these results, we present in Section 5 a pseudopolynomial algorithm for instances with integer weights. This is again based on splitting and joining instances but now at fixed-price vertices 

u, too. As mentioned above, this is more complex because we have to guess the weight of u

in the sub-instances. 

▶ Theorem 4. Stackelberg Vertex cover is solvable in O(|V |w3

> max

) time for trees with integer weights and a maximum weight of wmax .

Finally, we show that our approach cannot lead fundamentally to polynomial time algorithms for general trees. 

▶ Theorem 5. The Stackelberg-Vertex-Cover problem with commitments is NP -hard even with a single commitment. 

## 2 Definitions and Tools 

If the leader chooses a price function p : P → R+0 ∪ {∞} and the follower chooses a minimum-weight vertex cover X ⊆ V for w ∪ p, consider the set P \ X. These are the priceable vertices that the leader does not sell; thus, she could just as well set the weights to ∞, and X would still be a minimum-weight vertex cover. Therefore, there is no harm in requiring that leader strives to make follower buy all vertices in P that have a finite price. 

## 2.1 Feasible Pricing Schemes 

▶ Definition 6. A pricing scheme p : P → R+0 ∪ {∞} is called feasible for a graph G and a weight function w : V (G) → R ∪ {∞} if there is a minimum weight vertex cover X of G for the weight function p ∪ w with 

∀u ∈ P : p(u) < ∞ → u ∈ X . 

By feasible (G, w ) we denote the set of pricing schemes that are feasible for G and w. The 

revenue of a feasible pricing scheme p is 

∑ 

> u∈P:p(u)<∞

p(u) .

▶ Problem 7 (Stackelberg-VC) . Given a graph G = ( V, E ), a partition V = P ∪ F and a weight function w : F → R+0 , find a feasible pricing scheme p : P → R+0 ∪ {∞} of maximum revenue. 

## 2.2 Commitments 

In the course of our algorithms, we will encounter situations where the leader commits to making the follower buy certain vertices but not others. Formally, we define ¯V := {¯v | v ∈ V }.A commitment is a set C ⊆ V ∪ ¯V . It is understood that C is non-contradictory, i.e., v and 

¯v are never both in C. We call a v ∈ C a Yes-commitment to v and ¯v ∈ C a No-commitment to v.D. Scheder and J. Tantow 5

▶ Definition 8. A pricing scheme p : P → R+0 ∪ {∞} is feasible for G, w with commitment 

C if there exists a vertex cover X ⊆ V of minimum cost under w ∪ p such that 

1. u ∈ X for all u ∈ P with p(u) < ∞ (this is just the previous notion of feasibility); 

2. v ∈ X for all v ∈ C;

3. v̸ ∈ X for all ¯v ∈ C.By feasible (G, w, C ) we denote the set of all feasible pricing schemes for G, w with commit-ment C.

If the leader commits to all priceable vertices P being sold, the problem of maximizing revenue becomes tractable: 

▶ Lemma 9 ([ 3 ], Theorem 9) . Computing a feasible pricing scheme for G, w with commitment 

P can be done in polynomial time. 

A key insight of our work is that if G has a cut vertex u ∈ P , i.e. a vertex u such that removing u and its edges increases the number of connected components, then we can split 

G at u into parts and, for each part, solve a smaller Stackelberg-VC problem, albeit with commitments as boundary conditions. Thus, we define: 

▶ Problem 10 (Stackelberg-VC with Commitments) . Given a graph G = ( P ∪ F, E ), a weight function w : F → R+0 , and a set C of commitments, find a pricing scheme p ∈

feasible (G, w, C ) of maximum revenue. 

## 2.3 Splitting Lemmas 

We describe how an instance of Stackelberg VC with commitments can be broken into smaller instances. We say G splits into G1, . . . , G d at u if each Gi = ( Vi, E i) is an induced subgraph of G such that V1 ∪ · · · ∪ Vk = V and V1 ∩ · · · ∩ Vk = {u} and E = E1 ∪ · · · ∪ Ek.2 For the rest of the section, G always denotes a connected bipartite graph, u a cut vertex of G, and 

G1, . . . , G d are two graphs into which G splits at u.If f1, . . . , f d are functions with fi : Ai → R, with arbitrary domains Ai, then we denote by f1 + · · · + fd the function from A := A1 ∪ · · · ∪ Ad to R defined by combining the fi, and adding their values wherever their domains intersect: 

f1 + · · · + fd : A → R

x 7 → ∑

> 1≤i≤dx∈Ai

fi(x) .

▶ Lemma 11 (Split-Join Lemma) . The following two statements hold: 

1. Join. If for each 1 ≤ i ≤ d we have a weight function ci : V (Gi) → R and a minimum weight vertex cover Xi for Gi under ci such that either (i) u ∈ Xi for all i or (ii) u̸ ∈ Xi

for all i, then X := ⋃ Xi is a minimum weight vertex cover for G under c := c1 + · · · + cd.

2. Split. If c : V → R+0 is a weight function and X ⊆ V is a minimum weight vertex cover of G under c, then we can write c = c1 + · · · + cd with ci : V (Gi) → R+0 such that 

Xi := X ∩ V (Gi) is a minimum weight vertex cover of Gi under ci. Furthermore, if 

c(v) ∈ N for all v ∈ V , then we can choose each ci such that Im (ci) ⊆ N, too.           

> 2Note that we technically allow Vi={u}in the split; but even if we forbid this, the split will not be unique when 2≤d < deg (u). See the example: we could have split the central vertex into three parts differently.

6 Stackelberg Vertex Cover on trees 

In words: minimum vertex covers can be joined and split between G and G1, . . . , G d as long as they agree on whether to include u.4

3

20

0

5

3

0

2

3

5

8

4

2

3

73

2

9

5

2

2

0

0

5

3

0

3

2

9

5

2

2

4

3

8

4

2 2

3

2

3

7

0 3

> 2

22 222

0

2

0

Figure 2 Splitting and joining, using the dual solution to determine how the weight of u should be split. 

As a caveat, here is an example where joining the optimal vertex covers of the Gi might give a suboptimal vertex cover for G if the Gi don’t agree on u:22

5

8

4

2

3

73

2

9

5

2

2

3

73

2

9

5

2

0 0

8

4

> 5

22

Figure 3 The union of optimal vertex covers of the Gi can be suboptimal for G and c1 + · · · + cd

if the Gi don’t agree on u.

Proof. For Point 1, the join, let let yi : E(Gi) → R+0 be an optimal dual solution to the weighted vertex cover linear program for G, c i. We combine them to one big y : E(G) → R+0 .This y is a feasible solution to the dual of the weighted vertex cover LP with weight function 

c: if v̸ = u then v belongs to exactly one graph Gi and 

∑

> e∈Ev∈e

y(e) = ∑

> e∈Ev∈e

yi(e) ≤ ci(v),

For v = u we have an additional sum on both sides: 

∑

> e∈Eu∈e

y(e) = ∑

> e∈Eu∈ed

∑

> i=1

yi(e) = 

> d

∑

> i=1

∑

> e∈E(Gi)
> u∈e

yi(e) ≤

> d

∑

> i=1

ci(u) = c(u) .D. Scheder and J. Tantow 7

So y is a feasible dual solution. Since all Gi are bipartite, the minimum weight vertex covers 

Xi are optimal primal solutions. Since all Xi agree on u, we have cost (X) = ∑

> v∈X

c(v) = 

> d

∑

> i=1

∑

> v∈Xi

ci(v)=

> d

∑

> i=1

∑

> e∈E(Gi)

yi(e) (by strong LP duality) 

= ∑

> e∈E(G)

y(e) .

So the value of the primal solution X and the dual solution y agree and thus they are both optimal. For the second part, the split , take an optimal dual solution y : E(G) → R+0 . Define 

yi := y|E(Gi). When choosing how to split c into c1 + · · · + cd, the only thing to decide is how to set ci(u). The dual solution tells us how: Let vi be the unique neighbor of u in Gi.We set ci(u) := yi({u, v i}) for 1 ≤ i ≤ d − 1 and cd(u) = c(u) − c1(u) − · · · − cd−1(u). Since 

y is a feasible dual solution we have ∑di=1 yi({u, v i}) ≤ c(u), from which yd({u, v d}) ≤ cd(u)

immediately follows. For all other v ∈ V (Gi) we simply set ci(v) := c(v). Thus, each yi is a feasible dual solution for the minimum vertex cover LP of Gi with weights ci. We have 

∑

> e∈E

y(e) = 

> d

∑

> i=1

∑

> e∈E(Gi)

yi(e) ≤

> d

∑

> i=1

∑

> v∈Xi

ci(v) (by weak LP duality) 

= ∑

> v∈X

c(v) ,

where the last equality holds because all Xi agree on u. The left-most and right-most expression agree by strong duality , and the central inequality holds for each i individually; thus equality must hold for each i, witnessing that Xi is an optimal primal and yi is an optimal dual solution. Furthermore, if c(v) ∈ N for all v ∈ V , then by the integrality of the matching LP for bipartite graphs, we can choose the optimal dual solution y to consist of only natural numbers, too, and thus Im (ci) ⊆ N. ◀

This lemma lets us split an instance of Stackelberg-VC with commitments into multiple, smaller instances whenever G has a cut vertex u:

▶ Lemma 12 (Splitting a Stackelberg instance at a committed vertex) . Let G = ( P ∪ F, E ) be connected and bipartite and u ∈ P ∪ F a cut vertex, and suppose G splits into G1, . . . , G d

at u. Set Pi := P ∩ V (Gi) and Fi := F ∩ V (Gi). Let C ⊆ V ∪ ¯V be a set of commitments containing one of u and ¯u. Let Ci ⊆ C be the commitments belonging to vertices of graph Gi.

1. Join. If for each 1 ≤ i ≤ d we have a weight function wi : Fi → R+0 and a pricing scheme pi : Pi → R+0 that is feasible for Gi, w i with commitment Ci, then p := p1 + · · · pd

is feasible for G and w := w1 + · · · + wd with commitments C.

2. Split. If w : F → R+0 is a weight function and p : P → R+0 is feasible for G, w with commitment C, then we can split p = p1 + · · · pd and w = w1 + · · · + wd such that each 

pi is feasible for Gi, w i with commitment Ci. Furthermore, if Im (w), Im (p) ⊆ N then we can choose the wi, p i such that Im (wi), Im (pi) ⊆ N, too. 8 Stackelberg Vertex Cover on trees 

Proof. For the first part, the join , by assumption we have vertex covers X1, . . . , X d such that Xi is of minimum weight under ci := wi ∪ pi. Furthermore, Xi fulfills its commitments, meaning they all contain u or all don’t. We can now apply the Join-part of Lemma 11 and see that X := X1 ∪ · · · ∪ Xd is optimal under c := c1 + · · · + cd. Furthermore, it fulfills all commitments in C = C1 ∪ · · · ∪ Cd. Since c = p ∪ w it follows that the pricing scheme p is feasible for G, w with commitment C.For the split part, feasibility of p means that there is a vertex cover X of G that is minimum weight under c := p ∪ w and fulfills all commitments. The Split-part of Lemma 11 tells us that there is a way to write c = c1 + · · · + cd such that Xi := X ∩ V (Gi) is optimal under ci. The only choice is now to set pi := ci|Pi and wi := ci|Fi. Each Xi fulfills the commitment Ci and thus pi is a feasible pricing scheme for Gi, w i with commitment Ci,as claimed. The “furthermore” part also follows directly from the “furthermore” part of Lemma 11. ◀

Lemma 12 works whether u ∈ P or u ∈ F . However, this belies a stark algorithmic difference: if u ∈ P , then there is only one way to split w into w1 + · · · + wd among the graphs Gi. Therefore, we get an algorithmically “easy” splitting step: 

▶ Corollary 13. If the cut vertex u is priceable and C is a commitment containing either u

or ¯u, then 

opt (G, w, C ) = 

> d

∑

> i=1

opt (Gi, w i, C i) , (1) 

where wi := w|Fi and Fi := V (Gi).

If, on the other hand, u ∈ F , then to even construct the sub-instances (Gi, w i), we need to guess the correct splitting of w into w1, . . . , w d:

▶ Corollary 14. If the cut vertex u is fixed-price and C is a commitment containing either u

or ¯u, then 

opt (G, w, C ) = sup 

> w1,...,w d
> wi:Fi→R+0
> w1+··· +wd=wd

∑

> i=1

opt (Gi, w i, C i) , (2) 

where Fi := V (Gi).

There are infinitely many ways to split the weight of a fixed-price vertex in general for Corollary 14. From Lemma 11 we know that the split happens in a way such that the dual solutions stay feasible after the split. It is known that there exists a integral dual solution, if all weights are integers. We show now that for fixed price vertices with integer weights, there also exists an optimal solution with integer prices using a similar argument. This allows us to only consider splits into non-negative integers in this case, which results into a finite amount of splits. 

▶ Lemma 15. Any Stackelberg vertex cover instance on a bipartite graph with integer weights for the fixed price vertices has an optimal solution where every priceable vertex receives an integer price (or infinity). D. Scheder and J. Tantow 9

t

r

u1

u2

u3

v1

v2

w1

w2

w3 

> (a) An LCA-tree

t

r

u1

u2

u3

v1

v2

w1

w2

w3          

> (b) Not an LCA-tree, because the least common an-cestor of u2and v2is rand ris not priceable
> Figure 4 An example and a counter-example of an LCA-tree. Priceable vertices are shown in green.

Proof. Suppose we have an optimal pricing scheme p∗ where this is not the case. Then there must be a vertex v with a non-integer price. If v is not sold in the optimal solution, we can set it to infinity. Otherwise we consider the dual variables. Since v is sold, the dual variables must be tight at v and we can find a path P of edges where the dual variables are all non-integers. If P stops at a non-priceable vertex w, the dual variables are here not tight, since it has a integer weight, but the dual variables at w do not sum to an integer. Then we can increase the price of v which allows to increase/decrease the dual variables alternatingly on P .If P instead ends in another priceable vertex u, then this must also have a non-integer weight and we consider the parity of the length of P or alternatively if u and v are in the same partition of the bipartite graph. If they are, we can increase the price of v and decrease the price of u until one of them is an integer and as previously fix the dual variables. This solution has the same value and one non-integer vertex less. If they are not on the same side of the partition, then we can increase the price of both and thus p∗ is not optimal. ◀

## 3 Solving LCA trees 

We will show how to solve a special class of trees that we call LCA trees. For any vertices 

p1, p 2 ∈ P in an LCA-tree their least common ancestor is again in P .We will solve the Stackelberg-VC problem by splitting at every priceable vertex until we only get graphs with at most two priceable vertices that we can solve easily. Afterwards, we will recombine these using dynamic programming. As we only split at priceable vertices, we only need commitments for priceable vertices. If we have a no-commitment for a priceable vertex, we can just set its price to infinity, which requires buying all neighbors and splits the graph. Thus, we can assume that in the base-case we have a connected bipartite graph and for each priceable vertex a yes-commitment. Given a bipartite graph G with priceable vertices P , we can express the optimal revenue when selling all priceable vertices P as a linear program with 2|P | many constraints by comparing it to the price of a vertex cover X with X ∩ P = Q. For this let wQ be the 10 Stackelberg Vertex Cover on trees u

## v

## !

## ?

## u!

## v

## v

## v

## v

## ?

## ? ?

## ?    

> Figure 5 The instance on the left has a priceable leaf, carrying a commitment (!). Once we have decided on a commitment (?) at v, we can use the Split-Join lemma to split the instance into smaller once; in all but one, vis a priceable leaf, and the only one carrying a commitment; the remaining one, top-right, has exactly two priceable vertices, both carrying a commitment; this instance can be solved in linear time.

extension of the weight function w that 

wQ : V → R+0 ∪ {∞} 

u →



0 if u ∈ Q,

∞ if u ∈ P \ Q,

w(u) if u ∈ F .We denote by vc (G, c ) the weight of the minimum weight vertex cover for a graph G and weight function c : V → R+0 ∪ {∞} and by opt (G, w, C ) the optimal revenue given a graph 

G with weight function w on F and commitment C.

▶ Definition 16 (Linear program computing opt (G, w, P )). For P = {π1, . . . , π k} we introduce the variables p1, . . . , p k. We define the following linear program: 

maximize p1 + · · · + pk

subject to ∑

> i∈P

pi + vc( G, w P ) ≤ ∑

> i∈Q

pi + vc( G, w Q) ∀Q ⊆ P⃗ p ≥⃗ 0 .

Basically, the right-hand side is the minimum cost of all vertex covers X ⊆ V with 

X ∩ P = Q. Since the graph is a tree, we can compute vc (G, w, Q ) in linear time and thus construct the LP in time O(2 |P |n).

▶ Lemma 17. The linear program above computes opt (G, w, P ).

Proof. We show that the set of feasible solutions is the set of prices that sell exactly P . As we maximize the sum of the prices, the result follows. In order to sell exactly P the minimum weight vertex cover that includes all vertices from 

P must be the cheapest. This is exactly V C (G, w ′ 

> P

) plus the sum of the prices. Since wPD. Scheder and J. Tantow 11 u!                          

> v1v2u!
> T0
> u
> T1
> u
> T2
> u
> v1v2T0
> u
> Tv1
> v2
> v1
> Tv2
> Tu,v 1
> Tu,v 2
> u!
> Figure 6 The tree Tu. The trees T0
> u, T 1
> u, . . . , T d. The trees T0
> uand Tu,v iand Tvifor 1≤i≤d.

sets all priceable vertices from P have a price of 0 they are all bought. Every constraint makes sure that this is not larger than a vertex cover that only sells a part of the priceable vertices. This works as wQ again makes sure that only Q is bought. ◀

We could compute now opt (G, w, P ′) by setting all priceable vertices outside of P ′ to infinity and solve the remaining instances via this LP, but this is in general infeasible. 

▶ Lemma 18. opt (G, w, P ) can be computed in linear time if T is a tree and the number of priceable vertices is constant. 

Proof. Since the number of priceable vertices is constant, there is only a constant number of subsets P ′ ⊆ P that we have to check. For each we can compute in linear time the linear program above, which has |P ′| variables and 2|P ′| constraints. Building the LP requires us to compute vc (G, w Q) for each Q ⊆ P ′, which can be done in linear time. The LP itself can be solved in constant time. ◀

For the case of a component with only two priceable vertices we actually solve this even without an LP-algorithm in constant time with only addition, subtraction and minimum operations. Note that a similar linear program for arbitrary Stackelberg games is given in [ 7 ]. 

## 3.1 Dynamic Programming for LCA trees. 

To describe our strongly polynomial algorithm for LCA trees, we need a bit of terminology for talking about subtrees of T and subinstances of the original Stackelberg VC problem on 

T . Suppose we have chosen some priceable vertex to be the root of T .Let u be a priceable vertex. 

Tu is the subtree of T rooted at u, i.e., the subgraph of T induced by u and all its descendants. 

T 0 

> u

is the subtree induced by u and those fixed-priced descendants of u that see u but no priceable vertex besides u.Let u1, . . . , u d denote those children of u that are not in T 0 

> u

. Note that all ui are fixed-price, since otherwise the leader has a monopoly on the edge {u, u i} and opt = ∞. Let 

T iu denote the subtree induced by u, ui, and all descendants of ui.Since T is an LCA tree, Tui sees exactly one priceable vertex besides u, which we call vi.Let Tu,v i denote the graph induced by the set of vertices w that can see both u and vi

(which includes u and v). 12 Stackelberg Vertex Cover on trees 

See Figure 6 for an illustration. Our dynamic programming algorithm computes a table Rev where for each u ∈ P and commitment u? ∈ { u, ¯u}, the value Rev [u?] stores the value opt (Tu, w, u ?). We will now describe how to compute Rev [u?], assuming that Rev [v?] has already been computed for all priceable descendants v of u. By the Split-Join lemma, we have opt (Tu, w, u ?) = 

> d

∑

> i=0

opt (T iu, w, u ?) .

Since the optimal pricing scheme for (T iu, w, u ?) either sells vi or doesn’t, we have opt (T iu, w, u ?) = max  

> v?
> i∈{ vi,¯vi}

opt (T iu, w, {u?, v ? 

> i

}) . (3) Once we have a commitment on v? 

> i

, we can split T iu at vi into Tu,v i and Tvi and obtain opt (T iu, w, {u?, v ? 

> i

}) = opt (Tu,v i , w, {u?, v ? 

> i

}) + opt (Tvi , w, {u?, v ? 

> i

}) .

The first instance, (Tu,v i , w, {u?, v ? 

> i

}), has only two priceable vertices, eache carrying a commitment. Its optimal value can be computed in linear time by the LP described above. The second, instance, (Tvi , w, {u?, v ? 

> i

}), does not contain u anymore and thus can safely ignore the commitment u?. Its optimal value can be looked up under Rev [v? 

> i

]. Therefore, we can compute Rev [u?] by Rev [u] = opt (T 0 

> u

, w, u ?) + 

> d

∑

> i=1

max  

> v?
> i∈{ vi,¯vi}

(opt (Tu,v i , w, {u?, v ? 

> i

}) + Rev [v? 

> i

]) .

Finally, if r is the (priceable) root of T , we can compute the global optimum by opt (G, w ) = max 

> r?∈{ r, ¯r}

opt (G, w, r ?) = max 

> r?∈{ r, ¯r}

Rev [r?] .

▶ Theorem 2. The optimal value and the optimal pricing of the Stackelberg vertex cover game can be computed in linear time on LCA-trees. 

Note that this extends even to trees that are not LCA-trees but have constant visibility :where every fixed-price vertex can see only up to a constant number k of priceable vertices. What changes is (3): child ui of u sees up to k − 1 priceable vertices besides u; call this set 

Vi. Then (3) would become opt (T iu, w, u ?) = max   

> Con Vi

opt (T iu, w, {u?} ∪ C) , (4) where the maximization is over all 2|Vi|− 1 ≤ 2k−1 commitments C to the priceable vertices in Vi.

▶ Corollary 3. Stackelberg-Vertex-Cover is in FPT , when parameterized by the visibility number. 

The algorithm in Theorem 2 of course also works for paths and thus offers an alternative to the algorith by Eickhoff, Kauther and Peis[ 10 ]. In Figure 7 an illustration of how our algorithm computes the optimal revenue on a path is shown. D. Scheder and J. Tantow 13 11        

> 8
> 5
> 3
> 13
> 13 83
> 13 8313 83

max 

+ 

> 313 8

+    

> 313 8
> 13 813 8

max      

> 13 0
> 13
> 813
> 13 813 8

max 

> 0

Figure 7 Our algorithm working on a path. The example is a reduced version of the one used in [ 10 ], and we present it in a recursive fashion rather than in its dynamic programming version. Blue checkmarks mean a yes-commitment; red crosses a no-commitment. For each leaf, the value can be computed by a linear program with up to two variables and three constraints; or by hand. 

## 4 Solving cycles 

Let C be a cycle and contain at least one priceable vertex p. Either we sell all vertices in the optimal solution or there is at least one vertex that is not bought. The former can be computed in polynomial time, since the decision problem is in P[ 3]. In the latter case, this transforms into a path, which is a LCA-tree and thus solvable in polynomial time by Theorem 2. A more efficient way is possible for cycles of an even length with at least one priceable vertex p. In the first option we again set p to infinity and solve the path. The second option, we can transform it into a path by adding a copy of p′ that use one of the edges of p instead and have a commitment to buy both p and p′ in the optimal solution. We can solve this analogously as in Section 3, but exclude the options to not buy p or p′ in the dynamic programming steps. Since this is a bipartite cycle, the dual solutions allow us to split and join p and p′ in a solution as in Lemma 11 

## 5 Stackelberg-VC on trees with integer weights 

While the previous algorithms work for any weights, we will now restrict ourselves to the integer case. We believe that this is a natural restriction as most practical problems can be stated in integers or at least be converted into integers without a huge blow-up. We now want to split also fixed-price vertices using Corollary 14. By Lemma 15, we know that it suffices to only split the weight into integer weights, because there is an optimal solution with integer prices which implies that there is an integral dual solution. This will only result in a pseudo-polynomial algorithm. 

▶ Theorem 4. Stackelberg Vertex cover is solvable in O(|V |w3

> max

) time for trees with integer weights and a maximum weight of wmax .14 Stackelberg Vertex Cover on trees 

We again use a dynamic programming approach similar to Section 3. While we split there the instance at every priceable vertex, we now split it at every vertex. Our base cases are thus now single edges for which we want to compute the optimal revenue given commitments instead of connected components with at most two priceable vertices. As the base case only depends on the weights of the vertices of the edge and not the specific vertices, we can precompute this first in O(w2

> max

) time. We express the revenue of an edge as a function 

f (w1, w 2, c 1, c 2) with the weights w1 and w2 of the endpoints and the commitments c1, c 2.We obtain the optimal revenue on the edge from f (w1, w 2, c 1, c 2) given the commitments and weights or −∞ if this constellation is impossible. For notational consistency, we include a weight even when one of the vertices of the edge is priceable, but then the weight is irrelevant. In the definition of the function we consider the different cases of whether both are fixed-price or one is priceable and the other is fixed price and assume that one can obtain this information from the commitment. As at least one vertex must be bought on this edge f (w1, w 2, ¯v1, ¯v2) = −∞ for any weights 

w1 and w2. Both vertices can only be sold if both vertices have a weight of zero or if one is priceable, but then the revenue is zero. In any other case where both vertices should be sold, 

f results in −∞ .If only one vertex should be sold and both are fixed-price, then the weight of this vertex can’t be larger than the other weight. If one vertex is priceable and only this should be sold, we gain as a revenue the weight of the other vertex. If the fixed-price vertex should be sold, then we can set the price of the priceable vertex to ∞ and gain a revenue of 0.Now suppose that G is a tree with an arbitrary root. We will again consider subtrees rooted at v. Let Children (v) be the set of children of v, i.e. those neighbors in G with a higher depth starting from the root. For a vertex u ∈ V (G), a number x ∈ N, an index 

0 ≤ i ≤ | Children (u)|, and a commitment u? ∈ { u, ¯u}, we define a subproblem (Tu,i , w u,i , u ?)

as follows: 

1. Obtain Tu,i by removing all incident edges to u in T≤u except to its first i children and taking the connected component containing u.

2. Obtain wu,i by having each v ∈ V (Gu,i ) inherit its weight from the original instance 

(G, w )—except possibly u which, if fixed-price, gets assigned weight x.We use the table Rev [u, x, i, u ?] to hold the value opt (Gu,i , w u,i , {u?}).We will show how to compute Rev [u, x, i, u ?] from “lower” entries—those of vertices v

below u or for i′ < i . Let v1, . . . , v i be the i children of u in Gu,i and T1, . . . , T i the subtrees rooted there. For 0 ≤ y ≤ x, 0 ≤ z ≤ w(vi), and a commitment v? 

> i

∈ { vi, ¯vi}, we use the Split-Join-lemma to split (Gu,i , w u,i , {u?}) at u and then at vi into three instances: 

1. u with its first children and their subtrees with u having weight y: (Gu,i −1, w u,i −1[u 7 →

y], {u?}). The optimal value of this instance is Rev [u, y, i − 1, u ?] and can be looked up from the table in O(1) time. 

2. u with its ith child vi: formally the instance on graph ({u, v i}, [u 7 → x − y, v 7 → w(vi) −

z], {{ u, v i}} ). The optimal value of this instance is the function f (x − y, w (vi) − z, u ?, v ?),which we precomputed. 

3. vi with its subtree and vi having weight z, and commitment {v? 

> i

}. The value of this instance is Rev [v, z, |Children (v)|, v ?] and can be looked up from the table in O(1) time. By the Split-Join lemma, this gives us the optimal value if we guess y and z correctly; otherwise, it gives us a value that is less than the optimum. Thus, we can now compute D. Scheder and J. Tantow 15 

Rev [u, x, i, l ] in O(w(u) · w(vi)) time via Rev [u, x, i, u ?] = max  

> v?
> i∈{ v, ¯v}

max  

> 0≤y≤x

max 

> 0≤z≤w(vi)



Rev [u, y, i − 1, u ?]+ f (x − y, w (vi) − z, u ?, v ? 

> i

)+ Rev [vi, z, |Children (vi)|, v ? 

> i

]



The following figure illustrates how the instance (Tu,i , x, i, u ?) is split into three smaller instances: vi−1v1 . . . 

y x − y

w(vi) − z

z

u

vi

vi

u

!

> vi−1v1

# x

w(v i)

# . . . ? ?

?

! !

Figure 8 Computing Rev [u, x, i, u ?] where y is assigned to the first i − 1 children of u and x − y

to the edge to vi. vi assigns w(vi) − z to this edge and z to its children with 0 ≤ z ≤ w(vi)

For the leaves u, we cannot sell them in their subtree, which only contains them and no edges, if they have a positive weight. In all other cases, we get a revenue of zero. Note that this holds even for priceable leaves where the possible additional revenue is obtained by f on the top edge in the previous formula. Rev [u, x, 0, u ?] = 

{−∞ , if x > 0 and u? = u

0 , else For the time complexity we at first do O(w2

> max

) many steps to precompute the f values and then for ∑ 

> v∈V

deg (v) · wmax · 2 many table entries we compute the optimal value in 

O(w2

> max

) and thus get in total a runtime of O(|V |w3

> max

) which concludes the proof 3.In our approach we compute for all distributions of the weight to the subtrees the optimal solution. One might wonder whether there is a more clever way of finding the right distribution as the optimal distribution allows us to solve the problem very easily. This however seems unlikely. 

▶ Theorem 5. The Stackelberg-Vertex-Cover problem with commitments is NP -hard even with a single commitment. 

Proof. We reduce from the partition problem. Given a set A = {a1, a 2, . . . , a n} we construct the following branch for each element ai ∈ A with the common root v of weight 12

∑ni=1 ai.All other weights are denoted in the picture.      

> 3For trees O(|V|) = O(|E|)holds

16 Stackelberg Vertex Cover on trees 

v b : ai c : 2 ai

πi

d : ai 

> 2

π′  

> i
> Figure 9 A branch in the construction with the common root v

We claim that it is possible to get a vertex cover that includes v and gets a revenue of 34

∑ni=1 ai iff the partition instance has a solution. Using the split-join lemma, this is equivalent to finding a distribution of the weight of v to the individual branches, such that v

is sold in every branch and that maximizes the revenue. We can never distribute more than ai to the ai-branch or else b would be sold instead of 

v in this branch. Additionally, not both πi and π′ 

> i

can be sold for a positive amount, since at least one of c or d must be in the vertex cover to cover the edge {c, d }. Therefore, one of the priceable vertices πi and π′ 

> i

must have a neighbor x in the vertex cover and the neighbor of 

x can be removed from the vertex cover if it has a positive price decreasing the cost. If πi is not sold, then we can set the price to infinity and set π′ 

> i

to its maximum weight of ai 

> 2

. If πi is sold instead, then it is better to set π′ 

> i

to infinity so that the weight of d does not decrease the possible price for πi. Additionally, we see that πi can only be sold for a positive amount in a vertex cover that includes v, if v has in this branch a weight of 0 and if 

πi is set to more than ai it is better to sell c instead of b and πi

Using this, we can achieve 34

∑ni=1 ai when we have a solution Y of the partition instance by selling πi if ai ∈ Y and π′ 

> i

else. On the other hand, if we achieve a revenue of 34

∑ni=1 ai with a vertex cover X that includes v, then there must a set X′ of i such that πi is sold for ai. The set X′ is a solution for the partition instance, because Y = {1, . . . , n } \ X′ is the set of branches which distributed accepted the weight and thus ∑ 

> i∈Y

ai = 12

∑ni=1 ai. ◀

Note that this construction actually has a better solution that does not sell v. We can set π′ 

> i

always to infinity and buy b and πi in every branch. It is also not possible to directly encode the requirement of buying v by combining it with a node of very large weight, because then the whole weight of v can be distributed to the branch of the very large weight node. This theorem tells us that in order to find an optimal solution in polynomial time, we must somehow see that buying v here is the worse option without actually computing its value. 

## 6 Conclusion 

In this work we have extended the algorithmic toolbox for Stackelberg-Vertex-Cover, which previously allowed to solve the problem exactly if all priceable vertices were on the same side[ 7 ] (and improved by Baïou and Barahona[ 1]) and if the graph is a path [ 10 ]. Now there is an exact algorithm for LCA trees, a specialized parameterized algorithm compared to the general Stackelberg algorithm [ 3] and the pseudopolynomial algorithm for instances with integer weights offering multiple ways to solve the problems. Together with a currently unpublished NP -completeness result by Eickhoff, Kauther and Peis[ 9], this shows weak-NP -completess for the Stackelberg Vertex-Cover problem on trees D. Scheder and J. Tantow 17 

and settles the general complexity question. Remaining questions about Stackelberg Vertex Cover are whether this result can be extended towards graphs of bounded tree width and if there are other special classes of graphs apart from paths and cycles that admit polynomial time algorithms. An interesting case would be caterpillar and lobster graphs as they are quite close to a path. Another open problem is whether the FPT algorithm can be made fully combinatorial without relying on the linear programs. Outside of the Stackelberg-Vertex Cover problem it might be possible to reuse the technique of using the dual variables to split and combine smaller subproblems to other Stackelberg problems based on decision problems with a natural dual solution to obtain polynomial or pseudopolynomial algorithms. 

References 

1 Mourad Baïou and Francisco Barahona. Stackelberg bipartite vertex cover and the pre-flow algorithm. Algorithmica , 74(3):1174–1183, 2016. URL: https://doi.org/10.1007/ s00453-015-9993-x , doi:10.1007/S00453-015-9993-X .

2 Davide Bilò, Luciano Gualà, Guido Proietti, and Peter Widmayer. Computational aspects of a 2-player stackelberg shortest paths tree game. In Christos H. Papadimitriou and Shuzhong Zhang, editors, Internet and Network Economics, 4th International Workshop, WINE 2008, Shanghai, China, December 17-20, 2008. Proceedings , volume 5385 of Lecture Notes in Computer Science ,pages 251–262. Springer, 2008. doi:10.1007/978-3-540-92185-1\_32 .

3 Toni Böhnlein, Stefan Kratsch, and Oliver Schaudt. Revenue maximization in stackelberg pricing games: Beyond the combinatorial setting. In Ioannis Chatzigiannakis, Piotr Indyk, Fabian Kuhn, and Anca Muscholl, editors, 44th International Colloquium on Automata, Languages, and Programming, ICALP 2017, Warsaw, Poland, July 10-14, 2017 , LIPIcs, pages 46:1–46:13. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2017. URL: https: //doi.org/10.4230/LIPIcs.ICALP.2017.46 , doi:10.4230/LIPICS.ICALP.2017.46 .

4 Toni Böhnlein, Oliver Schaudt, and Joachim Schauer. Stackelberg packing games. In Zachary Friggstad, Jörg-Rüdiger Sack, and Mohammad R. Salavatipour, editors, Algorithms and Data Structures - 16th International Symposium, WADS 2019, Edmonton, AB, Canada, August 5-7, 2019, Proceedings , Lecture Notes in Computer Science, pages 239–253. Springer, 2019. 

doi:10.1007/978-3-030-24766-9\_18 .

5 Patrick Briest, Parinya Chalermsook, Sanjeev Khanna, Bundit Laekhanukit, and Danupon Nanongkai. Improved hardness of approximation for stackelberg shortest-path pricing. In Amin Saberi, editor, Internet and Network Economics - 6th International Workshop, WINE 2010, Stanford, CA, USA, December 13-17, 2010. Proceedings , volume 6484 of Lecture Notes in Computer Science , pages 444–454. Springer, 2010. doi:10.1007/978-3-642-17572-5\_37 .

6 Patrick Briest, Martin Hoefer, and Piotr Krysta. Stackelberg network pricing games. In Susanne Albers and Pascal Weil, editors, Proceedings of the 25th Annual Symposium on Theoretical Aspects of Computer Science, STACS 2008, Bordeaux, France, February 21-23, 2008 , LIPIcs, pages 133–142. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, Germany, 2008. URL: https://doi.org/10.4230/LIPIcs.STACS.2008.1340 , doi:10.4230/ LIPICS.STACS.2008.1340 .

7 Patrick Briest, Martin Hoefer, and Piotr Krysta. Stackelberg network pricing games. Al-gorithmica , 62(3-4):733–753, 2012. URL: https://doi.org/10.1007/s00453-010-9480-3 ,

doi:10.1007/S00453-010-9480-3 .

8 Jean Cardinal, Erik D. Demaine, Samuel Fiorini, Gwenaël Joret, Stefan Langerman, Ilan Newman, and Oren Weimann. The stackelberg minimum spanning tree game. Algorithmica ,59(2):129–144, 2011. URL: https://doi.org/10.1007/s00453-009-9299-y , doi:10.1007/ S00453-009-9299-Y .

9 Katharina Eickhoff, Lennart Kauther, and Britta Peis. Personal communication. 18 Stackelberg Vertex Cover on trees 

10 Katharina Eickhoff, Lennart Kauther, and Britta Peis. Stackelberg vertex cover on a path. In Argyrios Deligkas and Aris Filos-Ratsikas, editors, Algorithmic Game Theory - 16th International Symposium, SAGT 2023, Egham, UK, September 4-7, 2023, Proceedings , Lecture Notes in Computer Science, pages 22–39. Springer, 2023. doi:10.1007/978-3-031-43254-5\ _2 .

11 Christoph Grüne, Dorothee Henke, Eva Rotenberg, and Lasse Wulf. The complexity of stackelberg pricing games. CoRR , abs/2511.05700, 2025. URL: https://doi.org/10.48550/ arXiv.2511.05700 , arXiv:2511.05700 , doi:10.48550/ARXIV.2511.05700 .

12 Gwenaël Joret. Stackelberg network pricing is hard to approximate. Networks , 57(2):117–120, 2011. URL: https://doi.org/10.1002/net.20391 , doi:10.1002/NET.20391 .

13 Karsten Jungnitsch, Britta Peis, and Marc Schröder. Stackelberg max closure with multiple followers. Math. Oper. Res. , 47(4):3010–3024, 2022. URL: https://doi.org/10.1287/moor. 2021.1240 , doi:10.1287/MOOR.2021.1240 .

14 Martine Labbé, Patrice Marcotte, and Gilles Savard. A bilevel model of taxation and its application to optimal highway pricing. Management science , 44(12 Part 1):1608–1622, 1998. 

doi:10.1287/mnsc.44.12.1608 .

15 Sébastien Roch, Gilles Savard, and Patrice Marcotte. An approximation algorithm for stackelberg network pricing. Networks , 46(1):57–67, 2005. URL: https://doi.org/10.1002/ net.20074 , doi:10.1002/NET.20074 .

16 Heinrich von Stackelberg. Market Structure and Equilibrium . Springer, 2011. (Original published in German as Marktform und Gleichgewicht in 1934).