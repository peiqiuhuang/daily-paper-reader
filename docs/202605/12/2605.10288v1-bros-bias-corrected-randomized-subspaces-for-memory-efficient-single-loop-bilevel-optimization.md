---
title: "BROS: Bias-Corrected Randomized Subspaces for Memory-Efficient Single-Loop Bilevel Optimization"
title_zh: BROS：用于内存高效单循环双层优化的偏置修正随机子空间方法
authors: "Hengrui Zhang, Boao Kong, Engao Zhang, Kun Yuan"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10288v1"
tags: ["query:bopl"]
score: 10.0
evidence: 内存高效的单环双层优化
tldr: 针对随机双层优化（SBO）在大规模神经网络中面临的内存压力与收敛性权衡问题，本文提出了一种名为BROS的内存高效单圈优化方法。BROS通过在随机子空间中执行更新，并引入Rademacher双探针校正技术来获取无偏的Hessian-向量乘积估计。该方法在标准假设下实现了与精确单圈方法相同的O(ε⁻²)样本复杂度，并在多项深度学习任务中显著降低了内存消耗且保持了竞争性能。
selection_source: fresh_fetch
motivation: 现有的单圈双层优化方法在处理大型底层网络时，要么内存开销巨大，要么缺乏足够的收敛性理论保证。
method: BROS利用随机子空间更新和Rademacher双探针校正技术，实现了对Hessian-向量乘积的无偏估计。
result: "实验表明，BROS在超数据清洗和ViT样本重加权等任务中，可降低高达44.9%的峰值内存，且性能接近全空间基准。"
conclusion: BROS为大规模双层优化提供了一个兼具内存效率和强理论收敛保证的实用解决方案。
---

## 摘要
随机双层优化（SBO）已成为深度学习中超参数学习、数据重加权、表示学习和数据混合优化的标准框架。现有的精确单循环 SBO 方法和内存高效的代理 SBO 方法，要么对大型低层神经网络造成严重的内存压力，要么在标准假设下缺乏具有竞争力的收敛保证。在本文中，我们提出了 BROS，这是一种内存高效的单循环 SBO 方法，其收敛速度阶数与精确单循环 SBO 方法相同。BROS 在随机子空间中执行低层和辅助更新，并采用 Rademacher 双探针修正（Rademacher bi-probe correction）来恢复无偏的 Hessian-action 估计器。我们证明了在仅满足标准假设的情况下，BROS 保留了 MA-SOBA 寻找 $\varepsilon$-驻点的 $\mathcal O(\varepsilon^{-2})$ 样本复杂度。在超数据清洗、数据混合学习、超表示学习和 ViT 样本重加权上的实验表明，BROS 在将峰值内存降低高达 44.9% 的同时，紧密匹配了全空间基线的性能。

## Abstract
Stochastic bilevel optimization (SBO) has become a standard framework for hyperparameter learning, data reweighting, representation learning, and data-mixture optimization in deep learning. Existing exact single-loop SBO methods and memory-efficient surrogate SBO methods either create severe memory pressure for large lower-level neural networks or lack competitive convergence guarantees under standard assumptions. In this paper, we propose BROS, a memory-efficient single-loop SBO method with the same convergence rate order as exact single-loop SBO methods. BROS performs lower and auxiliary updates in randomized subspaces with a Rademacher bi-probe correction that recovers an unbiased Hessian-action estimator. We prove that BROS preserves the $\mathcal O(\varepsilon^{-2})$ sample complexity of MA-SOBA for finding an $\varepsilon$-stationary point under only standard assumptions. Experiments on hyper-data cleaning, data-mixture learning, hyper-representation learning, and ViT sample reweighting show that BROS reduces peak memory by up to 44.9% while closely matching full-space baseline performance.

---

## 论文详细总结（自动生成）

以下是对论文《BROS: Bias-Corrected Randomized Subspaces for Memory-Efficient Single-Loop Bilevel Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随机双层优化（SBO）在深度学习中应用广泛（如超参数优化、数据重加权、表示学习等）。然而，现有的方法面临**内存与收敛性的两难选择**：
*   **全空间方法（如 MA-SOBA）：** 虽然具有最优的收敛率 $O(\varepsilon^{-2})$，但在处理大规模底层网络时，存储辅助变量和计算 Hessian-向量乘积（HVP）会产生巨大的内存压力。
*   **内存高效的代理方法（如惩罚函数法、一阶法）：** 虽然减少了内存，但通常无法精确收敛到原始双层问题的解，或者需要更强的平滑性假设，且收敛速度较慢。
*   **研究动机：** 能否在保持单循环（Single-loop）算法的最优收敛率的同时，显著降低大规模底层模型的内存开销？

### 2. 核心方法论
论文提出了 **BROS（偏置修正随机子空间双层优化）**，其核心思想是将底层更新和辅助变量更新限制在随机采样的低维子空间中。
*   **随机子空间投影：** 借鉴单层优化中的 RSO/GaLore 思想，在每一步迭代中采样一个 Haar 随机投影矩阵 $P$，仅在投影后的子空间内更新底层变量 $Y$ 和辅助变量 $Z$。
*   **Rademacher 双探针修正（关键技术）：** 简单的子空间投影会导致 Hessian 动作（HVP）产生偏置，进而使超梯度估计失效。BROS 引入了层级 Rademacher 随机向量作为“探针”，通过一种巧妙的修正公式（公式 9），利用额外的子空间 HVP 查询来抵消投影引起的偏置，从而恢复无偏的 Hessian 动作估计。
*   **算法流程：** 采用单循环架构，在一个循环内同时更新上层变量 $x$、底层变量 $Y$、辅助变量 $Z$ 以及移动平均超梯度估计 $h$。所有底层相关的计算（梯度、HVP、JVP）均在投影空间完成。

### 3. 实验设计
论文在四个具有代表性的双层优化场景下进行了验证：
*   **数据集/场景：**
    1.  **MNIST 超数据清洗：** 处理 50% 标签噪声。
    2.  **数据混合学习（Data-mixture）：** 使用 280M 参数的 GPT 风格模型在 17 个 Pile 领域数据集上学习领域权重。
    3.  **CIFAR-10 超表示学习：** 使用 ResNet-18 学习特征表示。
    4.  **CIFAR-100 ViT 样本重加权：** 使用 Vision Transformer (ViT) 处理 20% 标签噪声。
*   **对比方法（Benchmarks）：** 包括全空间基准 **MA-SOBA**，以及内存高效的代理方法 **Penalty**（惩罚法）、**FdeHBO**（有限差分法）、**ZOFO**（零阶法）和 **DoReMi**。

### 4. 资源与算力
论文详细记录了实验所用的硬件资源：
*   **MNIST & CIFAR-10：** 使用单张 NVIDIA A100 GPU (80GB)。
*   **数据混合学习：** 使用 8 张 NVIDIA RTX 4090 GPU (24GB)，采用 DDP 分布式训练。
*   **ViT 样本重加权：** 使用单张 NVIDIA RTX PRO 6000 Blackwell GPU (96GB)。
*   **训练时长：** 未明确给出总时长，但提供了每步迭代的时间对比（BROS 的单步耗时与 MA-SOBA 相当，远快于零阶法）。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从简单的凸问题（MNIST）到大规模非凸深度学习模型（GPT, ViT），跨越了 CV 和 NLP 两个领域。
*   **消融实验：** 针对投影秩（Rank ratio $r/n$）进行了详细的消融研究（从 0.01 到 0.50），验证了不同压缩率对性能的影响。
*   **客观性：** 实验不仅对比了最终精度，还详细对比了峰值内存消耗和收敛曲线，证明了 BROS 在大幅减小内存的同时几乎没有性能损失。实验设计较为公平，使用了统一的超参数搜索和评估标准。

### 6. 主要结论与发现
*   **理论突破：** 证明了 BROS 在标准假设下保持了 $O(\varepsilon^{-2})$ 的样本复杂度，与全空间最优方法阶数一致。
*   **内存优势：** 在 ViT 任务中，BROS 将峰值内存从 23.62 GB 降低至 6.42 GB（**降低约 72.8%**）；在 GPT 代理模型训练中，内存降低了 **44.9%**。
*   **性能匹配：** BROS 的最终测试精度与全空间基准 MA-SOBA 非常接近，显著优于 Penalty 和 FdeHBO 等其他内存高效方法。

### 7. 优点
*   **理论与实践结合：** 既有严谨的收敛性证明，又在实际的大模型（GPT, ViT）上展现了显著的内存节省。
*   **无偏性修正：** 提出的 Rademacher 双探针修正技术解决了子空间优化中 Hessian 偏置的顽疾，具有普适性。
*   **即插即用：** 该方法与具体的优化器（如 Adam, SGD）解耦，易于集成到现有的深度学习框架中。

### 8. 不足与局限
*   **激活值内存：** 虽然显著降低了参数和梯度相关的内存，但在极大规模模型中，激活值（Activations）仍占据相当比例，BROS 对此的优化程度受限于子空间秩的选择。
*   **优化器限制：** 目前的收敛性分析主要基于 SGD 风格的更新，对于 Adam 等自适应优化器的理论证明仍有待完善。
*   **计算开销：** 为了实现偏置修正，每步需要额外的一次子空间 HVP 查询，虽然在内存上是节省的，但在计算量上略有增加。

（完）
