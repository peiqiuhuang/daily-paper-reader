---
title: "SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation"
title_zh: SCMAPR：面向复杂场景文本生成视频的自纠错多智能体提示词优化框架
authors: "Chengyi Yang, Pengzhen Li, Jiayin Qi, Aimin Zhou, Ji Wu, Ji Liu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05489v2"
tags: ["query:bopl"]
score: 8.0
evidence: 用于文本到视频生成的多智能体提示精炼
tldr: 针对文本生成视频（T2V）在复杂场景下因提示词模糊导致的生成质量差问题，本文提出SCMAPR框架。该框架采用多智能体协作模式，通过场景感知路由、策略驱动优化及语义验证驱动的自我修正机制，实现提示词的精细化重写。此外，研究者还推出了T2V-Complexity基准测试集。实验证明，SCMAPR在多个主流榜单上显著提升了视频的语义一致性和生成质量。
selection_source: fresh_fetch
motivation: 现有的T2V系统在处理复杂场景时，常因提示词的歧义和描述不足而导致生成效果不佳。
method: 提出一种多智能体框架，通过场景分类路由、策略化重写和基于语义验证的自我修正机制来优化提示词。
result: 在VBench、EvalCrafter和T2V-CompBench等多个基准测试中，SCMAPR均显著优于现有主流基线模型。
conclusion: SCMAPR通过多智能体协同优化，有效解决了复杂场景下的T2V提示词细化问题，提升了视频生成的准确性。
---

## 摘要
文本生成视频（T2V）受益于扩散模型的最新进展，但现有系统在复杂场景下仍面临挑战，而文本提示词的歧义性和描述不足通常会加剧这一问题。在这项工作中，我们将复杂场景下的提示词优化建模为一个阶段性的多智能体优化过程，并提出了 SCMAPR，即一种用于 T2V 提示词的场景感知自纠错多智能体提示词优化框架。SCMAPR 协调专门的智能体来执行以下操作：(i) 将每个提示词路由到基于分类法的场景以进行策略选择；(ii) 合成场景感知重写策略并执行基于策略的优化；(iii) 进行结构化语义验证，并在检测到违规时触发条件修正。为了明确 T2V 提示词中复杂场景的构成，提供代表性示例，并在这些具有挑战性的条件下进行严格评估，我们进一步引入了 T2V-Complexity，这是一个专门由复杂场景提示词组成的复杂场景 T2V 基准测试。在 3 个现有基准测试和我们的 T2V-Complexity 基准测试上进行的广泛实验表明，SCMAPR 在复杂场景下持续提升了文本-视频对齐度和整体生成质量，在 VBench 和 EvalCrafter 上的平均得分分别提高了 2.67% 和 3.28，在 T2V-CompBench 上相比 3 个最先进（SOTA）基线模型提升了高达 0.028。

## Abstract
Text-to-Video (T2V) generation has benefited from recent advances in diffusion models, yet current systems still struggle under complex scenarios, which are generally exacerbated by the ambiguity and underspecification of text prompts. In this work, we formulate complex-scenario prompt refinement as a stage-wise multi-agent refinement process and propose SCMAPR, i.e., a scenario-aware and Self-Correcting Multi-Agent Prompt Refinement framework for T2V prompting. SCMAPR coordinates specialized agents to (i) route each prompt to a taxonomy-grounded scenario for strategy selection, (ii) synthesize scenario-aware rewriting policies and perform policy-conditioned refinement, and (iii) conduct structured semantic verification that triggers conditional revision when violations are detected. To clarify what constitutes complex scenarios in T2V prompting, provide representative examples, and enable rigorous evaluation under such challenging conditions, we further introduce {T2V-Complexity}, which is a complex-scenario T2V benchmark consisting exclusively of complex-scenario prompts. Extensive experiments on 3 existing benchmarks and our T2V-Complexity benchmark demonstrate that SCMAPR consistently improves text-video alignment and overall generation quality under complex scenarios, achieving up to 2.67\% and 3.28 gains in average score on VBench and EvalCrafter, and up to 0.028 improvement on T2V-CompBench over 3 State-Of-The-Art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《SCMAPR: Self-Correcting Multi-Agent Prompt Refinement for Complex-Scenario Text-to-Video Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的文本生成视频（T2V）模型在处理“复杂场景”时表现不佳。这主要是因为用户提供的原始提示词（Prompts）往往存在歧义、描述不足或缺乏对视频特有的时空约束（如时间一致性、物理因果、复杂空间关系）的定义。
*   **研究背景**：现有的提示词优化方法多针对文本生成图像（T2I），侧重于词汇扩充，而忽视了视频生成中至关重要的动态相干性和语义保真度。此外，缺乏专门针对复杂场景的评估基准，导致难以对模型进行细粒度的诊断。

### 2. 方法论：SCMAPR 框架
SCMAPR 将提示词优化建模为一个**阶段性的多智能体协作过程**，包含 5 个核心阶段和 6 个专门的智能体：

*   **阶段 I：场景路由（Scenario Routing）**：由 `Scenario Router` 智能体根据预定义的 11 类分类法（10 种复杂场景 + 1 种普通场景）对输入提示词进行打标，确定优化的侧重点。
*   **阶段 II：策略合成（Policy Synthesis）**：`Policy Generator` 根据场景标签动态生成重写策略 $\pi$，明确哪些隐式约束（如实体、动作、时间阶段）需要显式化。
*   **阶段 III：策略驱动优化（Prompt Refinement）**：`Prompt Refiner` 在策略指导下将原始提示词重写为更清晰、更具模型友好性的描述。
*   **阶段 IV：语义验证（Semantic Verification）**：
    *   **原子化提取**：`Semantic Atomizer` 将原始提示词分解为原子元素（角色、物体、动作等）。
    *   **蕴含验证**：`Entailment Validator` 通过原子-块匹配（Atom-Chunk Matching），判断优化后的提示词是否涵盖了所有原始语义，并标注为：蕴含（ET）、缺失（MS）或矛盾（CT）。
*   **阶段 V：条件修正（Conditional Revision）**：若检测到语义缺失或矛盾，`Content Reviser` 会根据反馈进行针对性修正，直到满足验收标准。

### 3. 实验设计
*   **基准测试（Benchmarks）**：
    *   **现有基准**：VBench、EvalCrafter、T2V-CompBench。
    *   **新基准**：**T2V-Complexity**，包含 1000 个用户风格的提示词，均衡覆盖了 10 种复杂场景（如抽象描述、复杂空间关系、因果与物理等）。
*   **对比方法**：直接提示（Direct Prompting）、Open-Sora 的提示词优化器、RAPO（检索增强提示词优化）。
*   **底层模型（Backbones）**：采用了 LaVie 和 Wan2.2 两种主流的 T2V 扩散模型作为生成引擎。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备了 **8 张 NVIDIA H100 GPU** 的服务器上运行。
*   **模型配置**：使用 **DeepSeek-V3.2** 作为多智能体的底层大语言模型（LLM），使用 BGE-M3 作为语义匹配的嵌入模型。
*   **训练时长**：由于 SCMAPR 是一个**无需训练（Training-free）**的推理侧框架，因此不涉及模型训练时长，主要开销在于多轮 LLM 推理。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的基准测试上进行了广泛测试，涵盖了数千个提示词。
*   **消融实验**：针对场景路由、策略生成、验证与自纠错机制分别进行了消融实验，验证了各组件的贡献。
*   **客观性**：通过定量指标（如 VBench 的各项得分）和定性视觉对比（视频帧展示）相结合的方式进行评估，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **性能提升**：SCMAPR 在所有基准测试中均取得 SOTA 性能。在 VBench 上平均得分提升高达 2.67%，在 EvalCrafter 上提升 3.28，在 T2V-CompBench 上提升 0.028。
*   **语义保真度**：通过自纠错机制，SCMAPR 显著减少了重写过程中的“幻觉”现象，确保了优化后的提示词严格遵循用户原始意图。
*   **复杂场景优势**：在处理抽象概念、多实体交互和长程时间一致性方面，SCMAPR 表现出明显的优越性。

### 7. 优点与亮点
*   **多智能体协同**：将复杂的优化任务拆解为路由、策略、重写、验证、修正五个环节，逻辑清晰且可解释性强。
*   **自纠错闭环**：引入了基于原子语义的验证机制，解决了 LLM 重写提示词时容易丢失关键信息或产生矛盾的痛点。
*   **分类法贡献**：提出的 10 类复杂场景分类法为 T2V 领域的研究提供了更细致的视角。
*   **即插即用**：作为一种模型无关的框架，可以轻松集成到任何现有的 T2V 生成流水线中。

### 8. 不足与局限
*   **推理开销**：多智能体协作和多轮修正机制增加了推理阶段的计算延迟和 API 调用成本。
*   **LLM 依赖**：框架的效果高度依赖于底层 LLM（如 DeepSeek）的推理和指令遵循能力。
*   **分类法局限**：虽然涵盖了 10 类场景，但随着 T2V 技术的发展（如音视频同步），可能需要进一步扩展分类标签。
*   **实时性**：由于涉及多次验证和可能的修正循环，该方法目前难以满足实时视频生成的场景需求。

（完）
