---
title: "Telegraph English: Semantic Prompt Compression via Structured Symbolic Rewriting"
title_zh: 电报式英语：通过结构化符号重写的语义提示词压缩
authors: "Mikhail L. Arbuzov, Sisong Bei, Ziwei Dong, Dmitri Kalaev, Alexey A. Shvets"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04426v1"
tags: ["query:bopl"]
score: 8.0
evidence: 自动提示压缩与重写协议
tldr: "针对大模型提示词冗长导致的计算成本问题，本文提出Telegraph English (TE) 压缩协议。不同于传统的Token删除方法，TE通过结构化符号重写，将自然语言转化为由逻辑符号连接的原子事实行。该方法在实现约50%压缩率的同时，在LongBench-v2基准测试中展现出极高的精度保留，尤其在小模型上表现显著优于LLMLingua-2，为语义索引和高效提示词工程提供了新方案。"
selection_source: fresh_fetch
motivation: 传统的提示词压缩方法主要依赖Token删除，难以在保持高压缩率的同时精准保留复杂的语义逻辑和结构信息。
method: 提出一种基于符号重写的协议，将输入分解为原子事实行，并使用约40个逻辑和关系符号替代冗长短语，使压缩过程兼具语义分块功能。
result: "在LongBench-v2测试中，TE在50%压缩率下保留了GPT-4.1 99.1%的准确度，且在所有测试模型和任务中均优于LLMLingua-2。"
conclusion: TE通过显式的关系结构补偿了模型能力的局限，实现了压缩与语义索引的统一，证明了结构化重写比单纯删除Token更具优势。
---

## 摘要
我们介绍了“电报式英语”（Telegraph English, TE），这是一种将自然语言重写为富含符号、形式化结构方言的提示词压缩协议。与 LLMLingua-2 等通过训练分类器按固定比例删除低重要性 Token 的 Token 删除方法不同，TE 执行完全的语义重写：它将输入分解为原子事实行，用约 40 个逻辑和关系符号替换冗长短语，并使压缩率能够根据每个文档的信息密度进行自适应调整。行结构规则的一个结果是，压缩与语义分块（semantic chunking）成为了同一种操作——每一行输出都是一个独立可寻址的事实，因此压缩后的表示同时也是一个语义索引。我们在来自 LongBench-v2 的 4,081 个问答对上，针对五种 OpenAI 模型和两个难度级别对 TE 进行了评估。在 Token 减少约 50% 的情况下，TE 在 GPT-4.1 上的关键事实准确率保持在 99.1%，并且在所有测试的模型和任务中，在相同压缩率下均优于 LLMLingua-2。在较小的模型上，这种差距进一步扩大（在细节任务中最高达 11 个百分点），这表明显式的关系结构补偿了有限的模型容量。我们发布了语法规范、压缩提示词、基准数据和参考实现。

## Abstract
We introduce Telegraph English (TE), a prompt-compression protocol that rewrites natural language into a symbol-rich, formally-structured dialect. Where token-deletion methods such as LLMLingua-2 train a classifier to delete low-importance tokens at a fixed ratio, TE performs a full semantic rewrite: it decomposes the input into atomic fact lines, substitutes verbose phrases with $\sim$40 logical and relational symbols, and lets the compression ratio adapt to each document's information density. A consequence of the line-structure rule is that compression and semantic chunking become the same operation -- each output line is an independently addressable fact, so the compressed representation is simultaneously a semantic index. We evaluate TE on 4{,}081 question-answer pairs from LongBench-v2 across five OpenAI models and two difficulty levels. At roughly 50\% token reduction, TE preserves 99.1\% accuracy on key facts with GPT-4.1 and outperforms LLMLingua-2 at matched compression ratios on every model and task tested. The gap widens on smaller models -- up to 11 percentage points on fine-detail tasks -- suggesting that explicit relational structure compensates for limited model capacity. We release the grammar specification, compression prompt, benchmark data, and reference implementation.

---

## 论文详细总结（自动生成）

### 论文总结：Telegraph English (TE) —— 基于结构化符号重写的语义提示词压缩

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）在检索增强生成（RAG）和多智能体系统中的广泛应用，提示词（Prompt）的长度直接决定了推理成本和延迟。现有的压缩方法主要分为**抽取式**（如 LLMLingua-2，通过删除不重要的 Token）和**生成式**（如摘要化）。
然而，传统的 Token 删除方法存在三大缺陷：
*   **逻辑断裂**：删除 Token 可能破坏指代关系和逻辑连接词，导致模型幻觉。
*   **缺乏结构**：输出仅是原句的残缺版，无法直接进行语义索引或动态更新。
*   **不可持续**：仅能压缩输入，无法在多步对话中持续节省成本。
**Telegraph English (TE)** 旨在通过一种形式化的“符号方言”重写提示词，将压缩与语义分块（Semantic Chunking）合二为一。

#### 2. 方法论：核心思想与关键技术
TE 的核心是将自然语言重写为一种高度压缩、结构严密的符号化表示。
*   **核心思想**：遵循“忠实度优先于简洁度”的原则，将输入分解为**原子事实行**（Atomic Fact Lines），并使用约 40 个逻辑和关系符号进行替换。
*   **关键技术细节**：
    *   **符号系统**：使用特定符号表示逻辑关系，如 `→`（因果）、`⇒`（逻辑蕴含）、`∴`（因此）、`∵`（因为）、`∧/∨/¬`（逻辑与/或/非）等。
    *   **原子行结构**：规定每一行只能包含一个独立的事实或步骤，使压缩结果天然成为“语义索引”。
    *   **标签系统**：使用 `PAST:`、`FUTURE:`、`CTX:`（上下文范围）等标签来折叠冗长的语法结构。
    *   **压缩流程**：在单次 LLM 调用中执行六步推理序列：概念识别 → 声明提取 → 关系映射 → 冗余消除 → 数值验证 → 引用核对。
    *   **自适应压缩**：不设定固定压缩率，信息密度高的文本压缩少，冗余文本压缩多。

#### 3. 实验设计
*   **数据集**：使用 **LongBench-v2**，筛选出 339 份长文档，切分为 4,081 个评估单元。
*   **评测场景**：
    *   **Key Facts (4,081 对 QA)**：测试核心概念和主要声明的保留情况。
    *   **Fine Facts (801 对 QA)**：对抗性设计的细节测试，针对数值、条件限制等微小细节。
*   **对比方法**：**LLMLingua-2**（目前最强的 Token 删除基准），分别设定 50% 和 33% 的保留率。
*   **测试模型**：GPT-4.1, GPT-4o, GPT-4o-mini, GPT-4.1-nano。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 硬件型号或训练时长。
*   **实现方式**：压缩过程主要依赖 OpenAI 的 **o4-mini** 模型进行重写，评估过程使用 **GPT-4.1**。由于 TE 是一种基于 Prompt 的协议而非训练出的模型，其主要开销在于压缩时的 API 调用成本。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 4,081 组关键事实实验和 801 组精细细节实验，覆盖了 5 种不同参数规模的模型。
*   **充分性评价**：实验设计较为充分，涵盖了从超大模型到轻量级模型的性能表现，并对比了不同压缩率下的表现。通过引入“对抗性细节测试”，客观地揭示了 Token 删除方法在处理精确信息时的短板。

#### 6. 主要结论与发现
*   **高保真度**：在 50% 的 Token 压缩率下，TE 在 GPT-4.1 上保持了 **99.1%** 的准确率。
*   **全面超越基准**：在所有测试模型和任务中，TE 的表现均优于 LLMLingua-2。
*   **小模型效应显著**：模型规模越小，TE 的优势越明显。在细节任务中，TE 比 LLMLingua-2 高出多达 **11 个百分点**。这表明显式逻辑结构能有效补偿小模型推理能力的不足。
*   **结构化优势**：TE 的输出可直接用于 RAG 检索，无需二次分块，且支持动态上下文管理（如只保留标题或更新特定行）。

#### 7. 优点（亮点）
*   **压缩即索引**：创新性地将压缩与语义分块结合，输出结果具有高度的可解释性和可操作性。
*   **逻辑显性化**：通过符号化逻辑，减少了模型在理解复杂关系时的推理负担。
*   **全链路节省**：TE 格式可贯穿多步 Agent 流程，实现复利式的成本削减，而不仅仅是单次输入压缩。
*   **自适应性**：避免了固定压缩率导致的过度压缩或压缩不足。

#### 8. 不足与局限
*   **压缩成本与延迟**：TE 需要调用 LLM 进行重写，这增加了初始阶段的成本和延迟，不适合仅阅读一次的临时输入。
*   **语言限制**：目前仅支持英语，将其扩展到其他语言（尤其是粘着语或表意文字）具有挑战性。
*   **模型依赖性**：压缩质量高度依赖于执行重写任务的 LLM 的能力。
*   **动态管理缺乏实证**：虽然论文论证了动态上下文管理的架构优势，但尚未在多轮对话的生产环境中进行大规模量化评估。

（完）
