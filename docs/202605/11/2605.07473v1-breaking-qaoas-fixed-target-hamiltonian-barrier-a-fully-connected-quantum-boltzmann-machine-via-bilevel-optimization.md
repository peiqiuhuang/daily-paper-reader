---
title: "Breaking QAOA's Fixed Target Hamiltonian Barrier: A Fully Connected Quantum Boltzmann Machine via Bilevel Optimization"
title_zh: 突破 QAOA 的固定目标哈密顿量壁垒：一种通过双层优化的全连接量子玻尔兹曼机
authors: Jun Liu
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07473v1"
tags: ["query:bopl"]
score: 8.0
evidence: 将QAOA电路扩展为双层优化架构
tldr: 针对经典玻尔兹曼机和现有量子玻尔兹曼机（QBM）的局限性，本文提出一种基于双层优化架构的全连接QBM。该方法通过扩展QAOA电路，将内层循环用于能量最小化，外层循环用于优化目标哈密顿量参数。研究表明，该模型在单层电路下即可实现高精度状态测量，且在当前商业量子计算设备的噪声水平下表现出极强的鲁棒性，能稳定生成目标图像。
selection_source: fresh_fetch
motivation: 旨在克服传统QAOA固定目标哈密顿量的限制，并解决现有玻尔兹曼机在连接性和量子模拟上的不足。
method: 提出一种双层优化架构，结合QAOA内层能量最小化与外层对比散度学习来构建全连接量子玻尔兹曼机。
result: 模型在单层QAOA下达到0.9559的测量概率，且在双倍噪声强度下仍能保持目标态的最高测量概率。
conclusion: 该全连接QBM模型在低深度电路和高噪声环境下均具有优异的性能和鲁棒性，适用于图像生成等量子机器学习任务。
---

## 摘要
为了克服经典部分连接玻尔兹曼机和主流量子玻尔兹曼机（QBM）的局限性，本研究将传统的量子近似优化算法（QAOA）线路扩展为双层优化架构，并提出了一种全连接 QBM。内层循环训练基于传统 QAOA 线路的计算过程模拟正相能量最小化，而外层循环训练则通过优化目标哈密顿量的结构参数来模拟负相对比散度学习。研究发现：首先，该模型在 QAOA 线路仅使用单层（p=1）时即表现出卓越性能，在无噪声条件下测量目标量子态的平均概率达到 0.9559。其次，该模型表现出显著的噪声鲁棒性。在当前主流商用量子计算设备的典型噪声水平下，测量目标量子态的平均概率达到 0.6047；当噪声上升到强度翻倍的更严苛水平时，该概率仍保持在 0.3859。在这两种情况下，目标量子态在所有检测到的状态中均保持最高的测量概率，其数值是排名第二的状态的数倍。这表明即使噪声达到或超过当前主流商用量子计算设备的上限，该模型仍保持强大的鲁棒性。第三，在 p=1 且仅进行 10 次测量采样的逐块学习策略下，无论是否存在噪声干扰，该模型都能稳定生成目标“量子比特”网格图像，展示了在图像生成方面的强大鲁棒性。

## Abstract
To overcome the limitations of classical partially connected Boltzmann machines and mainstream quantum Boltzmann machines (QBMs), this work extends the conventional circuit of the quantum approximate optimization algorithm (QAOA) to a bilevel optimization architecture and proposes a fully connected QBM. The inner-loop training simulates positive phase energy minimization based on the computational process of the conventional QAOA circuit, whereas the outer-loop training simulates negative phase contrastive divergence learning by optimizing the structural parameters of the target Hamiltonian. It is found that, first, the model exhibits superior performance using only a single layer (p=1) in the QAOA circuit, with an average probability of 0.9559 in measuring the target quantum state under noiseless conditions. Second, the model exhibits notable noise robustness. Under the typical noise level of current mainstream commercial quantum computing devices, the average probability of measuring the target quantum state reaches 0.6047; when the noise rises to a more stringent level with doubled intensity, this probability remains at 0.3859. In both scenarios, the target quantum state maintains the highest measurement probability among all detected states, with a value several times higher than that of the second-ranked state. This indicates that the model retains strong robustness even when noise meets or exceeds the upper limit of current mainstream commercial quantum computing devices. Third, under a block-by-block learning strategy with p=1 and only 10 measurement shots, the model consistently generates the target "qubit" grid image regardless of noise interference, demonstrating strong robustness in image generation.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于双层优化架构的全连接量子玻尔兹曼机（QBM），旨在打破传统量子近似优化算法（QAOA）中目标哈密顿量固定的限制。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：经典全连接玻尔兹曼机（Boltzmann Machine）由于配分函数计算的指数级复杂度和 Gibbs 采样的缓慢收敛，在经典计算中难以应用。现有的量子玻尔兹曼机（QBM）大多局限于受限结构（RBM），且基于 QAOA 的方案通常假设目标哈密顿量是预先给定的。
*   **研究动机**：利用量子计算的并行性和叠加态特性，克服全连接结构的计算瓶颈。通过将 QAOA 的目标哈密顿量参数化，构建一个能够自动学习数据分布的生成模型，使其适配当前的噪声中等规模量子（NISQ）硬件。

### 2. 方法论
*   **核心思想**：将全连接玻尔兹曼机的学习过程转化为一个**双层优化（Bilevel Optimization）**问题。
*   **关键技术细节**：
    *   **哈密顿量映射**：将经典能量函数的偏置 $b_i$ 和权重 $w_{ij}$ 映射为目标哈密顿量 $H_1$ 中的 Pauli-Z 算子参数。
    *   **内层循环（正相模拟）**：固定哈密顿量参数，通过优化 QAOA 的变分参数（$\beta, \gamma$）来寻找当前参数下的能量最小化状态（基态）。
    *   **外层循环（负相模拟）**：固定 QAOA 演化参数，通过测量结果与目标分布的均方误差（MSE）作为损失函数，利用 **Parameter-shift rule（参数移位规则）** 计算梯度，更新哈密顿量的结构参数（$b_i, w_{ij}$）。
    *   **学习策略**：采用顺序（Sequential）更新方案，即在每一轮全局迭代中，先完成内层收敛，再进行外层更新。

### 3. 实验设计
*   **实验场景**：基于 4 量子比特电路的原理验证。
*   **任务类型**：
    1.  **分布学习**：学习一个单点分布（目标态为 $|1001\rangle$）。
    2.  **图像生成**：学习并生成一个 $8 \times 20$ 的网格图像（显示“qubit”字样）。
*   **Benchmark 与对比**：
    *   **电路深度对比**：对比了 QAOA 层数 $p=1$ 与 $p=2$ 的收敛效果。
    *   **噪声鲁棒性对比**：设置了无噪声、典型商用噪声（单比特门 0.5%，双比特门 2%）以及严苛噪声（单比特门 1%，双比特门 4%）三种环境。

### 4. 资源与算力
*   **计算平台**：使用 PennyLane 量子仿真平台进行数值模拟。
*   **算力说明**：论文**未明确提及**具体的物理硬件（如 GPU/CPU 型号）或具体的训练时长。由于是 4 量子比特的模拟，计算需求相对较低，重点在于算法逻辑的验证。

### 5. 实验数量与充分性
*   **实验规模**：针对每种配置（不同深度、不同噪声水平）均进行了 **20 次独立重复实验**。
*   **充分性评价**：实验设计较为系统，包含了消融实验（层数影响）、压力测试（高强度噪声）和实际应用展示（图像生成）。通过均值和标准差（误差带）展示结果，具有较好的客观性和说服力。但受限于模拟环境，量子比特规模较小。

### 6. 主要结论与发现
*   **深度并非越深越好**：在 NISQ 环境下，$p=1$ 的表现优于 $p=2$，因为更深的电路引入了更多的门操作和累积误差。
*   **高精度拟合**：无噪声下，$p=1$ 模型测量目标态的平均概率高达 0.9559。
*   **极强的鲁棒性**：在双倍于当前商用水平的噪声下，目标态的测量概率（0.3859）依然显著高于其他干扰态（数倍差距）。
*   **高效生成**：在图像生成任务中，仅需 10 次测量采样（Shots）即可在噪声环境下准确重建目标图案。

### 7. 优点
*   **架构创新**：打破了 QAOA 目标哈密顿量不可变的传统思维，为量子机器学习提供了新范式。
*   **硬件友好**：证明了极浅层电路（$p=1$）在全连接模型中的有效性，非常适合当前的 NISQ 设备。
*   **理论完备**：将统计物理中的玻尔兹曼学习逻辑与量子变分算法有机结合，解释性强。

### 8. 不足与局限
*   **规模扩展性**：实验仅限于 4 个量子比特，尚未探讨在大规模量子比特下是否存在梯度消失（贫瘠高原，Barren Plateaus）问题。
*   **分布复杂度**：主要测试了单点分布（One-point distribution），对于具有多个局部最优解或更复杂多峰分布的拟合能力有待进一步验证。
*   **经典模拟限制**：由于采用经典仿真，无法完全体现真实量子硬件中的相干性损失等非去极化噪声的影响。

（完）
