---
title: Robust Self-Training with Closed-loop Label Correction for Learning from Noisy Labels
title_zh: 针对噪声标签学习的闭环标签纠正鲁棒自训练
authors: "Zhanhui Lin, Yanlin Liu, Sanping Zhou"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13894v1"
tags: ["query:bopl"]
score: 8.0
evidence: 用于标签纠错的解耦双层优化
tldr: 针对深度学习中噪声标签导致性能下降的问题，本文提出一种基于解耦双层优化的自训练标签校正框架。该框架通过分类器与神经校正函数的协同演化，利用少量干净数据进行噪声后验模拟和特征知识迁移，构建闭环反馈系统以防止误差放大。实验证明该方法在保持高效率的同时达到了SOTA性能，并具有坚实的理论保障。
selection_source: fresh_fetch
motivation: 现有噪声标签处理方法往往存在样本利用效率低和计算开销过大的局限性。
method: 提出一种解耦双层优化框架，利用神经校正函数与分类器的协同演化及闭环反馈机制进行标签校正。
result: 在CIFAR和Clothing1M等多个基准数据集上实现了最先进的性能，并有效降低了训练时间。
conclusion: 该闭环自训练框架为噪声标签学习提供了一种高效且鲁棒的解决方案，兼具理论稳定性和实际应用潜力。
---

## 摘要
使用噪声标签训练深度神经网络仍然是一个重大挑战，通常会导致性能下降。现有的处理标签噪声的方法通常依赖于转移矩阵、噪声检测或元学习技术，但它们往往表现出噪声样本利用效率低且计算成本高的问题。在本文中，我们提出了一种使用解耦双层优化的自训练标签纠正框架，其中分类器和神经纠正函数共同演化。利用一个小型干净数据集，我们的方法采用噪声后验模拟和中间特征来传递真实标签知识，形成一个防止错误放大的闭环反馈系统。理论保证支撑了我们方法的稳定性，在 CIFAR 和 Clothing1M 等基准数据集上的广泛实验证实了其在缩短训练时间的同时达到了最先进的性能，突显了其在噪声标签学习中的实际应用价值。

## Abstract
Training deep neural networks with noisy labels remains a significant challenge, often leading to degraded performance. Existing methods for handling label noise typically rely on either transition matrix, noise detection, or meta-learning techniques, but they often exhibit low utilization efficiency of noisy samples and incur high computational costs. In this paper, we propose a self-training label correction framework using decoupled bilevel optimization, where a classifier and neural correction function co-evolve. Leveraging a small clean dataset, our method employs noisy posterior simulation and intermediate features to transfer ground-truth knowledge, forming a closed-loop feedback system that prevents error amplification. Theoretical guarantees underpin the stability of our approach, and extensive experiments on benchmark datasets like CIFAR and Clothing1M confirm state-of-the-art performance with reduced training time, highlighting its practical applicability for learning from noisy labels.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Robust Self-Training with Closed-loop Label Correction** 的框架，旨在解决深度学习在处理噪声标签（Noisy Labels）时容易过拟合噪声、泛化能力差的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度神经网络（DNNs）具有极强的记忆能力，在含有噪声的数据集上训练时会优先学习简单模式，随后逐渐“背诵”噪声标签，导致性能大幅下降。
*   **研究动机**：
    *   现有的**转移矩阵法**难以处理复杂的实例相关噪声（Instance-dependent noise）。
    *   **元学习方法**（Meta-learning）虽然效果好，但其双层优化的内循环计算开销巨大，且难以扩展到大规模场景。
    *   **样本筛选法**往往会丢弃大量有价值的噪声样本，导致数据利用率低。
*   **整体含义**：论文试图利用极少量的干净数据（Clean Dataset）作为“锚点”，通过一种高效、解耦的闭环反馈机制，实现对噪声标签的动态纠正。

### 2. 方法论
核心思想是让**分类器**和**神经纠正函数**（Correction Function）协同演化，形成闭环。
*   **噪声后验模拟（Noisy Posterior Simulation）**：为了在没有“噪声-干净”标签对的情况下训练纠正函数，作者设计了一个双头网络。其中一个头专门模拟噪声分布 $p(\tilde{y}|x)$，并在干净数据集上生成模拟的噪声标签，从而构建训练纠正函数的监督信号。
*   **神经纠正模块（Neural Correction Module）**：采用一个轻量级的 MLP，输入不仅包括噪声后验概率，还包括**中间特征表示**（Intermediate Features）。作者认为中间特征比最终输出包含更丰富的语义信息，能更好地识别分类错误。
*   **鲁棒凸组合（Robust Convex Combination）**：为了防止纠正过程中的误差放大，新标签被定义为历史预测值的凸组合。论文给出了理论证明（Proposition 1），确保这种组合方式下的期望风险是单调不增的，保证了训练的稳定性。
*   **闭环流程**：分类器在纠正后的标签上训练 $\rightarrow$ 提取更优特征 $\rightarrow$ 纠正函数利用更优特征进一步提升标签质量 $\rightarrow$ 循环往复。

### 3. 实验设计
*   **数据集**：
    *   **CIFAR-10 / CIFAR-100**：人工注入对称噪声（Symmetric）、非对称噪声（Asymmetric）和实例相关噪声（Instance-dependent）。
    *   **Clothing1M**：包含 100 万张图片的真实世界大规模噪声数据集。
*   **Benchmark（对比方法）**：
    *   基础方法：Cross-Entropy。
    *   转移矩阵类：FasTEN。
    *   元学习类：MW-Net, L2B, EMLC。
    *   其他先进方法：DMLP。
*   **实验设置**：在 CIFAR 中通常使用 5000 个干净样本（9:1 比例），并额外测试了仅 1000 个干净样本的极端情况。

### 4. 资源与算力
*   **硬件**：实验主要在单张 **NVIDIA RTX 4090 GPU** 上完成。
*   **训练时长**：
    *   在 CIFAR-100 上，该方法平均训练时间约为 **0.35 小时**，仅为元学习方法（如 L2B 的 1.61 小时）的约 1/4。
    *   在 Clothing1M 上，每轮（Epoch）耗时约 **0.55 小时**，而对比方法 L2B 需要 3.47 小时。
*   **效率说明**：标签纠正模块的优化仅占总训练时间的 **1%** 左右，计算开销极低。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 3 种噪声类型、3 种噪声比例（0.2, 0.4, 0.6）以及 2 个不同规模的数据集。
*   **消融实验**：非常充分。作者对比了：
    1.  输入模态的影响（中间特征 vs. 最终逻辑值 vs. 伪标签）。
    2.  凸组合机制的必要性（在数据稀缺时尤为关键）。
    3.  超参数 $\lambda$（损失平衡因子）和纠正频率的敏感性分析。
*   **客观性**：通过在真实世界数据集 Clothing1M 上的表现，证明了方法不仅在人工噪声下有效，在实际复杂噪声环境下同样鲁棒。

### 6. 主要结论与发现
*   **性能领先**：在所有测试的噪声场景下，该方法均达到了 SOTA 性能，尤其在复杂的实例相关噪声下优势明显。
*   **稳定性**：理论保证和实验均证明，该方法不会像传统自训练那样出现“误差累积”导致的性能崩溃。
*   **特征价值**：实验证实，使用中间特征作为纠正函数的输入显著优于直接使用 Softmax 输出的伪标签。
*   **高效性**：解耦的双层优化避免了计算高阶导数，使其在大规模数据集上具有极高的实用价值。

### 7. 优点
*   **理论支撑**：提供了关于风险界限的理论证明，增强了算法的可信度。
*   **计算友好**：相比元学习方法，极大地降低了显存占用和训练时间，适合工业级应用。
*   **闭环设计**：通过反馈机制解决了自训练中常见的标签漂移问题。
*   **灵活性**：纠正模块是轻量级的“插件”，易于集成到现有的分类架构中。

### 8. 不足与局限
*   **依赖干净数据**：虽然需求量小，但仍需要一小部分完全可靠的干净数据集（Dm），在某些极端领域（如罕见病医疗影像）获取这部分数据仍有难度。
*   **超参数调节**：虽然实验证明 $\lambda$ 和更新频率相对鲁棒，但在面对全新的噪声分布时，可能仍需微调以达到最优效果。
*   **冷启动依赖**：算法初期依赖一个“预热”（Warm-up）阶段，如果初始噪声极其严重，预热阶段提取的特征质量可能会影响后续纠正的收敛速度。

（完）
