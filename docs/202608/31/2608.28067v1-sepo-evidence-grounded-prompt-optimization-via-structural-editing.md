---
title: "SEPO: Evidence-Grounded Prompt Optimization via Structural Editing"
title_zh: SEPO：通过结构化编辑实现的基于证据的提示词优化
authors: "Xiaoyu Ma, Haoyue Liu, Yiwen Li, Jionghao Zhu, Zhichao Wang, Ye Chen, Xiaoying Tang"
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.28067v1"
tags: ["query:bopl"]
score: 9.0
evidence: 通过结构化编辑和反馈实现自动化提示词优化
tldr: SEPO是一种新型提示词优化框架，旨在解决现有方法将提示词视为不透明整体、缺乏局部可编辑性的问题。它通过两层结构化模式对提示词进行局部编辑，并建立编辑操作与样本效果（修复或破坏）之间的证据链反馈。实验表明，SEPO在多个模型上显著提升了准确率，同时大幅降低了优化成本并缩短了提示词长度，实现了更具可解释性和高效的自动化提示词工程。
selection_source: fresh_fetch
motivation: 现有的提示词优化器通常将提示词作为整体重写，导致优化过程缺乏局部可定位性、可归因性以及机器可读的编辑记录。
method: 引入两层结构化提示词模式，通过局部编辑稳定单元并建立“编辑-效果”证据链，利用历史反馈指导多路径搜索优化。
result: 在14个任务上，SEPO比最强基准GEPA提升了2.2至3.1个百分点，且优化成本更低，生成的提示词长度缩短了5倍以上。
conclusion: SEPO通过结构化编辑和证据反馈机制，使提示词优化变得可寻址且更高效，在优化时间和推理成本上均达到了帕累托前沿。
---

## 摘要
现有的仅限 API 的提示词优化器通常被描述为具有可解释性，但在实践中，这通常仅意味着事后可检查性：每次迭代仍然将提示词重写为一个不透明的字符串，留下的是全量提示词的差异轨迹，而非可定位、机器可读的编辑。本文介绍了 SEPO（结构化、基于证据的提示词优化），这是一种以“编辑-效果”谱系反馈为核心的多轨迹提示词优化器。SEPO 不再将每次迭代视为孤立的全量提示词重写，而是在双层提示词模式中局部编辑稳定的类型化单元，将每次编辑的目标和实现的结构化操作与其新修复或破坏的示例联系起来，并将此编辑-效果记录传递下去，以指导同一搜索分支上后续的架构调用。这使得提示词优化变得可寻址、可归因且可操作。在包含 14 个任务的留出测试集中，SEPO 在 Llama-3.1-8B-Instruct 上比最强基线 GEPA 提升了 3.1 个百分点，在 Qwen3-8B 上提升了 2.2 个百分点，宏准确率分别达到 61.9% 和 73.3%。SEPO 还处于优化时间和测试时间的帕累托前沿，消耗 2.9M 优化 token（GEPA 为 4.1M），且生成的提示词缩短了 5 倍以上。

## Abstract
Existing API-only prompt optimisers are often described as interpretable, but in practice, this usually means only post-hoc inspectability: each iteration still rewrites the prompt as one opaque string, leaving a trace of full-prompt diffs rather than localisable, machine-readable edits. This paper introduces SEPO (Structural, Evidence-grounded Prompt Optimization), a multi-trajectory prompt optimiser centred on edit-effect lineage feedback. Rather than treating each iteration as an isolated whole-prompt rewrite, SEPO locally edits stable, typed units in a two-layer prompt schema, links the target and realised structural operations of each edit to the examples it newly fixes or breaks, and carries this edit-effect record forward to guide later architect calls on the same search branch. This makes prompt optimisation addressable, attributable, and actionable. Across a 14-task held-out suite, SEPO improves over the strongest baseline, GEPA, by 3.1 pp on Llama-3.1-8B-Instruct and 2.2 pp on Qwen3-8B, reaching 61.9% and 73.3% macro accuracy. SEPO also lies on both the optimisation-time and test-time Pareto frontiers, spending 2.9M optimisation tokens versus 4.1M for GEPA and producing prompts over 5x shorter.

---

## 论文详细总结（自动生成）

这是一份关于论文《SEPO: Evidence-Grounded Prompt Optimization via Structural Editing》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的“仅限 API”的提示词优化器（如 OPRO, ProTeGi 等）通常将提示词视为一个**不透明的整体字符串**进行重写。
*   **研究动机**：
    *   **缺乏可定位性**：全量重写无法明确指出是提示词的哪一部分导致了性能提升或下降。
    *   **缺乏可归因性**：难以将特定的编辑操作与具体的样本效果（修复了哪些、破坏了哪些）联系起来。
    *   **提示词膨胀与回归**：由于缺乏对“父子版本”变动的精细控制，优化过程容易引入新的错误（回归），或导致提示词变得冗长且难以维护。
*   **整体含义**：SEPO 旨在通过**结构化编辑**和**证据链反馈**，使提示词优化过程变得可寻址、可归因且可操作。

### 2. 论文提出的方法论
SEPO 的核心思想是**编辑-效果谱系反馈（Edit-Effect Lineage Feedback）**，其关键技术细节包括：
*   **两层提示词模式（Two-layer Schema）**：将提示词表示为“顶层大纲（Outline）+ 步骤局部规则（Step-local Rules）”。这种结构允许优化器针对特定步骤进行局部修改，而不是重写全文。
*   **五步迭代流程**：
    1.  **父节点选择**：从存档中利用 Lexicase 选择算法挑选具有代表性的父代提示词。
    2.  **构建证据包**：收集被当前编辑“新修复”和“新破坏”的训练样本，作为反馈证据。
    3.  **归因架构师（Attribution Architect）**：分析证据，确定需要编辑的特定目标（如某个步骤或大纲结构）及失败原因。
    4.  **补丁架构师（Patch Architect）**：根据归因结果生成局部修改后的新提示词。
    5.  **评估与存档**：通过“预检查”和“全量评估”，根据准确率、提示词长度和唯一覆盖范围决定是否将其存入存档。
*   **操作符推理**：通过对编辑前后的结构进行 Diff 对比，自动识别操作类型（添加、删除、合并、精炼规则等），形成机器可读的编辑记录。

### 3. 实验设计
*   **数据集/场景**：涵盖 14 个留出任务，分为四大类：
    *   **BBH (8个任务)**：涵盖逻辑推理、因果判断、词语排序等。
    *   **MMLU-Pro (3个任务)**：计算机科学、经济学、心理学。
    *   **代码 (MBPP+)**：Python 代码生成。
    *   **推理 (GSM-Hard, HotpotQA)**：多步数学推理和多跳问答。
*   **基准模型（Worker LLMs）**：Llama-3.1-8B-Instruct 和 Qwen3-8B。
*   **对比方法（Baselines）**：
    *   未优化的种子提示词（SEED）。
    *   单字符串重写类：OPRO。
    *   结构化/分解类：APSF, MIPRO V2, MPO。
    *   进化/反射类：GEPA（最强基准）。

### 4. 资源与算力
*   **架构师模型（Optimiser）**：使用 Qwen3.5-397B-A17B 作为执行归因和补丁生成的“架构师”。
*   **算力消耗**：
    *   论文未明确说明具体的 GPU 型号和数量。
    *   **Token 消耗**：SEPO 在优化阶段平均消耗 2.9M tokens，显著低于 GEPA 的 4.1M。
    *   **预算控制**：每个任务/种子严格限制在 2000 次度量调用（Metric Calls）以内。

### 5. 实验数量与充分性
*   **实验规模**：在 14 个任务上进行了测试，每个任务运行 3 个独立随机种子，总计进行了大量的自动化迭代。
*   **消融实验**：针对核心原则（P1 局部编辑、P2 谱系反馈、P3 多轨迹搜索）分别进行了消融，验证了每个组件的必要性。
*   **客观性与公平性**：所有基准方法均在相同的度量调用预算（2000次）下运行，且起始于相同的任务无关种子提示词，确保了对比的公平性。

### 6. 论文的主要结论与发现
*   **性能领先**：SEPO 在 Llama 上比 GEPA 提升 3.1 pp，在 Qwen 上提升 2.2 pp。
*   **帕累托前沿**：SEPO 同时达到了**优化成本最低**和**推理成本最低**（提示词最短）的帕累托最优。其生成的提示词比 GEPA 短 5 倍以上。
*   **任务敏感性**：在需要严谨步骤的程序化任务（如词语排序、代码生成）中，SEPO 的优势最为明显（如词语排序任务提升了 18.9 pp）。
*   **可审计性**：SEPO 能够生成详细的编辑日志，记录“为什么改、改了哪、效果如何”，显著提升了优化过程的透明度。

### 7. 优点
*   **高效性**：通过局部编辑避免了全量重写的冗余计算，大幅降低了 Token 成本。
*   **可解释性强**：将抽象的提示词优化转化为具体的、可追踪的结构化编辑记录。
*   **鲁棒性**：利用“新修复/新破坏”的证据链有效抑制了优化过程中的回归现象。
*   **简洁性**：生成的提示词非常精炼，有利于降低下游推理的延迟和费用。

### 8. 不足与局限
*   **Schema 依赖**：目前采用固定的两层模式，对于极其复杂或非线性的 Agent 工作流，该模式可能需要进一步扩展。
*   **任务特定性**：SEPO 为每个任务生成专门的 Schema，目前尚不支持跨任务的通用提示词迁移。
*   **覆盖范围**：实验主要集中在单轮文本任务，尚未在多模态或长文本对话场景中进行验证。
*   **算法局限**：对于某些极长序列的算法任务（如超长列表排序），提示词优化的提升空间受限于模型本身的推理能力，而非提示词质量。

（完）
