---
title: "PEML: Parameter-efficient Multi-Task Learning with Optimized Continuous Prompts"
title_zh: PEML：基于优化连续提示的参数高效多任务学习
authors: "Anjir Ahmed Chowdhury, Syed Zawad, Xiaolong Ma, Xu Dong, Feng Yan"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14055v1"
tags: ["query:bopl"]
score: 9.0
evidence: 基于优化连续提示的参数高效多任务学习
tldr: "本研究针对大语言模型在多任务学习中的参数高效微调（PEFT）挑战，提出了PEML框架。现有方法如LoRA或Prefix Tuning往往孤立地优化模型权重或提示词，限制了多任务适配能力。PEML通过神经架构工程优化连续提示词，并结合低秩自适应技术协同调整模型权重。在GLUE和MMLU等多个基准测试中，PEML显著优于MTL-LoRA和MoE等主流方法，平均准确率提升达6.67%，为高效多任务模型部署提供了新方案。"
selection_source: fresh_fetch
motivation: 现有的参数高效微调方法在多任务学习中未能有效结合提示词优化与模型权重适配，限制了模型在处理复杂多任务时的性能表现。
method: 提出PEML框架，利用神经架构工程自动优化连续提示词，并同步结合低秩自适应（LoRA）技术对模型权重进行微调。
result: "在GLUE、MMLU等多个权威基准测试中，PEML相比现有SOTA方法实现了最高6.67%的平均准确率提升，单项任务增益高达10.75%。"
conclusion: 通过协同优化连续提示词与模型权重，PEML显著增强了大语言模型在多任务场景下的适配能力与资源利用效率。
---

## 摘要
参数高效微调（PEFT）被广泛用于使大语言模型（LLMs）适配各种任务。最近，针对多个任务微调单个 LLM 的需求日益增长，因为得益于任务间共享的共同特征，这种方式微调所需的总数据量更少。更重要的是，LLM 对资源需求极高，为多个任务部署单个模型有助于资源整合，且与为每个任务部署单独的大模型相比，消耗的资源显著减少。现有的 PEFT 方法（如 LoRA 和 Prefix Tuning）旨在使 LLM 适配特定任务。LoRA 及其变体侧重于针对任务调整模型本身，忽视了多任务学习中提示微调（prompt tuning）的重要性；而 Prefix Tuning 仅采用简单的架构来优化提示，这限制了其在多任务中的适配能力。为了实现高效的多任务学习微调，协同优化提示优化与模型适配至关重要。在这项工作中，我们提出了 PEML（参数高效多任务学习），它采用神经架构工程方法来优化连续提示，同时对模型权重进行低秩适配。我们通过创建一个自动化框架来原型化 PEML，用于优化连续提示并适配模型权重。我们在 GLUE、SuperGLUE、大规模多任务语言理解（MMLU）和常识推理基准测试上，将 PEML 与最先进的多任务学习方法（MTL-LoRA、MultiLoRa、C-Poly 和 MoE）进行了评估。评估结果显示，平均准确率提升高达 6.67%，个别任务的峰值增益达到 10.75%。

## Abstract
Parameter-Efficient Fine-Tuning (PEFT) is widely used for adapting Large Language Models (LLMs) for various tasks. Recently, there has been an increasing demand for fine-tuning a single LLM for multiple tasks because it requires overall less data for fine-tuning thanks to the common features shared among tasks. More importantly, LLMs are resource demanding and deploying a single model for multiple tasks facilitates resource consolidation and consumes significantly less resources compared to deploying individual large model for each task. Existing PEFT methods like LoRA and Prefix Tuning are designed to adapt LLMs to a specific task. LoRA and its variation focus on aligning the model itself for tasks, overlooking the importance of prompt tuning in multi-task learning while Prefix Tuning only adopts a simple architecture to optimize prompts, which limits the adaption capabilities for multi-task. To enable efficient fine-tuning for multi-task learning, it is important to co-optimize prompt optimization and model adaptation. In this work, we propose a Parameter-Efficient Multi-task Learning (\PM), which employs a neural architecture engineering method for optimizing the continuous prompts while also performing low-rank adaption for model weights. We prototype PEML by creating an automated framework for optimizing the continuous prompts and adapting model weights. We evaluate PEML against state-of-the-arts multi-task learning methods MTL-LoRA, MultiLoRa, C-Poly, and MoE, on the GLUE, SuperGLUE, Massive Multitask Language Understanding, and commonsense reasoning benchmarks. The evaluation results present an average accuracy improvement of up to 6.67%, with individual tasks showing peak gains of up to 10.75%.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **PEML**（Parameter-efficient Multi-Task Learning），一种旨在提升大语言模型（LLM）多任务处理能力的参数高效微调框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着模型规模扩大，全参数微调成本极高。现有的参数高效微调（PEFT）方法（如 LoRA 和 Prefix Tuning）主要针对单任务设计。
*   **核心挑战**：在多任务学习（MTL）场景下，LoRA 侧重于模型权重的低秩适配而忽视了输入提示（Prompt）的对齐；而 Prefix Tuning 架构过于简单固定，难以捕捉任务间的复杂关联。此外，为每个任务部署独立适配器会导致推理时的显存开销和频繁切换的延迟。
*   **研究目标**：通过协同优化“提示词架构”与“模型权重适配”，在减少资源消耗的同时，提升模型在多任务环境下的泛化性能。

### 2. 方法论
*   **核心思想**：将 **PrefixNAS**（神经架构搜索优化的连续提示）与 **LoRA**（低秩自适应）相结合，构建一个统一的、可自动优化的多任务微调框架。
*   **关键技术细节**：
    *   **PrefixNAS**：利用基于梯度的连续松弛技术，在预定义的搜索空间内动态寻找最优的提示词编码器架构。搜索空间包含线性变换、多种激活函数（ReLU, Tanh, GELU 等）、LayerNorm 和不同比例的 Dropout。
    *   **LoRA 适配**：在 Transformer 的所有投影层（Q, K, V, O）应用低秩矩阵更新。
    *   **联合优化流程**：
        1.  **内层优化**：通过 PrefixNAS 搜索并生成一个共享的、统一的连续提示向量，将其拼接到输入序列中。
        2.  **外层优化**：使用 TPE（Tree-structured Parzen Estimator）算法进行超参数搜索（如学习率、前缀长度等）。
        3.  **并行训练**：LoRA 参数与 PrefixNAS 架构参数同步更新，确保模型权重与输入对齐方式达到最优匹配。
    *   **推理部署**：训练完成后，LoRA 权重可合并至原模型，仅保留一个优化后的统一前缀模块，无需针对不同任务切换适配器。

### 3. 实验设计
*   **数据集/场景**：
    *   **GLUE & SuperGLUE**：通用语言理解任务。
    *   **MMLU**：大规模多任务语言理解。
    *   **常识推理**：包括 PIQA、SIQA、Winogrande、ARC 等 8 个任务。
*   **Benchmark 与对比方法**：
    *   对比了 **LoRA-MT**、**MultiLoRA**、**MTL-LoRA**、**C-Poly**、**MoE-LoRA** 等最先进的多任务 PEFT 方法。
    *   使用了 T5-Large、FLAN-T5-Large、LLaMA-7B 和 LLaMA2-7B 作为基础模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 (40GB) GPU**。
*   **训练/搜索时长**：
    *   **LLaMA 系列**：单次基准测试的搜索过程约需 **2 小时**（总计 16 GPU-hours）。
    *   **T5 系列**：搜索过程约需 **30 分钟**（总计 4 GPU-hours）。
*   **说明**：论文强调该搜索过程是针对每个基准测试的一次性投入。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括 4 个大型基准测试集，涵盖数十个子任务。
*   **消融与分析**：
    *   **消融实验**：验证了层数、块重复、跳跃连接等对性能的影响。
    *   **敏感性分析**：探讨了任务采样权重（如 HellaSwag 任务）对整体性能的权衡。
    *   **迁移性测试**：验证了 NAS 发现的架构在不同数据集间的通用性。
*   **充分性评价**：实验设计严谨，通过多次独立运行取平均值，并对比了最新的 SOTA 方法，实验结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能卓越**：PEML 在所有基准测试中均表现优异，平均准确率提升高达 **6.67%**，部分任务增益达 **10.75%**。
*   **架构优势**：自动搜索出的提示词架构显著优于手动设计的固定架构（如简单的 Embedding 层）。
*   **效率提升**：相比 MultiLoRA，PEML 避免了显存随任务/模块数量线性增长的问题；相比 MTL-LoRA，减少了推理时的适配器切换延迟（约减少 8%-17%）。
*   **协同效应**：证明了同时优化模型权重（LoRA）和输入对齐（PrefixNAS）比单一优化更有效。

### 7. 优点与亮点
*   **自动化程度高**：引入 NAS 解决了 PEFT 方法中提示词架构难以手动设计的痛点。
*   **部署友好**：统一的适配器设计极大地简化了多任务推理的复杂性。
*   **显存高效**：在保持高秩（High Rank）表达能力的同时，维持了较低的显存占用（VRAM）。
*   **理论支撑**：论文提供了详细的收敛性分析，证明了联合优化的稳定性。

### 8. 不足与局限
*   **搜索成本**：虽然是一次性成本，但 NAS 过程仍比标准 LoRA 微调需要更多的初始算力和时间。
*   **任务冲突**：在某些极端任务（如 HellaSwag）上，为了整体泛化性能，可能需要牺牲部分特定任务的精度（存在 MTL 常见的负迁移风险）。
*   **参数微增**：虽然是参数高效的，但相比纯 LoRA，PrefixNAS 引入了额外的架构参数（约占模型参数的 2%-8%）。

（完）
