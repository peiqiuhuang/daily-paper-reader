---
title: "GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent"
title_zh: GradMem：通过测试时梯度下降学习将上下文写入内存
authors: "Yuri Kuratov, Matvey Kairov, Aydar Bulatov, Ivan Rodkin, Mikhail Burtsev"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13875v1"
tags: ["query:bopl"]
score: 8.0
evidence: 在前缀记忆标记上进行梯度下降
tldr: 针对大语言模型处理长上下文时KV缓存内存开销大的问题，本文提出GradMem。该方法通过在推理阶段对少量前缀记忆令牌进行梯度下降优化，将上下文信息压缩进固定大小的记忆状态中。GradMem利用自监督重构损失进行迭代式写入，相比传统的单向前向写入方法，在键值检索和自然语言任务上表现出更强的记忆容量和泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决Transformer模型在处理长上下文时因KV缓存导致的巨大内存开销问题。
method: 通过在冻结模型权重的情况下，利用测试时梯度下降优化一小组前缀记忆令牌来重构上下文信息。
result: 在键值检索任务中显著优于前向写入方法，并在bAbI和SQuAD等自然语言理解任务中取得了竞争性结果。
conclusion: GradMem证明了基于损失驱动的迭代优化比传统的前向记忆写入能更有效地提升压缩记忆的存储容量和准确性。
---

## 摘要
许多大语言模型应用需要基于长上下文进行条件生成。Transformer 通常通过存储每层过去激活的大型 KV 缓存来支持这一点，这会产生巨大的内存开销。一个理想的替代方案是压缩内存：一次性读取上下文，将其存储在紧凑状态中，并根据该状态回答多个查询。我们在上下文移除设置下对此进行研究，即模型在推理时必须在无法访问原始上下文的情况下生成答案。我们引入了 GradMem，它通过逐样本的测试时优化将上下文写入内存。给定一个上下文，GradMem 在保持模型权重冻结的情况下，对一小组前缀内存标记执行几步梯度下降。GradMem 显式地优化模型级的自监督上下文重建损失，从而实现具有迭代误差校正的损失驱动写入操作，这与仅前向的方法不同。在关联键值检索任务上，GradMem 在相同内存大小的情况下优于仅前向的内存写入器，且额外的梯度步骤比重复的前向写入更有效地扩展了容量。我们进一步证明 GradMem 可以迁移到合成基准之外：配合预训练语言模型，它在包括 bAbI 和 SQuAD 变体在内的自然语言任务上取得了具有竞争力的结果，且仅依赖于编码在内存中的信息。

## Abstract
Many large language model applications require conditioning on long contexts. Transformers typically support this by storing a large per-layer KV-cache of past activations, which incurs substantial memory overhead. A desirable alternative is ompressive memory: read a context once, store it in a compact state, and answer many queries from that state. We study this in a context removal setting, where the model must generate an answer without access to the original context at inference time. We introduce GradMem, which writes context into memory via per-sample test-time optimization. Given a context, GradMem performs a few steps of gradient descent on a small set of prefix memory tokens while keeping model weights frozen. GradMem explicitly optimizes a model-level self-supervised context reconstruction loss, resulting in a loss-driven write operation with iterative error correction, unlike forward-only methods. On associative key--value retrieval, GradMem outperforms forward-only memory writers with the same memory size, and additional gradient steps scale capacity much more effectively than repeated forward writes. We further show that GradMem transfers beyond synthetic benchmarks: with pretrained language models, it attains competitive results on natural language tasks including bAbI and SQuAD variants, relying only on information encoded in memory.

---

## 论文详细总结（自动生成）

这是一份关于论文《GradMem: Learning to Write Context into Memory with Test-Time Gradient Descent》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在处理长上下文时，传统的 KV-cache 机制会产生巨大的内存开销，且难以生成可移植、可复用的上下文表示。
*   **研究动机**：现有的“压缩内存”方案（如 RMT）多采用前向传播（Forward-only）的方式将上下文编码进隐藏状态，这种方式缺乏反馈机制，无法在写入过程中纠正错误。
*   **整体含义**：本文提出 **GradMem**，将上下文存储视为一个**测试时优化问题**。通过在推理阶段对一小组“内存标记”（Memory Tokens）进行梯度下降，模型能够以迭代纠错的方式将上下文信息“写入”紧凑的内存状态中，从而在不访问原始上下文的情况下回答多个查询。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将内存标记视为可学习的参数，在推理时针对特定上下文进行微调（Test-Time Training），而保持预训练模型权重冻结。
*   **关键流程**：
    1.  **WRITE 阶段（写入）**：给定上下文 $C$，从元学习得到的初始内存 $M_0$ 开始，执行 $K$ 步梯度下降。目标是最小化自监督重构损失 $L_{write}$（即模型基于当前内存 $M$ 预测上下文 token 的交叉熵）。
        *   公式：$M_{k+1} = M_k - \alpha \nabla_{M_k} L_{write}(M_k; C)$
    2.  **READ 阶段（读取）**：丢弃原始上下文 $C$，仅使用优化后的内存 $\hat{M}$ 和查询 $Q$ 来预测目标 $Y$。
*   **关键技术细节**：
    *   **元学习（Meta-Learning）**：使用类似 MAML 的二阶优化方法训练模型参数 $\theta$ 和初始内存 $M_0$，使得模型只需极少数（$K \le 5$）梯度步数就能有效编码信息。
    *   **任务无关目标**：写入过程仅依赖于上下文重构，不需要下游任务的标签。
    *   **内存参数化**：内存被表示为 $m$ 个 $d$ 维向量，作为 Transformer 输入的前缀（Prefix）。

### 3. 实验设计
*   **数据集与场景**：
    *   **关联 KV 检索（Synthetic）**：核心基准，测试在固定内存大小下能存储多少对 Key-Value。
    *   **bAbI (QA1-5)**：测试逻辑推理和多事实组合能力。
    *   **Short SQuAD**：测试自然语言理解和精确信息提取。
    *   **WikiText-103**：测试语言建模中的长程依赖压缩。
*   **对比方法（Baselines）**：
    *   **Full-Attention Transformer**：使用完整 KV-cache 的性能上限。
    *   **Mamba**：强力的状态空间模型（SSM）代表。
    *   **RMT (Recurrent Memory Transformer)**：前向写入内存的代表。
    *   **ARMT (Associative RMT)**：使用关联矩阵存储信息的改进版前向模型。

### 4. 资源与算力
*   **硬件使用**：论文在延迟测试和消融实验中明确提到了使用 **NVIDIA A100 GPU**。
*   **算力开销**：
    *   **训练阶段**：由于需要通过梯度下降过程进行反向传播（二阶导数），计算和显存开销显著高于普通训练。
    *   **推理阶段**：WRITE 阶段比单次前向传播慢（因为有 $K$ 次梯度更新），但一旦写入完成，多次 READ 操作的开销极低（只需处理少量内存标记而非长上下文）。
*   **未明确信息**：论文未详细列出训练所有模型所需的总 GPU 小时数或集群规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 KV 检索任务做了详尽的容量扩展实验（从 4 对到 96 对 KV）。
    *   针对自然语言任务（bAbI, SQuAD, LM）验证了方法的泛化性。
    *   进行了关于梯度步数 $K$ 的外推实验（训练用 $K=1, 2$，推理用更大的 $K$）。
*   **充分性与公平性**：实验设计较为客观。作者特意对比了“重复前向前向写入”与“梯度写入”，证明了性能提升源于梯度优化而非仅仅是增加了计算量。同时，在相同内存大小（8 个标记）下进行对比，保证了比较的公平性。

### 6. 主要结论与发现
*   **梯度优于前向**：在相同内存容量下，基于梯度的写入显著优于纯前向传播的写入（如 RMT）。
*   **容量可扩展性**：增加测试时的梯度步数（$K$）能有效提升内存容量，甚至在推理时使用比训练时更多的步数（$K_{eval} > K_{train}$）仍能带来增益。
*   **选择性记忆**：在 KV 检索中，GradMem 倾向于优先降低 Value 的重构损失，表明模型学会了自动识别并存储对回答查询更有用的信息。
*   **跨模型迁移**：该机制可应用于预训练模型（如 GPT-2, Pythia），在自然语言任务上表现出竞争力。

### 7. 优点
*   **迭代纠错能力**：通过损失函数驱动，模型能感知哪些信息尚未存入内存并进行补充。
*   **计算-容量权衡**：用户可以通过调整梯度步数 $K$ 来灵活平衡推理成本与记忆精度。
*   **架构兼容性**：不需要修改 Transformer 的核心结构，只需在输入端增加可学习的内存标记。

### 8. 不足与局限
*   **训练复杂度高**：二阶导数的计算（Double Backward）对显存要求极高，且与现有的高性能算子（如 FlashAttention）不直接兼容，需要自定义高效内核。
*   **推理初始延迟**：首次读取长上下文并写入内存的时间较长，不适合单次查询的场景。
*   **任务偏差风险**：虽然重构损失是通用的，但对于某些不需要精确重构（如摘要、情感分析）的任务，该目标可能不是最优的。
*   **模型规模限制**：实验主要集中在较小规模的模型（如 124M, 160M），在大规模模型上的表现和训练稳定性尚待验证。

（完）
