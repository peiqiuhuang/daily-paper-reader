---
title: "DynaContext: Self-Improving Dynamic Contextualization of Optimized Prompts for Heterogeneous Parameter Extraction"
title_zh: DynaContext：针对异构参数提取的优化提示词自我改进动态上下文构建
authors: "Joe Yu, Shibin Thomas Stanley Paul, Sven Mayer"
date: 2026-08-22
pdf: "https://arxiv.org/pdf/2608.22014v1"
tags: ["query:bopl"]
score: 9.0
evidence: 优化提示词的自我改进动态上下文框架
tldr: 针对传统静态提示词优化在处理异构任务（如电子元器件参数提取）时缺乏灵活性且难以适应不同上下文约束的问题，本文提出了DynaContext框架。该框架将离线优化的核心指令与推理时的动态上下文适配相结合，通过证据路由、项特定提示词组合及验证门控的自我改进机制，实现了提示词的持续优化。实验表明，DynaContext在复杂参数提取任务中显著优于静态提示词方案，大幅提升了提取的准确率和F1值。
selection_source: fresh_fetch
motivation: 传统的静态提示词优化方法无法适应电子元器件参数提取中不同类别所需的差异化字段、约束和上下文证据。
method: 提出DynaContext框架，结合离线优化的核心指令、推理时的动态上下文适配以及基于验证门控的自我改进机制。
result: "在异构参数提取任务中，全配置下的F1分数从51.8%提升至71.0%，比现有的静态提示词流水线平均高出17.3个百分点。"
conclusion: 通过将动态上下文适配与离线提示词优化相结合，可以显著增强大语言模型在处理复杂、异构信息提取任务时的性能。
---

## 摘要
自动化的提示词和技能优化通常会生成单一的静态指令，这些指令在下一个优化周期之前会被重复用于各个推理实例。然而，当所需的上下文、约束和证据因实例而异时，这种方法无法进行自适应。例如，从电子元件描述中提取参数就打破了这一假设：电阻器、电容器、晶体管和连接器需要不同的字段、单位约束和示例，且每个输入都提供了不同的证据状态。我们提出了 DynaContext，这是一个将通过 GEPA 或 SkillOpt 学习的离线优化提取核心与推理时的上下文自适应以及验证门控的自我改进相结合的框架。DynaContext 将每个项目路由到内部、外部或备用证据路径，并根据核心、模式（schema）、证据、未解决字段和经过验证的示例构建特定于项目的提示词。确定性验证和大语言模型（LLM）评判器对每个输出进行门控，不确定的情况将提交人工审核，且只有经过人工验证的修正才会进入示例记忆库。在单类别基准测试中，平均准确率从基础提示词的 86.6% 提高到独立 SkillOpt 的 96.9%，以及最佳 DynaContext 配置下的 98.6%。在 850 个异构黄金参数事实中，平均字段级 F1 分数从无优化、无示例对照组的 51.8% 提高到仅使用动态示例的 59.2%，仅使用优化核心的 66.9%，以及两者结合的 71.0%。在保持模型不变的情况下，完整配置比已部署的静态提示流水线平均高出 17.3 个 F1 点。

## Abstract
Automated prompt and skill optimization typically produces a single static instruction that is reused across inference instances until the next optimization cycle. However, this approach cannot adapt when the required context, constraints, and evidence vary from one instance to another. For instance, parameter extraction from electronic component descriptions breaks this assumption: resistors, capacitors, transistors, and connectors require different fields, unit constraints, and demonstrations, and each input provides a different evidence state. We introduce DynaContext, a framework that combines an offline-optimized extraction core, learned with GEPA or SkillOpt, with inference-time contextual adaptation and validation-gated self-improvement. DynaContext routes each item through internal, external, or fallback evidence paths and composes an item-specific prompt from the core, schema, evidence, unresolved fields, and validated demonstrations. Deterministic validation and an LLM judge gate every output, uncertain cases go to human review, and only human-verified corrections enter the demonstration memory. On a single-category benchmark, average accuracy increases from 86.6% for the base prompt to 96.9% for standalone SkillOpt and 98.6% for the best DynaContext configuration. Across 850 heterogeneous gold parameter facts, average field-level F1 increases from 51.8% for an unoptimized, demonstration-free control to 59.2% with dynamic demonstrations alone, 66.9% with the optimized core alone, and 71.0% with both. Holding the model fixed, the full configuration outperforms the deployed static-prompting pipeline by 17.3 F1 points on average.

---

## 论文详细总结（自动生成）

这是一份关于论文《DynaContext: Self-Improving Dynamic Contextualization of Optimized Prompts for Heterogeneous Parameter Extraction》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**非结构化参数提取（UPE）**任务，特别是在电子元器件（如电阻、电容、晶体管等）领域。
*   **核心矛盾**：现有的提示词优化方法（如 GEPA、SkillOpt）通常生成一个**静态**的指令，在所有推理实例中重复使用。但在处理异构数据时，这种方法会失效。因为不同类别的元器件需要不同的字段模式（Schema）、单位约束和参考示例，且每个输入的证据状态（内部记录、外部手册或仅有描述文本）各不相同。
*   **研究目标**：开发一个能够根据每个输入实例动态调整上下文、并具备自我改进能力的框架，以解决复杂、异构的结构化预测问题。

### 2. 方法论：核心思想与关键技术
DynaContext 框架的核心思想是将**离线优化的提取核心**与**推理时的动态上下文适配**相结合。

*   **核心架构**：
    1.  **离线优化核心**：使用 GEPA 或 SkillOpt 预先学习一个通用的提取指令核心。
    2.  **确定性路由（Routing）**：根据输入信息（如是否有制造商零件号 MPN）将任务分流至三条路径：
        *   *内部路径（Internal）*：匹配到受信任的内部数据库，直接返回。
        *   *外部路径（External）*：通过 API 或 PDF 手册获取验证信息，LLM 仅补充缺失字段。
        *   *备用路径（Fallback）*：仅基于描述文本，通过分类器确定 Schema，并检索动态示例。
    3.  **动态提示词组合**：在推理时，根据路由结果、解析出的 Schema、收集的证据、未解决字段和检索到的验证示例，实时合成特定于该项的提示词。
    4.  **验证门控与自我改进**：
        *   **确定性验证**：检查单位、格式和 Schema 合规性。
        *   **LLM 评判器**：由 GEPA 优化的 LLM 担任评判，决定是否接受输出。
        *   **人工审核（HITL）**：不确定或验证失败的案例进入人工审核。
        *   **记忆更新**：只有经过人工验证的修正才会存入“示例记忆库”，用于后续的动态检索。

### 3. 实验设计
论文设计了两个主要基准测试，并使用了 8 种不同的 LLM 部署（包括 GPT-4o、GPT-5.4/5.5 系列、DeepSeek-V4-Pro 等）：
*   **单类别基准（Ceramic-Capacitor）**：包含 142 条陶瓷电容记录，用于测试在固定 Schema 下，动态上下文对准确率上限的提升。
*   **异构多类别基准（Multi-component）**：包含 133 条记录，涵盖 13 个三级类别（共 850 个黄金参数事实），用于测试处理异构 Schema 的能力。
*   **对比方法**：
    *   **Base Prompt**：未优化的基础提示词。
    *   **Static-10**：生产环境中现有的静态流水线（固定 10 个示例）。
    *   **独立优化核心**：仅使用 GEPA 或 SkillOpt 优化后的静态提示词。
    *   **消融实验**：分别测试“仅动态示例”、“仅优化核心”以及“两者结合”的效果。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **推理成本分析**：文中详细记录了不同配置下的**推理成本（美元）**和**运行时间（秒）**。结果显示，DynaContext 虽然增加了 Token 消耗（成本是静态提示词的 1.05 到 1.93 倍），但通过提高准确率，使得小型模型（如 GPT-5.4 mini）能够以极低的成本达到甚至超过大型模型（如 GPT-5.5）的性能。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个模型部署上进行了全量测试，涵盖了从基础模型到最强模型的跨度。
*   **消融实验**：进行了双因子消融实验（优化核心 × 动态示例），并使用了 Wilcoxon 符号秩检验（p=0.0039）来验证显著性。
*   **客观性**：实验采用了“配对设计”，即在相同的模型集合上比较不同方法，确保了方法效应的公平性。虽然数据集规模（数百条记录）在深度学习领域不算大，但对于高度专业化的工业参数提取任务，其覆盖的 850 个黄金事实具有较强的代表性。

### 6. 主要结论与发现
*   **性能飞跃**：在异构任务中，全配置 DynaContext 的 F1 分数达到 71.0%，比未优化的对照组（51.8%）提升巨大，比生产环境的静态流水线平均高出 **17.3 个 F1 点**。
*   **协同效应**：动态示例（+7.47 F1）和优化核心（+15.14 F1）具有正向叠加效应，尽管存在部分信息重叠。
*   **成本效益**：通过动态上下文，**小型模型可以匹配大型模型的表现**。例如，GPT-5.4 mini 在 DynaContext 框架下的表现优于成本高出 15 倍的 GPT-5.5 原始表现。
*   **准确率上限**：在固定 Schema 的单类别测试中，准确率最高可达 99.5%。

### 7. 优点（亮点）
*   **动态性与自适应**：打破了“一次优化，到处运行”的静态限制，实现了推理时的按需定制。
*   **工业级可靠性**：引入了确定性验证、LLM 评判和人工审核的三重门控，确保进入记忆库的数据是高质量的。
*   **证据溯源**：强制要求每个提取的字段必须带有来源证明（Provenance），符合工程决策的严谨需求。
*   **自我进化**：通过人工反馈闭环，系统能够随着使用时间的增加而不断积累更精准的示例。

### 8. 不足与局限
*   **数据集规模与偏差**：基准测试规模较小（约 275 条记录），且类别分布不均（电容和电阻占 74%），可能存在长尾类别覆盖不足的问题。
*   **数据私密性**：由于工业保密协议，底层的元器件原始数据无法公开，限制了结果的可复现性。
*   **外部依赖**：系统高度依赖外部 API 和 PDF 解析器的质量，若这些上游工具失效，系统性能会受限。
*   **Schema 识别瓶颈**：异构任务与单类别任务之间仍存在性能差距，说明“类别/Schema 识别”阶段仍有提升空间。

（完）
