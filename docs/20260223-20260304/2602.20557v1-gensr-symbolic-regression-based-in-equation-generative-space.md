---
title: "GENSR: Symbolic Regression Based in Equation Generative Space"
title_zh: GENSR：基于方程生成空间的符号回归
authors: "Qian Li, Yuxiao Hu, Juncheng Liu, Yuntian Chen"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20557v1"
tags: ["query:sr"]
score: 10.0
evidence: 基于生成潜空间的符号回归框架
tldr: 符号回归（SR）常在离散空间搜索，导致方程结构变化与数值表现不一致。本文提出GenSR框架，通过预训练条件变分自编码器（CVAE）将方程重参数化为具有符号连续性和局部数值平滑性的生成潜空间。该方法遵循“地图构建、粗略定位、精细搜索”范式，利用CMA-ES在潜空间进行优化。GenSR在预测精度、表达式简洁度和计算效率上均表现优异，且对噪声具有鲁棒性。
selection_source: fresh_fetch
motivation: 传统符号回归在离散空间搜索，导致方程结构修改与数值行为脱节，使得拟合误差反馈难以有效指导搜索。
method: 利用CVAE构建具有数值平滑性的方程生成潜空间，并结合CMA-ES算法在潜空间内进行从粗略定位到精细搜索的优化。
result: 实验证明GenSR在保持公式简洁的同时，显著提升了预测精度和计算效率，并在噪声环境下表现出极强的鲁棒性。
conclusion: GenSR从贝叶斯视角将符号回归转化为潜空间优化问题，为解决离散方程空间的搜索难题提供了理论保证和高效方案。
---

## 摘要
符号回归 (SR) 旨在揭示观测数据背后隐藏的方程。然而，大多数方法在离散方程空间中进行搜索，其中方程的结构修改很少与其数值行为一致，导致拟合误差反馈噪声过大，难以引导探索。为了应对这一挑战，我们提出了 GenSR，这是一个基于生成潜空间的符号回归框架，遵循“地图构建 -> 粗略定位 -> 精细搜索”的范式。具体而言，GenSR 首先预训练一个双分支条件变分自编码器 (CVAE)，将符号方程重新参数化为具有符号连续性和局部数值平滑性的生成潜空间。该空间可被视为方程空间中结构良好的“地图”，为搜索提供方向信号。在推理阶段，CVAE 将输入数据粗略定位到潜空间中有潜力的区域。随后，改进的 CMA-ES 利用平滑的潜梯度对候选区域进行精细化。从贝叶斯角度来看，GenSR 将符号回归任务重新定义为最大化条件分布 $p(\mathrm{Equ.} \mid \mathrm{Num.})$，而 CVAE 的训练通过证据下界 (ELBO) 实现了这一目标。这一新视角为 GenSR 的有效性提供了理论保证。大量实验表明，GenSR 在保持对噪声鲁棒性的同时，协同优化了预测准确性、表达式简洁性和计算效率。

## Abstract
Symbolic Regression (SR) tries to reveal the hidden equations behind observed data. However, most methods search within a discrete equation space, where the structural modifications of equations rarely align with their numerical behavior, leaving fitting error feedback too noisy to guide exploration. To address this challenge, we propose GenSR, a generative latent space-based SR framework following the `map construction -> coarse localization -> fine search'' paradigm. Specifically, GenSR first pretrains a dual-branch Conditional Variational Autoencoder (CVAE) to reparameterize symbolic equations into a generative latent space with symbolic continuity and local numerical smoothness. This space can be regarded as a well-structured `map'' of the equation space, providing directional signals for search. At inference, the CVAE coarsely localizes the input data to promising regions in the latent space. Then, a modified CMA-ES refines the candidate region, leveraging smooth latent gradients. From a Bayesian perspective, GenSR reframes the SR task as maximizing the conditional distribution $p(\mathrm{Equ.} \mid \mathrm{Num.})$, with CVAE training achieving this objective through the Evidence Lower Bound (ELBO). This new perspective provides a theoretical guarantee for the effectiveness of GenSR. Extensive experiments show that GenSR jointly optimizes predictive accuracy, expression simplicity, and computational efficiency, while remaining robust under noise.

---

## 论文详细总结（自动生成）

这是一份关于论文《GENSR: Symbolic Regression Based in Equation Generative Space》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的符号回归（SR）方法大多在**离散的方程空间**中进行搜索（如遗传算法、树搜索）。这种方式存在一个核心矛盾：方程结构的微小改变（编辑距离）往往导致数值行为的剧烈波动，使得拟合误差的反馈信号极其不稳定，搜索过程类似于“盲目尝试”。
*   **核心问题**：如何将离散、不连续的方程空间转化为一个**连续、平滑且可搜索的潜空间**，从而利用数值反馈信号引导搜索算法高效收敛。

### 2. 方法论：GenSR 框架
GenSR 提出了“**地图构建 → 粗略定位 → 精细搜索**”的新范式：
*   **核心思想**：利用双分支条件变分自编码器（CVAE）构建一个兼具“符号连续性”和“局部数值平滑性”的生成潜空间。
*   **关键技术细节**：
    *   **双分支 CVAE**：
        *   **后验分支（Posterior Branch）**：输入方程和数值样本，学习方程结构的分布，确保潜空间能准确解码回有效的数学表达式。
        *   **先验分支（Prior Branch）**：仅输入数值样本，学习数值特征到潜空间的映射。
    *   **损失函数**：通过重构损失（保证符号连续）和 KL 散度（将数值特征对齐到符号空间，保证数值平滑）共同优化。
    *   **贝叶斯视角**：论文证明了训练过程等价于最大化证据下界（ELBO），将 SR 任务转化为对 $p(\text{Equation} \mid \text{Data})$ 的概率推理。
*   **算法流程**：
    1.  **预训练**：在 500 万个合成方程数据集上训练 CVAE。
    2.  **推理定位**：输入观测数据，通过先验分支在潜空间中给出一个初始的高概率区域（粗略定位）。
    3.  **精细搜索**：使用改进的 **CMA-ES（协方差矩阵自适应进化策略）** 在潜空间内进行迭代。为提升效率，仅更新方差最大的前 $k$ 个维度（Top-k 策略），并采用对角协方差假设。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **SRBench**：包含 119 个 Feynman 物理方程、14 个 Strogatz 非线性动力学挑战。
    *   **Black-box 任务**：57 个来自 PMLB 的真实世界黑盒回归任务。
*   **对比方法**：共对比了 **18 种** 基准算法，包括：
    *   传统启发式：GPlearn, Operon, PySR (AFP)。
    *   深度学习/强化学习：DSR, E2ESR, SNIP, TPSR, RAG-SR。
    *   物理启发式：AI Feynman 2.0。
*   **评估指标**：拟合精度 ($R^2$)、方程复杂度（Token 数量）、计算耗时（Time）。

### 4. 资源与算力
*   **硬件设施**：使用了 **2 张 NVIDIA H800 (80GB)** GPU。
*   **训练时长**：预训练阶段共 200 个 epoch，每个 epoch 耗时约 1 小时，总计约 **200 小时**。
*   **模型规模**：模型包含约 **1.62 亿**（162.1M）个可训练参数。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常详尽，涵盖了标准 Benchmark 测试、不同噪声水平（0 到 0.1）下的鲁棒性测试。
*   **消融实验**：对潜空间维度（64 到 768）、CMA-ES 的种群大小、步长、Top-k 维度等关键超参数进行了深入分析。
*   **额外验证**：通过 t-SNE 可视化展示了潜空间的聚类特性，并额外设计了“方程族分类任务”来验证潜空间的表征能力。
*   **客观性**：使用了第三方维护的 SRBench 框架，确保了对比的公平性。

### 6. 主要结论与发现
*   **帕累托最优**：GenSR 在精度、复杂度和效率三个维度上均处于帕累托前沿（Pareto Front），能够以更短的时间找到更简洁、更准确的方程。
*   **鲁棒性**：在存在噪声的情况下，GenSR 的性能下降比传统离散搜索方法更缓慢，显示出生成空间对扰动的容忍度。
*   **空间特性**：实验证实了 GenSR 构建的潜空间确实实现了结构相似性和数值相似性的统一，解决了“编辑距离与数值行为脱节”的问题。

### 7. 优点与亮点
*   **范式创新**：将符号回归从“离散组合优化”转变为“连续空间概率推理”，理论框架（贝叶斯 ELBO）扎实。
*   **搜索效率**：通过 CMA-ES 在连续空间利用梯度/方向信号，避免了遗传算法中大量的随机变异和回溯。
*   **通用性**：预训练后的模型不仅能做 SR，其编码器还能用于方程分类等下游任务。

### 8. 不足与局限
*   **预训练依赖**：模型的性能高度依赖于预训练数据集的覆盖范围。如果目标方程包含预训练中未见过的特殊算子或极端结构，模型可能难以发现。
*   **计算开销**：虽然推理比某些复杂 GP 快，但 CMA-ES 在潜空间中的迭代仍需多次调用 Transformer 解码器，对于实时性要求极高的场景可能仍有压力。
*   **高维优化**：尽管采用了 Top-k 策略，但在极高维度的潜空间中进行进化策略搜索仍面临“维度灾难”的潜在风险。

（完）
