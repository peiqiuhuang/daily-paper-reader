---
title: Failure-Guided Co-Evolution of Prompts and Training Data
title_zh: 基于失败引导的提示词与训练数据协同演化
authors: "Tianyu Yuan, Zhuzhong Qian"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15209v1"
tags: ["query:bopl"]
score: 9.0
evidence: 自动提示词优化与协同演化
tldr: 针对自动提示词优化（APO）中训练数据固定导致反馈局限的问题，本文提出了FORGE框架。该方法将模型执行失败视为双重信号，通过将失败抽象为模式并合成新训练数据，实现提示词与训练数据的协同进化。实验表明，FORGE在八项基准测试中显著提升了性能，且合成数据具有良好的跨模型迁移能力，证明了联合优化指令与学习数据的有效性。
selection_source: fresh_fetch
motivation: 现有的自动提示词优化方法因训练数据固定，难以发现并覆盖未被训练集包含的潜在失败场景。
method: FORGE框架通过将失败案例抽象为模式，并利用四种变异策略合成新训练数据，实现提示词优化与数据生成的协同进化。
result: FORGE在八项基准测试中较基线提升了16.52个百分点，且其合成数据能显著提升其他优化算法的性能。
conclusion: 将失败作为提示词优化与数据合成的共享接口，能够有效提升模型在复杂任务中的表现与泛化能力。
---

## 摘要
自动提示词优化（APO）通过任务反馈修订提示词来改进语言模型程序，但通常保持训练数据不变。针对相同实例反复优化会将反馈局限于这些数据中已有的弱点，导致相关的失败条件无法被探索。因此，我们将每一次失败视为一种双重信号：它既指出了提示词应如何修订，也指出了应合成哪些新的训练证据。我们引入了 FORGE，这是一个实现提示词与训练数据协同演化的失败引导框架。FORGE 将不完美的执行抽象为可复用的失败模式，并通过四种互补的变异策略合成新的训练数据。经过验证的实例被反馈回提示词搜索过程，使更新后的提示词能够揭示后续的数据需求。在八个异构基准测试中，FORGE 将总分较未优化基线提高了 16.52 个百分点，并优于所有评估的 APO 基线。合成的数据也具有跨框架的迁移性：在一项迁移研究中，在匹配优化预算的情况下，这些数据使所有九个 APO 对比项提高了 2-9 分，使所有三个 GRPO 对比项提高了 4-8 分。这些结果确立了失败作为提示词优化与数据合成之间共享接口的作用，并展示了联合调整模型指令与模型学习内容所带来的益处。

## Abstract
Automatic prompt optimization (APO) improves language-model programs by revising prompts from task feedback, yet it typically holds its training data fixed. Repeatedly optimizing against the same instances confines feedback to weaknesses already represented in those data, leaving related failure conditions unexplored. We therefore view each failure as a dual signal: it indicates both how the prompt should be revised and what new training evidence should be synthesized. We introduce FORGE, a failure-guided framework that co-evolves prompts and training data. FORGE abstracts imperfect executions into reusable failure modes and synthesizes new training data through four complementary mutation strategies. Verified instances are fed back into prompt search, allowing updated prompts to expose the next data needs. Across eight heterogeneous benchmarks, FORGE improves the aggregate score over the unoptimized baseline by 16.52 percentage points and outperforms all evaluated APO baselines. The synthesized data also transfer beyond FORGE: in a transfer study, they improve all nine APO comparisons by 2--9 points and all three GRPO comparisons by 4--8 points under matched optimization budgets. These results establish failures as a shared interface between prompt optimization and data synthesis, and show the benefit of jointly adapting what a model is instructed to do and what it learns from.

---

## 论文详细总结（自动生成）

以下是对论文《Failure-Guided Co-Evolution of Prompts and Training Data》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的自动提示词优化（APO）方法通常在“固定训练数据”上进行迭代。这种方式存在局限性：模型反复针对同一批数据进行优化，容易陷入局部最优，且无法探索训练集中未覆盖的潜在失败场景。
*   **核心含义**：论文提出将“失败”视为一种双重信号，不仅用于指导提示词的修订，还用于指导训练数据的合成。通过这种“提示词-数据”协同演化机制，模型能够动态发现并填补知识盲区，从而实现更稳健的性能提升。

### 2. 方法论：FORGE 框架
*   **核心思想**：构建一个闭环系统，将执行失败抽象为可复用的“失败模式（Failure Modes）”，并基于此进行针对性的数据合成。
*   **关键技术细节**：
    *   **失败记忆（Failure Memory）**：将不完美的执行记录抽象为去语境化的机制描述，避免模型仅进行简单的同义词替换。
    *   **四种变异策略**：
        *   **正向（Positive）**：在良性变异下保持目标技能。
        *   **负向（Negative）**：创建对比案例，明确提示词不应适用的边界。
        *   **边界（Boundary）**：探测决策条件的临界点。
        *   **压力（Stress）**：增加干扰项或约束，测试鲁棒性。
    *   **代理验证（Agentic Verification）**：引入具备工具使用能力的验证器，确保合成数据在逻辑和任务要求上的有效性。
*   **算法流程**：在优化循环中，当验证集性能停滞时，触发数据合成；合成的新数据被加入训练集，驱动下一轮提示词搜索，形成“提示词更新 -> 暴露新失败 -> 合成新数据 -> 提示词进一步优化”的闭环。

### 3. 实验设计
*   **数据集与场景**：涵盖八个异构基准测试，包括多跳检索（HotPotQA, HoVer）、指令遵循（IFBench）、领域知识（FiNER-139, LawBench）、数学推理（AIME-2025, MMLU-Pro）及交互式智能体（AppWorld）。
*   **对比方法**：对比了未优化基线（Baseline）以及三种主流 APO 方法：MIPROv2、ACE 和 GEPA。
*   **评估指标**：各任务的准确率/得分，以及跨优化器和 GRPO 权重优化任务的迁移性能。

### 4. 资源与算力
*   **算力配置**：在 GRPO 迁移实验中，使用了 8 张 48GB NVIDIA RTX A6000 GPU。
*   **说明**：论文明确了实验环境（Ubuntu 20.04, Python 3.12.4, PyTorch 2.10.0），但未详细披露整个优化过程的总训练时长，仅说明了优化预算的对齐方式。

### 5. 实验数量与充分性
*   **实验覆盖**：进行了主实验（8 个基准）、组件消融实验（验证失败记忆与变异策略的必要性）、跨模型迁移实验（Qwen3.5-35B 与 Qwen3-8B）以及数据迁移实验（对比原始数据与增强数据）。
*   **客观性与公平性**：实验设计严谨，通过对齐任务模型、评估器和搜索预算，确保了对比的公平性。消融实验清晰地展示了各模块的增量贡献。

### 6. 主要结论与发现
*   **性能提升**：FORGE 在八项基准测试中平均较基线提升 16.52 个百分点，优于所有对比的 APO 方法。
*   **数据迁移性**：FORGE 合成的训练数据不仅对自身有效，还能显著提升其他提示词优化器（MIPROv2, ACE, GEPA）及 GRPO 权重优化算法的性能。
*   **协同效应**：证明了将训练数据视为“可变优化状态”是提升复杂 LM 程序性能的关键。

### 7. 优点
*   **创新性**：将“失败”从单纯的负面反馈转化为“数据合成的源泉”，视角独特。
*   **通用性**：四种变异策略（正、负、边界、压力）具有很强的逻辑完备性，适用于多种任务类型。
*   **可解释性**：通过失败模式的抽象，能够清晰地看到模型在哪些逻辑边界上存在缺陷。

### 8. 不足与局限
*   **计算开销**：由于引入了多轮次的合成与验证，相比于固定数据的 APO，FORGE 的优化过程可能需要更多的推理调用。
*   **依赖性**：高度依赖于“验证器”的质量，如果验证器本身无法准确判断合成数据的有效性，可能会引入噪声数据。
*   **应用限制**：对于那些无法通过程序化验证（如主观性极强的创意写作）的任务，该框架的适用性有待进一步验证。

（完）
