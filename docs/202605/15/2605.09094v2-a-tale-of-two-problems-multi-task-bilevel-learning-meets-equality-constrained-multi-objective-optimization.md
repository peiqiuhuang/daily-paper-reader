---
title: "A Tale of Two Problems: Multi-Task Bilevel Learning Meets Equality Constrained Multi-Objective Optimization"
title_zh: 两个问题的故事：多任务双层学习遇上等式约束多目标优化
authors: "Zhiyao Zhang, Myeung Suk Oh, Zhen Qin, Jiaxiang Li, Xin Zhang, Jia Liu"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.09094v2"
tags: ["query:bopl"]
score: 9.0
evidence: 将双层优化扩展到具有松弛凸性假设的多任务设置中
tldr: 本研究针对传统双层优化局限于单任务且依赖下层强凸性假设的问题，首次探讨了在下层一般凸性下的多任务双层学习（MTBL）。通过将其转化为等式约束多目标优化（ECMO）问题，并提出加权切比雪夫惩罚算法，实现了确定性和随机设置下的有限时间收敛。该方法不仅填补了ECMO研究的空白，还为解决复杂多任务学习问题提供了高效的理论框架和收敛保证。
selection_source: fresh_fetch
motivation: 现有的双层优化方法难以处理多任务场景及下层非强凸的情况，限制了其在复杂机器学习任务中的应用。
method: 将多任务双层学习问题重构为等式约束多目标优化问题，并提出一种基于KKT条件的加权切比雪夫惩罚算法进行求解。
result: "该算法在确定性和随机环境下均达到了 $O(ST^{-1/2})$ 的收敛率，并能通过调整偏好向量系统地探索帕累托前沿。"
conclusion: 研究成功建立了多任务双层学习与等式约束多目标优化之间的联系，为解决此类复杂优化问题提供了新的理论视角和有效的算法工具。
---

## 摘要
近年来，双层优化 (BLO) 因其在机器学习中的广泛应用而引起了极大关注。然而，大多数现有的双层优化研究仍局限于单任务设置，并依赖于下层强凸性假设，这极大地限制了它们在日益复杂的现代机器学习问题中的适用性。在本文中，我们首次尝试在松弛的下层一般凸性 (LLGC) 假设下，将双层优化扩展到多任务设置。为此，我们将具有 LLGC 的多任务双层学习 (MTBL) 问题重新表述为一个等式约束多目标优化 (ECMO) 问题。然而，ECMO 本身是一个尚未在文献中研究过的新问题。为了填补这一空白，我们首先建立了一种新的基于 Karush-Kuhn-Tucker (KKT) 的 Pareto 驻定性，作为 ECMO 算法设计的收敛准则。在此基础上，我们提出了一种加权切比雪夫 (WC) 惩罚算法，该算法在确定性和随机设置下均能达到 $O(ST^{-\frac{1}{2}})$ 的有限时间收敛率，收敛至基于 KKT 的 Pareto 驻定性，其中 $S$ 表示目标数量，$T$ 是总迭代次数。此外，通过在 $S$ 维单纯形上改变偏好向量，我们的 WC 惩罚方法能够系统地探索 Pareto 前沿。最后，ECMO 问题的解可以直接转化为原始 MTBL 问题的解，从而闭合了这两个基础优化框架之间的环路。

## Abstract
In recent years, bilevel optimization (BLO) has attracted significant attention for its broad applications in machine learning. However, most existing works on BLO remain confined to the single-task setting and rely on the lower-level strong convexity assumption, which significantly restricts their applicability to modern machine learning problems of growing complexity. In this paper, we make the first attempt to extend BLO to the multi-task setting under a relaxed lower-level general convexity (LLGC) assumption. To this end, we reformulate the multi-task bilevel learning (MTBL) problem with LLGC into an equality constrained multi-objective optimization (ECMO) problem. However, ECMO itself is a new problem that has not yet been studied in the literature. To address this gap, we first establish a new Karush-Kuhn-Tucker (KKT)-based Pareto stationarity as the convergence criterion for ECMO algorithm design. Based on this foundation, we propose a weighted Chebyshev (WC)-penalty algorithm that achieves a finite-time convergence rate of $O(ST^{-\frac{1}{2})$ to KKT-based Pareto stationarity in both deterministic and stochastic settings, where $S$ denotes the number of objectives, and $T$ is the total iterations. Moreover, by varying the preference vector over the $S$-dimensional simplex, our WC-penalty method systematically explores the Pareto front. Finally, solutions to the ECMO problem translate directly into solutions for the original MTBL problem, thereby closing the loop between these two foundational optimization frameworks.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Tale of Two Problems: Multi-Task Bilevel Learning Meets Equality Constrained Multi-Objective Optimization》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心挑战**：传统的双层优化（BLO）通常局限于单任务设置，且严重依赖“下层强凸性（LLSC）”假设。然而，现代机器学习任务（如大语言模型对齐、强化学习）往往涉及多个冲突目标，且下层问题通常仅满足“一般凸性（LLGC）”，这导致超梯度（Hyper-gradient）定义失效。
*   **研究动机**：填补多任务双层学习（MTBL）在一般凸性假设下的理论空白。
*   **整体含义**：论文通过将 MTBL 问题转化为一种全新的优化形式——等式约束多目标优化（ECMO），并为此建立了理论基础和高效算法，实现了从理论证明到实际复杂任务应用的闭环。

### 2. 论文提出的方法论
*   **核心思想**：将双层结构转化为单层约束结构。利用下层问题的一阶驻定条件（$\nabla_y g(x, y) = 0$）作为等式约束，将 MTBL 重新表述为 ECMO 问题。
*   **关键技术细节**：
    1.  **Pareto 驻定性重新定义**：由于传统无约束多目标优化的驻定性定义在有约束情况下失效，论文基于 KKT 条件为 ECMO 建立了新的 Pareto 驻定性准则。
    2.  **加权切比雪夫（WC）标量化**：利用 WC 标量化技术建立 ECMO 与单目标优化之间的映射，通过改变偏好向量（Preference Vector）来探索整个 Pareto 前沿。
    3.  **WC-Penalty 算法**：为了处理非光滑的 $L_\infty$ 范数和等式约束，算法引入松弛变量和惩罚项，将问题转化为可微的单层优化问题。
*   **算法流程**：采用投影梯度下降（Projected GD）更新变量。在每一步迭代中，同时更新上层变量、下层变量、松弛变量和惩罚项，无需复杂的隐式梯度计算。

### 3. 实验设计
*   **实验场景与数据集**：
    1.  **RLHF 奖励模型训练**：使用 HelpSteer 数据集，涉及 5 个冲突指标（帮助性、正确性、连贯性、复杂性、冗余性）。
    2.  **多目标 LLM 对齐**：基于 Llama-3.2（1B/3B）和 Llama-3.1（8B）模型进行数据加权微调。
    3.  **多任务元学习**：基于 MNIST 变体数据集。
*   **Benchmark 与对比方法**：
    *   **MTBL 基准**：MOML、MoCo、FORUM。
    *   **BLO 基准（结合线性标量化）**：ITD、SOBA。
*   **评估指标**：Pareto 前沿覆盖范围、收敛速度、超体积（Hypervolume）、$\epsilon$-指标。

### 4. 资源与算力
*   **硬件环境**：使用了由 **4 台 NVIDIA H100 GPU（每台 94GB 显存）** 组成的集群。
*   **软件框架**：基于 PyTorch 的 DistributedDataParallel（DDP）进行分布式训练。
*   **训练细节**：对于 1B 参数模型，迭代次数通常为 3,000 次；对于 8B 模型，展示了良好的可扩展性。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从合成数据实验到 80 亿参数规模的真实 LLM 实验。
*   **充分性评价**：实验设计非常充分。不仅对比了最先进的 MTBL 算法，还通过消融实验展示了不同偏好向量对 Pareto 前沿探索的影响。
*   **客观性**：每组实验均重复 5 次并标注标准差，使用了超体积等公认的多目标评估指标，对比过程考虑了计算时间与迭代次数的双重维度，表现出高度的客观性和公平性。

### 6. 论文的主要结论与发现
*   **理论突破**：首次证明了 WC-Penalty 算法在确定性和随机设置下均能达到 **$O(ST^{-1/2})$** 的有限时间收敛率（$S$ 为目标数，$T$ 为迭代数）。
*   **性能优越**：在 LLM 对齐任务中，该方法生成的超体积显著大于基准方法，且在处理多个冲突目标时表现出更强的鲁棒性。
*   **效率提升**：相比于需要计算 Hessian 逆矩阵的隐式梯度方法，WC-Penalty 算法作为单层投影梯度法，计算开销更低，运行速度提升了数倍至数十倍。

### 7. 优点
*   **理论完备性**：为 ECMO 这一新问题建立了从驻定性定义到收敛性分析的完整框架。
*   **假设松弛**：摆脱了下层强凸性的束缚，使其能应用于更广泛的非凸深度学习场景。
*   **帕累托探索**：通过加权切比雪夫法，能够比传统的线性标量化更系统、更均匀地探索非凸 Pareto 前沿。
*   **易于实现**：算法结构简单（单层更新），不需要复杂的二阶导数计算。

### 8. 不足与局限
*   **惩罚参数敏感性**：惩罚项系数 $u, v$ 的选择对收敛效果有一定影响，虽然论文给出了理论指导，但在实际应用中可能需要调参。
*   **等式约束限制**：目前主要针对等式约束（由下层优化的一阶条件导出），对于更复杂的带有不等式约束的多目标问题，理论扩展尚待进一步研究。
*   **内存开销**：虽然比 FORUM 等算法省内存，但在处理超大规模模型时，同时维护多组变量和梯度仍对显存有一定要求。

（完）
