---
title: "CALICO: A Human-Centered, Codebook-Aligned System for Annotation"
title_zh: CALICO：一种以人为中心、与编码簿对齐的标注系统
authors: "Boqin Yuan, Xiaoyi Gu, Fiona Li, Chang Wan, Angel Hsing-Chi Hwang, Jieyu Zhao"
date: 2026-09-13
pdf: "https://arxiv.org/pdf/2609.14726v1"
tags: ["query:bopl"]
score: 8.0
evidence: 标签监督的提示词优化
tldr: CALICO是一个以人为中心的标注系统，旨在解决大模型在科研标注中提示词不透明、难以修正的问题。它将提示词视为可编辑、可版本化的工件，集成了代码本解析、提示词生成、结果检查及多种优化器（含ReflectAgent）。该系统支持人类反馈与标签监督优化，显著提升了标注准确性，并能捕捉不同标注者的个性化解读，为科研人员提供了一个可审计、可复用的标注工作流。
selection_source: fresh_fetch
motivation: 现有大模型标注流程中提示词往往是固定的且对专家不透明，导致难以诊断和修正模型偏差。
method: CALICO通过将提示词转化为可版本化和可优化的工件，并集成多种提示词优化器与反射机制，实现人机协作的标注流程。
result: 在AI伴侣聊天记录标注任务中，CALICO使两名标注者的平均留存性能分别提升了13.0和7.4个百分点。
conclusion: CALICO通过提供透明、可审计的标注工作流，有效提升了标注质量并支持捕捉标注者的特定解读偏好。
---

## 摘要
大型语言模型正日益被用于扩展科学研究中基于编码簿（codebook）的标注工作，但现有的工作流程在将领域专家的编码簿转化为可靠、可修订且可审计的提示词（prompts）方面支持有限。提示词通常被视为固定的指令且对标注者不可见，这使得非技术背景的领域专家在模型输出违反编码簿准则时，难以诊断并纠正模型的行为。在本文中，我们提出了 CALICO，这是一种以人为中心、与编码簿对齐的标注工作流程，它将提示词视为可编辑、可版本化且可优化的工件。CALICO 集成了编码簿解析、提示词生成、结果检查、提示词版本控制、自然语言人类反馈，以及通过现有优化器（如 GEPA、MIPROv2 和 OPRO）与我们基于反思的优化器 ReflectAgent 进行的标签监督提示词优化。实证结果表明，我们在特定领域的 AI 伴侣聊天机器人对话编码簿上评估了 CALICO。在所有评估维度上，CALICO 将两位标注者的平均留出性能分别提高了 13.0 和 7.4 个绝对百分点。针对标注者特异性的分析进一步表明，优化后的提示词捕捉到了标注者特定的解读，而不仅仅是通用的编码簿澄清。CALICO 以 Web 应用程序的形式运行，引导用户从原始编码簿材料生成可检查、可导出的标签；网站、代码库和在线演示已在 https://calico-annotation.github.io/ 发布，并采用 Apache 2.0 许可证。

## Abstract
Large language models are increasingly used to scale codebook-based annotation in scientific research, but existing workflows provide limited support for translating domain experts' codebooks into reliable, revisable, and auditable prompts. Prompts are often treated as fixed instructions and hidden from annotators, making it difficult for non-technical domain experts to diagnose and correct model behavior when outputs violate codebook guidelines. In this paper, we present CALICO, a human-centered, codebook-aligned annotation workflow that treats prompts as editable, versioned, and optimizable artifacts. CALICO integrates codebook parsing, prompt generation, result inspection, prompt versioning, natural language human feedback, and label-supervised prompt optimization through existing optimizers such as GEPA, MIPROv2, and OPRO, together with our reflection-based optimizer, ReflectAgent. Empirically, we evaluate CALICO on domain-specific AI-companion chatbot conversation codebooks. Across evaluated dimensions, CALICO improves mean held-out performance by +13.0 and +7.4 absolute points for two coders, respectively. A coder-specificity analysis further suggests that optimized prompts capture coder-specific interpretations rather than only generic codebook clarification. CALICO runs as a web application that takes users from raw codebook materials to inspectable, exportable labels; the website, codebase, and live demo are released at https://calico-annotation.github.io/ under the Apache 2.0 License.

---

## 论文详细总结（自动生成）

你好！我是 Antigravity。很高兴为你分析这篇关于 **CALICO** 系统的论文。以下是基于论文内容的结构化总结：

### 1. 核心问题与研究动机
在社会科学、心理学等领域，研究人员通常依赖“编码簿”（Codebook）进行数据标注。虽然大语言模型（LLM）能提升标注效率，但现有工作流存在以下痛点：
* **提示词黑盒化**：提示词通常被视为固定指令，对领域专家不透明，导致专家难以诊断和修正模型偏差。
* **缺乏可审计性**：现有工具难以将专家的领域知识转化为可版本化、可修订的提示词。
* **标注主观性**：不同专家对同一编码簿的解读可能存在差异，通用提示词难以捕捉这种“标注者特异性”。

### 2. 方法论：CALICO 系统
CALICO 的核心思想是将提示词视为**可编辑、可版本化且可优化的工件**。其工作流包含四个模块：
* **编码簿解析代理**：支持多种格式（PDF, XLSX, JSON 等），自动提取维度、标签和规则，构建规范化架构。
* **提示词生成器**：将编码簿自动转化为可执行的提示词，并允许用户手动编辑和检查。
* **人机协作反馈**：在“冷启动”场景下，用户通过自然语言反馈系统性错误，系统将其转化为校准指南并更新提示词版本。
* **提示词优化器**：支持多种优化器（GEPA, MIPROv2, OPRO），并引入了 **ReflectAgent**。该优化器通过分析标注错误，提取决策边界规则，并将这些规则融入提示词中，实现主动式优化。

### 3. 实验设计
* **场景**：AI 伴侣聊天机器人对话分析。
* **数据集**：包含两个编码簿（Self-Disclosure 和 AI Behavior），由两名专家标注者（Coder A 和 Coder B）独立标注。
* **基准（Benchmark）**：对比了零样本（Zero-shot）提示词与经过 ReflectAgent 优化后的提示词。
* **对比方法**：在优化器性能评估中，对比了 OPRO、GEPA、MIPROv2 和 ReflectAgent。

### 4. 资源与算力
* **算力说明**：论文未明确列出具体的 GPU 数量或训练时长。
* **模型使用**：主要使用 `gpt-5.4-mini` 作为标注模型，并在附录中测试了 `gpt-5.5`、`gemini-3.1-pro` 和自托管的 `Qwen3-8B`（通过 vLLM 运行）。

### 5. 实验数量与充分性
* **实验覆盖**：进行了多维度评估，包括不同标注者的性能提升、不同优化器的对比、以及针对“标注者特异性”的交叉验证实验。
* **公平性**：所有优化器均从相同的零样本提示词出发，使用相同的确定性数据划分（Train/Val/Test），实验设计严谨且具有可比性。

### 6. 主要结论与发现
* **性能提升**：CALICO 显著提升了标注准确性，两位标注者的平均留存性能分别提升了 13.0 和 7.4 个百分点。
* **标注者特异性**：实验证明，优化后的提示词能够捕捉特定标注者的解读偏好，而非仅仅是通用的编码簿澄清（交叉验证显示，针对特定标注者优化的提示词在对方数据上表现较差）。
* **可解释性**：ReflectAgent 生成的规则库是可读的，便于专家审查和复用。

### 7. 优点
* **以人为中心**：将提示词管理权交还给领域专家，而非单纯依赖自动化。
* **高度可审计**：支持版本控制和 diff 查看，符合科学研究对透明度的要求。
* **灵活性**：兼容多种优化后端，且支持冷启动（无标注数据）和监督学习（有标注数据）两种模式。

### 8. 不足与局限
* **缺乏大规模用户研究**：目前主要基于离线数据评估，未进行实地用户可用性测试。
* **模型能力限制**：系统侧重于提示词优化，暂不支持对标注模型进行微调（Fine-tuning）。
* **应用范围**：目前主要针对离散标签标注，对于软标签分布（Soft label distributions）的标注支持有限。
* **预处理限制**：对长文本的切分和处理控制能力尚有提升空间。

（完）
