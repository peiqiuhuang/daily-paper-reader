---
title: Critical Patch-Aware Sparse Prompting with Decoupled Training for Continual Learning on the Edge
title_zh: 面向边缘侧持续学习的解耦训练关键补丁感知稀疏提示
authors: "Wonseon Lim, Jaesung Lee, Dae-Won Kim"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07399v1"
tags: ["query:bopl"]
score: 8.0
evidence: 参数高效的基于提示的持续学习
tldr: 针对边缘设备上持续学习面临的内存和计算资源限制，本文提出CPS-Prompt框架。该框架通过关键补丁采样（CPS）实现任务感知的令牌减少，并利用解耦的提示词与分类器训练（DPCT）降低反向传播开销。实验证明，在保持与SOTA相当的准确率的同时，该方法在峰值内存、训练时间和能效方面比基准提升了约1.6倍，显著增强了边缘端的训练效率。
selection_source: fresh_fetch
motivation: 现有的基于提示词的持续学习方法主要关注推理性能，忽视了边缘设备在在线训练时的内存和计算开销。
method: 提出CPS-Prompt框架，结合关键补丁采样以减少输入令牌，并采用解耦训练策略来降低反向传播的计算负担。
result: 在多个基准测试和真实硬件上，该方法在内存和能效上比CODA-Prompt提升1.6倍，且准确率保持在竞争水平。
conclusion: CPS-Prompt通过稀疏化和解耦训练，有效解决了边缘设备上持续学习的训练效率瓶颈，实现了性能与开销的平衡。
---

## 摘要
边缘设备上的持续学习（CL）不仅需要高准确率，还需要训练效率，以支持在严格的内存和计算约束下的设备端自适应。虽然基于提示的持续学习（PCL）具有参数效率并能实现极具竞争力的准确率，但以往的研究主要集中在准确率或推理性能上，往往忽略了设备端训练的内存和计算开销。在本文中，我们提出了 CPS-Prompt，这是一个关键补丁感知稀疏提示框架，通过集成用于任务感知令牌缩减的关键补丁采样（CPS）和用于减少反向传播开销的解耦提示与分类器训练（DPCT），明确针对训练时的内存占用和计算成本。在三个公共基准测试和真实边缘硬件上的实验表明，CPS-Prompt 在峰值内存、训练时间和能源效率方面比平衡的 CODA-Prompt 基准提升了约 1.6 倍，同时平均准确率与最先进的 C-Prompt 差距在 2% 以内，且在准确率上与 CODA-Prompt 保持相当的竞争力。代码可在 https://github.com/laymond1/cps-prompt 获取。

## Abstract
Continual learning (CL) on edge devices requires not only high accuracy but also training-time efficiency to support on-device adaptation under strict memory and computational constraints. While prompt-based continual learning (PCL) is parameter-efficient and achieves competitive accuracy, prior work has focused mainly on accuracy or inference-time performance, often overlooking the memory and computational costs of on-device training. In this paper, we propose CPS-Prompt, a critical patch-aware sparse prompting framework that explicitly targets training-time memory usage and computational cost by integrating critical patch sampling (CPS) for task-aware token reduction and decoupled prompt and classifier training (DPCT) to reduce backpropagation overhead. Experiments on three public benchmarks and real edge hardware show that CPS-Prompt improves peak memory, training time, and energy efficiency by about 1.6x over the balanced CODA-Prompt baseline, while maintaining accuracy within 2% of the state-of-the-art C-Prompt on average and remaining competitive with CODA-Prompt in accuracy. The code is available at https://github.com/laymond1/cps-prompt.

---

## 论文详细总结（自动生成）

以下是对论文《Critical Patch-Aware Sparse Prompting with Decoupled Training for Continual Learning on the Edge》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在边缘设备（如家用机器人、无人机、智能手机）上实现**持续学习（Continual Learning, CL）**面临严峻挑战。虽然现有的**基于提示词的持续学习（PCL）**方法通过冻结预训练骨干网络并仅更新轻量化提示词，实现了较高的参数效率和准确率，但它们往往忽略了**训练阶段**的资源消耗。
*   **核心痛点**：在边缘端进行在线训练时，反向传播产生的中间激活值会占用大量内存，且计算开销大，容易导致设备内存溢出或训练失败。
*   **研究目标**：开发一种既能保持高准确率，又能显著降低训练时内存占用、计算时间和能源消耗的 PCL 框架。

### 2. 论文提出的方法论：CPS-Prompt
论文提出了 **CPS-Prompt** 框架，由两个核心模块组成：

*   **关键补丁采样（Critical Patch Sampling, CPS）**：
    *   **核心思想**：利用 PCL 架构中现有的“冻结查询编码器（Query Encoder）”来识别图像中对当前任务最重要的补丁（Tokens）。
    *   **技术细节**：提取查询编码器最后一层的注意力权重（Attention）和值向量（Value）的范数，计算每个补丁的关键得分 $s_j$。通过温度缩放的 Softmax 构造采样分布，随机采样（无放回）保留一小部分关键补丁，丢弃冗余补丁。
    *   **作用**：在进入可训练的 Prompt 阶段前减少输入序列长度，从而直接降低训练时的激活值内存占用。

*   **解耦提示与分类器训练（Decoupled Prompt and Classifier Training, DPCT）**：
    *   **核心思想**：解决稀疏补丁训练导致的“表征失配”问题（即训练用部分补丁，推理用全量补丁）。
    *   **流程**：
        1.  **提示训练阶段**：使用 CPS 选出的稀疏补丁联合训练 Prompt 和分类器，学习任务自适应特征。
        2.  **分类器对齐阶段**：冻结 Prompt 参数，使用全量补丁仅微调分类器。
    *   **作用**：通过冻结 Prompt 减少了第二阶段的反向传播开销，同时确保分类器能适应全量输入，提升推理准确率。

### 3. 实验设计
*   **数据集**：CIFAR-100、ImageNet-R、CUB-200（均为 10 个任务的类增量设置）。
*   **Benchmark 任务**：类增量学习（Class-Incremental Learning）。
*   **对比方法**：
    *   **传统 CL 方法**：LwF, ER。
    *   **SOTA PCL 方法**：L2P, DualPrompt, CODA-Prompt, C-Prompt, OS-Prompt/OS-Prompt++。
    *   **令牌缩减方法**：ToMe（Token Merging）, PD（Patch Dropout）。

### 4. 资源与算力
*   **训练算力**：实验在 **NVIDIA RTX 4090 GPU** 上进行模型训练。
*   **边缘效率评估**：在真实的边缘硬件 **NVIDIA Jetson Orin Nano** 上测量峰值内存、训练时间和能源消耗。
*   **具体配置**：使用 ViT-Tiny/16 作为骨干网络，Batch Size 为 16，ImageNet-R 训练 50 epoch，其余数据集 20 epoch。

### 5. 实验数量与充分性
*   **实验规模**：所有实验均在 10 个不同的随机种子下运行并取平均值，确保了结果的统计显著性。
*   **消融实验**：详细分析了 CPS 和 DPCT 各自的贡献，并对比了随机采样（PD）与关键采样（CPS）的效果。
*   **参数敏感性**：对采样温度 $\tau$、训练相位比 $\lambda$ 以及补丁缩减比例 $r$ 进行了详尽的网格搜索和分析。
*   **客观性**：通过在真实边缘硬件上测试效率指标，而非仅在服务器上模拟，增强了实验的现实说服力。

### 6. 论文的主要结论与发现
*   **效率大幅提升**：相比基准方法 CODA-Prompt，CPS-Prompt 在峰值内存、训练时间和能效方面均提升了约 **1.6 倍**。
*   **准确率保持稳健**：在大幅减小开销的前提下，其准确率与 SOTA 方法 C-Prompt 的差距保持在 **2% 以内**，且优于其他令牌缩减技术（如 ToMe 和 PD）。
*   **稀疏性与鲁棒性**：即使在减少 60% 内存占用的极端情况下，CPS-Prompt 仍能保留基准模型 90% 以上的准确率。
*   **随机性的价值**：实验发现，带温度控制的随机采样比确定性的 Top-K 采样具有更好的泛化能力。

### 7. 优点
*   **关注点独特**：不同于多数研究只关注推理效率，本文切中边缘端“在线训练”的资源瓶颈。
*   **任务感知**：CPS 模块巧妙复用了 PCL 固有的查询机制，无需额外训练辅助模块即可实现高效的补丁选择。
*   **软硬件结合**：在 Jetson Orin Nano 上的实测数据证明了该方法在实际边缘部署中的可行性。

### 8. 不足与局限
*   **架构依赖**：该方法高度依赖于 Vision Transformer (ViT) 架构，对于 CNN 等其他骨干网络的适用性未做探讨。
*   **超参数敏感性**：相位比 $\lambda$ 和温度 $\tau$ 在不同数据集上需要微调以达到最优平衡，缺乏一种自适应调整机制。
*   **任务复杂度限制**：虽然在 10 任务设置下表现良好，但在更长序列或更复杂任务流下的表现（如 Prompt 库的扩展性）仍有待验证。

（完）
