---
title: Metadata-Free Meta-Reweighted Direct Preference Optimization under Noisy Preference Labels
title_zh: 噪声偏好标签下的无元数据元重加权直接偏好优化
authors: "Hua Qu, Yifan Li, Xiaodong Yuan"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.09796v1"
tags: ["query:bopl"]
score: 9.0
evidence: 用于大语言模型偏好对齐的双层优化
tldr: 针对直接偏好优化（DPO）对噪声数据敏感的问题，本文提出一种双层优化框架，通过元学习自动调整偏好数据的权重。该方法无需高质量元数据，利用任务无关的元知识驱动学习，并结合中心差分近似与LoRA技术降低计算开销。实验证明，该方法在多种噪声比例下均能显著提升大语言模型在摘要生成和对话任务中的对齐性能。
selection_source: fresh_fetch
motivation: 现实场景中的偏好数据常含有噪声，且缺乏高质量元数据，这严重削弱了DPO在模型对齐中的效果。
method: 提出一种双层优化框架，通过元学习权重函数对噪声数据进行重加权，并采用LoRA和中心差分近似实现高效训练。
result: 在TL;DR摘要和Anthropic HH对话任务上的实验表明，该方法在不同噪声率下均优于现有的DPO基准。
conclusion: 该研究为处理含噪声偏好数据的LLM对齐提供了一种无需元数据且计算高效的元重加权解决方案。
---

## 摘要
直接偏好优化 (DPO) 已成为将大语言模型 (LLM) 与人类偏好对齐的重要方法，因为它消除了显式奖励建模和强化学习优化的需求。然而，其性能严重依赖于偏好数据的质量，而现实场景中的噪声偏好数据会削弱对齐性能。为了解决这一问题，我们提出了一个双层优化框架，并在特定假设下证明了该框架可以在干净数据下恢复 DPO 的最优解。我们进一步推导了在非对称标签翻转噪声下可学习加权函数的先验形式。考虑到高质量的元数据可能难以获取，我们提出了一种与任务无关的元知识驱动方法，使得在元数据完全不可用的情况下也能进行元学习。为了降低 LLM 元学习中高阶梯度的高昂成本，我们将中心差分近似与 LoRA 微调相结合，并开发了一种可扩展的训练方案。在 TL;DR 摘要和 Anthropic HH 单轮对话上的实验表明，所提方法在不同噪声率下均优于多个 DPO 基线，提升了训练性能。

## Abstract
Direct Preference Optimization (DPO) has become an important method for aligning large language models (LLMs) with human preferences because it removes the need for explicit reward modeling and reinforcement learning optimization. However, its performance depends heavily on the quality of preference data, and noisy preference data in real-world settings can weaken alignment performance. To address this issue, we propose a bilevel optimization framework and prove, under certain assumptions, that this framework can recover the DPO optimum under clean data. We further derive a prior form for the learnable weighting function under asymmetric label-flipping noise. Considering that high-quality metadata may be difficult to obtain, we propose a task-agnostic meta-knowledge-driven method that enables meta-learning even when metadata is completely unavailable. To reduce the high cost of higher-order gradients in LLM meta-learning, we combine central-difference approximation with LoRA fine-tuning and develop a scalable training scheme. Experiments on TL;DR summarization and Anthropic HH single-turn dialogue show that the proposed method improves training performance over multiple DPO baselines under different noise rates.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）偏好对齐中的噪声数据问题，提出了一种创新的元学习重加权框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：直接偏好优化（DPO）虽然简化了对齐流程，但极易受到**噪声偏好标签**（即错误或矛盾的偏好标注）的影响。
*   **研究动机**：
    *   现有的鲁棒 DPO 方法通常依赖预定义的噪声分布或启发式规则，缺乏灵活性。
    *   现有的元学习重加权方法（如 MetaPO）通常需要一小部分**高质量、干净的元数据（Metadata）**来指导权重学习，但在现实场景中，获取这种“绝对正确”的标注成本极高甚至不可行。
    *   在 LLM 上进行元学习涉及高阶梯度计算，计算和显存开销巨大。

### 2. 方法论：PACMR-DPO
论文提出了 **PACMR-DPO**（Prompt-Augmentation-Consistency Meta-Reweighted DPO），其核心思想是通过双层优化自动学习样本权重，且无需干净元数据。

*   **双层优化框架**：
    *   **内层优化**：在带权重的噪声训练集上优化策略模型参数 $\omega$。
    *   **外层优化**：优化权重函数（VNet）参数 $\Theta$，使训练后的模型在元目标上表现最优。
*   **提示词增强一致性（PAC）**：这是本文的创新点。由于没有干净元数据，作者引入了“任务无关的元知识”——即模型对同一个问题的不同表达（通过回译等手段增强后的 Prompt）应给出一致的偏好判断。以此作为外层优化的监督信号。
*   **权重函数设计**：VNet 接收当前模型的隐式奖励差（$u$）和奖励总和（$\Delta$）作为输入，输出一个 0 到 1 之间的权重值。
*   **高效训练技术**：
    *   **中心差分近似（Central-Difference Approximation）**：利用有限差分法近似计算元梯度，避免了存储和计算二阶导数。
    *   **LoRA 空间扰动**：所有的参数更新和扰动均在 LoRA 低秩空间进行，极大地降低了显存占用。

### 3. 实验设计
*   **数据集**：
    *   **TL;DR 摘要任务**：Reddit 帖子摘要生成。
    *   **Anthropic HH 对话任务**：单轮对话的有用性与无害性对齐。
*   **噪声设置**：人工注入 20%、30%、40% 的随机翻转噪声（Random-flip noise）。
*   **对比基准（Benchmarks）**：
    *   标准 DPO、IPO（恒等偏好优化）、cDPO（保守 DPO）、rDPO（鲁棒 DPO）、Dr.DPO（分布鲁棒 DPO）。
*   **评估指标**：使用 GPT-5.1（模拟强模型裁判）进行两两对比，计算**胜率（Win-rate）**和**胜分（Win-score）**。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA GeForce RTX 4090 (24GB)** 显卡。
*   **软件框架**：基于 OpenRLHF 框架，使用 DeepSpeed ZeRO-3 优化。
*   **训练细节**：基座模型为 Llama-2-7B，采用 bf16 精度，LoRA Rank 为 16。每个任务训练 1 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同性质的任务（摘要 vs 对话）上进行了测试，涵盖了从中度到重度的三种噪声水平。
*   **消融实验**：
    *   对比了“学习权重”与“固定先验权重”的效果。
    *   进行了置信度阈值 $\tau$ 的敏感性分析。
    *   对比了“无元数据 PAC 目标”与“有干净元数据目标”的效果，证明了 PAC 目标的有效性。
*   **充分性评价**：实验设计较为全面，不仅有定量指标，还提供了 VNet 权重分布的可视化分析和定性案例展示，客观地证明了方法在处理噪声时的稳健性。

### 6. 主要结论与发现
*   **性能领先**：PACMR-DPO 在所有噪声水平下均优于标准 DPO 及其变体，尤其在 30% 和 40% 的高噪声环境下，优势更加明显。
*   **权重识别能力**：可视化结果显示，VNet 能够成功识别出被翻转的噪声样本，并为其分配极低的权重（如从 0.7 降至 0.3 左右）。
*   **元数据替代**：实验证明，利用提示词增强一致性作为监督信号，其效果可以媲美甚至超过使用少量干净元数据的方法。

### 7. 优点与亮点
*   **理论支撑强**：论文从条件风险和后验偏移的角度证明了双层优化恢复干净 DPO 最优解的可能性。
*   **实用性高**：无需昂贵的干净元数据，且通过中心差分和 LoRA 解决了 LLM 元学习的算力瓶颈，使其具备在大规模模型上部署的潜力。
*   **任务无关**：PAC 机制不依赖特定任务的标签，具有很强的通用性。

### 8. 不足与局限
*   **噪声类型单一**：实验主要针对随机翻转噪声（Random-flip），对于更复杂的实例相关噪声（Instance-dependent noise）或对抗性噪声的讨论较少。
*   **计算开销增加**：虽然使用了近似算法，但相比标准 DPO，PACMR-DPO 仍需额外的正负扰动前向计算，训练时间会有所增加。
*   **超参数敏感性**：置信度阈值 $\tau$ 和扰动尺度 $\epsilon$ 对最终效果有一定影响，可能需要针对不同任务进行微调。

（完）
