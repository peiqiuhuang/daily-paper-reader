---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v1"
tags: ["query:bopl"]
score: 9.0
evidence: 将自适应策略的发现建模为双层优化问题
tldr: 本研究针对语言智能体在推理阶段的测试时学习（TTL）问题，指出传统方法依赖手工设计的固定策略。为此提出了Meta-TTL框架，通过双层优化将适应策略的发现转化为学习过程。内层循环执行TTL，外层循环利用进化搜索在多样化任务上优化策略。实验证明，Meta-TTL在Jericho和WebArena-Lite基准测试中显著优于手工基准，展现了强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有的测试时学习方法依赖于人工设计的固定适应策略，无法针对特定任务环境实现最优的性能提升。
method: 提出Meta-TTL框架，利用双层优化结构，通过内层TTL过程和外层进化搜索来自动学习和优化适应策略。
result: 在Jericho和WebArena-Lite基准测试的分布内及分布外设置下，Meta-TTL均一致优于手工设计的基准方法。
conclusion: 实验结果表明，通过学习获得的适应策略能够编码可迁移的策略，从而在训练任务分布之外实现良好的泛化。
---

## 摘要
测试时学习 (TTL) 使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略 (actor policy)，从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为此，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现过程建模为双层优化问题的框架。在该框架内，内层循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在智能体性能的指导下，外层循环在多样化的训练任务分布上采用进化搜索，以迭代地优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内 (ID) 和分布外 (OOD) 设置，并使用了多种元智能体骨干网络。两个基准测试的结果均显示，Meta-TTL 一贯优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

### 论文总结：学习在测试时学习（Meta-TTL）

#### 1. 论文的核心问题与整体含义
*   **研究背景**：大型语言模型（LLM）智能体在部署到新环境时，往往难以即时调整策略。现有的“测试时学习”（Test-Time Learning, TTL）方法（如 Reflexion）允许智能体通过重复尝试来改进，但其核心的“自适应策略”（即如何根据失败经验更新下一次尝试的指令）通常是人工预设的固定提示词。
*   **核心问题**：人工设计的自适应策略往往基于直觉，无法针对特定任务环境实现最优的自我进化。
*   **研究动机**：作者认为有效的自适应能力本身应该是可学习的。论文提出了 **Meta-TTL** 框架，旨在通过在多种任务上进行元训练，自动发现并优化能够引导智能体在测试阶段实现自我改进的“学习算法”。

#### 2. 论文提出的方法论
*   **核心思想**：将 TTL 建模为一个**双层优化（Bi-level Optimization）**问题。
    *   **内层循环（Inner Loop）**：执行标准的 TTL 过程。智能体（Actor）在特定任务中进行多轮尝试，元智能体（Meta-Agent）根据历史轨迹重写 Actor 的系统提示词。
    *   **外层循环（Outer Loop）**：进化元训练。通过在训练任务分布上进行进化搜索，不断迭代优化元智能体的“元提示词”（Meta-Prompt），使其能生成更有效的反馈。
*   **关键技术细节**：
    *   **自适应策略实例化**：将自适应策略实现为一段自然语言形式的元提示词 $\phi$，它指导元智能体如何诊断错误、提取事实并规划下一步。
    *   **评估指标 (W-AUC)**：采用加权学习曲线下面积（Weighted Area Under the Learning Curve），对后期回合的成功给予更高权重，以衡量持续改进的能力。
    *   **进化搜索算法**：利用一个“提议 LLM”对当前表现优秀的元提示词进行反思和变异，通过局部验证（单任务）和全局验证（验证集全任务）来维护一个“专家池”。

#### 3. 实验设计
*   **实验场景与数据集**：
    *   **Jericho**：交互式小说游戏（文本冒险类），具有密集的动作奖励。
    *   **WebArena-Lite**：网络导航任务，具有稀疏的二元奖励（成功/失败）。
*   **基准测试 (Benchmark)**：
    *   **分布内 (ID)**：用于元训练和验证的任务。
    *   **分布外 (OOD)**：完全未见过的任务，用于测试策略的迁移能力。
*   **对比方法**：
    *   **Static**：无自适应能力的基准。
    *   **Reflexion**：经典的手工设计反馈方法。
    *   **Memory Agent**：基于记忆积累的方法。
    *   **Naive Meta-Agent**：使用相同架构但未经元优化的元智能体。
*   **模型骨干**：使用了 Gemini 3 Flash, GLM-5, 和 GPT-5 作为元智能体的后端。

#### 4. 资源与算力
*   **算力说明**：论文中未明确提及具体的 GPU 型号和数量。
*   **训练时长**：文中提到一个典型的实验案例（GPT-5 在 Jericho 任务上）经历了 26 次迭代，耗时约 **27 小时**。这表明该方法虽然在测试时是轻量级的（仅需推理），但在离线元训练阶段存在一定的计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的决策领域（游戏与网页导航），并在每个领域都划分了 ID 和 OOD 集合。
*   **充分性评价**：
    *   **多模型验证**：在三种不同的顶级 LLM 骨干上均验证了有效性，证明了框架的普适性。
    *   **消融与案例分析**：通过学习曲线图和具体的 Prompt 演化案例，深入分析了优化过程。
    *   **客观性**：引入了 Z-score 标准化来处理不同任务间奖励规模不一的问题，确保了专家选择的公平性。实验设计较为严谨。

#### 6. 论文的主要结论与发现
*   **性能提升**：Meta-TTL 在所有基准上均显著优于手工设计的 TTL 方法。在 Jericho ID 任务上，平均得分从 50.4 提升至 110.8（约 120% 提升）。
*   **泛化能力**：学习到的自适应策略在 OOD 任务上依然有效，说明它捕捉到了通用的“学习模式”（如如何进行信用分配、如何管理探索），而非仅仅死记硬背任务答案。
*   **涌现策略**：优化后的元提示词自发形成了结构化的反馈模式，包括强制性的诊断步骤、显式的信用分配协议以及受控的探索管理。

#### 7. 优点
*   **原理性创新**：将 TTL 从“手工设计提示词”提升到了“学习如何设计提示词”的元学习高度。
*   **可解释性与迁移性**：产出的结果是自然语言形式的“学习算法”，不仅人类可读，而且可以跨模型骨干直接迁移使用。
*   **无需权重更新**：在测试阶段仅通过 Prompt 改写实现适应，避免了昂贵的梯度计算和微调。

#### 8. 不足与局限
*   **奖励粒度依赖**：实验显示在奖励密集的 Jericho 上效果远好于奖励稀疏的 WebArena，说明该框架对环境反馈的质量有一定依赖。
*   **元训练成本**：进化搜索需要大量的环境交互和 LLM 调用，离线训练成本较高。
*   **纯 Prompt 限制**：目前仅限于 Prompt 空间的改写，对于需要深度参数调整的复杂适应场景可能存在上限。
*   **偏差风险**：如果训练任务分布过于单一，学习到的策略可能带有特定领域的偏见。

（完）
