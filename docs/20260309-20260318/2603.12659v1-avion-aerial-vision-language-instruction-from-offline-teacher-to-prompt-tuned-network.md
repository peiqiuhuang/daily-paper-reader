---
title: "AVION: Aerial Vision-Language Instruction from Offline Teacher to Prompt-Tuned Network"
title_zh: AVION：从离线教师到提示微调网络的航空视觉-语言指令
authors: "Yu Hu, Jianyang Gu, Hao Liu, Yue Cao, Jozsef Hamari, Zheng Liu, Mohsen Zardadi"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12659v1"
tags: ["query:bopl"]
score: 9.0
evidence: 在视觉和语言编码器中集成轻量级可学习提示词
tldr: 针对遥感图像视觉语言模型适配中语义覆盖不足和特征适应性差的问题，本文提出AVION知识蒸馏框架。该框架利用大语言模型构建并验证丰富的文本原型作为离线教师，引导集成轻量级可学习提示的视觉与语言编码器（学生网络）进行对齐。实验证明，AVION在保持泛化能力的同时，显著提升了少样本分类和跨模态检索性能，且参数量极小。
selection_source: fresh_fetch
motivation: 遥感场景中复杂的视觉外观和细粒度物体区分对现有视觉语言模型的语义覆盖和特征适应性提出了挑战。
method: 提出一种知识蒸馏框架，通过大语言模型生成并验证文本原型作为教师，指导学生网络中的轻量级提示微调。
result: 在六个遥感基准测试中，该方法在提升少样本分类准确率和跨模态检索召回率的同时，保持了对新类别的泛化能力。
conclusion: AVION通过离线教师指导的提示微调，有效地实现了视觉语言模型在遥感领域的高效适配与性能增强。
---

## 摘要
将视觉-语言模型适配到遥感图像仍面临挑战，主要归因于两个关键因素：文本表示中的语义覆盖有限，以及视觉特征的自适应能力不足。这些问题在航空场景中尤为显著，因为航空场景涉及多样的视觉外观和细粒度的物体区分。我们提出了 AVION，这是一个专为视觉-语言模型的遥感适配而设计的知识蒸馏框架。教师模块通过从大语言模型收集描述并利用遥感图像特征验证其有效性，从而构建语义丰富的文本原型。学生模块将轻量级且可学习的提示集成到视觉和语言编码器中，并在教师的指导下对齐嵌入及其跨模态关系。训练完成后，学生模块在推理过程中独立运行。在六个光学遥感基准数据集上的实验表明，AVION 提高了少样本分类和基类准确率，且不会降低对新类别的泛化能力。它还以极少的额外可训练参数提升了跨模态检索的平均召回率。

## Abstract
Adapting vision-language models to remote sensing imagery remains challenging due to two key factors: limited semantic coverage in textual representations and insufficient adaptability of visual features. These issues are particularly significant in aerial scenes, which involve various visual appearances and fine-grained object distinctions. We propose AVION, a knowledge distillation framework tailored for remote sensing adaptation of vision-language models. The teacher module constructs semantically rich textual prototypes by collecting descriptions from a large language model and verifying validity using remote sensing image features. The student module integrates lightweight and learnable prompts into both vision and language encoders, guided by the teacher to align embeddings and their cross-modal relationships. Once trained, the student operates independently during inference. Experiments on six optical remote sensing benchmarks show that AVION improves few-shot classification and base-class accuracy without degrading generalization to novel categories. It also enhances mean recall for cross-modal retrieval, with minimal additional trainable parameters.

---

## 论文详细总结（自动生成）

以下是对论文《AVION: Aerial Vision-Language Instruction from Offline Teacher to Prompt-Tuned Network》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：将通用视觉-语言模型（VLM）适配到遥感（RS）领域时存在两大瓶颈：
    1.  **语义贫乏（Semantic Poverty）**：遥感数据集通常仅提供简单的类别名称，无法捕捉航空图像中复杂的视觉变体（如不同季节、分辨率、传感器下的同一类别差异）。
    2.  **视觉僵化（Visual Rigidity）**：现有的参数高效微调（PEFT）方法往往冻结视觉编码器，导致模型难以处理遥感特有的俯视视角、尺度变化和异构性。
*   **研究背景**：虽然全量微调效果好，但计算成本高且在样本稀缺时容易过拟合。现有的提示学习（Prompt Learning）方法在遥感领域的泛化能力和细粒度区分度不足。

### 2. 核心方法论
AVION 提出了一个**知识蒸馏框架**，通过“离线教师”指导“轻量级学生”：
*   **LLM 驱动的领域提示（LLM-based Domain Prompting）**：利用大语言模型（如 Gemini）为每个遥感类别生成多条丰富的航空视角描述，弥补类别名称的语义不足。
*   **选择性原型聚合（Selective Prototype Aggregation）**：
    *   **RS-Flag 过滤**：根据预设规则（包含遥感关键词、排除地面视角词汇、长度限制）筛选 LLM 生成的文本。
    *   **视觉引导验证**：利用教师模型的图像特征计算文本描述的相似度得分，通过基于 MAD（中值绝对偏差）的鲁棒剪枝剔除离群值，最后加权聚合生成“文本原型”。
*   **三维度对齐蒸馏（Tri-aspect Alignment）**：
    1.  **视觉对齐 ($L_{img}$)**：引导学生模型的视觉提示（Visual Prompts）模拟教师的图像嵌入。
    2.  **文本对齐 ($L_{text}$)**：使学生的文本嵌入对齐经过视觉验证的丰富文本原型。
    3.  **相似度逻辑对齐 ($L_{logit}$)**：通过 KL 散度使学生的预测分布（类别间关系）趋近于教师。
*   **学生网络结构**：在冻结的视觉和语言编码器中插入轻量级的可学习深层提示（Deep Prompts）。

### 3. 实验设计
*   **数据集**：
    *   **分类任务**：AID, RESISC-45, EuroSAT, WHU-RS19, PatternNet, UCMerced（共6个基准）。
    *   **检索任务**：RSITMD, RSICD。
*   **实验场景**：少样本分类（1, 2, 4, 8, 16-shot）、基类到新类（Base-to-Novel）的泛化性能、跨模态检索。
*   **对比方法**：
    *   **零样本模型**：CLIP, RemoteCLIP, GeoRSCLIP。
    *   **PEFT 方法**：CoOp, CoCoOp, MaPLe, PromptKD, APPLeNet, MMRL 等。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA L4 GPU (24 GB)**。
*   **训练配置**：
    *   少样本实验训练 100 个 epoch；泛化与检索实验训练 50 个 epoch。
    *   优化器为 AdamW，学习率 $5 \times 10^{-4}$，Batch Size 为 4。
    *   教师模型采用 GeoRSCLIP (ViT-H/14)，学生模型采用 GeoRSCLIP (ViT-B/32)。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 8 个主流遥感数据集，进行了数百次实验运行。
*   **消融实验**：设计了极其详尽的增量消融（从 B0 到 B7 共 8 组配置），分别验证了深层提示、视觉对齐、文本原型增强、逻辑蒸馏及预热策略的有效性。
*   **客观性**：通过跨数据集验证（在 PatternNet 训练，在其他数据集测试）和更换不同 LLM（GPT-5, Llama-3.1）生成描述的对比，证明了方法的鲁棒性和通用性，实验设计严谨、公平。

### 6. 主要结论
*   **性能领先**：在所有 K-shot 设置下均达到或超过 SOTA，尤其在 8-shot 时领先第二名 2.65%。
*   **泛化能力强**：AVION 是对比方法中唯一一个在提升基类准确率的同时，未导致新类性能下降（甚至有所提升）的方法。
*   **检索效率高**：在固定图库（Frozen-gallery）的检索设定下，以极小的参数更新实现了优于全量微调的效果。

### 7. 优点与亮点
*   **语义增强方案巧妙**：通过 LLM 生成描述并结合视觉特征进行“后验验证”，有效解决了遥感图像“同类异构”的语义匹配问题。
*   **参数极其高效**：可训练参数量远低于主干网络的 1%，且推理时无需教师模型参与。
*   **即插即用**：不仅适用于遥感，在 ImageNet 等通用领域的实验也证明了其架构的普适性。

### 8. 不足与局限
*   **离线依赖**：文本原型的构建依赖于离线 LLM 调用和教师模型特征预计算，虽然不影响推理速度，但增加了训练前的准备环节。
*   **RS-Flag 规则依赖**：过滤规则（如关键词列表）带有一定的人工启发式色彩，可能需要针对极特殊的遥感任务（如非光学遥感）进行微调。
*   **计算开销**：虽然参数量小，但深层提示（Deep Prompting）在视觉编码器中会增加一定的 FLOPs（约 16%-35% 的注意力计算开销）。

（完）
