---
title: "Adaptive Prompt Structure Factorization: A Framework for Self-Discovering and Optimizing Compositional Prompt Programs"
title_zh: 自适应提示结构因子分解：一种用于自我发现和优化组合式提示程序的框架
authors: "Haoyue Liu, Zhichao Wang, Yongxin Guo, Haoran Shou, Xiaoying Tang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06699v1"
tags: ["query:bopl"]
score: 9.0
evidence: 自动提示词优化与结构自发现
tldr: 针对现有提示词优化方法中整体式编辑导致的组件耦合、归因不明及资源浪费问题，本文提出自适应提示结构分解（aPSF）框架。该框架利用架构师模型将提示词分解为语义因子，通过干预式单因子更新和误差导向的选择机制，实现高效的任务特定结构发现与优化。实验表明，aPSF在多个推理任务上超越了现有基准，显著提升了准确率并大幅降低了优化成本。
selection_source: fresh_fetch
motivation: 现有的提示词优化方法通常将提示词视为整体进行迭代，导致难以精准定位错误来源且优化效率低下。
method: 提出aPSF框架，通过架构师模型将提示词分解为独立语义因子，并结合干预式评分与误差导向的选择机制进行针对性优化。
result: "在多个推理基准测试中，aPSF不仅将平均准确率提升了2.16个百分点，还减少了高达87%的Token消耗。"
conclusion: aPSF通过结构化分解和精准归因，为大语言模型提供了一种更高效、可控且低成本的自动化提示词优化方案。
---

## 摘要
自动提示优化对于从大语言模型（LLM）中激发可靠的推理至关重要，然而，大多数仅限 API 的提示优化器迭代地编辑整体式提示，导致组件耦合、信用分配模糊、可控性受限且浪费 Token。我们提出了自适应提示结构因子分解（aPSF），这是一个仅限 API 的框架（提示输入/文本输出；无法访问模型内部），它使用一个架构师（Architect）模型来发现特定任务的提示结构作为语义因子。aPSF 随后执行干预性的单因子更新：干预性因子级评分通过验证性能的变化来估计每个因子的边际贡献，而错误引导的因子选择将更新路由到当前主要的错误来源，从而实现更具样本效率的优化。在多个高级推理基准测试中，aPSF 优于包括原则感知优化器在内的强基准模型，平均准确率提升高达 2.16 个百分点，并在 MultiArith 上减少了 45-87% 的 Token 优化成本，同时仅需 1 步即可达到峰值验证性能。

## Abstract
Automated prompt optimization is crucial for eliciting reliable reasoning from large language models (LLMs), yet most API-only prompt optimizers iteratively edit monolithic prompts, coupling components and obscuring credit assignment, limiting controllability, and wasting tokens. We propose Adaptive Prompt Structure Factorization (aPSF), an API-only framework (prompt-in/text-out; no access to model internals) that uses an Architect model to discover task-specific prompt structures as semantic factors. aPSF then performs interventional, single-factor updates: interventional factor-level scoring estimates each factor's marginal contribution via validation-performance changes, and error-guided factor selection routes updates to the current dominant failure source for more sample-efficient optimization. Across multiple advanced reasoning benchmarks, aPSF outperforms strong baselines including principle-aware optimizers, improving accuracy by up to +2.16 percentage points on average, and reduces optimization cost by 45--87% tokens on MultiArith while reaching peak validation in 1 step.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Prompt Structure Factorization (aPSF)》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的“仅限API”（API-only）提示词优化方法（如OPRO、APE）通常将提示词视为一个不可分割的整体（Monolithic）。
*   **研究动机**：
    *   **信用分配模糊**：整体式编辑无法确定是提示词的哪个部分导致了性能提升或下降。
    *   **可控性差**：无法有选择地固定某些关键部分（如输出格式）。
    *   **资源浪费**：单次变动往往同时改变多个功能角色，导致大量无效尝试和Token消耗。
*   **整体含义**：论文提出将提示词视为由多个“语义因子”组成的组合式程序，通过自动发现任务特定的结构并进行针对性的单因子优化，提升效率和可解释性。

### 2. 方法论
aPSF 框架分为两个主要阶段：
*   **阶段 1：自适应结构发现（Adaptive Structure Discovery）**
    *   利用一个**架构师模型（Architect LLM）**分析任务描述和示例，将提示词分解为 $K$ 个语义因子（如：问题分析、推理步骤、计算执行、格式检查等）。
    *   支持“从零开始”发现结构，或基于用户提供的初始提示词进行扩展和分解。
*   **阶段 2：干预式因子优化（Interventional Factor Optimization）**
    *   **单因子更新**：每次只编辑一个因子，冻结其他因子。这类似于坐标下降法，确保了改进的可归因性。
    *   **干预式评分**：通过替换单个因子后的验证集得分变化，量化该因子的边际贡献。
    *   **误差导向的因子选择（Routing）**：收集验证集中的失败案例，由架构师模型诊断错误原因，并将优化预算自动路由到当前的“瓶颈因子”（例如，若多为格式错误，则优先优化格式因子）。

### 3. 实验设计
*   **数据集**：涵盖了数学推理和逻辑推理的多个基准，包括 GSM8K、AQUA-RAT、MultiArith、GSM-Hard、BBH（17个子任务）、MMLU、MATH 和 GPQA。
*   **对比方法（Baselines）**：
    *   **传统优化器**：OPRO, APE, ProTeGi, GrIPS。
    *   **原则感知优化器**：ZERA, CriSPO。
    *   **程序化框架**：DSPy。
*   **实验设置**：使用 Qwen2.5-7B-Instruct 和 Llama-3.1-8B-Instruct 作为执行任务的 **Worker** 模型；使用 Qwen3-8B 和 gpt-oss-120b 作为 **Architect** 模型。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **2张 NVIDIA A100 80GB GPU** 用于 Worker 模型的推理。
*   **算力消耗**：论文强调了其极高的效率。在 MultiArith 数据集上，aPSF 相比基准方法减少了 **45%–87%** 的 Token 消耗，且通常在第 1 步迭代时就能达到峰值验证准确率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 6 个主要推理基准上进行了全面测试。
    *   对 BBH 的 17 个子任务逐一评估。
    *   每组实验运行 5 次并报告平均值和标准差，以消除随机性影响。
*   **消融实验**：
    *   验证了“结构化”本身的必要性（对比无结构模板）。
    *   对比了不同的调度策略（随机、轮询、贪婪 vs. 误差导向选择）。
    *   验证了“冻结其他因子”对稳定性的贡献。
*   **充分性评价**：实验设计非常充分，不仅涵盖了多种模型组合，还通过跨任务迁移实验验证了优化后提示词的泛化性，论证过程客观公平。

### 6. 主要结论与发现
*   **性能领先**：aPSF 在所有测试设置中均取得了最佳平均准确率，相比最强基准提升了高达 **2.16%**。
*   **极速收敛**：aPSF 能够迅速定位瓶颈，通常在 1-3 步内完成优化，而传统方法需要 6-10 步。
*   **任务自适应性**：不同任务会激活不同的因子优化路径。例如，数学任务侧重于“计算执行”因子，而逻辑任务侧重于“建立顺序”因子。
*   **可解释性**：通过因子分解，开发者可以清晰地看到每个组件对最终性能的贡献。

### 7. 优点
*   **高效率与低成本**：通过精准的单因子干预，极大地节省了 API 调用成本。
*   **解耦与归因**：解决了提示词工程中的“黑盒”问题，实现了组件级的信用分配。
*   **灵活性**：任务特定的因子分解比 DSPy 等框架预设的固定模板更具弹性。
*   **稳定性**：单因子更新机制避免了整体变动带来的性能剧烈波动。

### 8. 不足与局限
*   **架构师模型依赖**：优化效果高度依赖于 Architect 模型的能力，若 Architect 较弱，可能导致因子分解不合理或错误诊断。
*   **因子独立性假设**：该方法假设因子之间是部分独立的。虽然这简化了搜索空间，但可能会忽略某些因子之间的复杂协同效应（Synergy）。
*   **应用范围限制**：目前主要在文本推理任务上验证，尚未在多模态或工具调用（Tool-use）等更复杂的场景中广泛测试。
*   **超参数敏感性**：候选版本数量 $N$ 和接受阈值 $\delta$ 的设置对最终结果有一定影响。

（完）
