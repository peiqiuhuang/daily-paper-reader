---
title: "Two-Stage Prompt Optimization for Few-Shot Relation Extraction: From Reasoning-Guided Search to Gradient-Guided Refinement"
title_zh: 少样本关系抽取的两阶段提示优化：从推理引导的搜索到梯度引导的精炼
authors: "Aunabil Chakma, Mihai Surdeanu, Eduardo Blanco"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29639v1"
tags: ["query:bopl"]
score: 9.0
evidence: 使用梯度引导优化的自动提示词优化
tldr: 针对小参数语言模型在少样本关系抽取任务中提示词优化不足的问题，本文提出一种两阶段优化框架。第一阶段利用推理优化器进行全局自然语言改进，第二阶段通过提出的GradPO算法，利用梯度信号定位关键片段并进行局部微调。实验证明，该框架在FS-TACRED数据集上达到SOTA性能，有效提升了提示词的质量与模型的抽取准确度。
selection_source: fresh_fetch
motivation: 旨在解决小规模语言模型在少样本关系抽取中自动提示词优化效果不佳的问题。
method: 提出结合推理引导的全局搜索与梯度引导的局部精炼（GradPO）的两阶段提示词优化框架。
result: 在FS-TACRED和FS-FewRel数据集上的实验表明，局部精炼能显著提升初始提示词效果，并在FS-TACRED上取得领先成绩。
conclusion: 两阶段优化框架为提升小模型在复杂关系抽取任务中的表现提供了一种高效且一致的提示词优化方案。
---

## 摘要
针对较小语言模型的情景式少样本关系抽取，自动提示优化仍处于探索阶段。我们提出了一个两阶段框架，将基于推理的提示优化与基于梯度的提示优化相结合。第一阶段可以使用任何基于推理的优化器，在自然语言层面进行广泛的提示改进。第二阶段应用了我们提出的 GradPO，它利用损失和梯度信号来识别高影响力的提示片段，并通过局部编辑对其进行精炼。在 FS-TACRED 和 FS-FewRel 上的实验表明，局部精炼通常能改进第一阶段找到的提示，且 GradPO 是最稳定的精炼器。我们的框架在使用 Qwen3-4B 模型时，在 FS-TACRED 上达到了最先进的性能，并在 FS-FewRel 上保持了竞争力。

## Abstract
Automatic prompt optimization is still underexplored for episodic few-shot relation extraction with smaller language models. We propose a two-stage framework that combines reasoning-based prompt optimization with gradient-based prompt optimization. The first stage can use any reasoning-based optimizer to make broadprompt improvements in natural language. The second stage applies our GradPO, which uses loss and gradient signals to identify high-impact prompt spans and refine them with local edits. Experiments on FS-TACRED and FS-FewRel show that local refinement usually improves prompts found by the first stage, and GradPO is the most consistent refiner. Our framework achieves state-of-the-art performance on FS-TACRED with Qwen3-4B and remains competitive on FS-FewRel.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对少样本关系抽取（Few-Shot Relation Extraction, FSRE）任务的两阶段自动提示词优化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在少样本场景下，较小规模的语言模型（如 4B 参数量）对提示词（Prompt）极其敏感。现有的自动提示优化（APO）方法通常分为“基于推理”和“基于梯度”两类，前者擅长全局语义改写但缺乏局部精确性，后者擅长局部微调但搜索空间受限。
*   **研究背景**：FSRE 要求模型仅凭极少数示例（如 1-shot）识别实体间的语义关系。目前该领域多依赖人工设计提示词，缺乏针对小模型的高效自动优化方案。

### 2. 方法论
论文提出了一个结合全局搜索与局部精炼的两阶段框架：
*   **第一阶段：推理引导的全局搜索**
    *   利用现有的推理优化器（如 RPO、EvoPrompt 或 ETGPO）在自然语言空间进行广泛的语义改进。
    *   通过分析模型预测的错误反馈，生成多个候选提示词，并根据验证集表现筛选出最佳的初始提示词。
*   **第二阶段：梯度引导的局部精炼（GradPO）**
    *   **核心思想**：利用损失函数对提示词 Token 的梯度信号，定位对预测结果影响最大的“高影响力片段（Spans）”，而非单个 Token。
    *   **关键技术细节**：
        1.  **片段识别**：计算提示词中每个 Token 的梯度，将梯度值超过阈值的相邻 Token 组合成可编辑片段。
        2.  **候选生成**：使用目标模型自身生成片段的替换建议（GradPO-Gen）或基于 Token 概率分布进行采样（GradPO-Prob）。
        3.  **束搜索（Beam Search）**：在保持提示词自然语言结构的前提下，通过束搜索寻找能最小化“任务损失 + 流利度惩罚（Perplexity）”的最优片段组合。
    *   **公式目标**：$L(p, D) = L_{task}(p, D) + \lambda PPL(p)$，其中 $\lambda$ 控制流利度惩罚强度。

### 3. 实验设计
*   **数据集**：
    *   **FS-TACRED**：更具挑战性的现实场景少样本基准。
    *   **FS-FewRel**：基于 FewRel 构建的情景式基准。
*   **实验场景**：5-way 1-shot（5 类关系，每类仅 1 个示例），这是极具挑战性的设定。
*   **对比方法**：
    *   **第一阶段基准**：RPO（基于反馈）、EvoPrompt（基于进化算法）、ETGPO（基于错误分类）。
    *   **第二阶段对比**：GreaTer（逐 Token 梯度优化）、LPO（局部提示优化）。
*   **目标模型**：Qwen3-4B 和 Gemma3-4B。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **计算开销分析**：作者对比了 GradPO 与基准方法的计算成本。在 Qwen3-4B 实验中，GradPO 每次迭代产生约 105 个候选提示词。虽然前向传播次数增加，但由于主要开销在于验证集的评估（9000 个 episode），GradPO 增加的搜索成本仅占总评估成本的约 10.3%，在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了多轮测试。
    *   每组实验重复 5 次并报告均值、标准差及统计显著性（p < 0.05/0.01）。
    *   进行了跨模型迁移实验（Cross-model transfer）和迭代稳定性分析。
*   **充分性评价**：实验设计较为充分。通过组合不同的第一阶段和第二阶段优化器，验证了 GradPO 作为“通用精炼器”的有效性。消融实验和定性分析（展示优化前后的提示词）增强了结果的可信度。

### 6. 主要结论与发现
*   **互补性**：两阶段框架非常有效。第一阶段能快速提升性能，而第二阶段的 GradPO 能在已有的强力提示词基础上进一步挖掘潜力。
*   **GradPO 的优越性**：在 19 组实验案例中，有 17 组通过第二阶段精炼获得了提升，其中 GradPO 是表现最稳定且提升幅度最大的精炼器。
*   **SOTA 性能**：在 FS-TACRED 数据集上，使用 Qwen3-4B 模型将 F1 分数从 27.6 提升至 29.0，刷新了该模型在该任务上的纪录。
*   **稳定性**：相比于推理优化器在迭代中的波动，GradPO 的局部微调表现出更好的单调改进趋势。

### 7. 优点
*   **创新性结合**：首次系统性地结合了全局推理搜索与局部梯度精炼，解决了单一优化路径的局限。
*   **片段级优化**：相比于逐 Token 优化（如 GreaTer），GradPO 的片段级编辑能更好地保留自然语言的语义连贯性。
*   **模型自适应**：GradPO 仅依赖目标模型自身的信号，无需额外的超大规模模型作为优化器，降低了部署门槛。

### 8. 不足与局限
*   **超参数敏感性**：GradPO 依赖多个超参数（如片段数量、束宽、梯度采样大小），不同模型和数据集可能需要重新调优。
*   **迁移性较弱**：实验显示，针对特定模型优化的提示词在迁移到其他模型时效果有限，说明梯度优化具有高度的模型特异性。
*   **任务局限**：目前仅在非推理类任务（关系抽取）上验证，尚未在需要复杂链式推理（CoT）的任务中测试。
*   **模型规模限制**：主要针对 4B 左右的小模型，在超大规模模型（如 70B+）上的表现和必要性尚不明确。

（完）
