---
title: "$p1$: Better Prompt Optimization with Fewer Prompts"
title_zh: $p1$：用更少的提示词实现更好的提示词优化
authors: "Zhaolin Gao, Yu, Wang, Bo Liu, Thorsten Joachims, Kianté Brantley, Wen Sun"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08801v1"
tags: ["query:bopl"]
score: 9.0
evidence: 系统提示词优化与搜索
tldr: 本研究探讨了提示词优化的有效性，发现其成功取决于系统提示词间的奖励方差是否大于生成随机性带来的方差。研究指出，在异构数据集上增加用户提示词反而可能因方差稀释而损害优化效果。为此，作者提出了 p1 方法，通过筛选对不同系统提示词具有高区分度的少量用户提示词子集来提升优化效率。实验证明，p1 在推理任务上表现优异，仅需两个提示词即可获得泛化性极强的系统提示词。
selection_source: fresh_fetch
motivation: 旨在探究提示词优化在不同任务中效果差异的原因，并解决增加训练样本可能导致优化性能下降的悖论。
method: 提出 p1 过滤方法，通过筛选对候选系统提示词具有高奖励方差的小规模用户提示词子集，提高对系统提示词质量的辨别力。
result: 实验显示 p1 在推理基准测试中显著优于全量数据训练和 GEPA，仅需两个 AIME 24 样本即可训练出泛化性极佳的提示词。
conclusion: 提示词优化的关键在于样本的区分度而非数量，通过精选高方差样本可以实现更高效且泛化性更强的提示词搜索。
---

## 摘要
提示词优化通过搜索更好的系统提示词来改进语言模型，而无需更新其权重，但其有效性在不同任务之间差异巨大。我们研究了是什么因素使任务适合进行提示词优化。我们表明，不同系统提示词之间的奖励方差可以分解为两个部分：响应之间的方差（捕捉生成随机性）和系统提示词之间的方差（捕捉系统提示词质量的差异）。当系统提示词之间的方差足够大时，提示词优化会取得成功；但当响应之间的方差主导了系统提示词的方差时，优化则会失败。令人惊讶的是，我们进一步表明，扩展到更多的用户提示词可能会通过减少系统提示词之间的方差来损害优化，特别是在不同用户提示词偏好不同系统提示词的异构数据集上。受此启发，我们提出了 $p1$，这是一种简单的用户提示词过滤方法，它选择在候选系统提示词之间具有高方差的一小部分用户提示词子集。这一用户提示词子集能够区分系统提示词的优劣，从而使系统优化变得更加容易。在推理基准测试上的实验表明，$p1$ 相比于在全量数据集上训练显著提升了提示词优化的效果，并优于 GEPA 等强基准模型。值得注意的是，仅在来自 AIME 24 的两个提示词上进行训练，就能产生一个在其他推理基准测试上具有良好泛化能力的系统提示词。

## Abstract
Prompt optimization improves language models without updating their weights by searching for a better system prompt, but its effectiveness varies widely across tasks. We study what makes a task amenable to prompt optimization. We show that the reward variance across different system prompts can be decomposed into two components: variance among responses, which captures generation stochasticity, and variance among system prompts, which captures differences in system prompt quality. Prompt optimization succeeds when variance among system prompts is sufficiently large, but fails when variance among responses dominates the variance of the system prompts. Surprisingly, we further show that scaling to more user prompts can hurt optimization by reducing variance among system prompts, especially on heterogeneous datasets where different user prompts favor different system prompts. Motivated by this insight, we propose $p1$, a simple user prompt filtering method that selects a small subset of user prompts with high variance across candidate system prompts. This subset of user prompts allows one to distinguish a good system prompt from a bad one, making system optimization easier. Experiments on reasoning benchmarks show that $p1$ substantially improves prompt optimization over training on the full dataset and outperforms strong baselines such as GEPA. Notably, training on only two prompts from AIME 24 yields a system prompt that generalizes well to other reasoning benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《$p1$: Better Prompt Optimization with Fewer Prompts》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**提示词优化（Prompt Optimization）**在不同任务中表现极度不稳定的根本原因。尽管自动搜索系统提示词（System Prompt）能提升模型性能，但在指令遵循任务中效果显著，而在复杂数学推理（如 AIME）中往往收效甚微。
*   **研究动机**：揭示为何增加训练样本（User Prompts）有时反而会损害优化效果。
*   **核心发现**：在异构任务中，不同样本对系统提示词的偏好不同，在大数据集上取平均会“稀释”优化信号，导致模型无法分辨提示词的好坏。

### 2. 方法论：核心思想与算法流程
论文提出了 **$p1$** 方法，其核心思想是**“通过筛选高方差样本来增强信号”**。
*   **方差分解理论**：论文将奖励方差分解为两部分：
    1.  **响应方差（Variance among responses）**：由模型生成的随机性引起（噪声）。
    2.  **系统提示词方差（Variance among system prompts）**：由提示词质量差异引起（有效信号）。
*   **核心思想**：只有当“系统提示词方差”显著大于“响应方差”时，优化才有效。
*   **$p1$ 算法流程**：
    1.  **采样与评估**：使用初始策略采样多个候选系统提示词，并在原始数据集上进行多次响应采样。
    2.  **计算得分**：针对每个用户提示词，计算其在不同系统提示词下的奖励方差，并减去估计的响应方差，得到纯净的“系统提示词方差”得分。
    3.  **子集筛选**：选择得分最高（即最具区分度）的 $K_{top}$ 个用户提示词（默认仅为 2 个）。
    4.  **强化学习优化**：仅在这极小规模的子集上，利用 GRPO 等 RL 算法优化系统提示词生成策略。

### 3. 实验设计
*   **数据集**：
    *   **推理任务**：AIME 2024（训练），AIME 2025/2026、HMMT 2025/2026（测试）。
    *   **指令遵循**：IFBench。
*   **模型**：使用 Qwen3-4B-Instruct-2507 作为基础模型和提示词生成器，并在 Qwen3-1.7B 和 Qwen3-30B 上验证迁移性。
*   **对比基准（Baselines）**：
    *   **Base**：原始模型。
    *   **RL-all**：在全量数据集上进行强化学习优化。
    *   **GEPA**：一种基于反射和进化的强力提示词优化基准方法。

### 4. 资源与算力
*   **硬件配置**：使用了 **4 张 NVIDIA H100 GPU**。
*   **算力分配**：1 张 GPU 用于更新和生成系统提示词，3 张 GPU 用于并行生成响应以评估奖励。
*   **训练时长**：所有实验均在 **3 天（72 小时）** 的时间预算内完成。
*   **采样规模**：每步采样 $N=16$ 个系统提示词，每个提示词生成 $M$ 个响应（$M$ 随样本数 $K$ 调整，保持 $K \times M$ 恒定）。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个大类任务（推理与指令遵循）上进行了深入对比，涵盖了从 1.7B 到 30B 不同参数规模的模型。
*   **消融实验**：针对子集大小 $K_{top}$（1, 2, 4, 64 等）进行了详细消融，并分析了采样数 $M$ 对结果的影响。
*   **客观性**：实验不仅关注训练集奖励，还评估了在未见过的推理基准（HMMT）上的泛化能力，并对比了进化算法与 RL 算法，实验设计较为公平且具有说服力。

### 6. 主要结论与发现
*   **少即是多**：在数学推理等异构任务中，**仅用 2 个精心挑选的样本**进行优化，效果远好于使用全量数据集（30 个样本）。
*   **方差决定论**：提示词优化的成功取决于“信号（提示词方差）”与“噪声（响应方差）”的比率。增加样本数会降低信号强度。
*   **泛化性强**：通过 $p1$ 在 AIME 24 上优化出的系统提示词，不仅在 AIME 25/26 上表现优异，还能直接提升更大规模模型（30B）的性能。
*   **任务差异**：指令遵循任务是“同质的”，全量数据有效；而数学推理是“异构的”，必须进行样本筛选。

### 7. 优点
*   **理论深刻**：通过方差分解公式直观地解释了提示词优化在复杂任务中失效的数学本质。
*   **高效简洁**：$p1$ 方法不需要复杂的架构改动，仅通过数据过滤就实现了性能飞跃，极大地节省了计算资源。
*   **洞察直觉**：挑战了“数据越多越好”的传统认知，为长上下文推理模型的提示词工程提供了新思路。

### 8. 不足与局限
*   **奖励函数局限**：目前仅针对**二元奖励（Binary Reward，对或错）**进行了推导，尚未验证其在连续或密集奖励（Dense Reward）场景下的适用性。
*   **子集偏差风险**：虽然筛选出的子集在实验中表现良好，但如何保证该子集学到的提示词不会在某些特定类型的题目上产生负迁移，仍需更广泛的验证。
*   **搜索空间**：论文主要关注系统提示词的文本优化，未涉及多轮对话或更复杂的 Prompt 结构优化。

（完）
