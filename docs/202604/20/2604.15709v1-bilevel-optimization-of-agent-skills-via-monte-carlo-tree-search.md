---
title: Bilevel Optimization of Agent Skills via Monte Carlo Tree Search
title_zh: 基于蒙特卡洛树搜索的智能体技能双层优化
authors: "Chenyi Huang, Haoting Zhang, Jingxu Xu, Zeyu Zheng, Yunduan Lin"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15709v1"
tags: ["query:bopl"]
score: 9.0
evidence: 智能体技能与指令的双层优化
tldr: 针对大语言模型智能体技能优化中结构与内容高度耦合的难题，本文提出一种双层优化框架。该框架将技能优化建模为双层决策问题，外层利用蒙特卡洛树搜索（MCTS）确定技能结构，内层通过LLM精炼组件内容。在运筹学问答数据集上的实验证明，该方法能显著提升智能体执行特定任务的性能，为系统化设计智能体技能提供了新思路。
selection_source: fresh_fetch
motivation: 智能体技能的结构与内容存在强相互依赖性，导致其优化决策空间极其复杂，难以通过传统方法系统性提升。
method: 提出一种双层优化框架，结合MCTS进行外层结构搜索与LLM辅助的内层内容精炼。
result: 在开源运筹学问答（ORQA）数据集上的实验表明，该框架能有效优化技能并显著增强智能体的任务处理能力。
conclusion: 双层优化与MCTS的结合为LLM智能体技能的自动化设计与持续改进提供了一种高效且系统的方法。
---

## 摘要
智能体“技能”（skills）是指令、工具和支持资源的结构化集合，旨在帮助大语言模型（LLM）智能体执行特定类别的任务。实证研究表明，技能的设计会显著影响智能体的任务性能，但系统性地优化技能仍具挑战性。由于技能以结构化的方式包含指令、工具和支持资源，对其进行优化需要同时确定这些组件的结构以及每个组件包含的内容。这产生了一个复杂的决策空间，其中结构与组件之间存在强烈的相互依赖关系。因此，我们将这两个耦合的决策表示为技能结构和组件内容，并将技能优化建模为一个双层优化问题。我们提出了一个双层优化框架，其中外层循环采用蒙特卡洛树搜索来确定技能结构，而内层循环则在由外层循环选定的结构内细化组件内容。在两个循环中，我们都利用 LLM 来辅助优化过程。我们在一个开源的运筹学问答数据集上评估了所提出的框架，实验结果表明，该双层优化框架提升了配备优化后技能的智能体的性能。

## Abstract
Agent \texttt{skills} are structured collections of instructions, tools, and supporting resources that help large language model (LLM) agents perform particular classes of tasks. Empirical evidence shows that the design of \texttt{skills} can materially affect agent task performance, yet systematically optimizing \texttt{skills} remains challenging. Since a \texttt{skill} comprises instructions, tools, and supporting resources in a structured way, optimizing it requires jointly determining both the structure of these components and the content each component contains. This gives rise to a complex decision space with strong interdependence across structure and components. We therefore represent these two coupled decisions as \texttt{skill} structure and component content, and formulate \texttt{skill} optimization as a bilevel optimization problem. We propose a bilevel optimization framework in which an outer loop employs Monte Carlo Tree Search to determine the \texttt{skill} structure, while an inner loop refines the component content within the structure selected by the outer loop. In both loops, we employ LLMs to assist the optimization procedure. We evaluate the proposed framework on an open-source Operations Research Question Answering dataset, and the experimental results suggest that the bilevel optimization framework improves the performance of the agents with the optimized \texttt{skill}.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bilevel Optimization of Agent Skills via Monte Carlo Tree Search》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）智能体通常依赖“技能”（Skills）——即指令、工具和参考资料的结构化集合——来处理复杂任务。
*   **核心问题**：技能的设计（包括文件夹结构和具体文本内容）极大影响智能体性能。然而，技能的**结构**（Structure）与**内容**（Content）高度耦合，且决策空间具有离散、组合和路径依赖的特点，导致系统化优化极其困难。
*   **研究动机**：现有的优化方法往往只关注提示词（Prompt）或代码，缺乏对这种异构、结构化软件包的整体优化框架。

### 2. 方法论
论文提出了一种**双层优化框架**，将技能优化建模为 $S = (\theta, \phi)$ 的寻找过程（$\theta$ 为结构，$\phi$ 为内容）：
*   **外层循环（结构搜索）**：采用**蒙特卡洛树搜索（MCTS）**。
    *   **节点与边**：每个节点代表一种技能结构，边代表结构编辑动作（如增删组件、移动文件）。
    *   **LLM 引导推理**：利用 LLM 进行“分析-诊断-提案”三阶段推理，根据历史反馈生成新的结构候选。
    *   **搜索策略**：支持 UCB1 算法或针对高噪声环境的“混合概率采样”策略。
*   **内层循环（内容精炼）**：
    *   **对齐（Alignment）**：通过桥接操作将旧内容迁移至新结构中。
    *   **族群特定精炼**：根据编辑类型（如指令、脚本、元数据）调用专门的 LLM 优化器进行多轮尝试。
    *   **悲观评估（LCB）**：引入置信下界（Lower Confidence Bound）机制，对观察到的性能提升进行“去噪”处理，只有通过悲观门控的改进才会被采纳。

### 3. 实验设计
*   **数据集**：**ORQA (Operations Research Question Answering)**，这是一个运筹学问答基准，要求智能体识别优化模型的变量、约束和目标。
*   **Benchmark**：以 AI 自动生成的初始技能（Seed Skill）作为基准线。
*   **对比方法**：对比了两种 MCTS 配置：
    *   **配置 A**：保守型，使用 UCB1 选择和较少的搜索轮次。
    *   **配置 B**：探索型，使用混合概率选择和更多的搜索轮次。
*   **实验流程**：将数据集划分为搜索集（Search）、确认集（Confirm）和测试集（Test），模拟机器学习的训练/验证/测试过程。

### 4. 资源与算力
*   **模型使用**：运行时评估使用 `openai/gpt-5.2-codex`，优化推理（MCTS 决策）使用 `openai/gpt-5.4`。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号或训练总时长。
*   **Token 预算**：详细规定了各阶段的 Token 上限，例如提案阶段（Proposal）上限为 20,000 tokens，以支持复杂的上下文推理。

### 5. 实验数量与充分性
*   **实验规模**：优化过程基于 120 个样本的子集。
*   **充分性评价**：
    *   **客观性**：采用了严格的确认集（Confirm split）进行模型选择，避免了在搜索集上的过拟合。
    *   **深度**：展示了完整的 MCTS 搜索树演化路径，证明了算法并非随机搜索。
    *   **局限性**：实验仅在一个数据集（ORQA）上进行，缺乏跨领域（如代码生成、数据分析）的横向对比，实验总样本量较小。

### 6. 主要结论与发现
*   **性能提升**：优化后的技能在测试集上实现了 **+3.125%** 的准确率提升（从 90.625% 升至 93.75%）。
*   **结构发现**：搜索算法发现，将原本分散在参考文件中的知识“扁平化”集成到主指令文件（SKILL.md）中，并增加显式的“分类检查清单”，能显著增强智能体的执行效率。
*   **协同效应**：证明了结构调整（让信息更易获取）与内容精炼（让指令更清晰）的结合优于单一维度的优化。

### 7. 优点
*   **架构创新**：首次将双层优化与 MCTS 结合用于智能体技能包的自动化设计。
*   **抗噪设计**：内层循环的 LCB 机制有效解决了 LLM 评估结果波动大的问题，确保了优化路径的可靠性。
*   **符合标准**：严格遵循 Agent Skills 官方规范，具有较强的工程实用价值。

### 8. 不足与局限
*   **成本高昂**：依赖顶级 LLM（GPT-5.4）进行多轮 MCTS 搜索，Token 消耗和时间成本较高。
*   **泛化风险**：由于优化是在特定任务的小样本上完成的，该技能在处理非运筹学任务时的泛化能力未被验证。
*   **复杂度限制**：目前主要优化的是文本和简单脚本，对于包含复杂多文件依赖或大规模外部工具集的技能优化效果尚待观察。

（完）
