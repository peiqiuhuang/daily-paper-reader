---
title: Differentiating Minimal-Norm Solutions to Parametric Optimization Problems
title_zh: 参数化优化问题中最小范数解的求导
authors: "Baptiste Plaquevent-Jourdain, Jalal Fadili, Antonio Silveti-Falls"
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.28899v1"
tags: ["query:bopl"]
score: 9.0
evidence: 通过参数化优化进行双层规划求导
tldr: 本文针对双层规划和元学习中参数化优化问题的微分难题，提出了一种处理多解情况的新方法。传统隐式微分依赖雅可比矩阵可逆，而本文通过引入一致值域条件，利用吉洪诺夫正则化和保守集值场理论，证明了最小范数解映射存在广义导数。研究给出了适用于平滑凸目标及非平滑复合问题（如LASSO）的伪逆公式，并在数据投毒等实验中验证了其在病态设置下的有效性。
selection_source: fresh_fetch
motivation: 传统的隐式微分方法在优化问题存在多个解导致雅可比矩阵不可逆时失效，限制了其在复杂模型中的应用。
method: 利用吉洪诺夫正则化和保守集值场理论，在一致值域条件下推导出最小范数解的广义导数及伪逆计算公式。
result: 该方法成功应用于最小二乘、Huber回归和LASSO等典型问题，并在数据投毒和超清洗任务中验证了其有效性。
conclusion: 研究扩展了非平滑隐式微分在病态优化问题中的适用范围，为多解情况下的梯度计算提供了严谨的理论支撑。
---

## 摘要
对参数化优化问题进行求导是双层规划和元学习的核心，通常通过近似隐式微分来实现。隐函数定理要求对最优性条件的偏雅可比矩阵求逆，但在存在多个解时，这一要求无法满足。尽管如此，在这种情况下，可以将可逆性放宽为严格更弱的一致值域条件。在此条件下，通过利用极限 Tikhonov 正则化论证和保守集值场论，证明了最小范数解映射允许广义导数。通过对广义 Hessian 矩阵特征值的额外控制，证明了伪逆公式的合理性。该结论针对一类光滑凸目标函数建立，并扩展到了非光滑复合问题。这些假设在最小二乘法、Huber 回归和 LASSO 中得到了验证。由此产生的将非光滑隐式微分扩展到病态设置的方法，在数据投毒和数据超清洗问题上进行了实验检验。

## Abstract
Differentiating through parametric optimization problems is central to bilevel programming and meta-learning, often accomplished using approximate implicit differentiation. The implicit function theorem requires inverting a partial Jacobian of the optimality condition, which fails when there are many solutions. Nonetheless, in such cases it is possible to relax invertibility to a strictly weaker uniform range condition, under which it is shown that the minimal-norm solution mapping admits generalized derivatives by using a limiting Tikhonov regularization argument and conservative set-valued field theory. With additional control on the eigenvalues of the generalized Hessians, a pseudoinverse formula is justified. This is established for a class of smooth convex objectives and extended to nonsmooth composite problems. These assumptions are verified for Least-Squares, Huber regression and LASSO. The resulting extension of nonsmooth implicit differentiation to ill-posed settings is examined experimentally on data poisoning and data hypercleaning problems.

---

## 论文详细总结（自动生成）

这篇论文《Differentiating Minimal-Norm Solutions to Parametric Optimization Problems》深入探讨了在双层规划（Bilevel Programming）中，当底层优化问题存在多个解（即病态问题）时，如何进行隐式微分的理论与实践问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
在机器学习（如超参数优化、元学习、数据投毒）中，双层规划要求对底层问题的解 $x^*(\theta)$ 关于参数 $\theta$ 求导。传统的**隐式函数定理（IFT）**要求底层目标函数的 Hessian 矩阵必须可逆。然而，在过参数化模型或如 LASSO、最小二乘（当样本数小于特征数时）等常见问题中，底层问题往往有多个解，导致 Hessian 矩阵奇异，传统方法失效。
**核心问题：** 当底层问题存在解集而非唯一解时，如何定义并计算“最小范数解”的广义导数，并确保其在数学上的严谨性。

### 2. 论文提出的方法论
论文通过结合 **Tikhonov 正则化** 和 **保守集值场（Conservative Mappings）** 理论，建立了一套处理病态优化问题微分的框架：
*   **核心思想：** 引入正则化项 $\frac{\epsilon}{2}\|x\|^2$，使底层问题变为强凸并拥有唯一解 $x^*_\epsilon(\theta)$。当 $\epsilon \to 0^+$ 时，$x^*_\epsilon$ 收敛于原始问题的最小范数解 $x^*_{min}$。
*   **一致值域条件（Uniform Range Condition）：** 这是论文的关键假设。它要求混合偏导数矩阵 $M_\epsilon$ 的列必须落在 Hessian 矩阵 $H_\epsilon$ 的值域内，且满足某种一致有界性。这放宽了传统的可逆性要求。
*   **伪逆公式（Pseudoinverse Formula）：** 证明了在满足一致值域条件及特定的特征值演化条件下，正则化解的导数极限可以用 Hessian 的 **Moore-Penrose 伪逆** $H^\dagger$ 来表示，即 $\text{Jac } x^*_{min} \approx -H^\dagger M$。
*   **扩展至非光滑复合问题：** 利用近端算子（Proximal Operator）和前向-后向残差方程，将上述理论从光滑凸函数扩展到了包含 $L_1$ 正则化（如 LASSO）的非光滑复合优化问题。

### 3. 实验设计
论文设计了三个主要实验场景来验证理论：
*   **数据投毒（最小二乘法）：** 使用 Riboflavin 数据集（71 样本，4088 特征）。由于特征数远超样本数，底层解不唯一。对比了最小范数解与通过零空间扰动产生的非最小范数解在投毒攻击效率上的差异。
*   **数据投毒（Huber 回归）：** 使用纽约市 311 服务请求数据。Huber 损失是非光滑且可能病态的。实验验证了 Tikhonov 路径下导数的收敛性。
*   **数据超清洗（LASSO）：** 使用 Scikit-learn 的 Digits 手写数字数据集。通过构造具有冗余特征的矩阵来制造多解情况，研究正则化参数 $\epsilon$ 与求解精度之间的权衡。
*   **Benchmark：** 主要对比对象是“最小范数解”与“非最小范数解”在双层优化目标（上层损失）上的表现。

### 4. 资源与算力
*   **硬件：** 实验在 Apple M1 CPU 上完成。
*   **软件：** 使用 Python 环境，核心库包括 NumPy、JAX（用于自动微分）、Scikit-learn 和 SciPy。
*   **精度：** 采用 float64（双精度）以确保数值稳定性。
*   **时长：** 文中未明确给出总训练时长，但考虑到数据集规模较小（百至千量级样本）且在 CPU 上运行，计算开销主要集中在矩阵伪逆和 SVD 分解上，而非大规模深度学习训练。

### 5. 实验数量与充分性
*   **实验规模：** 数据投毒实验针对每个数据集运行了 20 个不同的随机种子（数据切分），超清洗实验运行了 5 个种子。
*   **充分性：** 实验涵盖了光滑（LS）、鲁棒（Huber）和稀疏（LASSO）三种典型优化类型，具有较好的代表性。
*   **客观性：** 论文通过精确构造零空间扰动来产生“合法的底层解”，从而在相同底层目标值下对比不同解对上层梯度的影响，这种对比设计非常公平且具有说服力。

### 6. 主要结论与发现
*   **理论可行性：** 证明了最小范数解在“可定义性（Definability）”框架下是路径可微的，其保守雅可比矩阵可以通过正则化路径的极限获得。
*   **伪逆的合理性：** 验证了在实际操作中直接使用伪逆 $H^\dagger$ 计算梯度的做法在特定条件下（如特征值快速消失或存在谱间隙）是理论正确的。
*   **性能优势：** 实验表明，在双层优化中选择**最小范数解**通常比选择其他任意解能获得更好的上层优化效果（例如在投毒攻击中造成更大的破坏，或在清洗任务中获得更高的精度）。
*   **正则化权衡：** 发现底层问题的求解精度与 Tikhonov 正则化强度之间存在权衡，过小的 $\epsilon$ 可能导致数值不稳定，而过大的 $\epsilon$ 会引入偏差。

### 7. 优点（亮点）
*   **数学严谨性：** 填补了病态优化问题隐式微分的理论空白，使用了先进的 o-minimal 几何和保守场理论。
*   **统一框架：** 一个框架同时解决了光滑和非光滑（复合）问题的多解微分难题。
*   **实践指导：** 为工业界常用的“遇到奇异矩阵就用伪逆”的启发式做法提供了理论支撑和适用边界。

### 8. 不足与局限
*   **假设限制：** “一致值域条件”虽然比可逆性弱，但在某些约束优化问题（如文中提到的 $\ell_\infty$ 球约束）中仍可能失效，导致解不连续。
*   **计算成本：** 计算伪逆或进行 Tikhonov 路径追踪在高维参数空间下（如大型神经网络）的计算成本可能非常高。
*   **收敛速率：** 论文虽然证明了收敛性，但对于导数收敛的具体速率（Convergence Rate）缺乏通用的定量分析，这在 Remark 32 中也有所提及。

（完）
