---
title: "PromptResponse: Optimizing Prompts for LLM Coding Tasks"
title_zh: PromptResponse：针对大语言模型（LLM）编程任务的提示词优化
authors: "Erik Thureck, Robert Kühnen, Tim Jacobowitz"
date: 2026-08-21
pdf: "https://arxiv.org/pdf/2608.21074v1"
tags: ["query:bopl"]
score: 8.0
evidence: 通过基于大语言模型的微调来优化编程任务的提示词
tldr: 本研究探讨了提示词格式和LLM自动优化对代码生成任务的影响。通过在HumanEval数据集上对比基准、JSON、Markdown、YAML及LLM优化后的五种提示词变体，并利用GPT-4o进行大规模测试，发现JSON等结构化格式能显著提升生成效率和稳定性，而LLM自动优化的提示词反而导致性能下降。研究为开发者提供了实用的提示词优化建议并开源了相关数据集。
selection_source: fresh_fetch
motivation: 针对大语言模型在代码生成任务中对提示词变化高度敏感且输出不稳定的问题，探索不同格式化策略对模型表现的影响。
method: 基于HumanEval数据集构建了五种语义相同但格式不同的提示词变体，并使用GPT-4o进行了超过8200次代码生成实验。
result: 实验表明JSON格式在提升生成效率和语法稳定性方面表现最佳，而经过LLM自动优化的提示词却导致任务性能显著下降。
conclusion: 简单的提示词格式化重构即可带来可衡量的性能提升，而复杂的自动优化则需谨慎考虑模型对齐问题。
---

## 摘要
大语言模型（LLM）正越来越多地应用于研究工作流和软件开发流水线中，但其输出对输入提示词（prompt）的变化仍然十分敏感。本文介绍了 «PromptResponse»，这是一项受控研究，旨在探讨编程任务提示词的格式化和基于 LLM 的微调如何影响生成代码的性能、效率和稳定性。我们使用了 HumanEval 数据集的五个语义相同但语法不同的变体——基准（baseline）、JSON、Markdown、YAML 以及 LLM 微调版本——让 GPT-4o 在超过 8200 次执行中解决其中的编程问题。我们的结果表明，一致的格式化（尤其是 JSON）提高了生成效率和语法稳定性，并在任务性能方面有小幅提升。相反，经过 LLM 微调的提示词导致任务性能显著下降，且在其他任何维度上均无明显改善。这些发现表明，仅靠低成本的重新格式化就能产生可衡量的改进，而微调则必须考虑模型对齐（alignment）。最后，我们根据研究结果提供了一系列实用建议，并发布了数据集变体和评估流水线，以供未来研究使用。

## Abstract
Large language models (LLMs) are increasingly used in research workflows and software development pipelines, yet their output remains sensitive to input prompt variations. This paper presents $\unicode{x00AB}$PromptResponse$\unicode{x00BB}$, a controlled study examining how formatting and LLM-based tuning of coding task prompts affect the resulting code's performance, efficiency, and stability. Using five semantically identical yet syntactically distinct variants of the HumanEval dataset$\unicode{x2014}$baseline, JSON, Markdown, YAML, and an LLM-tuned version$\unicode{x2014}$we had GPT-4o solve its coding problems over 8200$\unicode{x00A0}$executions. Our results show that consistent formatting$\unicode{x2014}$especially JSON$\unicode{x2014}$improves generation efficiency and syntactic stability, with minor gains in task performance. Conversely, the LLM-tuned prompts resulted in significantly degraded task performance without significant improvements in any other dimension. These findings suggest that low-effort reformatting alone can yield measurable improvements, while tuning must account for model alignment. We conclude our work with providing a set of practical recommendations informed by our results as well as releasing our dataset variants and evaluation pipeline for future work.

---

## 论文详细总结（自动生成）

### 论文总结：PromptResponse - 针对 LLM 编程任务的提示词优化

#### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）已广泛应用于软件开发和科研工作流，但其输出对提示词（Prompt）的微小变化高度敏感，这不仅影响了代码生成的性能，还威胁到了研究的可重复性和科学有效性。
*   **核心问题**：论文探讨了提示词的**语法格式化**（如 JSON, Markdown, YAML）以及**基于 LLM 的自动微调**（LLM-tuning）如何具体影响代码生成的任务性能（正确率）、生成效率（耗时）和提示词稳定性（输出一致性）。

#### 2. 方法论
*   **核心思想**：通过保持语义一致、仅改变语法结构的方式，对比不同提示词策略的效果。
*   **关键技术细节**：
    *   **格式化重构**：将原始 HumanEval 任务解析并填充到预定义的结构化模板中。
        *   **JSON/YAML**：严格的键值对结构，包含函数签名、描述、示例、约束等字段。
        *   **Markdown**：使用标题、代码块和表格等视觉化结构。
    *   **LLM 自动微调（Tuned）**：利用 Mistral-7B-Instruct-v0.2 模型对原始任务的文档字符串（docstrings）进行重写，旨在提升描述的清晰度和逻辑性，同时标准化格式。
*   **评估指标**：
    *   **Pass Rate (pass@1)**：代码通过测试用例的比例。
    *   **Efficiency**：包括生成耗时（Gen Duration）和代码执行耗时（Eval Duration）。
    *   **Stability**：使用 ROUGE-L 分数衡量同一提示词在多次执行中输出代码的相似度。

#### 3. 实验设计
*   **数据集**：使用经典的 **HumanEval** 数据集（包含 164 个 Python 编程问题）。
*   **对比变体（Benchmark）**：
    1.  **Vanilla**：原始 HumanEval 提示词（基准）。
    2.  **JSON**：结构化 JSON 格式。
    3.  **Markdown**：人类可读的 Markdown 格式。
    4.  **YAML**：简洁的 YAML 格式。
    5.  **Tuned**：经 Mistral 模型优化后的提示词。
*   **目标模型**：OpenAI 的 **GPT-4o**（版本：gpt-4o-2024-08-06）。

#### 4. 资源与算力
*   **算力设备**：实验在本地机器上进行评估，配置为 AMD Ryzen 7 7800X3D CPU, 64GB DDR5 RAM, 以及 **NVIDIA RTX 4070 Ti (12GB)** GPU。
*   **实验时长**：总计 8200 次 API 请求及评估，耗时约 **205 分钟**。
*   **模型调用**：通过 OpenAI API 顺序调用，禁用了流式传输以获取完整响应。

#### 5. 实验数量与充分性
*   **实验规模**：每个数据集变体包含 164 个任务，每个任务重复执行 **10 次**，总计 **8200 次** 独立执行。
*   **充分性与公平性**：
    *   实验采用了“新对话窗口”模式，消除了上下文干扰。
    *   使用了非参数统计检验（Kruskal-Wallis 和 Mann-Whitney U 检验）来验证显著性，并报告了效应量（Effect Size），确保了结论的统计严谨性。
    *   样本量覆盖了 HumanEval 的全部题目，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **JSON 格式表现最优**：在生成效率和语法稳定性方面显著优于基准，且在任务通过率上有小幅（虽非显著）提升。
*   **自动微调适得其反**：使用 Mistral 优化的提示词在 GPT-4o 上的表现**显著下降**。这表明不同 LLM 之间的提示词优化存在“失配”风险，微调必须考虑模型间的对齐问题。
*   **结构化提升效率**：JSON、YAML 和 Markdown 格式虽然增加了提示词长度（Token 数），但由于结构清晰，LLM 处理和生成代码的速度反而比非结构化的 Vanilla 格式更快。
*   **稳定性增强**：一致的格式化（尤其是 JSON 和 YAML）显著提高了模型输出的确定性（ROUGE-L 分数更高）。

#### 7. 优点
*   **多维度视角**：不仅关注代码正确性，还深入探讨了工业界关心的生成速度和输出稳定性。
*   **低成本优化方案**：证明了简单的格式重构（Reformatting）这种低成本手段往往比复杂的 LLM 自动重写更有效。
*   **实用建议**：为开发者提供了明确的指导，即在构建自动化编程流水线时应优先考虑 JSON 格式。

#### 8. 不足与局限
*   **数据集局限性**：HumanEval 属于合成的、短小的函数级任务，无法完全代表具有复杂依赖关系的真实世界软件开发。
*   **模型单一性**：主要实验基于 GPT-4o，结论是否适用于其他闭源模型（如 Claude）或开源模型（如 Llama）尚待验证。
*   **微调策略风险**：Tuned 版本的失败可能仅代表 Mistral 与 GPT-4o 之间的不兼容，并不能全盘否定“以模型优化提示词”的潜力，未来需探索同模型自优化。

（完）
