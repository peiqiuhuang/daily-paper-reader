---
title: A Barrier-Metric First-Order Method for Linearly Constrained Bilevel Optimization
title_zh: 一种用于线性约束双层优化的障碍度量一阶方法
authors: "Tenglong Hong, Paul Grigas"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11476v1"
tags: ["query:bopl"]
score: 9.0
evidence: 线性约束双层优化的阶方法
tldr: 针对具有固定多面体约束的二层优化问题，本文提出了一种基于对数阻碍平滑的一阶方法。该方法通过引入阻碍函数将约束下层问题转化为可微近似，并利用Dikin几何分析解决了边界附近平滑度不均匀的问题。该算法仅需一阶梯度信息，避免了昂贵的二阶矩阵求逆，在确定性和随机环境下均实现了优异的收敛速率，为处理复杂约束的二层优化提供了高效方案。
selection_source: fresh_fetch
motivation: 传统的二层优化方法在处理带约束的下层问题时，常面临目标函数非光滑及二阶矩阵求逆计算开销巨大的挑战。
method: 采用对数阻碍函数平滑下层约束，并结合Dikin几何分析开发了一种仅依赖梯度的代理梯度算法。
result: 在确定性设置下实现了 O(K^-2/3) 的收敛率，在随机设置下达到 O(K^-2/5)，并实现了有效的偏差控制。
conclusion: 该研究通过阻碍度量方法有效解决了线性约束二层优化的计算效率与收敛性问题，具有重要的理论与应用价值。
---

## 摘要
我们研究了具有固定多面体下层可行集的双层优化问题。此类问题具有挑战性，原因有二：一是活跃集的变化可能导致上层目标函数非光滑；二是现有的超梯度方法通常需要下层 Hessian 矩阵求逆或等效的线性方程组求解，这在计算上非常昂贵。为了解决这些问题，我们对下层问题采用对数障碍平滑处理，以获得受约束双层目标的微分近似，并针对由此产生的障碍平滑代理模型开发了一种代理梯度算法。该算法仅使用上层和下层目标的梯度；其唯一的二阶对象是由固定多面体约束确定的显式对数障碍 Hessian 矩阵。障碍平滑恢复了可微性，但欧几里得光滑常数在边界附近并非一致有界。因此，我们开发了一种局部 Dikin 几何分析，其中障碍度量在移动的下层中心附近提供了一个无需查询（oracle-free）的曲率尺度。这产生了一种障碍感知的调度方案，使迭代过程保持在局部性质良好的区域内。对于障碍平滑目标，我们证明了在 $K$ 次外层迭代后，确定性设置下的驻点收敛率为 $\widetilde{O}(K^{-2/3})$，而在仅上层存在有界随机噪声的情况下为 $\widetilde{O}(K^{-2/5})$，并给出了随着障碍参数减小的定量偏差控制。

## Abstract
We study bilevel optimization with a fixed polyhedral lower feasible set. Such problems are challenging for two reasons: active-set changes can make the upper objective nonsmooth, and existing hypergradient methods typically require lower-Hessian inversions or equivalent linear solves, which are computationally expensive. To address these issues, we adopt a logarithmic barrier smoothing of the lower problem to obtain a differentiable approximation of the constrained bilevel objective, and develop a proxy-gradient algorithm for the resulting barrier-smoothed surrogate. The algorithm uses only gradients of the upper and lower objectives; its only second-order object is the explicit logarithmic barrier Hessian determined by the fixed polyhedral constraints. Barrier smoothing restores differentiability, but Euclidean smoothness constants are not uniformly bounded near the boundary. We therefore develop a local Dikin-geometry analysis in which the barrier-metric provides an oracle-free curvature scale near the moving lower centers. This leads to barrier-aware schedules that keep the iterates inside locally well-behaved regions. For the barrier-smoothed objective, we prove stationarity rates of $\widetilde{O}(K^{-2/3})$ in the deterministic setting and $\widetilde{O}(K^{-2/5})$ under upper-level-only bounded stochastic noise after $K$ outer iterations, together with quantitative bias control as the barrier parameter decreases.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于线性约束双层优化的障碍度量一阶方法

#### 1. 核心问题与整体含义（研究动机和背景）
双层优化（Bilevel Optimization）在元学习、超参数调优和经济决策等领域有广泛应用。然而，当下层问题包含线性约束（多面体可行集）时，面临两大挑战：
*   **非光滑性**：下层约束的活跃集（Active-set）变化会导致上层目标函数变得非光滑，传统的梯度下降法难以直接应用。
*   **计算高昂**：现有的超梯度（Hypergradient）方法通常需要计算下层 Hessian 矩阵的逆或求解线性方程组，在大规模问题中计算成本极高。
本文的研究动机是开发一种既能处理约束带来的非光滑性，又能避免高昂二阶计算的一阶优化算法。

#### 2. 论文提出的方法论
*   **核心思想**：利用**对数障碍函数（Logarithmic Barrier）**对下层约束进行平滑处理，将约束优化转化为内点形式，从而恢复上层目标的可微性。
*   **关键技术细节**：
    *   **障碍平滑（Barrier Smoothing）**：引入障碍参数 $\mu$，将下层目标 $g(x, y)$ 替换为 $\psi_\mu(x, y) = g(x, y) + \mu \phi(y)$，其中 $\phi(y)$ 是多面体约束的对数障碍函数。
    *   **代理梯度（Proxy-gradient）构造**：借鉴无约束双层优化的思路，构造一个不依赖隐式微分的代理梯度方向，仅需上层和下层目标的一阶导数。
    *   **Dikin 几何分析**：由于障碍函数在边界附近会导致欧几里得空间下的曲率爆炸，论文引入了基于障碍 Hessian 矩阵的 **Dikin 度量**。该度量作为预处理器，能够自适应地调整步长，确保迭代点保持在可行域内部且性质良好的局部区域内。
*   **算法流程（BMFO）**：在外层迭代中，算法维护两个下层追踪器（Tracker），分别追踪精确的障碍最小化点和代理最小化点。这两个追踪器均使用基于障碍度量的预处理一阶更新，随后利用这些追踪器的结果更新外层变量 $x$。

#### 3. 实验设计
论文在两个主要场景上验证了算法性能：
*   **拥堵收费设计（Congestion Toll Design）**：模拟交通网络中的收费决策，下层是受容量约束的流量分配问题。
    *   **Benchmark**：不同维度的合成多面体约束集（维度 $n$ 从 50 到 1200）。
    *   **对比方法**：精确超梯度障碍法（Exact-HG-Barrier）、基于 CVXPYLayer 的微分层方法、平滑隐式梯度法（DSIGD）以及一阶约束双层方法（F2CBA）。
*   **约束 MNIST 超清洗（Constrained MNIST Hypercleaning）**：在标签受损的图像分类任务中学习样本权重，下层参数受到框约束和补丁质量约束。
    *   **对比方法**：主要对比了最新的约束双层优化算法 F2CBA。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M4 Pro 芯片**和 **48 GB 统一内存**的 16 英寸 MacBook Pro 上运行。
*   **算力说明**：论文未提及使用大规模 GPU 集群，表明该算法在单机 CPU/轻量级芯片环境下即可高效运行，侧重于算法的计算效率提升而非堆砌算力。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在收费设计实验中，测试了从低维到高维（最高 1200 维）的多个梯度，并设置了不同的约束紧致度（$\tau=0.2, 0.3$）。
    *   在 MNIST 实验中，测试了不同的标签损坏率（$p=0.2, 0.4$），并展示了验证损失随迭代次数和梯度计算次数的变化曲线。
*   **充分性评价**：实验设计较为充分，涵盖了合成数据和真实图像数据集，对比了多种基准方法，并从计算时间、收敛精度和稳定性等多个维度进行了评估，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **收敛率保证**：在理论上证明了该方法在确定性环境下达到 $\widetilde{O}(K^{-2/3})$ 的驻点收敛率，在随机环境下达到 $\widetilde{O}(K^{-2/5})$。
*   **计算效率**：BMFO 显著降低了单次迭代的计算开销。在收费设计实验中，随着维度增加，BMFO 的运行时间远低于需要 Hessian 求逆的方法。
*   **边界稳定性**：Dikin 几何分析确保了算法在接近约束边界时依然稳定，避免了欧几里得梯度法常见的可行性失效问题。

#### 7. 优点
*   **纯一阶实现**：无需计算 $f$ 和 $g$ 的二阶导数，也无需对下层 Hessian 求逆，极大地提升了扩展性。
*   **理论完备**：通过 Dikin 几何解决了障碍平滑带来的局部曲率控制难题，提供了严谨的收敛性证明。
*   **显式预处理**：利用约束自带的几何信息（障碍 Hessian）进行预处理，无需额外的二阶信息估计。

#### 8. 不足与局限
*   **约束类型限制**：目前仅限于**固定**的**多面体（线性）**下层约束，无法直接处理依赖于外层变量 $x$ 的约束或非线性约束。
*   **强凸性假设**：理论分析依赖于下层目标函数 $g$ 关于 $y$ 是强凸的假设。
*   **近似偏差**：算法优化的是障碍平滑后的目标 $F_\mu$，虽然证明了偏差受 $\mu$ 控制，但在极高精度要求下可能需要极小的 $\mu$，这会增加数值不稳定性。

（完）
