---
title: Single-Loop Gradient Algorithms for Pessimistic Bilevel Optimization Problems
title_zh: 悲观双层优化问题的单循环梯度算法
authors: "Qichao Cao, Bo Zeng, Shangzhi Zeng, Jin Zhang"
date: 2026-09-10
pdf: "https://arxiv.org/pdf/2609.11183v1"
tags: ["query:bopl"]
score: 10.0
evidence: 悲观双层优化的单层梯度算法
tldr: 本文针对悲观双层优化（PBO）问题，提出了一种基于平滑近似的单循环梯度算法。通过重构、惩罚和正则化技术，该方法避免了二阶导数计算和内层子问题的求解，实现了确定性和随机PBO问题的高效求解。研究不仅提供了非渐近收敛性理论保证，还通过垃圾邮件分类和智能预测优化等实验，验证了PBO在提供鲁棒解方面的显著优势，填补了该领域高效一阶算法的空白。
selection_source: fresh_fetch
motivation: 现有双层优化研究多集中于乐观情形，缺乏针对悲观双层优化（PBO）的高效一阶单循环算法。
method: 通过平滑近似、重构及正则化技术，设计了仅需一阶梯度信息的确定性和随机单循环优化算法。
result: 理论上证明了算法的非渐近收敛性，实验表明其在效率和鲁棒性上均优于传统的乐观双层优化方法。
conclusion: 该研究为悲观双层优化提供了一套高效、实用的计算框架，展现了其在复杂决策场景中的应用潜力。
---

## 摘要
双层优化近年来备受关注，特别是在高效数值方法的开发方面。尽管乐观双层优化已取得实质性进展，但悲观双层优化（PBO）的研究仍相对较少，尤其是在完全一阶、单循环梯度算法的设计方面。为了填补这一空白，我们通过重构、惩罚和正则化方法提出了 PBO 的平滑近似，并建立了关于极小值点和稳定性的收敛性保证。在此框架基础上，我们分别为确定性和随机性 PBO 开发了两种单循环算法。这两种算法仅使用一阶梯度信息，避免了二阶导数计算和内层子问题的求解。我们为所提算法建立了非渐近收敛速率，以提供理论保障。通过对合成问题和实际问题实例的系统性实证研究，我们证明了所提算法具有极高的有效性和效率。特别是在垃圾邮件分类和“智能预测-优化”（Smart Predict-then-Optimize）任务上的结果，进一步展示了 PBO 相较于传统乐观双层优化的优势，凸显了其在实际建模和提供稳健解方面的巨大潜力。

## Abstract
Bilevel optimization has recently attracted growing attention, particularly in the development of efficient numerical methods. Despite substantial progress on optimistic bilevel optimization, pessimistic bilevel optimization (PBO) remains much less explored, especially the design of fully first-order, single-loop gradient-based methods. To address this gap, we propose a smooth approximation of PBO through reformulation, penalization and regularization, and establish convergence guarantees in terms of both minimizers and stationarity. Building on this framework, we then develop two single-loop algorithms for deterministic and stochastic PBOs, respectively. Both use only first-order gradient information and avoid second-order derivatives and inner-loop subproblem solves. Non-asymptotic convergence rates for the proposed algorithms are established to provide theoretical guarantees. Through a systematic empirical study of both synthetic and practical problem instances, we demonstrate that our algorithms are highly effective and efficient. In particular, our results on spam classification and Smart Predict-then-Optimize further illustrate PBO's advantages over its classical optimistic bilevel counterpart, highlighting its strong potential for practical modeling and the delivery of robust solutions.

---

## 论文详细总结（自动生成）

这是一份关于论文《悲观双层优化问题的单循环梯度算法》（*Single-Loop Gradient Algorithms for Pessimistic Bilevel Optimization Problems*）的结构化总结：

### 1. 核心问题与研究背景
*   **研究背景**：双层优化（Bilevel Optimization）广泛应用于机器学习和决策科学。现有研究多集中于“乐观双层优化”（OBO），即假设下层决策者选择对上层最有利的解。
*   **核心问题**：在许多实际场景（如对抗性攻击、鲁棒决策）中，下层决策者可能采取对上层最不利的策略，即“悲观双层优化”（PBO）。PBO 具有三层结构，求解难度极大。
*   **研究动机**：现有 PBO 求解方法多依赖于二阶导数或嵌套循环，计算开销巨大且难以扩展。本文旨在设计一种高效的、仅需一阶信息的单循环算法，以解决大规模及随机环境下的 PBO 问题。

### 2. 方法论：核心思想与关键技术
*   **平滑近似（Smooth Approximation）**：通过引入惩罚项（处理下层约束）和正则化项（处理鞍点问题），将非光滑的悲观值函数转化为可微的平滑函数。
*   **单循环框架**：
    *   **SiPBA（确定性）**：通过一次梯度上升-下降更新来跟踪鞍点，并利用该估计值构造上层变量的近似梯度，避免了内层子问题的精确求解。
    *   **Sto-SiPBA（随机性）**：引入 STORM 型递归方差缩减估计器，处理随机预言机（Stochastic Oracle）环境下的梯度噪声。
*   **理论保证**：证明了平滑近似的收敛性（极小值点收敛）以及算法生成的序列收敛于原 PBO 问题的 C-稳定点（C-stationary point）。

### 3. 实验设计
*   **场景与数据集**：
    *   **合成问题**：确定性和随机性测试函数，用于验证收敛速率。
    *   **垃圾邮件分类**：跨语料库（TREC06, TREC07, EnronSpam, LingSpam）的鲁棒性测试。
    *   **智能预测-优化（SPO）**：基于 Bike Sharing 数据集的库存规划问题。
*   **对比方法**：与 AdaProx-PD、AdaProx-SG（梯度法）以及 Scholtes-C、Scholtes-D（MPCC 惩罚法）进行了对比。

### 4. 资源与算力
*   **硬件配置**：使用了配备双 Intel Xeon Gold 5218R CPU（共 40 核/80 线程）和 NVIDIA H100 GPU 的服务器。
*   **训练时长**：文中未详细列出每组实验的具体训练时长，但通过表格对比了平均运行时间（如在 $n=1000$ 的合成实验中，SiPBA 仅需约 1.9 秒，而 Scholtes 方法耗时超过 400 秒）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础合成问题到复杂实际应用（垃圾邮件、库存规划）的多个维度。
*   **充分性**：进行了参数敏感性分析（如表 3）、不同噪声水平下的收敛性测试以及与多种基准方法的对比。实验设计客观，通过多次随机种子取平均值，保证了结果的公平性。

### 6. 主要结论与发现
*   **效率优势**：所提算法在处理高维问题时，计算效率远高于现有的 MPCC 惩罚法。
*   **鲁棒性**：在垃圾邮件分类中，PBO 训练的分类器在跨语料库测试中表现出更强的泛化能力。
*   **决策质量**：在 SPO 任务中，PBO 能够有效处理下层解集的不确定性，在最坏情况下的任务损失（Pessimistic Task Loss）表现最优。

### 7. 优点
*   **算法创新**：首次实现了 PBO 的完全一阶、单循环算法，显著降低了计算复杂度。
*   **理论严谨**：提供了完整的非渐近收敛性分析，并给出了 C-稳定性的理论支撑。
*   **实用性强**：不仅适用于确定性问题，还通过方差缩减技术完美适配随机优化场景。

### 8. 不足与局限
*   **假设条件**：算法收敛依赖于一些较强的假设（如 LICQ、MFCQ 以及下层解集的参数化误差界），在某些复杂非凸场景下可能难以验证。
*   **参数敏感性**：虽然实验表明算法对部分参数鲁棒，但学习率（$\alpha, \beta$）和惩罚系数（$\rho$）的调度仍需精细调节。
*   **应用范围**：目前主要针对连续优化问题，对于离散组合优化场景的直接适用性有待进一步探索。

（完）
