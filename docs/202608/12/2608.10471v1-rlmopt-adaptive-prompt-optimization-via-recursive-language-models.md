---
title: "RLMOpt: Adaptive Prompt Optimization via Recursive Language Models"
title_zh: RLMOpt：通过递归语言模型实现的自适应提示词优化
authors: "Subhash Bangalore Satheesha, Nirvik Pande, Deepthi Duddempudi, Bharath Dandala"
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.10471v1"
tags: ["query:bopl"]
score: 10.0
evidence: 通过递归语言模型实现自动提示优化
tldr: RLMOpt 是一种新型提示词优化器，通过递归语言模型（RLM）实现自适应搜索策略。与传统预定义算法不同，RLMOpt 让模型自主决定候选生成、失败分析及预算分配，并结合确定性框架确保评分客观。在临床提取、多跳问答等四个基准测试中，RLMOpt 在性能、可靠性和效率（提示词更短、搜索次数更少）上均优于 GEPA 等现有方法，展现了更强的泛化能力。
selection_source: fresh_fetch
motivation: 现有的提示词优化方法依赖固定的搜索程序，限制了模型在复杂任务中根据反馈动态调整优化策略的能力。
method: 引入递归语言模型作为智能体，在工具化环境中自主执行任务分析、候选生成和预算管理，并配合确定性评分与选择机制。
result: "RLMOpt 在所有基准测试中均取得最佳得分，生成的提示词长度仅为 GEPA 的 27-79%，且在多次运行中表现出极高的稳定性。"
conclusion: 优化收益主要取决于初始提示词的提升空间而非搜索预算，高效优化的核心在于以最小搜索成本可靠地达到性能上限。
---

## 摘要
提示词优化器能够自动搜索可提升语言模型性能的提示词，但现有方法依赖于预定义的优化程序：算法决定探索哪些候选方案以及搜索如何进行，而语言模型仅负责生成或改进提示词建议。我们介绍了 RLMOpt，这是一种通过递归语言模型 (RLM) 使搜索策略本身由语言模型驱动的提示词优化器。RLM 智能体在基于工具的环境中运行，负责检查任务信息、分析失败案例、生成候选方案、分配评估预算并决定何时停止。一个确定性的框架作为智能体的补充，负责执行客观评分、基于帕累托的选择以及回归约束。我们在四个基准测试中评估了 RLMOpt，涵盖结构化临床信息提取 (Chia)、多跳问答 (HotpotQA)、可验证指令遵循 (IFBench-2025) 以及多轮工具调用智能体 (BFCL)。在单种子的匹配对比中，RLMOpt 在所有四个基准测试上均获得了最佳的留出集得分，并在四项任务的平均值上领先（0.610 对比 GEPA 的 0.589）。在不同种子上重复每个基准测试，共产生 11 组匹配的基准-种子对比，其中 RLMOpt 在 9 种情况下优于 GEPA。在所有 11 次运行中，它从未生成过性能低于初始种子的提示词，而 GEPA 有两次低于其起点。它也更加高效，在实现这些结果的同时使用了更少的搜索展开，且生成的提示词长度仅为 GEPA 生成的 27-79%。我们的结果进一步表明，优化收益主要取决于初始提示词中可用的提升空间，而非搜索预算。因此，高效的优化取决于能否以最少的搜索可靠地达到可用的提升空间。

## Abstract
Prompt optimizers automate the search for prompts that improve language-model performance, but existing methods rely on a predefined optimization procedure: the algorithm determines which candidates to explore and how the search progresses, while the language model generates or refines prompt proposals. We introduce RLMOpt, a prompt optimizer that makes the search policy itself language-model-driven through a recursive language model (RLM). The RLM agent operates over a tool-based environment, inspecting task information, analyzing failures, generating candidates, allocating evaluation budget, and deciding when to stop. A deterministic harness complements the agent by enforcing objective scoring, Pareto-based selection, and regression constraints.   We evaluate RLMOpt across four benchmarks spanning structured clinical information extraction (Chia), multi-hop question answering (HotpotQA), verifiable instruction following (IFBench-2025), and multi-turn tool-calling agents (BFCL). In a matched comparison at a single seed, RLMOpt obtains the best held-out score on all four benchmarks and leads the four-task mean (0.610 against 0.589 for GEPA). Repeating each benchmark across seeds yields 11 matched benchmark-seed comparisons, in which RLMOpt outperforms GEPA in 9 cases. Across all 11 runs, it never produced a prompt that underperformed its seed, whereas GEPA fell below its starting point twice. It is also more efficient, achieving these results with fewer search rollouts while producing prompts that are 27-79% the size of those produced by GEPA.   Our results further show that optimization gains are determined primarily by the headroom available in the seed prompt, rather than by the search budget. Efficient optimization therefore depends on reaching the available headroom reliably and with minimal search

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RLMOpt** 的自适应提示词（Prompt）优化框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的提示词优化器（如 GEPA, OPRO, MIPROv2）通常依赖**预定义的固定搜索算法**。虽然这些方法利用大语言模型（LLM）来生成或修改提示词，但“如何搜索”、“何时停止”、“关注哪些失败案例”等高层策略是由代码硬编码的，缺乏灵活性。
*   **研究动机**：作者提出将搜索策略本身交由 LLM 驱动。通过“递归语言模型（RLM）”，让优化器像一个智能体（Agent）一样，根据任务反馈自主决定探索路径，从而提高优化的效率和最终提示词的质量。

### 2. 方法论：核心思想与关键技术
RLMOpt 的核心架构分为两个部分：**RLM 智能体**和**确定性框架（Harness）**。
*   **RLM 智能体（搜索控制层）**：
    *   作为优化策略的“大脑”，在工具化环境中运行。
    *   通过编写代码（REPL）调用工具，执行任务检查、失败分析、候选生成和预算分配。
    *   **工具集**：包括内省工具（查看示例）、失败分析工具（总结错误模式）、合成工具（生成新候选）和评估工具。
*   **确定性框架（执行与约束层）**：
    *   负责所有不应由 LLM 随机决定的逻辑：执行任务模型、计算客观评分、维护候选历史。
    *   **帕累托选择（Pareto Selection）**：在多字段任务中，确保选出的候选提示词在各维度上表现均衡。
    *   **回归约束（Regression Constraints）**：强制执行“无回归”原则，即新提示词在任何单一字段上的得分下降不能超过阈值（如 5%），防止为了整体分提高而牺牲特定功能。
    *   **显著性门控**：只有当改进在统计上显著时才接受更新。

### 3. 实验设计
*   **数据集/基准测试**：
    1.  **Chia**：临床试验入组标准的结构化信息提取（多字段提取）。
    2.  **HotpotQA**：多跳问答（复杂推理）。
    3.  **IFBench-2025**：可验证的指令遵循（严格约束）。
    4.  **BFCL (Berkeley Function-Calling Leaderboard)**：多轮工具调用智能体任务。
*   **对比方法**：主要对比了 **GEPA**（目前最先进的基于进化的提示词优化器）以及初始的**种子提示词（Seed Prompt）**。
*   **评估指标**：留出集（Held-out）测试得分、搜索效率（Rollouts 数量）、提示词长度。

### 4. 资源与算力
*   **模型使用**：
    *   **优化器模型**：使用 **GPT-5.1**（作为 RLM 智能体进行推理和决策）。
    *   **任务模型**：使用 **GPT-4.1**（针对 Chia 任务）和 **GPT-4o-mini**（针对其他任务）。
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长，因为实验是基于闭源模型的 API 调用完成的。作者通过“下游展开次数（Rollouts）”和“Token 消耗量”来衡量计算成本。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同类型的基准测试上进行了实验。
*   **充分性**：为了排除随机性干扰，作者在不同种子上重复了实验，共进行了 **11 组匹配的基准-种子对比**。
*   **客观性**：所有方法使用相同的初始种子、相同的数据拆分和相同的任务模型，确保了对比的公平性。实验涵盖了从单轮提取到多轮 Agent 的多种场景，验证了泛化性。

### 6. 主要结论与发现
*   **性能领先**：RLMOpt 在所有四个基准测试中均取得了最高的留出集得分，平均得分优于 GEPA。
*   **极高的稳定性**：在 11 次运行中，RLMOpt **从未**产生过差于初始种子的提示词，而 GEPA 有 2 次出现了性能倒退。
*   **高效率与简洁性**：RLMOpt 使用更少的搜索次数达到了更好的效果，且生成的提示词长度仅为 GEPA 的 **27%–79%**，更加精简。
*   **“天花板”理论**：研究发现，优化的收益主要取决于初始提示词距离模型能力“天花板”的剩余空间（Headroom），而非搜索预算。如果模型本身能力已达上限，增加搜索预算也无济于事。

### 7. 优点与亮点
*   **自适应性**：搜索策略不再是死板的算法，而是能根据错误反馈进行“外科手术式”改进的智能体。
*   **可靠的约束机制**：通过确定性框架强制执行“无回归”和“显著性检查”，解决了 LLM 优化过程中常见的“拆东墙补西墙”问题。
*   **多组件支持**：不仅能优化单一提示词，还能同时优化 Agent 的系统提示词、工具描述和示例（Demonstrations）。

### 8. 不足与局限
*   **系统归因困难**：由于 RLMOpt 引入了多个组件（自适应策略、回归约束、多组件优化等），目前尚不完全清楚各组件对最终性能提升的具体贡献比例。
*   **停止策略尚需完善**：目前的自动停止条件（如 85% 分数目标）在某些极难任务上可能无法触发，主要还是依赖预算上限。
*   **小样本风险**：在验证集非常小（如少于 10 个样本）的情况下，依然存在过拟合验证集的风险。
*   **成本**：虽然下游任务调用减少了，但使用 GPT-5.1 作为优化器本身的推理成本可能较高。

（完）
