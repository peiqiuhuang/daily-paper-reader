---
title: Vision-Guided Text Prompt Tuning for Multimodal Sentiment Analysis
title_zh: 面向多模态情感分析的视觉引导文本提示微调
authors: "Xiaoran Kou, Jingyi Wu, Peng Sun, Yang Liu, Hong Chen"
date: 2026-09-06
pdf: "https://arxiv.org/pdf/2609.06497v1"
tags: ["query:bopl"]
score: 9.0
evidence: 参数高效的视觉引导文本提示微调
tldr: 针对多模态情感分析中视觉噪声干扰及全参数微调成本高的问题，本文提出视觉引导文本提示微调（VG-TPT）方法。该方法通过层级自适应提示将视觉情感线索注入冻结的BERT编码器，利用协同引导路由器实现样本与层级特定的动态调制，在保持极低参数更新量（2.4M）的同时，有效提升了情感分析的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 旨在解决多模态情感分析中视觉噪声干扰及全参数微调易过拟合且成本高昂的挑战。
method: 提出VG-TPT框架，通过协同引导路由器从提示库中动态选择视觉引导信息，并将其注入冻结的BERT编码器中进行层级化调制。
result: 在CMU-MOSEI和CMU-MOSI数据集上表现优异，仅需更新2.4M参数即可达到甚至超越全模态微调方法的性能。
conclusion: VG-TPT证明了通过视觉引导的提示微调，可以在参数高效的前提下实现多模态情感信息的精准融合与校准。
---

## 摘要
多模态情感分析需要对语言语义和非语言情感线索进行有效建模。一个核心挑战在于如何以可控、自适应且参数高效的方式，利用视觉面部证据来校准以文本为中心的情感理解。文本通常作为语义锚点，而视觉线索则为模糊或隐晦的表达提供补充证据；然而，不加区分的融合可能会引入视觉噪声并扭曲文本语义。此外，对大型视觉和文本编码器进行全参数微调成本高昂，且容易在有限且依赖特定场景的多模态情感分析（MSA）基准测试中产生过拟合。为了解决这些问题，我们提出了视觉引导文本提示微调（VG-TPT），将视觉-文本情感建模表述为对冻结文本表示的可控视觉校准。VG-TPT 通过层级自适应提示将视觉情感线索注入冻结的 BERT 编码器，而非依赖后期特征融合或全主干微调。协同引导路由器根据演进的文本状态和视觉引导特征，从可训练的提示库中组合提示，从而实现样本特定和层特定的调制。在 CMU-MOSEI 和 CMU-MOSI 上的实验表明，VG-TPT 在仅更新 240 万个可训练参数的情况下，持续优于纯文本基线，并取得了与多种全模态方法相当或更优的性能。代码已开源至 https://github.com/ma-tubu/VG-TPT。

## Abstract
Multimodal sentiment analysis requires effective modeling of both verbal semantics and non-verbal affective cues. A central challenge is to calibrate text-centered sentiment understanding with visual facial evidence in a controlled, adaptive, and parameter-efficient manner. Text usually serves as the semantic anchor, whereas visual cues provide complementary evidence for ambiguous or implicit expressions; however, indiscriminate fusion may introduce visual noise and distort textual semantics. Moreover, fully fine-tuning large visual and textual encoders is costly and prone to overfitting on limited and scenario-dependent MSA benchmarks. To address these issues, we propose Vision-Guided Text Prompt Tuning (VG-TPT), which formulates visual-text sentiment modeling as controllable visual calibration of frozen text representations. VG-TPT injects visual affective cues into a frozen BERT encoder through layer-wise adaptive prompts, rather than relying on late-stage feature fusion or full backbone tuning. A co-guided router composes prompts from a trainable prompt bank according to both the evolving text state and the visual guidance feature, enabling sample-specific and layer-specific modulation. Experiments on CMU-MOSEI and CMU-MOSI show that VG-TPT consistently improves over text-only baselines and achieves competitive or superior performance compared with several full-modality methods, while updating only 2.4M trainable parameters. The code is available at https://github.com/ma-tubu/VG-TPT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Vision-Guided Text Prompt Tuning for Multimodal Sentiment Analysis》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在多模态情感分析（MSA）中，如何以参数高效且可控的方式，利用视觉面部线索来校准以文本为核心的情感理解，同时避免视觉噪声干扰和全参数微调带来的过拟合风险。
*   **研究背景**：文本通常是情感表达的“语义锚点”，但往往存在模糊性；视觉线索（如面部表情）虽能提供补充证据，但直接融合容易引入噪声。现有的后期融合或全模态微调方法在计算成本和模型泛化能力上存在局限。

### 2. 方法论：VG-TPT 框架
*   **核心思想**：将视觉-文本情感建模转化为“对冻结文本表示的可控视觉校准”。
*   **关键技术细节**：
    *   **层级化视觉注入**：不进行后期特征拼接，而是通过层级自适应提示（Prompt）将视觉情感线索直接注入 BERT 编码器的内部层。
    *   **协同引导路由器（Co-guided Router）**：根据当前文本状态和视觉引导特征，动态计算路由权重，从可训练的提示库中组合出样本特定（Sample-specific）和层级特定（Layer-specific）的提示。
    *   **提示平衡机制**：引入平衡损失函数（Balance Loss），防止路由器过度依赖少数提示基，确保提示库的充分训练。
*   **算法流程**：提取视觉特征 -> 结合文本状态通过路由器计算权重 -> 从提示库组合提示 -> 注入 BERT 层进行交互 -> 最终分类/回归预测。

### 3. 实验设计
*   **数据集**：CMU-MOSI 和 CMU-MOSEI（多模态情感分析领域的标准基准）。
*   **对比方法**：
    *   纯文本基线（BERT, XLNet）。
    *   视觉单模态基线（V-Transformer）。
    *   全模态方法（EF-LSTM, MulT, MMIM）。
*   **评估指标**：MAE（平均绝对误差）、Acc-7（七分类准确率）、Acc-2（二分类准确率）及 F1 分数。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **参数效率**：该方法仅需更新约 2.4M 个参数（约占总参数量的 1.2%），显著低于全参数微调的 109M 参数量。

### 5. 实验数量与充分性
*   **实验覆盖**：包含主实验（与多种基线对比）、消融实验（验证提示生成、层级更新、路由机制的有效性）以及参数效率对比。
*   **客观性与公平性**：实验设计较为客观，通过对比不同变体（如移除自适应提示、移除文本状态等）验证了各模块的必要性。但由于对比方法中部分使用了音频模态，而 VG-TPT 仅使用视觉+文本，在跨模态对比上存在一定的非对称性。

### 6. 主要结论与发现
*   **性能表现**：VG-TPT 在仅更新极少量参数的情况下，性能优于纯文本基线，并能与使用更多模态（含音频）的全模态方法竞争。
*   **有效性**：证明了视觉线索对文本情感的校准作用，且层级化的提示注入比简单的后期融合更能捕捉细粒度的情感变化。

### 7. 优点
*   **参数高效**：极大地降低了微调成本，适合资源受限的部署环境。
*   **动态调制**：协同路由机制实现了样本和层级间的自适应，增强了模型对复杂情感表达的建模能力。
*   **可解释性**：通过提示库的激活分布，可以一定程度上观察模型如何利用视觉线索。

### 8. 不足与局限
*   **模态缺失**：目前仅关注视觉-文本，未涵盖音频模态，在某些依赖语调的情感场景下可能存在局限。
*   **视觉依赖**：依赖于单帧视觉特征提取，对于动态变化剧烈或长视频序列的情感表达，可能缺乏时间维度的深度建模。
*   **基准局限**：实验仅在 CMU 系列数据集上进行，未在更广泛的工业级或跨领域数据集上验证其泛化性。

（完）
