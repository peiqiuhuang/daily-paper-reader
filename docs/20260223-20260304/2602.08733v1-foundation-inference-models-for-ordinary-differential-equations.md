---
title: Foundation Inference Models for Ordinary Differential Equations
title_zh: 常微分方程的基础推理模型
authors: "Maximilian Mauel, Johannes R. Hübers, David Berghaus, Patrick Seifner, Ramses J. Sanchez"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08733v1"
tags: ["query:sr"]
score: 9.0
evidence: 从噪声轨迹中推断常微分方程向量场的基础模型
tldr: 针对从噪声轨迹中推断常微分方程（ODE）向量场的难题，本文提出了FIM-ODE，一种预训练的基础推理模型。该模型通过在低阶多项式向量场分布上进行预训练，利用神经算子实现了仅需单次前向传播即可从噪声数据中直接预测向量场。FIM-ODE在零样本任务中表现优异，且通过微调能快速稳定地适应复杂系统，性能超越了现有的符号回归、高斯过程及神经ODE等基准方法。
selection_source: fresh_fetch
motivation: 传统的ODE推断方法如符号回归和神经ODE往往需要复杂的训练流程或特定的先验知识，难以在缺乏专家经验的情况下高效应用。
method: 提出FIM-ODE模型，通过在低阶多项式向量场先验分布上预训练神经算子，实现从噪声轨迹到向量场的直接映射。
result: 实验表明FIM-ODE在零样本场景下优于ODEFormer，且在微调后能以更快的速度和更高的稳定性超越现代神经和高斯过程基准。
conclusion: FIM-ODE证明了预训练基础模型在ODE推断中的有效性，为科学建模提供了一种高效、通用的自动化工具。
---

## 摘要
常微分方程（ODEs）是科学建模的核心，但从噪声轨迹中推断其向量场仍然具有挑战性。目前的方法，如符号回归、高斯过程（GP）回归和神经常微分方程（Neural ODEs），通常需要复杂的训练流程和大量的机器学习专业知识，或者强烈依赖于特定系统的先验知识。我们提出了 FIM-ODE，这是一种预训练的基础推理模型（Foundation Inference Model），它通过在单次前向传递中直接从噪声轨迹数据预测向量场，从而摊销了低维 ODE 的推理成本。我们在具有低阶多项式向量场的 ODE 先验分布上预训练 FIM-ODE，并使用神经算子（neural operators）表示目标场。尽管使用了更简单的预训练先验分布，FIM-ODE 在各种方案下仍实现了强大的零样本性能，达到并通常优于最近的预训练符号基准模型 ODEFormer。预训练还为微调提供了强大的初始化，实现了快速且稳定的适配，在不需要机器学习专业知识的情况下，其表现优于现代神经和 GP 基准模型。

## Abstract
Ordinary differential equations (ODEs) are central to scientific modelling, but inferring their vector fields from noisy trajectories remains challenging. Current approaches such as symbolic regression, Gaussian process (GP) regression, and Neural ODEs often require complex training pipelines and substantial machine learning expertise, or they depend strongly on system-specific prior knowledge. We propose FIM-ODE, a pretrained Foundation Inference Model that amortises low-dimensional ODE inference by predicting the vector field directly from noisy trajectory data in a single forward pass. We pretrain FIM-ODE on a prior distribution over ODEs with low-degree polynomial vector fields and represent the target field with neural operators. FIM-ODE achieves strong zero-shot performance, matching and often improving upon ODEFormer, a recent pretrained symbolic baseline, across a range of regimes despite using a simpler pretraining prior distribution. Pretraining also provides a strong initialisation for finetuning, enabling fast and stable adaptation that outperforms modern neural and GP baselines without requiring machine learning expertise.

---

## 论文详细总结（自动生成）

以下是对论文《Foundation Inference Models for Ordinary Differential Equations》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在科学建模中，从观测到的噪声轨迹中推断常微分方程（ODE）的向量场（即系统识别）是一个核心难题。传统方法（如符号回归、高斯过程回归、Neural ODEs）通常需要针对每个数据集进行复杂的迭代优化，且对专家经验和系统先验知识依赖性强。
*   **核心目标**：提出一种名为 **FIM-ODE** 的基础推理模型，通过在大规模合成数据上进行预训练，将推理成本“摊销”。该模型能够以单次前向传播（Zero-shot）的方式，直接从稀疏、有噪声的轨迹中推断出底层的向量场。

### 2. 方法论
*   **核心思想**：利用“模拟推理”（Simulation-based Inference）的思路，在简单的多项式向量场先验上预训练一个神经算子，使其学习如何从轨迹转移特征映射到局部向量场。
*   **关键技术细节**：
    *   **预训练先验**：生成1-3维、最高3阶的稀疏多项式向量场。虽然形式简单，但能产生固定点、极限环和混沌吸引子等复杂行为。
    *   **输入表示**：不直接输入原始轨迹，而是提取“转移特征”，包括当前状态 $y_i$、位移 $\Delta y_i$、位移平方 $\Delta y_i^2$ 和时间间隔 $\Delta \tau_i$。
    *   **架构设计**：采用编码器-解码器结构。
        *   **编码器**：使用线性自注意力机制处理不规则采样的多轨迹数据，生成置换不变的上下文表示。
        *   **解码器**：采用函数式注意力（Functional Attention），根据查询位置 $x$ 从上下文表示中提取信息，输出该位置的向量场估计 $\hat{f}(x)$。
    *   **归一化**：引入可逆实例归一化（Spatial）和时间尺度中心化（Temporal），以应对不同系统间巨大的尺度差异。
    *   **损失函数**：使用学习到的不确定性加权 MAE 损失（基于拉普拉斯似然），自动平衡不同流速区域的贡献。

### 3. 实验设计
*   **数据集/场景**：
    *   **ODEBench**：包含61个经典ODE系统（如范德波尔振子、洛伦兹吸引子），涉及非多项式（三角、指数、有理）函数。
    *   **低数据量OOD评估**：针对 Van der Pol 和 FitzHugh-Nagumo 系统，测试在极短/稀疏轨迹下的预测能力。
    *   **真实世界数据**：CMU Motion Capture（人类运动捕捉），将高维动作投影到低维潜空间进行动力学推断。
*   **对比基准（Benchmarks）**：
    *   **符号回归类**：ODEFormer（目前最强的预训练符号基准）。
    *   **经典推理类**：NeuralODE、GPODE、npODE、ODE2VAE、LatentSDE、GP-DNF（基于高斯过程或神经变分推理的方法）。

### 4. 资源与算力
*   **算力设备**：使用了 **4张 NVIDIA A40 GPU**（每张48GB显存）。
*   **训练时长**：预训练过程持续了约 **5天**。
*   **模型规模**：总参数量约 **1300万（13M）**，其中8M用于向量场预测，5M用于不确定性估计。
*   **数据规模**：预训练使用了 **60万个** 合成多项式ODE系统。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    *   在 ODEBench 上进行了不同噪声水平（$\sigma=0$ 到 $0.05$）和采样率（$\rho=0$ 到 $0.5$）的组合测试。
    *   针对噪声敏感性，在振子实验中进行了 **100次独立试验** 并统计了均值和标准差。
    *   进行了关于数据集规模（10k到200k系统）和离散化步长的 **消融实验**。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了零样本性能，还展示了微调（Finetuning）后的提升，并诚实地讨论了在非多项式系统上的局限性。

### 6. 主要结论与发现
*   **局部表示的优势**：相比于 ODEFormer 追求全局符号表达式，FIM-ODE 的局部神经算子表示在数据覆盖区域更准确，且对分布外（OOD）的函数形式具有更好的鲁棒性。
*   **预训练的价值**：即使预训练先验仅限于低阶多项式，模型也能泛化到包含三角函数和有理函数的复杂系统。
*   **微调的稳定性**：预训练权重为 Neural ODE 训练提供了极佳的初始化，使得微调过程比从头训练更快速、更稳定，且在低数据场景下达到 SOTA 性能。

### 7. 优点
*   **高效推理**：推理过程是摊销的，单次前向传播即可完成，无需复杂的优化循环。
*   **处理不规则性**：天然支持多轨迹、不规则时间采样和高噪声数据。
*   **易用性**：不需要用户具备深厚的机器学习背景来调整求解器或先验参数。

### 8. 不足与局限
*   **维度限制**：目前仅支持 **1-3维** 系统。这主要是因为高维多项式系统极易出现数值爆炸，导致难以生成稳定的预训练数据。
*   **先验偏差**：多项式先验是非平稳的，随着远离原点，向量场幅值会迅速增长，这可能导致模型在远离观测数据的区域预测失效。
*   **架构固定**：模型的状态维度在设计时是硬编码的（需预设最大维度），缺乏处理任意维度输入的灵活性。

（完）
